# Requirements: HANGAR

**Defined:** 2026-09-02
**Core Value:** Plug in a ZONA, open a URL, and half a minute later the pad is doing something spectacular. If everything else fails, browser-to-hardware install must work.

## v1 Requirements

Requirements for initial release. Each maps to roadmap phases. IDs in brackets refer to the research
feature table in `.planning/research/FEATURES.md`.

### Foundation

- [x] **FOUND-01**: A bare browser page (no framework, no Editor) connects to a ZONA over Web Serial, fetches the touch element's existing Setup and Timer configs, writes the identical strings back, and stores — every step acknowledged by the module, the whole run a provable no-op on the hardware (the walking skeleton)
- [x] **FOUND-02**: The `_pad.ts` compiler, `pad-sim.ts` simulator and `pad-sim-host.ts` render loop are vendored into HANGAR, quarantined under a single vendor directory with a written sync procedure back to BOTOR, and their existing test suites pass with the **22 enumerated divergences** of `src/lib/fidelity/upstream-manifest.json` applied — across `_pad.ts` (10), `pad-sim.ts` (6), `tests/pad.test.js` (1) and `tests/pad-sim.test.js` (5); `pad-sim-host.ts` and `tests/pad-invariants.test.js` carry none — the three suites' counts themselves unchanged at 176, 96 and 9 _(amended 2026-09-10 by plan 11-16, as plan 11-03 recorded in advance: the wording "passing unchanged" was falsified by D-02 in plan 11-04, which edited one expectation in `tests/pad.test.js` and five in `tests/pad-sim.test.js` so the vendored suites agree with the comet decay landing on phase 0; `vendored-diff.spec.ts` reconstructs upstream's bytes through the manifest and still hashes them, so the divergence is a record rather than an absence)_
- [x] **FOUND-03**: `@intechstudio/grid-protocol` is pinned to the exact version BOTOR's cost baseline was measured against, and any bump is a test-gated change
- [x] **FOUND-04**: The repository carries a GPLv3 licence, a notices file for third-party licences (grid-protocol is itself GPLv3), and the deployed site serves a source archive of the deployed commit (GPLv3 section 6(d) — the repository itself stays private, per D-02)
- [x] **FOUND-05**: Compile, cost and fit are gated on the Lua formatter WASM being initialised, so no card can ever appear syntactically invalid or fail to measure because the formatter has not loaded yet

### Connect [A]

- [x] **CONN-01**: User sees one primary `CONNECT` control, enabled only when `"serial" in navigator && isSecureContext` — never gated by user-agent sniffing [A1]
- [x] **CONN-02**: User on an unsupported browser and user on an insecure context see two different messages, each naming the fix; the unsupported message names browsers that do work (Chrome, Edge, desktop Firefox 151+) and never says "Chromium" [A2]
- [x] **CONN-03**: User reads, before clicking, what the browser's port picker is, that the browser asks and not HANGAR, and that HANGAR sees nothing until they choose [A3]
- [x] **CONN-04**: User whose port is held by another program (typically Grid Editor) sees Grid Editor named as the likely culprit and the recovery in order — quit the other app, unplug, wait, replug, reload, connect — instead of the raw `Failed to open serial port` exception [A4]
- [x] **CONN-05**: User who cancels the picker sees a distinct "you cancelled" state, and user whose picker listed nothing sees the cable/driver branch (charge-only USB cable warning included) [A5]
- [x] **CONN-06**: Returning user is reconnected silently from `navigator.serial.getPorts()` without re-running the picker, and unplug/replug updates the UI through the `connect`/`disconnect` events instead of failing on the next write [A8]
- [x] **CONN-07**: The port picker is filtered to ZONA's USB identity only (VID 0x303a / PID 0x8123); bootloader identities are never offered, and after opening, the module is verified as a ZONA from its heartbeat before any control is enabled — any other module is refused with a plain message [A9]
- [x] **CONN-08**: User can see the connected module's type and firmware version, so "connected" means "connected to a ZONA", not "a port is open" [A7]

### Non-destructive install [B]

- [x] **SAFE-01**: Nothing is written to the module without an explicit click, and the connect screen says so out loud [B1]
- [x] **SAFE-02**: `TRY ON DEVICE` writes to RAM only and is the primary action; `KEEP ON DEVICE` stores to flash and is a visibly secondary, separate action; the two never sit as equal-weight buttons [B2]
- [x] **SAFE-03**: At connect — before any write is possible — HANGAR snapshots the touch element's Setup and Timer configs, and `PUT BACK` restores them with one click at any time [B3]
- [x] **SAFE-04**: The snapshot persists in `localStorage` keyed by module identity, so `PUT BACK` survives a closed tab [B4]
- [x] **SAFE-05**: `KEEP ON DEVICE` requires a confirmation that names what is being replaced ("the Setup and Timer scripts on your ZONA's touch element") and states that it survives a power cycle [B5]
- [x] **SAFE-06**: On a rig with more than one Grid module, the flash confirmation names the other modules and states that their current pages are stored too (PAGESTORE is a global broadcast); the action remains allowed
- [x] **SAFE-07**: "Installed" means an ACKNOWLEDGE frame was received for each event write, never a resolved writer promise; a write that lands one event but not the other is detected, reported plainly, and recovered by retry or by reloading the page from flash — the user is never left with a silent half-written config
- [x] **SAFE-08**: The install states its own speed honestly ("about a second") and confirms a settled state rather than showing a progress bar for a 200 ms operation [B6]
- [x] **SAFE-09**: Retries on timeout are bounded, and a lost connection mid-write ends in a named failure state with `PUT BACK` still offered, never an infinite retry loop

### Preview [C]

- [x] **PREV-01**: Every card in the catalog animates live in the firmware-faithful simulator with no hardware attached [C1]
- [x] **PREV-02**: The simulator consumes the exact compiler output that would be written to the module — no hand-authored animation anywhere [C2]
- [x] **PREV-03**: The site states in one line what the simulator matches exactly and what it cannot show (physical LED colour, diffusion, touch feel) [C3]
- [ ] **PREV-04**: The focused card accepts mouse-as-finger input so the user can play the instrument, not just watch it [C4]
- [x] **PREV-05**: Offscreen cards pause (IntersectionObserver with a wake margin), `prefers-reduced-motion` falls back to a still frame or play-on-hover, and the render path stays within budget at a dozen visible cards at 30 fps [C5]
- [x] **PREV-06**: Every ported preset's simulated output is pinned against an oracle derived independently of the compiler (firmware source or hardware capture), so a shared misreading cannot hide behind a green suite

### Catalog [D]

- [x] **CAT-01**: Every configuration has a deep link that lands on its detail view [D1]
- [x] **CAT-02**: User can sort by Featured and Name; no popularity metrics are shown or faked [D4] _(amended 2026-09-08 by plan 10-07, D-11: Newest removed)_
- [x] **CAT-03**: User can open a focus/detail view for one configuration where it runs at full frame rate with interaction, knobs, budget meters and install controls [D5]
- [ ] **CAT-04**: The catalog is a static data file of Profile-Cloud-shaped config objects plus tuning metadata, buildable with no backend

### Content

- [x] **CONT-01**: The nine BOTOR shelf presets — starfield, aurora, pinwheel, radar, faders, ninepads, tpad, dial, joystick — are in the catalog, each compiling to the same Lua as BOTOR at the pinned protocol version, **except at the divergences enumerated in `src/lib/fidelity/upstream-manifest.json` (the vendored compiler's own constants: the comet-family decay start and the two class-B guards, plan 11-04), declared per preset and per field in `src/lib/catalog/divergence.ts` (HANGAR's own values for the nine, plan 11-06) and held by `src/lib/fidelity/preset-baseline.spec.ts` and `src/lib/catalog/presets.spec.ts`, each row with its recorded reason, plan and date** _(amended 2026-09-10 by plan 11-16, as plan 11-03 recorded in advance and plans 11-05 and 11-06 widened: five of the nine — aurora, pinwheel, starfield, radar, dial, every preset whose `touch.kind` is `comet` or `perFinger` — no longer compile to BOTOR's bytes because BOTOR's bytes freeze a crossed cell at phase 1 to 7 of 255 forever, and four — aurora, pinwheel, starfield, joystick — plus ninepads carry HANGAR's own values for the bench's asks. The clause is not deleted: "the same Lua as BOTOR" is still the rule, the divergences are the enumerated exception, and a difference at any un-named site is still a STOP-and-report under D-08)_
- [x] **CONT-02**: At least six new configurations authored for spectacle are in the catalog, each fitting the 908/908 budget at its default knob positions and verified in the simulator
- [x] **CONT-03**: Every catalog entry has a name, a one-line description, feel-based tags (exactly three since 10-06: one `FOR`, two `FEELS`), a Featured flag and a default knob state

### Tuning [F]

- [x] **TUNE-01**: Each configuration exposes three to six knobs (from colour, speed, layout, brightness, MIDI destination and config-specific parameters) using one shared widget vocabulary [F1]
- [x] **TUNE-02**: Every knob change recompiles and re-simulates live; the recompile is debounced, the preview is not [F2]
- [x] **TUNE-03**: Two separate live meters show Setup and Timer usage as `chars / 908` with a percentage [F3]
- [x] **TUNE-04**: When the fit ladder trims a feature to stay in budget, the user is told so in one line rather than the preview silently diverging from their expectation [F4, generic wording acceptable in v1]
- [x] **TUNE-05**: An over-budget state disables `TRY ON DEVICE`, turns the offending meter red, names the knob that pushed it over and offers a one-click back-off; the click never reaches the wire to fail there [F5]
- [x] **TUNE-06**: User can reset one knob (double-click) or the whole configuration to defaults [F6]
- [x] **TUNE-07**: User can hit `SURPRISE ME` to randomise knobs into a state that is never over budget [X8]

### Sharing [E]

- [x] **SHARE-01**: The tuned state is encoded as the versioned base36 stamp in the URL hash (never the query string), and opening such a URL restores the knobs exactly [E1]
- [x] **SHARE-02**: User has an explicit `COPY LINK` control whose own state confirms the copy [E2]
- [x] **SHARE-03**: A stamp from an older HANGAR version fails gracefully — "this link was made with an older version" — and lands on the base configuration, never a subtly wrong one [E3] _(amended 2026-09-10 by plan 11-16: the forever-clause on `src/lib/share/fixtures/wild-stamps.json` — every captured payload keeps decoding to `restored` — now has exactly ONE recorded exception, and it is the first the tree has ever spent. Plan 11-09 appended "1" and "5" to POMODORO's `@MINS` on 2026-09-09 at the user's own request; appending is a resize, `stamp.ts`'s `shapeOf` sums a rack's option counts, so POMODORO's shape character moved `n` to `p` and the captured literal `xn33333` lands `older` instead of `restored`. That is precisely the graceful landing this requirement specifies and not the subtly-wrong one it forbids: indices 0..3 still name 15, 20, 25 and 50 minutes, proved by four payload literals captured before the change. The fixture is neither regenerated nor edited; the expectation moved into `stamp.spec.ts`, gated on `record.entry === "pomodoro"`, so a second entry appearing in that branch is a signal that somebody is resizing knobs casually. The site has never been public, so no real POMODORO link exists and the cost is zero in practice; the tripwire is spent per entry, and every future knob resize on any entry demotes that entry's existing links the same way)_
- [x] **SHARE-04**: Every catalog configuration has a build-time OG image rendered from the simulator, so a shared link unfurls with the pad picture and title on Discord [E4]

### Honest degrade [G]

- [x] **DEGR-01**: The full catalog, simulator, tuning and sharing work on every browser including iOS Safari — only install is absent [G1]
- [x] **DEGR-02**: Install controls are present but disabled with the reason inline on unsupported browsers, never hidden [G2]

### Identity

- [x] **IDENT-01**: The site implements the reference identity: true-black ground, a single acid-lime accent (~#D6FF4E), generative glyph-field texture as wallpaper, wide-tracked uppercase type, and the 9x9 pad outline as logo, loading state and card frame
- [x] **IDENT-02**: The site is motion-forward — a rack of running machines — while honouring `prefers-reduced-motion`

## v2 Requirements

Deferred to future release. Tracked but not in current roadmap.

### Connect

- **CONN-09**: Expanded cable/driver troubleshooting page [A6] — trigger: first "nothing showed up" report

### Non-destructive install

- **SAFE-10**: Page-change / VM-reset warning during an audition, driven by PAGEACTIVE reports [B7] — trigger: first "the audition disappeared" report

### Catalog

- **CAT-05**: Free-text search over name, tags and description [D2] — trigger: catalog exceeds ~20 entries
- **CAT-06**: Tag chips that filter on click and are individually removable [D3]
- **CAT-07**: Capability filters (needs MIDI destination, uses touch, animation only, fits at max brightness) [D6]
- **CAT-08**: Curated shelves beyond a single Featured sort [D7]
- **CAT-09**: Named deterministic slugs (`/c/starfield#stamp`) [X7]

### Tuning

- **TUNE-08**: Fit-ladder degradation names the exact feature trimmed via compiler introspection [F4 full]
- **TUNE-09**: Generated Lua viewable on demand, read-only [X5 extended]

### Sharing

- **SHARE-05**: Fully bidirectional URL state — knob drags update the hash via `replaceState` [E5]
- **SHARE-06**: `COPY LINK` offered as the unsupported-browser fallback ("open this on a desktop browser") [G3]

### Preview

- **PREV-07**: Before/after compare of the module's existing config against the candidate, where the existing config is parseable [X6]

### Content

- **CONT-04**: Community submissions accepted by pull request into the static catalog, with a review process

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Accounts, cloud library, community uploads [N1] | User-uploaded Lua is arbitrary code executed on a stranger's hardware from a stranger's website — categorically unlike a sandboxed shader. Also moderation, storage and abuse surface for a static site. Sharing is URL-encoded instead. |
| Full layer/zone authoring in the browser [N2] | QMK Configurator's friction chain is the cautionary tale. HANGAR tunes named configurations; authoring lives in BOTOR and the Editor. |
| Firmware update, bootloader, DFU [N3] | HANGAR must never be able to brick a module. The port picker never even offers bootloader identities. |
| Streaming the real ZONA's LEDs as the preview [N4] | Structurally capped at 3.3 Hz by the editor-heartbeat gate in firmware; it would make a spectacular config look broken. |
| Auto-apply on connect [N5] | Violates "nothing writes without a click"; reads as a website taking over your hardware. |
| A single Install button doing RAM and flash together [N6] | Collapses the audition/commit distinction that is the entire safety story. |
| User-agent sniffing for the install gate [N7] | Firefox 151 already broke that assumption once; Chrome Android exposes `navigator.serial` for Bluetooth only. Feature-detect. |
| Raw serial console / log pane [N8] | A debugging tool for firmware flashers; it turns a playground into a terminal. Plain-language errors instead. |
| Like counts, view counts, trending [N9] | Needs a backend; fake counts corrode the honesty everything else depends on. |
| QR codes [N10] | Install needs USB needs a desktop; a QR sends the config to the one device that can never install it. |
| Screenshots of the browser port picker [N11] | Chrome's and Firefox's differ and drift across versions; describe it in words. |
| Erase device / factory reset [N12] | A button whose only function is destroying the user's work — the opposite of `PUT BACK`. |
| Grid Editor as a dependency | The whole point of the Web Serial route. |
| Modules other than ZONA | Simulator, compiler and identity are all 9x9-specific. |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| FOUND-01 | Phase 2 | Complete |
| FOUND-02 | Phase 3 | Complete. **AMENDED 2026-09-10 (plan 11-16).** PROVED: the three vendored suites run at 176 / 96 / 9 with the 22 enumerated divergences of `upstream-manifest.json` applied — 10 in `_pad.ts`, 6 in `pad-sim.ts`, 1 in `tests/pad.test.js`, 5 in `tests/pad-sim.test.js` — every row matching exactly once, and `vendored-diff.spec.ts` (15) reconstructing upstream's bytes through both the deltas and the divergences to the recorded sha256. `git diff --stat 4131ff5 HEAD -- src/vendor/` names exactly the four files the manifest names and no other. NOT PROVED: that a future BOTOR re-sync merges cleanly — `VENDOR.md`'s eleven-step procedure and `scripts/record-upstream-manifest.mjs`'s carry-forward have been written and never run against a real upstream move. |
| FOUND-03 | Phase 1 | Complete |
| FOUND-04 | Phase 1 | Complete |
| FOUND-05 | Phase 3 | Complete |
| CONN-01 | Phase 6 | Complete (one control, in the header, on `/`, `/c/<id>/` and `/browse/`, enabled from `capabilityOf({hasSerial, secure})` and nothing else; no user-agent read exists anywhere in the tree, asserted by session-copy.spec.ts and the source scans; proven in both browsers) |
| CONN-02 | Phase 6 | Complete (two distinct messages, both rendered in a real browser: `unsupported` on WebKit with no `navigator.serial`, `insecure` through a shadowed `isSecureContext` - the first time the insecure branch has ever rendered in this project; the unsupported one names Chrome, Edge and desktop Firefox 151 and no engine) |
| CONN-03 | Phase 6 | Complete (the 130-character pre-click line beneath the header row in every not-connected state and in the chosen panel, never both at once; the Firefox two-step sentence ships unconditional and brand-free beneath `Nothing listed?`, because no non-brand behavioural signal for that prompt exists). **AMENDED BY NAME, 2026-09-08, plan 10-03 (10-UI-SPEC.md 4, R-02): `PICKER_EXPLAINER` and `PickerExplainer.svelte` are retired outright.** A paragraph predicting the browser's own port chooser is unnecessary text (D-08): the chooser explains itself the instant it appears, and it is the browser's chrome rather than HANGAR's, which is the very fact the paragraph existed to state. The half of the requirement that names a consequence - that nothing is seen or sent until the visitor chooses - is carried instead by `SAFE_NOTE`, which is on the screen in **every** state rather than in the not-connected ones only. The Firefox two-step sentence, `Nothing listed?` and its three steps, and the blocked-ports disclosure are untouched: they name next steps, and R-11's audit rule never retires those **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **AMENDED.** PROVED: the pre-click explanation is retired outright - `PICKER_EXPLAINER` and its component are gone - and the half of the requirement that names a consequence is carried by `SAFE_NOTE` plus **the browser's own port chooser**, which explains itself the instant it appears and is the browser's chrome rather than HANGAR's. The guarantee now renders in more states and on more surfaces than the paragraph it replaces ever reached. NOT PROVED: the Firefox two-step site-permission prompt ships an unconditional, brand-free sentence because **no non-brand behavioural signal for that prompt exists**, so a visitor on Chrome reads a sentence about a prompt they will not see. That is a deliberate trade, not an oversight, and it has no test because there is nothing to detect. |
| CONN-04 | Phase 6 | Complete against a scripted `NetworkError`: the named block, Grid Editor by name, the six steps in order, the browser's own `Failed to open serial port.` nowhere on the page, in node and in a browser from both the probe and the shipped header. That Grid Editor is what produces that error on a real machine is docs/SESSION-RUNBOOK.md row C - Phase 2's row 0 was never exercised - and is awaiting the user |
| CONN-05 | Phase 6 | Complete (`cancelled` is its own state with `You closed the chooser`; the empty-picker branch is the `Nothing listed?` disclosure beneath it, charge-only cable first, because the API cannot tell the two apart - one `NotFoundError`, three causes; the third cause, a site setting, gets its own disclosure) |
| CONN-06 | Phase 6 | Complete against a scripted serial: `getPorts()` on load offers `ZONA detected` with the port unopened, one click connects with zero `requestPort()` calls (never automatic, D-06), `disconnect` flips the header in the same turn, and the replug that mints a NEW port object is adopted by USB identity; the connection survives a four-hop client-router walk and a reload lands the offer. The grant surviving a browser restart (and for the deployed origin) and the replug on real hardware are runbook rows A and B, awaiting the user |
| CONN-07 | Phase 6 | Complete with the correction the research found: the filter is `0x303a/0x8123`, but every ESP32-S3 Grid module shares that identity, so the filter does NOT narrow the picker to a ZONA and the heartbeat verify is the whole of it - `not-zona` ships as a routine first-class state naming the module that answered. Proven against synthetic heartbeats; the rig half (a real second module named in the header, a real non-ZONA refused) is runbook row D, awaiting the user |
| CONN-08 | Phase 6 | Complete (type, firmware and active page in the header while connected - `ZONA · fw 1.5.5 · page 3` from the Phase 2 capture - LIVE rather than frozen at connect time, with the rig tail filling in as other modules announce themselves; `connected` is reached only after the heartbeat names a ZONA) |
| SAFE-01 | Phase 7 | Complete as a number: zero config writes across connect, the snapshot and every knob move, counted BY CLASS in node (install.spec.ts test 4) and in a browser (install.e2e.ts test 1, and session.e2e.ts's seven cable tests); every write attributable to one of four clicks; the session's own source scan still clean (session.spec.ts test 15, ten needles, the write view built with write.bind and never called). Both never-writes surfaces say so in the present tense: the header note's SAFE_PROMISE amended in 07-04 (its "cannot write at all" clause retired, session-copy.spec.ts test 5 rewritten) and the panel's two Phase 4 literals retired in 07-10. **AMENDED BY NAME, 2026-09-08, plan 10-03 (10-UI-SPEC.md 10.1, R-03): the mechanism is no longer `SAFE_PROMISE`.** That 88-character paragraph is retired site-wide - it rendered in the header note in three of the nine slot states and in the device disclosure only when a visitor opened it, and it was absent from the panel where the click happens. It is replaced by `SAFE_NOTE`, 35 characters, `Nothing is written without a click.`, rendered directly beneath the primary control on the chosen panel and beneath the header's device slot, unconditionally, in **every** state - writing, every failure and cannot-write-here included - on both surfaces that carry the primary. The guarantee is therefore on the connect screen in more states and on more surfaces than the paragraph it replaces ever reached, which is why SAFE-01 stays `[x]` rather than being retired. `session-copy.spec.ts` test 5 is rewritten again, holding `SAFE_NOTE`'s literal and its 35, and asserting that neither `SAFE_PROMISE` nor `PICKER_EXPLAINER` is exported. **AMENDED BY NAME, 2026-09-08, plan 10-12 (10-UI-SPEC.md 10.7, A-53), in two clauses.** (a) **Three clicks became four** - `CLEAR` joins `TRY ON DEVICE`, `PUT BACK` and `KEEP ON DEVICE` - and the number stops being a word in prose: `install-copy.ts` exports `WRITE_CLICKS`, a four-member `as const` asserted to have length 4 and asserted **equal to the four control labels**, so the next change moves a constant rather than a sentence. The proof itself did not weaken anywhere: `install.spec.ts` test 4 counts **by class**, CLEAR writes `CONFIG/EXECUTE` - the class already counted - so its reach was already total and the assertion needed no widening (G-06's documentary finding; A-53 corrects the approved §10.7 text that said the enumeration gained a class). What widened is `InstallAction`, which the compiler enforces for free. (b) **Eight needles became ten, and eight was already stale at nine before this phase** - the scan has read `.write(`, `RequestQueue`, `hostHeartbeat`, `sendConfig`, `storePage`, `fetchConfig`, `storeToFlash`, `writeBack` and `setInterval` since Phase 7, and plan 10-12 adds `clearToDefault` so a `clear.bind`-shaped export cannot slip past the scan that exists to catch exactly that shape. The jump from a recorded eight to a recorded ten is therefore a correction of a pre-existing staleness as well as an extension, and it is stated as one rather than renumbered quietly **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: zero writes **by class** across connect, the snapshot and every knob move, in node (`install.spec.ts` test 4) and in a browser (`install.e2e.ts` test 1, `session.e2e.ts`'s cable tests); **four** clicks, held in `WRITE_CLICKS` as a four-member `as const` asserted equal to the four control labels, so the next change moves a constant rather than a sentence; the guarantee itself on the control in **every** state on both surfaces that carry the primary, at 35 characters. `REQUIREMENTS.md:169`'s eight-needle figure is corrected to **ten**, having already been stale at nine before this phase - stated as a correction of pre-existing staleness as well as an extension. NOT PROVED: which of Open item 1's three forms the user wants - form 1 ships and both reversals are costed at one edit each. And the compiler's exhaustiveness audit over `InstallAction` **names nothing**, because nothing in `src/` switches over the union; the guarantee that carries weight is `WRITE_CLICKS`'s length and the by-class count, not the type. Deferred item 17. |
| SAFE-02 | Phase 7 | Complete: the primary is the one full-width accent fill; KEEP ON DEVICE is the Quiet tier (borderless, unfilled, 124.9x44 against 370x44) a hairline, the tuning region and PUT BACK away, live only after a settled try-on (Z-05); the confirmation's affirmative is bordered, never filled (07-09, 07-10, 07-12 tests 7 to 9). RAM-only on real hardware - a power cycle bringing the original back - is docs/INSTALL-RUNBOOK.md row D, awaiting the user **Amended 2026-09-09 by plan 10-13 (D-04, A-46, A-47):** a fourth control joined the Quiet tier beside `KEEP ON DEVICE` and the two weights did NOT move. D-04's sequence is a caption (`NEXT`, 4), DOM order and enablement, never equal weights - no numerals, no bracket, no shared background, no equalised widths and no second hairline - and the requirement is now held by a site-wide COUNT rather than by a pairwise reading: exactly one interactive class in `src/lib/ui` declares the accent fill and it is `TryOnDevice.svelte -> .primary` (`device-ui.spec.ts` test 10; planted on `KEEP ON DEVICE` it goes red naming the offender). **`CLEAR` and `KEEP ON DEVICE` are visually identical at rest, and that is recorded rather than hidden** - A-47's three channels are the words, the enablement set, and the confirmation `KEEP ON DEVICE` has and `CLEAR` does not, **Amended 2026-09-09 by plan 10-13.1 (A-41, A-46):** a shared control shape arrived and the ladder survived it. `.pill` is ONE rule in `src/app.css` applied by class, replacing eleven copies of the same three declarations across nine files; **Primary takes its radius on its existing accent fill, Secondary and the word rows take the outline, and QUIET TAKES NOTHING.** `KeepOnDevice.svelte` and `Clear.svelte` are BYTE-UNTOUCHED across the whole plan (`git diff --stat` empty), and six Quiet controls are asserted still to declare `border: 0` and `background: transparent`. Pilling Quiet would give `KEEP ON DEVICE` and `CLEAR` a border and flatten TWO controls into Secondary - so where D-15's universal pill and this ladder collide the ladder wins, and the no-pill rule now protects six controls rather than one. `instrument.spec.ts` scan 2 asserts the tier violation BEFORE any measurement, so a planted pill names the tier rather than the padding, and every pilled element is proved to RESOLVE both 44px axes over a directory-derived walk - which reaches `TryOnDevice.svelte`, `CopyLink.svelte`, `Knob.svelte`, `TagChip.svelte`, `ChosenPanel.svelte` and `BrowseToolbar.svelte`, none of which `DEVICE_COMPONENTS` covers two of which are behaviour. **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: the two install weights are unchanged through D-04 - exactly one interactive class in `src/lib/ui` declares the accent fill and it is `TryOnDevice.svelte -> .primary`, asserted as a site-wide count rather than as a pairwise reading, and planted on `KEEP ON DEVICE` it goes red naming the offender. A shared `.pill` shape arrived in 10-13.1 and the ladder survived it: Quiet takes nothing, and `KeepOnDevice.svelte` and `Clear.svelte` are byte-untouched across that plan. **A DEFECT IN THIS PLAN'S OWN QUALIFIER TABLE, NAMED RATHER THAN ABSORBED:** 10-14's `<interfaces>` says *"CLEAR takes a fourth tier distinguished on five non-colour channels"*. The shipped tree says otherwise and two amendments say why - **A-46 retired the Bare tier**, leaving **three** tiers, and `CLEAR` sits in **Quiet** beside `KEEP ON DEVICE`; **A-47** records that the two are visually identical at rest and separates them on **three** channels, two of which are behaviour. Neither four nor five is the shipped number. The observation wins. NOT PROVED: nothing here; the hardware half of the RAM-only claim is runbook row D and is the user's. |
| SAFE-03 | Phase 7 | Complete against the fake: the snapshot is taken at connect in the gated order (fetch-serial, fetch-setup, fetch-timer, canWriteBack, memory, then storage) before any write control enables, and PUT BACK is one click from every state with a session, restoring the snapshot's strings verbatim through the one writer (07-06, install.spec.ts tests 1 to 3 and 5; install.e2e.ts tests 1 and 2; 07-12 test 9). Row C - the pad back to what it was doing, by eye - is awaiting the user **Extended 2026-09-09 by plan 10-13:** the snapshot gate now covers a fourth write, and the way back is proved for it in a browser. `CLEAR` is enabled only with a snapshot in hand (the second term of `clearEnabled`, plan 10-12) and its own `@webkit` e2e title, run on both projects against the fake, ends on `PUT BACK` restoring the visitor's own configuration after a clear. **Row C now carries the clear as well as the put-back** - clear, watch the pad take up the firmware default, then `PUT BACK` and watch it come back - and is still awaiting the user. **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: `CLEAR` refuses without a snapshot with **zero writes of any class** - the second term of `clearEnabled`, exercised in node and in an `@webkit` browser title in both projects - and the snapshot record is read and never written. `PUT BACK` is one click from every state with a session. NOT PROVED, and this is the phase's one hardware row: **the hardware half is row C's clear paragraph and it is unanswered** (A-51: **no row H was added**, the clear folded into row C so the runbook's row count would not move). And **SAFE-04's key is still wire-unproven** - it was wire-unproven before this phase and it still is; nothing here changed that, and recording it as changed would be false. |
| SAFE-04 | Phase 7 | Complete against the fake WITH THE KEY WIRE-UNPROVEN: the record lives under hangar.snapshot.v1 keyed by the module's factory serial then page, never overwritten or deleted, surviving a throwing store and a FORGET THIS ZONA (07-03, 07-06, 07-11); the key is read with SERIALNUMBER/FETCH, which is source-verified and has never been observed - no hardware capture holds the frame and the desktop editor never sends one - and a timeout degrades to a session-only copy with its own honest sentence (07-04, 07-11). Rows A and F are awaiting the user; if row A reads the session-only form, SAFE-04 closes as partially verified rather than as a lie |
| SAFE-05 | Phase 7 | Complete: the confirmation names the touch element and the power cycle before the store is sent ("This replaces the Setup and Timer scripts on your ZONA’s touch element, and it survives a power cycle."), is a role=group and not a dialog, takes focus on the container so the naming sentence is read first, and has exactly two exits plus Escape with PAGESTORE/EXECUTE 0 until the commit (07-09, 07-10, 07-12 test 8). Row E - the store surviving a real power cycle - is awaiting the user **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **UNTOUCHED, and this qualifier records what was NOT done rather than rewording a claim.** Phase 7's `KEEP ON DEVICE` confirmation block still satisfies SAFE-05 whole. The approved 10-UI-SPEC ruled SAFE-05 "Extended" by a CLEAR confirmation naming what is removed; **A-45 removed the confirmation and the requirement was therefore not extended.** `CLEAR` is covered by SAFE-01, SAFE-03 and SAFE-07 and by the one line beside it; SAFE-05's subject is the control that **stores to flash**, and `CLEAR` writes RAM only. The row above is left exactly as Phase 7 wrote it, deliberately. NOT PROVED: nothing new; row E, the store surviving a real power cycle, is still the user's. |
| SAFE-06 | Phase 7 | Complete against a synthetic rig: three acknowledgements resolve one store, the fourth sentence names the modules in sx order ("Your EN16 and BU16 are on the same cable. Their current pages are stored too, because the store reaches every module at once."), and the action stays allowed - Phase 2's refuse-on-rig undone by name in 07-02 with sequence.spec.ts test 3 rewritten rather than deleted (07-12 test 8's rig page). Row G is optional and awaiting the user; every frame Phase 2 ever recorded carried SX 0, SY 0 |
| SAFE-07 | Phase 7 | Complete: settled from the acknowledgements only, never from a resolved write; a dropped second acknowledgement is `partial`, named by half ("Timer reached your ZONA and Setup did not."), with retry and PUT BACK offered; a refusal on the first leg is `nothing-landed`; the bytes written are the meters' bytes (wire-pin.spec.ts). Proven against scripted faults in node (install.spec.ts) and in a browser (install.e2e.ts test 4), which is what the criterion asks for; no hardware half **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: `cleared` is settled from **two `CONFIG/ACKNOWLEDGE` frames** and never from a resolved writer promise, against scripted faults in node and in a browser - the same rule Phase 7 established for the store, extended to the fourth write without weakening. NOT PROVED: **no hardware half**, and unlike SAFE-03 there is no runbook row for it, because a dropped acknowledgement is a fault a real module will not produce on demand. |
| SAFE-08 | Phase 7 | Complete: "about a second" appears once, before the click, and nowhere else on the site (Z-08); no bar, no spinner, no minimum duration; the busy label swaps with a 0s transition and PLAYING NOW is said from the acknowledgements (+39 to +47 ms to WRITING… under a hold); the 2000 ms "Still writing." line is the one escape hatch, spoken once (07-10, 07-12 tests 7 and 10). The wall time on hardware is row B, awaiting the user |
| SAFE-09 | Phase 7 | Complete: three attempts and no more (RETRY_ATTEMPTS, retryBackoffMs 120/240/360); a refusal and an abort are never retried; a lost link mid-write is a named state ("The ZONA was unplugged mid-write") with PUT BACK waiting for the module; the pacing escalation fires once and moves no attempt count; no infinite loop is possible by construction (install.spec.ts; install.e2e.ts tests 4 to 6; 07-12 test 10). No hardware half |
| PREV-01 | Phase 4 → Phase 10 | Complete (five presets animate; three quiet presets show their real static picture, declared and gated against the golden frames; tpad rests black and is out of the front-door row). **Extended 2026-09-08 by plan 10-05 (D-09).** Three of the four entries that recorded zero lit bytes at all five sampled ticks — GHOST, MORPH and ETCH — now animate on the browse grid from an authored gesture replayed through the shipped `TouchSampler` at the shipped rate, lighting 14, 19 and 5 of 81 cells. The motion is the firmware's; only the finger is HANGAR's, and `src/lib/sim/demo.spec.ts` asserts the driver never reaches an engine directly. **tpad is the one entry this cannot reach**: its draft enables no LED layer at all (`look.kind` and `touch.kind` both `none`, both disabled), measured at 0 of 81 over a drag, a two-finger scroll, a single tap, a two-finger tap and 2,000 idle ticks. It is named in `DARK_BY_CONSTRUCTION` with that measurement, its card carries a sentence of its own, and giving it a picture would mean changing the configuration — 10-UI-SPEC §9.3's rejected option (a) **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: every one of the 29 entries animates in the simulator with no hardware attached, and this phase made the preview MORE faithful in two places and NOTICED where it is not. More faithful: the comet-family decay lands on phase 0 in `pad-sim.ts` as well as in `_pad.ts` (11-04, both files moved together so the preview cannot show a stuck cell the device has stopped showing), and a pad whose 2D backing store is dropped gets its picture back by event and by paint-time guard (11-08.1, proved red on the code as it shipped in both engines). NOT PROVED, and recorded as fidelity gaps rather than bugs: (1) `src/lib/sim/touch.ts` never emits event code 9, so a visitor with a mouse can never perform the coalesced fast tap that five waves of this phase fixed — every class-B fix is right for the module and invisible in the browser (`TOUCH-CODE-9.md`); (2) the Lua host keeps ONE `_coordMax` for both axes where firmware has two (`D-11-13-a`); (3) `lua-host.ts` returns early on `gtt(index, 0)` without clearing the deadline, so a card that stopped itself that way would stop on the module and keep running in the preview (11-15); (4) whether WebKit ever emits `contextlost` for a 2D canvas was not observed in any run and is claimed in neither direction. |
| PREV-02 | Phase 4 (Lua-sourced entries: Phase 8) | Complete **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: the nine preset-backed entries still render from the vendored compiler's own output, and the twenty Lua entries from the exact bytes `renderLua` emits, which are the bytes the install path writes (`wire-pin.spec.ts`). NOT PROVED: that the compiler's output is BOTOR's — it deliberately is not at 22 enumerated sites (CONT-01, FOUND-02 above), and `lua-parity.spec.ts` (5) is the gate that still holds the vendored compiler's output against the vendored simulator through a real VM for all nine, green and unedited. |
| PREV-03 | Phase 4 → Phase 10 | **Complete at 44 characters, with the second clause amended by name and dated 2026-09-08** (plan 10-03, 10-UI-SPEC.md 4 and 13.2, R-04). The one line is `FIDELITY_LINE` in `src/lib/ui/fidelity-line.ts`, `Every pad here runs the firmware’s own code.`, rendered in both of its two places - the coverflow's `FidelityLine.svelte` and `/browse/`'s line beneath the grid - unconditionally and entry-independently, so it is never only beside the pads that flatter it. **What the simulator matches exactly is kept whole and is the whole of the shipped line.** What it cannot show - `What a screen cannot show: the real colour of the lights, the way they bleed into each other, and how the surface feels under a finger.` - is RETIRED, 187 characters of the original 231. It is retired under D-08 and the amendment register's audit rule: a string goes only when the control beside it, or the pixels beside it, already say the same thing, and a wall of thirty-six animating pads demonstrates the limits of a screen better than a sentence apologising for them. The clause named no risk, no consequence, no way back and no next step, which is the test R-11 protects the failure copy with **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **CLOSED at 44 characters**, from 231, and unconditional **wherever it appears** - both of its two sites, the coverflow's `FidelityLine.svelte` and `/browse/`'s line beneath the grid, entry-independent so it is never only beside the pads that flatter it. The 187 retired characters were an apology for what a screen cannot show, and D-08's audit rule is what allowed them to go. NOT PROVED: nothing outstanding; this is the one requirement in the twenty that this phase moved from `Pending` to closed. |
| PREV-04 | Phase 4 | Pending |
| PREV-05 | Phase 4 | Complete (row mounts at most seven pads; offscreen pause and reduced motion asserted; paint count recorded 135–216 per 2 s, no fps floor asserted — accepted deviation 3) |
| PREV-06 | Phase 3 | Complete **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: `firmware-oracle.spec.ts` is green and byte-unedited through a phase that was allowed to edit `pad-sim.ts` — its seventh test runs straight through the comet site plan 11-04 changed, was observed RED on a planted clamp of the phase walk and GREEN on the shipped start-constant change, which is the mechanical proof D-02's boundary held. `golden-frames.json` is unmoved because both frame fixtures sample with no touch input. NOT PROVED: the oracle covers the phase walk and the tick order; nothing independent of the simulator pins the two class-B guards 11-04 changed (`LIVE` and `phaseCond`'s held arm), which were verified against firmware C by reading rather than by a second implementation, and nothing measures preset-backed residue end to end — the residue probe iterates Lua entries only (plan 11-04's recorded gap). |
| CAT-01 | Phase 4 | Complete (eight prerendered /c/<id>/ pages; tpad has a catalog entry but no page because it writes no LEDs — /c/tpad/ lands on the shelf with a notice) **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: **29** prerendered `/c/<id>/` pages, one per catalog entry including the two authored this phase (`/c/wheels/`, `/c/radar-points/`), each with its own `<head>` and a real `og:image` (`og/build.spec.ts`, `artifacts.e2e.ts`). NOT PROVED, and decided rather than proved: the **nine** addresses plan 11-01 removed — `/c/hold/`, `/c/keys/`, `/c/learn/`, `/c/switch/`, `/c/etch/`, `/c/gridlock/`, `/c/life/`, `/c/slam/`, `/c/table/` — now fall through to `404.html`. Anyone who shared one gets a 404. Shipped as the honest default; the reversal is costed in `deferred-items.md` (a prerendered stub per removed id, or one redirect rule at the Worker, the second possible only because the site is on Cloudflare Workers rather than GitHub Pages) and is the user's decision. **PHASE 12 QUALIFIER, 2026-09-10 (plan 12-04).** PROVED: **26** prerendered `/c/<id>/` pages, one per catalog entry, each with its own `<head>` and a real `og:image`; the two non-vacuity floors that read the routed set (`og/build.spec.ts`, `artifacts.e2e.ts`) were re-chosen from 27 to 26 so they still guard rather than fail. NOT PROVED, and decided rather than proved for the SECOND time: **three more** addresses — `/c/lattice/`, `/c/forge/`, `/c/shuttle/` — now fall through to `404.html` beside the nine, so twelve shared links 404. The reversal is the same one, still costed in `deferred-items.md` (a prerendered stub per removed id, or one redirect rule at the Worker), and twelve stubs is the same edit as nine. **A SECOND DEAD LINK IS RECORDED HERE RATHER THAN DISCOVERED AT DEPLOY: `?for=keys`.** `keys` was minted as a `FOR` term by plan 10-06 as `harmonic`'s destination and shipped as a chip; plan 12-04 retired it because the three removals left it on one entry against the zero-singleton rule. `query.ts:181` drops an unknown `?for=` value SILENTLY under W-12 unamended, so such a link lands on the full catalog — not a 404, not an apology, and not the search field, because the search fallback at `:186` is reached only through the `tag` branch. It differs from `drums` (11-01) only in that `drums` was one of the fifty-five retired SHIPPED tags and `keys` never was, so `LEGACY_TAG_MAP` could not have carried it even if a row had been added. |
| CAT-02 | Phase 5.1 | Complete with one qualifier about provenance, not about behaviour: the three orders ARE the shipped comparators in src/lib/catalog/index.ts as 05.1-CONTEXT.md D-10 amends them - byFeatured() (featured first, then name, no addedAt tie-break), byNewest() and a plain code-point nameAsc, with no localeCompare and no Intl.Collator anywhere. 05.1-UI-SPEC.md W-08 AGREES with that rule and is NOT superseded; what the amendment supersedes is 05.1-RESEARCH.md, whose Standard Stack row and Do-Not-Hand-Roll row both recommend Intl.Collator("en", { sensitivity: "base" }). nameAsc is not exported, so src/lib/browse/sort.ts restates it and sort.spec.ts tests 2-4 gate the restatement against byFeatured()/byNewest()/byName() id sequences while test 5 asserts mechanically that neither localeCompare nor Intl is named. The no-popularity-metric half is held by browse-ui.spec.ts test 1 as a comment-stripped source scan with fragment-assembled needles, and by W-04 refusing a count on any chip. **Amended 2026-09-08 by plan 10-07 (D-11).** The requirement now reads TWO orders, not three: Newest is removed, and the removal is a data finding rather than a preference — `addedAt` holds three distinct values across thirty-six entries and one of them covers twenty, so the order's first block was twenty entries deep and in name order inside itself, which is the NAME sort wearing a date's name. `BrowseSort` is `"featured" | "name"`, `BROWSE_SORTS` is two, `newestOrder` and `orderFor`'s middle branch are deleted, and `sort.spec.ts` goes 6 → 5 with the NEWEST date-block test RETIRED BY NAME rather than deleted. `?sort=newest` falls back to the default SILENTLY and `query.spec.ts` asserts it, because that is the one retired value really out there on links people already sent. `addedAt` leaves `ListingEntry` and all thirty-six restatements in `listing.ts` while staying on the catalog entry as provenance, and NO entry file was touched. A third order was considered in its place (MOTION, animated first) and refused as redundant: the `FEELS` facet's `generative` and `still` answer that question as a filter. The no-popularity half is unchanged and its two gates still hold — `browse-ui.spec.ts` test 1 now walks `FacetRow.svelte` too **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **AMENDED.** PROVED: three sorts become two, and the removal is a **data finding** rather than a preference - `addedAt` holds three distinct values across thirty-six entries and one covers twenty, so Newest's first block was twenty entries deep and in name order inside itself. `addedAt` leaves the browse projection and **stays on the catalog entry** as provenance, with no entry file touched. `?sort=newest` falls back silently, asserted, because that is the one retired value on links already sent. NOT PROVED: nothing; the no-popularity-metric half is unchanged and both its gates still hold. |
| CAT-03 | Phase 5.1 | Complete with one qualifier that must not be read past: the INSTALL CONTROLS ARE PHASE 7'S AND ARE STILL ABSENT OR DISABLED. What this phase delivers is the focus view itself - all sixteen configurations now have a real prerendered /c/<id>/ page (05.1-05), an off-row entry gets a row of one with an arrow-less plate, the hero runs at full frame rate and takes mouse-as-finger, Phase 5's knobs and both 908 meters are there, and the way back restores sort, query, tags and scroll offset. TRY ON DEVICE connects and identifies only, KEEP ON DEVICE is really disabled, and nothing writes. The install half is docked, not built. **Amended 2026-09-08 by plan 10-06 (D-10).** The browse vocabulary is re-cut from 55 distinct tags — 27 of them on exactly one entry, with 28 chips standing — to **sixteen closed terms in two declared facets**, `FOR` (ten: what you would reach for it to do) and `FEELS` (six: how it behaves under a finger and an eye), with **exactly three on every one of the thirty-six entries** and **zero singletons by construction**. The rule for what earns a chip changes from a derivation to a membership test: a term is a chip because it is a facet member, so the row cannot drift as the catalog grows. `src/lib/browse/facets.ts` declares the vocabulary, `facets.spec.ts` holds it against `LISTING` in both directions with both health rules, and `copy.spec.ts` holds it against `CATALOG`. The facet rows themselves, the `?for=` and `?feels=` parameters and the card's three chips are 10-07's; 10-06 is data and gates only and renders nothing. **Amended 2026-09-08 by plan 10-07 (A-19, A-20, A-22, G-10).** The vocabulary 10-06 declared is now RENDERED. The toolbar's single count-derived tag row becomes **two captioned facet rows** — `FOR` (ten chips) then `FEELS` (six) — each a `role="group"` with its own Micro caption and an `aria-labelledby` derived from the facet's name, built from one `FacetRow.svelte` with a 44px floor on BOTH axes per member. Sixteen chips in two labelled rows fit above the grid at every width, so **there is no disclosure and there never was one** (A-20). `chipTags()` and the **outsider chip** are deleted together with the derivation that needed them, and `disabledTags()` survives with its predicate narrowed to "would this term, alone in its own facet, return zero given the OTHER facet's active set", which makes it rare rather than common and is asserted from a browser in both directions. The combining rule is **OR within a facet, AND across facets**, and it is proved by an assertion that INVERTS: the shipped e2e block asserted `playable ∩ generative` was smaller than either, which is now false at 21 against 13, and a cross-facet press on `modulation` (4 entries) proves the AND half. The address becomes `?for=` and `?feels=`; `?tag=` is read-only for one release and G-10 rules four cases — mapped becomes a chip, shipped-but-unmapped becomes `?q=`, never-a-tag is dropped, and an explicit `?q=` always wins. The front door gains a `FOR` **link** row: ten static `<a href="/browse/?for={term}">` in `build/index.html`, with `front-door.ts` still declaring zero specifiers **Amended 2026-09-09 by plan 10-13.1 (A-42, A-44, §19.1c, §19.1d):** the catalog surface takes the instrument register. Each facet row gains a two-digit index and a real U+2014 as **aria-hidden SIBLINGS** of its caption, never inside it - the caption literals in `src/lib/browse/facets.ts` stay byte-identical, because folding the index into a string would be a copy retirement against an audit A-30 closed at ten, and would also put furniture inside the group's accessible name. The form is on `/browse/`'s two rows and nowhere else: the front door mounts the same component with no index, and no device component renders it, because §10.2 and A-23 forbid step numerals where the regions ARE the device sequence. Each card gains a `+`-separated, column-aligned metadata block in `--font-mono` at `--color-ink-quiet` - **`--font-mono`'s SEVENTH use, not its sixth** (A-44), qualifying on the columns half of W-03 rather than the jitter half, with the argument written into the component as `ColourPicker.svelte`'s comment demanded. **No divider, border, zebra or new `--color-line` use was added to make a row read** (§19.1d), and reference C's dense tabular listing is explicitly NOT adopted as the layout - the grid carries thirty-six live pad canvases and the pads are the product, asserted rather than described and the ring still at eight **Amended 2026-09-09 by plan 10-13.2 (D-22, A-58, A-59):** no card reads as a failure. `tpad` is the one entry no gesture can light - its draft enables no LED layer, measured at 0 of 81 - and it rendered as a near-black rectangle beside eight cards that read as pads, because the cell STRUCTURE on a pad face is drawn by Layer 3's gutter grid in `--color-ground` and a black grid divides nothing over black cells. `.dots` now carries `color-mix(in srgb, var(--color-line-soft) 25%, transparent)` = 0.05 behind the canvas, so the gutters cut an all-unlit face into 81 dark lamps. **Global, not scoped to the dark entry** - the constraint is the same strength every other card's unlit cells have, and scoping it would make the one card that can never light the brightest unlit face on the site. **Nothing about the configuration changed and nothing was faked**: `restsBlack`, `DARK_BY_CONSTRUCTION` with its measurement, `demoPathFor("tpad")` undefined, `front-door.ts`'s exclusion, `frames.spec.ts`'s both-directions walk and `browse.e2e.ts`'s one exemption are byte-untouched and green, and the OG image, rendered from the engine in Node with no CSS, is still black. What changed is how an UNLIT cell is painted, not whether it is unlit. And the card's prose is now clean of the registration field (A-55), measured at 51 lattice pixels removed from `card-arc`'s description box alone **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **AMENDED.** PROVED: **sixteen terms in two facets, exactly three per entry, OR within a facet and AND across** - required rather than conventional, because `FOR` gives every entry exactly one term and a second `FOR` chip under pure AND would always return zero. The count-derived tag row and the outsider chip are retired **together with the derivation that needed them**, and `disabledTags()` is **kept** with its predicate narrowed. The combining rule is proved by an assertion that INVERTS rather than one that passes: the shipped block asserted an intersection was smaller than either operand, and it is now false at 21 against 13. NOT PROVED: nothing in CAT-03's own subject. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: the detail view still opens every one of the 29 entries with knobs, both meters and the install controls, including the two authored this phase; the browse vocabulary is eight `FOR` and six `FEELS` terms, sixteen down to **fourteen**, and every filter path over it is green in node and in both browser engines. NOT PROVED: nothing in CAT-03's own subject. What this phase changed here is the vocabulary's size, not its shape. |
| CAT-04 | Phase 4 | Pending — and deliberately still unchecked after plan 10-06. **Amended 2026-09-08 by plan 10-06 (D-10).** The browse vocabulary is re-cut from 55 distinct tags — 27 of them on exactly one entry, with 28 chips standing — to **sixteen closed terms in two declared facets**, `FOR` (ten: what you would reach for it to do) and `FEELS` (six: how it behaves under a finger and an eye), with **exactly three on every one of the thirty-six entries** and **zero singletons by construction**. The rule for what earns a chip changes from a derivation to a membership test: a term is a chip because it is a facet member, so the row cannot drift as the catalog grows. `src/lib/browse/facets.ts` declares the vocabulary, `facets.spec.ts` holds it against `LISTING` in both directions with both health rules, and `copy.spec.ts` holds it against `CATALOG`. **This does not complete CAT-04**, whose subject is the shape of the catalog data file rather than the words a visitor browses by; what 10-06 changes is one field of every record. The row is amended so the vocabulary a future reader finds here is the shipped one. **Amended again 2026-09-08 by plan 10-07 (A-19, A-20, A-22).** The facet rows, the `?for=` and `?feels=` parameters and the card's three chips 10-06 deferred are all shipped. **This still does not complete CAT-04**, and it is deliberately left unchecked for the second time: its subject is the SHAPE OF THE CATALOG DATA FILE — Profile-Cloud-shaped config objects plus tuning metadata, buildable with no backend — and neither 10-06 nor 10-07 touched that. 10-06 changed one field of every record; 10-07 changed how a visitor filters on it. The row is amended so a future reader finds the shipped browse behaviour here rather than a stale one **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **AMENDED, AND DELIBERATELY STILL `Pending` FOR THE THIRD TIME.** PROVED: the same sixteen terms, two facets, three per entry re-cut across twenty-eight source files, and rendered. NOT PROVED, and this is why the box stays empty: **CAT-04's subject is the SHAPE of the catalog data file** - Profile-Cloud-shaped config objects plus tuning metadata, buildable with no backend - and nothing in this phase touched that. 10-06 changed one field of every record and 10-07 changed how a visitor filters on it. Ticking it here would claim something the tree does not support, so it is not ticked. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** **DELIBERATELY STILL `Pending` FOR THE FOURTH TIME, and `requirements mark-complete` was NOT run for exactly this reason.** PROVED: the catalog is still a static set of TypeScript modules under `src/lib/catalog/` buildable with no backend — 9 preset-backed entries whose VALUES HANGAR now owns in `presets.ts` (plan 11-05) plus 20 hand-authored Lua entries — and this phase removed nine, re-authored three, added two and moved five preset definitions into HANGAR without touching the SHAPE of that data. NOT PROVED, which is why the box stays empty: CAT-04's subject is that shape — Profile-Cloud-shaped config objects plus tuning metadata — and no plan in this phase claimed to deliver it; four plans (11-05, 11-14, 11-15 and this one) each carried it in their `requirements` frontmatter and each declined to tick it in writing. |
| CONT-01 | Phase 4, amended and closed at Phase 11 | Complete under the amended wording. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: the nine are in the catalog as nine `padsim` entries (`presets.spec.ts` test 3 pins the id list against the vendored shelf); `preset-baseline.spec.ts` (20) still compares the vendored compiler's output character for character against BOTOR's own fixture, re-cut only at the declared substrings; `presets.spec.ts` (4) diffs every field of HANGAR's nine against the vendored nine across the union of both objects' keys and refuses any difference not in `divergence.ts`'s seventeen rows with a reason, a plan and a date; `firmware-oracle.spec.ts` (7 + 1 todo) is green and unedited, which is the mechanical proof the simulator's phase walk was not touched while the compiler's constants were. NOT PROVED: that BOTOR's own bytes are what a ZONA runs today — nothing in this phase touched a device — and that `preset-baseline.json` still describes BOTOR's current compiler: it predates plan 11-04, is stale for pinwheel (305 against 312), radar (438/445), joystick (535/542) and faders (513/520), and is green only through its own substitution table (plan 11-06's finding, carried in `deferred-items.md`). |
| CONT-02 | Phase 8, closed at Phase 9 | Complete (twenty configurations authored in Phase 9 taking the catalog from sixteen to thirty-six, twenty-seven of them hand-authored Lua; every one canonical, in budget on both events at its defaults AND at every corner of its own knob cross-product — 701 combinations over 1,402 measured events, proved by the separability identity rather than by enumeration — every one running in a real Lua 5.4 VM through the scripted gesture without error, and EVERY CALL SITE checked against the registered host surface by the new D-07 gate. Hardware: the thirty-two-row audition is presented to the user and unanswered) **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: **twenty** hand-authored Lua entries (eighteen after plan 11-01's nine removals, plus WHEELS in 11-15 and RADAR POINTS in 11-14) — every one canonical, accepted by `checkSyntax`, in budget on both events at its defaults and at every corner of its knob cross-product, `lua-entries.sweep.spec.ts` green at **1,331 combinations / 2,662 measurements** with the colour dimension sampled at 27 literals per colour knob; the tightest margin in the catalog is **WHEELS at 895 of 908, 13 free on the Setup at the RGB444 picker corner**, then STRIP at 875 / 33, then CONSOLE at 844 / 64; every entry runs the scripted gesture in a real Lua 5.4 VM with `host.errors` empty (`lua-smoke.spec.ts`, 25 tests), and both class gates — `decay-idiom.spec.ts` with `KNOWN_VIOLATIONS` empty and `touch-guard.spec.ts` with two justified exceptions — are green. NOT PROVED: any of it on a ZONA. Every claim this phase makes about a configuration was measured in the simulator, in the Lua host or read out of firmware C; the twenty-five rows of `docs/HARDWARE-AUDITION.md` are the user's and unanswered, and the bench sheet's four-faders line was never tested. |
| CONT-03 | Phase 4 (metadata gate for new entries: Phase 8; closed at Phase 9; amended Phase 10) | Complete (every entry's name, one-line description, four feel tags, Featured flag and arrival date declared in the entry file and restated in LISTING, gated field by field in both directions, with the copy counted by src/lib/catalog/copy.spec.ts rather than read). **Amended 2026-09-08 by plan 10-05 (R-10).** `RESTS_DARK_NOTE` is retired: it said a pad rests dark, and three of the four cards that carried it now show a demonstration gesture instead, so the sentence described something the visitor cannot see. `DEMO_TOUCH_NOTE` replaces it and does a job the old one did not — a card that appears to animate on its own, when the pad in truth needs a finger, makes a claim about somebody's hardware that is not true. Trackpad keeps a sentence of its own. Both pinning assertions were rewritten rather than deleted, into a pair that also asserts every `restsBlack` entry has **exactly one** of a demo path or a row in `DARK_BY_CONSTRUCTION`, in both directions; `listing.spec.ts`, `copy.spec.ts` and `frames.spec.ts` stay at 5 tests each. **Amended again 2026-09-08 by plan 10-06 (D-10): "four feel tags" becomes EXACTLY THREE — one `FOR` term and two `FEELS`, drawn from the sixteen in `src/lib/browse/facets.ts`.** The three-for-a-preset / four-for-a-hand-authored split that nine shelf entries already broke is gone; `copy.spec.ts`'s count rule is now one rule for every entry, and the vocabulary it checks against is imported from `facets.ts` rather than restated as a fifty-five-line literal. `copy.spec.ts` stays at 5 tests **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **AMENDED.** PROVED: "four feel tags" becomes **exactly three** - one `FOR` and two `FEELS` - re-cut in all **twenty-eight** source files as well as in `listing.ts`, and the re-cut is proved by reverting one entry file and watching the both-directions equality name it rather than by reading the diff. `LEGACY_TAG_MAP` declares a destination for all fifty-five old terms, twenty-two mapped and thirty-three explicitly nowhere, so **no singleton was folded into a chip its author never saw**. NOT PROVED: nothing; `copy.spec.ts` holds the count rule for every entry and imports the vocabulary rather than restating it. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: every one of the **29** entries carries exactly three tags, one `FOR` and two `FEELS`, over a vocabulary of **eight** `FOR` terms — `drums` and `clips` retired in plan 11-01 under D-01, `ninepads` re-homed to `play` and `stage` to `shortcuts` on how each card feels rather than on arithmetic — with the zero-singleton rule unweakened and every FOR term carrying two or more entries (modulation 8, show 5, sequencing 4, mixing 3, shortcuts 3, keys 2, pointing 2, play 2). NOT PROVED, and recorded where a later remover will see it: the FEELS floor. `still` sits at **exactly 6**, the floor `facets.spec.ts` asserts (`precise` moved off it to 7 when WHEELS took it in 11-15); a tenth removal in any later phase breaks a FEELS rule, and `facets.ts`'s header says so. |
| TUNE-01 | Phase 5 (knob data for Lua entries: Phase 8; re-confirmed at Phase 9); colour widened at Phase 10 plan 08 | Complete (every hand-authored entry carries three to six knobs over the vendored KnobKind vocabulary, each with an index default, each token live in at least one event and none a prefix of another, and the whole cross-product measured. No Phase 9 entry ships a sixteen-value channel knob, which is what kept the sweep affordable at twenty-seven entries; 10-08 widens the colour knob from a six-swatch palette to the whole reachable RGB444 lattice - 4,096 positions, read()/apply() as index arithmetic through the vendored quantiseColour - and amends X-05/X-06 so the colour kind is chosen by KIND ALONE. Per 10-UI-SPEC 11.2 the count is of KNOBS, not controls: the colour kind is one knob presented as three rails) **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **AMENDED BY ONE CLAUSE.** PROVED: "three to six knobs" counts **knobs, not controls** - the colour kind is one knob presented as three rails - there is **one picker per panel** however many colour knobs an entry declares, and the `colour` kind is chosen **by kind alone** (X-05/X-06 amended), never by inspecting an options array. NOT PROVED: a Lua colour knob still shows a swatch row inside the picker rather than three rails; deferred item 6. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: every entry still exposes three to six knobs in the shared widget vocabulary, and this phase moved knobs on exactly two entries — NINE PADS gained an appended `count` knob (`9` / `16`) and sits at the six-knob ceiling (11-06); POMODORO's `@MINS` gained two appended values (11-09). Every knob added or re-cut kept its arity or appended, never inserted, and the reachability sweep's compiler-route Pass A moved exactly once, 19,502 to 20,782, by NINE PADS' own cross-product doubling. NOT PROVED: nothing in TUNE-01's own subject. Recorded: `precise` is now carried by WHEELS on the strength of 1,024 distinct bend positions, which is arithmetic over the simulator's coordinates — the module has the sensor. |
| TUNE-02 | Phase 5 | Complete **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: the live cost forecast is **`cost()` only, never `fit()`** - the guard is the assertion that has held `fitState` to one call site since Phase 5, and the negative check that called the fitter from the forecast went red **there**, so which guard caught it is a measurement rather than a guess. Memoised on the index vector, so a hit publishes on a microtask and only a miss waits the existing debounce; hover **and** focus, gated twice against touch because the two gates fail differently; never animated, with `transition: none` on the ghost and on the fill while a forecast shows. NOT PROVED: the forecast is offered on **option rows only** - a rail has no candidate element to hover and no keyboard candidate at all, and five of the twelve knob kinds are rails unconditionally. Counted rather than claimed; deferred item 4. |
| TUNE-03 | Phase 5 | Complete |
| TUNE-04 | Phase 5 | Complete (guard, unreachable in practice: reachability.sweep.spec.ts costs all 32,852 reachable knob states and none crosses 908, so the fit ladder is proven against a measured over-budget reserve in ladder.spec.ts and watched in a browser on /dev/tune/, never met by a visitor) **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: knob **locks** ship - a 44px both-axes `HOLD`/`HELD` toggle per row with its state in the accessible name and a second channel that spends no colour - and both locks and `MIX TWO` are **ephemeral, with zero new reachable states**: a held rack's stamp is byte-identical to an unheld one, measured off a non-default vector, and the crossover is proved by a seeded property over all 36 entries with knobs (9,000 runs, 36,000 results). NOT PROVED, and the row's `[x]` is Phase 5's and is **not re-claimed here**: **`MIX TWO` is built, tested and NOT MOUNTED.** `MixTwo.svelte` renders its four results only when a consumer supplies `onchild`, and the only thing on the page that owns a `SimHost` is `Coverflow.svelte`, which this phase promised not to edit. Mounting it also owes `TuningRegion.svelte`'s height reservation a term that 10-UI-SPEC gives it nowhere. Deferred item 8, and it is one of the items handed to the user at the gate. |
| TUNE-05 | Phase 5; re-proved at Phase 10 plan 08 | Complete (guard, unreachable in practice: the red meter, the disabled TRY ON DEVICE with its reason, the named knob and TURN IT DOWN are all real code, reachable only by passing a real reserved to cost() - e2e/tuning.e2e.ts tests 9 and 10 are a browser doing exactly that; 10-08 re-proves it against the lattice in two passes - 19,502 + 24,576 = 44,078 states, zero over 908 - and CORRECTS A-11's claim that the picker is what keeps it unreachable: the worst-cost card is tpad at 907 of 908 and tpad has NO colour knob. The dearest colour-bearing preset is ninepads at 640 of 908, and the 268-character margin is asserted as an equality so the day it moves the suite says so) **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** **STAYS PROVEN-UNREACHABLE, WITH THE MARGIN AS A NUMBER**, and the numbers were re-observed at the gate rather than quoted: `ninepads` **640 of 908, 268 free**; `tpad` **907 of 908 and it has no colour knob**; **zero** of Pass B's 24,576 colour states over the wall; over-budget 0 and Pass B colours excluded 0, across 19,502 + 24,576 = 44,078 states in 87.0 s. The 268 is asserted as an **equality**, so the day it moves the suite says so. NOT PROVED: nothing about reachability. What remains open is 10-UI-SPEC Open item 9 - whether the unaffordable-detent guard should exist at all - which was answered "build it" precisely so the margin is a number that can move. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** STAYS PROVEN-UNREACHABLE, re-observed at the gate: over-budget **0** and Pass B colours excluded **0** across **20,782 + 24,576 = 45,358** compiler-route states (107.9 s); `ninepads` **640 of 908, 268 free** at its dearest colour and `tpad` **907 of 908, 1 free**; the Lua route at **51,888 + 135,168 = 187,056** vectors and `lua-entries` at **1,331 combinations** all inside 908. The dearest hand-authored margin is now **WHEELS at 895, 13 free** on the Setup, then STRIP at 875 / 33 — both authored this phase against the picker corner from the start. NOT PROVED: nothing about reachability. Recorded as a defect this phase found in its own documents: eight entry headers and plans were quoting the wrong corner (the declared-palette or defaults figure rather than the RGB444 picker corner the 908 gate reads), every one optimistic; the gate's own sweep of the remaining headers is in `docs/TESTING.md`. **AMENDED 2026-09-10 by plan 12-05: THE 268 MOVED, THE SUITE SAID SO, AND WHAT IT SAID IS A FINDING RATHER THAN A SHRINKING MARGIN.** NINE PADS' shipped grid moved from 3x3 to 4x4 at the user's second asking, and `reachability.sweep.spec.ts`'s pinned equality went red at **271**. The three characters are not a saving: the two-pass split rests on a separability licence — *a colour contributes to an event's length only through its three decimal literals* — that is **FALSE on this card**. At 3x3 the compiler emits the chosen colour AND a dimmed variant of it; at 4x4 it emits it once, so the colour's contribution depends on `grid`, and Pass B — which ranks colours with every other knob at its DEFAULT — now picks `102,102,102` where it used to pick `255,255,255`. Measured at all four corners: 3x3 **637 / 640**, 4x4 **627 / 627**. **The TRUE worst reachable NINE PADS state is therefore still 640 of 908, 268 free, and it is UNMOVED** — 3x3 is one click away — and `colour-picker.spec.ts:507-510` pins `NINEPADS_WORST = 640` / `NINEPADS_FREE = 268` independently and is green. The sweep's equality now pins **271**, which is what that file is able to produce, with all four corners written beside it; over-budget is **0** either way, so this is a reporting weakness on one card and not a hole in the guard. Recorded in `.planning/phases/12-touch-framework/deferred-items.md` with what a fix would cost. TUNE-05 stays complete: nothing about reachability changed. |
| TUNE-06 | Phase 5 | Complete |
| TUNE-07 | Phase 5 | Complete |
| SHARE-01 | Phase 5 (extended at Phase 10 plan 08) | Complete - 10-08 adds format w, which carries twelve raw bits of colour per colour knob beside the other knob indices, and round-trips both routes in two passes: the compiler half at 44,078 vectors and the Lua half at 234,784) **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: format **`w`** is claimed and emitted for the 25 colour-bearing Lua entries, carrying twelve raw bits per colour knob, and round-trips both routes in two passes at 44,078 and 234,784 vectors - both reproduced exactly at the gate. NOT PROVED: nothing in `w`'s own subject. `STAMP_OPTION_CEILING` stays 32 and is exempt **by format**; raising it was measured breaking exactly two assertions and both are the value pin itself. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: the stamp still round-trips both routes at the gate — the compiler route at 45,358 vectors and the Lua route at 187,056 (format `w` 51,486, format `x` 382), both reproduced exactly. NOT PROVED, and the sentence this phase owes: **every knob value list re-cut this phase keeps every existing link decodable and changes what it renders.** EUCLID `@TRAIL` (64/100/150 to 63/84/126), MORPH `@DECAY` (20/80/120 to 21/84/126) and CHORUS `bloomSpeed` (a rate 2..12 became a spread 8..24, the knob's MEANING moved) in 11-02; SHUTTLE's `rest` values (four dim track colours became four bright reverse colours) in 11-12; STRIP's crossfader palette in 11-13. A stamp encodes an index, `stamp.spec.ts` compares indices, and no test in the tree can tell a re-cut value from an unchanged one — the entry headers say so, and nothing else does. POMODORO is the one entry whose links do NOT still restore, and it is SHARE-03's exception above. |
| SHARE-02 | Phase 5 | Complete |
| SHARE-03 | Phase 5 (extended at Phase 10 plan 08) | Complete - 10-08's rule is that the old format is never removed, only stopped being emitted. Format x keeps decoding forever, proved against src/lib/share/fixtures/wild-stamps.json: 54 literals captured with the unmodified encoder and committed one commit BEFORE format w existed, so the claim is evidence rather than a round trip of the new encoder against itself) **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: **format `x` still lands `restored`**, and it is evidence rather than a tautology because the 54 literals it is checked against were captured with the **unmodified encoder and committed one commit BEFORE format `w` existed**. The rule is that an old format is never removed, only stopped being emitted; `cull` and `quadrant` still emit `x`. NOT PROVED: nothing. **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** **AMENDED.** PROVED: format `x` still lands `restored` for every captured payload but one — the fixture holds **36** literals over 18 entries since plan 11-01 deleted the 18 records naming removed entries (the row above says 54, which was true at the Phase 10 gate), every one still the byte-for-byte capture from `b3f99bb`, and `stamp.spec.ts` (9) checks them all. The ONE exception is `xn33333`, POMODORO, which lands `older` since plan 11-09 appended two `@MINS` values and moved its shape character `n` to `p`; that is the graceful landing this requirement specifies, and the four literals `wn0f2145f31`..`wn3f2145f31` prove indices 0..3 still name 15, 20, 25 and 50. Five other value lists were re-cut this phase without a resize — EUCLID `@TRAIL`, MORPH `@DECAY`, CHORUS `@SPREAD` (11-02), SHUTTLE `rest` (11-12) and STRIP's crossfader palette (11-13) — so every link to them still decodes `restored` and renders something slightly different from what its author saw; `stamp.spec.ts` compares indices and cannot see that, and only POMODORO's four captured literals guard the insertion mistake at all. NOT PROVED: nothing about the landing itself. What is recorded rather than proved is the price: the tripwire is spent per entry, and the next knob resize on any entry demotes that entry's links the same way. |
| SHARE-04 | Phase 5 | Complete with two qualifiers: routed entries only - 8 of the 16 catalog entries, because the excluded eight are not in FRONT_DOOR, have no prerendered /c/<id>/ page and therefore no <head> to carry an og:image; and a real Discord unfurl is unverifiable until the Basic Auth embargo lifts, because worker/index.js gates the whole site fail-closed and no crawler ever reaches the head the tests check **PHASE 11 GATE QUALIFIER, 2026-09-10 (plan 11-16).** PROVED: **29 OG images, 172,699 bytes**, one per entry, rebuilt from empty by `gen-og.mjs` at the gate (`BASE_OG_BYTES` was 213,919 over 36 before plan 11-01; 161,754 over 27 after it), every absolute `og:image` resolving to a real file under `build/` and every image but the three declared `restsBlack` — tpad, ghost, morph — carrying a lit LED. NOT PROVED, and measured by plan 11-11: **no test asserts what a demo path DEPICTS.** GHOST's old demo path left in place produced a plausible social preview that never pressed the erase key the whole redesign is about, and every gate stayed green. An entry that does not rest black cannot carry a demo path at all, so SHUTTLE's red stop row, STRIP's second control in motion and WHEELS' spring are invisible in their previews by construction. The Discord unfurl itself is still unverifiable behind the Basic Auth gate. |
| DEGR-01 | Phase 5 | Complete (iOS is approximated by WebKit at a phone viewport: e2e/tuning-webkit.e2e.ts's five tagged tests run in both projects and cover the front door, choosing without sideways scroll, a knob turn with both meters settling, COPY LINK's confirm state and a shared link landing with install present but disabled) |
| DEGR-02 | Phase 7 (browser-capability half delivered in Phase 4) | Complete on WebKit at a phone viewport and on the desktop project: TRY ON DEVICE and KEEP ON DEVICE present, really `disabled`, with the reason adjacent ("This browser cannot write to a ZONA. Everything else on this page works." in the honesty slot and the KEEP cell); PUT BACK absent by decision (Z-12: offering to restore nothing is worse than no control); the body names Chrome, Edge and desktop Firefox 151 and no engine (07-11's extension of the shipped degrade test with its original assertions byte-identical; 07-12 test 11 tagged and run on both projects) **Extended 2026-09-09 by plan 10-13:** `CLEAR` is the third install control held to the rule - present, really `disabled`, with `This browser cannot write to a ZONA.` inline - and its contrast with `PUT BACK`'s absence is asserted as ONE object in a dedicated `@webkit` title run on both projects, and again as source in `device-ui.spec.ts` (`PutBack.svelte` gates its whole render on `putBackState`; `Clear.svelte` contains zero `{#if}`). Planted the other way round, it goes red. **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: `CLEAR` is **present-but-disabled with its reason inline** - `This browser cannot write to a ZONA.` - the third install control held to the rule, asserted in both browser projects and again as source, and **contrasted with `PUT BACK`'s absence**: `PutBack.svelte` gates its whole render on `putBackState` and `Clear.svelte` contains zero `{#if}`. Planted the other way round it goes red, which is what makes the contrast an assertion rather than a description. NOT PROVED: nothing; Z-12's ruling that offering to restore nothing is worse than no control is a decision, not a claim about the tree. |
| IDENT-01 | Phase 4 → Phase 10 | **Complete, closed 2026-09-08** (plan 10-04, 10-UI-SPEC.md §8, D-07 confirmed). Phase 4 shipped the ground, the accent, the glyph field, the tracked uppercase and the 9x9 mark; Phase 10 adds the CRT and glitch treatment that was the identity's remaining half. Four layers, each named and each scoped: **G** the page ground (`body::before`, a 3px halftone and a radial-gradient vignette), **S** scanlines and an `feTurbulence` noise tile on pad frames, **R** one roll bar for the whole page, **T** a 180 ms `step-end` tear on `navigator.serial`'s `connect` event and on no other. The palette is unchanged — nine tokens, three hexes, and the reference implementation's cold blue became the accent at its alpha rather than a fourth colour. **The scoping is asserted rather than promised:** `src/lib/ui/aesthetic.spec.ts`'s seven source scans hold that no CRT layer is set on `.stage` or between `.stage` and a `.slot`, that no text-bearing element is a descendant of a CRT container, that no CRT selector names a `canvas`, that the noise tile declares no `fill`, that every `--crt` colour lives in the one file `identity.spec.ts` reads, and that `.crt-band`'s five geometry literals are string-equal to `Coverflow.svelte`'s `.band` **Amended 2026-09-09 by plan 10-13.1 (D-15, D-16, D-17, A-37 to A-44):** the identity gains its second register. The front door keeps the CRT exactly as wave 4 built it; browse, the panels, the tuning region and the whole device flow become the INSTRUMENT register of D-15's three references - a 48px registration lattice of gradient plus marks with one distinguished cross, pill-outlined controls, a monospace metadata block and rows that align without rules. **No colour is added:** nine tokens, three hexes, the accent reserved list still eight, the lattice in `--color-line-soft`. `src/lib/ui/instrument.spec.ts`'s five scans hold it, and its walk is DERIVED FROM THE DIRECTORY rather than hand-declared - the repository's first. **The register line is a line of AUTHORSHIP, not of DOM containment, and §19.1g's statement of it is false against the shipped tree:** `FrontDoor.svelte` mounts `Coverflow.svelte`, so the panels and the whole device flow are DOM descendants of `.front-door` and always have been. What is asserted is that every moving or texturing CRT layer is SCOPED to `.front-door` and no instrument rule is AUTHORED inside it. **Layer G is the one declared exception**, on every route, reclassified as reference B's image-making device rather than scoped off - D-16 is wrong that the ground is already the hero shell's, and scan 1 says so by name — which is byte-untouched **Amended 2026-09-09 by plan 10-13.2 (D-22, A-55 to A-57, A-60):** the register the wave before added is corrected where it was destructive rather than decorative. §19.1a fixed the marks' geometry and never fixed WHERE THEY MAY LAND, and both roots are ancestors of the reading column - measured on the built site at 1280x900, **418 text-bearing elements under a lattice root with no opaque ancestor**, two crossings inside the `HANGAR` wordmark's box and thirteen inside the browse headline's, with crosses on `Available after a try-on.`, on `CLEAR` and on every card description. **The lattice is now a GROUND: it paints where nothing is standing on it.** One declaration, `.lattice > :where(*) { background-color: var(--color-ground) }`, with `:where()` load-bearing so a Svelte-scoped rule at 0,2,0 wins and no component has to be listed, and declared ABOVE `.pill` because at equal specificity source order decides. Three declared exceptions and no fourth - `/browse/`'s `.grid` stays transparent so the field keeps painting between the cards, `CatalogCard`'s `.card` and `BrowseGrid`'s `.empty` take the ground, the last found by a DOM walk rather than by reading a template. **Knocking the field back with an opacity was rejected on principle**: it changes the symptom's amplitude and not its cause. Layer G's declaration and route scope are untouched; its halftone no longer shows under those blocks, at a computed cost of about one part in 255. After: **0, 0 and 0 unoccluded on all three surfaces, with the card gutters still bare lattice**, proved in a browser by `e2e/aesthetic.e2e.ts` test 5 against a control arm, because a source scan is structurally blind to paint order - which is how the lattice shipped painting nothing with every scan green. The palette is still nine tokens and three hexes **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: nine tokens, three hexes and no fourth hue survived a whole type swap, two texture members outside the ladder (`--crt-scanline`, `--crt-noise`) and four CRT layers, and `--font-display` is asserted equal to EVERY display `@font-face` family in `src/app.css` rather than to the first one found. `identity.spec.ts` 7, `aesthetic.spec.ts` 8, `instrument.spec.ts` 6, all green at the gate. NOT PROVED: **the Grifter licence question is the user's and is unresolved** - the files declare `PERSONAL USE` in their OpenType name table, the family ships behind one swappable token pending the user's confirmation, and the blocker is redistribution rather than use, because `postbuild.mjs:82-89` publishes `git archive HEAD` at an unauthenticated URL. It is handed over as Open item 2 with 10-02's costed reversal beside it. Also not proved, and not provable here: whether Grifter reads well beside Inter, which is the user's eye and is section C.3 of the checkpoint. |
| IDENT-02 | Phase 4 → Phase 10 | **Complete, closed 2026-09-08** (plan 10-04, 10-UI-SPEC.md §8.6 and §8.7). The motion is the rack of running machines Phase 4 shipped plus one roll bar and one tear; `prefers-reduced-motion` is honoured **and asserted at the compositor in both browser projects**, which is new. `e2e/browse.e2e.ts:140-148` and `e2e/first-experience.e2e.ts:87` read the 9x9 canvas backing store, and a CSS overlay painted above a canvas never touches those bytes — a sweeping scanline bar would have left every shipped assertion green. `e2e/aesthetic.e2e.ts` reads `getComputedStyle(el).animationName` instead, and it means something only because the tear's animation is declared **at rest and paused**: it reads `crt-tear` normally and `none` under the preference, rather than `none` in both states. **Two vacuity holes are opened explicitly first** — `SCREEN: TEXTURED` selected, because the default under reduced motion is `FLAT` and `FLAT` mounts no roll bar; and `navigator.hardwareConcurrency` forced to 8, because a four-core runner mounts none either. The preference is one of THREE switches, all three asserted: the OS preference reaches the two layers that move, the visible `SCREEN` control in the footer reaches all four (the two static ones have no OS setting at all, which is the whole argument for it), and `hardwareConcurrency <= 4` drops the roll bar, asserted in its own right at a forced 4. **Extended 2026-09-08 by plan 10-05:** the preference now also reaches a fourth thing — a demonstration gesture is uninvited motion, so under reduced motion a demo card resets, replays its path to the end **once**, and freezes, rather than looping. `SimHost.stillFrame()`'s second branch, asserted in node for idempotence and in the browser as a **universal**: `e2e/browse.e2e.ts`'s reduced-motion walk lost its four-entry `restsBlack` exemption and now **Amended 2026-09-09 by plan 10-13.1 (A-39, A-40):** the instrument register adds NOTHING that moves. The lattice and the halftone are static, so `prefers-reduced-motion` has nothing new to turn off and the block gains no line; scan 3 asserts the lattice declares no `animation`, no `transition` and no `transform`, and `e2e/aesthetic.e2e.ts`'s four compositor assertions were re-run UNCHANGED IN WORDING against a fresh build - 8 passed in both engines. **§19.1e's second halftone density was measured and REFUSED**: chromium 16.70 ms in both arms (delta 0.00), webkit-phone 94.00 ms against 82.00 ms (delta 12.00) at p95, against a declared threshold of 2 ms. The halftone stays at one 3px pitch, and `DENSITIES` is a named constant scan 4 asserts, so the fallback is a state rather than a silence requires a lit still frame of every card that has an LED layer at all **Amended 2026-09-09 by plan 10-13.2 (A-58):** still nothing that moves. The unlit-cell wash added to `PadFrame.svelte`'s Layer 1 is a static `background-color`, and `aesthetic.spec.ts` scan 8 asserts `.dots` declares no `animation`, no `transition`, no `transform` and no `filter`, so the `prefers-reduced-motion` block gains no line for the second wave running. The four compositor assertions in `e2e/aesthetic.e2e.ts` were re-run UNCHANGED against a fresh build and are green in both projects **PHASE 10 GATE QUALIFIER, 2026-09-09 (plan 10-14).** PROVED: `prefers-reduced-motion` asserted at the **compositor level** in both browser projects, with both vacuity holes opened first - `SCREEN: TEXTURED` selected, because the default under the preference is `FLAT` and `FLAT` mounts no roll bar, and `navigator.hardwareConcurrency` forced to 8, because a four-core runner mounts none either. A demo card replays its path once and freezes. `SCREEN: FLAT` reaches the two static layers no motion preference can, which is the whole argument for a visible control. Four compositor titles, both projects, re-run unchanged against a fresh build at the gate. NOT PROVED: whether the CRT reads as screen texture behind the interface or as dirt on the words - the user's eye, checkpoint section C.1 - and whether the instrument register reads as an instrument rather than as decoration, checkpoint section C.2, which 10-13.1's five source scans can prove the shape of and cannot prove the effect of. |

**Coverage:**
- v1 requirements: 50 total
- Mapped to phases: 50 ✓
- Unmapped: 0

Every v1 requirement maps to exactly one phase. Per-phase counts: Phase 1 (2), Phase 2 (1),
Phase 3 (3), Phase 4 (13), Phase 5 (12), Phase 6 (8), Phase 7 (10), Phase 8 (1).

---
*Requirements defined: 2026-09-02*
*Last updated: 2026-09-09 after plan 10-13.2 (D-22, A-55 to A-60: the aesthetic pass corrected where it was destructive to the UX. IDENT-01's lattice becomes a GROUND - one `:where()` rule, three declared exceptions, no new node, 418 unoccluded text-bearing elements down to 0, and Layer G's cost computed rather than estimated. IDENT-02 gains nothing that moves, again. CAT-03's one permanently dark card is drawn as a pad with nothing lit rather than as a card that failed to load, with every honesty gate byte-untouched and re-run. No tick-mark moved - all three were already complete. Both fixes are proved in a browser, because a source scan cannot see paint order). Previously: after plan 10-13.1 (D-15 to D-18, A-37 to A-44: the phase's aesthetic pass. IDENT-01 gains the instrument register - one shared pill, a gradient registration lattice on two roots, the index form and the monospace metadata block - with the register line recorded as a line of AUTHORSHIP because the shipped tree makes the DOM reading false, and D-16's claim about the ground recorded as wrong. IDENT-02 gains nothing that moves and the second halftone density was measured at 12 ms and refused. SAFE-02's three-tier ladder survived the shared shape with both Quiet controls byte-untouched. CAT-03 gains the index furniture and the seventh mono use. No tick-mark moved - all four were already complete. `src/lib/ui/instrument.spec.ts` is the repository's first directory-derived component walk). Previously: after plan 10-13 (D-04, D-19, D-20, D-21, A-45 to A-52: `CLEAR` shipped as a Quiet-tier control with no confirmation and no tier of its own; SAFE-02 amended to record that the sequence is a caption, an order and an enablement and that the two weights are now held by a site-wide accent count; SAFE-03 and DEGR-02 extended to the fourth write click. No tick-mark moved - all three were already complete, and what changed is what each row can claim). Previously: after plan 10-07 (D-11, A-19, A-20, A-22, G-10: the Newest sort removed and CAT-02 amended to two orders by name; the two facet rows, the `?for=`/`?feels=` migration and the front door's `FOR` link row shipped, with CAT-03 amended and CAT-04 amended and deliberately left unchecked for the second time, because its subject is the catalog data file rather than the browse vocabulary). Previously: after plan 10-06 (D-10: the tag vocabulary re-cut from fifty-five terms to sixteen in two facets, exactly three per entry, zero singletons; CAT-03 and CAT-04 amended by name and CAT-04 deliberately left unchecked, CONT-03's "four feel tags" amended to three). Previously: after plan 10-05 (D-09 and R-10: PREV-01 extended to the three dark entries a demonstration finger can reach, IDENT-02 extended to the reduced-motion branch that replays a gesture once and freezes, and CONT-03 amended by the retirement of RESTS_DARK_NOTE. One card - tpad - cannot be lit by any gesture, because its configuration enables no LED layer at all; it is named in DARK_BY_CONSTRUCTION with the measurement, and lighting it would mean changing the configuration, which 10-UI-SPEC 9.3 rejects by name. The hardware halves of SAFE-02 to SAFE-06 and Phase 6 rows A to F remain presented and unanswered - no agent has written a byte to a real ZONA)*

*Last updated: 2026-09-09 at the **Phase 10 gate** (plan 10-14). Twenty rows gained a
`PHASE 10 GATE QUALIFIER` saying what was proved **and** what was not: IDENT-01, IDENT-02, SAFE-01,
SAFE-02, SAFE-03, SAFE-05, SAFE-07, PREV-03, CONN-03, CAT-02, CAT-03, CAT-04, CONT-03, TUNE-01,
TUNE-02, TUNE-04, TUNE-05, SHARE-01, SHARE-03, DEGR-02. **Four carry an explicit unresolved half** -
Grifter's licence (IDENT-01, Open item 2), row C's clear paragraph and SAFE-04's still-wire-unproven
key (SAFE-03), the form of SAFE-01's guarantee (Open item 1), and MIX TWO built-but-not-mounted
(TUNE-04). **No checkbox was ticked by this phase**, and that is deliberate: every one of the twenty
was closed by an earlier phase and this phase extends, amends or records-as-untouched. **CAT-04 stays
`Pending` for the third time** - its subject is the shape of the catalog data file and nothing here
touched it - and **TUNE-04 keeps Phase 5's `[x]` without this phase re-claiming it**, because MIX TWO
is not mounted. **SAFE-05 is the qualifier that says less than the approved spec promised**: §4 read
"Extended - CLEAR's confirmation names what is removed", A-45 removed the confirmation, and the honest
record is that SAFE-05 stands exactly as Phase 7 closed it. The hardware halves of SAFE-02 to SAFE-06,
Phase 6 rows A to F, `docs/INSTALL-RUNBOOK.md` rows A to G including **row C's clear half**, and all
thirty-two `docs/HARDWARE-AUDITION.md` rows remain presented and unanswered - **no agent has written a
byte to a real ZONA, and no agent has deployed**.*
