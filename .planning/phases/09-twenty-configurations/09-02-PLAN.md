---
phase: 09-twenty-configurations
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/browse/filter.spec.ts
  - src/lib/browse/sort.spec.ts
  - src/lib/catalog/copy.spec.ts
  - src/lib/sim/lua-smoke.spec.ts
  - src/lib/og/build.spec.ts
  - e2e/artifacts.e2e.ts
autonomous: true
requirements: [CONT-02, CONT-03]

must_haves:
  truths:
    - "Every browse assertion that hard-codes sixteen, forty-one, thirty-two or nine is either derived from the shipped data or recorded in one named block a wave updates deliberately, so a plan that adds three configurations does not spend its context rewriting arithmetic."
    - "The NEWEST order is asserted as blocks of equal dates in descending order rather than as two blocks of seven and nine, so a third addedAt date is a structural fact instead of a red test."
    - "Every user-facing string a configuration carries is counted by a script: no exclamation mark, no emoji, no ASCII apostrophe between letters in HANGAR-authored copy, no hyphen standing in for a dash, one line, at most a hundred and ten characters, and no two descriptions the same."
    - "A hand-authored configuration whose whole output is keyboard or mouse is not reported as a silent instrument: the execution gate asks whether the scripted gesture produced any output, and asserts both kinds really occur in the catalog."
    - "Two search anchors the shipped suites depend on - a query for aurora leaving one card and a query for ghost leaving one - are protected by a gate rather than by hoping nobody reuses the word."
  artifacts:
    - path: "src/lib/catalog/copy.spec.ts"
      provides: "The CONT-03 copy gate over every catalog string, counted rather than read. 5 tests, fixed count."
    - path: "src/lib/browse/filter.spec.ts"
      provides: "The same six tests, with the tag census and the chip row moved into one RECORDED block and every per-tag expectation derived from LISTING"
    - path: "src/lib/browse/sort.spec.ts"
      provides: "The same six tests, with counts derived from LISTING.length and NEWEST asserted as date blocks"
  key_links:
    - from: "src/lib/catalog/copy.spec.ts"
      to: "src/lib/catalog/listing.ts"
      via: "LISTING and RESTS_DARK_NOTE - the copy a visitor actually reads is the copy that is counted"
      pattern: "RESTS_DARK_NOTE"
    - from: "src/lib/sim/lua-smoke.spec.ts"
      to: "src/lib/sim/lua-host.ts"
      via: "host.hid alongside host.midi, so a keyboard configuration is output rather than silence"
      pattern: "\\.hid"
---

<objective>
Make the gates survive twenty new entries, and give the twenty a copy gate to be written against.

Wave 2. Blocked on 09-01: this plan quotes its seven-name block and runs against the tree it left -
so its quick-run assertions are against `PREV_FILES` and `PREV_TESTS`, never against the frozen
`BASE_FILES` and `BASE_TESTS`.

Three kinds of work, and they belong together because they are all "what breaks the moment the first
new configuration lands":

**The literals.** `filter.spec.ts` and `sort.spec.ts` hard-code sixteen entries, forty-one tags,
thirty-two singletons, a nine-chip row with its counts, a six-name disabled row, "eight are featured",
a two-block NEWEST structure and a written-out sixteen-name sequence. Left alone, every entry wave
would spend a third of its context on arithmetic. Some of it should be derived; some of it is a
deliberately reviewable record and should stay a literal, in one place, updated on purpose.

**The copy gate.** Twenty new names, twenty descriptions and eighty tags are about to be written.
`catalog.spec.ts` checks length and shape; nothing counts apostrophes, exclamation marks, emoji or a
hyphen doing a dash's job, and the "no two descriptions are the same" rule lives only in a 112-second
Playwright run. CLAUDE.md and `src/lib/device/install-copy.ts:44-46` state the house rule; this makes
it a script.

**The output gate.** `lua-smoke.spec.ts` test 2 fails an entry that "produced no MIDI at all" -
*"a silent instrument is exactly what the gate exists to notice"*. Five configurations on this
phase's slate send keystrokes and nothing else. A keyboard macro pad is not a silent instrument; it
is a keyboard. The gate has to ask the question it means.

**What this plan deliberately does NOT do.** It does not author a configuration, does not touch
`index.ts`, `listing.ts`, `front-door.ts` or `frames.json`, and does not change what any spec proves -
only how it says it. Every one of the six files it touches keeps its exact test count.

Purpose: CONT-02's growth headroom, CONT-03's copy rule made mechanical.
Output: one new spec (5 tests), five edited specs and fixtures, zero behaviour changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-twenty-configurations/09-CONTEXT.md
@.planning/phases/09-twenty-configurations/09-VALIDATION.md
@.planning/phases/09-twenty-configurations/09-01-SUMMARY.md
@.planning/research/CATALOG-SURFACE.md
@src/lib/browse/filter.spec.ts
@src/lib/browse/sort.spec.ts
@src/lib/catalog/listing.ts
@src/lib/sim/lua-smoke.spec.ts

<standing_rules>
- **Never edit anything under `src/vendor/`.**
- **Never touch a sibling repository.**
- **No agent connects to or writes to a device.**
- No Claude or Anthropic attribution in any file, comment or commit message, and no co-author
  trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults per `.prettierrc`. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`, the message file in the scratchpad.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- e2e at `--workers 3`; remove `test-results/` by hand.
- The project spends no money.
</standing_rules>

<interfaces>
The literals this plan is about, at the lines they sit on today. Confirm each with `grep -n` before
editing; a line number that has moved means somebody else edited the file and this plan should say so.

**Nineteen rows, seventeen of them in the two browse specs** and the last two floors 09-10 raises.
Three of `sort.spec.ts`'s
`16` literals are invisible to the obvious grep: Prettier wrapped `.toHaveLength(\n  16,\n)` across
three lines, so the number sits alone on `:110`, `:146` and `:167` with no `toHaveLength(16)` text
anywhere near it. They are enumerated here because a grep for `toHaveLength(16)` finds three of the
six and leaves three to go red at nineteen entries.

| Where | Today | Treatment |
|---|---|---|
| `filter.spec.ts:107` | `expect(LISTING.length).toBe(16)` | floor: `toBeGreaterThanOrEqual(RECORDED.entries)` |
| `filter.spec.ts:140` | `drums` -> `["ninepads","euclid"]` | derived from `LISTING` |
| `filter.spec.ts:179,184` | `generative` 3, `gestural` 3, both -> `["ghost"]` | derived, plus the shrinking-intersection property kept |
| `filter.spec.ts:232` | `41 distinct tags` | RECORDED block |
| `filter.spec.ts:233-238` | `NINE_CHIPS` + counts `[4,3,3,3,2,2,2,2,2]` | RECORDED block |
| `filter.spec.ts:247` | `32 singletons` | RECORDED block |
| `filter.spec.ts` `withDrums` six names | six chip names | derived, both directions already asserted below it |
| `sort.spec.ts:59, 83, 112` | `16`, on one line each | `LISTING.length` |
| `sort.spec.ts:109-110` | `.toHaveLength(` then a bare `16,` - "the shipped comparator returned all sixteen", FEATURED | `LISTING.length` |
| `sort.spec.ts:145-146` | the same wrapped shape, NEWEST | `LISTING.length` |
| `sort.spec.ts:166-167` | the same wrapped shape, NAME | `LISTING.length` |
| `sort.spec.ts:102` | `3 * 16 * 15` | `3 * n * (n - 1)` |
| `sort.spec.ts:127` | `eight are featured` | RECORDED block |
| `sort.spec.ts:133,136` | `names.slice(0, 8)` and `names.slice(8)` - the featured/plain split, hard-coded at 8 | derived from `LISTING.filter(e => e.featured).length` |
| `sort.spec.ts:141` | the test title *"NEWEST puts the seven newest before the nine older"* | renamed; the sizes leave the title with the assertion |
| `sort.spec.ts:155-160` | seven `2026-09-04` then nine `2026-09-02` | date blocks, descending |
| `sort.spec.ts` NAME sequence | sixteen names written out | independent comparator + three witness pairs |
| `og/build.spec.ts:215` | `>= 16` | left alone here; 09-10 raises it |
| `e2e/artifacts.e2e.ts:69` | `>= 16` | left alone here; 09-10 raises it |

`src/lib/browse/filter.ts:121-125` derives the standing chip row: every tag carried by two or more
entries, count descending then name ascending. That is the rule; the nine names are today's data.

`src/lib/catalog/listing.ts` exports `LISTING`, `ROUTED`, `RESTS_DARK_NOTE`, `listingIndex`,
`listingById`. `RESTS_DARK_NOTE` is the one sentence a resting-black card carries and
`listing.spec.ts:243-250` already asserts it verbatim for every `restsBlack` entry.

`src/lib/sim/lua-host.ts` (after 09-01):

```ts
get midi(): readonly HostMidi[];
get hid(): readonly HostHid[];   // { call: "gmms" | "gmbs" | "gks"; args: number[] }
```

The two vocabulary facts the copy gate needs, from `src/lib/browse/typographic.ts:3-17` and
`catalog.spec.ts:113-119`: a **vendored** preset sentence keeps its ASCII apostrophe and must stay
byte-equal to the shelf, and `typographic.ts` converts an ASCII apostrophe **between two letters** to
U+2019 at render time and changes nothing else. So the gate's apostrophe rule applies to
HANGAR-authored copy only, and the preset sentences are exempt **by source kind**, never by a name
list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 9-02-01: The browse literals - derived where they are arithmetic, recorded where they are a review</name>
  <files>src/lib/browse/filter.spec.ts, src/lib/browse/sort.spec.ts</files>

  <read_first>
    - `src/lib/browse/filter.spec.ts` in full, and `src/lib/browse/filter.ts:110-140`
    - `src/lib/browse/sort.spec.ts` in full, and `src/lib/browse/sort.ts`
    - `.planning/research/CATALOG-SURFACE.md` section 3, the "Counts and literals that DO move" table
  </read_first>

  <action>
The rule that decides each case, and it is worth writing at the top of both files:

> A number that is **arithmetic over the shipped data** is derived. A number that is **a review of
> the shipped data** stays a literal, in one named block, so that changing it is a decision somebody
> made rather than a test somebody silenced.

Both files keep **exactly six tests**. Nothing is added and nothing is removed.

**1. `src/lib/browse/filter.spec.ts`.**

Add one block near the top, above `NINE_CHIPS`, and move every census number into it:

```ts
/**
 * TODAY'S TAG CENSUS, RECORDED ON PURPOSE.
 *
 * Derived facts - a chip is a tag two or more entries carry, count descending
 * then name ascending (filter.ts:121-125) - are asserted as rules below and
 * need no maintenance. These four are a REVIEW: they say what the vocabulary
 * currently looks like, so a wave that adds configurations sees the row it
 * moved and decides whether it likes it. A wave updates this block; it never
 * deletes an assertion against it.
 *
 * Re-recorded by: 08-06 (sixteen entries), then every entry wave of phase 09.
 *
 * This block has a reader outside the repository's source: 05.1-UI-SPEC.md,
 * "The tag chips", quotes the row and its counts verbatim, and the header
 * comment above NINE_CHIPS already says so. A wave that moves the row updates
 * that document in the same commit - 09-03 is the first, and it carries the
 * instruction.
 */
const RECORDED = {
  entries: 16,
  tags: 41,
  singletons: 32,
  chips: ["playable", "generative", "gestural", "hypnotic", "ambient",
          "colour", "drums", "expressive", "readable"] as const,
  chipCounts: [4, 3, 3, 3, 2, 2, 2, 2, 2] as const,
} as const;
```

Then:

- Test 2's `expect(LISTING.length).toBe(16)` becomes
  `expect(LISTING.length, "the listing was actually read").toBeGreaterThanOrEqual(RECORDED.entries)`.
  A floor, not an equality: the assertion's job here is non-vacuity.
- The three per-tag expectations become derived. Compute the expected id list from `LISTING` with the
  same predicate the module is being asked about, assert the module agrees, **and** assert the
  derived list is non-trivial so a broken predicate cannot pass by returning everything:

  ```ts
  const carrying = (tag: string) =>
    LISTING.filter((e) => e.tags.includes(tag)).map((e) => e.id);
  expect(carrying("drums").length, "drums is carried by more than one entry")
    .toBeGreaterThan(1);
  expect(ids(filterListing(LISTING, "", ["drums"])), "the drums chip")
    .toEqual(carrying("drums"));
  ```

  Do the same for `generative` and `gestural`, and keep the intersection property exactly as it is -
  `both.length` strictly less than either - because that is the assertion that catches a union.
  The intersection may become empty as the catalog grows; assert
  `both.length <= Math.min(...)` and, separately, that **some** pair of chips has a non-empty
  intersection, so the property is still observed on real data.
- The typed-word test (`"nine drums"` -> `["ninepads"]`) keeps its literal expectation only if it is
  still exactly one entry; make it derived the same way, requiring `> 0` and `< carrying("drums").length`.
- Test 5's three census numbers read from `RECORDED`. Keep both rule assertions verbatim - every chip
  carried twice or more, every excluded tag exactly once - and add one:
  `expect(chips.length + excluded.length).toBe(known.length)`, which is what makes the two counts a
  partition rather than two independent numbers.
- Test 6's six-name `withDrums` list becomes derived: compute, for each standing chip, whether
  `filterListing(LISTING, "", ["drums", tag])` is empty, and assert `disabledTags(...)` equals exactly
  that set in the same order the module returns. The both-directions loop underneath it already
  proves each answer; assert additionally that the derived disabled set is **neither empty nor every
  chip**, so the test still says something.

**2. `src/lib/browse/sort.spec.ts`.**

Add the same shape of block:

```ts
/** Recorded on purpose; see filter.spec.ts's RECORDED. */
const RECORDED = { entries: 16, featured: 8 } as const;
```

- Every `16` becomes `LISTING.length`; every `toHaveLength(16)` becomes
  `toHaveLength(LISTING.length)`, with one `toBeGreaterThanOrEqual(RECORDED.entries)` at the top of
  test 1 as the non-vacuity floor. **There are six `16`s, not three.** Three sit on their own line
  (`:59`, `:83`, `:112`) and three were wrapped by Prettier onto a line of their own inside a
  multi-line `expect(...).toHaveLength(\n  16,\n)` - `:110` in FEATURED, `:146` in NEWEST, `:167` in
  NAME. Work from the interfaces table, not from a grep for `toHaveLength(16)`, which finds half of
  them.
- `expect(compared).toBe(3 * 16 * 15)` becomes
  `toBe(BROWSE_SORTS.length * LISTING.length * (LISTING.length - 1))`.
- "eight are featured" reads `RECORDED.featured`, and gains a rule beside it:
  `expect(sorted.filter(e => e.featured).length).toBe(LISTING.filter(e => e.featured).length)`, so the
  recorded number is a review and the equality is the test.
- **The featured/plain split is derived, not the literal 8.** `:133` and `:136` slice the name
  sequence at a hard-coded 8 - `names.slice(0, 8)` and `names.slice(8)` - so the two "block is in name
  order" assertions go red the moment 09-03 makes `RECORDED.featured` nine. They are the two literals
  most easily missed, because the number they carry is the featured count and not the entry count.
  Derive the boundary once, above them, and slice with it:

  ```ts
  const f = LISTING.filter((e) => e.featured).length;
  expect(names.slice(0, f), "the featured block is in name order")
    .toEqual([...names.slice(0, f)].sort(nameOrderOnNames));
  expect(names.slice(f), "the plain block is in name order")
    .toEqual([...names.slice(f)].sort(nameOrderOnNames));
  ```

  `f` is the same quantity the `lastFeatured` / `firstPlain` assertion above already pins, so this is
  arithmetic over the shipped data and belongs on the derived side of the rule. `RECORDED.featured`
  stays as the review beside it.
- **NEWEST is restructured, title included.** Replace the two hard-coded blocks with the structural
  property, and write the reason in a comment: a phase that adds configurations adds a third date, and
  an assertion shaped as "seven then nine" is red for a reason that is not a fault. **Rename the test
  too.** `:141` reads *"NEWEST puts the seven newest before the nine older, and agrees with the
  catalog"*, which is a claim about two block sizes and is simply false at thirty-six entries and
  three dates. A title that survives the phase and still says what the test does:

  ```ts
  it("NEWEST orders the dates newest first, name ascending inside each block, and agrees with the catalog", () => {
  ```

  A stale title is worse than a stale literal: nothing goes red, and the runner prints a sentence that
  is not true.

  ```ts
  const dates = sorted.map((e) => e.addedAt);
  // Descending, and constant within each block.
  const distinct = [...new Set(dates)];
  expect(distinct.length, "there is more than one date to order")
    .toBeGreaterThan(1);
  expect(distinct, "the blocks run newest first")
    .toEqual([...distinct].sort().reverse());
  expect(dates, "the sequence is exactly its blocks, concatenated").toEqual(
    distinct.flatMap((d) => LISTING.filter((e) => e.addedAt === d).map(() => d)),
  );
  // And within a block, name ascending - the tie-break the catalog uses.
  for (const d of distinct) {
    const names = sorted.filter((e) => e.addedAt === d).map((e) => e.name);
    expect(names, `${d}: the block is in name order`)
      .toEqual([...names].sort(nameOrderOnNames));
  }
  ```

- **The NAME sequence.** Delete the sixteen written-out names and replace them with two things. First
  the property, against a comparator written **independently in the spec** rather than imported - that
  is what stops it being a tautology:

  ```ts
  const byCodePoint = (a: string, b: string) => (a < b ? -1 : a > b ? 1 : 0);
  expect(sorted.map((e) => e.name))
    .toEqual([...LISTING].map((e) => e.name).sort(byCodePoint));
  ```

  Then the reviewable witness that survives any catalog size - the adjacent pairs only a code-point
  order produces, asserted by position rather than by a full list:

  ```ts
  const seq = sorted.map((e) => e.name);
  const before = (a: string, b: string) =>
    expect(seq.indexOf(a), `${a} precedes ${b} by code point`)
      .toBeLessThan(seq.indexOf(b));
  before("ARC", "Aurora");      // R (82) is below u (117)
  before("SONAR", "Starfield"); // the same fact, on a different pair
  ```

  Keep the existing synthetic `apple` / `Zebra` test and the `localeCompare` / `Intl` source scan
  exactly as they are: they are the mechanical half of the rule and they do not move.

**Observe the negative check, twice.** With the edits in place:

1. Change one entry's `tags` in `listing.ts` to add a tag a second entry already carries, run
   `filter.spec.ts`, and confirm **test 5** goes red on `RECORDED.tags` or `RECORDED.chips` - the
   recorded row is still a tripwire. Restore with `git checkout -- src/lib/catalog/listing.ts`.
2. Change one entry's `addedAt` in `listing.ts` to a third date, run `sort.spec.ts`, and confirm the
   NEWEST test **passes** - which is the point of the restructure - while `listing.spec.ts` goes red
   because the listing no longer agrees with the catalog. Restore.

Record both exit codes, the failing test names and the reasoning in the SUMMARY.

Run `npm run format`, `npm run lint`, `npm run check`.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/browse/filter.spec.ts src/lib/browse/sort.spec.ts 2>&1 | node scripts/check-counts.mjs 2 12 &amp;&amp; npm run test:quick 2>&1 | node scripts/check-counts.mjs $PREV_FILES $PREV_TESTS &amp;&amp; npm run check 2>&1 | grep -Ei "error|warning" &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/browse/filter.spec.ts` reports **6 passed** and `src/lib/browse/sort.spec.ts` reports **6 passed**. Neither count moved.
    - `grep -c "  it(" src/lib/browse/filter.spec.ts` prints `6` and the same for `sort.spec.ts`.
    - Each file has exactly one `RECORDED` block: `grep -c "const RECORDED" src/lib/browse/filter.spec.ts` prints `1`, and the same for `sort.spec.ts`.
    - No bare sixteen survives as an entry count anywhere in either file, wrapped or not: `grep -nE "\b16\b" src/lib/browse/filter.spec.ts src/lib/browse/sort.spec.ts` prints only lines inside the two `RECORDED` blocks, and every such line is quoted in the SUMMARY with its line number. A word-boundary grep is what this criterion requires, because `grep "toHaveLength(16)"` misses the three literals Prettier put on their own lines.
    - No hard-coded featured/plain split survives: `grep -n "slice(0, 8)\|slice(8)" src/lib/browse/sort.spec.ts` prints nothing, and `grep -q "LISTING.filter((e) => e.featured).length" src/lib/browse/sort.spec.ts` exits 0.
    - The NEWEST title no longer names two block sizes: `grep -c "seven newest before the nine older" src/lib/browse/sort.spec.ts` prints `0`.
    - The written-out name sequence is gone: `grep -c "Four faders" src/lib/browse/sort.spec.ts` prints `0`.
    - The independence of the NAME comparison survives: `grep -q "byCodePoint" src/lib/browse/sort.spec.ts` exits 0 and the `localeCompare` / `Intl` source scan is unedited.
    - `npm run test:quick 2>&1 | node scripts/check-counts.mjs $PREV_FILES $PREV_TESTS` exits 0 - this task moves no count. `PREV_FILES` and `PREV_TESTS` are 09-01's, the tree as it left it; `BASE_FILES` and `BASE_TESTS` are the clean tree and are never the thing a wave asserts against.
    - Both negative checks were observed with the stated outcomes and reverted; `git diff --quiet -- src/lib/catalog/listing.ts` exits 0 afterwards.
    - `npm run check 2>&1 | grep -Ei "error|warning"` prints `0 errors, 0 warnings`; `npm run lint` exits 0.
  </acceptance_criteria>

  <done>Adding a configuration moves one recorded block and nothing else: every count that is arithmetic is derived, the NEWEST order is asserted as date blocks, and the NAME order is a property with two reviewable witnesses instead of a list that grows.</done>
</task>

<task type="auto">
  <name>Task 9-02-02: The copy gate - every catalog string counted, never read</name>
  <files>src/lib/catalog/copy.spec.ts</files>

  <read_first>
    - `src/lib/device/install-copy.spec.ts:300-330` and `src/lib/device/session-copy.spec.ts:370-395`
      - the house shape for a counted copy gate, including the emoji property scan
    - `src/lib/browse/typographic.ts` in full - what is converted at render, and what is not
    - `src/lib/catalog/catalog.spec.ts:40-70` - what is already asserted, so this file does not
      duplicate it
    - `e2e/browse.e2e.ts:255-270` - the description-uniqueness assertion this file mirrors into the
      quick run
    - `.planning/research/CATALOG-SURFACE.md` section 5d, 5e and 5f - the shipped voice and vocabulary
  </read_first>

  <action>
Create `src/lib/catalog/copy.spec.ts` - **exactly 5 tests, fixed count**, every one looping over the
entries internally and naming the entry and the offending string. It imports `CATALOG` and `LISTING`
and nothing that costs a chunk; it is a spec, so importing the catalog is free.

Write the header in the file's own voice, and make it say the two things a later reader needs:

- **Why a second copy spec exists beside `catalog.spec.ts`.** That file asserts *shape* - a
  description is one line and at most a hundred and ten characters. This one asserts *typography and
  vocabulary*, which is a different kind of claim and is about to be made twenty times over. Twenty
  new descriptions and eighty new tags is exactly the volume at which reading them is not checking
  them.
- **Why the apostrophe rule is by source kind, never by a name list.** A `preset` entry's `name` and
  `description` are read off the vendored shelf and are asserted byte-equal to it in
  `catalog.spec.ts:113-119`; `src/lib/browse/typographic.ts` converts an ASCII apostrophe between two
  letters to U+2019 **at render time**. So Radar's *"your finger's position"* is correct in the data
  and correct on the screen, and "fixing" it in the catalog would break the shelf equality. Every
  string HANGAR authors - every hand-authored entry's description, every `quiet` line, every tag - is
  held to the typographic form in the source.

**Test 1 - descriptions are one line, bounded, and distinct.**
For every entry in `CATALOG` and every entry in `LISTING`: non-empty, no newline, at most 110
characters. Then the uniqueness rule, in both collections and case-folded: no two configurations may
share a description. `e2e/browse.e2e.ts:258-263` asserts this against the rendered page; the reason to
mirror it here is arithmetic - a collision found in a 30-second quick run costs a minute, and the same
collision found in a 112-second Playwright run costs an evening. Say that in the comment. Report the
longest description and its length in the failure message so a near-miss is visible.

**Test 2 - the punctuation is load-bearing.**
Assemble the corpus as `{ entry, field, text }` triples over every HANGAR-authored string: the
`description`, `name` and `tags` of every entry whose `source.kind` is not `"preset"`, plus every
`quiet` line in `LISTING` and `RESTS_DARK_NOTE` itself, plus the two HANGAR-authored `quiet` lines in
`FRONT_DOOR`. Assert the corpus is non-empty and record its size, then, for every member:

- zero `!` - `install-copy.ts:44-46`'s rule, project-wide;
- zero code points matching `\p{Extended_Pictographic}` with the `u` flag - CLAUDE.md forbids emoji;
- **no ASCII apostrophe between two letters.** The needle is `/[A-Za-z]'[A-Za-z]/`. U+2019 is what
  HANGAR writes; the source carries it literally, as GHOST's and MORPH's already do
  (`listing.ts:251, 276`);
- **no hyphen doing a dash's job.** Refuse ` - ` (space hyphen space), ` -- ` and `--` anywhere in a
  description or a quiet line. A real em dash is U+2014, and a semicolon is usually better; a tag may
  contain a hyphen because the slug grammar requires it, so tags are exempt from this rule only.
- three dots `...` refused in favour of U+2026, and U+2026 itself refused in a **description** with
  the shipped reason: an ellipsis means "this is still happening", never "there is more", and
  descriptions are never clamped (`CatalogCard.svelte:40-44`).

Every refusal names the entry, the field, the offending character and the character's code point in
hex. That is what makes the gate a script rather than a reading.

**Test 3 - the tag vocabulary.**
Every tag matches `^[a-z][a-z0-9-]*$` (already in `catalog.spec.ts`, asserted here too because this
file is where a reader will look for it, and a second assertion costs nothing). Then the two shipped
conventions, asserted as rules with the entry named: a `preset` entry carries exactly **3** tags; a
hand-authored entry carries exactly **4**. Then the typo guard: maintain a `KNOWN_TAGS` sorted array
in this file, assert every tag in the catalog is a member, and assert every member is carried by at
least one entry - both directions, so a retired tag is red too. `KNOWN_TAGS` starts as today's
forty-one and a wave that coins a tag adds it here on purpose. That is the whole mechanism against
`playble`.

**Test 4 - the two search anchors.**
`e2e/browse.e2e.ts:719-734` asserts that `?q=aurora` leaves exactly one card, and uses it to prove a
browse load that fetches zero WebAssembly. `filter.spec.ts` asserts `filterListing(LISTING, "ghost")`
returns exactly `["ghost"]`. Both are load-bearing and both are one careless word away from breaking.
Assert directly: for each of the two anchor words, exactly one entry's searchable text - name,
description and tags, case-folded with `fold` from `$lib/browse/filter` - contains it, and that entry
is the one with the matching id. Write the reason in the comment, naming both consumers. Add the same
assertion for `drums` inverted: **more than one** entry carries it, because the typed-word test in
`e2e/browse.e2e.ts:337-349` asserts the query narrows without emptying.

**Test 5 - the census, printed.**
The "counted by script and never by eye" requirement made literal. Build a table over every
HANGAR-authored string: total strings, total characters, the longest description with its entry id and
length, the count of U+2019 occurrences, the count of U+2014 occurrences, the distinct-tag count, the
singleton count and the chip count. Assert only three things - the corpus is non-empty, at least one
U+2019 occurs (so the typographic form is genuinely in use and the apostrophe rule in test 2 is not
green by absence), and the longest description is at most 110 - and `console.log` the table when
`COPY_CENSUS=1` is set. The point of the test is the table; the point of the assertions is that the
table was computed over real data.

Run `npm run format`, `npm run lint`, `npm run check`.

**Observe the negative check, three times, one per rule that will actually be tested by twenty new
strings.** In `src/lib/catalog/listing.ts` and its catalog entry in lockstep (or the assertion fails
for the wrong reason, from `listing.spec.ts`):

1. Change GHOST's U+2019 to an ASCII apostrophe -> **test 2** red, naming `ghost`, `description` and
   `U+0027`.
2. Add `!` to one hand-authored description -> **test 2** red.
3. Copy one entry's description onto another -> **test 1** red, naming both ids.

Restore each with `git checkout --` and record all three failing messages and exit codes.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/catalog/copy.spec.ts 2>&1 | node scripts/check-counts.mjs 1 5 &amp;&amp; npm run test:quick 2>&1 | node scripts/check-counts.mjs $((PREV_FILES + 1)) $((PREV_TESTS + 5)) &amp;&amp; npm run check 2>&1 | grep -Ei "error|warning" &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/catalog/copy.spec.ts` reports **5 passed** in 1 file, and `grep -c "  it(" src/lib/catalog/copy.spec.ts` prints `5`.
    - The apostrophe rule is by source kind: `grep -q "source.kind" src/lib/catalog/copy.spec.ts` exits 0, and no entry id appears as a literal exemption - `grep -c "\"radar\"\|\"aurora\"" src/lib/catalog/copy.spec.ts` prints `0` except inside test 4, which names its two anchors on purpose; state which line each occurrence is on in the SUMMARY.
    - `KNOWN_TAGS` exists and holds today's tags: `node -e "..."` reporting its length, recorded in the SUMMARY against the census this plan computes.
    - `COPY_CENSUS=1 npx vitest run --project server src/lib/catalog/copy.spec.ts` prints the census table, and the table is pasted into the SUMMARY verbatim.
    - `npm run test:quick 2>&1 | node scripts/check-counts.mjs $((PREV_FILES + 1)) $((PREV_TESTS + 5))` exits 0. The new totals are recorded in the SUMMARY under `PREV_FILES` and `PREV_TESTS` for plan 09-03 to read; `BASE_FILES` and `BASE_TESTS` are copied forward from 09-01 unchanged.
    - All three negative checks were observed red on the stated test, naming the stated entry, and reverted; `git diff --quiet -- src/lib/catalog/` exits 0 afterwards.
    - `npm run check 2>&1 | grep -Ei "error|warning"` prints `0 errors, 0 warnings`; `npm run lint` exits 0.
  </acceptance_criteria>

  <done>Every string a visitor reads on a card is counted by a script: the punctuation rule is mechanical, the tag vocabulary cannot absorb a typo, two search anchors the suite depends on are protected, and the census is a table rather than an impression.</done>
</task>

<task type="auto">
  <name>Task 9-02-03: A keyboard is not silence, and the phase gets one date</name>
  <files>src/lib/sim/lua-smoke.spec.ts</files>

  <read_first>
    - `src/lib/sim/lua-smoke.spec.ts` in full, especially test 2 and the `SmokeRun` type
    - `src/lib/sim/lua-host.ts` - `recordHid`, the `hid` accessor and the comment at `:429`
      (*"Nothing in HANGAR consumes them"*)
    - `.planning/research/ZONA-CAPABILITIES.md` section 2.6 point 2 - HID is recorded, never simulated
  </read_first>

  <action>
**1. Widen the execution gate's question.** `lua-smoke.spec.ts` keeps **exactly 3 tests**.

Add `hid: readonly HostHid[]` to `SmokeRun` and capture `host.hid` in `smoke()` beside `host.midi` -
one line each, in the single pass the file already makes.

Test 2's MIDI assertion becomes an **output** assertion:

```ts
const output = run.midi.length + run.hid.length;
expect(
  output,
  `${entry.id}: produced no MIDI and no HID at all across ${run.totalTicks} ` +
    "ticks - the scripted gesture never reached anything this configuration sends",
).toBeGreaterThan(0);
```

Rewrite the comment above it, keeping the shipped sentence's intent and correcting its scope:

> An entry that emits nothing was not exercised by the gesture. Fix the gesture, never this
> assertion: a silent instrument is exactly what the gate exists to notice. **What counts as
> non-silent is MIDI or HID.** `gmms`, `gmbs` and `gks` are recorded and inert in the browser
> (`lua-host.ts:429`), and a configuration whose whole output is keystrokes - a scene switcher, a
> shuttle, a macro pad - is a keyboard, not a silent instrument. Its output is invisible in HANGAR
> and that is a fact about the simulator, stated on the card and auditioned at a bench, not a
> failure of the entry.

Then add the non-vacuity half, in the same test, so the widening cannot hide a regression:

```ts
const midiEntries = runs.filter((r) => r.midi.length > 0).map((r) => r.id);
expect(
  midiEntries.length,
  "no hand-authored configuration produced MIDI at all - the widening above " +
    "would then be hiding a broken host rather than admitting a keyboard",
).toBeGreaterThan(0);
```

The matching HID-side assertion is **not** added here: no hand-authored entry sends HID yet, so it
would be red on arrival. Leave a comment at that exact spot naming the plan that adds it - the wave
that lands the first keystroke configuration - and what it will say: *at least one entry produced
HID*. Say why the two halves land in different plans rather than pretending: an assertion that cannot
be true today is not a gate, it is a scheduled failure.

Extend the `SMOKE_REPORT=1` line to print both counts per entry, so a bench reader sees which kind of
output each configuration produced.

**2. Record the phase's one `addedAt`.**

Every one of the twenty configurations in this phase carries **the same** `addedAt`, and this task
fixes it. Record in the SUMMARY, under the name `PHASE_ADDED_AT`, the date this plan runs, in
`YYYY-MM-DD`:

```
PHASE_ADDED_AT = <the date this task runs>
```

Every entry plan from 09-03 to 09-09 copies that string verbatim into every entry file and every
`LISTING` row it writes, and none of them invents its own. The reason is the NEWEST sort: `addedAt`
blocks a page into groups, and seven waves running on seven days would produce seven blocks of three,
which is a page that looks like it was built by a machine rather than a catalog with a history. One
date says the honest thing - twenty configurations arrived together.

Task 9-02-01 already restructured `sort.spec.ts` to assert blocks rather than two fixed sizes, so
three blocks is a structural pass. Confirm that: after the phase this listing will hold
`2026-09-02` (nine ported), `2026-09-04` (seven from Phase 8) and `PHASE_ADDED_AT` (twenty). State
the expected end state in the SUMMARY.

Run `npm run format`, `npm run lint`, `npm run check`.

**Observe the negative check.** Replace the `self:gms(...)` call in
`src/lib/catalog/entries/morph.ts`'s Setup template with nothing (it is the only entry with no Timer,
so it is the cheapest to silence), run `lua-smoke.spec.ts`, and confirm **test 2** goes red naming
`morph` and the new "no MIDI and no HID" message rather than the old one. Restore with
`git checkout -- src/lib/catalog/entries/morph.ts`. Record both exit codes and the message.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts 2>&1 | node scripts/check-counts.mjs 1 3 &amp;&amp; npm run test:quick 2>&1 | node scripts/check-counts.mjs $((PREV_FILES + 1)) $((PREV_TESTS + 5)) &amp;&amp; npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 &amp;&amp; npm run check 2>&1 | grep -Ei "error|warning" &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/sim/lua-smoke.spec.ts` reports **3 passed**; `grep -c "  it(" src/lib/sim/lua-smoke.spec.ts` prints `3`.
    - The gate reads both logs: `grep -q "run.hid.length" src/lib/sim/lua-smoke.spec.ts` exits 0.
    - The deferred half is marked, not forgotten: `grep -q "at least one entry produced HID" src/lib/sim/lua-smoke.spec.ts` exits 0 and it sits inside a comment, not an assertion.
    - `SMOKE_REPORT=1 npx vitest run --project server src/lib/sim/lua-smoke.spec.ts` prints a MIDI count and a HID count per entry; the seven lines are pasted into the SUMMARY.
    - `PHASE_ADDED_AT` is recorded in the SUMMARY as a single `YYYY-MM-DD` string, with the sentence that every plan from 09-03 to 09-09 copies it verbatim.
    - `npm run test:quick 2>&1 | node scripts/check-counts.mjs $((PREV_FILES + 1)) $((PREV_TESTS + 5))` exits 0 against the 09-01 baseline; `npm run test:sweep` reports `4 19`.
    - The negative check was observed red on test 2 with the new message, and reverted; `git diff --quiet -- src/lib/catalog/entries/morph.ts` exits 0 afterwards.
    - `npm run check 2>&1 | grep -Ei "error|warning"` prints `0 errors, 0 warnings`; `npm run lint` exits 0.
  </acceptance_criteria>

  <done>The execution gate asks whether a configuration produced any output and still refuses a catalog in which nothing produces MIDI, the HID half of that guarantee is scheduled rather than faked, and the twenty configurations of this phase have one arrival date fixed before the first of them is written.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project server src/lib/browse/filter.spec.ts src/lib/browse/sort.spec.ts` reports 12 passed in 2 files.
- `npx vitest run --project server src/lib/catalog/copy.spec.ts` reports 5 passed in 1 file.
- `npx vitest run --project server src/lib/sim/lua-smoke.spec.ts` reports 3 passed in 1 file.
- `npm run test:quick` reports `PREV_FILES + 1` files and `PREV_TESTS + 5` tests against 09-01's block, checked with `scripts/check-counts.mjs`. In clean-tree terms that is `BASE_FILES + 1` and `BASE_TESTS + 4`, which is the phase total 09-10 asserts - state both in the SUMMARY so the two arithmetics are visibly the same arithmetic.
- `npm run test:sweep` reports `4 19`.
- `npm run check 2>&1 | grep -Ei "error|warning"` prints `0 errors, 0 warnings`; `npm run lint` exits 0.
- `git diff --stat HEAD -- src/vendor/ src/lib/catalog/index.ts src/lib/catalog/listing.ts src/lib/catalog/front-door.ts src/lib/catalog/frames.json` prints nothing: this plan changed no data.
- All six negative checks observed with their stated outcomes and reverted.
</verification>

<success_criteria>
Adding a configuration to this repository now costs one recorded block in `filter.spec.ts`, one in
`sort.spec.ts`, and the three declarations the catalog has always required - never a rewrite of the
browse arithmetic. Every string a visitor reads is counted by a script rather than reviewed by eye,
including the two search words the shipped suites depend on. A configuration whose whole output is
keystrokes can pass the execution gate honestly, and a catalog in which nothing plays still cannot.
</success_criteria>

<output>
After completion, create `.planning/phases/09-twenty-configurations/09-02-SUMMARY.md`. Carry the
seven-name block first - `BASE_FILES`, `BASE_TESTS`, `PREV_FILES`, `PREV_TESTS`, `BASE_SWEEP`,
`BASE_E2E`, `PREV_E2E` - with the frozen pair copied from 09-01 and the rolling pair as this plan left
them, then:

- **`PHASE_ADDED_AT`**, the one date every entry plan copies, on its own line and impossible to miss.
- The `RECORDED` block from each of the two browse specs, verbatim, so a later wave knows exactly
  which four numbers and which two lists it is updating.
- The full `KNOWN_TAGS` array and the `COPY_CENSUS=1` table, verbatim - every entry plan writes tags
  against them.
- The copy gate's five rules stated in one list, in the imperative, so an entry plan can quote it:
  no exclamation mark, no emoji, typographic apostrophe, no hyphen as a dash, no ellipsis in a
  description, one line, at most 110 characters, unique, four tags on a hand-authored entry.
- The two protected search anchors and their two consumers.
- The `SMOKE_REPORT=1` per-entry MIDI and HID counts for the seven shipped entries.
- The named spot in `lua-smoke.spec.ts` where the HID non-vacuity assertion goes, and which plan adds it.
- All six negative-check results.
</output>
