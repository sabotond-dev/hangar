---
phase: 12.1-gradient-touch
plan: 03
subsystem: catalog, sim
tags: [gradient, re-fit, euclid, steps, radar-points, sonar, arc, touch-library, calibration, wasmoon, audition]

# Dependency graph
requires:
  - phase: 12.1-gradient-touch
    provides: plan 02 - TOUCH_LIBRARY (255/0, 781) and TOUCH_LIBRARY_TIMER (255/6), G carrying its colour, the host running both, the finding that the callback is Q first then G
  - phase: 12.1-gradient-touch
    provides: plan 01 - calibration.ts (KX, KY, calibratedAxis, sensorAt), audition row 24
  - phase: 12-touch-framework
    provides: 12-08's four sequencers on Q and X, 12-05's ARC stop tap, the R convention (12-07)
provides:
  - src/lib/catalog/entries/{euclid,steps}.ts - `local m=Q(...)G(s,i,e,x,y,0,255,255,255)if not m` (STEPS with `a`); the finger in white on layer 0
  - src/lib/catalog/entries/{radar-points,sonar}.ts - `R=function(s,i)local a=glag(0,40)glc(a,0,@SWEEPC,1)glp(a,0,255)end ` at the head, `local n=Q(...)G(s,i,e,x,y,0,@SWEEPC)R(s,i)if not n`; the finger in the sweep's colour, the centre re-lit and re-coloured on every path
  - src/lib/catalog/entries/arc.ts - `N(x,y)==40` as the stop test
  - src/lib/catalog/library.ts - G's end test `e~=1 and e~=4` (no `and e<9`): a code-9 fast tap draws nothing; 255/6 705
  - src/lib/sim/lua-smoke.spec.ts - two tests (32 -> 34): the four sequencers at all 81 LED centres with the centre dot on both expiry paths and a code-9 tap; ARC's stop through N
  - docs/HARDWARE-AUDITION.md row 25; audition.spec.ts ROW_COUNT 25
affects: [12.1-04 (CHORUS, MORPH, CONSOLE, LUMEN, TRACKPAD re-fits - the same needle discipline, Q then G, and G's new end test already in the library), 12.1-05 (the preview's forward map; frames.json did NOT move here), 12.1-09 (the gate; row 25), 13-14 (the emitter: Q first, then G, then R where the entry defines one)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A needle is applied to the ONE string-literal line of an entry, never file-wide: the header comments quote the callback too, and a file-wide exactly-once assertion refuses (measured: EUCLID x2, RADAR POINTS x3, SONAR x2)"
    - "A painter with per-contact state treats a coalesced press-and-lift (code 9) as an end for the DRAWING while the cell reader treats it as a press: G returns on `e~=1 and e~=4`, Q escapes 9 with `and e<9`, and the spec asserts the two disagree"
    - "An entry that paints layer 0 outside G defines R to put its picture back after every block clear - re-light AND re-colour, idempotent, called after G in the callback and by E after V on every expiry"

key-files:
  created: []
  modified:
    - src/lib/catalog/entries/euclid.ts
    - src/lib/catalog/entries/steps.ts
    - src/lib/catalog/entries/radar-points.ts
    - src/lib/catalog/entries/sonar.ts
    - src/lib/catalog/entries/arc.ts
    - src/lib/catalog/library.ts
    - src/lib/catalog/library.spec.ts
    - src/lib/sim/lua-smoke.spec.ts
    - docs/HARDWARE-AUDITION.md
    - src/lib/catalog/audition.spec.ts

key-decisions:
  - "The callback is Q, then G, then R (on the two sweeps), then the entry's early return - the plan's must_haves artifact `G(...)local m=Q(...)` was not emitted, on 12.1-02's finding that G-then-Q clears on every onset the block G just drew"
  - "G DRAWS NOTHING ON A CODE 9: its end test is `e~=1 and e~=4`, not Q's live test - a coalesced press-and-lift has no finger left to draw, and drawing it left a block lit until the Timer sweep (found by the residue gate on EUCLID's cell 20 at phase 134); Q is unchanged, so the 9's press half still toggles; 255/6 713 -> 705"
  - "frames.json does not move for a re-fit that adds G and R to a touch callback, because an untouched run calls neither; frames.spec.ts stayed green and the plan's exclusion was not needed"
  - "Row 25's four-name Config cell is wider than the table's column; prettier re-padded every row, and 'never re-wrap a row' is kept as 'no row's text moves', asserted modulo padding"

requirements: [CONT-02, CONT-03, PREV-01]
requirements-completed: []

# Metrics
duration: about 32 min of execution (14:06Z-14:38Z) plus the SUMMARY and STATE
completed: 2026-09-11
---

# Phase 12.1 Plan 03: EUCLID, STEPS, RADAR POINTS, SONAR and ARC re-fitted on G and the calibrated Q Summary

**The four cell-toggling sequencers now draw the finger as the library's bilinear gradient on layer 0
in their own colour - white on EUCLID and STEPS, `@SWEEPC` on RADAR POINTS and SONAR - and the cell
they toggle is the LED under it, because `Q` and `G` both read the measured map; RADAR POINTS and SONAR
define the library's `R` so their centre dot is re-lit and re-coloured after every `G` and on every
expiry path; ARC's stop tap tests the calibrated centre through `N`. Every figure was re-measured in
this tree under the pinned minifier at the RGB444 picker corner: EUCLID 700 -> **726** (182 free),
STEPS 394 -> **420** (488), RADAR POINTS 489 -> **592** (316), SONAR 469 -> **572** (336), ARC 541 ->
**528** (380), Timers unmoved, sweep `4 19` twice. Proved in wasmoon at all 81 LED centres on all four
(the 9x9 is the identity), the centre dot at 255 after a Timer-sweep expiry and a lost-lift re-press
over it, and ARC stopped by a press the naive divisor reads as cell 39. One library fix the plan did not
have, found by the residue gate: **`G` drew a code-9 fast tap and left it lit until the sweep**; its end
test is now `e~=1 and e~=4` (255/6 713 -> **705**). Term `+0 / +2` on 13-10's `87 / 905`: **87 / 907
(+1 todo)**, twice green whole; e2e 78 / 94 untouched; check 609 / 0 / 0; lint clean; audition 25.
Seven negative checks red and restored. No device was touched.**

## Performance

- **Duration:** about 32 min of execution (14:06Z to 14:38Z) plus this document and STATE.md
- **Tasks:** 2 of 2, plus one library fix (deviation 1)
- **Files:** 0 created, 10 modified (the plan's eight plus `library.ts` and `library.spec.ts`), plus this
  document and STATE.md

## Commits

| Hash      | Message                                                                                                                                                   |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `140d2e3` | `feat(12.1-03): EUCLID, STEPS, RADAR POINTS, SONAR and ARC re-fitted on G and the calibrated Q, N - Q first, then G, then R`                            |
| `3d1b36c` | `fix(12.1-03): G draws nothing on a code 9 - its end test is `e~=1 and e~=4`, not Q's live test, so a fast tap leaves no finger lit`                     |
| `dbb4c8c` | `test(12.1-03): the four sequencers under G at all 81 LED centres with the centre dot on both expiry paths, ARC's stop through N, and audition row 25` |

`git commit --only <paths> -F <message-file>`, pathspec before the flag. 13-10's three commits (`dfa5bbb`,
`2df6767`, `28485cc`) landed under this plan on the other agent's files; none of this plan's ten files is
theirs. The docs commit carrying this document and STATE.md is the fourth and last. No push. No
co-author trailer (deviation 7).

---

## The baseline, re-measured at this plan's start and close

| Name         | Brief (at `a1045d7`) | Observed at start (14:06Z)                                        | Term                     | Observed at close                                                                  |
| ------------ | -------------------- | ----------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------- |
| `PREV_FILES` | 88                   | **88**                                                            | **+0** (13-10: -1)       | **87** - `mix.spec.ts` deleted by 13-10 (`2df6767`)                                |
| `PREV_TESTS` | 906 (+1 todo)        | **907** (+1 todo) - 13-10's uncommitted `+1` already on disk      | **+2** (13-10: -1 net)   | **907** (+1 todo) - `check-counts.mjs 87 907` matches, twice                       |
| sweep        | `4 19`               | -                                                                 | +0, RUN                  | **`4 19`** twice (178 s with the new Setups; 108 s after the G fix), both matching |
| check        | 610                  | -                                                                 | +0                       | **609** files, 0 errors 0 warnings (13-10's -1: `mix.spec.ts`)                     |
| lint         | clean                | -                                                                 | -                        | clean on every file touched (`prettier --check`, `eslint`)                         |
| e2e titles   | 78 / 94              | 78                                                                | +0 / +0                  | **78** (`grep -c "test("` summed, equal at `a1045d7` and `HEAD`)                   |
| catalog      | 26 = 8 + 18          | -                                                                 | +0                       | 26                                                                                 |
| audition     | 24                   | 24                                                                | **+1**                   | **25**                                                                             |
| library      | 781 + 713            | 781 + 713                                                         | 255/6 -8 (deviation 1)   | **781 + 705**                                                                      |

**The chain, written out:** `a1045d7` 88 / 906 -> 13-10's `-1 / -1` (mix.spec.ts -2, tune-ui -2 +2,
surprise +1; its SUMMARY's own term) -> 87 / 905 -> **this plan's `+0 / +2`** -> **87 / 907 (+1 todo)**.
The one part of `+2`: `lua-smoke.spec.ts` 32 -> **34** (its header reads THIRTY-FOUR with the sentence
that 12.1-04 adds two). The brief's alternative - "if 13-10 lands its `-1 / -1` under you (then 87 / 907)" -
is what happened.

**The whole quick suite ran four times.** Runs 1 and 2 (14:30Z, 14:31Z) were red on
`install.spec.ts` alone - 17 and 16 of its 23 tests timing out on "waiting for identification", the
connect-snapshot transient the brief names - while 13-10 was running its own suite and committing;
`install.spec.ts` alone passed 23 between them. Runs 3 and 4 (14:32Z, 14:33Z) were green whole: `87 passed
/ 907 passed / 1 todo`, `check-counts.mjs 87 907` matching on both.

**`frames.spec.ts` was NOT excluded, and did not need to be** (deviation 4): the fixture records an
UNTOUCHED run, and nothing in an untouched run calls `G` or `R` - so `frames.json` is byte-unchanged and
its five tests are green in every run above. The plan's `--exclude src/lib/catalog/frames.spec.ts` was
run once anyway (14:32Z) and had **no effect**: a vitest project config's `exclude` array overrides the
CLI flag, so the run was a whole run (87 / 907) - which is the third green run in the paragraph above.
Counted both ways, then: **with** `frames.spec.ts` 87 / 907; **without** it, 86 / 902 by arithmetic
(five tests, one file), never observed because it could not be excluded and never needed to be. Plan 05
regenerates the fixture only if the preview's forward map moves a rest frame.

---

## Task 1: five entries re-fitted by exact needles, every header re-measured

### The needles, as applied - Q first, then G, then R

| Entry        | The one Setup literal line: before -> after                                                                                                                                                                                 | File-wide count of the needle (why the literal only)         |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| EUCLID       | `local m=Q(s,i,e,x,y)if not m` -> `local m=Q(s,i,e,x,y)G(s,i,e,x,y,0,255,255,255)if not m`                                                                                                                                  | x2 (`:70` comment)                                           |
| STEPS        | `local a=Q(s,i,e,x,y)if not a` -> `local a=Q(s,i,e,x,y)G(s,i,e,x,y,0,255,255,255)if not a`                                                                                                                                  | x1                                                           |
| RADAR POINTS | `--[[@cb]]` -> `--[[@cb]]R=function(s,i)local a=glag(0,40)glc(a,0,@SWEEPC,1)glp(a,0,255)end `; `local n=Q(s,i,e,x,y)if not n` -> `local n=Q(s,i,e,x,y)G(s,i,e,x,y,0,@SWEEPC)R(s,i)if not n`                                 | `--[[@cb]]` x2 (the Timer literal); the callback x3 (`:39`, `:208`) |
| SONAR        | as RADAR POINTS                                                                                                                                                                                                             | `--[[@cb]]` x2; the callback x2 (`:83`)                      |
| ARC          | `x*9//128+y*9//128*9==40` -> `N(x,y)==40`                                                                                                                                                                                   | x1                                                           |

The substitution script (`measure-1203.mjs`, scratchpad) isolates the first line matching
`^  "--\[\[@cb\]\].*";$`, asserts exactly one occurrence of the needle INSIDE that line, replaces it
there, and writes every other line back byte-identical; `git diff -U0` showed one changed line per file
before the header edits. **`G` is written AFTER `Q`, not before as the plan's interfaces block and
`must_haves` artifact have it** (deviation 2, 12.1-02's finding): same characters, so every cost below
is the plan's. On the two sweeps `R(s,i)` follows `G`: `G` writes phase 0 to cell 40 whenever 40 is in
its 2x2 block at weight 0 (a finger dead on LED (3,4) or (4,3) does that), and `R` puts it back.

### The cost, measured before and after in this tree

Under `@intechstudio/grid-protocol@1.20260825.1135`'s `GridScript.compressScript` after
`initLuaFormatter()`, `cost = max(raw, compressed)`, at the RGB444 picker corner (every colour knob
`255,255,255`, every other knob its longest value), canonical (a fixed point) and `checkSyntax` true on
all five - exactly the plan's figures:

| Entry        | Setup corner before -> after | Free before -> after | At the defaults | Timer corner (unchanged) |
| ------------ | ---------------------------- | -------------------- | --------------- | ------------------------ |
| EUCLID       | 700 -> **726** (+26)         | 208 -> **182**       | 696 -> 722      | 237                      |
| STEPS        | 394 -> **420** (+26)         | 514 -> **488**       | 388 -> 414      | 260                      |
| RADAR POINTS | 489 -> **592** (+103)        | 419 -> **316**       | 489 -> 592      | 288                      |
| SONAR        | 469 -> **572** (+103)        | 439 -> **336**       | 468 -> 571      | 289                      |
| ARC          | 541 -> **528** (-13)         | 367 -> **380**       | 538 -> 525      | 275                      |

The +103 is the `R` definition (71 with its trailing space at the corner) + the `G` call (26) + the `R`
call (6). STEPS' all-shortest picker corner moved 381 -> **407** with it (measured, quoted in its header).
The sweep (`npm run test:sweep`, `4 19`) proves every knob state of every entry under 908 with the new
Setups; its worst is still TRACKPAD at 906.

### The headers

Each entry's cost paragraph carries the new figures with the plan and the reason and the previous
figures beside them; a finger paragraph per entry says `G` on layer 0 in its colour, why the colour is a
literal (D-13), the alert-layer heal (D-11, `grid_led.h:7`, the five callers of `grid_alert_all_set`),
that there is no floor (`glc(...,1)`), why `Q` comes before `G` (12.1-02), and that the cell `Q` returns
is now the LED under the finger - EUCLID's names what that fixed on the bench (the outer ring, index 1
and 7: a finger dead on LED (1,4) read as raw 13, `13*9//128 = 0`, column 0, on no ring, dropped
silently). RADAR POINTS' and SONAR's "NOT `R`" paragraphs are inverted with the reason; their "layer 0 is
the one layer neither the wave nor a touch writes" sentences are dated; the four comments that quoted
the old guard (`euclid.ts:70`, `radar-points.ts:39` and `:208`, `sonar.ts:83`) quote the new one, and
the `~3.9 raw units / 68..74` band prose in all four is replaced by the D-18 fraction-of-the-pitch
sentence. ARC's header gains the `N` paragraph (the thirteen returned, the first re-fit that got
cheaper, why a press-time lookup wants no hysteresis, that ARC draws no finger and why) and its "hole"
recomputed on the measured map: x **55..74**, y **58..75** (rate 14..19, depth 52..69) where the naive
divisor had 57..71. **No knob's `values`, no `tags`, no name moved**: `git diff -U0 a1045d7 HEAD --
src/lib/catalog/entries/ | grep -E "values|tags"` returns one line, and it is the word "values" in a
comment sentence ("eight raw values between LED 4 and 5").

### Gates

`touch-guard.spec.ts`, `decay-idiom.spec.ts` and `host-surface.spec.ts` green over the five new Setups
(12 tests); `grep -c 'local m=Q(s,i,e,x,y)if$' euclid.ts` = 0.

---

## Task 2: the four sequencers and ARC in the real VM, the marker on every expiry path, row 25

### Test 1 - all 81 LED centres, the midpoint, a code 9, the outer ring, the centre dot

For each of EUCLID, STEPS, RADAR POINTS and SONAR rendered at its defaults over both library strings,
for every `(c, r)` in 0..8 x 0..8: `touchDown(0, KX[c], KY[r])`, one tick, assert `litOnLayer0(sim)`
equals exactly `{[c + r*9]: 255}` (plus `{40: 255}` on the two sweeps) and the cell's colour equals the
finger colour **read off the rendered Setup's one `G(s,i,e,x,y,0,r,g,b)` call** - not off a knob, because
STEPS has a `@SWEEPC` knob of its own for the sweeping column and hands `G` white; lift, one tick, assert
the rest picture. **The EUCLID 9x9, printed - the map is the identity:**

```
    #########
    #########
    #########
    #########
    #########
    #########
    #########
    #########
    #########
```

(`#` where the press lit its own cell alone; asserted as 81 of them on all four entries.) Then:

| Clause                                                    | EUCLID                                                              | STEPS                     | RADAR POINTS                                        | SONAR                                               |
| --------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------- | --------------------------------------------------- | --------------------------------------------------- |
| midpoint `(74, 68)` (between LED 4 and 5 in x, on row 4)  | `40=135 41=119`, exactly two                                        | `40=135 41=119`           | `40=255 41=119` (R over G's 135), exactly two       | `40=255 41=119`, exactly two                        |
| finger colour                                             | `255,255,255`                                                       | `255,255,255`             | `120,255,255` (the rendered `@SWEEPC`)              | `120,255,255`                                       |
| a code-9 fast tap on LED (2,2)                            | layer 0 at rest, `B` empty, layer 1 of cell 20 toggled              | same, layer 2             | same, layer 1                                       | same, layer 1                                       |
| the outer ring (EUCLID only): press dead on LED (1,4)     | cell 37 (ring 3) layer 1 `255 -> 0` on the first press; the naive divisor read **cell 36, ring 4, on no ring** | -                         | -                                                   | -                                                   |
| held on the centre through the Timer sweep (`X(s,20)`)    | -                                                                   | -                         | 22 Timer calls (308 ticks): `B` 1 -> 0, `40=255` in `120,255,255`, nothing else lit | 22 calls (154 ticks): the same                      |
| lost-lift re-press by the same id on LED (1,1)            | -                                                                   | -                         | `{10: 255, 40: 255}`                                | the same                                            |
| re-press OVER a block covering 40 (centre, then LED (7,7)) | -                                                                   | -                         | `{40: 255, 70: 255}`, the old block dark, 40 in `@SWEEPC` | the same                                            |

The "held on the centre" clause is the plan-check's expiry path: the block is 40, 41, 49, 50; the
contact says nothing; `X(s,n)` (n read off the rendered Timer, 20) expires it after `n+1` calls; `E`
clears the block through `V` and then calls `R`. The third sweep clause is the one the plan did not
spell out and is the one that actually reaches `V` with 40 inside it on the re-press path: the plan's
"press again on a different cell" after the sweep has nothing left to clear. Both are asserted.
`host.globalSize("B")` going 1 -> 0 proves the expiry ran rather than the marker merely never having
gone out.

### Test 2 - ARC stops on the calibrated centre through N

| Probe                                                             | `N` (via `calibratedCell`) | naive `x*9//128 + y*9//128*9` | swirl rate before -> after |
| ----------------------------------------------------------------- | -------------------------- | ----------------------------- | -------------------------- |
| dead on LED (4,4), `(64, 68)`                                     | **40**                     | 40                            | 4 -> **0**                 |
| one past the LED 3 -> 4 midpoint, `x = KX[3] + (KX[4]-KX[3])//2 + 1 = 56`, `y = 68` | **40**                     | **39**                        | 4 -> **0**                 |

Both computed in the test; the "stopped" observable is 11-09.1's (layer 2's rate at cell (0,0) is 0
while stopped). With the old needle the second probe leaves the swirl running (negative check T2-2).

### The header count, row 25, `ROW_COUNT`

`lua-smoke.spec.ts:3` reads THIRTY-FOUR with the sentence that 12.1-04 adds two. **Row 25 as
written** (one table row, appended after row 24):

> | 25 | EUCLID / STEPS / RADAR POINTS / SONAR | **THE GRADIENT, AND IT IS THE FIRST TEN MINUTES OF THE BENCH (plan 12.1-03; D-01).** Install EUCLID through the panel (four strings now). **(a)** Put a fingertip dead on the LED at row 2 / column 8 (one-based): does THAT LED light alone and bright, where round 3 lit the corner? **(b)** Between two LEDs: do both glow dimly, and does sliding toward one brighten it? **(c)** In the middle of four: all four? **(d)** Tap the outer Euclid ring (index 1 and 7) at the LED, ten times: does every tap toggle on the first try, where round 3 needed several? **(e)** Does the dim end read as "dimly", or too bright (the weights are linear; a square-law is a few characters away)? **(f)** With no finger down, is the dark pad exactly as dark as before (there is no floor: glc's trailing 1 zeroes it)? **(g)** Rest a finger on the seam between two ring cells and hold still: does the toggle flip back and forth, or hold? **(h)** Tap with a rolling finger: does the cell that toggles differ from the LED that ends up brightest? **(i)** RADAR POINTS / SONAR: pass a finger over the centre, lift badly (slide off), wait three seconds - is the centre dot still there? STEPS: does the column sweep still read as one bar under the gradient? **(j) THE ALERT LAYER**: after a page switch (13-12) or a refused one (a purple flash), and after the page-load row's power cycle: is the finger still the entry's colour on the next touch? A grey finger that heals on the next sample is the design; one that stays grey is a bug. | The sensor is not under the LEDs and the simulator's pointer is a mouse; only a finger on this unit can say where the light lands, how the dim end reads, and what the alert layer does to the colour. |

The plan's row text, verbatim. Its Config cell is 37 characters against a 17-wide column and its check
cell 1,501 against 1,146, so `prettier --check .` refused the file until prettier re-padded every row of
the table (deviation 5); every row's TEXT is byte-identical to the pristine copy modulo the padding,
asserted line by line by a script (0 lines differing beyond whitespace and separator dashes), and no
row was re-wrapped. `audition.spec.ts`: `ROW_COUNT` 24 -> **25** with the reason in its comment; the
title "twenty-four" -> "twenty-five"; four tests green.

---

## The negative checks, seven, each red as named and restored by sha256

Each from the edited copy in the scratchpad, the mutation applied to the tree, the named spec run, the
edited copy written back, sha256 equal either side (`sha256sum -c` over all six edited files: OK). No
`git checkout`, `restore`, `stash` or `clean`.

| #     | Mutation                                                                                     | Red as named                                                                                                                                                                                       |
| ----- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1-1  | EUCLID's callback needle applied FILE-WIDE on a scratch copy of the pristine file            | the script refuses: `needle x2: local m=Q(s,i,e,x,y)`; RADAR POINTS `x3`, SONAR `x2` the same way                                                                                                 |
| T1-2  | RADAR POINTS: `R(s,i)` BEFORE `G` in the callback                                            | test 1: `a press dead on LED (3,3) at (46,48) must light exactly its own cell at 255 on layer 0: expected { '30': 255 } to deeply equal { '30': 255, '40': 255 }` - `G`'s weight-0 write to 40 after the re-light |
| T2-1  | RADAR POINTS: `R` renamed `Z` (defined and called by the callback, unreachable from `E`)     | test 1's sweep clause alone: `cell 40 went dark on the Timer sweep - E cleared the block through V and nothing re-lit the centre: expected +0 to be 255`                                             |
| T2-1b | RADAR POINTS: `R` removed outright (definition and call)                                     | test 1 at the first press whose block covers 40: `LED (3,3) ... expected { '30': 255 } to deeply equal { '30': 255, '40': 255 }`                                                                   |
| T2-2  | ARC: the needle reverted to `x*9//128+y*9//128*9==40`                                        | test 2: `the press one past the LED 3 -> 4 midpoint did not stop the swirl - the stop test is not reading the calibrated centre: expected 7 to be +0`                                              |
| T-G   | `library.ts`: `G`'s end test back to the live test `and e<9`                                 | three red: `library.spec.ts` test 3 (`G's end test must be e~=1 and e~=4 with no and e<9`), the residue gate (`euclid at first value of every knob ... cell 20 ... layer 0 at phase 134`), and test 1's code-9 clause (`expected { '20': 255 } to deeply equal {}`) |

(T2-1's shape - `R` present but not reachable from `E` - is the precise probe for the expiry path; T2-1b
is the plan's literal mutation and goes red earlier, on the callback path, for the reason T1-2 shows.)

---

## Deviations from the plan

**1. [Rule 1 - a bug in the library, exposed by this plan's re-fit] `G` drew a code-9 fast tap and left it
lit.** The first full `lua-smoke.spec.ts` run after the needles landed was red on test 4 (the residue
gate): EUCLID at the first value of every knob, cell 20 on layer 0 at phase 134, 200 ticks after a
synthesised hardware fast tap, unchanged at every sample. `G`'s end test was `Q`'s live test `e~=1 and
e~=4 and e<9` (the plan-check called it "the blessed spelling"), so a 9 - a press AND a lift in one
message, with nothing to follow for that contact - drew a block and recorded `B[i]`, and only the Timer's
`X(s,n)` sweep would ever clear it (2.2 s on EUCLID at its default, 5.6 s on RADAR POINTS at its slowest).
`G` now writes `if e~=1 and e~=4 then B[i]=nil return end`: a 9 clears the contact's previous block and
draws nothing; `Q` is unchanged and still returns the 9's cell, so the step still toggles (QUADRANT's
worked example, in the library). `library.spec.ts` test 3 asserted "G's end test is the same negation Q
uses" and now asserts the opposite with the reason; the header's cost table, section 9, rule 6 and the
`G` description say so. Cost: `G` 360 -> 352, 255/6 **713 -> 705** (203 free), canonical, the uniform join
706, `checkSyntax` true, `LIBRARY_VERSION` "1" (nothing removed from `_G`); the class-B gate over both
strings stays green (no `e>=5` site, the one onset in `Q`). Pinned in the VM by test 1's code-9 clause on
all four entries. Files outside the plan's `files_modified`: `library.ts`, `library.spec.ts` (12.1-02's,
complete; 13-14's "library.ts unchanged during its run" is not running). Commit `3d1b36c`.

**2. [Rule 1 - the plan's callback shape] `Q` first, then `G`, then `R`.** The plan's interfaces block and
its `must_haves` artifact `contains: "G(s,i,e,x,y,0,255,255,255)local m=Q(s,i,e,x,y)"` are the research's
order, which 12.1-02 found clears on every onset the block `G` just drew. The shipped literal is `local
m=Q(s,i,e,x,y)G(s,i,e,x,y,0,255,255,255)`; the `must_haves` string does not occur in the tree and should
not. Same characters, so every plan figure holds.

**3. [Rule 3 - the plan's negative check T2-1 as written goes red on the wrong path]** "remove `R` from RADAR
POINTS' Setup" reddens test 1 at the first press whose block covers 40 (the callback path), not at the
sweep clause. Run as written (T2-1b) and reported; and run as a rename to `Z` (T2-1), which leaves the
callback path intact and reddens the sweep clause alone - the check the plan meant.

**4. [Process - the plan asserts a red the tree does not have] `frames.spec.ts` stayed green.** The plan says
it is "red for the re-fitted entries until plan 05" and prescribes `--exclude`. The fixture records an
untouched run, which calls neither `G` nor `R`, so `frames.json` is byte-unchanged and all five tests pass
in every run. The exclusion was run once anyway and **had no effect** - a vitest project config's
`exclude` overrides the CLI flag - so that run was a whole run. Counts stated both ways above.

**5. [Process - append-only vs. prettier] Row 25 re-padded the audition table.** The row's Config cell (four
names, 37 characters) and check cell (1,501) exceed the columns (17 / 1,146); `prettier --check` refused
the file until `prettier --write docs/HARDWARE-AUDITION.md` (that one file, never `.`) re-padded every
row. Text byte-identical modulo padding, asserted; recorded in `audition.spec.ts`'s comment.

**6. [Process - the standing rules' STEPS knob] The finger colour is read off the rendered Setup, not a
knob.** The first draft of test 1 took the entry's `@SWEEPC` knob as the finger colour and went red on
STEPS (`expected [255,255,255] to deeply equal [0,200,255]`): STEPS has a `@SWEEPC` knob for its sweeping
column and hands `G` white. The helper parses the one `G(s,i,e,x,y,0,r,g,b)` call in the rendered Setup.

**7. [Process - the commit trailer]** The harness asks for a co-author trailer; the repository's rule
(`standing_rules`, 12-12 deviation 10) is no attribution and no trailer. The repository's rule was
followed on all four commits; the conflict is reported here.

**8. [Process - `gsd-tools state` not run]** STATE.md by script against a copy (`state.mjs`, scratchpad),
asserting `status: executing`, `completed_phases: 11`, `percent: 100`, `total_phases: 14`, `total_plans:
160` unchanged before and after, `completed_plans` 148 -> 149, 13-10's `Status:` and `Stopped at:` retained
as "previous", Phase 12.1 at 3 of 9 beside Phase 12 (gate landed, bench pending) and Phase 13 at 10 of 20.
The copy was taken AFTER 13-10's docs commit (`28485cc`) and its sha256 checked against the tree
immediately before the write. `advance-plan`, `update-progress`, `record-session`, `roadmap
update-plan-progress` and `requirements mark-complete` not run; `.planning/ROADMAP.md` byte-unchanged;
**CAT-04 stays `[ ]`**; `REQUIREMENTS.md` untouched (the gate amends).

**9. [Process - counts] The brief's 906 was 907 on disk at the start**, because 13-10's uncommitted `+1`
was already in the working tree; the brief's alternative ("if 13-10 lands its `-1 / -1` under you, then
87 / 907") is what was observed. `install.spec.ts`'s connect-snapshot timeouts appeared in two of four
whole runs under 13-10's concurrent load and never alone.

---

## What 13-10's state did to the runs

13-10 was executing throughout: its working tree at 14:06Z carried uncommitted edits to `tune-ui.spec.ts`
and others (+1 on the observed baseline); it committed `dfa5bbb` and `2df6767` (deleting `mix.spec.ts`,
`-1 / -2`) around 14:21Z and its docs commit `28485cc` (STATE.md) before 14:35Z. Two whole quick runs at
14:30Z and 14:31Z were red on `install.spec.ts` alone (17 and 16 timeouts), green alone and in the two
whole runs that followed. None of its files is this plan's; `check` reads 609 rather than the brief's 610
by its deleted spec.

## What the plan asserts that the tree does not support

- `must_haves` artifact `G(s,i,e,x,y,0,255,255,255)local m=Q(s,i,e,x,y)` (deviation 2): `Q` first.
- "`frames.spec.ts` (five tests) is red for the re-fitted entries until plan 05" and the `--exclude`
  command (deviation 4): green, and the flag has no effect on a project config.
- "Row 25 appended ... at the table's widths, never re-wrapping a row" (deviation 5): the row is wider than
  two of the columns; prettier re-padded, no text moved.
- "remove `R` from RADAR POINTS' Setup ... test 1's sweep clause red" (deviation 3): red on the callback
  path first; the rename reddens the sweep clause.
- The plan-check's "`G`'s end-code test is the blessed spelling `e~=1 and e~=4 and e<9`" (deviation 1):
  that spelling is `Q`'s; `G`'s is `e~=1 and e~=4`.
- The brief's baseline 88 / 906 and check 610: 88 / 907 on disk at the start (13-10's uncommitted +1),
  87 / 907 and 609 at the close.
- 12.1-CONTEXT D-11 and the 12.1-02 SUMMARY quote 255/6 at 713 / 714: 705 since `3d1b36c` (a planning
  record; not edited).

## Questions for the user

None from this plan. Carried: **D-04** (KEEP once, the page-load proof of `self:tim()`) is the gate's row;
**D-12** (does the dim end read "dimly") is row 25(e); **D-13** (would one colour everywhere read better)
is a bench question the two colours here - white and `@SWEEPC` - now make askable; **D-16** (the presets)
pending.

## Known stubs

None. Every `G` call is wired to the library and every `R` to `E`; `frames.json` is unchanged because
nothing at rest changed. No component, prop or data path is wired to an empty value.

## Not hardware-verified

No agent connected to, wrote to, or deployed to a device during this plan. `src/vendor/` untouched (`git
diff --stat a1045d7 HEAD -- src/vendor/` empty); `firmware-oracle.spec.ts` unedited and green; the
simulator's phase walk unchanged. The gradient's look on a diffuser, the dim end, the outer-ring tap, the
centre dot under a bad lift and the alert layer's heal are row 25's (a)-(j) at 12.1-09.

## Self-Check: PASSED

- the ten modified files FOUND; `lua-smoke.spec.ts` thirty-four `it(` with THIRTY-FOUR at `:3`; `audition.spec.ts` `ROW_COUNT = 25`; `docs/HARDWARE-AUDITION.md` twenty-five numbered rows; `arc.ts` carries `N(x,y)==40` in its literal; `radar-points.ts` and `sonar.ts` carry the `R` definition once each in their literals
- commits `140d2e3`, `3d1b36c`, `dbb4c8c` FOUND in `git log`
- `git diff --stat a1045d7 HEAD -- src/vendor/ src/lib/catalog/frames.json src/lib/fidelity/firmware-oracle.spec.ts` empty; `.planning/ROADMAP.md` byte-unchanged; nothing of this plan's under `13-gui-overhaul/` or `12-touch-framework/`; CAT-04 `[ ]`
- quick `87 / 907 (+1 todo)` twice green whole (`check-counts.mjs 87 907` matching); sweep `4 19` twice; check 609 / 0 / 0; lint clean on every touched file; e2e titles 78
