---
phase: 12.1-gradient-touch
plan: 07
type: execute
wave: 7
band: 2
depends_on: ["12.1-06"]
files_modified:
  - src/lib/tune/model.ts
  - src/lib/tune/model.spec.ts
  - src/lib/device/install.svelte.ts
  - src/lib/device/install.spec.ts
  - src/lib/device/snapshot.ts
  - src/lib/device/snapshot.spec.ts
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-07]
count_term: "PREV_FILES + 0 / PREV_TESTS + 3 (model.spec +1, install.spec +1, snapshot.spec +1); e2e + 0 / + 0 by grep; sweep 4 19 unmoved by declaration"

must_haves:
  truths:
    - "TRY ON DEVICE writes four strings through the one writer, PUT BACK restores four, CLEAR writes four firmware defaults, and the classifier names which of four landed in write order - the impossible partial (a later slot landed, an earlier did not) still impossible."
    - "A Lua entry lands the library's two halves; a preset lands the two firmware defaults; the tuner publishes both fields empty for a preset and the store is the ONE place they become defaults, exactly as 12-03 placed the first."
    - "The snapshot holds four strings under the next key beside the old, the old is read and never overwritten or deleted, and a record from before this phase restores the 255/6 default with a flag the store publishes."
  artifacts:
    - path: "src/lib/tune/model.ts"
      provides: "ConfigStrings.systemTimer; landLua publishes TOUCH_LIBRARY_TIMER"
      contains: "systemTimer"
    - path: "src/lib/device/install.svelte.ts"
      provides: "#pageTimer beside #pageInit; four-string TRY / PUT BACK / CLEAR; the classifier over four"
      contains: "SYSTEM_DEFAULT_TIMER"
    - path: "src/lib/device/snapshot.ts"
      provides: "the next key with ConfigQuad; readSnapshot returning systemTimer with its provenance flag"
      contains: "systemTimer"
  key_links:
    - from: "src/lib/device/install.svelte.ts"
      to: "src/lib/transport/sequence.ts"
      via: "writeAll over a four-key ConfigSet; fetchAll reading four"
      pattern: "systemTimer"
    - from: "src/lib/tune/model.ts"
      to: "src/lib/catalog/library.ts"
      via: "TOUCH_LIBRARY_TIMER published beside TOUCH_LIBRARY"
      pattern: "TOUCH_LIBRARY_TIMER"
---

<objective>
The store, the snapshot and the tuner over four strings.

**The shape is 12-03's, one field wider.** 12-03 added the third string and put the substitution of the
empty string to `SYSTEM_DEFAULT_SETUP` in exactly one place - `install.svelte.ts`'s `#pageInit` - because
`ladder.spec.ts:275` keeps a firmware default out of `src/lib/tune/`. The fourth string follows the same
road: `ConfigStrings.systemTimer`; `landLua` publishes `TOUCH_LIBRARY_TIMER`; the preset landing publishes
`""`; `#pageTimer` beside `#pageInit` substitutes `SYSTEM_DEFAULT_TIMER`; CLEAR's defaults gain the
fourth; the classifier - SAFE-07's own machinery, widened by 12-03 to three step ids - reads four from
`SLOTS`. **`system` is not metered and neither is `systemTimer`** (`model.ts:204`'s rule; the meter's
fixed line is 13-10's question, `12.1-VALIDATION.md` hand-offs).

**The snapshot: the next key, beside the old.** 12-03's rules are the whole design and are repeated
verbatim, not paraphrased: a new key beside the old; the old read and never written, overwritten or
deleted; a record without the new field reads with the **default** in its place and a flag the store
publishes. **Which key is "next" depends on what 13-12 left** - it wrote `snapshot.ts` for per-page
keying and may have added a version (`12.1-VALIDATION.md` R-9); read `13-12-SUMMARY.md` and name the key
in this plan's SUMMARY.

**What 13-12 also changed here.** `install.svelte.ts` now carries the page-target gate and the
destination review (13-12); PUT BACK names its page. None of that moves; the fourth string threads
through the existing phases, actions and legs, and `WRITABLE_PHASES`, the write guard and the fifteen
phases are unchanged - a test asserts the phase list is byte-identical before and after.

Purpose: SAFE-03, SAFE-05, SAFE-07 and SAFE-04 covering everything HANGAR writes, which is now four.
Output: one field, one substitution, one key, three tests. Term `+0 / +3`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-06-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-12-SUMMARY.md
@.planning/phases/12-touch-framework/12-03-SUMMARY.md
@.planning/phases/12-touch-framework/12-07-SUMMARY.md
@src/lib/tune/model.ts
@src/lib/tune/model.spec.ts
@src/lib/device/install.svelte.ts
@src/lib/device/install.spec.ts
@src/lib/device/snapshot.ts
@src/lib/device/snapshot.spec.ts
@src/lib/transport/sequence.ts
@src/lib/catalog/library.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (12-12 deviation 10). **No
  emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Everything here is against the synthetic
  fixture and scripted faults in node; the ZONA half is runbook rows I and J.
- **Sibling repositories read-only; `src/vendor/` untouched; no manifest row.**
- **Do not edit `.planning/ROADMAP.md`.** Nothing under `13-gui-overhaul/` or `12-touch-framework/`.
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress`.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push.
- **`snapshot.ts` imports nothing** - `snapshot.spec.ts` test 1 asserts zero specifiers, `import type`
  included; the default comes from the caller. **`ladder.spec.ts:275`** keeps `lib/protocol` out of
  `src/lib/tune/`. Both stay green and unedited.
- **The one writer is `sequence.ts`'s `writeAll`** and it is not edited here (plan 06 landed it). If it
  must be, stop and ask.
- **Nothing writes without a click; RAM before flash; snapshot before write; ACK before done** - none
  of it loosened for the fourth string.
- **State every count as a carried name plus a delta, re-measured at this plan's start**, then assert
  **`+0 / +3`** with its three parts.
- **Concurrency:** 13-14 waits for Band 2; no Phase 13 plan writes these six files between 13-12 and
  13-17. `install-copy.ts` (the sentences) is plan 08's, not this plan's - the classifier's four names
  come from `SLOTS[].label`, so no copy file moves here.
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## `model.ts`

```ts
export type ConfigStrings = {
  readonly systemTimer: string;   // 255/6 - TOUCH_LIBRARY_TIMER for a Lua entry, "" for a preset
  readonly system: string;        // 255/0 - as today
  readonly setup: string;
  readonly timer: string;
};
export interface TunerOptions { systemSetup?: string; systemTimer?: string; /* both empty by default */ }
```

`landLua` publishes `systemTimer: systemTimer === "" ? TOUCH_LIBRARY_TIMER : systemTimer` beside the
existing `system` line (`:827`); the preset landing publishes both unchanged. `numbers` still carries two
figures. **`model.spec.ts` +1**: a Lua entry's landing carries both library strings and a preset's
carries both empty, and the one place the empties become defaults is named (`install.svelte.ts`).

## `install.svelte.ts`

- `#pageTimer(config, P)` beside `#pageInit`: `config.systemTimer === "" ? P.SYSTEM_DEFAULT_TIMER :
  config.systemTimer` (`heavyModules()` already resolves `P` lazily; add the export to the module shape
  it resolves).
- `tryOnDevice`: `strings = { systemTimer: this.#pageTimer(config, P), system: this.#pageInit(config, P), setup, timer }`.
- `clear`: `defaults` gains `systemTimer: P.SYSTEM_DEFAULT_TIMER`, and the header sentence that a KEEP
  after CLEAR stores nothing gains 255/6 (`print("tick")` runs once on the module and prints to nobody;
  `cfg_default_flag` - 12-RESEARCH 1c - applies to it as to the others).
- `#snapshot`: `fetched` reads four; `canWriteBack` guards four; `readSnapshot` takes the two defaults.
- The classifier `#classify`: the "which landed" naming reads `SLOTS` (four labels, write order); the
  impossible partial is now "a later slot landed and an earlier did not", asserted.
- `lastWritten`, `putBack`, the store proof (`#storeLeg`'s refetch comparison) read four.

**`install.spec.ts` +1**: CLEAR writes four defaults in `SLOTS` order and PUT BACK writes the four
snapshotted strings in order; the classifier over a scripted fault on the FIRST write (255/6) is
`nothing-landed`, on the third a `partial` naming System timer and System as landed and Timer as not;
the phase list `WRITABLE_PHASES` is byte-identical to the file's value before this plan (read from a
scratch copy of HEAD's file) - 13-12's states untouched. The existing three-write assertions become four
in place (the test that counts writes per RAM leg, the order test).

## `snapshot.ts`

```ts
export const SNAPSHOT_KEY_V3 = "hangar.snapshot.v3";   // OR the next version after 13-12's - NAME IT
export type ConfigQuad = ConfigTriple & { readonly systemTimer: string };
export type ReadEntry = ConfigQuad & { readonly fromV1: boolean; readonly fromV2: boolean };
export function readSnapshot(store, moduleId, page, defaults: { system: string; systemTimer: string }): ReadEntry | undefined;
export function persistIfAbsent(store, moduleId, page, quad: ConfigQuad, takenAt: string): PersistOutcome;   // writes the NEW key only
```

Read order: the new key, then the older ones; an older record returns with `systemTimer` = the default
and `fromV2: true` (and `fromV1` as today for the oldest). **`persistIfAbsent` writes the new key only and
never deletes an older one.** The header's "TWO KEYS SINCE PHASE 12" paragraph becomes three (or however
many 13-12 left plus one) and keeps every rule verbatim. **`snapshot.spec.ts` +1**: an older record with
three strings reads with the 255/6 default and `fromV2`; a new record round-trips four; the old key is
byte-unchanged after a `persistIfAbsent`; `removeItem` still never called (the existing scan).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-07-01: The tuner publishes both halves; the store writes, restores and clears four</name>
  <files>src/lib/tune/model.ts, src/lib/tune/model.spec.ts, src/lib/device/install.svelte.ts, src/lib/device/install.spec.ts</files>

  <read_first>
    - `src/lib/tune/model.ts:195-235`, `:450-470`, `:670-690`, `:780-830` - `ConfigStrings`, `TunerOptions`,
      `land`, `landLua` and the preset route
    - `src/lib/device/install.svelte.ts` in full as 13-12 left it - `#pageInit` (`:759-770`),
      `tryOnDevice` (`:905-925`), `clear` (`:975-1045`), `#snapshot` (`:630-700`), `#classify` (`:1241-`),
      and **13-12's page-target additions**
    - `src/lib/device/install.spec.ts` - the 23 tests; the leg ordering; the three-write order test
    - `12-03-SUMMARY.md` - "the ONE place the empty string becomes SYSTEM_DEFAULT_SETUP" and why
    - `13-12-SUMMARY.md` - the classifier's states and the store's shape as landed
  </read_first>

  <action>
Per the interfaces block. **Keep 13-12's lines byte-for-byte** where the fourth string does not pass
through them. **`install.spec.ts`**: the three-write assertions moved to four in place; the new test as
described; `wire-pin.spec.ts` is plan 08's and may stay red for one wave - name it.

**Negative checks:** land the library's Timer for a preset in a scratch copy of `model.ts` - `model.spec`
red and `ladder.spec.ts` still green (the rule is about defaults, not the library); write 255/0 before
255/6 by reordering the `ConfigSet` literal - nothing changes (the order is `SLOTS`'), and the SUMMARY says
that is the point; drop `#pageTimer`'s substitution so a preset lands `""` - the new install test red on
CLEAR-vs-preset parity. Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/tune/model.spec.ts src/lib/tune/ladder.spec.ts src/lib/device/install.spec.ts</automated>
    `model.spec.ts` **+1**; `install.spec.ts` **+1** with every three-write assertion now four;
    `ladder.spec.ts` green and unedited.
  </verify>

  <done>
A Lua entry lands both halves and a preset lands both empties, the store substitutes both defaults in
one place each, TRY / PUT BACK / CLEAR write four in `SLOTS` order, the classifier names four and the
impossible partial is still impossible, 13-12's states untouched; three negative checks red and restored.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-07-02: The snapshot's next key beside the old, older records restoring the 255/6 default</name>
  <files>src/lib/device/snapshot.ts, src/lib/device/snapshot.spec.ts, src/lib/device/install.svelte.ts</files>

  <read_first>
    - `src/lib/device/snapshot.ts` in full **as 13-12 left it** - the header's numbered rules, the
      two-keys paragraph, `readSnapshot`, `persistIfAbsent`, and any key 13-12 added
    - `src/lib/device/snapshot.spec.ts` - test 1 (zero specifiers), the v1-beside-v2 tests, the
      `removeItem` scan
    - `12-03-SUMMARY.md` - the v1-beside-v2 precedent, quoted not paraphrased
  </read_first>

  <action>
Per the interfaces block, with the key named after reading 13-12's SUMMARY. `install.svelte.ts`'s
`#snapshot` passes both defaults and publishes `snapshotFromV2` beside `snapshotFromV1` (the panel's
`install-snapshot` line - plan 08's dev page reads it). **`snapshot.spec.ts` +1** as described; the
existing tests moved to four strings in place.

**Negative checks:** let `persistIfAbsent` write the old key - the "old key byte-unchanged" clause red;
return an older record without the flag - red on `fromV2`; add an `import type` - test 1 red. Restore,
sha256 both sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/device/snapshot.spec.ts src/lib/device/install.spec.ts && npm run check 2>&1 | grep -Ei "error|warning"</automated>
    `snapshot.spec.ts` **+1**; check `0 ERRORS 0 WARNINGS` again (plan 06's named errors closed, except
    `wire-pin.spec.ts` if it is a type error there - name it for plan 08).
  </verify>

  <done>
Four strings under the next key beside the old, the old read and never written or deleted, an older
record restoring the 255/6 default with a published flag, zero imports kept; three negative checks red
and restored.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`, or names `wire-pin.spec.ts`
  alone for plan 08; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs <observed files> <observed tests + 3>` with
  `wire-pin.spec.ts` named if red - **this plan's term is `+0 / +3`, written out** with its parts.
- `git diff --quiet -- src/lib/transport/sequence.ts src/lib/transport/sequence.spec.ts` - the one
  writer unmoved by this plan.
- `grep -c "test(" e2e/*.e2e.ts` summed, unchanged.
- `git diff --stat HEAD -- src/vendor/` empty; `git diff --quiet -- .planning/ROADMAP.md`.
</verification>

<success_criteria>
1. Four strings written, restored and cleared through the one writer, with the classifier naming four.
2. The tuner publishes both library halves for a Lua entry and both empties for a preset, and the store
   is the one place they become defaults.
3. The snapshot's next key holds four; older records restore the 255/6 default and say so.
4. 13-12's page-target machinery is byte-untouched where the fourth string does not pass through it.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-07-SUMMARY.md` carrying: the
observed baseline and `+0 / +3` with its parts; **the snapshot key named** and how each older record
reads; the classifier's four labels and the partial it names; CLEAR's four defaults; the `WRITABLE_PHASES`
identity; `wire-pin.spec.ts`'s state; the negative checks; **the sentence 13-17 reads**: `landLua`
publishes four fields, the store cannot tell a producer apart, and the frame count is four (five with
255/4); and the sentence that no device was touched.
</output>
