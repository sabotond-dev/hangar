---
phase: 12.1-gradient-touch
plan: 08
type: execute
wave: 8
band: 2
depends_on: ["12.1-07"]
files_modified:
  - src/lib/device/install-copy.ts
  - src/lib/device/install-copy.spec.ts
  - src/lib/device/wire-pin.spec.ts
  - src/routes/dev/install/+page.svelte
  - e2e/fake-zona.ts
  - e2e/install.e2e.ts
  - e2e/session.e2e.ts
  - docs/INSTALL-RUNBOOK.md
  - src/lib/ui/intro/HeroSurface.svelte
  - src/lib/ui/Coverflow.svelte
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-05, SAFE-07, PREV-02]
count_term: "PREV_FILES + 0 / PREV_TESTS + 0 (install-copy.spec's sentence moves, wire-pin pins both strings - written out as +0); e2e + 0 / + 0 titles and runs, the suite RUN in chunks with every count of three inside the titles now four; runbook rows A-H + 2 (I, J); sweep 4 19 unmoved by declaration"

must_haves:
  truths:
    - "The bytes that reach 255/0 and 255/6 are the two exported library strings verbatim, pinned; a connect issues four fetches and zero writes; one RAM leg is four acknowledged writes in the order 255/6, 255/0, 0/6, 0/0 - proved in a browser against the fake ZONA."
    - "The KEEP confirmation names what it now replaces - four scripts - and the partial-landing sentences name which of four landed, in the shipped register, handed to 13-18 by name."
    - "The preview's y axis goes through the measured map on the one canvas that survives Phase 13's waves 8-11, and the case of the other is recorded rather than lost."
    - "The two hardware rows this phase cannot answer are written, lettered and unanswered: the page-load proof the user may decline (D-04), and the four-string CLEAR / PUT BACK with a pre-12.1 record."
  artifacts:
    - path: "src/routes/dev/install/+page.svelte"
      provides: "the fourth textarea install-system-timer, first in write order"
      contains: "install-system-timer"
    - path: "src/lib/device/wire-pin.spec.ts"
      provides: "255/0 pinned to TOUCH_LIBRARY and 255/6 to TOUCH_LIBRARY_TIMER, four writes in order"
      contains: "TOUCH_LIBRARY_TIMER"
    - path: "docs/INSTALL-RUNBOOK.md"
      provides: "rows I and J"
      contains: "| **I** |"
  key_links:
    - from: "e2e/install.e2e.ts"
      to: "e2e/fake-zona.ts"
      via: "zona.seen(CONFIG, FETCH) at 4; the fake's second RAM holding 255/6"
      pattern: "toBe\\(4\\)"
    - from: "src/lib/ui/intro/HeroSurface.svelte"
      to: "src/lib/sim/touch.ts"
      via: "mapAxis(..., \"y\") on the second line"
      pattern: "\"y\"\\)"
---

<objective>
The browser half of the fourth string, the sentence that names it, and the two rows only a ZONA answers.

**Everything below the store landed at plan 07; this plan proves it where a browser can see it.** The
probe page `/dev/install/` gets a fourth textarea, first in write order, because it is "the sure route
for pasting an arbitrary page init at a module" (its own header) and the page-load proof (D-04) needs a
way to put a library into 255/6 without the tuner. The fake ZONA already models two RAMs (12-02); it
gains nothing structural - `synthetic.ts` answers 255/6 with the package default and stores a write under
its event (plan 06 verified the round trip) - and the e2e needles that count three become four: `CONFIG/
FETCH` **4**, frames on the wire **5** (the serial and four fetches), a RAM leg **four** writes, the
refetch rounds four. **`install.e2e.ts`'s by-class zero-writes proof stays zero**, with 13-12's
`PAGEACTIVE/EXECUTE` needle in its list.

**`wire-pin.spec.ts`** pins the landed 255/0 to `TOUCH_LIBRARY` today (12-07 deviation 1). It now pins
255/6 to `TOUCH_LIBRARY_TIMER` and asserts both are their own minified form, and reads four writes in
order at `:439`.

**The sentence** (D-08). SAFE-05's confirmation - held from both sides by `install-copy.spec.ts`'s
`AMENDED_BY_THE_THIRD_SCRIPT` - reads *"This replaces the Setup and Timer scripts on your ZONA's touch
element and the page's own init script, and it survives a power cycle."* Four strings make it
untrue-by-omission. It gains one clause in the **shipped** register (13-18 rewrites every one of these
under D-05 and must carry the four-string fact; the hand-off is in `12.1-VALIDATION.md`), and the constant
becomes `AMENDED_BY_THE_FOURTH_SCRIPT`.

**The one token** (D-21): `HeroSurface.svelte:137` gets `, "y"` now that 13-07..13-11 have landed;
`Coverflow.svelte:520` gets it only if 13-09 left the file alive.

Purpose: SAFE-03 and SAFE-07 proved over four in a browser; the user told what a KEEP replaces; the
bench given its two rows.
Output: one textarea, one pin, one sentence, two rows, one token, the suite run. Term `+0 / +0`,
runbook `+2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-07-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-05-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-12-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-07-SUMMARY.md
@.planning/phases/12-touch-framework/12-03-SUMMARY.md
@.planning/phases/12-touch-framework/12-12-SUMMARY.md
@src/lib/device/install-copy.ts
@src/lib/device/install-copy.spec.ts
@src/lib/device/wire-pin.spec.ts
@src/routes/dev/install/+page.svelte
@e2e/fake-zona.ts
@e2e/install.e2e.ts
@e2e/session.e2e.ts
@docs/INSTALL-RUNBOOK.md

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (12-12 deviation 10). **No
  emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Rows I and J are the user's; this plan
  does not claim the page-load proof works.
- **Sibling repositories read-only; `src/vendor/` untouched; no manifest row.**
- **Do not edit `.planning/ROADMAP.md`.** Nothing under `13-gui-overhaul/` or `12-touch-framework/` -
  **`13-COPY-NEW.md` included**: the sentence is quoted in this plan's SUMMARY and handed to 13-18 through
  `12.1-VALIDATION.md`.
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress`.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push. **Do not commit while the suite is running**
  (`artifacts.e2e.ts` reads `HEAD`).
- **Append-only:** `docs/INSTALL-RUNBOOK.md` - rows I and J appended to the checklist table, the "Seven
  rows" sentence amended by a dated line beneath it rather than rewritten; no row reflowed.
- **The `/dev/install/` page names exactly two static specifiers** (`config-shape.spec.ts` test 13); the
  fourth textarea adds none. Its one typed literal rule (the page-init starting value) extends to the
  fourth box with the same reason.
- **`install-copy.ts` is on the first paint of `/` with zero imports**; the sentence changes, the shape
  does not.
- **State every count as a carried name plus a delta, re-measured at this plan's start**, then assert
  **`+0 / +0`**, written out, and e2e **`+0 / +0`** titles and runs with the suite run.
- **Playwright's transients, and the rule for each** (`12.1-VALIDATION.md`): wrangler 4.128.0 dies with
  an empty `ProxyController` error after about 85 s however it is started - **run the suite in five file
  chunks on fresh detached servers** (`e2e-chunks.sh` in the session scratchpad, 13-07 deviation 9) and
  sum the `N passed` lines; `webkit-phone`'s tail `Could not connect to server` is the same death, rerun
  the chunk; `session.e2e.ts:701` (`D-12-12-a`) rerun alone with `--workers 1` before it is read as a
  regression; `install.spec.ts` / `config-shape.spec.ts` timeouts under low memory rerun the file alone;
  free memory recorded beside every run; `test-results/` removed after. A partial red from a dead server
  is not the suite.
- **Concurrency:** 13-08 had `session.e2e.ts` open for the `/c/` -> `/playground/` re-aim; it has landed
  by now (Band 2 follows 13-12). **Re-read every e2e file immediately before editing and edit needle by
  needle, never by rewrite.** 13-17 (later) reads `install.e2e.ts` and `fake-zona.ts`; 13-18 (later)
  rewrites `install-copy.ts`.
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## `/dev/install/+page.svelte`

A fourth `$state` and textarea, **first** in the page's write-order list and in the DOM:

```svelte
let systemTimer = $state("--[[@cb]]--[[page timer]]");
const pair = () => ({ systemTimer, system, setup, timer });   // still called pair(); four strings now
<textarea data-testid="install-system-timer" bind:value={systemTimer} rows="2"></textarea>
```

The header's "THE THREE strings TRY ON DEVICE writes come from three textareas" paragraph becomes four
with the sentence that the first box is 255/6 and goes on the wire first; `install-snapshot`'s line
shows the fourth length and the `fromV2` flag beside `fromV1`.

## `e2e/install.e2e.ts` and `e2e/session.e2e.ts` - the needles that move

| Needle (as of 12-12) | Becomes |
|---|---|
| `zona.seen("CONFIG", "FETCH")).toBe(3)` (`:444`) | `4` |
| "Four frames, not three: the serial and the three fetches" / `length).toBe(4)` (`:448-449`) | five / `5` |
| "One RAM leg, three writes" (`:521`) and every "Two RAM legs, three writes each" | four |
| "The snapshot s three, plus one refetch round of three" (`:597`, `:634`) | four |
| `refetch-setup` / `refetch-timer` `toHaveLength(3)` (`:630-631`) | unchanged (three ROUNDS), plus `refetch-system-timer` at 3 |
| "FIVE, NOT SIX: the page init once, the Timer once, the Setup three" (`:678`) | six, not seven |
| `readout(page, "install-system")` block (`:290`) | gains `systemTimer` from `install-system-timer` |
| the fake's SECOND RAM read of 255 (`:524-526`) | reads 255/6 as well |
| `session.e2e.ts:315` "all three config reads" | four; the timer fetch is still the last chunk |

The zero-writes proof: `CONFIG/EXECUTE` **0**, `PAGESTORE/EXECUTE` 0, `HEARTBEAT/EXECUTE` 0, and 13-12's
`PAGEACTIVE/EXECUTE` 0 - the needle list one longer since 13-12 and unchanged here; `WRITE_CLICKS`'s length
unchanged (no new click - a fourth string is not a fifth click).

## `wire-pin.spec.ts`

`:374` `toEqual({ systemTimer: TOUCH_LIBRARY_TIMER, system: TOUCH_LIBRARY, ...rendered })`; `:388-398`
both landed strings equal their exports and their own minified forms; `:439` `const [systemTimer, system,
timer, setup] = writes;` with the ACTIONSTRING of each asserted; `:505` the preset's `systemTimer: ""`.
Five tests, count unchanged.

## `install-copy.ts`

The confirmation sentence gains one clause: *"This replaces the Setup and Timer scripts on your ZONA's
touch element and the page's own init and timer scripts, and it survives a power cycle."* -
`AMENDED_BY_THE_FOURTH_SCRIPT` in the spec, holding it from both sides as the third did. The partial
sentences (`"Timer reached your ZONA and Setup did not."` and its siblings) are built from `SLOTS[].label`
if they are built, or listed for four if they are literals - read the file and say which; either way the
spec asserts the four names in write order. **Quote the new sentence verbatim in the SUMMARY** for 13-18.

## Runbook rows

`| **I** | The library's second half on a page load (12.1 D-04; SAFE-05) _(the user may decline)_ | **Only if you are willing to store the library to flash once; PUT BACK restores it and CLEAR + KEEP leaves nothing behind.** On EUCLID, connected: TRY ON DEVICE, wait for PLAYING NOW, touch the pad to see the gradient. KEEP ON DEVICE, wait for KEPT. Pull the cable, wait five seconds, plug it back in. **Before HANGAR reconnects**, tap a ring cell. Does the finger light as a gradient and the step toggle? Then reconnect and PUT BACK. **If the tap after the power cycle does nothing and the module's LEDs show the alert flash only**: report that, and if a MIDI monitor is open report whether a print reached it. | Whether the system Setup's self:tim() defines the second half on a PAGE LOAD - not on a write - was never tapped (research B.6). A write runs the body; only a KEEP and a power cycle run init.lua. |`

`| **J** | Four strings: CLEAR, PUT BACK, and a record from before 12.1 (SAFE-03, SAFE-07, 12.1 D-22) | Connected on any Lua card: CLEAR - the pad returns to the firmware's own touch default (the dim white finger halo), and nothing prints anywhere; then TRY ON DEVICE and PUT BACK - your own configuration back, by eye. If this browser has a snapshot from before 12.1 (a KEEP or TRY before today), connect with it: does the panel say the record predates the timer slot, and does PUT BACK still restore? Write down the four lengths install-snapshot shows. | CLEAR now writes print("tick") into 255/6 and a KEEP after it must store nothing there (cfg_default_flag); the panel cannot see flash. |`

## The token

`src/lib/ui/intro/HeroSurface.svelte:137`: `y: mapAxis(event.clientY - rect.top, rect.height, engine.coordMax, "y"),`.
`src/lib/ui/Coverflow.svelte:520`: the same **only if the file exists** (`test -f`); if 13-09 deleted it,
record that and touch nothing.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-08-01: The sentence that names four, and the pin over both strings</name>
  <files>src/lib/device/install-copy.ts, src/lib/device/install-copy.spec.ts, src/lib/device/wire-pin.spec.ts</files>

  <read_first>
    - `src/lib/device/install-copy.ts` - the confirmation block, the partial-landing sentences, and
      whether they are literals or built from labels
    - `src/lib/device/install-copy.spec.ts:150-170`, `:415-455` - `AMENDED_BY_THE_THIRD_SCRIPT` and the
      two directions it is held from
    - `src/lib/device/wire-pin.spec.ts:230-250`, `:370-400`, `:435-445`, `:500-510`
    - `12-12-SUMMARY.md` "The four amendments" - SAFE-05's shipped sentence, verbatim
    - `13-CONTEXT.md` D-05 - why the register is NOT changed here
  </read_first>

  <action>
Per the interfaces block. **Negative checks:** revert the clause in a scratch copy - the spec red naming
the fourth script from both directions; swap the two library strings in the tuner's landing (scratch
copy of `model.ts`) - `wire-pin` red on `:388`'s identity. Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/device/install-copy.spec.ts src/lib/device/wire-pin.spec.ts src/lib/config-shape.spec.ts</automated>
    `install-copy.spec.ts` count unchanged with the constant renamed; `wire-pin.spec.ts` **5**, both
    strings pinned; `config-shape.spec.ts` green (the copy module still imports nothing).
  </verify>

  <done>
The KEEP confirmation names four scripts and is held from both sides; the partial sentences name four in
write order; the landed 255/0 and 255/6 are pinned to the two exports verbatim and the four writes read
in order; two negative checks red and restored.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-08-02: The fourth textarea, the fake, and every count of three inside the titles becoming four - the suite run in chunks</name>
  <files>src/routes/dev/install/+page.svelte, e2e/fake-zona.ts, e2e/install.e2e.ts, e2e/session.e2e.ts</files>

  <read_first>
    - `src/routes/dev/install/+page.svelte` in full - the header's rules, `pair()`, the readouts
    - `e2e/fake-zona.ts:100-130`, `:200-230` - the state shape and the 255 fetch condition 12-02 added
    - `e2e/install.e2e.ts:1-140` (the block map and the slow tests), `:280-300`, `:436-460`, `:484-560`,
      `:590-640`, `:670-690`, `:720-770`, `:800-810`, `:840-930` - every count of three
    - `e2e/session.e2e.ts:255-320` - the poll helper and its message
    - `13-12-SUMMARY.md` - the zero-writes needle list as extended and the fake's active-page model
    - `13-07-SUMMARY.md` deviation 9 and `e2e-chunks.sh` - the chunk method
  </read_first>

  <action>
**1.** The textarea per the interfaces block. **2.** `fake-zona.ts`: verify 255/6 is answered from the
second RAM and stored on write (it is `synthetic.ts` underneath - read the fetch condition at `:207-220`
and widen it to event 6 only if it names event 0). **3.** Every needle in the table, one at a time, each
re-read before the edit. **4.** `npm run build`, then the suite in five chunks on detached servers, the
`N passed` lines summed; then the whole thing a second time if any chunk needed a rerun. Titles counted
with `grep -c "test("` summed over `e2e/` - unchanged.

**Negative checks:** leave `CONFIG/FETCH` at 3 in a scratch copy - the connect test red on the count;
put the fourth textarea after `install-system` in `pair()`'s object - nothing changes (the order is
`SLOTS`'), and the SUMMARY says so.
  </action>

  <verify>
    <automated>npm run build && bash "$SCRATCH/e2e-chunks.sh" 2>&1 | grep -E "passed|failed|chunk"</automated>
    Five chunks green, summing to the observed run count (94 at `91ab755`; 13-12 and 13-13 add `+1 / +1`
    each - **recompute from 13-13's and 13-12's SUMMARYs and write the sum out**); `grep -c "test("` at
    the observed title count - e2e `+0 / +0`.
  </verify>

  <done>
The probe page has four boxes in write order, the fake holds 255/6 in its second RAM, every count of
three inside the titles reads four, the zero-writes proof is still zero with 13-12's needle in place,
and the suite is green in chunks at the observed count with every transient handled by its rule.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-08-03: Rows I and J, and the y token</name>
  <files>docs/INSTALL-RUNBOOK.md, src/lib/ui/intro/HeroSurface.svelte, src/lib/ui/Coverflow.svelte</files>

  <read_first>
    - `docs/INSTALL-RUNBOOK.md:150-178` - the table's shape, row H's conditional form, the two notes
      under it
    - `12.1-CONTEXT.md` D-04, D-21, D-22
    - `12.1-05-SUMMARY.md`'s sentence for this plan
    - `src/lib/ui/intro/HeroSurface.svelte:130-140`; `test -f src/lib/ui/Coverflow.svelte`
  </read_first>

  <action>
Rows I and J appended per the interfaces block, inside the column widths; a dated line under the "Seven
rows" paragraph: "Nine rows since 12.1-08 (2026-09-11): I is the page-load proof the user may decline, J
the four-string CLEAR / PUT BACK." The token on `HeroSurface.svelte:137`; `Coverflow.svelte:520` only if
the file exists. `npm run check` 0 / 0; `npm run test:quick` unchanged (the intro's unit tests do not
drive the pointer).

**Negative check:** none that edits source; the runbook's shape is asserted by reading it back through
the same regex `audition.spec.ts` uses for its table, in a scratch script, counting **ten** lettered rows.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -Ei "error|warning" && grep -c "^| \*\*[A-J]\*\* |" docs/INSTALL-RUNBOOK.md</automated>
    `0 ERRORS 0 WARNINGS`; ten lettered rows.
  </verify>

  <done>
Rows I and J are written, lettered, unanswered, with I marked as the row the user may decline; the y axis
of the surviving canvas goes through the map; the other canvas's case is recorded.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs <observed files> <observed tests>` - **this
  plan's term is `+0 / +0`, written out**; Band 2's three terms sum to `+0 / +5`, stated here once.
- `npm run build`; the e2e suite in chunks, summed, at the observed run count; `grep -c "test("` at the
  observed title count - **e2e `+0 / +0`, written out**.
- `npm run test:sweep` not run - no Lua moved; `4 19` by declaration.
- `git diff --numstat -- docs/INSTALL-RUNBOOK.md` - insertions and at most re-aligned rows.
- `git diff --stat HEAD -- src/vendor/` empty; `git diff --quiet -- .planning/ROADMAP.md`; nothing under
  `.planning/phases/13-gui-overhaul/` in `git status`.
</verification>

<success_criteria>
1. Four fetches, zero writes on a connect; four acknowledged writes per RAM leg in order - in a browser.
2. Both library strings pinned to the wire verbatim.
3. The KEEP confirmation names four scripts; the sentence is quoted for 13-18.
4. Rows I and J are the bench's, written and unanswered; the y token is on the surviving canvas.
5. The suite is green in chunks at the observed count, every transient handled by its rule.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-08-SUMMARY.md` carrying: the
observed baseline and `+0 / +0`; Band 2's sum `+0 / +5`; the e2e chunk table with memory beside each
and every rerun named; the needle table before and after; the confirmation sentence verbatim (**for
13-18**); rows I and J as written; whether `Coverflow.svelte` existed; the negative checks; and the
sentence that no device was touched and the page-load proof is not claimed.
</output>
