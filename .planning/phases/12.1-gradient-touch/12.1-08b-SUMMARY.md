---
phase: 12.1-gradient-touch
plan: 08b
subsystem: catalog, sim, vendor, fidelity, tune, device
tags: [gradient, calibration, presets, K, Z, Y, N, G, touchLibrary, manifest, parity, mirror, wire, audition, band-2, D-26, D-27]

# Dependency graph
requires:
  - phase: 12.1-gradient-touch
    provides: plan 02 - the library over two slots (781 + 705), G reading U over the measured knots, LIBRARY_PARTS as the one copy, the uniform-join fixed-point assertion
  - phase: 12.1-gradient-touch
    provides: plan 03 - G's code-9 end test, the needle discipline, D (rate 250, step 6) as the house decay idiom
  - phase: 12.1-gradient-touch
    provides: plan 07 - ConfigStrings.systemTimer; the Lua route landing both library halves; the store's #pageInit / #pageTimer substitution in one place
  - phase: 12.1-gradient-touch
    provides: plan 08 - wire-pin.spec.ts's four-frame pin with the first two frames' bytes asserted; the question that this plan moves AURORA's empty-string assertion
  - phase: 12.1-gradient-touch
    provides: plan 08a - lua-smoke at THIRTY-SEVEN; the deferred item that library.ts's caller lists for N and G were stale
  - phase: 11-bench-corrections
    provides: plan 04 - the register for a vendored edit (a declared intendedDivergence row per contiguous hunk, the neighbouring line carried where a needle would match twice, the sentence that fixing the compiler alone would leave the preview lying)
provides:
  - src/lib/catalog/library.ts - Z (the block origin and the two fractions, 111), Y (one corner's weight, 70), K (the decaying bilinear stamp through D, 176), G through Z and Y (221, identical output), N in 255/0; 255/0 842 / 66 free, 255/6 873 / 35 free; twenty-one globals; LIBRARY_VERSION "1"
  - src/vendor/botor/_pad.ts - PadState.touchLibrary; the touch handler emitting K(x,y,1,252) for the comet, K(x,y,1,252,A-i*B,i*B,C) for the per-finger trail, G(s,i,e,x,y,1,r,g,b) for the glow, N(x,y) for the zones and the faders when the field is set; byte-identical output when it is not - eight declared manifest rows
  - src/vendor/botor/pad-sim.ts - the mirror (calibrated, libraryRaw, libraryCell, libraryBlock, libraryWeight, libraryStamp, libraryFinger; the three touchPaint cases, touchZone, fadersBody, libraryBlocks); ledTick untouched - eight declared manifest rows
  - src/lib/fidelity/upstream-manifest.json - 22 -> 38 intendedDivergence rows on two files, each with the reason, plan 12.1-08b and 2026-09-11; both pristine sha256 reconstruct
  - src/lib/catalog/presets.ts - state.touchLibrary = { kx: KX, ky: KY } on all nine; the eight costs the compiler's own (361 / 413 / 349 / 391 / 491 / 565 / 525 / 592; tpad 902)
  - src/lib/catalog/divergence.ts - nine state.touchLibrary rows, eight cost rows (five moved, three new)
  - src/lib/tune/model.ts - a preset lands TOUCH_LIBRARY_TIMER and TOUCH_LIBRARY exactly as a Lua entry does; the library resolved once per tuner in buildTuner
  - src/lib/fidelity/lua-parity.spec.ts 5 -> 6 - the eight HANGAR presets under a finger, the compiler's Lua beside the library in wasmoon against a live PadSim, 36,936 layer records equal after every sample
  - src/lib/catalog/library.spec.ts 5 -> 6, src/lib/sim/lua-smoke.spec.ts 37 -> 38, src/lib/catalog/host-surface.spec.ts (fifty sites, twenty-one names)
  - docs/HARDWARE-AUDITION.md row 28 (the eight preset cards, clauses (a)-(f)) with the dated scope line; audition.spec.ts ROW_COUNT 28
affects: [12.1-09 (the gate reads the manifest 6 / 38, the library 842 / 873, the eleventh term +0 / +3, row 28, the deferred fader question), 13-14 (twenty-one names; the emitter may call K, Z, Y; library.ts unchanged from here on), 13-15 (K and Z exist), 13-17 (every card lands the library; the preset landing publishes both strings - superseding 12.1-VALIDATION's hand-off line), 13-19 (arc.ts's stale N-caller comment), 13-20 (the 12.1 offset's manifest term is +16 rows on two files)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A vendored edit is gated on ONE optional state field and mirrored in both engines from that field: PadState.touchLibrary is read by the compiler as 'emit library calls' and by the simulator as 'these are the knots to mirror', so the two cannot disagree, and every state without it - the vendored shelf included - compiles and simulates byte-identically (proved by diffing the nine vendored PRESETS' and defaultState()'s emitted Lua before and after, and by three fixtures' sha256)"
    - "A vendored file imports nothing from src/lib/: the library's U is transcribed into pad-sim.ts (calibrated) with the knots taken from the state, and lua-parity test 6 is what holds the transcription to the Lua - the compiler's own emitted Setup running in wasmoon beside the library against the mirror, 243 records after every sample"
    - "A stamp lands on 0 by quantising its start, not by choosing its timeout: K computes w*weight//4096 then //6*6 and hands the pair to D, so every cell's walk at rate 250 freezes on exactly 0; the zero-weight skip (if z>0) is what keeps a finger dead on an LED from writing D(n,l,0) over a neighbour's decaying trail"
    - "Manifest rows are generated from the diff, not typed: one row per hunk with two context lines as the anchor, each vendored text asserted to occur exactly once in the vendored body and each upstream text exactly once in HEAD's body before the reasons are attached - the rule vendored-diff.spec.ts applies, applied first"
    - "A reserve that pushes a card over 908 is re-derived when the card's cost moves: DIAL 646 -> 592 took three specs' { setup: 300 } from -38 over to inside; 354 reproduces the same -38 so every downstream figure holds, and the arithmetic is written beside the constant"

key-files:
  created: []
  modified:
    - src/lib/catalog/library.ts
    - src/lib/catalog/library.spec.ts
    - src/lib/catalog/host-surface.spec.ts
    - src/lib/sim/lua-smoke.spec.ts
    - src/lib/sim/lua-host.spec.ts
    - src/lib/sim/lua-host.ts
    - src/vendor/botor/_pad.ts
    - src/vendor/botor/pad-sim.ts
    - src/vendor/botor/VENDOR.md
    - src/lib/fidelity/upstream-manifest.json
    - src/lib/fidelity/vendored-diff.spec.ts
    - src/lib/catalog/presets.ts
    - src/lib/catalog/divergence.ts
    - src/lib/catalog/presets.spec.ts
    - src/lib/tune/reachability.sweep.spec.ts
    - src/lib/tune/model.ts
    - src/lib/tune/model.spec.ts
    - src/lib/tune/ladder.spec.ts
    - src/lib/tune/surprise.spec.ts
    - src/lib/tune/knobs.preset.spec.ts
    - src/lib/device/wire-pin.spec.ts
    - src/lib/device/install.spec.ts
    - src/lib/fidelity/lua-parity.spec.ts
    - docs/HARDWARE-AUDITION.md
    - src/lib/catalog/audition.spec.ts
    - .planning/phases/12.1-gradient-touch/deferred-items.md

key-decisions:
  - "D-27 'mirror' recorded (the user, 2026-09-11, through the orchestrator) and not re-asked: compiler and simulator both under manifest rows, presets landing the library"
  - "N moves to 255/0 because 255/6 with N measures 934 (26 over) and N is the map, which the split rule puts in 255/0 anyway; D to 255/0 instead (891 / 850) measured and not taken because D writes LEDs"
  - "K is gated on brush 1 and rate 250 (D's rate) in both engines; the wide brush and any other rate keep the naive cell - trailMs is no knob and 420 on all nine, asserted in presets.spec.ts inside the walk"
  - "The library is resolved once in buildTuner rather than awaited inside measurePadsim, so the preset landing stays synchronous after compileState + costOf and the two specs that flush with 64 microtask hops keep flushing; the import is still dynamic and buildTuner is itself reached through a dynamic import"
  - "JOYSTICK is declared at 491, not the planner's 492: the substitution harness read max(raw, compressed) over one space the emitter's joiner never writes; the compiler's own canonical figure is 491 and presets.spec.ts test 4 holds it"
  - "divergence.ts imports KX and KY from calibration.ts (its header said 'imports NOTHING'): a row has to hold the value it declares and typing the eighteen numbers would be the second copy D-02 forbids; the header now says which one thing it imports and why"
  - "FOUR FADERS' level stays raw (D-14 extended, R-22): the calibrated form measures 540 against 525 (+15) and is recorded in deferred-items.md for row 28(d)'s answer"
  - "The vendored headers' 'Modified for HANGAR:' lines were not edited: 11-04's wording already defers to the manifest as the authority ('this line is not a second copy of it'), and the sentinel must stay within the first twelve lines"

requirements: [CONT-02, CONT-03, PREV-01, PREV-02, SAFE-03, SAFE-07]
requirements-completed: []

# Metrics
duration: about 105 min of execution (21:24Z-23:10Z: the baseline runs, four tasks, three sweeps, four whole quick runs, eight negative checks) plus this document and STATE.md
completed: 2026-09-12
---

# Phase 12.1 Plan 08b: The eight presets onto the gradient Summary

**The eight preset cards draw a finger where the LEDs are and as a gradient, and the preview agrees
with the device.** The touch library grew `Z`, `Y` and `K` (the decaying bilinear stamp through
`D`, every start quantised to a multiple of 6 so every cell lands on exactly 0), `G` was re-written
through `Z` and `Y` with identical output at 361 sampled points, and `N` moved to 255/0: **842 / 66
free + 873 / 35 free**, both fixed points, twenty-one globals. The vendored compiler emits
`K(x,y,1,252)` for the comet (AURORA, STARFIELD, RADAR, DIAL), `K(x,y,1,252,255-i*60,i*60,128)` for
PINWHEEL, `G(s,i,e,x,y,1,255,187,0)` for JOYSTICK's glow and `N(x,y)` for NINE PADS' zones and FOUR
FADERS' rails when a state carries `touchLibrary`, and the vendored simulator mirrors the same
measured map - **sixteen declared manifest rows** on two files (22 -> 38), both pristine sha256
reconstructing, `ledTick`'s extract at the same sha256 before and after, the vendored shelf compiling
byte-identically, the three fidelity fixtures byte-unchanged, the oracle and the vendored tests
unedited. The eight costs at the defaults are the compiler's own: **361 / 413 / 349 / 391 / 491 / 565
/ 525 / 592**, tpad 902; the sweep costed all 44,078 reachable states and every one fits. A preset
lands the library's two halves like every other card, pinned on the wire. The proof the mirror is
right is `lua-parity` test 6: the compiler's own Lua beside the library in wasmoon against a live
`PadSim`, **36,936 layer records equal after every sample** on all eight. Row 28 appended, unanswered.
Term **`+0 / +3`** on the observed 90 / 933 (+1 todo): **90 / 936 (+1 todo)**, twice green whole.
Audition 27 -> **28**. Only the gate (12.1-09) remains.

## Task 12.1-08b-01: the decision, recorded (2026-09-11) - not asked

The checkpoint's question - whether D-26 item 2 lands as designed (compiler and simulator under
manifest rows, one state field, presets landing the library), compiler-only, or not at all - was
put to the user by the orchestrator before this plan ran and answered on 2026-09-11, recorded in
`12.1-CONTEXT.md` as **D-27**:

> *"mirror"*

Recorded here verbatim with its date and source, the 12.1-01 / D-03 way; nothing was asked. The
plan proceeded on the `mirror` branch: both `_pad.ts` and `pad-sim.ts` edited through declared rows
in `src/lib/fidelity/upstream-manifest.json`, each with its reason; the preset landing publishing
both library strings exactly as a Lua entry does; the simulator's phase walk proved untouched by the
hash of its extract; `firmware-oracle.spec.ts` green and unedited. `grep -c "12.1-08b-01"` over this
file reads at least one.

## The baseline, observed at start and at close

HEAD `608a7aa` at start. The planner's harness (`measure-12.1-d26.mjs`, its GHOST section cut because
12.1-08a had already landed GHOST) reproduced every figure before an edit was made: the library 781 +
705; the eight presets 415 / 477 / 403 / 445 / 543 / 550 / 520 / 646 and tpad 902; the revision 842 /
66 + 873 / 35; `255/6` with `N` 934; the K2 deltas -54 / -64 / -54 / -54 / -51 / +15 / +5 / -54; the
wasmoon readings (below); `G'` against the shipped `G` 0 of 361 differ; `N` at the 81 centres 0 wrong.

| Count | Start (observed) | Close | Delta |
| --- | --- | --- | --- |
| quick suite | **90 / 933** (+1 todo) at `608a7aa` | **90 / 936** (+1 todo), twice green whole (`check-counts 90 936` on two `--maxWorkers=2` runs; the two default runs before them each had one named transient - below) | **`+0 / +3`**: `library.spec` 5 -> 6, `lua-smoke` 37 -> 38, `lua-parity` 5 -> 6 |
| check | 627 / 0 / 0 | 627 / 0 / 0 | 0 |
| lint | clean | clean | - |
| e2e | 80 titles / 96 runs | 80 by `grep -c "test("` over `e2e/*.e2e.ts`; 96 by `playwright test --list` | `+0 / +0`; the suite not run (no e2e file moved) |
| catalog | 26 = 8 + 18 | 26 (`catalog.spec`, `frames.spec`'s eight ported entries) | 0 |
| sweep | `4 19` | `4 19` RUN three times (task 02 green; task 03 red once on the margin literal below; green after it moved) | 0 |
| audition rows | 27 | **28** | +1 |
| manifest | 6 files, 22 rows | 6 files, **38** rows | **M = 16** (8 + 8) |
| library | 781 + 705 | **842 / 66 + 873 / 35** | +61 / +168 |
| fixtures | `preset-baseline.json` `187c31fc...`, `golden-frames.json` `bf54f15c...`, `frames.json` `9f9cd666...` | the same three, `sha256sum -c` OK after task 03 and at close | byte-unchanged |
| oracle | `firmware-oracle.spec.ts` `fe69dd51...`, `firmware-oracle.ts` `2a7865c3...` | the same; `git diff --quiet HEAD -- ... src/vendor/botor/tests/` holds | unedited |
| `ledTick` | lines 889-896, sha256 `3640e5853ce5c667c1b498a1cb1db10926be7722896552654b7398d4ded69017` | lines 893-900 (four lines inserted above it), the **same** sha256; `git diff -U0 -- pad-sim.ts \| grep -c pha` reads **0** | untouched |
| vendored files | `_pad.ts` `c8f4ccb3...`, `pad-sim.ts` `2651e236...` | `_pad.ts` `5efe88c5...`, `pad-sim.ts` `824ab64d...` - moved by the sixteen rows only; the reconstructed pristine bytes hash to the manifest's `4e2c7a49...` / the recorded pad-sim sha256 (`vendored-diff` green) | declared |

Band 2's four terms now sum to `+0 / +8`; the phase's eleven to `+1 / +25` on the chains' arithmetic
(the gate reconciles the offset against Phase 13's concurrent terms).

## Task 12.1-08b-02: the library (`56f8e89`)

**Both strings re-measured in this tree** under the pinned `compressScript` after `padReady()`,
canonical, `checkSyntax` true:

| Slot | Parts | Uniform join | Shipped | Free |
| --- | --- | --- | --- | --- |
| 255/0 | header 32 + map 65 + U 126 + W 86 + E 88 + Q 294 + X 74 + **N 60** + call 10 (nine parts, eight joins) | 843 | **842** | **66** |
| 255/6 | marker 9 + V 62 + **G 221** + **Z 111** + **Y 70** + **K 176** + A 150 + D 68 (eight parts, seven joins) | 874 | **873** | **35** |

The uniform join compresses to the shipped string on both slots (test 1's fixed-point assertion,
unchanged in form). `N`'s move: `V G' Z Y K N A D` measures **934**, 26 over - the reason, re-measured
here; `D` to 255/0 instead measures 891 / 850 and is not taken (D writes LEDs; the split rule). The
text of `N` did not change with its slot (test 6 asserts the literal). `LIBRARY_VERSION` stays **"1"**,
and the sentence says which it is: this revision ADDED `Z`, `Y`, `K`, CHANGED `G` and MOVED `N` - a
name the pair defines either way - so nothing left `_G`.

**`K`'s VM cases** (lua-smoke test 38, every expectation from `calibratedAxis`, never typed; the
harness's readings reproduced to the digit):

| Point | Raw | K's stamps (cell=start/timeout) | The naive comet would have stamped |
| --- | --- | --- | --- |
| dead on LED (4,4) | (64, 68) | 40=252/t42 | cell 40 |
| midway between LED 4 and 5 in x, row 4 | (74, 68) | 40=132/t22 41=114/t19 | cell 41 alone |
| the centre of four | (74, 75) | 40=72/t12 41=60/t10 49=60/t10 50=48/t8 | cell 50 alone |
| the raw corner (127,127) | (127, 127) | 80=252/t42 | cell 80 |
| the bench case, dead on LED (7,1) | (120, 12) | **16**=252/t42 | **cell 8** - the defect |

Plus: a finger dead on LED (5,4) beside a decaying trail at cell 42 left cell 42 at its own phase
(180/t30 after the sample) and wrote nothing to the zero-weight cells 50 and 51 - the `if z>0` skip;
`K` handed (255,0,0) set the four cells' stops to `glcStops(255,0,0,true)` and a `K` handed none left
them; `G` through `Z` and `Y` drew the TS twin's picture at all 81 LED centres and the midpoint.
`library.spec` test 6 drove `K` through the real host at 102 points (81 centres, sixteen midpoints,
the centre of four, four raw corners): 121 stamps, every start a multiple of 6, timeout start/6, every
one observed at [0, 0, 0] after sixty ticks; and `G` against the 12.1-03 text held as a literal
(352 characters, the one literal copy of a library function outside `library.ts`) over the raw plane
every 7 units: 361 samples, 29,241 layer-0 records equal, dark after the lift.

`host-surface.spec`: **fifty** call sites (forty before; `N`'s three moved to 255/0, `Z` brought five,
`Y` one, `K` six, `G'` seven where `G` had nine), twenty-one names, the resolved host set the same
seven (`K` reaches `glpfs` and `glt` through `D`, `glc` directly), the stranger names `J` / `JJ` / `KZ`
(`Z` is the library's now). EUCLID 726 and TRACKPAD's Timer 510 at the picker corner unmoved (their
call sites did not change); `lua-entries.sweep` inside the sweep green.

The header (12.1-08a's deferred item taken): `N`'s paragraph names ARC, GHOST twice, the compiler's
zone and fader emission and 13-15; `G`'s the eight, GHOST and the compiler's glow; `U`'s callers gain
`Z`; `D`'s gain `K`; section 2's revision paragraph; section 9's parts arithmetic; the firmware grep
extended to `Z`, `Y`, `K` across all fourteen `grid-fw/common/src/lua/*.lua` files (read-only): zero
matches.

## Task 12.1-08b-03: the compiler, the mirror, the manifest, the nine states (`dbfb3e7`)

**The gate proved first.** The nine vendored `PRESETS` and `defaultState()` compiled to the same Lua
before and after the `_pad.ts` edit (`shelf-before.lua` = `shelf-after-pad.lua`, sha256
`cf5236ce...`); `pad.test.js` 176 and `pad-sim.test.js` 96 green and unedited.

**The manifest rows, M = 16**, each `plan: "12.1-08b"`, `dated: "2026-09-11"`, generated from
`git diff -U2` with the two context lines as the anchor and every text verified to occur exactly once
on both sides before the reason was attached:

| # | File | Site | Reason, in one line |
| --- | --- | --- | --- |
| 1 | `_pad.ts` | `PadState.touchLibrary` | the one conduit for the gradient on the presets (D-26 item 2, D-27); absent on every BOTOR state so upstream's output is byte-identical; not encoded by `encodeStamp`; inlining measured 578 / Setup and does not fit |
| 2 | `_pad.ts` | `clonePadState` | the knots travel with the state through normalise / ground / withChange / decodeStamp, so a tuned copy still emits the calibrated calls |
| 3 | `_pad.ts` | `libraryOn` / `libraryXY` | the two helpers every site reads; a widened axis hands `N(x//8,y//8)` (12.1-04's precedent) |
| 4 | `_pad.ts` | `touchPaint` case comet | `K(x,y,1,252)` at brush 1 and rate 250, the naive cell otherwise; AURORA 415 -> 361, STARFIELD 403 -> 349, RADAR 445 -> 391, DIAL 646 -> 592 (-54, knob-independent) |
| 5 | `_pad.ts` | case perFinger | the same `K` handed the closed-form hue; PINWHEEL 477 -> 413 (-64) |
| 6 | `_pad.ts` | case glow | the parked dot doused then `G(s,i,e,x,y,1,r,g,b)` per contact; the re-park unchanged and after `G`; JOYSTICK 543 -> 491 (-52); two fingers no longer fight over one dot |
| 7 | `_pad.ts` | `zoneStatements` | `local n=N(x,y)` through `ledZoneTerm`, inverted axes as `8-c`, snake and columns through the same terms; NINE PADS 550 -> 565 (+15 at 4x4, +11 at 3x3); the user's row-2-column-8 finding |
| 8 | `_pad.ts` | `sendsPaint` faders | `N(x,y)%9*n//9` (inverted `(8-N(x,y)%9)*n//9`); `v=127-y` raw (D-14); FOUR FADERS 520 -> 525 (+5); row 28(d) |
| 9 | `pad-sim.ts` | `libraryBlocks` | the library's `B[i]`; the fidelity sentence: fixing the compiler alone leaves the preview showing a cell where the device shows a gradient - D-02's loss from the other side; `ledTick` untouched by any row, proved by hash |
| 10 | `pad-sim.ts` | `rebuild` | the store cleared with the rest of the per-contact state |
| 11 | `pad-sim.ts` | case comet | `libraryStamp` under the same gate; proved by `lua-parity` test 6 |
| 12 | `pad-sim.ts` | case perFinger | `libraryStamp` with the hue |
| 13 | `pad-sim.ts` | case glow | the parked dot doused and forgotten, then `libraryFinger` on layer 1 in the scaled touch colour |
| 14 | `pad-sim.ts` | the mirror helpers | `libraryOn`, `calibrated` (U transcribed - the same integer arithmetic as `calibratedAxis`, copied not imported), `libraryRaw`, `libraryCell` (N), `libraryBlock` (Z), `libraryWeight` (Y), `libraryStamp` (K through D), `libraryFinger` (G) |
| 15 | `pad-sim.ts` | `touchZone` | the nearest cell through `ledZoneTerm`, inverted as `8-c` |
| 16 | `pad-sim.ts` | `fadersBody` | the LED column under the pointer, `c*count//9`; the level raw |

`vendored-diff.spec.ts` green at 6 files / 38 rows: the rows inverted in reverse array order rebuild
HEAD's bytes, then 11-04's rows and the deltas rebuild the pristine sha256 of both files. Its plan-id
regex widened **by name** (`^11-[0-9]{2}$|^12\.1-08b$`), as its own comment asks a later phase to do;
`VENDOR.md` gained the dated paragraph under Status naming the sixteen sites. The vendored files'
`Modified for HANGAR:` lines were not edited (11-04's wording already names the manifest as the
authority and says the line is not a second copy; the sentinel stays within twelve lines).

**The nine states**: `presets.ts` sets `state.touchLibrary = { kx: KX, ky: KY }` before
`normalisePadState` (which clones and therefore carries it - row 2). `divergence.ts`: nine
`state.touchLibrary` rows (`hangar: { kx: KX, ky: KY }`, `vendored: undefined`, one reason, generated
from a list of the nine ids rather than typed nine times), five cost rows moved with a dated sentence
(AURORA 415 -> 361, PINWHEEL 477 -> 413, STARFIELD 403 -> 349, NINE PADS 550 -> 565, JOYSTICK 543 ->
491), three new (RADAR 391 vs 445, FOUR FADERS 525 vs 520, DIAL 592 vs 646). `presets.spec.ts`: the
plan-id regex widened by name; `trailMs === 420` and the field asserted on all nine inside the walk
(R-21's premise as a gate, no count moved). `presets.spec` 4 and `catalog.spec` green.

**Every preset at the defaults, before -> after** (the compiler's own, `presets.spec` test 4):

| Preset | Before | After | Delta | Free | Emitted needle |
| --- | --- | --- | --- | --- | --- |
| AURORA | 415 | **361** | -54 | 547 | `K(x,y,1,252)` |
| PINWHEEL | 477 | **413** | -64 | 495 | `if e~=3 and e<5 or e>8 then K(x,y,1,252,255-i*60,i*60,128)end` |
| STARFIELD | 403 | **349** | -54 | 559 | `K(x,y,1,252)` |
| RADAR | 445 | **391** | -54 | 517 | `K(x,y,1,252)` |
| JOYSTICK | 543 | **491** | -52 | 417 | `if s.l then glp(glag(0,s.l),1,0)s.l=nil end G(s,i,e,x,y,1,255,187,0)` |
| NINE PADS | 550 | **565** | +15 | 343 | `local n=N(x,y)local z=n%9*4//9+n//9*4//9*4` |
| FOUR FADERS | 520 | **525** | +5 | 383 | `local f=N(x,y)%9*4//9 local v=127-y` |
| DIAL | 646 | **592** | -54 | 316 | `K(x,y,1,252)`; the angle raw |
| tpad | 902 | 902 | 0 | 6 | no site reached |

JOYSTICK reads 491 through the compiler, not the planner's 492 (a `max(raw, compressed)` reading over
one space the emitter's joiner never writes). The other seven match the plan's table.

**The reachability sweep's worst-position table** (`4 19`, 44,078 states, two passes, every state
fitting), beside 11-16's:

| Preset | 11-16's worst | Now | maxTimer | Free |
| --- | --- | --- | --- | --- |
| AURORA | 429 | **375** | 55 | 533 |
| PINWHEEL | 486 | **422** | 55 | 486 |
| STARFIELD | 422 | **368** | 55 | 540 |
| RADAR | 458 | **404** | 55 | 504 |
| JOYSTICK | 551 | **501** | 24 | 407 |
| NINE PADS | 637 reported (640 true) | **648** reported (651 true) | 163 | 260 |
| FOUR FADERS | 524 | **529** | 24 | 379 |
| DIAL | 707 | **653** | 55 | 255 |

NINE PADS' worst is its 3x3 position (the `count` knob), where the zone needle costs **+11**
(`n%9//3+n//9//3*3` against `x*3//128+y*3//128*3`) - the one delta that is NOT knob-independent, and
the shape is the same at both grids. `reachability.sweep.spec.ts` pins that margin as a literal written
"so that the day it stops being 271 the suite says so": it said so (red once), and the literal moved
**271 -> 260** with the reason beside it (deviation 4). tpad at 907 / 908 is unchanged.

**Fidelity, byte for byte**: `src/lib/fidelity/` (vendored-diff 15, preset-baseline, golden-frames,
firmware-oracle, lua-parity), `presets.spec`, `catalog.spec`, `frames.spec`, `front-door.spec`,
`pad.test.js`, `pad-sim.test.js` - 11 files, 354 green (+1 todo); `sha256sum -c` OK on the three
fixtures; the oracle pair and `src/vendor/botor/tests/` unedited (`git diff --quiet HEAD` holds).
`npm run check` 627 / 0 / 0; `npm run lint` clean. ESLint does NOT run on the vendored files (they are
ignored in `eslint.config.js`, as `VENDOR.md` says; the plan's "eslint still runs on them" is not what
the tree does) - the two files were type-checked by `tsc --noEmit` and by `svelte-check` instead.

## Task 12.1-08b-04: the wire, the parity proof, row 28 (`1c812da`)

**`model.ts`**: the library's two strings are resolved ONCE per tuner at the top of `buildTuner`
(`await import("../catalog/library")`, dynamic, memoised; `buildTuner` is itself reached through a
dynamic import from the UI, so the catalog's first paint still carries neither), and both landings
read `landedSystem` / `landedSystemTimer` - the preset landing (`measurePadsim`) publishes
`TOUCH_LIBRARY_TIMER` and `TOUCH_LIBRARY` exactly as `measureLuaRoute` does, an explicit
`systemSetup` / `systemTimer` still winning on both routes. Resolved there and not awaited inside
`measurePadsim` because the preset landing's chain is `padReady -> compileState -> costOf -> land`
and `model.spec.ts` / `wire-pin.spec.ts` flush it with a fixed 64 microtask hops; a real asynchronous
boundary in the middle of that chain is what the file's own comment warns stops the preset route
landing at all. The `ConfigStrings` and `TunerOptions` notes are rewritten: every card lands the
library since 12.1-08b; the store's `#pageInit` / `#pageTimer` substitution stays in its one place and
is CLEAR's alone. `config-shape.spec.ts` 14 green.

**`model.spec.ts`** (deviation 5): the two tests that asserted the old preset behaviour - 12-03's "a
preset lands what the caller gave, the empty string" and 12.1-07's "both empties for a preset" - are
inverted in place with the reason and the plan, count unchanged; the compiled Setup is asserted to
contain `K(x,y,1,252)` so the landed library is not dead weight.

**`wire-pin.spec.ts`** (4): test 1 pins every compiler-driven landing's `system` to `TOUCH_LIBRARY`
and `systemTimer` to `TOUCH_LIBRARY_TIMER` and its Setup to a `K(`, `G(` or `N(` call; test 3 pins
AURORA's first two frames to the two exports verbatim with `ACTIONLENGTH` at their lengths - **the
assertion 12.1-08 said this plan would move** (the frames stay four; the strings become the
library's); the header and test 2's "preset half" paragraph rewritten. **`install.spec.ts`** (24,
count unchanged): the empty-string leg re-titled as the substitution's own proof (reachable from
`/dev/install/`'s empty textareas and a caller that names none), 12-03's CLEAR-vs-preset parity
inverted in the comment; CLEAR's four defaults unchanged and asserted as before.

**`lua-parity.spec.ts` 5 -> 6.** `luaRoute` gains an optional `{ system, systemTimer }` spread into
the host (tests 1-5 unchanged, running the vendored shelf with none). Test 6: for each of the eight
HANGAR presets (`PRESETS.filter(p => p.id !== "tpad")`, the field asserted on each), a live
`PadSim(preset.state)` and the compiler's own Lua beside both library strings in wasmoon; nineteen
samples driven into both - `touchDown` at the nine diagonal LED centres and the four corner LEDs, a
second contact on LED (2,6), a `touchMove` to the midpoint between LED 4 and 5, the second contact's
lift, the first's, a `touchTap` on the bench case LED (7,1), and 49 ticks of decay - one `run(1)` after
each, then all 243 layer records field by field (as test 1 does) and the frame hash; the compared
count asserted as the literal **8 x 19 x 243 = 36,936**; 18 or 19 of every preset's 19 samples lit
layer 1 (non-vacuity per card). The hi-res case once on the text: a widened NINE PADS reads
`local n=N(x//8,y//8)`, the shipped grid `local n=N(x,y)`, the state without the field
`local z=x*4//128+y*4//128*4`. Green on the first run; N7 below is what proves it is load-bearing.

**Row 28** appended after row 27 with the planner's text verbatim (clauses (a)-(f); (d) the raw fader
value), and the dated scope line after the table (the document's title covers hand-authored
configurations; widened in writing for this one row - R-11's rule). Prettier re-padded **nothing**:
the Config cell "the eight preset cards" is narrower than the column, so eight lines were inserted and
every other line is byte-identical (asserted against the pristine copy). Its lower-case Config cell
claims no name under `namesIn`, so the live-name gate is untouched. `ROW_COUNT` 27 -> **28** with the
note; `audition.spec` 4 green.

**Four more specs moved under Rule 3** (deviations 6 and 7): `ladder.spec.ts`, `model.spec.ts` and
`surprise.spec.ts` carried `{ setup: 300 }` as the reserve that puts DIAL "genuinely over 908" (646 +
300 = 946, -38); at 592 that reserve leaves DIAL inside, so the ladder never runs. 592 + **354** = 946
reproduces the same -38 and every downstream figure (four ladder steps, the resolved fallback) - the
arithmetic is written beside each constant. `knobs.preset.spec.ts` asserted NINE PADS' emitted zone
divisor as `x*3//128` / `x*4//128`; it asserts the calibrated forms now.

## The quick suite, four whole runs

| Run | Command | Result |
| --- | --- | --- |
| 1 | `npm run test:quick` | 2 red: `knobs.preset.spec` (the zone needle) and `surprise.spec` (the 300 reserve) - both real, fixed above (deviations 6, 7) |
| 2 | `npm run test:quick` | 4 red: `install.spec.ts`'s connect-snapshot tests (the snapshot at connect, the unanswered serial, the empty fetch, the four writes) on `timed out waiting for identification` - the transient the brief names, under default workers |
| 3 | `npm run test:quick -- --maxWorkers=2` | **90 / 936 (+1 todo)**, `check-counts 90 936` matches, 44.1 s |
| 4 | `npm run test:quick -- --maxWorkers=2` | **90 / 936 (+1 todo)**, `check-counts 90 936` matches, 43.5 s |

## The eight negative checks

Every one from a scratch copy, restored by copy-back with `sha256sum -c` on both sides; none came
back green.

| # | Check | Red on |
| --- | --- | --- |
| N1 | `//6*6` dropped from `K` | `library.spec` test 6: "midway between LED 2 and 3 in x, row 4: cell 38 started at 133, not a multiple of 6" |
| N2 | `N` left in 255/6 as well | test 1 (934 characters, -26 free - the reason `N` moved), test 2 (fourteen definitions), test 4 (nine parts), test 6 ("N is defined in the map's slot: expected [0, 6] to equal [0]") |
| N3 | the `if z>0` skip removed | `lua-smoke` 38: "K dead on LED (4,4): not the twin's cells and starts" - three zero-weight cells written |
| N4 | one new manifest row's `reason` emptied | `vendored-diff`: "src/vendor/botor/pad-sim.ts: intended divergence ... reason must be a sentence" |
| N5 | one new row dropped (the `_pad.ts` fader row) | `vendored-diff`: "_pad.ts: reconstructed 153134 bytes, upstream is 152570. A change of 564 bytes is in neither list" |
| N6 | `touchLibrary` on aurora only | `presets.spec`: pinwheel's state without the knots, its `state.touchLibrary` row declaring no difference, its cost 477 against the declared 413 (the rows are per preset, so the eight undeclared states go red as the plan's first branch said) |
| N7 | the faders' mirror site reverted to `x*count//d` in `pad-sim.ts` | `lua-parity` 6: "faders after 'down on LED (4,4)': layer(41, 1).pha - the Lua beside the library and the simulator's mirror disagree: expected 0 to equal 255" |
| N8 | `ROW_COUNT` 29 | `audition.spec`: "checklist rows: expected 28 to be 29" |

## Deviations from the plan

**1. [Rule 3 - Blocking] `lua-host.spec.ts` and `lua-host.ts` moved with `N`.** The spec hard-codes the
two slots' name lists (`["G","V","N","A","D"]` absent from a host given 255/0 alone); `N` is in 255/0
now, so the lists read `V G Z Y K A D` / `U W E Q X N KX KY`, and `lua-host.ts`'s `systemTimer` doc
names the same. Outside the plan's file set; one assertion, one comment. Commit `56f8e89`.

**2. [Rule 3 - Blocking] `vendored-diff.spec.ts`'s plan-id regex widened by name.** It admitted
`^11-[0-9]{2}$` only, with a comment asking a later phase to widen it deliberately in its own plan;
`^12\.1-08b$` joins it. Outside the plan's file set. Commit `dbfb3e7`.

**3. [Rule 3 - Blocking] `presets.spec.ts`'s plan-id regex widened by name** (`^1[12]-[0-9]{2}$` could
not admit a dotted phase). The plan anticipated the rows and not the regex. Same commit.

**4. [Rule 3 - Blocking] `reachability.sweep.spec.ts`'s margin literal 271 -> 260.** The plan said
"table moves, assertion unchanged"; the file pins NINE PADS' dearest-colour margin as an equality on
purpose, and NINE PADS moved by +11 at its 3x3 worst. Moved with the reason and the true-worst
arithmetic (651 / 257) beside it; recorded in `deferred-items.md` so "260" is not taken for the true
figure. Same commit.

**5. [Rule 3 - Blocking] `model.spec.ts`'s two preset-landing tests inverted in place.** The plan named
`install.spec.ts` and `wire-pin.spec.ts` as the specs that move; `model.spec.ts` held the same
assertion from the tuner's side (and its own comment said 12.1-08b would change it). Count unchanged.
Commit `1c812da`.

**6. [Rule 3 - Blocking] DIAL's reserve 300 -> 354 in `ladder.spec.ts`, `model.spec.ts` and
`surprise.spec.ts`.** DIAL 646 -> 592 took the "genuinely over 908" state inside the budget; 354
reproduces 946 / -38 exactly. Same commit.

**7. [Rule 3 - Blocking] `knobs.preset.spec.ts`'s zone-divisor needles.** `x*3//128` / `x*4//128` are
the naive forms; the calibrated `local n=N(x,y)local z=...` forms are asserted, with the sentence.
Same commit.

**8. [Design, inside the plan's words] the library resolved once in `buildTuner`** rather than awaited
inside `measurePadsim`. The plan says "the same dynamic import `landLua` uses"; it is the same import,
hoisted, for the microtask reason above. `measureLuaRoute`'s own import went with it.

**9. [Finding] JOYSTICK 491, not 492.** Recorded in `divergence.ts`'s cost row and in row 6's reason.

**10. [Finding] `divergence.ts` imports `calibration.ts`.** Its header said it imported nothing; it
now says the one thing it imports and why (D-02's "no number is typed twice"). Same commit as task 03.

**11. [Not what the tree does] ESLint on the vendored files.** The plan's "eslint still runs on them -
keep it clean" is not the tree's configuration (`eslint.config.js` ignores `src/vendor/`, as
`VENDOR.md` documents); `tsc --noEmit` and `svelte-check` cover them.

**12. [Not what the tree does] prettier re-padding row 28.** Expected by the plan; the cell is
narrower than the column and nothing was re-padded (proved byte-identical outside eight lines).

**13. [Scope] `arc.ts:207-208`'s stale "ARC is N's one caller today" left for 13-19** - 12.1-08a's
second deferred item names 13-19 as the alternative taker and `arc.ts` is in neither plan's file set.

## Anything the plan asserts that the tree does not support

- "eslint still runs on them" (deviation 11); "prettier re-pads - assert text identical modulo
  padding" (deviation 12: nothing re-padded; asserted byte-identical instead).
- "the deltas are knob-independent" - true of seven; NINE PADS' is +15 at 4x4 and +11 at 3x3 (the
  shape is the same, the LED-side term differs by grid). The sweep says by how much.
- "JOYSTICK 543 -> 492" - 491 (above).
- "`reachability.sweep.spec.ts`: table moves, assertion unchanged" - one literal moved (deviation 4).
- The plan's checkpoint text says the preset landing today "writes the firmware defaults"; the tuner
  published the empty string and the STORE substituted the defaults - the same wire outcome, stated
  more precisely in `install.spec.ts`'s rewritten comment.

## For 13-17, 13-14, 13-15, 13-19, 13-20

- **13-17**: every card lands the library; the preset landing publishes `TOUCH_LIBRARY_TIMER` and
  `TOUCH_LIBRARY` exactly as `landLua` does - resolved once in `buildTuner`; the store's `#pageInit` /
  `#pageTimer` defaults are CLEAR's alone. **This supersedes 12.1-VALIDATION's hand-off line "the
  preset landing publishes both empty and the store substitutes both defaults."**
- **13-14**: the library's function set is `U W E Q X N` in 255/0 and `V G Z Y K A D` in 255/6 -
  **twenty-one names** in `LIBRARY_GLOBALS`; the emitter may call `K` (a decaying stamp through `D`),
  `Z`, `Y` and `N`; `library.ts` does not change again in this phase.
- **13-15**: `K(x,y,l,w,r,g,b)` exists for a decaying finger beside `G` for a live one; `Z(x,y)`
  returns the block origin and the two fractions.
- **13-19**: `arc.ts:207-208`'s caller sentence is stale (GHOST and the compiler call `N` too).
- **13-20**: the 12.1 offset's manifest term is **+16 rows on two files** (22 -> 38); the library
  842 + 873; audition +5 (rows 24-28).

## The runbook and the bench

No device was connected to, written to or deployed to by this plan. **Row 28 is the user's**, handed
over unanswered: (a) AURORA's tail under the fingertip and the two-LED blend, (b) JOYSTICK's dot and
its park, (c) NINE PADS' sixteen pads at their edges first time, (d) FOUR FADERS' rails and the raw
level - the calibrated form costed at 540 / 525 in `deferred-items.md`, (e) PINWHEEL's two colours,
(f) DIAL's trail. CAT-04 stays `[ ]`. `.planning/ROADMAP.md` untouched; nothing under
`13-gui-overhaul/` or `12-touch-framework/`; no `git checkout`, `git restore`, `git stash` or
`git clean` was run; every restore was a copy back with the hash compared.

## Questions for the user

None that change this plan's shape. Three carried:

1. **Row 28(d)**: should FOUR FADERS' level become calibrated (+15 per fader Setup, 540 / 525)? A
   "yes" is one emission site, its mirror and one cost row.
2. **The vendored headers' `Modified for HANGAR:` lines** were left at 11-04's wording, which defers
   to the manifest. If the gate wants the sixteen rows named there too, it is one line per file inside
   the stripped header block (no hash moves).
3. **`arc.ts`'s caller sentence** (13-19 or a one-line quick fix).

## Self-Check: PASSED

- `src/lib/catalog/library.ts` contains `function K(` - FOUND
- `src/vendor/botor/_pad.ts` contains `touchLibrary` and `K(x,y,1,` (the emitted form `K(${libraryXY(s)},1,`, rendering `K(x,y,1,252)` - the parity test's compiled text proves it) - FOUND
- `src/vendor/botor/pad-sim.ts` contains `touchLibrary` - FOUND
- `src/lib/fidelity/upstream-manifest.json` contains `12.1-08b` (sixteen rows) - FOUND
- `src/lib/fidelity/lua-parity.spec.ts` contains `TOUCH_LIBRARY_TIMER` - FOUND
- `src/lib/catalog/presets.ts` -> `calibration.ts` via `touchLibrary` - FOUND
- `src/lib/tune/model.ts` -> `library` via `TOUCH_LIBRARY_TIMER` - FOUND
- commits `56f8e89`, `dbfb3e7`, `1c812da` - FOUND in `git log`
- no file created; `+0 / +3` observed as 90 / 936 (+1 todo) twice; manifest 22 -> 38; audition 28

**Only the gate (12.1-09) remains.**
