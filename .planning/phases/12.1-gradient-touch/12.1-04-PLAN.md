---
phase: 12.1-gradient-touch
plan: 04
type: execute
wave: 4
band: 1
depends_on: ["12.1-03"]
files_modified:
  - src/lib/catalog/entries/chorus.ts
  - src/lib/catalog/entries/morph.ts
  - src/lib/catalog/entries/console.ts
  - src/lib/catalog/entries/lumen.ts
  - src/lib/catalog/entries/trackpad.ts
  - src/lib/sim/lua-smoke.spec.ts
  - docs/HARDWARE-AUDITION.md
  - src/lib/catalog/audition.spec.ts
autonomous: true
requirements: [CONT-02, CONT-03, PREV-01]
count_term: "PREV_FILES + 0 / PREV_TESTS + 2 (lua-smoke +2); audition 25 + 1; sweep 4 19 run and unmoved; CHORUS 796 -> 836, MORPH 772 -> 828, CONSOLE 781 -> 839, LUMEN 707 -> 741, TRACKPAD Setup 903 (0) and Timer 490 -> 510 at the picker corner"

must_haves:
  truths:
    - "The three tightest cards in the catalog carry the gradient inside 908 at every corner - CHORUS at 72 free, MORPH at 80, CONSOLE at 69 - all under the 890 BUDGET_ERROR line, measured in this tree."
    - "MORPH's finger is cleared on an end code even though its callback returns early, because G is called before the early return."
    - "LUMEN's finger is drawn in the cell's own palette colour, and A still sends what it sent."
    - "TRACKPAD's edge flash is centred on the calibrated row and column of the finger, in a Timer with room, with the Setup at 903 untouched."
  artifacts:
    - path: "src/lib/catalog/entries/morph.ts"
      provides: "G before the early return"
      contains: "if i>0 then return end G(s,i,e,x,y,0,255)if e==3 or e>=5 and e<9 then return end"
    - path: "src/lib/catalog/entries/trackpad.ts"
      provides: "the flash centres through U"
      contains: "(U(c[2]//8,KY)+32)//64"
  key_links:
    - from: "src/lib/catalog/entries/lumen.ts"
      to: "src/lib/catalog/library.ts"
      via: "glc(a,0,r,g,b,1) inside F so G's layer-0 phase renders in the palette colour"
      pattern: "glc\\(a,0,r,g,b,1\\)"
---

<objective>
CHORUS, MORPH, CONSOLE, LUMEN - and TRACKPAD's flash.

**These are the tight ones.** Measured by the planner on the measured knots, to the character against
the research: CHORUS 796 -> **836** (72 free), MORPH 772 -> **828** (80), CONSOLE 781 -> **839** (69),
LUMEN 707 -> **741** (167), TRACKPAD's Setup **903 / 5 untouched** and its Timer 490 -> **510** (398).
All under the 890 `BUDGET_ERROR` line (`_pad.ts:3076-3078`); CONSOLE at 69 free is the tightest card
this phase touches. **Re-measure every one before its header carries it.**

**Three shapes that are not the sequencers'.** MORPH returns on `e==3 or e>=5 and e<9` before anything
else; `G` must see end codes to clear the block, so it is called first (research "Pattern: G before the
entry's own early return"). LUMEN recolours the cursor cell on layers 1 and 2 inside its own `F`; the
finger takes the same palette colour on layer 0 (`glc(a,0,r,g,b,1)` beside the two that exist). CONSOLE
needs one extra 81-cell `glc` loop because its init paints through `P` per column. TRACKPAD is relative
motion and draws no finger; what moves is the Timer's two flash centres, from the hi-res naive divisor
to the map (`c[2]//8` because `txma(1023)` makes the firmware's lerp the identity at eight times the
scale - research 1.1), D-17.

Purpose: the rest of the hand-authored finger, inside budgets that had almost no room.
Output: five entries re-fitted, two VM tests, one audition row. Term `+0 / +2`, audition `+1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-RESEARCH.md
@.planning/phases/12.1-gradient-touch/12.1-03-SUMMARY.md
@.planning/phases/12-touch-framework/12-09-SUMMARY.md
@.planning/phases/12-touch-framework/12-10-SUMMARY.md
@.planning/phases/12-touch-framework/12-11-SUMMARY.md
@src/lib/catalog/library.ts
@src/lib/catalog/calibration.ts
@src/lib/catalog/entries/chorus.ts
@src/lib/catalog/entries/morph.ts
@src/lib/catalog/entries/console.ts
@src/lib/catalog/entries/lumen.ts
@src/lib/catalog/entries/trackpad.ts
@docs/HARDWARE-AUDITION.md

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (12-12 deviation 10). **No
  emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Row 26 is the user's.
- **Sibling repositories read-only; `src/vendor/` untouched; the phase walk never changes;
  `firmware-oracle.spec.ts` green and unedited; no manifest row.** `_pad.ts:3076-3078` is cited for the
  890 line, never imported.
- **Do not edit `.planning/ROADMAP.md`.** Nothing under `13-gui-overhaul/` or `12-touch-framework/`.
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress`.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push.
- **Append-only:** `docs/HARDWARE-AUDITION.md` - row 26 appended, `ROW_COUNT` moved.
- **Every cost at the RGB444 picker corner under the pinned `compressScript` after `initLuaFormatter()`,
  canonical - RUN, never sketched**, with the harness's `corner()` and `checkSyntax`.
- **No new knob, no value-count change, no tag moves.** MORPH's `@DECAY` keeps the inline idiom (`D`'s
  42-tick ceiling); TRACKPAD's `D` calls stay multiples of six; `decay-idiom.spec.ts` blind to `D(`.
- **Both Phase 11 gates over every string.**
- **State every count as a carried name plus a delta, re-measured at this plan's start**, then assert
  **`+0 / +2`**.
- **Concurrency:** none of this plan's files is 13-08..13-13's; 13-19 re-cases names later. `lua-smoke`
  edited by appending; header 44 -> 46. **`frames.spec.ts` is red for the re-fitted entries until plan 05
  regenerates** - run the quick suite, name the file and its reason, assert everything else (the 12-10
  precedent).
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## The needles, exact (from the harness; each present exactly once)

| Entry | Before -> after |
|---|---|
| CHORUS init | `glc(a,2,@BLOOMC,1)glp(a,2,0)end` -> `glc(a,2,@BLOOMC,1)glp(a,2,0)glc(a,0,255,255,255,1)end` |
| CHORUS callback | `local n=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255)local n=Q(s,i,e,x,y)` |
| MORPH init | `for a=0,80 do glc(a,2,@TRAILC,1)glp(a,2,0)end` -> `for a=0,80 do glc(a,2,@TRAILC,1)glp(a,2,0)glc(a,0,@TRAILC,1)end` |
| MORPH callback | `if i>0 or e==3 or e>=5 and e<9 then return end` -> `if i>0 then return end G(s,i,e,x,y,0,255)if e==3 or e>=5 and e<9 then return end` |
| CONSOLE init | `for c=0,8 do self.v[c]=4 P(self,c)end` -> `for a=0,80 do glc(a,0,255,255,255,1)end for c=0,8 do self.v[c]=4 P(self,c)end` |
| CONSOLE callback | `local n=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255)local n=Q(s,i,e,x,y)` |
| LUMEN palette | `glc(a,1,r,g,b,1)glc(a,2,r,g,b,1)` -> `glc(a,0,r,g,b,1)glc(a,1,r,g,b,1)glc(a,2,r,g,b,1)` |
| LUMEN callback | `local n=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255)local n=Q(s,i,e,x,y)` |
| TRACKPAD Timer | `c[2]*9//1024` -> `(U(c[2]//8,KY)+32)//64` and `c[1]*9//1024` -> `(U(c[1]//8,KX)+32)//64` |

**MORPH's single-contact rule** (`if i>0 then return end`) stays first: a second finger is refused before
`G` sees it, so `B[1]` is never set and the sweep never clears it - which is the same rule as before,
now split from the end-code test. Say so in MORPH's header. **Both Phase 11 gates**: the `e>=5` in
MORPH's test keeps its `and e<9`; nothing here writes an onset.

## Audition row 26

`| 26 | CHORUS / MORPH / CONSOLE / LUMEN / TRACKPAD | **THE OTHER FIVE UNDER THE GRADIENT (plan 12.1-04).** **(a)** CHORUS: does the white finger read against the bloom, and does the chord still land on the pad under the finger? **(b)** MORPH: the finger in the trail colour - does it clear on a lift, and does a second finger stay refused? **(c)** CONSOLE: mute and unmute with the finger drawn - does the strip you meant move? **(d)** LUMEN: the finger in the cell's own palette colour - and watch the host's CC: does it reach 0 and 127 where you expect, or a third of an LED inside the edge (A sends the raw sensor value, unchanged; a calibrated A costs 25 characters and changes what the DAW receives - say if you want it)? **(e)** TRACKPAD: move slowly right, then up, left, down - is the edge flash centred on the row / column your finger is actually on, where round 3 put it a cell early near the edges? | LUMEN's CC range and the flash's centre are things a DAW and an eye settle; the simulator's pointer cannot say where a fingertip is on this unit. |`

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-04-01: Five entries by exact needles, the three tight ones under 890, TRACKPAD's Timer through the map</name>
  <files>src/lib/catalog/entries/chorus.ts, src/lib/catalog/entries/morph.ts, src/lib/catalog/entries/console.ts, src/lib/catalog/entries/lumen.ts, src/lib/catalog/entries/trackpad.ts</files>

  <read_first>
    - each entry's header cost paragraph and finger paragraph
    - `12-09-SUMMARY.md` (CHORUS's `R`, CONSOLE's `P` and its 56-free history, MORPH's decay idiom),
      `12-11-SUMMARY.md` (LUMEN's palette `F` and `A`), `12-10-SUMMARY.md` (TRACKPAD's Timer at 490 and
      the header the gate corrected)
    - `12.1-CONTEXT.md` D-13, D-14, D-17
  </read_first>

  <action>
**1. Substitute** each needle with the occurrence-asserting script. **2. Re-measure** every touched
entry at the picker corner - expect CHORUS 836, MORPH 828, CONSOLE 839, LUMEN 741, TRACKPAD Setup 903 and
Timer 510, canonical, `checkSyntax` true; **if CHORUS, MORPH or CONSOLE lands above 890, stop and report
the figure** - the plan does not authorise dropping anything from those cards. **3. Headers**: figures
and free characters; the finger paragraph (colour per D-13; MORPH's G-before-return and its single-
contact rule; LUMEN's `A` unchanged per D-14 with the sentence that raw reaches 0/127 a third of an LED
inside the outer LEDs; TRACKPAD's centres through `U` with `c//8` explained). **4. Sweep.**

**Negative checks:** put MORPH's `G` after its early return in a scratch copy - task 02's MORPH test goes
red on the lift; make TRACKPAD's `c[2]//8` into `c[2]` - the flash-row test goes red (the clamp inside
`U` pins every hi-res value to the last knot). Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && npx vitest run --project server src/lib/catalog/touch-guard.spec.ts src/lib/catalog/decay-idiom.spec.ts src/lib/catalog/host-surface.spec.ts</automated>
    The sweep unmoved; both class gates and the surface gate green over the five.
  </verify>

  <done>
Five entries changed by exact needles; every figure re-measured in this tree and under 890; MORPH's
finger clears on end codes with the single-contact rule kept first; LUMEN's `A` unchanged and said so;
TRACKPAD's Setup byte-identical and its Timer at 510 through the map; the sweep green; two negative
checks red and restored.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-04-02: The four Setups and TRACKPAD's flash in the real VM, and row 26</name>
  <files>src/lib/sim/lua-smoke.spec.ts, docs/HARDWARE-AUDITION.md, src/lib/catalog/audition.spec.ts</files>

  <read_first>
    - the 12.1 describe block in `lua-smoke.spec.ts` (plans 02 and 03), and the existing CHORUS, MORPH,
      CONSOLE, LUMEN and TRACKPAD blocks (how each is driven; TRACKPAD's `txma(1023)` and its flash
      observable from 12-10)
  </read_first>

  <action>
Append two tests to the 12.1 block:

1. **The four Setups**: for each of CHORUS, MORPH, CONSOLE, LUMEN at its defaults - a press at
   `(KX[c], KY[r])` for a sample of nine centres (the four corners, the four edge midpoints, the centre)
   lights cell `c + r*9` alone on layer 0 at 255 **in the entry's colour** (read the layer-0 colour for
   the cell: white for CHORUS and CONSOLE, `@TRAILC`'s default for MORPH, LUMEN's palette colour for
   that cell); a midpoint press lights two; **MORPH**: press, lift with code 5 - layer 0 dark (the block
   cleared before the early return); a second contact pressed while the first is down lights nothing new
   (`i>0` refused first); **LUMEN**: `A`'s CC pair on a move is byte-identical to what 12-11's test
   recorded for the same raw coordinates (unchanged by this phase - assert it by re-driving that test's
   gesture and comparing the MIDI log).
2. **TRACKPAD's flash centre**: drive a rightward motion with the finger at hi-res `y = KY[1] * 8`; assert
   the flash lands on **row 1** (read from the Timer's paint), and print beside it the row the naive
   `c[2]*9//1024` would give (`96*9//1024 = 0`) - the one-row-off the bench saw near the edges.

Header 44 -> 46. Row 26 appended; `ROW_COUNT` 25 -> 26; title "twenty-five" -> "twenty-six".

**Negative checks:** remove LUMEN's `glc(a,0,r,g,b,1)` in a scratch copy - test 1 red on the colour
(the cell renders in no colour on layer 0); revert TRACKPAD's `KY` needle - test 2 red on the row.
Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/audition.spec.ts</automated>
    `lua-smoke.spec.ts` **46**; `audition.spec.ts` **10** at `ROW_COUNT` 26.
  </verify>

  <done>
The four Setups light the LED-centre cell in their own colour and a midpoint two; MORPH clears on a lift
and refuses a second finger; LUMEN's CC pair is byte-identical to 12-11's; TRACKPAD flashes the calibrated
row where the naive divisor was one off; row 26 written; two negative checks red and restored.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs <observed files> <observed tests + 2>`, with
  `frames.spec.ts` named as red until plan 05 - **this plan's term is `+0 / +2`, written out**.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - run, unmoved.
- `grep -c "test(" e2e/*.e2e.ts` summed, unchanged.
- `git diff --stat HEAD -- src/vendor/` empty; `git diff --quiet -- .planning/ROADMAP.md`; no `values`
  or `tags` array changed; `git diff -U0 -- src/lib/catalog/entries/trackpad.ts` touches the Timer string
  and the header only (the Setup string byte-identical).
</verification>

<success_criteria>
1. CHORUS, MORPH and CONSOLE carry the gradient at 72, 80 and 69 free, measured, under 890.
2. MORPH's finger clears on an end code despite the early return.
3. LUMEN's finger is in the palette colour and `A` is unchanged, proved byte-for-byte.
4. TRACKPAD's flash is centred through the map with its Setup untouched.
5. Row 26 hands the five to the bench with D-14's question written beside LUMEN.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-04-SUMMARY.md` carrying: the
observed baseline and `+0 / +2`; the five figures before and after with free characters and the 890
line; TRACKPAD's flash row beside the naive one; LUMEN's byte-identity; row 26 as written; the negative
checks; the red `frames.spec.ts` named with plan 05 as its closer; and the sentence that no device was
touched.
</output>
