---
phase: 12.1-gradient-touch
plan: 09
type: execute
wave: 9
band: gate
depends_on: ["12.1-08"]
files_modified:
  - docs/TESTING.md
  - docs/HARDWARE-AUDITION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/12.1-gradient-touch/deferred-items.md
autonomous: false
requirements: [SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-07, PREV-01, PREV-02, CONT-02, CONT-03, CAT-04]
count_term: "PREV_FILES + 0 / PREV_TESTS + 0 (the gate writes no tests - written out, so the term count is the plan count: NINE); the 12.1 offset stated once for 13-20: tests + 21, files + 1, e2e + 0 / + 0, audition + 3, runbook + 2, OG re-rendered; the library 782 / 126 + 683 / 225"

must_haves:
  truths:
    - "Every number this phase claims is re-measured on a clean tree against a fresh build, the nine-term chains are summed with the term count asserted at nine against every prose statement of it, and the 12.1 offset is stated once by name so 13-20 reconciles Phase 13's chain from both ends."
    - "Every requirement the fourth string or the gradient falsifies as written is amended by name and dated with the old text kept, and CAT-04 stays unticked with its sixth deliberate decline written in the standing form."
    - "The bench rows are handed over unanswered, in an order whose first ten minutes re-observe the finding, with the page-load proof marked as the row the user may decline - and nothing in this phase is called hardware-verified."
  artifacts:
    - path: "docs/TESTING.md"
      provides: "the section 'Phase 12.1's suites, measured at the gate', appended, reflowing nothing"
      contains: "Phase 12.1"
    - path: ".planning/phases/12.1-gradient-touch/deferred-items.md"
      provides: "sections A-E in 12-12's form; D-16's manifest route; the planner errors named"
  key_links:
    - from: "docs/TESTING.md"
      to: ".planning/phases/12.1-gradient-touch/12.1-VALIDATION.md"
      via: "the nine-term chain re-derived from the runner's JSON and compared to the projection"
      pattern: "nine"
---

<objective>
The gate, in 12-12's register: every number re-measured, requirements amended by name, the bench rows
handed over unanswered.

**What this gate is for.** Nine plans have each asserted a term against an observed baseline while
Phase 13 ran beside them; nobody has yet summed the terms on one clean tree or stated what the sum
did to Phase 13's chain. 12-12 did this for Phase 12 with a twelve-term chain, the Phase 13 offset stated
once by name, every entry at three corners, and three quick runs and two e2e runs with the memory beside
each and every transient named. This plan does the same with **nine** terms, and the number nine is
asserted against every prose statement of it in this plan, in `12.1-VALIDATION.md` and in `TESTING.md`'s
new section.

**Which requirements the gradient falsifies, as written.** Not the rows themselves - the Phase 12 gate
qualifiers dated 2026-09-11 that say "three strings" (PREV-02, SAFE-03, SAFE-05, SAFE-07) are false since
plan 06, and CONT-02's "the library 769 with every caller" is false since plan 02. PREV-01's qualifier
gains the map sentence; SAFE-04's the next key. Seven rows, each amended by appending a dated line with
the old text kept, exactly as 12-12 amended four. **CAT-04 stays `[ ]`** and its sixth deliberate decline
is written in the form `REQUIREMENTS.md:187`'s five earlier ones use.

**The bench, and the one thing that is not proved.** Rows 24-26 (audition) and I-J (runbook) are the
phase's; rows 2-12 of the Phase 12 gate were never run in round 3 and are re-handed on the re-fitted
entries. Row I - the KEEP, the power cycle, the tap - is the only proof of the two-slot mechanism on a
page load and the user has not said whether they will store the library to flash once (D-04). The
checkpoint says so and takes a decline as an answer.

Purpose: the phase closed on measurements, and the user handed what only a finger can settle.
Output: one TESTING.md section, seven amendments, one deferred-items file, one checkpoint. Term `+0 / +0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-RESEARCH.md
@.planning/phases/12.1-gradient-touch/12.1-01-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-02-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-03-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-04-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-05-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-06-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-07-SUMMARY.md
@.planning/phases/12.1-gradient-touch/12.1-08-SUMMARY.md
@.planning/phases/12-touch-framework/12-12-SUMMARY.md
@.planning/phases/12-touch-framework/deferred-items.md
@docs/TESTING.md
@docs/HARDWARE-AUDITION.md
@docs/INSTALL-RUNBOOK.md

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (12-12 deviation 10). **No
  emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** The rows are handed over; the phase is
  recorded as **gate landed, bench pending**, never complete, until the user has run them.
- **Sibling repositories read-only; `src/vendor/` untouched** (`git diff --stat HEAD -- src/vendor/`
  empty is one of the gate's lines); `firmware-oracle.spec.ts` green and unedited; the manifest's 22
  rows unchanged.
- **Do not edit `.planning/ROADMAP.md`** - the row for whoever updates it is written in the SUMMARY, as
  12-12 did. Nothing under `13-gui-overhaul/` or `12-touch-framework/` (`deferred-items.md` is THIS
  phase's, created under `12.1-gradient-touch/`).
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress` and `requirements mark-complete`; **Phase 12.1 not marked complete**.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push. **Do not commit while the suite runs.**
- **Append-only:** `docs/TESTING.md` (a section inserted whole; stale counts in earlier sections listed
  by line under a "Corrections to earlier sections" heading, never edited in place - 12-12 deviation 2),
  `docs/HARDWARE-AUDITION.md` (cost cells corrected in place only where a figure is stale, as 12-12 did
  for ARC and TRACKPAD; no row reflowed), `.planning/REQUIREMENTS.md` (dated qualifier lines appended;
  no row re-wrapped). **CAT-04 stays `[ ]`.**
- **Every cost re-measured** at three corners with the harness and `measureLua` after `padReady()`, as
  `lua-entries.sweep.spec.ts` test 1 does; a figure carried from a SUMMARY is not a measurement.
- **State every count as a carried name plus a delta**: this plan's term is `+0 / +0`, written out; the
  chain is nine terms and the number nine is asserted.
- **Playwright's transients**: the suite in five chunks on detached servers, twice, memory beside each,
  every rerun named, `D-12-12-a` rerun alone before it is read; `test-results/` removed after.
- **Concurrency:** 13-14 may start once this plan's SUMMARY exists (its executor reads `12.1-02-SUMMARY.md`
  and `12.1-07-SUMMARY.md` per `12.1-VALIDATION.md`'s hand-offs). 13-20 (wave 20) appends to the same
  three docs later, append-only.
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## The block to re-measure, on a clean tree against a fresh `npm run build`

| Name | Observed at `91ab755` | 12.1's chain | Phase 13's concurrent terms (13-08..13-13, from their SUMMARYs) | Observed now |
|---|---|---|---|---|
| quick files | 88 | +1 = 89 | (13-09 -1, 13-10 -1, 13-13 +2, 13-12 +1, …) | measure |
| quick tests | 901 (+1 todo) | +21 = 922 | (13-08 +3, 13-09 -6, 13-10 -1, 13-11 +3, 13-13 +8, 13-12 +6, …) | measure |
| e2e titles / runs | 78 / 94 | +0 / +0 | (13-12 +1/+1, 13-13 +1/+1) | count / run |
| `svelte-check` | 608 | provenance | provenance | report |
| catalog / OG | 26 / 26 | +0 / +0, OG re-rendered | +0 | 26 / 26, bytes |
| audition | 23 | +3 = 26 | +0 | 26 |
| runbook rows | 8 (A-H) | +2 = 10 (A-J) | 13-12 +1 (its page-switch row, lettered by 13-12) | count |
| the library | 769 / 139 | 782 / 126 + 683 / 225 | - | measure |
| sweep | `4 19` | `4 19` | `4 19` | `4 19` |

**The two ends must meet on every row**: 12.1's chain plus Phase 13's terms equals the observation, or the
difference is named as a document defect (12-12's "the catalog took neither drawn branch" is the model).
Read every 13-0N/1N-SUMMARY that landed between `91ab755` and now and list its term; the Phase 13 plans
that landed are whichever exist - **do not assume the list above is complete**.

## `TESTING.md`'s new section

"Phase 12.1's suites, measured at the gate", inserted whole before the vendored-tree section, after
Phase 12's: the nine-term chains; the 12.1 offset stated once by name for 13-20; the per-file table from
the runner's JSON (`--reporter=json`), rebuilding the 21 from the other end; the quick runs, the sweep and
the two chunked e2e runs with memory and every transient named; **every hand-authored entry at three
corners** (all-shortest, defaults, picker) with both library strings' rows and every function's caller
(`U W E Q X` / `V G N A D`, the callers read from the rendered entries); the ten re-fits' before/after;
the frames and OG bytes; the planner errors named (below); "Corrections to earlier sections" by line.

## The seven amendments, and the decline

| Row | Old clause (kept) | Appended, dated 2026-09-11 for 12.1 |
|---|---|---|
| **PREV-02** | "the three strings the preview runs are the three the install path writes" (12-12's qualifier) | four: the preview's host runs 255/6 and 255/0 as the module does (`self:tim()`), and `wire-pin.spec.ts` pins both to the wire |
| **SAFE-03** | "HANGAR snapshots THREE strings" | four - the system Timer first; the next snapshot key; a record from before 12.1 restores the 255/6 default with a published flag |
| **SAFE-05** | the sentence naming "the page's own init script" | "… the page's own init and timer scripts …", held as `AMENDED_BY_THE_FOURTH_SCRIPT`; 13-18's rewrite must carry the fact |
| **SAFE-07** | "three event writes in one order … which of the three landed" | four, 255/6 first, from `SLOTS`; the impossible partial restated over four |
| **CONT-02** | "the library 769 with every caller … eighteen inside 908 at every corner" | 782 + 683 across two slots, ten functions each with a caller, the ten re-fits' picker-corner figures, the three tight ones under 890; the finger drawn as a bilinear gradient, not a cell |
| **PREV-01** | 12-12's qualifier ("the preview runs the library but cannot produce a wobbling finger…") | the pointer and the demo finger go through the sensor map of ONE prototype unit (Probe C, 2026-09-11); another module may differ; the cell model superseded by `G` |
| **SAFE-04** | "`.v2` is written, v1 is read as system = default" | the next key is written, every older key read, never overwritten or deleted; the wire-unprovenness of `SERIALNUMBER/FETCH` **unchanged** (say so - 10-14's and 12-12's sentence) |
| **CAT-04** | `[ ]` | the sixth deliberate decline, in the standing form, naming that no 12.1 plan touched the catalog's shape |

## `deferred-items.md`, in 12-12's five sections

**A** what the phase could not do, each with what closes it: the presets uncalibrated (D-16) **with the
manifest route written out**; a calibrated `A` (+25, D-14) if the bench asks; the square-law `G` (+15,
D-12) if the bench asks; the second Probe C pass (row 24); `scripts/` still has no chunk runner; the
research's `docs/TOUCH-PROBE.md` that never existed. **B** the hardware findings carried (Probe C's low-
side compression; the outer-segment hold band of ~2 values). **C** the readings retired by the probe (the
research's hypothesis knots; 12-07's `<11` window and its 68..74 band, correct on the wrong divisor).
**D** decisions open for the user: D-04; one colour everywhere (D-13); the LED pitch (D-20); whether
the page-load fallback (255/4 or the firmware side) is wanted if row I fails. **E** Phase 12's
`deferred-items.md` items this phase touched, by name.

**The planner errors, named** (the form 12-12 used - "Things the plan asserts that the tree does not
support" - listed here in advance so the executor checks each): the check-counts literals in every plan
assume `88 / 901` and are allowed to move; plan 06's "expect errors here until plan 07" may be zero
errors if TypeScript infers the missing key differently - report either; the research's seven-value band;
the `(116,15)` bench case; `wire-pin` "free now"; `CALIBRATION-PROBE.md` / `TOUCH-PROBE.md`; the
"reduced gradient" no-branch; the snapshot key named `v3` in the research; anything else found.

## The checkpoint's rows, in order (`12.1-VALIDATION.md` "The bench rows")

1. Audition **25(a-d)** - EUCLID under the gradient, the first ten minutes.
2. Audition **24** - the Probe C re-run, five minutes.
3. Runbook **I** - KEEP, power-cycle, tap; **the user may decline**, and a decline is an answer.
4. Audition **25(e-i)** - the look: the dim end, the floor, the seam, the rolling finger, the centre dot.
5. Audition **26** - CHORUS, MORPH, CONSOLE, LUMEN, TRACKPAD.
6. Audition **2-12** - the Phase 12 gate's rows round 3 never ran, on the re-fitted entries.
7. Runbook **J** - CLEAR four, PUT BACK four, the pre-12.1 record.
8. Runbook **C** again - PUT BACK with four frames per click, by eye.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-09-01: The block re-measured, the nine-term chains summed, the offset stated once, TESTING.md appended</name>
  <files>docs/TESTING.md, docs/HARDWARE-AUDITION.md</files>

  <read_first>
    - `12-12-SUMMARY.md` in full - the register this task copies: the re-measured block, the run table,
      the chains with the term count asserted, the picker-corner table, the negative checks
    - every `12.1-0N-SUMMARY.md` - each plan's observed baseline and term
    - every Phase 13 SUMMARY that landed after `91ab755` - its term, for the offset
    - `docs/TESTING.md`'s Phase 12 section (the shape to append after) and `scripts/check-counts.mjs`'s
      header
    - `docs/HARDWARE-AUDITION.md` rows 1-26 - every cost cell, to be re-measured
  </read_first>

  <action>
**1.** Clean tree (`git status --porcelain` empty of this plan's work), fresh `npm run build`, free
memory recorded. **2.** `npm run check`, `npm run lint`, `npm run test:quick` (JSON reporter for the
per-file table) twice, `npm run test:sweep`, the e2e suite in chunks twice; every red named and rerun by
its rule. **3.** The chains summed per the interfaces block; **assert the term count**: `grep -c` the
word "nine" in this plan, in `12.1-VALIDATION.md`'s chain section and in the new TESTING.md section
against the nine columns of the block, and name any prose statement that says otherwise. **4.** The 12.1
offset stated once by name. **5.** Every hand-authored entry at three corners with both library strings;
every audition cost cell compared and corrected in place where stale (name each, 12-12's precedent). **6.**
The TESTING.md section inserted whole; `git diff --numstat` proves zero deletions in that file.

**Negative checks (two, 12-12's form):** move `ROW_COUNT` to 27 in a scratch copy of `audition.spec.ts` -
red naming 26; set one `intendedDivergence` `reason` to `""` in a scratch copy of the manifest -
`vendored-diff.spec.ts` red naming the file. Restore, sha256 both sides (the manifest's hash beside
12-12's `8853783d…`).
  </action>

  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs <observed files> <observed tests> && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && git diff --numstat -- docs/TESTING.md | awk '{ if ($2 != 0) exit 1 }'</automated>
    The quick run at the observed pair with 12.1's chain plus Phase 13's terms meeting it on every row;
    the sweep `4 19`; zero deletions in TESTING.md.
  </verify>

  <done>
Every number re-measured on a clean tree, the nine-term chains summed with the term count asserted, the
12.1 offset stated once for 13-20, every entry at three corners with both library rows, the audition's
cost cells corrected where stale, TESTING.md appended without a deletion, two negative checks red and
restored.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-09-02: Seven requirements amended by name and dated, CAT-04's sixth decline, deferred-items.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/12.1-gradient-touch/deferred-items.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` rows PREV-01, PREV-02, SAFE-03, SAFE-04, SAFE-05, SAFE-07, CONT-02,
      CAT-04, and the coverage table rows for each (`:194-219`), plus `:187`'s form for the decline and
      12-12's four appended amendments as the shape
    - `12-touch-framework/deferred-items.md` in full - the five-section form and the items this phase
      closes or carries
    - `12.1-CONTEXT.md` D-12, D-13, D-14, D-16, D-20, D-25
  </read_first>

  <action>
Per the interfaces block: each amendment appended as a dated line with the old text kept and the plan
named; the coverage rows extended the same way; CAT-04's decline; `deferred-items.md` created with its
five sections and the planner errors as found in task 01. A script that finds each row by its id, asserts
one occurrence, appends, and diffs to prove no other line moved.

**Negative check:** run the script against a scratch copy with one id misspelled - it refuses on the
occurrence count and writes nothing.
  </action>

  <verify>
    <automated>git diff --numstat -- .planning/REQUIREMENTS.md | awk '{ if ($2 > 8) exit 1 }' && grep -c "amended 2026-09-11 by plan 12.1-09" .planning/REQUIREMENTS.md</automated>
    At most the eight re-aligned rows deleted (seven amendments plus CAT-04's line), the grep at the
    number of rows amended; `CAT-04` still `- [ ]`.
  </verify>

  <done>
Seven rows amended by name and dated with the old text kept, CAT-04 unticked with its sixth decline in
the standing form, `deferred-items.md` in five sections with D-16's route and the planner errors named.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12.1-09-03: The bench rows handed over, in order, unanswered</name>

  <files>docs/HARDWARE-AUDITION.md, docs/INSTALL-RUNBOOK.md, docs/CALIBRATION-PROBE.md</files>

  <read_first>
    - `docs/HARDWARE-AUDITION.md` rows 24, 25, 26 and 2-12 - still the user's, every one unanswered
    - `docs/INSTALL-RUNBOOK.md` rows I and J (12.1-08) and row C (12-03)
    - `12.1-CONTEXT.md` D-04 (the row the user may decline) and D-24 (the order)
    - this phase's `deferred-items.md` as task 02 wrote it
  </read_first>

  <action>
Present the sections below and stop. **Do not run any part of the bench list. No agent in this phase
connected to a device, performed a write, or deployed**, and this checkpoint is where that promise is
handed over. The order is D-24's: the first ten minutes re-observe the finding of 2026-09-11; the probe
re-run is second; row I third and declinable. Record the user's answer verbatim in this plan's SUMMARY
under a dated heading, a decline on row I included; nothing here assumes one.
  </action>

  <what-built>
Phase 12.1 on the tree: a finger drawn bilinearly across the 2x2 LEDs around it, weighted by distance,
through a sensor map measured on your ZONA (Probe C, 2026-09-11) - in the library's two halves (255/0
at 782, 255/6 at 683, the slot you approved), on ten re-fitted entries inside 908 at every corner, in
the preview's pointer and demo finger, and on the wire as a fourth string that TRY writes, PUT BACK
restores and CLEAR resets. Every semantic proved in a real Lua VM and against the fake ZONA; every
cost measured under the pinned minifier; the suites green at the gate. **Nothing has been seen on a
desk since Probe C.**
  </what-built>
  <how-to-verify>
In this order - the first ten minutes re-observe what you reported on 2026-09-11:

1. `docs/HARDWARE-AUDITION.md` **row 25 (a)-(d)** on EUCLID: the LED at row 2 / column 8 alone under a
   centred finger; both LEDs dim between two; four in the middle of four; the outer ring toggling on the
   first tap.
2. **Row 24**: run `docs/CALIBRATION-PROBE.md` once more (five minutes) and report the twenty-one
   triples, hand and flatness, and the LED pitch in millimetres if you know it.
3. `docs/INSTALL-RUNBOOK.md` **row I** - **only if you are willing to KEEP the library to flash once**;
   PUT BACK restores it. If you would rather not, say so and that is the answer.
4. **Row 25 (e)-(i)**: the dim end, the dark pad's floor, the seam, the rolling finger, the centre dot.
5. **Row 26**: CHORUS, MORPH, CONSOLE, LUMEN, TRACKPAD's edge flash.
6. **Rows 2-12** of the audition, on the re-fitted entries.
7. **Row J**: CLEAR, PUT BACK, and a snapshot from before today if this browser has one.
8. **Row C** again: PUT BACK, four frames per click, by eye.

One line per row - pass, fail, or a note - under a dated `## Results` heading in the document the row
lives in, or in a bench note as before. Rows 24 and I want numbers and a yes/no respectively.
  </how-to-verify>
  <resume-signal>Type "approved", or paste the rows' results (a decline on row I is a result)</resume-signal>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick` twice at the observed pair - **this plan's term is `+0 / +0`, written out; the chain
  is NINE terms and every prose statement of the count says nine**.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19`.
- `npm run build`; the e2e suite twice in chunks at the observed run count, `grep -c "test("` at the
  observed title count.
- `git diff --stat HEAD -- src/vendor/` empty; `firmware-oracle.spec.ts` unedited since `b3a554d`;
  `vendored-diff.spec.ts` green with 22 manifest rows; `git diff --quiet -- .planning/ROADMAP.md`;
  `.planning/phases/13-gui-overhaul/` untouched.
- `git diff --numstat` on `docs/TESTING.md` shows zero deletions; on `REQUIREMENTS.md` and
  `HARDWARE-AUDITION.md` at most re-aligned rows.
</verification>

<success_criteria>
1. The nine-term chains are summed on a clean tree with the term count asserted, and 12.1's offset is
   stated once for 13-20.
2. Every hand-authored entry and both library strings are re-measured at three corners.
3. Seven requirements are amended by name and dated with the old text kept; CAT-04 is unticked with its
   sixth decline.
4. `deferred-items.md` carries D-16's route, the open decisions and the planner errors.
5. The bench rows are handed over unanswered in the order whose first ten minutes re-observe the
   finding, row I marked as declinable, and the phase is recorded as gate landed, bench pending.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-09-SUMMARY.md` carrying: the
re-measured block with both ends meeting on every row; the nine-term chains; the 12.1 offset by name;
the picker-corner table with both library rows; the run table with memory and every transient; the seven
amendments before and after; CAT-04's decline; the deferred items by section; the ROADMAP row for
whoever updates it (**not edited here**); the planner errors named; the checkpoint handed over (task 03
not answered here); and the sentence that no device was touched, nothing was deployed, and the phase is
gate landed, bench pending.
</output>
