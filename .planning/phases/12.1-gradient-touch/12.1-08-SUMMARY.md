---
phase: 12.1-gradient-touch
plan: 08
subsystem: device
tags:
  [
    band-2,
    fourth-string,
    system-timer,
    keep-confirmation,
    partial-sentences,
    wire-pin,
    probe-page,
    fake-zona,
    e2e-chunks,
    runbook-rows,
    page-load-proof,
    y-axis,
    d-04,
    d-08,
    d-21,
    d-22,
    d-26,
    safe-02,
    safe-03,
    safe-05,
    safe-07,
    prev-02,
    negative-checks,
    counts,
  ]
requires:
  - phase: 12.1-gradient-touch
    plan: 07
    provides: "ConfigStrings.systemTimer first in write order; landLua publishing TOUCH_LIBRARY_TIMER beside TOUCH_LIBRARY; #pageTimer beside #pageInit; TRY / PUT BACK / CLEAR over four through SLOTS; #classify over the landed prefix with landedSlots / failedSlots; hangar.snapshot.v3 with snapshotFromV2 beside snapshotFromV1; install-copy.ts's unions at three rows each; wire-pin.spec.ts green at four frames with the first frame's bytes unasserted; the probe's pair() at systemTimer: \"\""
  - phase: 12.1-gradient-touch
    plan: 05
    provides: "mapAxis(offset, extent, max, axis = \"x\") through calibration.ts's sensorAt; the sentence that HeroSurface.svelte:137 and the workspace route's y line each gain \", \\\"y\\\"\" and that Coverflow.svelte does not exist"
  - phase: 13-gui-overhaul
    plan: 12
    provides: "runbook row I after H; the zero-writes proof extended to PAGEACTIVE and PAGEDISCARD in install.e2e.ts and session.e2e.ts; the fake's switch and page-count model in synthetic.ts; e2e 80 titles / 96 runs in five chunks"
  - phase: 12-touch-framework
    plan: 03
    provides: "the third script's amendments held from both sides (AMENDED_BY_THE_THIRD_SCRIPT); the e2e counts of three inside the install titles"
provides:
  - "install-copy.ts: CONFIRM_REPLACES names four scripts (the clause 'and the page's own init and timer scripts'); LandedWords / FailedWords name all four slots in write order on every row; the partial's step says 'all four'; install-copy.spec.ts's AMENDED_BY_THE_FOURTH_SCRIPT holding the confirmation from both sides and asserting the four names in write order over all three partials"
  - "wire-pin.spec.ts: the published systemTimer pinned to TOUCH_LIBRARY_TIMER and its own minified form; the four writes read by element and event in SLOTS order with the first frame's ACTIONSTRING and ACTIONLENGTH asserted; a EUCLID landing pinning 255/6 to TOUCH_LIBRARY_TIMER and 255/0 to TOUCH_LIBRARY byte for byte on the wire; four tests"
  - "/dev/install/: the fourth textarea install-system-timer, first on the page and first on the wire; pair() over the four boxes; install-snapshot reading four lengths plus ' v1' / ' v2' when the record predates a slot; exactly two static specifiers still"
  - "install.e2e.ts / session.e2e.ts: every count of three inside the titles now four - CONFIG/FETCH 4 per connect, six frames per connect, zero writes of every class with 13-12's needles in place, four acknowledged writes per RAM leg in SLOTS order, the refetch round four, the partial six not seven, the unplug at the seventh frame decoding as 255/6, CLEAR four defaults - proved in a browser against the fake; 80 titles / 96 runs unchanged"
  - "docs/INSTALL-RUNBOOK.md rows J (the page-load proof, D-04 answered yes, the permission recorded not asked) and K (four strings: CLEAR, PUT BACK, a pre-12.1 record), eleven lettered rows, none answered"
  - "HeroSurface.svelte:137 and playground/[id]/+page.svelte:447 through mapAxis(..., \"y\")"
affects: [12.1-08b, 12.1-09, 13-14, 13-17, 13-18, 13-20]
tech-stack:
  added: []
  patterns:
    - "A copy amendment is held from both sides: the contract's row must exist, the shipped string must be the one the module produces under that name, and an insertion must give the contract's sentence back when deleted; a rewrite must name what made the contract untrue. The table is named for the latest amendment and keeps the earlier rows"
    - "A closed-union sentence over an ordered writer names the landed PREFIX in the writer's order on every row, so reading any row left to right reads the slot list top to bottom; a spec asserts the four names appear in write order and that landed + failed partition them"
    - "A wire pin over an exported string is two identities in two tests: the tuner publishes the export (published === export), and the transport writes the published (frame === published); together the export IS the bytes, and the spec lands a real entry to read both frames"
    - "A runbook row that needs a user's permission records the permission (date, decision) rather than asking; the safety sentence stays"
key-files:
  created: []
  modified:
    - src/lib/device/install-copy.ts
    - src/lib/device/install-copy.spec.ts
    - src/lib/device/wire-pin.spec.ts
    - src/lib/device/install.svelte.ts
    - src/lib/device/install.spec.ts
    - src/lib/ui/InstallState.svelte
    - src/lib/ui/shell/device-clause.ts
    - src/lib/ui/device-ui.spec.ts
    - src/routes/dev/install/+page.svelte
    - e2e/install.e2e.ts
    - e2e/session.e2e.ts
    - docs/INSTALL-RUNBOOK.md
    - src/lib/ui/intro/HeroSurface.svelte
    - src/routes/playground/[id]/+page.svelte
key-decisions:
  - "The partial words are LITERALS (closed unions), not built from SLOTS[].label, and every row now names all four in write order - the two older rows were rewritten to name the system timer that also landed; the union's consumers (the classifier, the panel's fallback, the device clause, two specs) moved with them under Rule 3"
  - "Only CONFIRM_REPLACES gained the fourth-script clause; SNAPSHOTTING_BODY and identifiedBody keep the third script's clause and are 13-18's, named for it in the spec's table"
  - "The probe's install-snapshot renders the two flags separately (' v1' / ' v2' after the four lengths) because the page renders the store's values verbatim; the PANEL's sentence 'the record predates the timer slot' should fire on snapshotFromV1 || snapshotFromV2 because both restore the 255/6 default - that sentence is 13-17 / 13-18's, and this is the answer to 12.1-07's question 1"
  - "fake-zona.ts is byte-unchanged: the four-string state lives in synthetic.ts's ZonaState.system (element 255 keyed by event) beside configs (element 0), 255/6 answered by ramRead's fall-through for a factory module and stored on write; the fake's mismatch condition names the touch element's Setup, not event 0, so nothing was widened"
  - "The runbook's row J is written with the amended wording (D-26 item 3, 2026-09-11): the clause '(the user may decline)' is not written and the lead sentence records the permission; the dated line under 'Seven rows' reads eleven"
  - "session.e2e.ts's poll message names the Setup fetch as the last config chunk, not the Timer's as the plan's needle table said: since 12.1-06 the fetch order is SLOTS' (255/6, 255/0, 0/6, 0/0) and 0/0 is last"
patterns-established:
  - "Copy amendments named for the latest script and held from both sides, with the older rows kept"
  - "The wire pin as two identities over a real entry's landing"
requirements-completed: [SAFE-02, SAFE-03, SAFE-05, SAFE-07, PREV-02]
metrics:
  duration: "about 110min execution (+ three builds, three quick runs, the e2e suite twice in five chunks with two chunk reruns, five negative checks)"
  completed: "2026-09-11"
---

# Phase 12.1 Plan 08: The sentence that names four, the pin over both strings, the fourth textarea, the suite over four, two runbook rows and the y token Summary

**The KEEP confirmation says a store replaces four scripts and every partial names the four in write
order; 255/6 and 255/0 are pinned to `TOUCH_LIBRARY_TIMER` and `TOUCH_LIBRARY` byte for byte on the wire;
the probe page pastes an arbitrary page timer first in write order; the e2e suite proves four fetches
and zero writes on a connect and four acknowledged writes per RAM leg in a browser against the fake,
green twice in five chunks at 96 runs; runbook rows J and K are written, lettered after 13-12's I and
unanswered; and the preview's y axis goes through the measured map on both surviving canvases.**

Band 2's third and last plan, on `ab2ee64` after `12.1-07-SUMMARY.md`'s "What 12.1-08 will find" was
read first and every item in it closed. **No device was touched and nothing was deployed.** Every ZONA
in this plan is `synthetic.ts`'s; the page-load proof (row J) is not claimed to work - the row is
where it would be shown, and the one KEEP it needs is the user's hand under D-26 item 3. **CAT-04
stays `[ ]`.** `src/vendor/`, `sequence.ts`, `library.ts`, `synthetic.ts`, `fake-zona.ts`,
`firmware-oracle.spec.ts`, `protocol-pin.spec.ts` and `forbidden-instructions.spec.ts` are
byte-unchanged since `ab2ee64` (`git diff --quiet` each), `.planning/ROADMAP.md` untouched, nothing
under `13-gui-overhaul/` or `12-touch-framework/`. **13-14 is released: 12.1-08 has landed.** Band 2
is complete; 12.1-08b (the presets, D-27 mirror) is next, then the gate (12.1-09).

## For 13-18 - the sentences, verbatim

13-18 rewrites `install-copy.ts` under 13-CONTEXT D-05 and must carry the four-string fact. These are
the shipped strings as of `b5ecd40`, quoted here because `13-COPY-NEW.md` is Phase 13's file and this
plan does not touch it; `12.1-VALIDATION.md` names the hand-off.

**The KEEP confirmation (SAFE-05), `CONFIRM_REPLACES`:**

> This replaces the Setup and Timer scripts on your ZONA’s touch element and the page’s own init and timer scripts, and it survives a power cycle.

The insertion is exactly ` and the page’s own init and timer scripts` on 07-UI-SPEC's contract row
("This replaces the Setup and Timer scripts on your ZONA’s touch element, and it survives a power
cycle."); deleting it gives the contract back character for character, asserted.

**The partial sentences (SAFE-07, `partialBlock(landed, failed).detail`), one per reachable prefix
of SLOTS, read left to right as 255/6, 255/0, 0/6, 0/0:**

> The system timer reached your ZONA and the page init, the Timer and the Setup did not. What is on the module now is part of this configuration and part of your own.

> The system timer and the page init reached your ZONA and the Timer and the Setup did not. What is on the module now is part of this configuration and part of your own.

> The system timer, the page init and the Timer reached your ZONA and the Setup did not. What is on the module now is part of this configuration and part of your own.

**The partial's first step:** `Click TRY ON DEVICE to send all four again` (was "all three").

**Left for 13-18, named:** `SNAPSHOTTING_BODY` ("Taking a copy of the Setup and Timer scripts already
on your ZONA’s touch element, and the page’s own init script.") and `identifiedBody` ("... Its own
Setup and Timer are saved here, and the page’s own init script, so PUT BACK can undo anything you
try.") still name three. They are true-by-omission of the timer the snapshot also copies; this plan
changed the confirmation only, as written, and the spec's table says whose the other two are.

## What was built

### Task 01 - the sentence that names four, and the pin over both strings (`b5ecd40`)

**`install-copy.ts`.** `CONFIRM_REPLACES` gained "and timer" inside its clause (above), with a doc
naming the KEEP that stores 255/6 and the spec constant that holds it. The two closed unions were
rewritten so every row names the four slots in write order: `LandedWords` is `"The system timer"` |
`"The system timer and the page init"` | `"The system timer, the page init and the Timer"`;
`FailedWords` is unchanged (`"the page init, the Timer and the Setup"` | `"the Timer and the Setup"`
| `"the Setup"`). **The plan's "read the file and say which": literals**, not built from
`SLOTS[].label` - the module imports nothing and authors no grammar; the store publishes the labels
separately as `landedSlots` / `failedSlots`. The union's doc table shows the three rows and says
reading a row left to right is reading SLOTS top to bottom. `partialBlock`'s step reads "all four
again"; its doc records the 12-03 and 12.1-08 amendments. No cap, no ledger entry and no other
sentence moved; the module still imports nothing (`config-shape.spec.ts` test 13 green).

**`install-copy.spec.ts` (6 tests, count unchanged).** `AMENDED_BY_THE_THIRD_SCRIPT` is
`AMENDED_BY_THE_FOURTH_SCRIPT`, its doc gaining the fourth script's paragraph and the rule that the
constant is named for the latest amendment; `PAGE_SCRIPTS_CLAUSE` beside `PAGE_INIT_CLAUSE`.
`CONFIRM_REPLACES`'s row carries the new shipped string and the new insertion; the two partial detail
rows carry the rewritten sentences with `names: "system timer"`; a third detail row and a third step
row for the system-timer-only branch (12.1-07 left it unwalked); the step rows say "all four". The
system-timer-only partial joins `OTHER_BRANCHES`, so test 2's `long` list holds all three forms.
**The four names in write order:** `SLOT_NAMES_IN_WRITE_ORDER` (`system timer`, `page init`,
`Timer`, `Setup`) and `PARTIALS_IN_WRITE_ORDER` (the three pairings); for each, the detail starts
with the sentence, each name is found after the previous one (case-sensitive, so "system timer"
cannot stand in for "Timer"), the names in `landed` followed by the names in `failed` equal the four
in order (the prefix and the rest partition them), and the step says "all four". Test 6's "two
partials and no third" is "three and no fourth", the system timer first.

**`wire-pin.spec.ts` (4 tests, count unchanged).** The header reads FOUR FRAMES since 12.1-07 and
says what this file pins since 12.1-08. `ELEMENT_SYSTEM` and `ELEMENT_TOUCH` imported. Test 2 pins
`landed.config.systemTimer` to `TOUCH_LIBRARY_TIMER` (the comment 12.1-07 left, "the minimum that
keeps this pin green", is gone), asserts the two halves are distinct strings, and asserts the page
timer's length is its own measure beside the page init's. Test 3 reads the four writes as
`[systemTimer, system, timer, setup]`, asserts `[ELEMENTNUMBER, EVENTTYPE]` over the four is
`[[255, 6], [255, 0], [0, 6], [0, 0]]`, and asserts the first two frames' `ACTIONSTRING` and
`ACTIONLENGTH` against the published strings (for AURORA, a preset, both empty - `writeAll` does not
substitute; the store does, in one place, before it calls `writeAll`). **Then it lands EUCLID** and
reads the four frames again: 255/6's `ACTIONSTRING` is `TOUCH_LIBRARY_TIMER` and its `ACTIONLENGTH`
is `TOUCH_LIBRARY_TIMER.length`; 255/0's is `TOUCH_LIBRARY` at `TOUCH_LIBRARY.length`; and
`GridScript.compressScript` of each export equals the bytes on the wire (a canonical string's
minified form is itself, so nothing shortened it on the way). Test 2 pins published === export, test
3 pins frame === published, so the exports ARE the bytes. `:505`'s preset literal already carried
`systemTimer: ""`.

**The four frames, as pinned** (EUCLID at the defaults through `writeAll` into the fake):

| # | Element / event | `ACTIONSTRING` | `ACTIONLENGTH` |
| --- | --- | --- | --- |
| 1 | 255 / 6 | `TOUCH_LIBRARY_TIMER` verbatim (`--[[@cb]]function V(n)for d=0,3 do gl…`) | 705 |
| 2 | 255 / 0 | `TOUCH_LIBRARY` verbatim (`--[[@cb]]H={}T={}C=0 P={}B={}L=0 KX={…`) | 781 |
| 3 | 0 / 6 | the rendered Timer (`config.timer`) | `config.timer.length` |
| 4 | 0 / 0 | the rendered Setup (`config.setup`) | `config.setup.length` |

**Outside the plan's list, under Rule 3 (deviation 1).** Renaming the union literals is a type change
for every consumer: `install.svelte.ts`'s `#classify` (the two older literals, one comment), `InstallState.svelte`'s
fallback (`install.landed ?? ...`), `device-clause.ts`'s partial title, `device-ui.spec.ts`'s two
titles and `install.spec.ts`'s six assertions each moved to the new literal. Nothing else in those
files moved; `install.spec.ts` is 24 green, `device-ui.spec.ts` green, `check` 627 / 0 / 0.

### Task 02 - the fourth textarea, the fake, and every count of three inside the titles becoming four; the suite in chunks (`5b93d6a`)

**`/dev/install/+page.svelte`.** `let systemTimer = $state("--[[@cb]]--[[page timer]]")` under the
page init's doc, which now covers both typed literals with the same reason (a starting value a
visitor overwrites, not a claim about the firmware; naming `$lib/protocol` would be a third
specifier). `pair()` is `{ systemTimer, system, setup, timer }` with the doc that its order is for
the reader and the wire order is SLOTS'. The fourth textarea, `data-testid="install-system-timer"`,
is the FIRST box on the page, labelled "Page timer". The header's paragraph reads FOUR strings from
four textareas, names 255/6 as the first on the wire, and says why the page-load row needs the box
(a way to put the library's second half into 255/6 without the tuner). **`install-snapshot`** reads
`durable 22 24 18 18` shape - four lengths in write order - followed by ` v1` (a Phase 7 record, both
system strings defaults) or ` v2` (a Phase 12 record, the page timer a default) or nothing (v3). The
page still names exactly two static specifiers (`config-shape.spec.ts` test 13, 14 green).

**The `fromV1 || fromV2` decision (12.1-07's question 1).** The PROBE renders the two flags
separately - the page's rule is the store's values verbatim, and at most one flag is true (each names
the key the record came from). The PANEL's sentence "the record predates the timer slot" - rendered
by nothing yet - is true of EITHER key, because both restore the 22-character 255/6 default; when
13-17 / 13-18 write that line it should fire on `snapshotFromV1 || snapshotFromV2`, and the probe's
doc and runbook row K say so. Decided from the plan ("the `fromV2` flag beside `fromV1`") and from the
store's field doc.

**`e2e/fake-zona.ts` - byte-unchanged, and where the four-string state lives.** The plan asked to
"widen the fetch condition to event 6 only if it names event 0". It names `EVENT_SETUP` on
`ELEMENT_TOUCH` (the mismatch fault's target, 12-02) - not event 0 of the system element - so nothing
was widened. The state is `synthetic.ts`'s `ZonaState.system: Record<number, string>` (element 255
keyed by event, `systemFlash` beside it) next to `configs` / `flash` for element 0, exactly as 13-12
found for the page model; `ramRead` answers 255/6 from `state.system[6]` when a test set it and from
the package's `SYSTEM_DEFAULT_TIMER` otherwise, and the EXECUTE branch stores a write under its
element and event. 12.1-06 verified the round trip in `sequence.spec.ts`; this plan verified it in
a browser: after a try-on `zona.state.system[6]` is the fourth textarea's text and after a put-back
it is the fixture's own.

**`e2e/install.e2e.ts`.** `MODULE_SYSTEM_TIMER = "--[[@cb]]function M:tim()return 2 end"` - not the
package default, for the same reason `MODULE_SYSTEM` is not - in the fixture's `system` map beside
`MODULE_SYSTEM`. `probePair` reads four in write order. The header's RAM-leg sentence says four
events and about 800 ms.

**The needle table, before and after** (the plan's table, plus what the file held that the table
did not name):

| Site | Before (12-12 / 13-12) | After (12.1-08) |
| --- | --- | --- |
| test 1 `install-snapshot` | `durable ${system} ${setup} ${timer}` (three lengths) | `durable ${systemTimer} ${system} ${setup} ${timer}` (four, no key suffix) |
| test 1 step list | `fetch-serial, fetch-system, fetch-setup, fetch-timer, fetch-page-count` | `fetch-serial, fetch-system-timer, fetch-system, fetch-timer, fetch-setup, fetch-page-count` (SLOTS order) |
| test 1 `CONFIG/FETCH` | `toBe(3)` | `toBe(4)` |
| test 1 frames on the wire | "Five frames since 13-12" / `toBe(5)` | six / `toBe(6)` |
| test 1 zero-writes needles | `CONFIG/EXECUTE 0, PAGESTORE 0, PAGEACTIVE 0, PAGEDISCARD 0, HEARTBEAT 0` | unchanged, still zero, 13-12's two in the list |
| test 1 empty module, two attempts | `toBe(6)` | `toBe(8)` |
| test 2 title | "lands all three acknowledgements" | "lands all four acknowledgements" |
| test 2 step list (twice) | `write-system, write-timer, write-setup, restore-page-change` | `write-system-timer, write-system, write-timer, write-setup, restore-page-change` |
| test 2 `CONFIG/EXECUTE` | 3 then 6 | 4 then 8; `state.system[6]` asserted after the try-on and the put-back, `systemFlash[6]` untouched |
| test 3 keep step list | `store, refetch-system, refetch-setup, refetch-timer` | `store, refetch-system-timer, refetch-system, refetch-timer, refetch-setup` |
| test 3 counts | `EXECUTE 3`, `FETCH 6` | `EXECUTE 4`, `FETCH 8`; `systemFlash[6]` asserted stored |
| test 3 mismatch rounds | `refetch-system/setup/timer` each 3; `FETCH 3 + 9` | plus `refetch-system-timer` at 3 (three ROUNDS, unchanged); `EXECUTE 4`; `FETCH 4 + 12` |
| test 4 partial drops | `nth 3, 4, 5` | `nth 4, 5, 6` (acknowledgements 1-3 land; the Setup's three attempts are 4, 5, 6) |
| test 4 partial steps / count | `write-system, write-timer, write-setup timeout 3`; "FIVE, NOT SIX" / 5 | `write-system-timer, write-system, write-timer, write-setup timeout 3`; "SIX, NOT SEVEN" / 6; `state.system[6]` asserted landed |
| test 4 first write refused | `write-system nack 1`; count 1 | `write-system-timer nack 1`; count 1 (unchanged); `write-system` also proved absent; `state.system[6]` untouched |
| test 4 first write deaf | `write-system timeout 3`; count 3 | `write-system-timer timeout 3`; count 3 (unchanged) |
| test 5 unplug frame | `n = 6` (five before it); `write-system aborted 1`; decodes as 255 / `EVENT_SETUP` | `n = 7` (six before it); `write-system-timer aborted 1`; decodes as 255 / `EVENT_TIMER` |
| test 5 after replug | `FETCH 6` (two snapshots of three) | `FETCH 8` (two of four) |
| test 6 store never confirms | `EXECUTE 3`; put-back's RAM leg "FOUR steps"; `EXECUTE 6` | `EXECUTE 4`; "FIVE steps"; `EXECUTE 8` |
| workspace try-on (test 7) | `EXECUTE 3` | `4` |
| workspace keep (test 8) and rig | `EXECUTE 3`, rig `3` | `4`, `4` |
| test 9 three RAM legs | `EXECUTE 9` | `12` |
| test 10 two settled legs | `EXECUTE 6` | `8` |
| CLEAR test: the poll before the click | `.toBe(3)` | `.toBe(4)` |
| CLEAR test: the clear's delta | `toBe(3)` ("THREE firmware defaults") | `toBe(4)` ("FOUR firmware defaults") |
| CLEAR test: the tail | `EXECUTE 9` ("THREE CONFIG/EXECUTE each") - **not in the plan's table; found by the first chunk run** | `12` |
| LUMEN two clicks (test 12) | `EXECUTE 6` | `8` |
| 13-12's destination test | `writes: 0` throughout | unchanged |
| `WRITE_CLICKS` | five | unchanged - a fourth string is not a fifth click |
| `session.e2e.ts` `onlyReads` | polls `3 * connects` "all three config reads ... the timer fetch is the last chunk"; asserts `3 * connects`; `5 * connects` chunks | polls `4 * connects` "all four ... in SLOTS order ... the Setup fetch is the last config chunk"; `4 * connects`; `6 * connects` |
| `session.e2e.ts` comments (`:776`, `:789`, `:937`, `:1170`, `:1536`) | "three reads", "TEN reads" | "four reads", "TWELVE reads" |

**The suite, run twice in five chunks on fresh detached servers.** The first run was on the build at
`b5ecd40` with task 02's edits on the tree; the second on the build at `166d18b`, the committed
tree. Free memory is the figure at the chunk's start.

| Chunk | Files | Run 1 | Run 2 |
| --- | --- | --- | --- |
| 1 | `install`, `session` | 34 runs: **31 passed, 3 red** at 0.48 GB free - `install:425` `CONFIG/FETCH` 9 for 8 (one fetch retried under three workers; the same test alone read 8 with every step `ok 1`), `install:1904` CLEAR chromium 12 for 9 (a needle the plan's table did not name - fixed) and webkit-phone 13 for 9 (the same needle plus one retry); **rerun whole: 34 passed** at 0.25 GB | **34 passed** at 0.60 GB |
| 2 | `browse`, `browse-webkit` | 21 passed at 0.30 GB | 21 runs: 20 passed, `browse.e2e.ts:963` (the wall's paint count, `waitForFunction` at 30 s) red at 1.16 GB; alone `--workers 1` 1 passed at 0.93 GB; **rerun whole: 21 passed** at 0.83 GB |
| 3 | `tuning`, `tuning-webkit` | 20 passed | 20 passed at 0.95 GB |
| 4 | `catalog`, `fidelity`, `first-experience`, `library` | 10 passed at 0.21 GB | 10 passed at 1.33 GB |
| 5 | `artifacts`, `radius`, `skeleton`, `smoke` | 11 passed | 11 passed at 1.00 GB |
| **Sum** | | **96** | **96** |

The sum is 13-12's 96 (= 95 at 13-13 + 1), recomputed: 34 + 21 + 20 + 10 + 11. Titles by
`grep -c "test("` summed over `e2e/`: **80** before and after (`install.e2e.ts` 15, `session.e2e.ts`
14, `browse` 12, `tuning` 10, `first-experience` 5, `tuning-webkit` 5, `browse-webkit` 4, `smoke` 4,
`artifacts` 3, `catalog` 2, `fidelity` 2, `skeleton` 2, `library` 1, `radius` 1). **e2e `+0 / +0`,
written out.** `test-results/` removed after. No chunk died on wrangler's fatal; no `webkit-phone`
`Could not connect`; `session.e2e.ts:701` green in both runs.

**A finding about the chunk method (deviation 4).** The session's `e2e-chunks.sh` (13-07 deviation
9) stops each server with `taskkill /F /T /PID`; invoked from this session's shell, Git Bash rewrote
`/F` and `/T` to `F:/` and `T:/`, so every server outlived its chunk - nine `wrangler dev` trees were
alive after run 1, holding `build/` (`npm run build` failed `EPERM` twice, exactly the failure the
brief names) and answering on 4173 under the next chunk's server. `e2e-chunks-1208.sh` in the
scratchpad is the same script with the stop through PowerShell (`Stop-Process` over every process
whose command line matches `wrangler.*dev --port 4173`, plus `workerd.exe`), `MSYS_NO_PATHCONV=1`,
and the free-memory figure printed per chunk; it reports `server after stop: HTTP 000` after every
chunk of run 2. The first pattern (`wrangler dev --port 4173`) missed wrangler's leaf process
(`wrangler-dist\cli.js dev --port 4173`) once; widened, and the leaf killed by hand. Nothing in the
tree changed for this; the scratch script is the record.

### Task 03 - the page-load row, the four-strings row, and the y token (`166d18b`)

**`docs/INSTALL-RUNBOOK.md`.** Two rows appended after 13-12's row I, at the table's widths in code
points (7 / 72 / 426 / 1732 / 1062 / 6), no existing row re-wrapped (`git diff --numstat` 6 / 0:
two rows and a four-line dated paragraph), prettier clean, `Pass` cells empty. The letters are the
two after I - **J and K** - read from the file (13-12's SUMMARY said the same). Eleven lettered rows
(`grep -c "^| \*\*[A-Z]\*\* |"` reads 11), asserted by a scratch script through `audition.spec.ts`'s
parser shape (trim, `|` at both ends, split, trim each cell): `ABCDEFGHIJK`, J's row cell containing
`12.1 D-04` and not "may decline", J's "Do this" containing the recorded permission, K's row starting
"Four strings", K's "Passes when" naming `cfg_default_flag`, every `Pass` cell empty.

**Row J, as written** (the amended wording - D-04 answered yes on 2026-09-11, D-26 item 3):

> **J** | The library's second half on a page load (12.1 D-04; SAFE-05) | **You agreed to store the
> library to flash once for this row (D-26, 2026-09-11); PUT BACK restores it and CLEAR + KEEP leaves
> nothing behind.** On EUCLID, connected: `TRY ON DEVICE`, wait for `PLAYING NOW`, touch the pad to see
> the gradient. `KEEP ON DEVICE`, wait for `KEPT`. Pull the cable, wait five seconds, plug it back in.
> **Before HANGAR reconnects**, tap a ring cell. Then reconnect and `PUT BACK`. | The tap after the
> power cycle lights the finger as a gradient and toggles the step, with HANGAR not yet connected - so
> the library's second half was defined by the page load itself and not by a write. **If the tap after
> the power cycle does nothing and the module's LEDs show the alert flash only**: report that, and if a
> MIDI monitor is open report whether a print reached it; `attempt to call a nil value (global 'G')` is
> the failure D-04 names, and its fallback with no new doubt is written there (drop `self:tim()` from
> 255/0, open every re-fitted Setup with `ele[#ele]:tim()`). After `PUT BACK` the pad plays your own
> again, and `install-snapshot` on `/dev/install/` reads four lengths with no `v1` or `v2` after them.
> | Whether the system Setup's `self:tim()` defines the second half on a PAGE LOAD - not on a write -
> was never tapped (research B.6). A write runs the body; only a KEEP and a power cycle run init.lua,
> and only a module power-cycles. **Nothing in plan 12.1-08 claims the page-load proof works; this row
> is where it would, and the KEEP is your hand, not an agent's.** |

**Row K, as written:**

> **K** | Four strings: CLEAR, PUT BACK, a pre-12.1 record (SAFE-03, -07; D-22) | Connected on any Lua
> card: `CLEAR`; then `TRY ON DEVICE` and `PUT BACK`. If this browser has a snapshot from before 12.1
> (a `KEEP` or `TRY` before today), connect with it and `PUT BACK`. Then on `/dev/install/`, connect,
> and **write down the four lengths `install-snapshot` shows and whether a `v1` or `v2` follows
> them.** | After `CLEAR` the pad returns to the firmware's own touch default (the dim white finger
> halo) and nothing prints anywhere: CLEAR now writes `print("tick")` into 255/6 and the firmware's
> page init into 255/0 beside the touch pair's defaults, and a KEEP after it must store nothing there
> (cfg_default_flag). After `TRY ON DEVICE` then `PUT BACK` your own configuration is back, by eye, on
> all four slots - the page timer restored first. With a pre-12.1 record: `install-snapshot` reads `v2`
> (a Phase 12 record; 255/6 restored as the firmware's 22-character default) or `v1` (a Phase 7 record;
> both system slots restored as defaults), the panel's own line about the record predating the timer
> slot fires on either, and `PUT BACK` still restores. **Four lengths, and the key, written down.** |
> The panel cannot see flash: whether a KEEP after CLEAR stores nothing in 255/6 (cfg_default_flag) and
> whether the firmware's own `print("tick")` in 255/6 prints to nobody are module facts. Whether a
> record taken before the timer slot existed still restores the pad is a fact about this browser's
> storage and a real module's answer to the 22-character default, which the scripted ZONA restores
> because it was told to. |

The plan's row K text said "Four strings: CLEAR, PUT BACK, and a record from before 12.1 (SAFE-03,
SAFE-07, 12.1 D-22)" - 86 characters, over the Row column's 70; it is shortened to fit without
re-wrapping the table. The plan's row J put the observation in the "Do this" column; here it is split
across "Do this" / "Passes when" / "Why a machine cannot" as every other row is. The "Why" column's
"the panel cannot see flash" clause is K's.

**The dated line under "Seven rows"** (not rewritten; a paragraph beneath it): "Eleven rows since
12.1-08 (2026-09-11): 13-12's page-switch row, then the page-load proof (D-04 answered yes,
2026-09-11), then the four-string CLEAR / PUT BACK. Rows J and K are Phase 12.1's, written and
unanswered; the gate (12.1-09) hands them over in its own order."

**The token (D-21).** `src/lib/ui/intro/HeroSurface.svelte:137` and
`src/routes/playground/[id]/+page.svelte:447` (the route's line is 447 today, 436 when 12.1-05
wrote its sentence; 13-11 and 12.1-07 added lines above it) each read
`y: mapAxis(event.clientY - rect.top, rect.height, engine.coordMax, "y"),`. `git diff --numstat`:
**1 / 1 on each** - one insertion, one deletion, nothing more was needed. **`Coverflow.svelte` does
not exist** (`test -f` false; 13-09 deleted it, as 12.1-05 recorded), so the route is the second site
and the plan's third file is a no-op on a missing path. `check` 627 / 0 / 0 and `lint` clean after;
the intro's unit tests do not drive the pointer, and the quick suite is unchanged.

## Counts, as carried names plus deltas

Measured at this plan's start on `ab2ee64`, with no other agent running.

| Name | Carried from (12.1-07) | This plan's term | Observed |
| --- | --- | --- | --- |
| `PREV_FILES` | **90** | **+0** | **90** |
| `PREV_TESTS` | **933** (+1 todo) | **+0** (install-copy.spec's sentence moves inside its 6; wire-pin.spec pins both strings inside its 4; every assertion moves inside a test) | **933** (+1 todo), all green |
| Band 2's three terms | | 12.1-06 +0 / +2, 12.1-07 +0 / +3, 12.1-08 +0 / +0 | **+0 / +5**, stated here once |
| `BASE_SWEEP` | `4 19` | +0 | `4 19` by declaration (no Lua moved; not run) |
| e2e titles | **80** | **+0** by `grep -c "test("` | **80** |
| e2e runs | **96** | **+0**, the suite RUN in chunks twice | **96** = 34 + 21 + 20 + 10 + 11 |
| `BASE_CHECK` | 627 / 0 / 0 | +0 | **627 files, 0 ERRORS, 0 WARNINGS** (three times) |
| audition rows | 27 | +0 | 27 (untouched) |
| runbook rows | A-I (9) | **+2** (J, K) | **A-K (11)** |
| catalog | 26 | +0 | 26 (untouched) |
| library | 781 / 705 | +0 | `library.ts` byte-unchanged since `ab2ee64` |

**The quick suite, three whole runs.** Run 1 (after task 03, before the rebuild): 932 passed, 1
red - `radius.spec.ts` layer B's stale-build guard (`build/` predated the two `.svelte` edits),
cleared by the rebuild exactly as 12.1-06 and 12.1-07 saw. Run 2 (the committed tree, fresh build at
`166d18b`, 1.14 GB free of 15.26): 932 passed, 1 red - **the named quick-suite transient**,
`install.spec.ts` "the snapshot is taken at connect, in order, before ready" timed out waiting for
identification; `install.spec.ts` alone 24 / 24. Run 3 (`--maxWorkers=2`, 0.57 GB free): **90 files,
933 passed, 1 todo - `check-counts.mjs 90 933` matches.** `+0 / +0`, written out.

### The suites, as run

| Command | Result |
| --- | --- |
| `npx vitest run --project server src/lib/device/install-copy.spec.ts src/lib/device/wire-pin.spec.ts src/lib/config-shape.spec.ts` | 24 passed (6 + 4 + 14); wire-pin **4**, install-copy 6 |
| `... --maxWorkers=2 src/lib/device/install.spec.ts src/lib/ui/device-ui.spec.ts src/lib/ui/shell` | 47 passed |
| `npm run check` | 627 files, 0 errors, 0 warnings - after task 01, after task 03 |
| `npm run lint` | exit 0 (prettier --check over the tree, then eslint) |
| `npx vitest run --project server --maxWorkers=2 \| node scripts/check-counts.mjs 90 933` | matches |
| `npm run build` | clean at `b5ecd40` (run 1's build) and at `166d18b` (`postbuild: 166d18b…`, source archive 1995 KB); two `EPERM` failures in between while orphaned servers held `build/` (deviation 4) |
| `bash e2e-chunks-1208.sh` | five chunks, 96 in each whole run (table above) |
| `git diff --quiet ab2ee64 -- src/vendor/ .planning/ROADMAP.md src/lib/transport/sequence.ts src/lib/catalog/library.ts src/lib/transport/fixtures/synthetic.ts e2e/fake-zona.ts src/lib/fidelity/firmware-oracle.spec.ts src/lib/protocol-pin.spec.ts src/lib/protocol/forbidden-instructions.spec.ts` | silent |
| `git status --short \| grep 13-gui-overhaul` | nothing |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The union literals' consumers would not type-check**
- **Found during:** Task 01
- **Issue:** The plan's "the partial sentences name which of four landed" means rewriting the two
  older `LandedWords` literals (12.1-07 recorded them as understating by one slot). They are
  consumed as literals by `install.svelte.ts`'s classifier, `InstallState.svelte`'s fallback,
  `device-clause.ts`, `device-ui.spec.ts` and `install.spec.ts` - five files outside the plan's list -
  and `check` would have reported errors in each.
- **Fix:** Each consumer moved to the new literal (`"The system timer and the page init"`,
  `"The system timer, the page init and the Timer"`); no logic moved. `install.svelte.ts` gained one
  comment line naming the plan.
- **Files modified:** the five named
- **Commit:** `b5ecd40`

**2. [Rule 1 - Bug, in the plan's needle table] A count of three the table did not name**
- **Found during:** Task 02, chunk 1's first run
- **Issue:** The CLEAR test's tail (`install.e2e.ts:2005`) asserted `CONFIG/EXECUTE` at 9 ("the
  try-on, the clear and the put-back are THREE CONFIG/EXECUTE each"); the plan's table listed the
  test's poll (3 -> 4) and delta (3 -> 4) but not the tail. Chromium read 12, webkit-phone 13 (12 plus
  one retried write under three workers).
- **Fix:** 12, with the comment reading FOUR each. Both projects green on the whole rerun.
- **Files modified:** `e2e/install.e2e.ts`
- **Commit:** `5b93d6a`

**3. [Rule 1 - Bug, in the plan's needle table] "the timer fetch is still the last chunk"**
- **Found during:** Task 02
- **Issue:** The plan's row for `session.e2e.ts:315` says "four; the timer fetch is still the last
  chunk". Since 12.1-06 the fetch order is SLOTS' (255/6, 255/0, 0/6, 0/0), so the last config chunk
  `#snapshot` issues is the touch **Setup**'s (0/0), and `install.spec.ts` asserts that order.
- **Fix:** The poll message names the Setup fetch as the last config chunk. The poll itself waits on
  the count, so the message is descriptive; it is now also true.
- **Files modified:** `e2e/session.e2e.ts`
- **Commit:** `5b93d6a`

**4. [Rule 3 - Blocking, in the method] The chunk script's servers never stopped**
- **Found during:** Task 02, before negative check B's build
- **Issue:** `e2e-chunks.sh`'s `taskkill /F /T /PID` had its flags rewritten to `F:/` and `T:/` by
  Git Bash's path conversion when run from this session, so nine `wrangler dev` trees stayed alive
  after run 1, held `build/` (`npm run build` failed `EPERM` twice) and listened on 4173 beside each
  new server.
- **Fix:** Every process whose command line matched `wrangler` and every `workerd.exe` stopped
  through PowerShell (the session's own bash shells that matched were stopped too); a copy of the
  script, `e2e-chunks-1208.sh`, with `MSYS_NO_PATHCONV=1`, a PowerShell stop over
  `wrangler.*dev --port 4173` and `workerd.exe`, and free memory per chunk. Nothing in the tree
  changed; run 2 and both reruns used the copy, each chunk reporting `HTTP 000` after its stop.
- **Files modified:** none in the repository (scratchpad only)

### Recorded, not fixed

- `SNAPSHOTTING_BODY` and `identifiedBody` still name three scripts (above, "Left for 13-18").
- `e2e/fake-zona.ts` is in the plan's file list and is byte-unchanged (nothing structural was
  needed; the reason is in task 02).
- The plan's row K title is 86 characters against a 70-character column and was shortened; the
  plan's row J was one column's text and is split across the table's three (task 03).
- `12.1-05-SUMMARY.md`'s line number for the route (`:436`) is `:447` today; the line itself is the
  one described.
- Negative check B (below) ran once against the old build before deviation 4 was found - that run
  proved nothing and is not counted; the check was re-run against a rebuilt plant.
- The quick-suite transient (`install.spec.ts`'s identification wait) fired once in three whole
  runs; `--maxWorkers=2` cleared it, as 12.1-07 found.

## Negative checks

| # | Task | Plant | Where | Result | Restored |
| --- | --- | --- | --- | --- | --- |
| 1a | 01 | the clause reverted to "init script" in the module | `install-copy.ts` | **red by name**: `CONFIRM_REPLACES does not ship the string this row says it ships` (1 failed, 5 passed) | copy back, sha256 `33228268f8062226` both sides |
| 1b | 01 | the spec's row claims the OLD clause as the insertion | `install-copy.spec.ts` | **red from the other side**: `CONFIRM_REPLACES's amendment is not an insertion - something other than the added clause moved` | copy back, sha256 `0550e90f84dfb395` both sides |
| 2 | 01 | the two library halves swapped in the Lua landing | `model.ts` | **red on both pins**: test 2 `euclid: the published pair is not renderLua's text` and test 3 `255/6 is not TOUCH_LIBRARY_TIMER, verbatim: expected '--[[@cb]]H={}T={}…' to be '--[[@cb]]function V(n)…'` (2 failed, 2 passed) | copy back, sha256 `fcc06942361145ba` both sides |
| A | 02 | `CONFIG/FETCH` left at 3 in the connect test | `install.e2e.ts` | **red on the count**: `Expected: 3, Received: 4` at `:484`, on a fresh server | copy back, sha256 `07198a67d25b7144` both sides |
| B | 02 | `pair()` as `{ system, systemTimer, setup, timer }`, rebuilt | `+page.svelte` | **nothing changes**: the TRY test 1 passed, `write-system-timer ok 1` still first - the order is SLOTS', not the caller's | copy back, sha256 `70baa8fe7669deb2` both sides; rebuilt at `166d18b` after |
| - | 03 | none that edits source; the runbook read back through the audition parser's shape | scratch script | eleven lettered rows `ABCDEFGHIJK`, J and K as asserted above, every `Pass` cell empty | - |

No `git checkout`, `git restore`, `git stash` or `git clean` was run; every restore is a copy back from
the scratch directory with the hash compared.

## What 12.1-08b and 12.1-09 will find

- **Band 2 is complete** (12.1-06, 12.1-07, 12.1-08) and **13-14 is released**. Phase 12.1 stands at 9
  of 11; 12.1-08b (the presets, D-27 "mirror") is next, then the gate.
- **The confirmation and the three partial sentences are quoted above for 13-18**; `13-COPY-NEW.md`
  was not touched.
- **Runbook rows J and K** are the bench's, unanswered; the gate hands them over in its order (D-24)
  with D-26 item 3's permission already recorded in J.
- **The probe's `install-snapshot`** reads `durable <4 lengths>[ v1| v2]`; the panel's "predates the
  timer slot" line should fire on either flag.
- **`e2e-chunks-1208.sh`** is the chunk script that stops its servers on this machine; the original's
  `taskkill` flags are mangled by Git Bash path conversion when run from a Claude shell.
- 12.1-08b changes what a PRESET lands (`system` and `systemTimer` no longer empty for the nine); the
  wire-pin's test 3 asserts AURORA's two system frames carry the empty string today - **12.1-08b will
  move that assertion** (the frames stay four; the strings become the library's or the preset's own).

## Questions for the user

None that change this plan's shape. Two recorded:

1. `SNAPSHOTTING_BODY` and `identifiedBody` still say "the page's own init script" though the
   snapshot copies the page timer too. Left for 13-18 by the plan's scope ("the sentence changes, the
   shape does not"); if they should move before 13-18, it is two rows in the same table.
2. Row J is written with the permission recorded (D-26 item 3). If the wording should still carry
   the choice to decline, it is one clause in the row's first sentence.

## Self-Check: PASSED

- `src/routes/dev/install/+page.svelte` contains `install-system-timer` - FOUND
- `src/lib/device/wire-pin.spec.ts` contains `TOUCH_LIBRARY_TIMER` - FOUND
- `docs/INSTALL-RUNBOOK.md` contains `12.1 D-04` - FOUND; eleven lettered rows
- `e2e/install.e2e.ts` -> `e2e/fake-zona.ts` via `zona.seen("CONFIG", "FETCH")).toBe(4)` - FOUND
- `src/lib/ui/intro/HeroSurface.svelte` -> `src/lib/sim/touch.ts` via `mapAxis(..., "y")` - FOUND
- commits `b5ecd40`, `5b93d6a`, `166d18b` - FOUND in `git log`
- no file created; `+0 / +0` observed as 90 / 933 (+1 todo), check-counts matches; e2e 80 / 96 twice
