---
phase: 12.1-gradient-touch
plan: 03
type: execute
wave: 3
band: 1
depends_on: ["12.1-02"]
files_modified:
  - src/lib/catalog/entries/euclid.ts
  - src/lib/catalog/entries/steps.ts
  - src/lib/catalog/entries/radar-points.ts
  - src/lib/catalog/entries/sonar.ts
  - src/lib/catalog/entries/arc.ts
  - src/lib/sim/lua-smoke.spec.ts
  - docs/HARDWARE-AUDITION.md
  - src/lib/catalog/audition.spec.ts
autonomous: true
requirements: [CONT-02, CONT-03, PREV-01]
count_term: "PREV_FILES + 0 / PREV_TESTS + 2 (lua-smoke 32 -> 34); audition 24 + 1; sweep 4 19 run and unmoved; EUCLID 700 -> 726, STEPS 394 -> 420, RADAR POINTS 489 -> 592, SONAR 469 -> 572, ARC 541 -> 528 at the picker corner"

must_haves:
  truths:
    - "On the four cell-toggling sequencers a finger is drawn as a gradient on layer 0 in its own colour and the step it toggles is the LED under it - the toggle and the light agree because both read the calibrated map - and the outer Euclid ring is reachable at the LED instead of a third of a cell inward."
    - "RADAR POINTS' and SONAR's centre marker survives a finger passing over it AND every expiry path - the Timer sweep and a lost-lift re-press - because the entry's R re-lights and re-colours it and E calls R after V."
    - "ARC's stop tap tests the calibrated centre through N and no longer the naive divisor."
    - "Every figure in every header is re-measured at the RGB444 picker corner in this tree, no knob and no tag moves, and no header comment quotes a guard the Setup no longer carries."
  artifacts:
    - path: "src/lib/catalog/entries/euclid.ts"
      provides: "G before Q, white"
      contains: "G(s,i,e,x,y,0,255,255,255)local m=Q(s,i,e,x,y)"
    - path: "src/lib/catalog/entries/arc.ts"
      provides: "N(x,y)==40 as the stop test"
      contains: "N(x,y)==40"
  key_links:
    - from: "src/lib/catalog/entries/radar-points.ts"
      to: "src/lib/catalog/library.ts"
      via: "R re-lights and re-colours cell 40; E calls R after V, and the callback calls R after G"
      pattern: "R=function\\(s,i\\)local a=glag\\(0,40\\)glc\\(a,0,@SWEEPC,1\\)glp\\(a,0,255\\)end"
---

<objective>
The four sequencers and ARC, re-fitted.

**What the map alone gives them, before any gradient** (research section C): every `Q` caller's cell
moves to where the LEDs are. The bench case (row 1, col 7, zero-based) toggles cell 16 instead of the
corner; EUCLID's outer ring (index 1 and 7) becomes reachable at the LED instead of a third of a cell
inward - which is the whole of *"i need to tap multiple times"* (research section E: the outer LED ring
is on no Euclid ring at all and `if not v then return end` dropped the press silently). That fix landed in
plan 02 and costs these entries **nothing**.

**What this plan adds is the light, in the entry's colour, handed to `G`** (D-11). `G(s,i,e,x,y,0,r,g,b)`
on the line before `Q`; no init-loop colouring, because `G` colours its four cells on every call and an
alert-layer rewrite heals on the next sample. Measured by the planner on the measured knots: EUCLID
700 -> **726** (182 free), STEPS 394 -> **420** (488), RADAR POINTS 489 -> **592** (316), SONAR 469 ->
**572** (336), ARC 541 -> **528** (380, the naive `x*9//128+y*9//128*9==40` replaced by `N(x,y)==40`).
**Re-measure every one in this plan before its header carries it.**

**RADAR POINTS and SONAR: the centre dot, on every path.** Both light cell 40 on layer 0 at init as the
sweep's origin. `V` clears a 2x2 that can cover 40 - and it runs not only under `G` in the callback but
**on every expiry through `E`** (the Timer sweep, a lost-lift re-press), where the plan-check found the
re-light missing. So each defines the library's release convention: `R=function(s,i)local a=glag(0,40)
glc(a,0,@SWEEPC,1)glp(a,0,255)end` - idempotent, as `R` must be (12-07), called by `E` **after** `V` on
every expiry, and called by the callback after `G`. It re-colours as well as re-lights, so an alert
cannot take the marker's colour either (D-15).

**The needles are exact and are applied to the string literal only.** `local m=Q(s,i,e,x,y)` also occurs
in `euclid.ts:70` (a header comment); `local n=Q(...)` in `radar-points.ts:39` and `:208`, `sonar.ts:83`;
a file-wide exactly-once assertion refuses. The substitution script targets the one line matching
`^  "--\[\[@cb\]\].*";$`, and **the header comments that quote the old guard are rewritten to quote the
new one** so they do not go stale.

Purpose: the entries the bench complained about, drawing the finger where it is, in a colour that heals.
Output: five entries re-fitted, two VM tests, one audition row. Term `+0 / +2`, audition `+1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-RESEARCH.md
@.planning/phases/12.1-gradient-touch/12.1-PLAN-CHECK.md
@.planning/phases/12.1-gradient-touch/12.1-02-SUMMARY.md
@.planning/phases/12-touch-framework/12-08-SUMMARY.md
@.planning/phases/12-touch-framework/12-05-SUMMARY.md
@src/lib/catalog/library.ts
@src/lib/catalog/calibration.ts
@src/lib/catalog/entries/euclid.ts
@src/lib/catalog/entries/steps.ts
@src/lib/catalog/entries/radar-points.ts
@src/lib/catalog/entries/sonar.ts
@src/lib/catalog/entries/arc.ts
@src/lib/catalog/lua-entries.sweep.spec.ts
@docs/HARDWARE-AUDITION.md

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (the repository's rule wins
  over the harness's reminder, 12-12 deviation 10). **No emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Every re-fit is proved in wasmoon and
  measured under the pin; how it looks on a diffuser is audition row 25's.
- **Sibling repositories read-only; `src/vendor/` untouched; the phase walk never changes;
  `firmware-oracle.spec.ts` green and unedited; no manifest row.**
- **Do not edit `.planning/ROADMAP.md`.** Nothing under `13-gui-overhaul/` or `12-touch-framework/`.
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress`.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push.
- **Append-only:** `docs/HARDWARE-AUDITION.md` - row 25 appended inside the table, `ROW_COUNT` moved, no
  row reflowed.
- **Every cost at the RGB444 picker corner under the pinned `compressScript` after `initLuaFormatter()`,
  `cost = max(raw, compressed)`, canonical - RUN, never sketched**, with the scratchpad harness's
  `corner()` (every colour knob `255,255,255`, every other knob its longest value) and `checkSyntax`;
  `measure-12.1-d11.mjs` carries the needles and the figures.
- **No new knob and no change to any knob's value count; no tag moves** (CONT-03). The finger colour is
  a literal (D-13).
- **Both Phase 11 gates over every string; `D`'s 42-tick ceiling; `decay-idiom.spec.ts` blind to `D(`.**
- **State every count as a carried name plus a delta, re-measured at this plan's start**, then assert
  **`+0 / +2`** with its one part.
- **Concurrency:** 13-08 / 13-09 / 13-11 on `src/routes/`, `src/lib/ui/`, `src/lib/browse/`, `e2e/`,
  `scripts/gen-og.mjs` - none of this plan's files. **13-19 (wave 19) will re-case the names in these
  entry files later; nothing here touches a name.** `lua-smoke.spec.ts` edited by appending; header count
  32 -> 34.
- **`frames.json` is NOT regenerated here** - plan 05 regenerates it once for all ten re-fits, so
  `frames.spec.ts` (five tests) is red for the re-fitted entries until then. **The quick run therefore
  excludes it**: `npx vitest run --project server --exclude src/lib/catalog/frames.spec.ts 2>&1 | node
  scripts/check-counts.mjs <observed files - 1> <observed tests + 2 - 5>` - `check-counts.mjs` dies on any
  failure, so the exclusion is the only way to assert the rest (the 10-05 / 12-10 shape), and the SUMMARY
  names the excluded file and its closer.
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## The needles, exact, applied to the Setup string literal only (from `measure-12.1-d11.mjs`)

| Entry | Callback: before -> after | Elsewhere |
|---|---|---|
| EUCLID | `local m=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255,255,255)local m=Q(s,i,e,x,y)` | rewrite the comment at `:70` |
| STEPS | `local a=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255,255,255)local a=Q(s,i,e,x,y)` | - |
| RADAR POINTS | `local n=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,@SWEEPC)R(s,i)local n=Q(s,i,e,x,y)` | `--[[@cb]]` -> `--[[@cb]]R=function(s,i)local a=glag(0,40)glc(a,0,@SWEEPC,1)glp(a,0,255)end ` (the definition first in the Setup); rewrite the comments at `:39` and `:208` |
| SONAR | as RADAR POINTS | as RADAR POINTS; rewrite the comment at `:83` |
| ARC | `x*9//128+y*9//128*9==40` -> `N(x,y)==40` | - |

**No init-loop needle** (D-11): the layer is coloured by `G` per call. RADAR POINTS' and SONAR's existing
`local h=glag(0,40)glc(h,0,@SWEEPC,1)glp(h,0,255)` at init stays as it is.

**Layer 0 is the firmware's alert layer** (`grid_led.h:7`); `grid_alert_all_set` rewrites its colour on a
CONFIG write, on page-discard completion, on a refused page change, on a TX overflow and at boot
(`12.1-CONTEXT.md` D-11). `G` re-colours its four cells every call and `R` re-colours 40 on every expiry,
so the exposure is one flash and a self-healing tint - say so in each header once; audition row 25(j)
asks the bench to confirm it. There is **no floor**: `glc(...,1)` forces the layer's min to 0.

## Audition row 25

`| 25 | EUCLID / STEPS / RADAR POINTS / SONAR | **THE GRADIENT, AND IT IS THE FIRST TEN MINUTES OF THE BENCH (plan 12.1-03; D-01).** Install EUCLID through the panel (four strings now). **(a)** Put a fingertip dead on the LED at row 2 / column 8 (one-based): does THAT LED light alone and bright, where round 3 lit the corner? **(b)** Between two LEDs: do both glow dimly, and does sliding toward one brighten it? **(c)** In the middle of four: all four? **(d)** Tap the outer Euclid ring (index 1 and 7) at the LED, ten times: does every tap toggle on the first try, where round 3 needed several? **(e)** Does the dim end read as "dimly", or too bright (the weights are linear; a square-law is a few characters away)? **(f)** With no finger down, is the dark pad exactly as dark as before (there is no floor: glc's trailing 1 zeroes it)? **(g)** Rest a finger on the seam between two ring cells and hold still: does the toggle flip back and forth, or hold? **(h)** Tap with a rolling finger: does the cell that toggles differ from the LED that ends up brightest? **(i)** RADAR POINTS / SONAR: pass a finger over the centre, lift badly (slide off), wait three seconds - is the centre dot still there? STEPS: does the column sweep still read as one bar under the gradient? **(j) THE ALERT LAYER**: after a page switch (13-12) or a refused one (a purple flash), and after the page-load row's power cycle: is the finger still the entry's colour on the next touch? A grey finger that heals on the next sample is the design; one that stays grey is a bug. | The sensor is not under the LEDs and the simulator's pointer is a mouse; only a finger on this unit can say where the light lands, how the dim end reads, and what the alert layer does to the colour. |`

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-03-01: Five entries re-fitted by exact needles on the string literal, every header re-measured, stale comments rewritten</name>
  <files>src/lib/catalog/entries/euclid.ts, src/lib/catalog/entries/steps.ts, src/lib/catalog/entries/radar-points.ts, src/lib/catalog/entries/sonar.ts, src/lib/catalog/entries/arc.ts</files>

  <read_first>
    - each entry's header - the cost paragraph ("THE CORNER THE 908 GATE READS IS 700 / 237" and its
      siblings), the "finger" paragraph this task rewrites, and **the comment lines that quote the guard**
      (`euclid.ts:70`, `radar-points.ts:39` and `:208`, `sonar.ts:83`)
    - `12-08-SUMMARY.md` - how the four sequencers were put on `Q` and what each header says about the
      finger (the "armed cells toggle under the finger and that IS the feedback" sentence is superseded)
    - `12-05-SUMMARY.md` - ARC's stop tap and its eighteen-character fix
    - `src/lib/catalog/lua-entries.sweep.spec.ts` test 1 - how a string is measured and canonicalised
    - `chorus.ts:129` - the one `R` in the catalog today, as the shape RADAR POINTS' and SONAR's follow
  </read_first>

  <action>
**1. Substitute** each needle per the interfaces block with a script that isolates the one line matching
`^  "--\[\[@cb\]\].*";$` in the file, asserts exactly one occurrence of the needle **inside that line**,
replaces it there, and leaves every other line byte-identical. **Do not retype a Setup.** Then rewrite
the four header comments named above so each quotes the new guard.

**2. Re-measure** every touched entry at the picker corner with the harness (copy
`measure-12.1-d11.mjs`, point it at the edited files, run): expect EUCLID 726 / 237, STEPS 420 / 260,
RADAR POINTS 592 / 288, SONAR 572 / 289, ARC 528 / 275, each canonical, `checkSyntax` true. **If any
differs, stop and name it** - the needle landed somewhere the harness did not model.

**3. Headers**: the corner figure and free characters updated; a paragraph per entry on the finger: `G`
on layer 0 in white for EUCLID and STEPS, in `@SWEEPC` for the two sweeps, with the alert sentence and
`R`'s two duties (re-light and re-colour 40 on every expiry and after every `G`); the sentence that the
cell `Q` returns is now the LED under the finger and what that fixed on the bench (EUCLID: the outer
ring); ARC: `N` and the thirteen returned. **No knob, no tag, no name moves.**

**4. `npm run test:sweep`** - `4 19` green: every entry inside 908 at every corner with the new Setups.

**Negative checks:** apply EUCLID's callback needle to the whole file in a scratch copy - the script
refuses on count (two occurrences: `:70` and the literal); put `R(s,i)` before `G` in RADAR POINTS - task
02's marker test goes red on the callback path because `V` runs after the re-light. Restore, sha256 both
sides.
  </action>

  <verify>
    <automated>npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && npx vitest run --project server src/lib/catalog/touch-guard.spec.ts src/lib/catalog/decay-idiom.spec.ts src/lib/catalog/host-surface.spec.ts && test "$(grep -c 'local m=Q(s,i,e,x,y)if$' src/lib/catalog/entries/euclid.ts)" = "0"</automated>
    The sweep unmoved; both class gates and the surface gate green over the five new Setups (the
    surface gate is what refuses an entry that assigns a library name - `R=` is a convention it admits);
    no comment still quoting the old guard.
  </verify>

  <done>
Five Setups changed by exact needles on the string literal with the occurrence count asserted there;
four stale comments rewritten; every corner figure re-measured in this tree and written into its header
with the finger paragraph; RADAR POINTS and SONAR define an idempotent `R`; the sweep `4 19` green; two
negative checks red and restored.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-03-02: The four sequencers and ARC under G in the real VM, the marker on every expiry path, and row 25</name>
  <files>src/lib/sim/lua-smoke.spec.ts, docs/HARDWARE-AUDITION.md, src/lib/catalog/audition.spec.ts</files>

  <read_first>
    - `lua-smoke.spec.ts` - the 12.1 describe block plan 02 appended (its host helper and layer-0
      phase/colour readers) and the existing EUCLID / STEPS / RADAR POINTS / SONAR / ARC blocks (how each
      entry is rendered at its defaults and driven; how a Timer sweep is advanced)
    - `src/lib/catalog/calibration.ts` - `KX`, `KY`, `calibratedAxis`
    - `src/lib/catalog/audition.spec.ts` - `ROW_COUNT` (24 after plan 01) and its four `it(`
  </read_first>

  <action>
Append two tests to the 12.1 describe block:

1. **The four sequencers**: for each of EUCLID, STEPS, RADAR POINTS, SONAR rendered at its defaults with
   both library strings - for every `(c, r)` in 0..8 x 0..8 press at `(KX[c], KY[r])`, lift, and assert
   that while pressed layer 0 held cell `c + r*9` at 255 **in the entry's finger colour** (white; the
   default `@SWEEPC` for the two sweeps) and **no other layer-0 cell above 0**, with the one exception
   that RADAR POINTS and SONAR hold cell 40 at 255 always; then a midpoint press (between LED 4 and 5 in
   `x`, on `KY[4]`) lights exactly two layer-0 cells; then the toggle: on EUCLID a press at
   `(KX[1], KY[4])` (the outer ring, index 1) flips a step - assert the entry's own state table or layer-1
   phase moved, which is the tap the bench said needed several tries. **Then the marker on the expiry
   paths** (RADAR POINTS and SONAR): press on `(KX[4], KY[4])` so the block covers 40 and never lift;
   advance the Timer past the sweep window (`X(s,n)` with the entry's own `n`) - cell 40 is at 255 in
   `@SWEEPC` after the sweep; press again with the same contact id on a different cell (the lost-lift
   re-press) - 40 still at 255. Print, once, the 81 cells that lit for EUCLID as a 9x9 of `.`/`#` so the
   SUMMARY can show the map is the identity.
2. **ARC's stop**: a press at `(KX[4], KY[4])` stops the swirl (the entry's existing "stopped" observable
   from 12-05's test); a press at `x = KX[3] + (KX[4]-KX[3])//2 + 1, y = KY[4]` (56 on these knots) -
   which `calibratedAxis` rounds to LED 4 and the naive divisor to column 3 - **also stops it**; print
   both cells beside the naive divisor's answer, computed in the test.

Header count 32 -> 34. Row 25 appended per the interfaces block; `ROW_COUNT` 24 -> 25; the test title
"twenty-four" -> "twenty-five".

**Negative checks:** remove `R` from RADAR POINTS' Setup in a scratch copy - test 1's sweep clause red
(40 dark after the sweep); revert ARC's needle - test 2 red on the off-centre press. Restore, sha256 both
sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/audition.spec.ts</automated>
    `lua-smoke.spec.ts` **34**; `audition.spec.ts` **4** at `ROW_COUNT` 25.
  </verify>

  <done>
Every LED centre lights its own cell alone in the entry's colour on all four sequencers, a midpoint
lights two, the outer-ring tap toggles, the sweeps keep their centre dot through a sweep expiry and a
lost-lift re-press, ARC stops on the calibrated centre where the naive divisor would not; row 25 asks
the bench the first ten minutes' questions and the alert-layer one; two negative checks red and restored.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npx vitest run --project server --exclude src/lib/catalog/frames.spec.ts 2>&1 | node scripts/check-counts.mjs <observed files - 1> <observed tests + 2 - 5>` - `frames.spec.ts` (five tests) excluded and named, red for the re-fitted entries until plan 05; **this plan's term is `+0 / +2`, written out**.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - run, unmoved.
- `grep -c "test(" e2e/*.e2e.ts` summed, unchanged - e2e `+0 / +0`.
- `git diff --stat HEAD -- src/vendor/` empty; `git diff --quiet -- .planning/ROADMAP.md`; no knob's
  `values` array and no `tags` array changed (`git diff -U0 -- src/lib/catalog/entries/ | grep -E "values|tags"` empty).
</verification>

<success_criteria>
1. The four sequencers draw the finger as a gradient on layer 0 in their own colour and toggle the LED
   under it, proved at all 81 centres in a real VM.
2. RADAR POINTS' and SONAR's centre dot survives the finger, the sweep expiry and the lost-lift re-press.
3. ARC stops on the calibrated centre.
4. Every header figure is a measurement made in this tree; no knob, tag or name moved; no comment quotes
   a guard the Setup no longer carries.
5. Row 25 hands the finding to the bench in the order the gate will use, with the alert-layer clause.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-03-SUMMARY.md` carrying: the
observed baseline and `+0 / +2`; the five corner figures before and after with free characters; the
EUCLID 9x9 identity print; the marker's two expiry paths as observed; ARC's two stop cells beside the
naive answer; the sweep line; row 25 as written; the excluded `frames.spec.ts` named with plan 05 as its
closer; the negative checks; and the sentence that no device was touched.
</output>
