---
phase: 12.1-gradient-touch
plan: 03
type: execute
wave: 3
band: 1
depends_on: ["12.1-02"]
files_modified:
  - src/lib/catalog/entries/euclid.ts
  - src/lib/catalog/entries/steps.ts
  - src/lib/catalog/entries/radar-points.ts
  - src/lib/catalog/entries/sonar.ts
  - src/lib/catalog/entries/arc.ts
  - src/lib/sim/lua-smoke.spec.ts
  - docs/HARDWARE-AUDITION.md
  - src/lib/catalog/audition.spec.ts
autonomous: true
requirements: [CONT-02, CONT-03, PREV-01]
count_term: "PREV_FILES + 0 / PREV_TESTS + 2 (lua-smoke +2); audition 24 + 1; sweep 4 19 run and unmoved; EUCLID 700 -> 740, STEPS 394 -> 434, RADAR POINTS 489 -> 550, SONAR 469 -> 530, ARC 541 -> 528 at the picker corner"

must_haves:
  truths:
    - "On the four cell-toggling sequencers a finger is drawn as a gradient on layer 0 and the step it toggles is the LED under it - the toggle and the light agree because both read the calibrated map - and the outer Euclid ring is reachable at the LED instead of a third of a cell inward."
    - "RADAR POINTS' and SONAR's centre marker survives a finger passing over it, re-lit after every G call."
    - "ARC's stop tap tests the calibrated centre through N and no longer the naive divisor."
    - "Every figure in every header is re-measured at the RGB444 picker corner in this tree, and no knob and no tag moves."
  artifacts:
    - path: "src/lib/catalog/entries/euclid.ts"
      provides: "G before Q; layer 0 coloured white in the init loop"
      contains: "G(s,i,e,x,y,0,255)"
    - path: "src/lib/catalog/entries/arc.ts"
      provides: "N(x,y)==40 as the stop test"
      contains: "N(x,y)==40"
  key_links:
    - from: "src/lib/catalog/entries/radar-points.ts"
      to: "src/lib/catalog/library.ts"
      via: "glp(glag(0,40),0,255) after the G call, because V can cover cell 40"
      pattern: "G\\(s,i,e,x,y,0,255\\)glp\\(glag\\(0,40\\),0,255\\)"
---

<objective>
The four sequencers and ARC, re-fitted.

**What the map alone gives them, before any gradient** (research section C): every `Q` caller's cell
moves to where the LEDs are. The bench case (row 1, col 7, zero-based) toggles cell 16 instead of 17;
EUCLID's outer ring (index 1 and 7) becomes reachable at the LED instead of a third of a cell inward -
which is the whole of *"i need to tap multiple times"* (research section E: the outer LED ring is on no
Euclid ring at all and `if not v then return end` dropped the press silently). That fix landed in plan
02 and costs these entries **nothing**.

**What this plan adds is the light.** `G(s,i,e,x,y,0,255)` on the line before `Q`, on layer 0, coloured
in the init loop; for RADAR POINTS and SONAR the centre marker re-lit after every `G` because `V` clears
a 2x2 that can cover cell 40 (D-15, +20 each). Measured by the planner on the measured knots, every
figure to the character against the research: EUCLID 700 -> **740** (168 free), STEPS 394 -> **434**
(474), RADAR POINTS 489 -> **550** (358), SONAR 469 -> **530** (378), ARC 541 -> **528** (380, the naive
`x*9//128+y*9//128*9==40` replaced by `N(x,y)==40`, thirteen shorter). **Re-measure every one in this
plan before its header carries it.**

**The needles are exact** and the harness proved each is present exactly once (`measure-12.1-plan.mjs`
throws on a missing needle). Substitution by string, never by hand-retyping a Setup.

Purpose: the entries the bench complained about, drawing the finger where it is.
Output: five entries re-fitted, two VM tests, one audition row. Term `+0 / +2`, audition `+1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-RESEARCH.md
@.planning/phases/12.1-gradient-touch/12.1-02-SUMMARY.md
@.planning/phases/12-touch-framework/12-08-SUMMARY.md
@.planning/phases/12-touch-framework/12-05-SUMMARY.md
@src/lib/catalog/library.ts
@src/lib/catalog/calibration.ts
@src/lib/catalog/entries/euclid.ts
@src/lib/catalog/entries/steps.ts
@src/lib/catalog/entries/radar-points.ts
@src/lib/catalog/entries/sonar.ts
@src/lib/catalog/entries/arc.ts
@src/lib/catalog/lua-entries.sweep.spec.ts
@docs/HARDWARE-AUDITION.md

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (the repository's rule wins
  over the harness's reminder, 12-12 deviation 10). **No emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Every re-fit is proved in wasmoon and
  measured under the pin; how it looks on a diffuser is audition row 25's.
- **Sibling repositories read-only; `src/vendor/` untouched; the phase walk never changes;
  `firmware-oracle.spec.ts` green and unedited; no manifest row.**
- **Do not edit `.planning/ROADMAP.md`.** Nothing under `13-gui-overhaul/` or `12-touch-framework/`.
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress`.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push.
- **Append-only:** `docs/HARDWARE-AUDITION.md` - row 25 appended inside the table, `ROW_COUNT` moved, no
  row reflowed.
- **Every cost at the RGB444 picker corner under the pinned `compressScript` after `initLuaFormatter()`,
  `cost = max(raw, compressed)`, canonical - RUN, never sketched**, with the scratchpad harness's
  `corner()` (every colour knob `255,255,255`, every other knob its longest value) and `checkSyntax`.
- **No new knob and no change to any knob's value count; no tag moves** (CONT-03). The finger colour is
  a literal (D-13).
- **Both Phase 11 gates over every string; `D`'s 42-tick ceiling; `decay-idiom.spec.ts` blind to `D(`.**
- **State every count as a carried name plus a delta, re-measured at this plan's start**, then assert
  **`+0 / +2`** with its one part.
- **Concurrency:** 13-08 / 13-09 / 13-11 on `src/routes/`, `src/lib/ui/`, `src/lib/browse/`, `e2e/`,
  `scripts/gen-og.mjs` - none of this plan's files. **13-19 (wave 19) will re-case the names in these
  entry files later; nothing here touches a name.** `lua-smoke.spec.ts` edited by appending; header count
  42 -> 44.
- **`frames.json` is NOT regenerated here** - plan 05 regenerates it once for all ten re-fits, so
  `frames.spec.ts` will be red between this plan and plan 05 for the re-fitted entries; **run the quick
  suite with `frames.spec.ts` excluded from the count and say so in the SUMMARY**, exactly as 10-05 and
  12-10 did between an entry change and its regeneration. The `check-counts` literal in this plan's
  verification accounts for that by naming the excluded file.
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## The needles, exact (from the harness; each present exactly once in the shipped Setup)

| Entry | Init loop: before -> after | Callback: before -> after |
|---|---|---|
| EUCLID | `glc(a,2,@RINGC,1)glp(a,2,0)end` -> `glc(a,2,@RINGC,1)glp(a,2,0)glc(a,0,255,255,255,1)end` | `local m=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255)local m=Q(s,i,e,x,y)` |
| STEPS | `glp(a,2,self.p[n]and 255 or 0)end` -> `glp(a,2,self.p[n]and 255 or 0)glc(a,0,255,255,255,1)end` | `local a=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255)local a=Q(s,i,e,x,y)` |
| RADAR POINTS | `glc(c,2,@SWEEPC,1)glp(c,2,0)` -> `glc(c,2,@SWEEPC,1)glp(c,2,0)glc(c,0,@SWEEPC,1)` | `local n=Q(s,i,e,x,y)` -> `G(s,i,e,x,y,0,255)glp(glag(0,40),0,255)local n=Q(s,i,e,x,y)` |
| SONAR | as RADAR POINTS | as RADAR POINTS |
| ARC | - | `x*9//128+y*9//128*9==40` -> `N(x,y)==40` |

**Layer 0 is the firmware's alert layer** (`grid_led.h:7`); firmware paints it white for 64 ticks after
every CONFIG write (`grid_decode.c:1281`), so for 0.6 s after a TRY the finger layer flashes in the
entry's layer-0 colour - cosmetic, already the case for RADAR POINTS and SONAR today. Say so in each
header once. **The floor**: `glc(a,0,255,255,255,1)` makes every dark cell glow at about byte 6 (research
C, "Layer 0's brightness floor") - a third layer at the same floor the other two already sit on; audition
row 25(f) asks whether it shows.

## Audition row 25

`| 25 | EUCLID / STEPS / RADAR POINTS / SONAR | **THE GRADIENT, AND IT IS THE FIRST TEN MINUTES OF THE BENCH (plan 12.1-03; D-01).** Install EUCLID through the panel (four strings now). **(a)** Put a fingertip dead on the LED at row 2 / column 8 (one-based): does THAT LED light alone and bright, where round 3 lit the corner? **(b)** Between two LEDs: do both glow dimly, and does sliding toward one brighten it? **(c)** In the middle of four: all four? **(d)** Tap the outer Euclid ring (index 1 and 7) at the LED, ten times: does every tap toggle on the first try, where round 3 needed several? **(e)** Does the dim end read as "dimly", or too bright (the weights are linear; a square-law is fifteen characters away)? **(f)** With no finger down, is the dark pad visibly brighter than before (a byte-6 floor on a third layer)? **(g)** Rest a finger on the seam between two ring cells and hold still: does the toggle flip back and forth, or hold? **(h)** Tap with a rolling finger: does the cell that toggles differ from the LED that ends up brightest? **(i)** RADAR POINTS / SONAR: pass a finger over the centre - does the centre dot vanish under it? STEPS: does the column sweep still read as one bar under the gradient? | The sensor is not under the LEDs and the simulator's pointer is a mouse; only a finger on this unit can say where the light lands and how the dim end reads. |`

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-03-01: Five entries re-fitted by exact needles, every header re-measured</name>
  <files>src/lib/catalog/entries/euclid.ts, src/lib/catalog/entries/steps.ts, src/lib/catalog/entries/radar-points.ts, src/lib/catalog/entries/sonar.ts, src/lib/catalog/entries/arc.ts</files>

  <read_first>
    - each entry's header - the cost paragraph ("THE CORNER THE 908 GATE READS IS 700 / 237" and its
      siblings) and the "finger" paragraph this task rewrites
    - `12-08-SUMMARY.md` - how the four sequencers were put on `Q` and what each header says about the
      finger (the "armed cells toggle under the finger and that IS the feedback" sentence is superseded)
    - `12-05-SUMMARY.md` - ARC's stop tap and its eighteen-character fix
    - `src/lib/catalog/lua-entries.sweep.spec.ts` test 1 - how a string is measured and canonicalised
  </read_first>

  <action>
**1. Substitute** each needle per the interfaces block with a script that asserts exactly one
occurrence before replacing (the harness's `rep()`), on the entry's Setup string literal in the `.ts`
file. **Do not retype a Setup.**

**2. Re-measure** every touched entry at the picker corner with the harness (copy
`measure-12.1-plan.mjs`, point it at the edited files, run): expect EUCLID 740 / 237, STEPS 434 / 260,
RADAR POINTS 550 / 288, SONAR 530 / 289, ARC 528 / 275, each canonical, `checkSyntax` true. **If any
differs, stop and name it** - the needle landed somewhere the harness did not model.

**3. Headers**: the corner figure and free characters updated; a paragraph per entry on the finger: `G`
on layer 0 (white for EUCLID and STEPS; `@SWEEPC` for the two sweeps, with the re-lit centre and its
+20); the alert-layer sentence; the sentence that the cell `Q` returns is now the LED under the finger
and what that fixed on the bench (EUCLID: the outer ring); ARC: `N` and the thirteen returned. **No knob,
no tag, no name moves.**

**4. `npm run test:sweep`** - `4 19` green: every entry inside 908 at every corner with the new Setups.

**Negative checks:** apply EUCLID's callback needle twice in a scratch copy - the assertion script
refuses on count; put `G` after `Q` in RADAR POINTS - the smoke test of task 02 (the centre marker) goes
red because `glp(glag(0,40),0,255)` then runs before the block clear. Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && npx vitest run --project server src/lib/catalog/touch-guard.spec.ts src/lib/catalog/decay-idiom.spec.ts src/lib/catalog/host-surface.spec.ts</automated>
    The sweep unmoved; both class gates and the surface gate green over the five new Setups (the
    surface gate is what refuses an entry that assigns a library name).
  </verify>

  <done>
Five Setups changed by exact needles with the occurrence count asserted; every corner figure re-measured
in this tree and written into its header with the finger paragraph; the sweep `4 19` green; two negative
checks red and restored.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-03-02: The four sequencers and ARC under G in the real VM, and row 25</name>
  <files>src/lib/sim/lua-smoke.spec.ts, docs/HARDWARE-AUDITION.md, src/lib/catalog/audition.spec.ts</files>

  <read_first>
    - `lua-smoke.spec.ts` - the 12.1 describe block plan 02 appended (its host helper and layer-0 reader)
      and the existing EUCLID / STEPS / RADAR POINTS / SONAR / ARC blocks (how each entry is rendered at
      its defaults and driven)
    - `src/lib/catalog/calibration.ts` - `KX`, `KY`, `calibratedAxis`
  </read_first>

  <action>
Append two tests to the 12.1 describe block:

1. **The four sequencers**: for each of EUCLID, STEPS, RADAR POINTS, SONAR rendered at its defaults with
   both library strings - for every `(c, r)` in 0..8 x 0..8 press at `(KX[c], KY[r])`, lift, and assert
   that while pressed layer 0 held cell `c + r*9` at 255 and **no other layer-0 cell above 0**, with the
   one exception that RADAR POINTS and SONAR hold cell 40 at 255 always; then a midpoint press (between
   LED 4 and 5 in `x`, on `KY[4]`) lights exactly two layer-0 cells; then the toggle: on EUCLID a press at
   `(KX[1], KY[4])` (the outer ring, index 1) flips a step - assert the entry's own state table or layer-1
   phase moved, which is the tap the bench said needed several tries. Print, once, the 81 cells that lit
   for EUCLID as a 9x9 of `.`/`#` so the SUMMARY can show the map is the identity.
2. **ARC's stop**: a press at `(KX[4], KY[4])` stops the swirl (the entry's existing "stopped" observable
   from 12-05's test); a press at `x = KX[3] + (KX[4]-KX[3])//2 + 1, y = KY[4]` - which `calibratedAxis`
   rounds to LED 4 and the naive divisor to column 3 - **also stops it**; print both cells beside the
   naive divisor's answer.

Header count 42 -> 44. Row 25 appended per the interfaces block; `ROW_COUNT` 24 -> 25; the title
"twenty-four" -> "twenty-five".

**Negative checks:** revert EUCLID's init-loop needle in a scratch copy (no layer-0 colour) - test 1 red
because `glp` on an uncoloured layer-0 cell renders 0 (say what the reader saw); revert ARC's needle -
test 2 red on the off-centre press. Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/audition.spec.ts</automated>
    `lua-smoke.spec.ts` **44**; `audition.spec.ts` **10** at `ROW_COUNT` 25.
  </verify>

  <done>
Every LED centre lights its own cell alone on all four sequencers, a midpoint lights two, the outer-ring
tap toggles, the sweeps keep their centre dot, ARC stops on the calibrated centre where the naive
divisor would not; row 25 asks the bench the first ten minutes' questions; two negative checks red and
restored.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs <observed files> <observed tests + 2>` with
  `frames.spec.ts`'s five expected red for the four sequencers until plan 05 regenerates - **run the quick
  suite, name the red file and its reason, and assert the count of everything else**; this plan's term is
  `+0 / +2`, written out.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - run, unmoved.
- `grep -c "test(" e2e/*.e2e.ts` summed, unchanged - e2e `+0 / +0`.
- `git diff --stat HEAD -- src/vendor/` empty; `git diff --quiet -- .planning/ROADMAP.md`; no knob's
  `values` array and no `tags` array changed (`git diff -U0 -- src/lib/catalog/entries/ | grep -E "values|tags"` empty).
</verification>

<success_criteria>
1. The four sequencers draw the finger as a gradient on layer 0 and toggle the LED under it, proved
   at all 81 centres in a real VM.
2. RADAR POINTS' and SONAR's centre dot survives the finger.
3. ARC stops on the calibrated centre.
4. Every header figure is a measurement made in this tree; no knob, tag or name moved.
5. Row 25 hands the finding to the bench in the order the gate will use.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-03-SUMMARY.md` carrying: the
observed baseline and `+0 / +2`; the five corner figures before and after with free characters; the
EUCLID 9x9 identity print; ARC's two stop cells beside the naive answer; the sweep line; row 25 as
written; the red `frames.spec.ts` named with plan 05 as its closer; the negative checks; and the
sentence that no device was touched.
</output>
