---
phase: 12.1-gradient-touch
plan: 04
subsystem: catalog, sim
tags: [gradient, re-fit, chorus, morph, console, lumen, trackpad, touch-library, calibration, wasmoon, audition]

# Dependency graph
requires:
  - phase: 12.1-gradient-touch
    provides: plan 03 - the needle discipline (one string-literal line, exactly once inside it), Q first then G, G's end test `e~=1 and e~=4` (255/6 705), row 25 and the re-padded audition table
  - phase: 12.1-gradient-touch
    provides: plan 02 - TOUCH_LIBRARY (255/0, 781) and TOUCH_LIBRARY_TIMER (255/6), G carrying its colour, U and the knot tables in the Lua, the host running both strings
  - phase: 12.1-gradient-touch
    provides: plan 01 - calibration.ts (KX, KY, calibratedAxis, sensorAt)
  - phase: 12-touch-framework
    provides: 12-09's CHORUS (R, X), CONSOLE (P) and MORPH (Q for the trail cell alone) on the library; 12-11's LUMEN on Q and A; 12-10's TRACKPAD with the Timer-painted flash
provides:
  - src/lib/catalog/entries/{chorus,console}.ts - `local n=Q(s,i,e,x,y)G(s,i,e,x,y,0,255,255,255)if not n`; the finger in white on layer 0
  - src/lib/catalog/entries/morph.ts - `if i>0 then return end local c=Q(s,i,e,x,y)G(s,i,e,x,y,0,@TRAILC)if e==3 or e>=5 and e<9 then return end`; the single-contact rule first, Q then G in front of the end test, the finger in the trail colour
  - src/lib/catalog/entries/lumen.ts - `local n=Q(s,i,e,x,y)G(s,i,e,x,y,0,@CURSORC)if e~=1 and e~=4 and e<9 then return end`; the finger in the cursor colour, the palette glc pair untouched, A unchanged
  - src/lib/catalog/entries/trackpad.ts - the Timer's flash centres `(U(c[2]//8,KY)+32)//64` and `(U(c[1]//8,KX)+32)//64`; the Setup byte-identical
  - src/lib/sim/lua-smoke.spec.ts - two tests (34 -> 36): the four Setups at nine LED centres and a midpoint in their own colours, MORPH's lift and second finger, the colour knobs followed, LUMEN's A byte-identical to 12-11; TRACKPAD's flash centre through the map on both axes
  - docs/HARDWARE-AUDITION.md row 26; audition.spec.ts ROW_COUNT 26
affects: [12.1-05 (the preview's forward map; frames.json did NOT move here), 12.1-09 (the gate; row 26; D-14's question), 13-14 (the emitter: Q first, then G, then the entry's early return - MORPH's shape shows an early return can be split around the two calls)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "An entry whose early return precedes its Q call moves BOTH Q and G in front of that return, in that order: G before Q is wiped by Q's onset E on the press that drew it (measured: the plan's G-first MORPH lit nothing on a press), and G behind the return never sees the lift; the entry's own single-contact rule stays ahead of both"
    - "A negative check that changes a colour literal cannot redden a test that reads its expectation off the same literal; the colour path is tested by rendering the entry's colour knob at a non-default, non-white index and asserting the VM's layer-0 colour equals the KNOB's value"
    - "A Timer that wants one axis of the calibrated cell writes N out per axis - `(U(v//8,K)+32)//64` - with `//8` as the hi-res-to-sensor step under txma(1023)"

key-files:
  created: []
  modified:
    - src/lib/catalog/entries/chorus.ts
    - src/lib/catalog/entries/morph.ts
    - src/lib/catalog/entries/console.ts
    - src/lib/catalog/entries/lumen.ts
    - src/lib/catalog/entries/trackpad.ts
    - src/lib/sim/lua-smoke.spec.ts
    - docs/HARDWARE-AUDITION.md
    - src/lib/catalog/audition.spec.ts

key-decisions:
  - "MORPH's callback is `if i>0 then return end local c=Q(...)G(...)if e==3 or e>=5 and e<9 then return end` - Q hoisted in front of the end test WITH G, not the plan's `G(...)if ... then return end local c=Q(...)`: driven in wasmoon the plan's order lights nothing on a press (Q's onset E clears the block G just drew, 12.1-02's finding) and the finger appears only on the first move; same characters, 814 either way"
  - "The colour path is asserted through the knob, not the literal: MORPH at trailColour index 2 (0,200,255) and LUMEN at cursor index 1 (255,240,200) draw the layer-0 finger in the knob's value; the plan's negative check 'hand LUMEN's G 255,255,255 instead of @CURSORC' cannot redden the clause that reads the colour off the rendered Setup, because @CURSORC's default IS 255,255,255 - it reddens this clause"
  - "LUMEN's A is unchanged (D-14), proved byte for byte: 12-11's own gesture re-driven on the entry and on a Setup that is nothing but the same A call gives the same axis-CC log, [16,61] [17,65] [16,62], and equals 12-11's recorded answers; raw, so 0 and 127 are reached a third of an LED inside the outer LEDs, and row 26(d) asks the user"
  - "TRACKPAD's +20 lands in the Timer (490 -> 510, 398 free) and nowhere else: the position is already stored hi-res in s.p and the flash was already painted from the Timer, so the Setup line is sha256-identical at 903 / 5"

requirements: [CONT-02, CONT-03, PREV-01]
requirements-completed: []

# Metrics
duration: about 22 min of execution (14:46Z-15:08Z) plus the SUMMARY and STATE
completed: 2026-09-11
---

# Phase 12.1 Plan 04: CHORUS, MORPH, CONSOLE, LUMEN and TRACKPAD re-fitted on G and the calibrated Q Summary

**The three tightest cards in the catalog carry the gradient inside 908 with room, and cheaper than
the pre-coloured shape D-11 replaced: CHORUS 796 -> 822 (86 free), MORPH 772 -> 814 (94), CONSOLE
781 -> 807 (101), all under the 890 BUDGET_ERROR line; LUMEN 707 -> 733 (175) in its cursor colour with
`A` byte-identical to 12-11; TRACKPAD's Setup sha256-identical at 903 / 5 and its Timer 490 -> 510
(398) with both flash centres through the measured map. Every figure re-measured in this tree under
the pinned compressScript at the RGB444 picker corner, canonical, checkSyntax true; sweep `4 19`
twice, unmoved (worst still TRACKPAD's Setup at 906). Proved in wasmoon: nine LED centres light
their own cell alone at 255 in each entry's colour on all four, a midpoint lights the pair at the
computed weights, MORPH clears on a code-5 lift and refuses a second finger, both colour knobs move
the finger, and TRACKPAD's flash at hi-res `KY[1]*8` lands on row 1 where the naive divisor read 0.
One shape correction the plan did not have: MORPH's `G` before `Q` draws nothing on a press, so `Q`
moved in front of the end test with it. Term `+0 / +2` on 12.1-03's `87 / 907`: **87 / 909 (+1 todo)**,
twice green whole; audition 26; e2e 78 / 94 untouched. Six negative checks red and restored by sha256.
No device was touched.**

## Performance

- **Duration:** about 22 min of execution (14:46Z to 15:08Z) plus this document and STATE.md
- **Tasks:** 2 of 2
- **Files:** 0 created, 8 modified (the plan's eight, exactly), plus this document and STATE.md

## Commits

| Hash      | Message                                                                                                                                                                                                                              |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `cdfbfce` | `feat(12.1-04): CHORUS, MORPH, CONSOLE, LUMEN re-fitted on G after the calibrated Q, and TRACKPAD's flash centred through the map`                                                                                                   |
| `849d9ff` | `test(12.1-04): CHORUS, CONSOLE, MORPH and LUMEN under G at nine LED centres in their own colours, MORPH's lift and second finger, LUMEN's A byte-identical to 12-11, TRACKPAD's flash centre through the map, and audition row 26` |

`git commit --only <paths> -F <message-file>`, pathspec before the flag. 13-11 was executing throughout
on its own files (uncommitted at this plan's close: `DeviceDetails`, `DeviceMark`, `DeviceSlot`,
`InstallState`, `KeepConfirm`, `radius-allowlist.ts`, `shell.spec.ts`, `shell/ContextBar`, `shell/Footer`,
`shell/Header`, `shell/shell.svelte.ts`, `routes/+layout.svelte`, `routes/playground/[id]/+page.svelte`,
plus three new files); none of this plan's eight is theirs and nothing of theirs is in these two
commits. The docs commit carrying this document and STATE.md is the third and last. No push. No
co-author trailer (deviation 5).

---

## The baseline, re-measured at this plan's start and close

| Name         | Brief (at `65d79f1`) | Observed at start (14:46Z)             | Term                          | Observed at close                                                                                            |
| ------------ | -------------------- | -------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `PREV_FILES` | 87                   | **87**                                 | **+0**                        | **87**                                                                                                       |
| `PREV_TESTS` | 907 (+1 todo)        | **907** (+1 todo), green whole         | **+2**                        | **909** (+1 todo) - `check-counts.mjs 87 909` matches, twice (15:05Z, 15:07Z)                                |
| sweep        | `4 19`               | -                                      | +0, RUN                       | **`4 19`** twice (106 s, 126 s), both matching; worst 906 at TRACKPAD's Setup, unmoved                        |
| check        | 609                  | -                                      | +0                            | **613** files, 1 error - 13-11's `shell.spec.ts:771` (`deviceActions` not in `ShellFill`); +4 files are theirs |
| lint         | clean                | -                                      | -                             | clean on every file this plan touched; `prettier --check .` warns on 13-11's `DeviceSlot.svelte`, `ContextBar.svelte` |
| e2e titles   | 78 / 94              | 78                                     | +0 / +0                       | **78** (`grep -c "test("` summed)                                                                            |
| catalog      | 26 = 8 + 18          | -                                      | +0                            | 26                                                                                                           |
| audition     | 25                   | 25                                     | **+1**                        | **26**                                                                                                       |
| library      | 781 + 705            | 781 + 705                              | +0                            | **781 + 705** - `library.ts` untouched                                                                       |

**The chain, written out:** 12.1-03's `87 / 907` -> **this plan's `+0 / +2`** -> **87 / 909 (+1 todo)**.
The one part of `+2`: `lua-smoke.spec.ts` 34 -> **36** (its header reads THIRTY-SIX). 13-11's `+3` did
not land under this plan: its specs are uncommitted on disk and its `shell.spec.ts` edit is red in
`check`, so the brief's alternative (87 / 912) did not arise.

**The whole quick suite ran four times.** Run 1 (15:01Z) was red on `radius.spec.ts` alone (two tests:
a stale allowlist row `DeviceDetails.svelte (clearedBy 13-11)` and a build stale against 13-11's edit
of `routes/playground/[id]/+page.svelte`) - 13-11's file, in progress; 907 passed, 909 counted. Run 2
(15:02Z, with the second sweep and 13-11 running) added the connect-snapshot transient the brief
names: `install.spec.ts` > "the snapshot is taken at connect" timed out waiting for identification -
green alone (23) between runs. Runs 3 and 4 (15:05Z, 15:07Z) were **green whole: `87 passed / 909
passed / 1 todo`**, `check-counts.mjs 87 909` matching on both.

**`frames.spec.ts` was NOT excluded, and did not need to be**, exactly as 12.1-03 found: the fixture
records an untouched run, nothing at rest calls `G`, and `frames.json` is byte-unchanged; its five
tests are green in every run above and in the focused gate run (`touch-guard`, `decay-idiom`,
`host-surface`, `frames`: 4 files / 17 tests). Counted both ways: **with** `frames.spec.ts` 87 / 909;
without it 86 / 904 by arithmetic, never observed. Plan 05 is the fixture's closer if the preview's
forward map moves a rest frame.

---

## Task 1: five entries by exact needles on the literal, every header re-measured

### The needles, as applied

| Entry    | The one literal line: before -> after                                                                                                                                                                                                                                 | File-wide count of the needle (why the literal only)                    |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| CHORUS   | `local n=Q(s,i,e,x,y)` -> `local n=Q(s,i,e,x,y)G(s,i,e,x,y,0,255,255,255)`                                                                                                                                                                                             | x1                                                                      |
| MORPH    | `if i>0 or e==3 or e>=5 and e<9 then return end local c=Q(s,i,e,x,y)` -> `if i>0 then return end local c=Q(s,i,e,x,y)G(s,i,e,x,y,0,@TRAILC)if e==3 or e>=5 and e<9 then return end ` (the trailing space is where `local q=0` follows)                                    | x1 (the `:276` comment quotes the guard across two lines; rewritten)     |
| CONSOLE  | `local n=Q(s,i,e,x,y)` -> `local n=Q(s,i,e,x,y)G(s,i,e,x,y,0,255,255,255)`                                                                                                                                                                                             | **x2** (`:101` comment; rewritten to quote the new line)                |
| LUMEN    | `local n=Q(s,i,e,x,y)` -> `local n=Q(s,i,e,x,y)G(s,i,e,x,y,0,@CURSORC)`                                                                                                                                                                                                | x1; the palette `glc(a,1,@CURSORC,1)glc(a,2,@CURSORC,1)` pair untouched |
| TRACKPAD | Timer: `c[2]*9//1024` -> `(U(c[2]//8,KY)+32)//64`; `c[1]*9//1024` -> `(U(c[1]//8,KX)+32)//64`                                                                                                                                                                          | **x2** for `c[2]` (`:116` comment; rewritten), x1 for `c[1]`; Setup untouched |

The script (`measure-1204.mjs`, scratchpad) isolates the string-literal lines (`^  "--\[\[@cb\]\].*";$`,
plus `const TIMER = "..."` for CHORUS's one-line Timer), asserts exactly one occurrence of the needle
INSIDE the named line (the Setup line for four, the Timer line for TRACKPAD), replaces it there, and
writes every other line back byte-identical; `git diff --stat` showed one changed line per file before
the header edits. `grep -c 'c\[2\]\*9//1024' trackpad.ts` = 0 after.

**`G` is written AFTER `Q` on all four, not before as the plan's interfaces block has it** (12.1-03
deviation 2, 12.1-02's finding), and **MORPH's `Q` moved in front of its end test with `G`** (deviation
1, below). Same characters as the plan's shapes, so every figure below is the plan's.

### The cost, measured before and after in this tree

Under `@intechstudio/grid-protocol@1.20260825.1135`'s `GridScript.compressScript` after
`initLuaFormatter()`, `cost = max(raw, compressed)`, at the RGB444 picker corner (every colour knob
`255,255,255`, every other knob its longest value), canonical (a fixed point) and `checkSyntax` true on
every string - exactly the plan's figures:

| Entry    | Setup corner before -> after | Free before -> after | Under 890 by | At the defaults   | Timer corner before -> after   | Timer free  |
| -------- | ---------------------------- | -------------------- | ------------ | ----------------- | ------------------------------ | ----------- |
| CHORUS   | 796 -> **822** (+26)         | 112 -> **86**        | 68           | 793 -> 819        | 29 -> 29                       | 879         |
| MORPH    | 772 -> **814** (+42)         | 136 -> **94**        | 76           | 768 -> 810        | 0 -> 0 (no Timer)              | 908         |
| CONSOLE  | 781 -> **807** (+26)         | 127 -> **101**       | 83           | 758 -> 784        | 0 -> 0 (no Timer)              | 908         |
| LUMEN    | 707 -> **733** (+26)         | 201 -> **175**       | 157          | 704 -> 730        | 0 -> 0 (no Timer)              | 908         |
| TRACKPAD | 903 -> **903** (+0)          | 5 -> **5**           | -            | 903 -> 903        | 490 -> **510** (+20)           | 418 -> **398** |

CHORUS at 86 free is the tightest card this plan touches. MORPH's +42 is the `G` call (26) plus the end
test written out a second time (39) minus the 24 characters lifted from the first guard, plus a space.
TRACKPAD's +20 is +10 per axis; **the whole of it lands in the Timer** - the position is already stored
hi-res in `s.p` and the flash was already painted from the Timer, so no character had to land in the
Setup and the brief's stop condition did not fire. TRACKPAD's Setup line is **sha256-identical**
(`8a1b3ea4...` at `65d79f1` and at `HEAD`); `git diff -U0 65d79f1 HEAD -- trackpad.ts` touches the
Timer literal, comments and the header only.

### The headers

Each entry's cost paragraph carries the new figures, the 890 line and the previous figures beside them;
a finger paragraph per entry says `G` on layer 0 in its colour (white on CHORUS and CONSOLE, `@TRAILC`
on MORPH, `@CURSORC` on LUMEN), why the colour is a literal or the entry's own token and not a new knob
(D-13), the alert-layer heal (D-11, `grid_led.h:7`), that there is no floor (`glc(...,1)`), and why `Q`
comes before `G` (12.1-02). MORPH's header carries the four-point paragraph on its first line - the
single-contact rule first, `Q` before `G`, `G` before the end test, the end test unchanged - with the
measurement that the plan's order lights nothing on a press, and its "`Q` from behind the end test"
paragraph is inverted with the consequence (the expiry path now runs on a lift; nothing was ever held).
LUMEN's carries the `@CURSORC` reasoning, the untouched palette pair (three sites now), and the D-14
paragraph: `A` unchanged, raw reaches 0 and 127 a third of an LED inside the outer LEDs (a finger dead on
LED 8 reads 126, on LED 0 reads 1), a calibrated `A` at about +25 named as the user's call, row 26(d).
TRACKPAD's `:116` bullet explains `c//8` as the hi-res-to-sensor step under `txma(1023)` and names the
bench case (raw y 12, hi-res 96, naive row 0). CHORUS's `+-10 raw units / ~3.9` hysteresis prose is
replaced by the D-18 fraction-of-the-pitch sentence, as 12.1-03 did on the four sequencers. **No knob's
`values`, no `tags`, no name moved**: `git diff -U0 65d79f1 HEAD -- src/lib/catalog/entries/ | grep -E
"values:|tags:"` is empty.

### Gates

`touch-guard.spec.ts`, `decay-idiom.spec.ts`, `host-surface.spec.ts` and `frames.spec.ts` green over the
five new strings (4 files / 17 tests); the sweep `4 19` in 106 s; every one of the 34 existing
`lua-smoke` tests green on the new Setups before a new test was written - including CHORUS's
one-chord-at-a-time test (`R` idempotent: finger 0 lifting while not the owner sends nothing, the quiet
owner released on Timer call 21 at 2.1 s, forty calls of wobble send nothing), MORPH's corner taps (one
message per corner, slow tap, fast tap and after a stroke, on all four corners) and its suppression
test, LUMEN's two, and TRACKPAD's whole-gesture test (whose right drag at y = 511 and up drag at x = 520
still centre on row 4 / column 4 through the map: `U(63,KY) = 240 -> 4`, `U(65,KX) = 259 -> 4`).

---

## Task 2: the four Setups and TRACKPAD's flash in the real VM, and row 26

### Test 1 - nine centres, the midpoint, MORPH's lift and second finger, the colour knobs, LUMEN's A

For each of CHORUS, CONSOLE, MORPH and LUMEN rendered at its defaults over both library strings, for the
nine `(c, r)` of the four corners, four edge midpoints and the centre: `touchDown(0, KX[c], KY[r])`, one
tick, `litOnLayer0(sim)` equals exactly `{[c + r*9]: 255}` and the cell's layer-0 colour equals the
finger colour **read off the rendered Setup's one `G(s,i,e,x,y,0,r,g,b)` call**; lift, one tick, layer 0
dark. Then the midpoint `(74, 68)`: exactly two cells, `40=135 41=119`, on all four. Printed:

```
  CHORUS:  nine centres lit their own cell (0 8 72 80 4 36 44 76 40) in 255,255,255; midpoint (74,68) lit 40=135 41=119
  CONSOLE: the same, in 255,255,255
  MORPH:   the same, in 180,255,255 (the rendered @TRAILC)
  LUMEN:   the same, in 255,255,255 (the rendered @CURSORC)
```

| Clause                                                    | Observed                                                                                                                                                                                  |
| --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MORPH: press LED (2,3), lift with code 5                  | press `{29: 255}`, `B` 1; after the lift layer 0 `{}`, `B` 0 - cleared through `Q`'s `E` and `G`'s end branch, both in front of the early return                                          |
| MORPH: a second contact (id 1) pressed and moved while 0 is down | layer 0 still `{29: 255}`, `B` still 1, **0 MIDI** from the second contact - `i>0` refused before `Q` or `G` saw it                                                                    |
| MORPH at `trailColour` index 2 (`0,200,255`, non-default) | the layer-0 finger reads `0,200,255` - the finger follows the knob                                                                                                                       |
| LUMEN at `cursor` index 1 (`255,240,200`)                 | the layer-0 finger reads `255,240,200` - `@CURSORC` is the colour path                                                                                                                   |
| LUMEN's `A` over 12-11's gesture (DOWN 60,60; MOVE 61,60; MOVE 61,62; MOVE 62,62) | axis CCs `cc16=61 cc17=65 cc16=62` on the entry AND on a Setup that is nothing but `A(s,i,e,x,y,16,17,0)`, equal by `toEqual` on `[ch, p1, p2]` triples, and equal to 12-11's recorded `[[0,16,61],[0,17,65],[0,16,62]]` |

The colour-knob clause (3b) is the one the plan did not spell out and is the one that makes the LUMEN
colour path testable (deviation 2): clause 1 reads the colour off the literal and would accept a
literal white, because `@CURSORC`'s default IS white.

### Test 2 - TRACKPAD's flash centre through the map, both axes

| Probe                                    | Edge phases (k = 0..8), first lit tick | Brightest | Lit run   | Through the map | Naive `v*9//1024` |
| ---------------------------------------- | -------------------------------------- | --------- | --------- | --------------- | ----------------- |
| rightward at hi-res `y = KY[1]*8 = 96`   | column 8: `228 246 228 180 0 0 0 0 0`  | **row 1** | rows 0..3 | 1               | **0**             |
| upward at hi-res `x = KX[1]*8 = 104`     | row 0: `228 246 228 180 0 0 0 0 0`     | **col 1** | cols 0..3 | 1               | **0**             |

The expected run is derived in the test from the reach knob (5) clamped at the edge (`glim(o+k,0,8)`
folds k = -2 and -1 onto row 0, the later write winning at 234 - 6 = 228); the naive row is computed in
the test and asserted `not.toBe` the calibrated one, so the probe cannot pass vacuously. The one-row-off
is the bench's: a finger dead on LED row 1 reads raw 12, and `12*8*9//1024 = 0`.

### The header count, row 26, `ROW_COUNT`

`lua-smoke.spec.ts:3` reads THIRTY-SIX. **Row 26 as written** (one table row, appended after row 25,
the plan's text verbatim):

> | 26 | CHORUS / MORPH / CONSOLE / LUMEN / TRACKPAD | **THE OTHER FIVE UNDER THE GRADIENT (plan 12.1-04).** **(a)** CHORUS: does the white finger read against the bloom, and does the chord still land on the pad under the finger? **(b)** MORPH: the finger in the trail colour - does it clear on a lift, and does a second finger stay refused? **(c)** CONSOLE: mute and unmute with the finger drawn - does the strip you meant move? **(d)** LUMEN: the finger in the cursor colour - and watch the host's CC: does it reach 0 and 127 where you expect, or a third of an LED inside the edge (A sends the raw sensor value, unchanged; a calibrated A costs 25 characters and changes what the DAW receives - say if you want it)? **(e)** TRACKPAD: move slowly right, then up, left, down - is the edge flash centred on the row / column your finger is actually on, where round 3 put it a cell early near the edges? | LUMEN's CC range and the flash's centre are things a DAW and an eye settle; the simulator's pointer cannot say where a fingertip is on this unit. |

Its Config cell (five names, 42 characters) is wider than the 37-wide column 12.1-03 left, so
`prettier --check` refused the file until `prettier --write docs/HARDWARE-AUDITION.md` (that one file)
re-padded every row a second time (deviation 4); every other row's text is byte-identical to the pristine
copy modulo padding and separator dashes, asserted line by line by a script (0 lines differing), and no
row was re-wrapped. `audition.spec.ts`: `ROW_COUNT` 25 -> **26** with the reason in its comment; the title
"twenty-five" -> "twenty-six"; four tests green.

---

## The negative checks, six, each red as named and restored by sha256

Each from the edited copy in the scratchpad, the mutation applied to the one literal line, the named
test run, the edited copy written back, sha256 equal either side (`sha256sum -c` over all five entry
files: OK, five times). No `git checkout`, `restore`, `stash` or `clean`.

| #     | Mutation                                                                                                    | Red as named                                                                                                                                                                          |
| ----- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1-1  | MORPH: `G` after its early return, the old guard `if i>0 or e==3 or e>=5 and e<9 then return end` in front  | test 1: `morph: after the lift from LED (0,0) layer 0 is not dark: expected { '0': 255 } to deeply equal {}` - the lift never reaches `Q` or `G`, and MORPH has no Timer to sweep it |
| T1-1b | MORPH: the plan's own literal shape - `G` first, then the end test, then `local c=Q(...)`                    | test 1: `morph: a press dead on LED (0,0) at (1,0) must light exactly its own cell at 255 on layer 0: expected {} to deeply equal { '0': 255 }` - `Q`'s onset `E` clears the block `G` drew |
| T1-2  | TRACKPAD: `c[2]//8` -> `c[2]` (hi-res handed to `U` unscaled)                                               | test 2: `the flash is not centred on row/column 1 ... expected 6 to be 1` - `U(96, KY)` reads LED 6 (96 is a legal raw value); values above 126 would clamp to the last knot           |
| T2-1  | LUMEN: `G(s,i,e,x,y,0,255,255,255)` in place of `@CURSORC`                                                  | test 1 clause 3b: `lumen: the finger did not follow the cursor knob to 255,240,200 ... expected [ 255, 255, 255 ] to deeply equal [ 255, 240, 200 ]`                                  |
| T2-2  | TRACKPAD: the `KY` needle reverted to `c[2]*9//1024`                                                        | test 2: `the flash is not centred on row/column 1 ... expected +0 to be 1`                                                                                                             |
| T1-0  | the file-wide count of every needle, asserted by the script before any write                                 | CONSOLE's callback needle x2 and TRACKPAD's `c[2]` needle x2 file-wide (the two comments) - a file-wide `replace` would have rewritten a comment in the plan's own words; the script refused file-wide and applied to the literal |

(T1-2's red is on the row as the plan says, but at the plan's own probe point the reason is not "the
clamp pins every hi-res value to the last knot": `96` is inside 0..127, so `U` reads it as a raw value
and lands on LED 6. The clamp explanation holds for hi-res values above 126, which is most of the pad.)

---

## Deviations from the plan

**1. [Rule 1 - the plan's MORPH shape lights nothing on a press] `Q` moved in front of the end test
with `G`.** The plan's interfaces block, its `must_haves` artifact and its objective write MORPH as
`if i>0 then return end G(s,i,e,x,y,0,@TRAILC)if e==3 or e>=5 and e<9 then return end local c=Q(s,i,e,x,y)` -
`G` before `Q`. Driven in wasmoon before the needle was applied (a scratch spec, deleted, never
committed) and again as negative check T1-1b: on a press `G` draws the block and records `B[0]`, then
`Q`'s onset `E(s,0)` clears it through `V` - the press lights **nothing** and the finger appears on the
first move. 12.1-02's finding, which the plan itself cites for the other three entries. The shipped line
is `if i>0 then return end local c=Q(s,i,e,x,y)G(s,i,e,x,y,0,@TRAILC)if e==3 or e>=5 and e<9 then return end`:
the single-contact rule first, `Q` then `G`, both in front of the end test so a lift reaches them. Same
characters (814 at the corner, 810 at the defaults). Consequence stated in MORPH's header: `Q`'s expiry
path now runs on a lift for this card (`H[0]`, `T[0]` and `B[0]` dropped by `E`); on live codes nothing
moved, because an onset expired `H[i]` before the cell was read either way, and nothing was ever held.
The plan's `must_haves` string `if i>0 then return end G(s,i,e,x,y,0,@TRAILC)if e==3 or e>=5 and e<9 then return end`
does not occur in the tree and should not. Commit `cdfbfce`.

**2. [Rule 2 - the plan's colour negative check cannot redden the test it names] A colour-knob clause
added to test 1.** "Hand LUMEN's `G` `255,255,255` instead of `@CURSORC` in a scratch copy - test 1 red
on the colour" cannot happen at the defaults: `@CURSORC` renders `255,255,255` there, and test 1 reads
the expected colour off the rendered `G` call (12.1-03's helper, deviation 6 there), so literal and
token are indistinguishable. Clause 3b renders MORPH at `trailColour` index 2 and LUMEN at `cursor`
index 1 - non-default, non-white - and asserts the VM's layer-0 colour equals the **knob's** value.
T2-1 goes red there, as the table shows. Commit `849d9ff`.

**3. [Process - the plan's T1-2 explanation] "The clamp inside `U` pins every hi-res value to the last
knot"** is not what happens at the plan's probe (`y = 96`): 96 is a legal raw value and `U` reads it as
LED 6. The check is red on the row either way; the sentence is corrected in the table above.

**4. [Process - append-only vs. prettier] Row 26 re-padded the audition table again.** Five names (42
characters) against 12.1-03's 37-wide column; `prettier --write` on that one file, text byte-identical
modulo padding, asserted; recorded in `audition.spec.ts`'s comment.

**5. [Process - the commit trailer]** The harness asks for a co-author trailer; the repository's rule
(`standing_rules`, 12-12 deviation 10) is no attribution and no trailer. The repository's rule was
followed on all three commits; the conflict is reported here.

**6. [Process - `gsd-tools state` not run]** STATE.md by script against a copy (`state.mjs`,
scratchpad), asserting `status: executing`, `completed_phases: 11`, `percent: 100`, `total_phases: 14`,
`total_plans: 160` unchanged before and after, `completed_plans` 149 -> 150, 13-10's and 12.1-03's
`Status:` and `Stopped at:` retained as "previous", Phase 12.1 at 4 of 9 beside Phase 12 (gate landed,
bench pending) and Phase 13 at 10 of 20 with 13-11 in flight. The copy's sha256 checked against the tree
immediately before the write. `advance-plan`, `update-progress`, `record-session`, `roadmap
update-plan-progress` and `requirements mark-complete` not run; `.planning/ROADMAP.md` byte-unchanged;
**CAT-04 stays `[ ]`**; `REQUIREMENTS.md` untouched (the gate amends).

**7. [Process - the plan's `--exclude` command]** Not run: 12.1-03 measured that a vitest project
config's `exclude` overrides the CLI flag, and `frames.spec.ts` is green (nothing at rest calls `G`).
Counts stated both ways above.

---

## What 13-11's state did to the runs

13-11 was executing throughout on `src/lib/ui/{DeviceDetails,DeviceMark,DeviceSlot,InstallState,
KeepConfirm}.svelte`, `radius-allowlist.ts`, `shell.spec.ts`, `shell/{ContextBar,Footer,Header}.svelte`,
`shell/shell.svelte.ts`, `routes/+layout.svelte`, `routes/playground/[id]/+page.svelte` and three new
files, uncommitted at this plan's close. Effects, all theirs: `radius.spec.ts` red in quick runs 1 and 2
(a `clearedBy 13-11` allowlist row for `DeviceDetails.svelte` whose declaration is already gone, and a
build stale against their `+page.svelte` edit); `check` 613 files / 1 error at `shell.spec.ts:771`;
`prettier --check .` warning on `DeviceSlot.svelte` and `ContextBar.svelte`. The connect-snapshot
transient (`install.spec.ts`) appeared once, in the run that overlapped the second sweep. Runs 3 and 4
were green whole. None of its files is this plan's; this plan's eight are untouched by it.

## What the plan asserts that the tree does not support

- The interfaces block's CHORUS, CONSOLE and LUMEN needles (`G(...)local n=Q(...)`) and MORPH's
  `G(...)if ... then return end local c=Q(...)` with its `must_haves` artifact: `Q` first, measured
  (deviation 1). The plan's objective sentence "G must see end codes to clear the block, so it is called
  first" is true of the end test and false of `Q`.
- "hand LUMEN's `G` `255,255,255` instead of `@CURSORC` ... test 1 red on the colour" (deviation 2):
  green at the defaults as the test was specified; red on the added knob clause.
- "the clamp inside `U` pins every hi-res value to the last knot" at `y = 96` (deviation 3): LED 6.
- "`frames.spec.ts` (five tests) is red for the re-fitted entries until plan 05" and the `--exclude`
  quick-run command (deviation 7): green, and the flag has no effect on a project config.
- "Row 26 appended ... `ROW_COUNT` moved" with the standing rule's "never re-wrap a row" read as "at
  the table's widths" (deviation 4): the row is wider than the column; prettier re-padded, no text moved.
- `12.1-CONTEXT.md` D-11 quotes 255/6 at 714: 705 since 12.1-03's `3d1b36c` (a planning record, not
  edited). D-17's "490 -> 510" is the tree's figure exactly (12-12's corrected 490, not 12-10's 488).

## Questions for the user

None new from this plan; the plan's one question is row 26(d), written beside LUMEN as D-14 asks:
does the raw CC reach 0 and 127 where you expect, or a third of an LED inside the edge - a calibrated
`A` is about 25 characters and changes what the DAW receives. Carried: **D-04** (KEEP once) is the
gate's row; **D-12** (the dim end) is row 25(e); **D-13** (one colour everywhere?) is now askable across
four colours - white, `@SWEEPC`, `@TRAILC`, `@CURSORC`; **D-16** (the presets) pending.

## Known stubs

None. Every `G` call is wired to the library; `frames.json` is unchanged because nothing at rest
changed. No component, prop or data path is wired to an empty value.

## Not hardware-verified

No agent connected to, wrote to, or deployed to a device during this plan. `src/vendor/` untouched
(`git diff --stat 65d79f1 HEAD -- src/vendor/` empty); `firmware-oracle.spec.ts` unedited and green; the
simulator's phase walk unchanged; `library.ts` and `lua-host.ts` untouched. Whether the white finger
reads against CHORUS's bloom, whether MORPH's clears on a real lift, whether the strip CONSOLE moves is
the one under the finger, where LUMEN's CC reaches 0 and 127, and whether TRACKPAD's flash sits on the
finger's row are row 26's (a)-(e) at 12.1-09.

## Self-Check: PASSED

- the eight modified files FOUND; `lua-smoke.spec.ts` thirty-six `it(` with THIRTY-SIX at `:3`; `audition.spec.ts` `ROW_COUNT = 26`; `docs/HARDWARE-AUDITION.md` twenty-six numbered rows; `lumen.ts` carries `G(s,i,e,x,y,0,@CURSORC)` in its literal (and once in its header); `trackpad.ts` carries `(U(c[2]//8,KY)+32)//64` in its Timer literal (and once in its header); `morph.ts` carries the shipped first line once, in the literal
- commits `cdfbfce`, `849d9ff` FOUND in `git log`
- `git diff --stat 65d79f1 HEAD -- src/vendor/ src/lib/catalog/frames.json src/lib/fidelity/firmware-oracle.spec.ts src/lib/catalog/library.ts src/lib/sim/lua-host.ts` empty; `.planning/ROADMAP.md` byte-unchanged; nothing of this plan's under `13-gui-overhaul/` or `12-touch-framework/`; CAT-04 `[ ]`
- quick `87 / 909 (+1 todo)` twice green whole (`check-counts.mjs 87 909` matching); sweep `4 19` twice; check 613 with 13-11's one in-progress error named; lint clean on every touched file; e2e titles 78
