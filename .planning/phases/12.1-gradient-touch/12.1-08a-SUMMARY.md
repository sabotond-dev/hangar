---
phase: 12.1-gradient-touch
plan: 08a
subsystem: catalog, sim
tags: [gradient, calibration, ghost, N, G, erase-key, demo-finger, frames, og, audition, band-1, D-26]

# Dependency graph
requires:
  - phase: 12.1-gradient-touch
    provides: plan 02 - the library's N(x,y) (255/6 today) and G(s,i,e,x,y,l,r,g,b) reading U over the measured knots; the host over both library strings
  - phase: 12.1-gradient-touch
    provides: plan 03 - the needle discipline (one string-literal line, the needle exactly once inside it), the code-9 end test in G, ARC's N(x,y)==40 as the press-time-lookup precedent
  - phase: 12.1-gradient-touch
    provides: plan 05 - the finding that GHOST was the one demo-driven card still on x*9//128 and that its trace sat a column out; the demo finger at the knots; frames.json samples no finger
provides:
  - src/lib/catalog/entries/ghost.ts - N(x,y)==80 at the erase key, glag(0,N(x,y)) for the comet and the ghost cell, G(s,i,e,x,y,0,@RECC) after the id gate; Setup 491 / Timer 409 at the picker corner, re-measured; the header's key, budget, gradient and Timer paragraphs rewritten with the LED (7,7) defect named
  - src/lib/sim/lua-smoke.spec.ts 36 -> 37 - GHOST under N and G in wasmoon with every cell from KX / KY; the 11-11 GHOST test re-aimed at the knots
  - src/lib/sim/demo.ts - the GHOST path's comment inverted (cells 1 and 6 draw in columns 1 and 6); comment lines only
  - docs/HARDWARE-AUDITION.md row 27 - GHOST under the gradient and the calibrated key, clauses (a)-(d); (d) is the user's design question
  - src/lib/catalog/audition.spec.ts - ROW_COUNT 27 with the dated note
affects: [12.1-08b (moves N to 255/0; GHOST's two N call sites keep working because the name does not change; adds one lua-smoke test for K -> 38), 12.1-09 (the gate reads GHOST's 491 / 409 and this term for the eleventh-term arithmetic), 13-19 (re-cases entry names later; ghost.ts's header names this plan)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A press-time lookup with no state takes N, not Q: GHOST's key test and its Timer's cell site have no contact to register and no hysteresis to hold (the Timer lights a STORED or REPLAYED point), so both read N over the same U and the key and the comet agree on where the LEDs are - the ARC precedent (12.1-03) applied twice in one entry"
    - "An entry with its own contact model keeps it: the library's expiry (Q + X + E + R) is not bolted onto GHOST because it would end a STILL finger's recording after n Timer ticks (the firmware's change gate means a motionless contact sends nothing), and 'a motionless finger still records' is the card's one promise about recording; the alternative is measured (530 / 417) and left to a bench question rather than taken"
    - "A test that mirrors an entry's cell arithmetic moves with the entry: the 11-11 GHOST test drove naive centres and asserted x*9//128+y*9//128*9, so the re-fit turned it red; it is re-aimed at the knots (ledCentre, calibratedCell) with its three claims unchanged, in the same commit as the re-fit, so every commit is green"
    - "A negative check that comes back green is a missing clause, not a pass: N1 (G before the id gate) turned nothing red because no test drove a second contact through GHOST; the 37th test gained a second-finger clause and N1 then bit on cell 50"

key-files:
  created: []
  modified:
    - src/lib/catalog/entries/ghost.ts
    - src/lib/sim/demo.ts
    - src/lib/sim/lua-smoke.spec.ts
    - docs/HARDWARE-AUDITION.md
    - src/lib/catalog/audition.spec.ts

key-decisions:
  - "N at both of GHOST's cell sites and G after the id gate, exactly the plan's three needles; no Q, no X, no R (the still finger); the alternative Q + R + X(s,150) measured at Setup 530 / Timer 417 and recorded, not taken - row 27(d) asks the user"
  - "The finger colour is @RECC, not white (D-13): GHOST's recording-colour knob names exactly the thing the finger is; at the picker corner it renders 255,255,255 so the cost is the white literal's"
  - "library.ts NOT edited although its header lists N's callers (ARC, 13-15) and G's (eight): the orchestrator's brief for this plan forbids touching library.ts short of a Rule-1 bug, which a caller-list comment is not; the two stale caller sentences (library.ts section 5 and arc.ts's 'ARC is N's one caller today') are logged for 12.1-08b, which rewrites N's paragraph when it moves N to 255/0"
  - "The 11-11 GHOST test is re-aimed in Task 1's commit rather than Task 2's, so the tree is green at every commit; the 37th test lands in Task 2 as planned"
  - "ghost.png's move (5,197 -> 5,234 B) is explained at the frame level: the end frame is exactly the pre-12.1-05 render's frame (16 23 24 31 32 38 39 48 59 68 69 80), the trace under the drawn finger again; the end frame has no finger (the lift is at tick 352, the period ends at 600), so G adds nothing to the OG and the plan's 'adds the layer-0 gradient to the demo frame' does not describe the picture"

requirements: [CONT-02, CONT-03, PREV-01]
requirements-completed: []

# Metrics
duration: about 50 min of execution (17:57Z-18:30Z for the two tasks, the baseline run and the build retries) plus this document and STATE.md
completed: 2026-09-11
---

# Phase 12.1 Plan 08a: GHOST onto the gradient and the calibrated key Summary

**GHOST's erase key is the LED at (8,8) and nothing else, its comet and its ghost light the LED under
the point, and its live finger is the library's bilinear gradient in `@RECC` - three needles applied
to the two string literals alone (`x*9//128+y*9//128*9==80` -> `N(x,y)==80`; `G(s,i,e,x,y,0,@RECC)`
after `if i>0 then return end`; `glag(0,x*9//128+y*9//128*9)` -> `glag(0,N(x,y))`), measured in this
tree under the pinned minifier at the RGB444 picker corner: Setup **478 -> 491** (417 free; 475 ->
486 at the defaults), Timer **422 -> 409** (499 free; 418 -> 405 at the defaults), both fixed points,
`checkSyntax` true, the plan's figures reproduced to the character. The defect the re-fit closes is
proved in wasmoon with the naive reading printed beside it: LED (7,7) reads `(120, 115)` on the
measured map, which `x*9//128` read as cell 80, so a finger on LED (7,7) while a loop played erased
the loop; under `N` it is cell 70 and a new recording starts. `lua-smoke` 36 -> **37**; `frames.json`
byte-identical under `UPDATE_FRAMES=1` (sha256 `9f9cd666...` either side); `ghost.png` 5,197 ->
5,234 B, the one OG image that moved, the other 25 byte-identical (154,099 -> 154,136 B); row 27
appended and unanswered; `ROW_COUNT` 27; sweep `4 19` run and unmoved. Term `+0 / +1` on the
observed **90 / 927 (+1 todo)** (13-12's `+1 / +6` already on disk at start): **90 / 928 (+1 todo)**,
twice green whole. Four negative checks red and restored by sha256. No device was touched; row 27 is
the user's.**

## Performance

- **Duration:** about 50 min (17:57Z baseline to 18:30Z second commit) plus this document and STATE.md
- **Tasks:** 2 of 2
- **Files:** 0 created, 5 modified (`ghost.ts`, `demo.ts`, `lua-smoke.spec.ts`, `HARDWARE-AUDITION.md`,
  `audition.spec.ts`), plus this document and STATE.md. `frames.json` regenerated byte-identical;
  `static/og/` re-rendered and gitignored whole

## Commits

| Hash      | Message                                                                                                                                                                                                                           |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0cc9248` | `feat(12.1-08a): GHOST on the calibrated N at both cell sites and the library's gradient in @RECC after the id gate - the erase key is LED (8,8) and LED (7,7) records, Setup 478 -> 491 and Timer 422 -> 409 at the picker corner` |
| `72eb6c3` | `test(12.1-08a): GHOST under N and G in the real VM - the key at LED (8,8) and not at LED (7,7), the comet, the gradient and the ghost on the LED's own cell at all 81 centres, the second finger refused; row 27 appended, ROW_COUNT 27` |

`git commit --only <paths> -F <message-file>`, pathspec before the flag; no push; no co-author
trailer (deviation 4). The first commit carries `ghost.ts`, `demo.ts` and the RE-AIM of the 11-11
GHOST test in `lua-smoke.spec.ts` (deviation 1) so the tree is green at that commit; the second
carries the thirty-seventh test, row 27 and `ROW_COUNT`. `eedf056` (`docs(12.1): D-27 mirror`, the
planner's) landed between this plan's start (`1eb90ae`) and `0cc9248`; 13-12 was executing throughout
on its own files and landed nothing during this plan (its working tree grew from 8 to 17 modified
files plus the untracked `DestinationReview.svelte`, none of them this plan's).

---

## The baseline, re-measured at this plan's start and close

| Name          | Brief (at `1eb90ae`)   | Observed at start (17:57Z)                                                                                                                                                             | Term                        | Observed at close (18:25Z-18:30Z)                                     |
| ------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | --------------------------------------------------------------------- |
| quick         | 89 / 921 (+1 todo)     | **90 / 927 (+1 todo)**, 3 red - `forbidden-instructions` (PAGEDISCARD named in 13-12's open `install.svelte.ts`), `instrument` scan 2 (13-12's `DestinationReview.svelte` padding), `radius` layer B (stale `build/`, older than a committed `+page.svelte`) - all 13-12's or the build's; 13-12's `+1 / +6` was already on disk | **`+0 / +1`** (lua-smoke 36 -> 37) | **90 / 928 (+1 todo)**, all green, twice (`check-counts.mjs 90 928` matching on both) |
| lua-smoke     | 36                     | 36                                                                                                                                                                                     | +1                          | **37** (THIRTY-SEVEN), the file alone green three times                |
| sweep         | 4 19                   | -                                                                                                                                                                                      | RUN (an entry's Lua moved)  | **4 19**, worst still TRACKPAD's Setup at 906 of 908, over budget 0    |
| check         | 624                    | -                                                                                                                                                                                      | -                           | **627 / 0 / 0** (13-12's three new files)                             |
| lint          | clean                  | -                                                                                                                                                                                      | -                           | exit 0 (`prettier --check .` and `eslint .`), every file touched clean |
| e2e by grep   | 79 titles              | 79                                                                                                                                                                                     | `+0 / +0`                   | **80** - the `+1` is in 13-12's uncommitted `e2e/install.e2e.ts` (HEAD 14, tree 15); this plan touched no e2e file; the suite was not run |
| catalog       | 26                     | 26                                                                                                                                                                                     | +0                          | 26                                                                    |
| audition      | 26                     | 26                                                                                                                                                                                     | +1 (row 27)                 | **27**, `audition.spec.ts` 4 green                                    |
| library       | 781 + 705              | 781 + 705 (harness)                                                                                                                                                                    | untouched                   | 781 + 705; `git diff --stat HEAD -- library.ts lua-host.ts src/vendor/ src/lib/fidelity/` empty |
| frames.json   | `9f9cd666...`          | `9f9cd666f0de08f4...`                                                                                                                                                                  | byte-identical              | `9f9cd666f0de08f4...` after `UPDATE_FRAMES=1`; `golden-frames.json` `bf54f15c...` unmoved |
| OG            | 26 at 154,099 B        | 26 at 154,099 B, `ghost.png` 5,197 B                                                                                                                                                   | `ghost.png` moves           | 26 at **154,136 B**, `ghost.png` **5,234 B**, the other 25 byte-identical |

**What 13-12 did to the runs.** Its `+1 / +6` (device-ui, install, page-target specs) was on disk
before this plan's baseline, so the observed start is 90 / 927 and not the brief's 89 / 921; the
three reds at start were its open files and a stale build, and all three were green by the close
(13-12 had rebuilt in between - `build/` is dated 18:21Z and `ghost.png` read 5,234 B before this
plan's own first `npm run build`, so 13-12's build picked up `0cc9248`). `npm run build` here died
twice: once in SvelteKit's compile on a concurrent `.svelte-kit/output/server/manifest-full.js`
write (`ENOENT`, 12.1-05's transient) and twice on `EPERM` over `build/` (a server holds it); the
`gen-og.mjs` step - this plan's deliverable - completed on every attempt with identical sha256 on
all 26 images. 12.1-06 did not run under this plan.

---

## Task 1: the three needles, the header re-measured, the sweep run

### The needles, as applied (file-wide count of each 'before' needle -> where it was applied)

| Site                     | Before                                        | After                                                              | File-wide count | Applied                                   |
| ------------------------ | --------------------------------------------- | ------------------------------------------------------------------ | --------------- | ----------------------------------------- |
| Setup, the key           | `x*9//128+y*9//128*9==80`                     | `N(x,y)==80`                                                       | x1              | inside line 198's literal, exactly once   |
| Setup, after the id gate | `if i>0 then return end if e==4 or e>8 then`  | `if i>0 then return end G(s,i,e,x,y,0,@RECC)if e==4 or e>8 then`   | x1              | inside line 198's literal, exactly once   |
| Timer, the cell          | `glag(0,x*9//128+y*9//128*9)`                 | `glag(0,N(x,y))`                                                   | x1              | inside line 201's literal, exactly once   |

The harness (`measure-1208a.mjs`, session scratchpad, `measure-1203.mjs`'s shape) isolates the two
lines matching `^  "--\[\[@cb\]\].*";$`, asserts each needle exactly once INSIDE its line, substitutes
and writes every other line back byte-identical; `git diff -U0` before the header edits showed one
changed line per literal (`@@ -198 +198 @@`, `@@ -201 +201 @@`). The cornered Setup after the needles
is byte-identical to the plan's canonical string (`cmp` against line 222 of the plan: identical, 491
characters). `grep -c "x\*9//128" ghost.ts` reads 2 after the header edits - both in prose, past
tense, the plan named; `grep -c "N(x,y)"` reads 6 (two literals, four prose). `git diff -U0 | grep -E
"values|tags|name:"` returns nothing outside comments: no knob, tag or name moved; shape character
`6` unchanged.

### The cost, measured in this tree (harness, `@intechstudio/grid-protocol@1.20260825.1135`'s `compressScript` after `initLuaFormatter()`, `cost = max(raw, compressed)`, fixed point, `checkSyntax` true on every row)

| String                          | Picker corner (both colour knobs `255,255,255`, `@LEN` 250, `@CCX` 102, `@CH` 15) | Free | Defaults (`0,255,180` / `255,80,255` / 250 / 16 / 0) |
| ------------------------------- | -------------------------------------------------------------------------------- | ---- | ---------------------------------------------------- |
| Setup, before (reproduced)      | 478                                                                              | 430  | 475                                                  |
| **Setup, after**                | **491** (+13: -13 for the key, +26 for `G(...,255,255,255)`)                     | 417  | **486** (+11: -13, +24 for `G(...,0,255,180)`)       |
| Timer, before (reproduced)      | 422                                                                              | 486  | 418                                                  |
| **Timer, after**                | **409** (-13)                                                                    | 499  | **405** (-13)                                        |
| alt Setup: `R=function(s,i)s.h=nil end` + `Q(s,i,e,x,y)` before `G` | 530 (NOT taken)                                             | 378  | -                                                    |
| alt Timer: `X(s,150)` after `local s=self` | 417 (NOT taken)                                                        | 491  | -                                                    |

Every figure the plan carried (478 / 475 / 422 / 418; 491 / 486 / 409 / 405; 530 / 417) reproduced
exactly; the Setup differs from the plan by nothing. The library as shipped read 781 + 705 in the same
run.

### The header, rewritten (not appended)

- **"SCREEN CELL 80"**: a second paragraph - the key is tested through `N` since 12.1-08a (D-26 item
  1), so it is the LED at (8,8) and not the sensor's saturated corner; `x*9//128+y*9//128*9==80` read
  cell 80 for every raw pair with both axes at 114 or above, which on the measured map is where LED
  (7,7) sits (`(120, 115)`, `120*9//128 = 8`, `115*9//128 = 8`): **a finger on LED (7,7) while a loop
  played erased the loop**; under `N` it is cell 70. Both sites read the same `U`.
- **"THE BUDGET"**: SHIPPED reads 491 / 417 free (486 at the defaults) and 409 / 499 free (405), with
  478 / 422 named as the pre-12.1-08a figures and the three needles' widths; the A/B/C table stays.
- **"THE FINGER IS THE LIBRARY'S GRADIENT"** (new): `G` on layer 0 in `@RECC` after the id gate, before
  the onset block; no `Q` so no "Q first"; `G`'s end test and `s.h=e<9` agree on a tap; why `@RECC`
  (D-13); why `G` carries the colour and there is no floor (D-11, `grid_led.h:7`, the five callers of
  `grid_alert_all_set`; `glc(...,1)`); why `G` and not `Q`/`X`/`R` (the still finger) with 530 / 417 on
  record and row 27(d) named; what a lost lift leaves (one lit 2x2 block the next onset's `G` clears).
- **"THE TIMER IS 20 ms"**: the cell the comet and the ghost light is `N(x,y)`, the LED under the raw
  pair, so a replayed point lands on the LED the finger was over; until 12.1-08a it was the naive
  ninth (12.1-05's column-out finding); `s.g` still holds the RAW pair so the CC pair is unchanged (D-14).
- The cost sentence ("cell 80 - the LED at (8,8), through `N`") and the `@RECC` knob's comment (the
  knob now also colours the layer-0 gradient) adjusted.

### `demo.ts`

The GHOST path's two dated paragraphs inverted: the corner is read through `N` as LED (8,8); cells 1
and 6 draw in columns 1 and 6, the trace under the drawn finger again; the 12.1-05 finding kept as
history. `git diff --numstat` 12 / 10, comment lines only (every changed line begins ` *`).

### The gates

`npm run test:sweep | check-counts.mjs 4 19` matches (1,296 combinations in 5.2 s; worst 906 at
`none/none/trackpad/hi=false/grid=false`; GHOST `passA 320 passB 8192 format w payload 11`).
`touch-guard`, `decay-idiom`, `host-surface`, `frames` alone: 4 files / 17 passed.

### `library.ts`'s caller list - read, and NOT edited

The library's header DOES list callers: section 5 names `N`'s (`ARC's stop tap (12.1-03, N(x,y)==40)
and 13-15's region lookup`) and `G`'s (`EUCLID, STEPS, RADAR POINTS, SONAR (12.1-03), CHORUS, MORPH,
CONSOLE, LUMEN (12.1-04) - eight`), and section 4 already names GHOST as a single-pointer card. The
plan says "add GHOST in a one-line edit"; the orchestrator's brief for this run says `library.ts` is
not to be touched short of a Rule-1 bug. A caller-list comment is not a Rule-1 bug, so the brief was
followed: `library.ts` is byte-unchanged and the two now-incomplete sentences (library.ts section 5's
`N` and `G` caller lists; `arc.ts:207-208`'s "ARC is `N`'s one caller today") are logged in
`deferred-items.md` for 12.1-08b, which rewrites `N`'s paragraph when it moves `N` to 255/0 (R-20).
GHOST calls `N` where it is today, in 255/6, through the same global name - the move changes nothing
for the entry.

---

## Task 2: the VM, `frames.json`, the OG render, row 27

### The thirty-seventh test - "draws GHOST's live finger as the gradient in @RECC, lights the comet and the ghost on the LED through N, and erases on LED (8,8) but records on LED (7,7) where the naive divisor read cell 80"

Through `open(entryById("ghost"))` - the host over both library strings, the entry's `gtt(0,20)`
Timer stepped by `run` - at the defaults; every expected cell from `KX` / `KY` (`calibratedCell`,
`expectedFinger`, `ledCentre`), never typed. The run log:

```
GHOST UNDER N AND G (plan 12.1-08a):
  LED (8,8) at (126,126): N -> cell 80, naive -> cell 80; pressed with 11 points recorded -> s.n 0
  LED (7,7) at (120,115): N -> cell 70, naive -> cell 80 (THE FIXED DEFECT: the old test erased here); pressed with a recording present -> s.n 2, a new recording
  81 LED centres: gradient at 255 on layer 0, comet on layer 1 and ghost on layer 2 all on the LED's own cell; the naive divisor puts 45 of the 81 centres on a different cell
    ######### (x9)
  midpoint x=74 on row 4: layer 0 = {"40":135,"41":119} (cells 40 and 41 at the bilinear weights)
  a second finger on LED (5,5) under contact 0 on LED (3,3): layer 0 held cell 30 alone; s.n 2 -> 3
  a drag over LEDs (1|4|7,3): the ghost replays cells 28 31 34 (the naive divisor would have lit 27 31 35); the key pulsed
```

| Claim                          | Asserted                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. the key                     | after 11 recorded points a press at `(KX[8],KY[8])` leaves `s.n == 0`, layers 1 and 2 all phase 0 (`armVector`), the erasing finger drawn at `{80: 255}` on layer 0 while down and dark after the lift; a fresh recording, then a press at `(KX[7],KY[7])` leaves `s.n` in 1..2 (a NEW recording - not erased, not appended), the comet on cell 70, the key's pulse at 80; `naiveCellOf(KX[7],KY[7]) === 80` and `calibratedCell === 70` asserted and printed |
| 2. the comet cell              | at all 81 centres, after `run(3)`, layer 1's lit set is exactly `{cell, 80}` (the comet and the key's first pulse, which the Timer arms on the first tick after an onset because `s.n > 0`), the comet's `fre` 250; at cell 80 the loop erases first (the card's own rule) and presses again on an empty pad |
| 3. the gradient                | at the same centres layer 0 is exactly `{cell: 255}` and equals `litOf(expectedFinger(x,y).phases)`, in `fingerColourOf(entry)` which equals the `@RECC` knob's default `0,255,180` (the colour path through `@RECC`); the midpoint `x = 74` lights exactly `{40: 135, 41: 119}`; every lift leaves layer 0 `{}`; a `touchTap` (code 9) at `(KX[2],KY[2])` records exactly one point, lights no gradient, and replays on layer 2 at cell 20 |
| 3d. the second finger (added)  | contact 0 on LED (3,3), contact 1 on LED (5,5): layer 0 holds `{30: 255}` alone through the second finger's press and lift; `s.n` did not restart                                                                                                                                                                                                                  |
| 4. nothing else moved          | after each lift layer 2 replays the point on the LED's own cell; a drag over LEDs (1,3) (4,3) (7,3) replays exactly `28 31 34` over 200 ticks (the naive set `27 31 35` asserted different); the red key pulsed while `s.n > 0`                                                                                                                                       |

Header line THIRTY-SIX -> **THIRTY-SEVEN** with the sentence that 12.1-08a added GHOST and 12.1-08b
adds one for `K`. The file alone: **37 passed** (three runs).

### The 11-11 GHOST test, re-aimed (deviation 1)

"records a GHOST path, replays it, and takes it back on the red corner - twice" drove
`cellCentre(column)` (the naive ninths) and asserted against `cellOf = x*9//128+y*9//128*9`. Under
`N` it went red on round 1: `expected [29, 31, 33, 80] to deeply equal [28, 31, 34, 80]` - the naive
centres 21 / 64 / 106 on row 49 read as LEDs 2 / 4 / 6 through the map. Re-aimed: `pathX =
PATH_COLUMNS.map(c => ledCentre(c, "x"))`, `pathY = ledCentre(3, "y")`, `cellOf = calibratedCell`; the
two reset pixels `keyPoint = KX[8] + 1 - round * 2` (125 and 123, both cell 80 to `N`; the first
attempt, 123 and 121, found 121 is column 7 - the outer x segment's cell starts at 123). Its three
claims are unchanged and green: `round 1: replayed [28,31,34,80] on CC 16 values [13,64,120]; after
the reset +1 -> 0 lit, +2 -> 0 lit, +200 -> 0 lit`, round 2 the same. The wire claim now carries the
knots (13, 64, 120) because GHOST sends the raw pair.

### `frames.json` - regenerated and byte-identical

| Step                                                                                | Result                                                                                                   |
| ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| sha256 at start                                                                     | `9f9cd666f0de08f4b56600db9f1ba2d46fc43727b698a9edfd11f4ec796a6eb3`                                       |
| `UPDATE_FRAMES=1 npx vitest run --project server src/lib/catalog/frames.spec.ts`    | rewrote the fixture and **failed by design** (`frames.spec.ts:127`), `1 failed / 4 passed`               |
| `sha256sum -c` and `cmp` against the pristine copy                                  | **OK - BYTE-IDENTICAL**                                                                                  |
| plain `frames.spec.ts`                                                              | **5 passed**                                                                                             |
| `golden-frames.json`                                                                | `bf54f15c7fee5af9...` unmoved                                                                            |

GHOST rests black (`restsBlack: true`) and the fixture samples `engine.run(tick)` with no finger, so a
re-fit that lights only under a finger cannot move it - 12.1-05's finding, held.

### The OG render - 26 sizes, `ghost.png` the one that moved

| Image        | Before | After    | Delta   | Image        | Before | After | Delta |
| ------------ | ------ | -------- | ------- | ------------ | ------ | ----- | ----- |
| arc          | 6786   | 6786     | 0       | pinwheel     | 6739   | 6739  | 0     |
| aurora       | 6643   | 6643     | 0       | pomodoro     | 6563   | 6563  | 0     |
| chorus       | 6508   | 6508     | 0       | quadrant     | 6210   | 6210  | 0     |
| console      | 5174   | 5174     | 0       | radar        | 6713   | 6713  | 0     |
| cull         | 5913   | 5913     | 0       | radar-points | 6181   | 6181  | 0     |
| dial         | 6776   | 6776     | 0       | snake        | 4611   | 4611  | 0     |
| euclid       | 6029   | 6029     | 0       | sonar        | 5337   | 5337  | 0     |
| faders       | 6328   | 6328     | 0       | stage        | 5868   | 5868  | 0     |
| **ghost**    | 5197   | **5234** | **+37** | starfield    | 6957   | 6957  | 0     |
| joystick     | 4326   | 4326     | 0       | steps        | 5931   | 5931  | 0     |
| lumen        | 6834   | 6834     | 0       | strip        | 5917   | 5917  | 0     |
| morph        | 4602   | 4602     | 0       | trackpad     | 4443   | 4443  | 0     |
| ninepads     | 5270   | 5270     | 0       | wheels       | 6243   | 6243  | 0     |

Total 154,099 -> **154,136 B**; sha256 of the other 25 equal to the start snapshot (`sha256sum -c`: one
FAILED, `ghost.png`); three renders (18:15Z, 18:18Z, 18:24Z) with every sha256 equal between them.
None tracked (`.gitignore:23` is `static/og/`), so nothing to commit.

**Why `ghost.png` moved, at the frame level** (`probe-demo-1208a.mjs`, scratchpad: GHOST's demo path
driven to the end of its 600-tick period through the real `TouchSampler` at the knots, once on the
pristine strings and once on the committed ones):

| Entry strings           | End frame (tick 600), 12 lit                       | Mid-drag tick 20                                                                    |
| ----------------------- | -------------------------------------------------- | ----------------------------------------------------------------------------------- |
| pre-12.1-08a (`x*9//128`) | `8 14 16 31 32 38 39 48 59 77 79 80` (sha `6073...`) - 12.1-05's "new" column | `54 56 72 80` - the comet at the naive cells, the key                        |
| **12.1-08a (`N` + `G`)** | **`16 23 24 31 32 38 39 48 59 68 69 80`** (sha `a5fb...`) - **12.1-05's "old" column**, the pre-12.1-05 render's frame | `47 (0,126,89) 55 56 64 80` - the layer-0 gradient in `@RECC` under the finger beside the comet |

The end frame is the second ghost looping after the lift at tick 352, so it holds no finger: `G`
contributes nothing to the OG and the picture is exactly the one the 12-12 render had (5,234 B
then, 5,234 B now) - the knots through `N` land on the authored cells as the naive centres through
the naive divisor once did, which is what "the trace sits under the drawn finger again" means in
bytes. The plan's "adds the layer-0 gradient to the demo frame" describes the mid-drag frames, which
the OG does not capture. `og/build.spec.ts` (at `src/lib/og/`, not the plan's `src/lib/catalog/og/`)
green inside the quick suite.

### Row 27 and `ROW_COUNT`

Row 27 appended after row 26 as the plan's text verbatim (one line, 1,162 characters, six columns).
`prettier --check` refused the file; `prettier --write docs/HARDWARE-AUDITION.md` (that one file)
re-padded **row 27 alone**: a script compared every other line to the pristine copy and found 344 of
344 verbatim (0 re-padded, 0 differing) - the check column was already 1,503 wide, wider than row 27's
cell. `audition.spec.ts`: `ROW_COUNT` 26 -> **27** with the dated note (clauses (a)-(d), (d) the user's
design question, 530 / 417 named), the row-count title "twenty-six" -> "twenty-seven"; 4 green. Row
27 is the user's and unanswered; nothing in this plan is hardware-verified.

---

## The negative checks, four, each restored by sha256

| Check | Planted (scratch copy written into the tree)                                        | Result                                                                                                                                                                                                                                                                                                                                                       |
| ----- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| N1    | `ghost.ts`: `G(s,i,e,x,y,0,@RECC)if i>0 then return end` - `G` before the id gate   | **First pass GREEN on both** `touch-guard.spec.ts` (3 passed) and the smoke tests (nothing drives a second contact through GHOST) - as the plan allowed for. The check was trusted and the TEST was found wanting: clause 3d (a second finger) added to the 37th test; **second pass RED as named**: `a second finger on LED (5,5) draws no gradient - G is behind the id gate: expected { '30': 255, '50': 255 } to deeply equal { '30': 255 }`. `touch-guard` stays green under N1 (it reads the event pattern, not the gate's order) |
| N2    | `ghost.ts`: the key needle alone reverted to `x*9//128+y*9//128*9==80`               | RED: `ghost: a press on LED (7,7) must NOT erase - a new recording starts: expected 0 to be greater than 0`                                                                                                                                                                                                                                                  |
| N3    | `audition.spec.ts`: `ROW_COUNT = 28`                                                 | RED naming 27: `checklist rows: expected 27 to be 28`                                                                                                                                                                                                                                                                                                        |
| N4    | `frames.json`: aurora tick 0's `nonZeroBytes` 149 -> 148                             | RED as 12.1-05's tripwire: `aurora at tick 0: lit byte count moved from 148 to 149`; pristine copy restored, `sha256sum -c` OK                                                                                                                                                                                                                              |

---

## Deviations from Plan

**1. [Rule 3 - blocking; a file the plan owns] The 11-11 GHOST test in `lua-smoke.spec.ts` mirrored
the naive divisor and went red under the re-fit.** Found during Task 1's first whole-file run (the plan
foresaw only `+1` in this file). Fixed by re-aiming it at the knots (`ledCentre`, `calibratedCell`, two
reset pixels inside LED (8,8)'s calibrated cell), its three claims unchanged, committed with the re-fit
in `0cc9248` so that commit is green; the 37th test landed in `72eb6c3`. The term is still `+0 / +1`.

**2. [Rule 2 - a negative check with nothing to bite] N1 came back green; the 37th test gained a
second-finger clause (3d).** GHOST's `if i>0 then return end` was pinned by nothing; with `G` behind
it, a second contact must draw no gradient. Added and N1 then red as named. The plan's fallback
sentence ("the id gate then only protects GHOST's own state") is not needed.

**3. [Process - `library.ts` not edited although its caller list exists]** The plan's conditional
one-line edit yields to the orchestrator's brief for this run (no `library.ts` short of a Rule-1 bug).
Two stale caller sentences logged in `deferred-items.md` for 12.1-08b. `git diff --stat HEAD --
src/lib/catalog/library.ts` empty.

**4. [Process - the commit trailer]** The harness asks for a co-author trailer; the repository's rule
(12-12 deviation 10, the plan's standing rules) is no attribution and no trailer. The repository's
rule was followed on both commits; the conflict is reported here.

**5. [Process - `npm run build` did not complete; the OG step did]** Three attempts: `ENOENT` on
`.svelte-kit/output/server/manifest-full.js` (a concurrent `.svelte-kit/` write, 12.1-05's transient),
then `EPERM` on `build/` twice (a server holds it - 13-12's). `gen-og.mjs` ran to completion each time
with identical sha256 on all 26 images, which is this plan's deliverable; `build/` itself was refreshed
by 13-12's build at 18:21Z (it carries `0cc9248`, and `radius.spec.ts` layer B is green in both close
runs). Not retried further; named.

**6. [Process - `gsd-tools state` not run; STATE.md by script]** Against a copy, the five invariants
(`status: executing`, `completed_phases: 11`, `percent: 100`, `total_phases: 14`, `total_plans: 160`)
asserted before and after; `completed_plans` unchanged at 152 (Phase 12.1 is outside `total_plans`);
Phase 12.1 to **6 of 11** (Band 1 + 08a) beside Phase 12 (gate landed, bench pending) and Phase 13 (12
of 20, 13-12 in flight); the copy's sha256 checked against the tree immediately before the write.
`advance-plan`, `update-progress`, `record-session`, `roadmap update-plan-progress` and `requirements
mark-complete` not run; `.planning/ROADMAP.md` byte-unchanged; **CAT-04 stays `[ ]`**; `REQUIREMENTS.md`
untouched (the gate amends).

**7. [Process - the plan's path for `og/build.spec.ts`]** The plan names
`src/lib/catalog/og/build.spec.ts`; the file is `src/lib/og/build.spec.ts`. It ran inside the quick
suite (green); the planned four-file verify line was run as three files plus the suite.

---

## What the plan asserts that the tree does not support

- "this render puts the trace under the drawn finger **and adds the layer-0 gradient to the demo
  frame**" - the OG is captured at the END of the path (`gen-og.mjs:101-106`), after the lift at tick
  352, so no finger is in the frame and `G` adds nothing; the move is the trace alone, back to the
  pre-12.1-05 picture (5,234 B, the 12-12 figure).
- "`src/lib/catalog/og/build.spec.ts`" - the file is `src/lib/og/build.spec.ts` (deviation 7).
- "the observed baseline **89 / 921**" - 13-12's `+1 / +6` was already on disk; the tree read 90 / 927
  at start with three reds, all theirs or the build's, and 90 / 928 green at the close.
- "(N1) `touch-guard.spec.ts` or the smoke test of task 02 must go red" - neither did until the
  second-finger clause was added (deviation 2); `touch-guard` cannot see the gate's order.
- "add GHOST in a one-line edit to `library.ts`" - conditional on the caller list existing, which it
  does; yielded to the run's brief (deviation 3).

## Questions for the user (row 27; nothing here is hardware-verified)

1. **Row 27(d):** with a loop recording, pause mid-drag with the finger perfectly still for two
   seconds, then continue - does the recording keep going through the pause (it should; the library's
   expiry is deliberately not used), and would you rather a lost lift ended the loop after three
   seconds instead? The alternative (`Q` + `R` + `X(s,150)`) is costed at Setup 530 / Timer 417 and is a
   one-commit change if the answer is yes - at the price of a still finger's recording ending after 3 s.
2. **Row 27(a)-(c):** does the comet's head sit under the fingertip and do both LEDs glow between two;
   does the ghost retrace the LEDs crossed (not one column inward at the edges); does LED (7,7) with a
   loop playing start a new recording rather than erasing, and does LED (8,8) erase?
3. Not asked by the plan but visible in the probe: the gradient under a moving finger sits beside the
   comet on layer 1 in the same `@RECC` - on the bench, does the live 2x2 gradient read as the finger
   and the single decaying cell as its trail, or do the two blur into one? (D-13's "one colour
   everywhere" row already asks the general form.)

## Known Stubs

None. Row 27 is a bench row by design, not a stub.

## Self-Check: PASSED

- `src/lib/catalog/entries/ghost.ts`, `src/lib/sim/demo.ts`, `src/lib/sim/lua-smoke.spec.ts`,
  `docs/HARDWARE-AUDITION.md`, `src/lib/catalog/audition.spec.ts` exist and carry the changes above
  (`N(x,y)==80` in ghost.ts, `THIRTY-SEVEN` in lua-smoke.spec.ts, `| 27  |` in HARDWARE-AUDITION.md).
- Commits `0cc9248` and `72eb6c3` are in `git log`.
- `frames.json` sha256 equal to the start snapshot; `static/og/` 26 files at 154,136 B.
- No device was connected to, written to or deployed to. `src/vendor/` untouched; `firmware-oracle.spec.ts`
  unedited and green inside the quick suite; `.planning/ROADMAP.md` untouched; CAT-04 `[ ]`.
