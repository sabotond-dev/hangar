---
phase: 12.1-gradient-touch
plan: 06
type: execute
wave: 6
band: 2
depends_on: ["12.1-05", "13-12"]
files_modified:
  - src/lib/protocol/constants.ts
  - src/lib/protocol/constants.spec.ts
  - src/lib/transport/sequence.ts
  - src/lib/transport/sequence.spec.ts
  - src/lib/transport/fixtures/synthetic.ts
  - src/routes/dev/skeleton/+page.svelte
autonomous: true
requirements: [SAFE-03, SAFE-07, PREV-02]
count_term: "PREV_FILES + 0 / PREV_TESTS + 2 (constants.spec +1, sequence.spec +1); e2e + 0 / + 0 by grep; sweep 4 19 unmoved by declaration"

must_haves:
  truths:
    - "HANGAR addresses the system element's Timer (255/6) as it addresses its Setup - read from the pinned package, never typed - and the header that refused it says instead that D-19 retired the refusal and 255/4's is 13-17's pending removal."
    - "The one writer writes FOUR strings in the order 255/6, 255/0, 0/6, 0/0 - the Timer before the Setup that calls it, on the same rule that already puts 0/6 before 0/0 - and the one fetcher reads four; the order lives in an ordered slot list so 13-17 adds 255/4 as one row, not a fifth function."
    - "sequence.spec.ts asserts that 255/4 still never appears in any frame and that 255/6 appears exactly once per install, first."
  artifacts:
    - path: "src/lib/protocol/constants.ts"
      provides: "SYSTEM_DEFAULT_TIMER through defaultFor(ELEMENT_SYSTEM, EVENT_TIMER); the header rewritten"
      contains: "SYSTEM_DEFAULT_TIMER"
    - path: "src/lib/transport/sequence.ts"
      provides: "SLOTS - the ordered list; ConfigSet and FetchedSet with systemTimer; writeAll and fetchAll over the list"
      contains: "systemTimer"
  key_links:
    - from: "src/lib/transport/sequence.ts"
      to: "src/lib/protocol/constants.ts"
      via: "ELEMENT_SYSTEM + EVENT_TIMER as the first slot"
      pattern: "EVENT_TIMER"
    - from: "src/lib/transport/sequence.spec.ts"
      to: "src/lib/transport/fixtures/synthetic.ts"
      via: "the fixture's separate system RAM answers 255/6 and stores it"
      pattern: "systemOf|ELEMENT_SYSTEM"
---

<objective>
The fourth string on the wire: the protocol constants and the one writer.

**Band 2 starts here, and it starts only after `13-12-SUMMARY.md` exists.** 13-12 (wave 13, the hard
Phase-12 band) rewrites `constants.ts`, `install.svelte.ts`, `snapshot.ts`, `install.e2e.ts` and
`fake-zona.ts` for the page target; this plan and the two after it are written against its **landed**
shape. Read its SUMMARY first; if it does not exist, stop. `sequence.ts` is the one file in this plan
13-12 only *reads* (`restorePageChange`, `targetOf`), and 13-12's own verification asserts it unmoved
during 13-12 - which is why the seam waits.

**What the constants say today, and why it is wrong now.** `constants.ts`'s header: *"WHY EVENTS 4 AND 6
OF THE SYSTEM ELEMENT ARE NEVER WRITTEN AND NEVER FETCHED … Event 6 is a second timer whose default is
`print("tick")`, which nothing in HANGAR arms … so it is left exactly where firmware put it."* D-19
(2026-09-10) made that a pending removal; D-03 (2026-09-11, the user's "yes") is the reason to remove
the event-6 half **here**, where it is first needed. The event-4 half stays exactly as 13-17's pending
removal and the header says so in those words (`12.1-CONTEXT.md` D-06).

**The one writer becomes an ordered list.** `writeAll` writes 255/0, 0/6, 0/0 today with the order in
the function and two reasons in its header. The fourth string goes **first**: a CONFIG/EXECUTE registers
the body and runs it at once (`grid_decode.c:1283-1288`), and 255/0's `self:tim()` needs the method
registered before it runs - the same reason 0/6 precedes 0/0. Rather than a fourth `writeOne` line, the
order becomes data: `SLOTS`, an ordered list of `{ element, event, key, label, stepId }`, so 13-17 adds
255/4 as one row and `install.spec.ts`'s order test reads the list.

Purpose: the wire admits the slot the user approved, in the order the firmware needs, as a list the next
slot joins.
Output: one constant, one list, two tests. Term `+0 / +2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-gradient-touch/12.1-CONTEXT.md
@.planning/phases/12.1-gradient-touch/12.1-VALIDATION.md
@.planning/phases/12.1-gradient-touch/12.1-RESEARCH.md
@.planning/phases/12.1-gradient-touch/12.1-02-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-12-SUMMARY.md
@.planning/phases/12-touch-framework/12-02-SUMMARY.md
@.planning/phases/12-touch-framework/12-03-SUMMARY.md
@.planning/phases/13-gui-overhaul/SLOT-ARITHMETIC.md
@src/lib/protocol/constants.ts
@src/lib/protocol/constants.spec.ts
@src/lib/transport/sequence.ts
@src/lib/transport/sequence.spec.ts
@src/lib/transport/fixtures/synthetic.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer** (12-12 deviation 10). **No
  emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** The four-write order is proved against the
  synthetic fixture; on a ZONA it is the two runbook rows after 13-12's (plan 08).
- **Sibling repositories read-only; `src/vendor/` untouched; no manifest row.** `grid_decode.c:1283-1288`
  is a citation.
- **Do not edit `.planning/ROADMAP.md`.** Nothing under `13-gui-overhaul/` or `12-touch-framework/`.
- **`gsd-tools state` commands corrupt STATE.md**: script against a copy, assert invariants, skip
  `roadmap update-plan-progress`.
- **No `git checkout/restore/stash/clean`.** Scratch copies with sha256. **Never `prettier --write .`.**
  `git commit --only <paths> -F <msgfile>`; no push.
- **`@intechstudio/grid-protocol` stays at its exact pin**; `protocol-pin.spec.ts` green and unedited;
  `forbidden-instructions.spec.ts` green (the encoder's name stays in `descriptors.ts` only).
- **D-20's rule (Phase 7): a firmware default is READ from the package, never typed.** The one literal of
  the 255/6 default lives in `constants.spec.ts`, pinning the package's value.
- **State every count as a carried name plus a delta, re-measured at this plan's start** - 13-12 and 13-13
  have landed by now and the totals have moved; Band 1 left `+1 / +16` on its observed baseline. Measure,
  then assert **`+0 / +2`**.
- **Concurrency:** 13-13 (wave 12) and 13-12 (wave 13) have landed; **13-14 must not start until Band 2
  has landed** (`12.1-VALIDATION.md` hand-offs) - say in the SUMMARY that 12.1-06 is on the tree so the
  Phase 13 orchestrator holds 13-14. No Phase 13 plan writes this plan's five files between 13-12 and
  13-17.
- **Every agent on Fable 5.1.**
</standing_rules>

<interfaces>

## `constants.ts`

```ts
export const SYSTEM_DEFAULT_TIMER = defaultFor(ELEMENT_SYSTEM, EVENT_TIMER);   // 255/6's own default
```

`SYSTEM_EVENTS` already holds all three system events; `defaultFor` already throws on a missing one.
**The header**: the "WHY EVENTS 4 AND 6 … ARE NEVER WRITTEN" section is rewritten as "TWO ELEMENTS, FOUR
OF THEIR FIVE EVENTS": 255/0 and 255/6 since 12.1 (the library's two halves, the second permitted by
D-19 and chosen by the user on 2026-09-11), 0/0 and 0/6; **event 4 is still never written or fetched
here, and that is 13-17's pending removal under D-19, not a rule** - the utility-button sentence kept as
the reason it was once refused. The `ELEMENT_SYSTEM` doc comment's "Only its setup (0) is ever
addressed" moves with it.

## `sequence.ts`

```ts
/** One row per slot HANGAR writes, IN WRITE ORDER. 13-17 adds 255/4 as a row here and nowhere else. */
export const SLOTS: readonly {
  readonly element: number; readonly event: number;
  readonly key: keyof ConfigSet;           // "systemTimer" | "system" | "timer" | "setup"
  readonly label: string;                  // "System timer" | "System" | "Timer" | "Setup"
  readonly write: string; readonly fetch: string; readonly refetch: string;   // the pinned step ids
}[] = [
  { element: ELEMENT_SYSTEM, event: EVENT_TIMER, key: "systemTimer", label: "System timer", write: "write-system-timer", fetch: "fetch-system-timer", refetch: "refetch-system-timer" },
  { element: ELEMENT_SYSTEM, event: EVENT_SETUP, key: "system",      label: "System",       write: "write-system",       fetch: "fetch-system",       refetch: "refetch-system" },
  { element: ELEMENT_TOUCH,  event: EVENT_TIMER, key: "timer",       label: "Timer",        write: "write-timer",        fetch: "fetch-timer",        refetch: "refetch-timer" },
  { element: ELEMENT_TOUCH,  event: EVENT_SETUP, key: "setup",       label: "Setup",        write: "write-setup",        fetch: "fetch-setup",        refetch: "refetch-setup" },
];
export interface ConfigSet  { systemTimer: string; system: string; timer: string; setup: string; }
export interface FetchedSet { systemTimer: FetchedEvent; system: FetchedEvent; timer: FetchedEvent; setup: FetchedEvent; }
```

`writeAll` and `fetchAll` iterate `SLOTS`; the existing step ids and labels are unchanged for the three
old rows so every consumer of `write-system` / `fetch-setup` (`install.spec.ts`, `install.e2e.ts`, plan
05's gate) keeps reading the same strings. `writeBack` (the skeleton's adapter) passes the fetched
`systemTimer` through. `runNoOpCycle` and `runBurstProbe` read four. **The header's two reasons stay and
gain the third**: 255/6 first because 255/0 calls it.

## `synthetic.ts`

Already answers a fetch of 255/6 with the package's default when nothing has been written (`:370-376`)
and stores a system write under its event (`:460`). **Verify, do not restructure**: a test in
`sequence.spec.ts` proves the round trip through the fixture for all four; the `ZonaState` shape is not
re-keyed (12-02's rule for `moduleState`).

## `sequence.spec.ts`

The write-order test (`:440-470`): four step ids in order; the loop at `:466` becomes **two assertions** -
no frame ever addresses `255/4`; exactly one frame per install addresses `255/6` and it is the first.
The fetcher half reads four ids. **New test**: `SLOTS` is the single source of the order - assert
`writeAll`'s recorded frames equal `SLOTS.map(...)` element-for-element, and that a `ConfigSet` handed
over with its keys in another order changes nothing (the existing "the order is not the caller's"
property, re-proved over four).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12.1-06-01: The constant, read from the package, and the header that stops refusing event 6</name>
  <files>src/lib/protocol/constants.ts, src/lib/protocol/constants.spec.ts</files>

  <read_first>
    - `src/lib/protocol/constants.ts` in full - the header, `defaultFor`, the three defaults, and
      **whatever 13-12 changed** (`git log -1 -- src/lib/protocol/constants.ts`; read the diff)
    - `src/lib/protocol/constants.spec.ts` - how the three defaults are pinned today
    - `13-CONTEXT.md` D-19 - the exact wording of what is retired and what is not
    - `12.1-CONTEXT.md` D-06
  </read_first>

  <action>
Add `SYSTEM_DEFAULT_TIMER` per the interfaces block; rewrite the header section by section (keep 13-12's
additions byte-for-byte); `constants.spec.ts` +1: `SYSTEM_DEFAULT_TIMER` equals the package's
`get_element_events(SYSTEM)` entry for `value === 6` and its length is what the research read (22 -
verify, and record the actual string in the SUMMARY), and `defaultFor(255, 4)` still returns the
utility default without anything in `constants.ts` exporting it.

**Negative check:** type the default as a literal in `constants.ts` in a scratch copy - the spec's
identity test still passes but `forbidden-instructions.spec.ts` / the D-20 rule is the reason it is
refused; instead assert with `grep -c 'print("tick")' src/lib/protocol/constants.ts` equal to **0** in the
verify block. Restore.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/protocol/ && test "$(grep -c 'print(\"tick\")' src/lib/protocol/constants.ts)" = "0"</automated>
    `constants.spec.ts` **+1**; `protocol-pin.spec.ts`, `forbidden-instructions.spec.ts`,
    `descriptors.spec.ts` green and unedited; no literal default in `constants.ts`.
  </verify>

  <done>
`SYSTEM_DEFAULT_TIMER` read through `defaultFor`, pinned by one test against the package, the header no
longer refuses event 6 and names event 4 as 13-17's pending removal, 13-12's lines untouched.
  </done>
</task>

<task type="auto">
  <name>Task 12.1-06-02: SLOTS, four writes in order, four fetches, the fixture verified</name>
  <files>src/lib/transport/sequence.ts, src/lib/transport/sequence.spec.ts, src/lib/transport/fixtures/synthetic.ts, src/routes/dev/skeleton/+page.svelte</files>

  <read_first>
    - `src/lib/transport/sequence.ts:265-420` - `FetchedSet`, `fetchAll`, `ConfigSet`, `writeAll`'s
      header and body, `writeBack`; `:480-560` - `runNoOpCycle`, `runBurstProbe`
    - `src/lib/transport/sequence.spec.ts:400-500` - the write-order test and the `:466` loop
    - `src/lib/transport/fixtures/synthetic.ts:355-380`, `:445-470` - the system RAM and its flash copy
    - `12-02-SUMMARY.md` - how `writeAll` was generalised from two to three and what it deliberately did
      not re-key
    - `13-12-SUMMARY.md` - anything it says about `sequence.ts` (it should have left it unmoved)
    - `src/routes/dev/skeleton/+page.svelte:25-60` and `:350-395` - the one consumer of `FetchedSet`
      outside the store
  </read_first>

  <action>
**1.** `SLOTS`, `ConfigSet`, `FetchedSet` per the interfaces block; `writeAll` / `fetchAll` over the list;
every other caller in `sequence.ts` reads four. **2.** The header: the third reason. **3.**
`sequence.spec.ts`: the order test moved to four with the two halves of the old assertion; the new
`SLOTS`-is-the-source test; the fetcher half at four ids; a round trip through `synthetic.ts` that writes
four and reads four back, asserting the system element's two events are held apart from the touch
element's two (12-RESEARCH Pitfall 1's conflation stays impossible). **4.** `synthetic.ts`: change only
if the round trip fails, and say what. **5.** `src/routes/dev/skeleton/+page.svelte` (`:30` imports
`FetchedSet`; `:356-393` reads three fields, echoes them through `writeBack` and compares three in its
store proof): the readout line and the comparison read **four**, so the fourth string is inside the
skeleton's no-op proof rather than silently outside it; nothing else on the page moves (its two-specifier
rule stands, `config-shape.spec.ts` green).

**Negative checks:** swap the first two rows of `SLOTS` in a scratch copy - the order test goes red
naming `write-system` before `write-system-timer`; add a 255/4 row - the "never 255/4" assertion goes
red. Restore, sha256 both sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/transport/</automated>
    `sequence.spec.ts` **+1**; `capture.spec.ts`, `fake.spec.ts`, `queue.spec.ts`, `transport.spec.ts`
    green (the capture fixture's step ids unchanged for the three old rows).
  </verify>

  <done>
The order is data, four writes go out 255/6 first and four fetches come back, 255/4 is still never
addressed and 255/6 exactly once per install, the fixture round-trips all four with the two RAMs apart;
two negative checks red and restored.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` - **expect errors here until plan 07**: `install.svelte.ts`
  and `model.ts` build `ConfigSet`s with three keys. **Say so**, list the errors, and confirm they are
  exactly the four-key mismatch in the files plan 07 owns (`install.svelte.ts`, `wire-pin.spec.ts`,
  `model.ts`) and nothing else. This is the 12-02 -> 12-03 shape: the transport lands first, the store
  follows in the next wave. `npm run lint` clean.
- `npm run test:quick` - `install.spec.ts` and `wire-pin.spec.ts` may be red for the same reason; **name
  them and assert the count of everything else**; this plan's term is `+0 / +2`, written out.
- `git diff --quiet -- src/lib/device/ src/lib/tune/` - plan 07's files untouched here.
- `grep -c "test(" e2e/*.e2e.ts` summed, unchanged.
- `git diff --stat HEAD -- src/vendor/` empty; `git diff --quiet -- .planning/ROADMAP.md`.
</verification>

<success_criteria>
1. 255/6 is addressed through the package like every other event, and the header says what D-19 retired
   and what 13-17 still owns.
2. The order is an ordered list; four writes 255/6 first; four fetches; 13-17 adds a row.
3. 255/4 never; 255/6 exactly once per install, first - asserted.
4. The type errors left for plan 07 are named and are exactly the store's and the tuner's.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-gradient-touch/12.1-06-SUMMARY.md` carrying: the
observed baseline (after 13-12 / 13-13) and `+0 / +2`; the 255/6 default string as read from the package
with its length; `SLOTS` as shipped; the frame order as recorded by the test; the check errors left for
plan 07 by file; the negative checks; the sentence that 13-14 must wait for Band 2; and the sentence
that no device was touched.
</output>
