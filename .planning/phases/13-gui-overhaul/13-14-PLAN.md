---
phase: 13-gui-overhaul
plan: 14
type: execute
wave: 14
depends_on: ["13-12"]
files_modified:
  - src/lib/sandbox/model.ts
  - src/lib/sandbox/geometry.ts
  - src/lib/sandbox/geometry.spec.ts
  - src/lib/sandbox/emit.ts
  - src/lib/sandbox/emit.spec.ts
  - src/lib/sandbox/cost.ts
  - src/lib/store/schema.ts
  - .planning/phases/13-gui-overhaul/13-COPY-NEW.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, CONT-02]

must_haves:
  truths:
    - "A region can never leave the 9x9 and can never overlap another, because the compiled cell-to-region map holds ONE index per cell - the data structure makes the rule unrepresentable rather than merely discouraged."
    - "The surface's coordinates are one-based in the interface and zero-based in the model, and the translation has a named door the way knobPosition and colourPosition already do - not an inline arithmetic expression in four components."
    - "The emitted string is measured under the pinned minifier at the RGB444 picker corner for one, four, eight, twelve and sixteen elements, and each figure is recorded - a compiler whose cost is estimated is a compiler that fails at install."
    - "Dead branches are eliminated: a surface of four faders emits no button, XY or knob code, and the saving is proved by a measured pair rather than asserted."
    - "The data half is the same under every answer 13-02 recorded, because the probe already proved the slot the runtime needs; what the answer changes is the ceiling in element kinds, and this plan emits against the recorded ceiling rather than against a guess."
    - "Duplicate finds a free window of the same size and never silently deletes; two edge-adjacent regions with no gap raise a warning at the region, because a press on the seam is a coin flip."
  artifacts:
    - path: "src/lib/sandbox/model.ts"
      provides: "Region, Surface, the element types, the cap, and the one-based/zero-based door"
      contains: "Region"
      min_lines: 90
    - path: "src/lib/sandbox/emit.ts"
      provides: "The region table G, the 81-entry cell map M, the callback, and dead-branch elimination"
      contains: "dead"
    - path: "src/lib/sandbox/cost.ts"
      provides: "The measured cost of an emitted surface under the pinned minifier at the picker corner"
  key_links:
    - from: "src/lib/sandbox/emit.ts"
      to: "src/lib/catalog/library.ts"
      via: "the emitted callback calls the Phase 12 library's functions by their final names, read from 12-07-SUMMARY"
      pattern: "TOUCH_LIBRARY"
    - from: "src/lib/sandbox/geometry.ts"
      to: "src/lib/sandbox/emit.ts"
      via: "the cell-to-region map is the same structure that enforces non-overlap, built once"
      pattern: "cellMap"
---

<objective>
The Sandbox's data half: a region model whose rules are unrepresentable to break, and an emitter whose
cost is measured rather than hoped.

**This is the first wave that cannot start before Phase 12 closes**, and the reason is not scheduling:
the emitted callback calls Phase 12's touch library by name. **A revision of the Phase 12 plans is in
flight and will change 12-07's `Q` (same-id expiry) and its callers.** The function set `Q A E X F D`
is stable in outline, `F` is dropped, and `Q`'s signature and semantics are **not final**. So the rule
for this plan and the three after it: **read `12-07-SUMMARY.md` at execution time and consume the
library's final shape from it - never from `12-07-PLAN.md`, never from `13-RESEARCH.md`.**

**The measured facts this plan is built on** (`13-RESEARCH.md` §3.1, produced in this tree with the
pinned `compressScript`):

1. **The naive compiler is dead.** A generic four-type dispatcher costs **1,067 with one element on the
   surface** and **1,166 with four** - over a whole event's budget before anything is placed. Any plan
   that assumes "emit a block per element" is planning something that cannot be installed.
2. **Dead-branch elimination is worth about 470 characters.** Emitting only the branches a surface
   actually uses takes the four-fader case from 1,166 to **697**. It is the cheapest large win and it
   needs no firmware question at all.
3. **The split is what makes the PDF's own surface possible**, and **the probe already proved the slot
   it needs**: the runtime lives in the **touch Timer** - whose body the touch Setup can call, and whose
   globals persist, observed on the user's ZONA at `493a21a` - and the per-surface data table lives in
   the touch Setup (**366** at four elements, **811** at sixteen). The runtime is 13-15's; **this plan
   builds the data half**, which is the same under every answer 13-02 recorded.

**Overlap is not discouraged, it is unrepresentable.** §8 says default to preventing overlaps. With an
81-entry **cell-to-region map** `M`, each cell holds **one** region index - so an overlapping surface
cannot be expressed, let alone emitted. `M` costs 165 characters and replaces a search loop that costs
more than that at four elements and much more at sixteen. Layered hit-testing (§8's conditional) would
need `M` to hold a list, and that cost is the reason not to.

**The boundary matters at onset and only at onset.** Probe A Q2 measured the cell boundary as **one unit
wide** - a still finger at x=71/72 flips cells. For a *region* it matters much less, because a contact
**keeps the region it landed in** for the whole gesture; it matters at the **press**, where a finger on
the seam between two regions is a coin flip. Phase 12's hysteresis cannot help on a first sample (there
is no prior cell). **So the mitigation is in the editor, not the runtime**: warn when two regions are
edge-adjacent with no gap, which is §8's *"explain the conflict at the affected region"* applied one
step earlier.

**One-based in the interface, zero-based in the model.** The PDF's inspector shows `Column 1, Row 1` for
the top-left cell. HANGAR is zero-based everywhere (`glag(0, n)`, `n = row*9 + col`). That translation
is a real interface and it gets a **named door**, the way `knobPosition` and `colourPosition` already
are - not four inline `- 1`s in four components.

Purpose: the Sandbox's rules, expressed as data structures, and its cost, expressed as measurements.
Output: five modules, nine tests, five measured costs. Term `+2 / +9`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-gui-overhaul/13-VALIDATION.md
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-RESEARCH.md
@.planning/phases/13-gui-overhaul/13-02-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-12-SUMMARY.md
@.planning/phases/12-touch-framework/12-07-SUMMARY.md
@.planning/phases/12-touch-framework/12-12-SUMMARY.md
@.planning/phases/12-touch-framework/PROBE-RESULTS-2026-09-10.md
@src/lib/catalog/library.ts
@src/lib/catalog/lua-entries.sweep.spec.ts
@src/lib/tune/view.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Every claim here is measured under the
  pinned minifier or run in wasmoon; nothing is claimed to work on hardware.
- **`src/vendor/` is not touched.** **Never touch a sibling repository.**
- **Do not edit `.planning/ROADMAP.md`.**
- **`gsd-tools state advance-plan` and `record-session` destroy lines.** Copy `STATE.md` first, repair
  by inverse edit, skip `roadmap update-plan-progress`.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- Commit with `git commit --only <paths> -F <message-file>`. Do not push. Free services only.
- **Where this plan is not 100% sure, it asks (D-01).**
- **State every count as a carried name plus a delta.** This plan carries `PREV_FILES 89` /
  `PREV_TESTS 910` from **13-12** (which now runs at wave 13, immediately before this one) and asserts
  **`+2 / +9`**, written out.
- **Every Lua string measured with `GridScript.compressScript` after `initLuaFormatter()`;
  `cost = max(compressed.length, source.length)`; measured at the RGB444 picker corner**, which for a
  surface means **every region at its longest colour literal**.
- **Both Phase 11 gates apply to every string this emitter produces**: `e>=5` needs `and e<9`; onset is
  `(e==4 or e>8)`. **Phase 12's library rules apply too**: hysteresis margin 3, expiry on both paths,
  nothing built on code 9. The emitter's own tests run both class gates over its output.
- **The touch library's final shape comes from `12-07-SUMMARY.md`.** If that SUMMARY does not exist yet
  or records a different function set than this plan assumes, **stop and say so** rather than emitting
  a call to a function that may have been dropped.
- **The sweep's member list stays `4 19`.** The Sandbox's cost gate is a **server** spec, by 12-07's
  own precedent and for its own reason.
- **Any user-facing string this plan invents lands in `13-COPY-NEW.md`.** §16 gives the overlap line
  (*"This region overlaps Filter. Choose another area or resize it."*) verbatim; the off-surface, the
  edge-adjacency warning and the cap message are HANGAR's.
</standing_rules>

<interfaces>

## The model

```ts
type ElementKind = "fader-v" | "fader-h" | "button" | "xy" | "knob";
type Region = {
  id: string; name: string; kind: ElementKind;
  col: number; row: number; w: number; h: number;   // ZERO-based, 0..8
  cc: number; cc2?: number; channel: number;         // 1..16 in the UI, 0..15 on the wire
  colour: [number, number, number];                  // RGB444
  latch?: boolean;                                   // button only
};
type Surface = { id: string; name: string; regions: Region[]; schema: 1 };
```

**The cap is sixteen** (D-14 Q4) with the live budget meter, because *"you have room for four more"* is
a better interface than a hard refusal - and the meter already exists.

**The named door:**

```ts
export const toDisplay = (zeroBased: number) => zeroBased + 1;
export const fromDisplay = (oneBased: number) => oneBased - 1;
```

Two functions, one header paragraph, and **every component uses them**. The header cites
`knobPosition` and `colourPosition` as the precedent.

## The emitted shapes

```
G = { {x0,x1,y0,y1,type,cc,cc2,ch,r,g,b}, ... }   the region table, one row per region
M = { ...81 numbers... }                          cell -> region index, 0 for none
```

**`x0..y1` are precomputed in raw 0..127 units**, not derived from cells at runtime: a fader's value is
`127-(y-y0)*127//(y1-y0)` and the compiler knows `y0` and `y1` exactly. This is the single largest
runtime saving after dead-branch elimination.

Measured, from the research: `M` alone is **165**, a four-row `G` is **141**, and the full touch Setup
with the callback is **366** at four elements and **811** at sixteen.

**Dead-branch elimination** is a property of the emitter: the branch text for a kind is emitted only if
some region uses it. **Prove it with a measured pair** - four vertical faders with and without the
other three branches - and record both numbers. The research's pair is 697 against 1,166; **re-measure
rather than quoting it.**

## The slot picture, as the answered probe leaves it

```
system Setup   Phase 12's touch library                          (unchanged, read from 12-07-SUMMARY)
touch  Timer   the Sandbox runtime Z  +  Phase 12's sweep call   PROVED reachable from Setup
touch  Setup   s.tim(s) + G + M + Y() + the callback             this plan
system mapmode a third 908, ONLY if 13-02's checkpoint asked for its bench row
```

**This plan emits the touch Setup half and it is the same under every answer**: `G`, `M`, `Y()`, the
callback, and the eleven-character call that pulls the runtime in. What 13-02's answer changes is
**how many element kinds one surface may mix** - the runtime's ceiling, which is 13-15's - and, if the
third slot was taken, where the runtime sits. **Read `SLOT-ARITHMETIC.md` and `13-02-SUMMARY.md` for
the recorded ceiling and say which one this plan emitted against.**

**The inline fallback is retained as a contingency and not as a branch**: if the emitted Setup plus an
inlined two-kind runtime measures smaller than the split for a small surface, record the pair - the
research measured four vertical faders inline at **697** - and say which the emitter chose and why.

## The geometry rules, all six

1. **On the surface**: `col + w <= 9`, `row + h <= 9`, `col >= 0`, `row >= 0`, `w >= 1`, `h >= 1`.
2. **No overlap**: the cell map is built by writing each region's cells; a cell written twice is a
   conflict, reported with **both** region names.
3. **A minimum size per kind**: a Knob needs a centre dead zone, so a 1x1 or 2x2 knob is refused -
   **13-15 derives the exact rule from the arithmetic** and this plan takes it as a parameter rather
   than inventing it.
4. **Duplicate to a free area**: search for a free `w x h` window in the cell map, in reading order;
   if none exists, **offer resize and never silently delete** (§8's own words).
5. **The previous valid value survives an invalid edit** (§8), with an inline message until corrected.
6. **Edge-adjacency warning**: two regions sharing a boundary with no gap raise a warning - not an
   error - naming both, because Probe A Q2's one-unit boundary makes a press on the seam a coin flip.

## The cap and the meter

Sixteen regions maximum, **and the meter is the real gate**: a surface of sixteen tiny faders fits, a
surface of eight with long colour literals might not. `cost.ts` answers *"this surface costs N of 908,
and you have room for about M more elements"*, where M is derived by adding a representative region and
re-measuring - **not** by dividing.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13-14-01: The region model, the named door, and six geometry rules the data structure enforces</name>
  <files>src/lib/sandbox/model.ts, src/lib/sandbox/geometry.ts, src/lib/sandbox/geometry.spec.ts, src/lib/store/schema.ts, .planning/phases/13-gui-overhaul/13-COPY-NEW.md</files>

  <read_first>
    - `13-RESEARCH.md` §3.1 "Overlap, snapping, off-surface" in full
    - `PROBE-RESULTS-2026-09-10.md` Q2 (the one-unit boundary) and Q6.5 (the lost lift)
    - `src/lib/tune/view.ts`'s `knobPosition` and `colourPosition` - **the named-door precedent**
    - `bible/HANGAR-ZONA-GUI-design-specification.md` §8 in full
    - `src/lib/store/schema.ts` - where a `Surface` record's shape belongs
  </read_first>

  <action>
**1. `model.ts`** per the interfaces block, with the cap, the element kinds and **the named door** with
its precedent cited in the header.

**2. `geometry.ts`**: `buildCellMap(regions)` returning the 81-entry map **or** a conflict naming both
regions; `validate(region, surface)` applying rules 1, 3 and 5; `freeWindow(w, h, map)` for rule 4; and
`adjacencyWarnings(regions)` for rule 6. **The cell map is built once and is both the validator and the
emitter's input** - two structures would drift.

**3. `geometry.spec.ts`, four tests:**
   1. **off-surface**: every edge case of rule 1 - a 2x6 at row 4, a 9x1 at column 1, a zero width -
      refused with the offending field named; and the **previous valid value survives**;
   2. **overlap is unrepresentable**: two regions sharing one cell produce a conflict naming both, and
      the map is never returned half-built;
   3. **duplicate**: a free `w x h` window is found in reading order; a full surface returns "no space"
      and **nothing is deleted** - assert the original region list is byte-identical after a failed
      duplicate;
   4. **adjacency**: two regions with a shared boundary warn and name both; the same two with one cell
      of gap do not; **and a warning never blocks** - assert the surface still validates.

**4. `schema.ts`** gains the `Surface` record shape with `schema: 1` in the body, per 13-06's rule.

**Ledger** the off-surface message, the edge-adjacency warning and the cap message. §16's overlap line
is verbatim and is not ledgered.

**Negative checks:** allow a cell to be written twice - expect test 2 red; return a half-built map on
conflict - expect test 2's second clause red; delete on a failed duplicate - expect test 3 red.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sandbox/geometry.spec.ts</automated>
    Four green; the cell map proved to be the single structure behind both the validator and the
    emitter.
  </verify>

  <done>
The region model exists with the sixteen cap and a named one-based/zero-based door citing its
precedent; the cell map is built once and enforces non-overlap by construction; six geometry rules land
with four tests including a failed duplicate that deletes nothing and a warning that never blocks; the
`Surface` shape carries its schema in the body; three strings ledgered.
  </done>
</task>

<task type="auto">
  <name>Task 13-14-02: The emitter, dead-branch elimination proved by a measured pair, and five costs at the picker corner</name>
  <files>src/lib/sandbox/emit.ts, src/lib/sandbox/emit.spec.ts, src/lib/sandbox/cost.ts</files>

  <read_first>
    - **`12-07-SUMMARY.md`** - the library's final function set, its measured canonical cost, and
      **whether `Q`'s signature moved in the revision that was in flight**. If the SUMMARY is absent or
      disagrees with this plan, **stop and report**
    - `12-12-SUMMARY.md` - the phase's closing library row and its free characters
    - `src/lib/catalog/lua-entries.sweep.spec.ts` test 1 - **how a string is measured and canonicalised
      in this tree**; reuse it exactly
    - `src/lib/catalog/touch-guard.spec.ts` and `decay-idiom.spec.ts` - the two class gates whose
      needles run over the emitted string
    - `SLOT-ARITHMETIC.md` and `13-02-SUMMARY.md` - **the recorded ceiling in element kinds**, and
      whether the user bought a third slot
  </read_first>

  <action>
**1. `emit.ts`**: `G`, `M`, the callback, and the eleven-character call that pulls the runtime in from
the slot 13-02 recorded. **Dead-branch elimination is a parameter of the emit, not a post-processing
step**, and it applies to the runtime as well as to the data because HANGAR emits both per surface. Numbers are emitted at their
exact width; colours at the picker corner are the longest literals and the cost function uses them.

**2. `cost.ts`**: measure with `initLuaFormatter()` then `compressScript`, `max(compressed, source)`,
**canonicalised** (run the minifier's output back through itself until it is a fixed point) exactly as
`lua-entries.sweep.spec.ts` test 1 does. Answer both *"N of 908"* and *"room for about M more"*, where M
is found by **adding a representative region and re-measuring**, not by division.

**3. `emit.spec.ts`, five tests:**
   1. **the five costs**: 1, 4, 8, 12 and 16 elements at the picker corner, each printed, each under
      908, with the research's figures (366 at four, 811 at sixteen) printed **beside** them and any
      difference named as a research correction;
   2. **dead-branch elimination**: four vertical faders emitted with and without the other three
      branches, both measured, the saving printed, and the research's 697/1,166 pair printed beside it;
   3. **the map**: `M` has exactly 81 entries, every one either 0 or a valid region index, and it agrees
      with `geometry.ts`'s map cell for cell;
   4. **both class gates over the emitted string**: every `e>=5` followed by `and e<9`, every onset
      `(e==4 or e>8)`, and the decay needles where a decay is emitted - **run the gates' own needles,
      do not re-write them**;
   5. **`checkSyntax` is true** for every one of the five surfaces, and the emitted callback names only
      functions the library actually exports **as read from `12-07-SUMMARY.md`** - assert the set
      equals, not contains.

**4. If any of the five costs exceeds 908**, that is a **finding, not a failure**: record it with the
element count it broke at, and say what the cap becomes. D-14 Q4's sixteen is a target the measurement
either confirms or corrects.

**Negative checks:** emit all four branches for a fader-only surface - expect test 2's saving to
collapse and the assertion to name it; break one region's `y1` precomputation so the runtime would have
to derive it - expect test 1's cost to move and the assertion to catch it; call a library function that
`12-07-SUMMARY` does not list - expect test 5 red.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sandbox/ && npm run test:quick 2>&1 | node scripts/check-counts.mjs 91 919</automated>
    `geometry.spec.ts` 4 + `emit.spec.ts` 5 = **9**; all five costs printed with the research's figures
    beside them; the sweep **unmoved at `4 19`**.
  </verify>

  <done>
The emitter produces `G`, `M` and the callback with dead-branch elimination as a parameter and every
number at its exact width; the cost function measures canonically at the picker corner and answers "room
for about M more" by measurement rather than division; five costs recorded beside the research's; both
Phase 11 class gates run over the emitted string with the gates' own needles; the library's function
set asserted as an equality against `12-07-SUMMARY.md`; any cost over 908 recorded as a finding with the
cap corrected.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs 91 919` - `PREV_FILES + 2` /
  `PREV_TESTS + 9`. **This plan's term is `+2 / +9`, written out.**
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - **unchanged by choice**, and the
  SUMMARY says the cost gate is a server spec for 12-07's own reason.
- `grep -c "test(" e2e/*.e2e.ts | awk -F: '{s+=$2} END {print s}'` at 13-13's number - e2e `+0 / +0`.
- `git diff --stat HEAD -- src/vendor/` unchanged; `git diff --quiet -- .planning/ROADMAP.md`;
  `git diff --quiet -- src/lib/catalog/library.ts` - **Phase 12's library is read, never edited.**
</verification>

<success_criteria>
1. Overlap and off-surface are unrepresentable rather than merely validated, through one cell map that
   is both the validator and the emitter's input.
2. The one-based/zero-based translation has a named door with its precedent cited, and no component
   does the arithmetic itself.
3. Five costs are measured canonically at the picker corner and printed beside the research's figures,
   with every difference named as a research correction.
4. Dead-branch elimination is proved by a measured pair, not asserted.
5. Both Phase 11 class gates run over the emitted string using the gates' own needles, and the library's
   function set is asserted as an equality against `12-07-SUMMARY.md`.
6. The emitter is one implementation with a parameter, so the recorded ceiling changes what is emitted
   and not how - and the ceiling used is recorded with the document it came from.
</success_criteria>

<output>
After completion, create `.planning/phases/13-gui-overhaul/13-14-SUMMARY.md` carrying: the carried
counts plus `+2 / +9`; **the recorded ceiling in element kinds and the document it came from**; the five measured
costs beside the research's, with every correction named; the dead-branch pair; `M`'s and `G`'s measured
sizes; the library function set as read from `12-07-SUMMARY.md` and asserted; whether any cost exceeded
908 and what the cap became; the three ledgered strings; and the sentence that nothing was installed.
</output>

## Phase 12.1 hand-off (2026-09-11)

Appended 2026-09-11 under the orchestrator's permission after the Phase 12.1 plan-check found that
`12.1-VALIDATION.md`'s hand-offs had no reader here. **Append-only: no line above this heading was
edited or reflowed.** Phase 12.1 (gradient touch, nine plans, `.planning/phases/12.1-gradient-touch/`)
lands its Band 1 concurrently with 13-08..13-13 and its Band 2 between 13-12 and 13-14; this plan must not start until 12.1-08 has landed, and reads `12.1-02-SUMMARY.md` and `12.1-07-SUMMARY.md` before task 01, beside `12-07-SUMMARY.md`. The full
text is `12.1-VALIDATION.md` "The hand-offs".

- **The function set is no longer 12-07's six in one string.** It is `U W E Q X` in the system Setup
  (255/0, exported `TOUCH_LIBRARY`, 782) and `V G N A D` in the system Timer (255/6, exported
  `TOUCH_LIBRARY_TIMER`, 714). `F` is still gone; `R` is still the convention; `LIBRARY_GLOBALS` is
  derived from both strings (eighteen names; `KX` and `KY` are two capitals).
- **Task 02's test 5** asserts the emitted callback's function set **equals** the library's export list:
  read that list from `12.1-02-SUMMARY.md`, not `12-07-SUMMARY.md`; the callback names at most `G`, `N`,
  `U`, `E`, `X`, `R`.
- **The cell is `N(x,y)`**, the nearest calibrated cell through the measured sensor map, never
  `x*9//128`; the emitted `M` lookup is `M[N(x,y)]`.
- `git diff --quiet -- src/lib/catalog/library.ts` during this plan's run holds because 12.1 Band 2
  precedes this plan; the file it asserts unchanged is 12.1's shape.
