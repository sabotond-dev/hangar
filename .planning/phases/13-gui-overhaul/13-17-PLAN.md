---
phase: 13-gui-overhaul
plan: 17
type: execute
wave: 17
depends_on: ["13-16"]
files_modified:
  - src/lib/sandbox/land.ts
  - src/lib/device/install.spec.ts
  - src/lib/device/install.svelte.ts
  - src/lib/store/transfer.ts
  - src/lib/ui/sandbox/SurfaceActions.svelte
  - e2e/sandbox.e2e.ts
  - docs/INSTALL-RUNBOOK.md
  - .planning/phases/13-gui-overhaul/13-COPY-NEW.md
autonomous: true
requirements: [BUILD-03, BUILD-05, SAFE-01, SAFE-02, SAFE-03, SAFE-07, SAFE-09, KEEP-04, SHARE-01]

must_haves:
  truths:
    - "A Sandbox surface installs through the SAME one writer, the same snapshot, the same acknowledgements and the same phases as a catalog configuration - there is no second write path, and a test asserts that there is not."
    - "The surface lands as the same three strings a hand-authored entry lands: the system slot, the touch Setup and the touch Timer, in the order 12-03 established, with the classifier naming which of three landed."
    - "An over-budget surface disables Apply before the click, names what pushed it over, and never reaches the wire to fail there - which is TUNE-05's rule applied to a new producer."
    - "A surface shares as a file and not as a link, and the stamp codec gains no format letter - the decision is D-14 Q7 and the codec is proved untouched."
    - "Nothing about the install is claimed to work on hardware: the bench row is written and handed over, and the phase's own gate is where it is answered."
  artifacts:
    - path: "src/lib/sandbox/land.ts"
      provides: "The surface's landing: three strings, measured, in the same shape the tuner publishes for an entry"
      contains: "system"
  key_links:
    - from: "src/lib/sandbox/land.ts"
      to: "src/lib/transport/sequence.ts"
      via: "writeBoth, storeToFlash and writeBack unchanged; the surface is a third producer of the same pair"
      pattern: "writeBoth"
    - from: "src/lib/sandbox/land.ts"
      to: "src/lib/tune/model.ts"
      via: "the landing shape is the tuner's own, so the install store cannot tell a surface from an entry"
      pattern: "landLua"
---

<objective>
The loop closed: a surface the visitor built reaches the ZONA through machinery that has never been
changed for it.

**This is what D-03 bought.** The mockup had no firmware execution and §17's first-release list does not
mention transfer at all; the user chose the full loop anyway, because *"a Sandbox that cannot reach the
device would be a mockup on the live site, and HANGAR's core value is that install must work."* Waves 14
through 16 built the compiler, the runtime and the editor. **This wave is the last twenty lines**, and
it is the wave with the most opportunity to do something quietly wrong.

**The rule, stated as the plan's first constraint: no second write path exists.** `sequence.ts`'s
`writeBoth`, `writeBack`, `storeToFlash`, `fetchBoth`, `targetOf` and `WriteTarget` do not change. The
install store's fifteen phases, four actions, two legs and its classifier do not change. **The Sandbox
is a third producer of exactly the shape the tuner already publishes** - the first is the preset
landing, the second is `landLua`, and this is the third. If a field must be added to make that true,
**it is added to the shape and not to the writer**, and the SUMMARY says which.

**What lands, and in what order** (12-03's three-string install): the **system** slot, the touch
**Setup**, the touch **Timer**. Under 13-02's `yes` branch the runtime occupies a second system slot and
the touch Setup carries `G`, `M` and the callback; under `no` the runtime is inlined into the touch
Setup and the system slot still carries Phase 12's library, because the preview and the entries need it.
**Either way it is three strings through one writer**, and the classifier that names which of the three
landed - SAFE-07's own machinery, widened by 12-03 - is unchanged.

**`TUNE-05` applies to a producer it has never seen.** An over-budget surface must disable Apply
**before** the click, turn the meter red, name what pushed it over, and never reach the wire. For an
entry that is a knob; for a surface it is **an element, a colour or a name** - and the message says
which. That is a new sentence in an old rule, and it is ledgered.

**Share is a file, not a link** (D-14 Q7). A surface has no catalog entry, and the stamp's payload is a
vector of knob indices validated against an entry's own declaration. A fixed sixteen-region envelope
would be **roughly 420 characters of hash**, which is not a share. §11 requires export/import anyway and
13-13 built it. **The codec gains no letter and is proved untouched**; `y` and `z` stay reserved.

Purpose: the surface reaches the hardware through the safest path in the project, and nothing about
that path was loosened to let it.
Output: one landing module, one actions component, three tests, one e2e title, one bench row. Term
`+0 / +3`, e2e `+1 / +1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-gui-overhaul/13-VALIDATION.md
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-RESEARCH.md
@.planning/phases/13-gui-overhaul/13-16-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-14-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-15-SUMMARY.md
@.planning/phases/12-touch-framework/12-03-SUMMARY.md
@src/lib/transport/sequence.ts
@src/lib/device/install.svelte.ts
@src/lib/device/install.spec.ts
@src/lib/tune/model.ts
@src/lib/share/stamp.ts
@src/lib/store/transfer.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.**
- **No agent connects to, writes to, or deploys to a device.** Every proof here is against the fake
  ZONA; **the install is a bench row and this plan does not claim it works.**
- **`src/vendor/` is not touched.** **Never touch a sibling repository.**
- **Do not edit `.planning/ROADMAP.md`.**
- **`gsd-tools state advance-plan` and `record-session` destroy lines.** Copy `STATE.md` first, repair
  by inverse edit, skip `roadmap update-plan-progress`.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- Commit with `git commit --only <paths> -F <message-file>`. Do not push. Free services only.
- **Where this plan is not 100% sure, it asks (D-01).**
- **State every count as a carried name plus a delta.** This plan carries `PREV_FILES 94` /
  `PREV_TESTS 941` from 13-16 and asserts **`+0 / +3`** and e2e **`+1 / +1`**, written out.
- **`src/lib/transport/sequence.ts` is not edited.** If it must be, **stop and ask**: it is the one
  writer, it is the reason `SAFE-01` through `SAFE-09` hold, and a change to it invalidates four
  requirement qualifiers at once.
- **The stamp codec is not edited and gains no format letter.**
- **Every string measured at the RGB444 picker corner** - for a surface, every region at its longest
  colour literal - under the pinned minifier, canonicalised.
- **Any user-facing string this plan invents lands in `13-COPY-NEW.md`.** `Apply to ZONA` and
  `Store on ZONA` are §9's and are verbatim; the over-budget message for a surface, the export's
  success line and the surface-has-no-link explanation are HANGAR's.
</standing_rules>

<interfaces>

## The landing shape, and why it is the tuner's

`model.ts` publishes a landing that the install store consumes: the three strings plus whatever metadata
the store needs. `landLua` publishes Phase 12's library as the system string; the preset landing
publishes the firmware default. **The Sandbox publishes a third variant** - and the install store
**must not be able to tell them apart**. Assert that: build a landing from a surface and one from an
entry, and assert the store's own consumption path is identical.

**If the store needs to know** - for a caption, for a name in a confirmation - it takes a **label** in
the landing, not a branch on a kind. A branch on a kind in the install store is the beginning of a
second write path.

## The three strings under each branch

| Slot | 13-02 = yes | 13-02 = no |
|---|---|---|
| system setup | Phase 12's library (unchanged) | Phase 12's library (unchanged) |
| system mapmode | **the Sandbox runtime** | not written |
| touch Setup | `ele[1]:map()` + `G` + `M` + the callback | the runtime **inlined** + `G` + `M` + the callback |
| touch Timer | the expiry sweep call | the expiry sweep call |

**Under `yes` there are four strings, not three.** That is a real change to the install shape 12-03
established, and it is **not** a change this plan may make silently: if the writer would have to send a
fourth frame, **stop and ask**, because `SAFE-07`'s classifier reads three step ids and `install.e2e.ts`
enumerates them. Record the arithmetic either way. **Under `no` it is three and nothing moves** - which
is one more reason the fallback branch is not a consolation prize.

## The over-budget rule, restated for a surface

| Entry | Surface |
|---|---|
| a knob pushed it over | **an element, a colour, or a name** pushed it over |
| the offending meter turns red | the same |
| Apply is disabled before the click | the same |
| one-click back-off | **remove the last element**, or shorten the name - say which the message offers |

The message names the cause. **Ledger it.**

## Share, and what is proved rather than argued

- **Playground share: unchanged.** `/c/<id>#z.<stamp>` (or whatever 13-08's answer made the address),
  three landings, the codec untouched. Assert `stamp.ts` and its spec and sweep are byte-identical.
- **Surface share: a file**, through 13-13's `transfer.ts`. The button says so and the explanation
  says why in one clause - it is a surface, not a variation of a catalog entry, and there is nothing for
  a link to point at.
- **`y` and `z` stay reserved.** Say so, so a later phase knows they were left deliberately.

## The bench row

Appended to `docs/INSTALL-RUNBOOK.md`, unanswered:

> Build the mockup's own surface - a 2x6 Fader, an XY pad, a Button and a Knob - or, on the fallback
> branch, four faders and a button. Apply to ZONA. Report: does each region respond only within its own
> rectangle; does a finger dragged off a fader's end keep driving that fader; does the button's note
> stop when you lift, **and when you lift badly**; does the pad light where your finger is; and does
> PUT BACK bring your own configuration back.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13-17-01: The surface lands through the one writer, and a test asserts there is no second path</name>
  <files>src/lib/sandbox/land.ts, src/lib/device/install.spec.ts, src/lib/device/install.svelte.ts, src/lib/ui/sandbox/SurfaceActions.svelte, docs/INSTALL-RUNBOOK.md, .planning/phases/13-gui-overhaul/13-COPY-NEW.md</files>

  <read_first>
    - `src/lib/tune/model.ts` - **`landLua` and the preset landing**, the two existing producers whose
      shape this joins
    - `src/lib/transport/sequence.ts` - `writeBoth`, `writeBack`, `storeToFlash`, `targetOf`; **read,
      not edited**
    - `12-03-SUMMARY.md` - the three-write shape, the classifier over three step ids, and the
      impossible partial it names
    - `src/lib/device/install.spec.ts` - the leg ordering and the three-write order test
    - `13-14-SUMMARY.md` and `13-15-SUMMARY.md` - the branch taken and the runtime's slot
  </read_first>

  <action>
**1. `land.ts`** produces the landing in the tuner's own shape, measured and canonicalised, with the
label rather than a kind.

**2. Check the frame count first.** Under 13-02's `yes` branch the surface may need a **fourth** string.
**Count the frames the writer would send before writing any code.** If it is four, **stop and ask**:
the classifier, the e2e enumeration and `SAFE-07`'s amended text all describe three. Record the
arithmetic, the question and the answer. If it is three, say so and continue.

**3. `SurfaceActions.svelte`** - `Apply to ZONA` and `Store on ZONA` wired to the **existing** install
store, with the destination review from 13-12 and the page target from the same plan. **No new
confirmation, no new phase, no new action.**

**4. `install.spec.ts` +3:**
   1. **the shapes are indistinguishable**: a landing built from a surface and one from an entry are
      consumed identically by the store - assert the consumption path, not the payload;
   2. **the write order**: the same order 12-03 established, with the classifier naming which of three
      (or four) landed and the impossible partial still impossible;
   3. **over budget refuses before the wire**: a surface over 908 disables Apply, names the cause, and
      **zero frames are sent** - assert the frame count is zero, which is `TUNE-05`'s "never reaches the
      wire" clause as a number.

**5. The bench row**, unanswered.

**Negative checks:** branch on the kind inside the install store - expect test 1 red; send a frame while
over budget - expect test 3 red naming the count; reorder the three writes - expect test 2 red.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/device/ src/lib/sandbox/ && git diff --quiet -- src/lib/transport/sequence.ts</automated>
    `install.spec.ts` **+3**; `sequence.ts` **unedited**; every existing device spec at its previous
    count.
  </verify>

  <done>
A surface lands in the tuner's own shape through the one writer with no second path, proved by asserting
the store's consumption rather than the payload; the frame count was checked before any code was
written and a fourth string, if the branch needed one, was asked about rather than shipped; over budget
is proved by a frame count of zero; the bench row is written and unanswered; `sequence.ts` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 13-17-02: A surface shares as a file, the codec proved untouched, and one end-to-end title</name>
  <files>src/lib/store/transfer.ts, src/lib/ui/sandbox/SurfaceActions.svelte, e2e/sandbox.e2e.ts, .planning/phases/13-gui-overhaul/13-COPY-NEW.md</files>

  <read_first>
    - `src/lib/store/transfer.ts` as 13-13 left it - the export shape and the six validation steps
    - `src/lib/share/stamp.ts:110-130` - **`HANGAR_FORMAT_LETTERS` and the reserved `y` and `z`**, and
      the comment that says no letter is allocated there
    - `13-13-SUMMARY.md` - fork D's answer, because a surface export may or may not carry collection
      membership
    - `e2e/fake-zona.ts` - the fake as 13-12 extended it
  </read_first>

  <action>
**1. Export a surface** through `transfer.ts` unchanged - the `ExportFile` shape already covers
`kind: "sandbox"` and 13-13's validation already checks regions. **Assert that no new code path was
needed**; if one was, that is a 13-13 defect and this plan names it.

**2. The share control** says the surface exports as a file, and the one-clause explanation says why.
**Ledger both strings.**

**3. Prove the codec untouched**: `git diff --quiet` on `stamp.ts`, its spec and the sweep fixture, and
assert `HANGAR_FORMAT_LETTERS` still reads `["w","x","y","z"]` with `y` and `z` unclaimed. **Say in the
SUMMARY that they were left deliberately** and that D-14 Q7 is the reason.

**4. One e2e title** (chromium): build a two-element surface, export it, re-import it, and assert the
regions round-trip; then, against the fake ZONA, apply it and assert the acknowledged writes and the
resulting phase - **the whole loop, in one title, on a fake.**

**Negative checks:** allocate `y` for a surface - expect the codec's own assertion red, and if none
exists, **write one** rather than relying on the diff; corrupt one region in the exported file and
re-import - expect 13-13's validator to refuse with the region named.
  </action>

  <verify>
    <automated>npm run build && npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright 99</automated>
    The suite at **`99`** runs (recompute from 13-16's observation) with `grep -c "test("` at `83`;
    `stamp.ts` and its spec byte-identical.
  </verify>

  <done>
A surface exports and re-imports through 13-13's machinery with no new code path, or the defect is
named; the share control explains in one clause why there is no link; the stamp codec is proved
byte-identical with `y` and `z` deliberately unclaimed; one e2e title walks the whole loop against the
fake ZONA.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs 94 944` - `PREV_FILES + 0` /
  `PREV_TESTS + 3`. **This plan's term is `+0 / +3`, written out.**
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - unchanged.
- `npm run build`; `npx playwright test --workers 3` at `99` with `grep -c "test("` at `83`;
  **e2e `+1 / +1`, written out.**
- `git diff --quiet -- src/lib/transport/sequence.ts src/lib/share/stamp.ts src/lib/share/stamp.spec.ts`
  - the one writer and the codec both untouched.
- `git diff --stat HEAD -- src/vendor/` unchanged; `git diff --quiet -- .planning/ROADMAP.md`.
</verification>

<success_criteria>
1. A surface installs through the same writer, the same snapshot, the same acknowledgements and the
   same phases, and a test asserts the store cannot tell a surface from an entry.
2. The frame count was established **before** any code was written, and a fourth string - if the branch
   needed one - was asked about rather than shipped.
3. Over budget disables Apply before the click and sends **zero** frames, asserted as a count.
4. A surface shares as a file with a one-clause explanation, and the codec is proved byte-identical
   with `y` and `z` deliberately unclaimed.
5. One e2e title walks build, export, import and apply against the fake ZONA.
6. Nothing is claimed to work on hardware; the bench row is written and unanswered.
</success_criteria>

<output>
After completion, create `.planning/phases/13-gui-overhaul/13-17-SUMMARY.md` carrying: the carried
counts plus `+0 / +3` and e2e `+1 / +1`; **the frame count and whether a fourth string was needed**,
with the question and its answer if it was; the landing shape beside `landLua`'s and the preset
landing's; the three (or four) measured strings at the picker corner with their free characters; the
over-budget frame count; the export path and whether any new code was needed; the codec's proof; the
bench row as written; every ledgered string; and the sentence that no device was touched.
</output>
