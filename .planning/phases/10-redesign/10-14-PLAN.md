---
phase: 10-redesign
plan: 14
type: execute
wave: 14
depends_on: ["10-13"]
files_modified:
  - docs/TESTING.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-redesign/deferred-items.md
autonomous: false
requirements: [IDENT-01, IDENT-02, SAFE-01, SAFE-02, SAFE-03, SAFE-05, SAFE-07, PREV-03, CONN-03, CAT-02, CAT-03, CAT-04, CONT-03, TUNE-01, TUNE-02, TUNE-04, TUNE-05, SHARE-01, SHARE-03, DEGR-02]

must_haves:
  truths:
    - "Every projection this phase planned against has been replaced by an observation taken on this machine, on a clean tree, against a fresh production build."
    - "The phase total is written as a chain from the baseline the first plan measured, not as an absolute count somebody could reconcile two ways."
    - "Every requirement this phase extended, amended or closed has a qualifier saying exactly what was proved and exactly what was not."
    - "The three Open-for-the-user items are restated with their measured reversal costs, so the user is choosing between things that have numbers."
    - "Nothing under src/vendor moved, no agent touched a device, no agent deployed, and the one hardware row this phase created is handed over unanswered."
  artifacts:
    - path: "docs/TESTING.md"
      provides: "Every per-file count and every cost figure re-measured at the end of the phase, replacing this phase's projections"
    - path: ".planning/phases/10-redesign/deferred-items.md"
      provides: "Every deferred idea with what would close it: T3, T5, T6, the CRT-over-text mitigations, the front-door ring, and whatever the fourteen plans found"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirements: TBD replaced by the twenty owned IDs, each with its closure qualifier"
      pattern: "Requirements:"
---

<objective>
The phase gate: every number re-measured, every requirement given a qualifier, and the one hardware row
handed to the user.

Wave 14. Blocked on 10-13. Three tasks, the last of which is a checkpoint.

**What a gate is for in this repository.** Not a victory lap. Thirteen plans have each asserted a
carried name plus a delta; this plan is the only one that asserts against `BASE_FILES` and
`BASE_TESTS`, and its number is the phase total **written as a chain** so 10-01's baseline and 10-14's
total are visibly the same arithmetic seen from two ends.

**And it is where the projections die.** `10-VALIDATION.md` carries a delta table the planner
estimated. Every row of it is replaced here by an observation, and **any row that was wrong is named
as wrong** rather than quietly corrected — a validation document whose estimates were never checked
teaches the next phase to estimate carelessly.

**The three Open-for-the-user items** were each planned as recommended, with their reversals costed by
the plan that shipped them. This plan restates all three in one place with their numbers, so the user
is choosing between measured things rather than between descriptions.

Purpose: every requirement this phase touched given a qualifier; the phase closed honestly.
Output: one re-measured document, one deferred-items file, one ROADMAP row, one checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/10-redesign/10-01-SUMMARY.md
@.planning/phases/10-redesign/10-13-SUMMARY.md
@docs/TESTING.md
@docs/PIN-POLICY.md
@docs/INSTALL-RUNBOOK.md

<standing_rules>
- **Never edit anything under `src/vendor/`.** **Never touch a sibling repository.**
- **No agent connects to or writes to a device, and no agent deploys.** `npm run deploy` is not run.
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Prettier defaults. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- e2e at `--workers 3`; remove `test-results/` by hand and confirm `git status --porcelain` empty.
- **This plan asserts against `BASE_*`. Every other plan asserted against `PREV_*`.**
- The project spends no money.
</standing_rules>

<interfaces>
**The chain to write out, not the total alone.** From 10-01's `BASE_TESTS`, plan by plan:

```
BASE_TESTS
  +6 (10-01: aesthetic 1, font-assets 5)
  +1 (10-02: identity 6 -> 7)
  +0 (10-03)
  +6 (10-04: aesthetic 1 -> 7)
  +5 (10-05: demo 2, host +3)
  +4 (10-06: facets 4)
  -1 (10-07: sort 6 -> 5)
  +0 (10-08)
  +3 (10-09: tune-ui +2, surprise +1)
  +6 (10-10: colour-picker 6)
  +4 (10-11: mix.spec.ts created with 2, tune-ui +2)
  +3 (10-12: install +3)
  +3 (10-13: device-ui +3)
  = BASE_TESTS + 40
```

**Each term above is the sum of that plan's own tasks, re-derived from the per-file table in
`10-VALIDATION.md` rather than copied from the plan's `check-counts` line.** Revision 1 carried
`+4 (10-12: install +3, session +1)` and totalled 39; 10-12's own task 02 says twice, at `:310` and
`:332`, that `session.spec.ts` **does not move** — the tenth needle goes inside test 15 — and no other
file in that plan moves either. The `session +1` term is retired by name, the plan asserts
`PREV_TESTS+3`, and the term here is **+3**. (The phase total is `BASE_TESTS + 40`, stated once at `:107` and nowhere else in this paragraph - a total repeated in prose is how three of this phase's miscounts survived a revision.) A term here that disagrees with its plan's own
`check-counts` line is a defect in one of the two, and the gate is the place it surfaces: reconcile
before running, not after.

and `BASE_FILES + 6` (`aesthetic`, `font-assets`, `demo`, `facets`, `mix`, `colour-picker`), and
`BASE_E2E + 12`, written as four terms and not three:

```
BASE_E2E
  +8 (10-04: aesthetic.e2e.ts, 4 titles × 2 projects)
  +0 (10-05: gen-og and the dark exemption, no title moves — measured)
  +0 (10-07: D-11 and A-19, six sites, every edit inside an existing title)
  +4 (10-13: install.e2e.ts, 2 titles × 2 projects)
  = BASE_E2E + 12
```

**The 10-07 term is 0, not −2**, and the evidence is six sites rather than two:
`e2e/browse.e2e.ts:268` is a single title whose body walks four sorts, so D-11 deletes assertions
inside it; `:1029-1120` is a whole test built on the retired sort and on `filterListing`'s old arity
and is **restated inside its own title**; `:852-888`'s three written-address regexes, `:332-404`'s
inverted semantics and `:121`'s type annotation are all sub-title edits; `:437` is examined and left.
`browse-webkit.e2e.ts:79` **is** edited — it declares its own `orderOf` with `newest` in the literal
union, and revision 1's claim in this document that the file "never names `newest`" was false and is
retracted here, at `10-07:5b` and at `10-VALIDATION`'s 10-07 row. Editing it changes no assertion and
moves no title, so the term stays 0. 10-07 states the measurement; this gate carries it.
**These are the planner's arithmetic. Reconcile each against the SUMMARYs and against the observed
run, and where they differ, the observation wins and the difference is named.**

**The twenty requirements and their qualifiers**, each of which must say what was proved **and what
was not**:

| ID | Qualifier must state |
|---|---|
| IDENT-01 | nine tokens, three hexes, no fourth hue — through a font swap, two texture members outside the ladder and four CRT layers; `--font-display` asserted equal to **every** display `@font-face` family; **the Grifter licence question is the user's and is unresolved** |
| IDENT-02 | reduced motion asserted at the **compositor level** in both projects, with both vacuity holes opened first; a demo card replays once and freezes; `SCREEN: FLAT` reaches the two layers no motion preference can |
| SAFE-01 | zero writes by class across connect, snapshot and every knob move, in node and in a browser; **four** clicks, held in `WRITE_CLICKS`; the guarantee on the control in every state; **`REQUIREMENTS.md:169`'s eight-needle figure corrected to ten, having been stale at nine** |
| SAFE-02 | the two install weights asserted unchanged through D-04; CLEAR takes a fourth tier distinguished on five non-colour channels |
| SAFE-03 / SAFE-04 | CLEAR refuses without a snapshot with **zero writes of any class**; the snapshot record is read and never written; **the hardware half is row H and is unanswered** |
| SAFE-05 | a second confirmation naming what is removed and what a power cycle brings back; **no rig sentence, because CLEAR does not store** |
| SAFE-07 | `cleared` from two ACKs, never from a resolved writer promise, proved against scripted faults; **no hardware half** |
| PREV-03 | **closed** at 44 characters, unconditional wherever it appears |
| CONN-03 | amended: the pre-click explanation retired, `SAFE_NOTE` and the browser's own prompt carrying the intent |
| CAT-02 | three sorts become two; `addedAt` out of the projection and kept on the entry |
| CAT-03 / CAT-04 | sixteen terms, two facets, exactly three per entry, OR within and AND across; the count-derived row and the outsider chip retired; `disabledTags()` kept |
| CONT-03 | "four feel tags" amended to exactly three, re-cut across twenty-eight source files |
| TUNE-01 | knobs counted as knobs; one picker per panel; the `colour` kind chosen by kind alone |
| TUNE-02 | the forecast is `cost()` only, memoised, hover-and-focus, never touch, never animated |
| TUNE-04 | locks and `MIX TWO`, both ephemeral, zero new reachable states |
| TUNE-05 | **stays proven-unreachable, with the margin as a number**: `ninepads` 640 of 908, 268 free; `tpad` 907 of 908 with no colour knob; zero of Pass B's 24,576 states over the wall |
| SHARE-01 / SHARE-03 | format `w` claimed; **format `x` still landing `restored`, proved against literals captured before `w` existed** |
| DEGR-02 | CLEAR present-but-disabled with its reason inline, contrasted with `PUT BACK`'s absence |

**The three Open-for-the-user items, restated with their measured costs:**

1. **SAFE-01's form.** Shipped: `SAFE_NOTE` at 35 characters on the control. Form 2 (the disclosure) is
   one render site; form 3 (an explicit dated retirement) is one constant deleted and one
   `REQUIREMENTS.md` note. Quote 10-03's numbers.
2. **Grifter.** Shipped: D-14 as written. The A-02 reversal is **seven edits** — quote 10-02's
   numbered recipe verbatim — and **no component is touched and no test is rewritten**, which is what
   the one-token requirement bought. The unresolved half stays unresolved: `postbuild.mjs:82-89`
   publishes `git archive HEAD` at an unauthenticated URL, so a font file in the tracked tree is
   **redistributed**, which is a different permission from displaying it on your own site.
3. **The front-door ring.** Shipped: eight, plus the `FOR` link row. Growing it is blocked today by
   `front-door.spec.ts:110-112`'s `preview === "padsim"` requirement, which twenty-seven of
   thirty-six entries fail. Quote 10-07's deferred item.

**The deferred items to collect**, at minimum: T3 (`SURPRISE ME` as a shelf of five), **T5 (hardware
A/B audition — a *safety* risk, not a technical one: it adds a class of click that writes twice per
gesture and cannot be verified without hardware)**, T6 (knobs on the pad); the CRT-over-text
mitigations and the new gate class they need (Open item 8); the front-door ring; whether
`INSTALL-RUNBOOK.md` should have a gated row count; whichever gates 10-03 and 10-12 found narrower
than they read; and everything the fourteen SUMMARYs recorded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-14-01: Every projection replaced by an observation</name>
  <files>docs/TESTING.md, .planning/phases/10-redesign/deferred-items.md</files>

  <read_first>
    - all thirteen SUMMARYs in `.planning/phases/10-redesign/`
    - `10-VALIDATION.md`'s delta table, per-file table and cost table
    - `docs/TESTING.md` in full
    - `docs/PIN-POLICY.md` item 4
  </read_first>

  <action>
**1. Re-measure everything, on a clean tree, in this order**, recording free memory beside each:

```
npm run check 2>&1 | grep -Ei "error|warning"
npm run lint
npm run test:quick   2>&1 | node scripts/check-counts.mjs <observed> <observed>
npm run test:sweep   2>&1 | node scripts/check-counts.mjs 4 19
npm run build
npm run test:quick   (again - three specs read build/)
npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright <observed>
```

Then `rm -r test-results` by hand and confirm `git status --porcelain` empty.

**2. Replace `10-VALIDATION.md`'s three projected tables with observations, in `docs/TESTING.md`:**

- **per-file counts** for every file this phase touched, plus the five created;
- **the cost table**: the three sweep wall clocks against `BASE_SWEEP_WALL`, the build time against
  09's 12 s at sixteen entries and 09-VERIFICATION's figure at thirty-six, the e2e wall time and free
  memory, and `static/og/`'s total against 210,926;
- **the delta chain** from `<interfaces>`, reconciled row by row. **Name every row the planner got
  wrong** — a validation document whose estimates were never checked teaches the next phase to
  estimate carelessly.

**3. `docs/PIN-POLICY.md` item 4's stake.** It already records that canonical compressed form is a
property of a specific minifier version. Add one sentence: the pin now also decides whether the 4,096
lattice literals fit, so a protocol bump is gated by `reachability.sweep.spec.ts`'s two passes as well
as by the twenty-seven stored strings.

**4. `deferred-items.md`.** Every item from `<interfaces>` plus everything the thirteen SUMMARYs
recorded, each with **what would close it** — not a wish list, a work list. T5 carries its safety
reason verbatim.

**Negative check:** raise one non-vacuity floor by one (`facets.spec.ts`'s, or `aesthetic.spec.ts`'s
walk floor). Expect its own test red, naming the observed count. Revert; confirm `git diff --quiet`.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -Ei "error|warning" ; npm run lint && npm run test:quick && npm run test:sweep && npm run build</automated>
    Every gate green, every number recorded, `test-results/` removed, tree clean.
  </verify>

  <done>
`docs/TESTING.md` carries observations where `10-VALIDATION.md` carried projections, with every wrong
estimate named. The delta chain is reconciled row by row. `PIN-POLICY.md` item 4's stake is raised by
one sentence. `deferred-items.md` exists with what would close each item.
  </done>
</task>

<task type="auto">
  <name>Task 10-14-02: The phase gate against a fresh production build, twenty qualifiers and six criteria</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `10-01-SUMMARY.md` — `BASE_FILES`, `BASE_TESTS`, `BASE_SWEEP`, `BASE_SWEEP_WALL`, `BASE_E2E`
    - `.planning/ROADMAP.md`'s Phase 10 block, including `**Requirements**: TBD` and `**Plans**: TBD`
    - `.planning/REQUIREMENTS.md`'s traceability table
  </read_first>

  <action>
**1. The gate, against a fresh production build**, with every arithmetic written out:

- `npm run test:quick` at **`BASE_FILES + 6` / `BASE_TESTS + 40`**, with the chain from
  `<interfaces>` written out plan by plan so the two ends visibly meet, **and each term checked
  against that plan’s own `check-counts` line** — a plan whose asserted delta and whose tasks
  disagree is a defect that must be named here rather than absorbed into the total;
- `npm run test:sweep` at `4 19` with the three wall clocks against `BASE_SWEEP_WALL`, and the four
  totals from 10-08 reproduced (19,502 / 24,576 / 44,078 / 234,784);
- `npx playwright test --workers 3` at **`BASE_E2E + 12`**, as the four-term chain in
  `<interfaces>`: aesthetic 4×2 (10-04), **10-05 explicitly 0**, **10-07 explicitly 0** (six sites,
  every edit inside an existing title, `browse-webkit.e2e.ts:79` included), install 2×2 (10-13);
- `npm run licenses` clean, `THIRD-PARTY.md` naming Inter and Grifter and not Quicksand,
  `licenses/` holding no Quicksand file;
- `npm run build` producing `build/source-<sha>.tar.gz`, and `tar -tzf` showing **no font binary** and
  **`static/fonts/README.md` present**;
- `src/lib/fidelity/vendored-diff.spec.ts` **14** and `lua-parity.spec.ts` **5** green;
- `git diff --stat HEAD -- src/vendor/` **empty**;
- `git diff --stat <phase-start-sha>..HEAD -- src/lib/ui/Coverflow.svelte` **empty** — the phase's
  standing promise, proved once at the end as well as fourteen times along the way.

**If any number disagrees with the chain, the observation wins**, and the disagreement is traced to
the plan that moved it rather than absorbed into the total.

**2. `ROADMAP.md`'s Phase 10 block.** Replace `**Requirements**: TBD` with the twenty owned IDs.
Replace `**Plans**: TBD` with the fourteen, each with a one-line objective. Mark the phase row
`14/14`. **Amend the Goal line by name**: it currently says "Grifter and Inter **Italic**", and D-12
withdrew the italic — rewrite it, dated, rather than silently correcting it.

**3. `REQUIREMENTS.md`'s traceability.** Write the twenty qualifiers from `<interfaces>`. Each says
what was proved **and what was not**. Three of them carry an explicit unresolved half — Grifter's
licence, row H, and SAFE-04's key which was already wire-unproven before this phase and still is.

**4. Walk ROADMAP's six Phase 10 success criteria, one at a time, the way Phases 7 and 9 closed.**
The twenty requirement qualifiers are not a substitute for this: they say what each requirement
proved, and the six criteria are what the phase promised. Every one is covered by content already
planned; **nothing in the phase states so at the gate**, and a criterion nobody walks is a criterion
nobody checked. For each, name the plan and the artefact that answers it, and say what is **not**
answered:

1. **impeccable, Grifter and Inter, and the licence discharged into `THIRD-PARTY.md`.** The honest
   answer has three parts. **impeccable is installed** — at `~/.claude/skills/impeccable/`, a
   user-scoped tool, not a project dependency; `10-UI-SPEC.md` §0 was authored through it and answers
   its six gates; **no plan runs `npx impeccable install`** and `npm ls impeccable` stays empty. That
   is V-06, and it is met. **Grifter carries headlines and Inter carries body text** — 10-02, with
   `--font-display` asserted equal to every display `@font-face` family. **Inter *Italic* does not
   ship**: D-12 withdrew it, which is the same amendment step 2 makes to the Goal line. And the
   licence is **discharged but not resolved**: `gen-licenses.mjs` records Grifter's family, licensee
   and licence name in `THIRD-PARTY.md`, the binary is `export-ignore`d from `git archive HEAD` and
   `static/fonts/README.md` stands in the archive in its place — but **whether Hanson Method's
   licence permits redistribution at all is the user's question and is unanswered.** It is handed
   over as **Open item 2**, with 10-02's costed reversal beside it. Say all of that; a criterion half
   met is not a criterion failed, but recording it as met would be false.
2. **Front door and browse both first-class.** 10-07: the ring stays at eight and gains the `FOR`
   link row (A-22), `/browse/` carries two captioned facet rows of sixteen chips with no disclosure,
   and `?tag=` migrates rather than dropping. Not answered: whether the ring should grow — blocked by
   `front-door.spec.ts:110-112` and deferred.
3. **One sequence, plus CLEAR under every Phase 7 rail, with its own confirmation and bench row.**
   10-12 and 10-13: `InstallAction` **4**, `WRITE_CLICKS` length 4 equal to the four control labels,
   `ClearConfirm.svelte`, `INSTALL-RUNBOOK.md` **row H**. Not answered: row H itself, which is
   10-14-03's checkpoint and the user's.
4. **Playful, legible tuning; a stylized RGB picker; one idea nobody does.** 10-09, 10-10, 10-11: the
   locks, the **live cost forecast** (the idea the survey found in no product in the category), the
   three-rail lattice picker with A-09's fence, and `MixTwo`. Not answered: nothing.
5. **The paragraphs gone, each by a named amendment with its spec rewritten and its reservation
   re-measured, and SAFE-01's fate said out loud.** 10-03 and 10-05: ten retirements, each an
   amendment with its pinning assertion rewritten in the same commit, five reservations re-derived
   from the measured `CH_PER_LINE`, and `SAFE_NOTE` at 35 with `REQUIREMENTS.md:169` amended and
   dated. Not answered: which of Open item 1's three forms the user wants — form 1 ships, both
   reversals are costed.
6. **A reduced-motion escape hatch asserted in both browser projects, and the accent palette still
   governing the chrome.** 10-04's `e2e/aesthetic.e2e.ts` (four titles, both projects) plus
   `aesthetic.spec.ts`'s seven scans; and 10-09/10-10's censuses — the reserved list still **eight**,
   `--color-over` still **three**, nine tokens and three hexes — which is how the picker producing
   any colour for the pad leaves the chrome alone. Not answered: whether the CRT *reads well*, which
   is the user's eye and is stated as such below.

**5. The three Open items** restated in the SUMMARY with their measured costs, quoting 10-02's,
10-03's and 10-07's numbers rather than describing them.

**Negative check:** none is needed here and adding one would be theatre. **Instead, state the one
thing this gate cannot prove**: that the CRT reads well, that Grifter looks right beside Inter, and
that a clear leaves a real ZONA dark. Two of those are the user's eye and one is row H.
  </action>

  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs BASE_FILES+6 BASE_TESTS+40 && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright BASE_E2E+12</automated>
    All three against a fresh `npm run build`, with `test-results/` removed and
    `git status --porcelain` empty.
  </verify>

  <done>
The gate is green against a fresh production build with the chain written out and every disagreement
traced. `ROADMAP.md` names twenty requirements and fourteen plans, and its Goal line's "Inter Italic"
is amended by name and dated. `REQUIREMENTS.md` carries twenty qualifiers, three with explicit
unresolved halves. **All six of ROADMAP's Phase 10 success criteria are walked one at a time**, each
naming the plan that answers it and what it leaves unanswered — criterion 1 recorded as *discharged
into `THIRD-PARTY.md`, resolution handed to the user as Open item 2*, and its "Inter Italic" clause
amended by D-12. The three Open items are restated with numbers. The archive omits the binary and
carries its note.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10-14-03: Row H, the three decisions, and the two things only an eye can settle</name>

  <files>docs/INSTALL-RUNBOOK.md</files>

  <read_first>
    - `docs/INSTALL-RUNBOOK.md` — row H, written in 10-13; this checkpoint is that row, inline
    - `10-VALIDATION.md` section Manual-Only Verifications — why row H is not automatable
    - `10-UI-SPEC.md` Open for the user, items 1, 2 and 3
  </read_first>

  <action>
Present the three sections below to the user and stop. **Do not run any part of section A.**
**No agent in this phase connects to a device, performs a write, or deploys**, and this checkpoint is
where that promise is handed over rather than quietly kept.
  </action>

  <what-built>
Everything in fourteen plans, automated to the edge of what a machine can say:

- a new type pair behind one swappable token, with the display face reachable through exactly one
  custom property and one `@font-face` block;
- four CRT layers that cannot reach a text node, two switches and one capability check, asserted at
  the compositor level in both browser projects;
- no card resting dark, by supplying a finger rather than changing a configuration;
- sixteen tag terms in two facets, three per entry, two sorts, and no shared link dropped;
- a colour picker built on the hardware's own 4,096-colour lattice, a live budget forecast, knob locks
  and `MIX TWO`;
- **a fourth device write — CLEAR — with its own confirmation, exercised end to end against the
  scripted ZONA in both browser projects, and never once against real hardware by any agent.**
  </what-built>

  <how-to-verify>
**A. `docs/INSTALL-RUNBOOK.md` row H, on a real ZONA.** Rows A–G are still open from Phases 6 and 7;
row H is new and does not depend on them, though doing A–G first will make it read better.

1. Plug in the ZONA, open the site, connect from the header, choose a card.
2. `TRY ON DEVICE`, wait for `PLAYING NOW`. The pad is doing the new thing.
3. Click `CLEAR`. **Read the block before clicking anything in it.** It should say `REMOVES`, then
   `This empties the Setup and Timer in your ZONA’s memory. A power cycle brings back whatever is
   stored.`, then `PUT BACK still restores what was there when you connected.`
4. Confirm. **Do not pull the cable while the label reads `CLEARING…`.**
5. **Watch the pad. It should go dark and stay dark.** That is the whole row: only a module can show
   that two empty scripts leave the touch element actually empty rather than merely acknowledged.
6. Click `PUT BACK`. The pad should come back to exactly what it was doing before step 2.
7. Optionally: pull the cable, wait, plug it back in. The module should come up running **whatever is
   stored in its flash** — not the cleared state.

Record: what the pad did at step 5, how long the clear took, whether the border LEDs flashed, and
whether step 7 behaved.

**B. Three decisions that are yours, each with its cost measured.**

1. **SAFE-01's form.** Shipped as `SAFE_NOTE`, 35 characters on the control, in every state, on both
   surfaces that carry the primary. Is that the guarantee you want, or should the 88-character
   sentence come back in the disclosure, or should SAFE-01's second clause be retired outright with a
   dated note? Forms 2 and 3 are one edit each.
2. **Grifter.** Shipped as D-14 says: served from the site, excluded from the source archive, recorded
   by family, licensee and licence name. **The unresolved half is redistribution, not use** — the site
   publishes `git archive HEAD` at a public URL, and even a written commercial-use grant does not
   clear that; only an explicit grant to include the file in a publicly downloadable source archive
   does. The recommendation stands: **one email to the foundry asking specifically about the
   source-archive case**, ship Grifter meanwhile, and treat Archivo as the prepared fallback. The swap
   is seven edits and touches no component and no test.
3. **The front-door ring.** Kept at eight, with a `FOR` link row beneath the pads making it the entry
   to browse rather than its rival. Growing it is blocked today by a shipped assertion that every row
   member is a `padsim` entry, which twenty-seven of thirty-six now fail.

**C. Two things only your eye can settle**, and no test in this repository can:

1. **Does the CRT read as screen texture behind the interface, or as dirt on the words?** Open `/` and
   `/browse/` in a dim room. Then set `SCREEN: FLAT` in the footer and look again. The treatment is
   supposed to be invisible until you notice it.
2. **Does Grifter look right beside Inter?** The wordmark at 28px / 0.50em, the configuration name at
   20px, and every button label at 12px / 0.18em are all one weight, separated by size and tracking
   rather than by weight, because the shipped Grifter family has no 900. That is a deliberate
   constraint and it is the thing most worth a second opinion.
  </how-to-verify>

  <resume-signal>
Type "approved" to close the phase with row H and the three decisions recorded as open, or describe
what you found — row H's result, your ruling on any of the three, or anything the CRT and the type
pair look wrong doing.
  </resume-signal>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing; `npm run lint` clean.
- `npm run test:quick` at `BASE_FILES + 6` / `BASE_TESTS + 40`, chain written out.
- `npm run test:sweep` at `4 19`, four totals reproduced, three wall clocks against `BASE_SWEEP_WALL`.
- `npx playwright test --workers 3` at `BASE_E2E + 12`; `test-results/` removed; tree clean.
- `npm run licenses` clean; the archive omits the font binary and carries its note.
- `git diff --stat HEAD -- src/vendor/` and
  `git diff --stat <phase-start>..HEAD -- src/lib/ui/Coverflow.svelte` both empty.
</verification>

<success_criteria>
1. Every projection in `10-VALIDATION.md` is replaced by an observation, and every wrong estimate is
   named as wrong.
2. The phase total is a chain from 10-01's baseline, and any disagreement is traced to the plan that
   moved it.
3. Twenty requirements carry qualifiers that say what was proved **and** what was not, with three
   explicit unresolved halves.
4. `ROADMAP.md`'s `Requirements: TBD` and `Plans: TBD` are filled, and its "Inter Italic" is amended
   by name and dated.
5. All six of ROADMAP's Phase 10 success criteria are walked one at a time, each with the plan that
   answers it and the half it leaves open — criterion 1 answered honestly: the licence is discharged
   into `THIRD-PARTY.md` and the resolution is the user's, as Open item 2.
6. The three Open items are restated with measured costs quoted from the plans that shipped them.
7. Row H is handed to the user, unanswered, with no agent having touched a device and no agent having
   deployed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-14-SUMMARY.md` with the observed gate
numbers, the delta chain reconciled plan by plan with every wrong estimate named, the twenty
qualifiers, the six-criterion walk with each criterion's answering plan and its unanswered half, the
three Open items with their measured reversal costs, the deferred-items list, and the checkpoint's
outcome.
</output>
