---
phase: 10-redesign
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/app.css
  - src/lib/ui/PadFrame.svelte
  - src/lib/ui/FrontDoor.svelte
  - src/lib/ui/ScreenToggle.svelte
  - src/routes/+layout.svelte
  - src/lib/ui/aesthetic.spec.ts
  - e2e/aesthetic.e2e.ts
  - src/lib/device/session.svelte.ts
autonomous: true
requirements: [IDENT-01, IDENT-02]

must_haves:
  truths:
    - "Four CRT layers ship and none of them can reach a text node, because no text-bearing element is a descendant of a CRT container and a source scan says so."
    - "Coverflow.svelte is byte-untouched, and its three load-bearing rules are asserted in both directions rather than merely obeyed."
    - "A visitor can turn the whole treatment off from the footer on every route, including the two layers that do not move and that no motion preference can reach."
    - "Under prefers-reduced-motion both moving layers have animationName none, asserted at the compositor level in both browser projects - which the shipped canvas-backing-store assertions structurally cannot see."
    - "The glitch fires on exactly one surface and one event, carries no filter of any kind, and never lands on a product surface or a pad canvas."
  artifacts:
    - path: "src/lib/ui/ScreenToggle.svelte"
      provides: "The SCREEN word row, its localStorage persistence, its reduced-motion default, both 44px axes"
      contains: "hangar.screen.v1"
    - path: "src/lib/ui/aesthetic.spec.ts"
      provides: "Seven source scans - scan 4 from 10-01 plus the six that gate the layers this plan ships"
    - path: "e2e/aesthetic.e2e.ts"
      provides: "Four browser gates, both projects, each with its non-vacuity assertion first"
  key_links:
    - from: "src/lib/ui/FrontDoor.svelte"
      to: "src/lib/ui/Coverflow.svelte"
      via: ".crt-band's five geometry literals string-equal to .band's, asserted across the two files"
      pattern: "clamp\\(260px, 52vmin, 560px\\)"
    - from: "src/lib/ui/ScreenToggle.svelte"
      to: "src/app.css"
      via: "data-screen on <html> setting --crt, from which every layer's display, background-image and animation derive"
      pattern: "--crt"
---

<objective>
The CRT and the glitch, placed so they cannot reach a word, and gated so the off switch is one
attribute rather than a hunt.

Wave 4. Blocked on 10-03 and, through it, on 10-02: `--crt-scanline` is already in `src/app.css`
where `identity.spec.ts` can see it, and the widened `rgb()` regex is already green.

Four layers, from the ground up: **G** the page ground (`body::before`, halftone + noise + vignette),
**S** scanlines and noise on pad frames only, **R** one roll bar for the whole page inside a new
`.crt-band` shell, **T** the tear, a pseudo-element overlay on `.crt-band::after`, on the
`navigator.serial` `connect` event and on no other event.

**The load-bearing rule, from §0's register split:**

> The CRT is a property of a **pad frame, the coverflow band's own box or the page ground**. It is
> never a property of a panel, a control, a toolbar or a text node.

**Two things this plan must not break, and both are asserted in both directions.** `Coverflow.svelte`
is not edited — not one line — and `git diff` proves it. And `.band`'s `overflow: clip` and
`mask-image` and each slot's inline `opacity` and `filter: brightness()` are asserted **present**, not
merely permitted, because a scan that only forbade would go green on a tidy-up that deleted the
band's mask.

**V-01 lands here.** 10-01 shipped `aesthetic.spec.ts` with scan 4 alone, because the other six scan
for elements that did not exist. This plan brings the file to **seven**, and each scan lands **in the
same commit as the layer it gates**. Never afterwards: `10-RESEARCH.md` §3.5's arithmetic is not
disputed — a 0.186-alpha black line over `--color-ink-quiet` drops it from 5.57:1 to about 3.9:1 while
`identity.spec.ts` stays green, because it computes contrast from the declared alpha. The resolution
is not to make the gate composite an overlay; it is to make the overlay **unable to reach the text**,
and to assert the scoping structurally.

**V-02 lands here too.** §8.5 asks for the Layer S cost measurement in Wave 0, but its stated method
needs Layer S and the `SCREEN` control to exist. Task 1 takes it **before Layer S's selector is
written**, with a throwaway injected stylesheet against the shipped `/browse/`; task 3 re-takes it
against the real control and reconciles.

Purpose: D-07 confirmed, with an off switch that is two switches and a third that is automatic.
Output: four layers, one visible control, seven source scans, four browser gates, `Coverflow.svelte`
byte-untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/10-redesign/10-RESEARCH.md
@.planning/phases/10-redesign/10-01-SUMMARY.md
@.planning/phases/10-redesign/10-02-SUMMARY.md
@src/app.css
@src/lib/ui/Coverflow.svelte
@src/lib/ui/PadFrame.svelte
@src/lib/ui/FrontDoor.svelte
@src/lib/ui/aesthetic.spec.ts
@src/lib/sim/host.ts
@src/lib/sim/paint.ts

<standing_rules>
- **Never edit anything under `src/vendor/`.** **Never touch a sibling repository.**
- **`src/lib/ui/Coverflow.svelte` is not edited in this plan. Not one line.** Prove it with
  `git diff --stat HEAD -- src/lib/ui/Coverflow.svelte` at the end of every task.
- **No agent connects to or writes to a device, and no agent deploys.** The `connect` event is
  exercised through the existing fake serial, never through hardware.
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- e2e at `--workers 3`; remove `test-results/` by hand and confirm `git status --porcelain` empty.
- **State every count as a carried name plus a delta.**
- The project spends no money.
</standing_rules>

<interfaces>
**The layer table, with its relationship to the 3D context — which is the part the spec's own first
pass got wrong.** The 3D rendering context is established by **`.stage`**
(`Coverflow.svelte:983-990`), the only element carrying `perspective` and `transform-style:
preserve-3d`. `.band` is **outside** it, and `:962-970` puts `overflow: clip`,
`overflow-clip-margin: 6px` and the edge `mask-image` on `.band` **deliberately**.

| z | Layer | Element | Relationship |
|---|---|---|---|
| −1 | **G** | `body::before`, `position: fixed; inset: 0; pointer-events: none` | behind all content; never an ancestor |
| 3 | **S** | `PadFrame.svelte`'s frame `::after`, one pseudo-element, two background layers, inside the 10px radius | a leaf of the 3D tree, the position `filter: brightness()` already legally occupies |
| 4 | **R** | a real `<div>` inside `.crt-band` | a sibling of the whole Coverflow output |
| 5 | **T** | `.crt-band::after` | a sibling's pseudo-element |

> **No CRT layer is ever set on `.stage`, and no CRT layer is ever an element between `.stage` and a
> `.slot`.** Every layer is a sibling of `.band`, or a leaf inside a slot, or behind all content.

**`.crt-band` — the box, and why it is not `inset: 0`.** `Coverflow.svelte` renders four top-level
siblings — `.band` (`:807`), `.plate` (`:873`), `.fidelity` (`:884`) and, while chosen, `.panel`
(`:930`) — and `FrontDoor.svelte:181` wraps the lot in one `<div class="row">`. A bar at `inset: 0`
would sweep the 20px Heading carrying the configuration's name and the fidelity prose beneath it.

| Property | Value |
|---|---|
| Parent | `<div class="row">`, which gains `position: relative` |
| Position | `position: absolute; inset-block-start: 0; inset-inline: 0` |
| Block size | `clamp(260px, 52vmin, 560px)` |
| Inline size | `min(100vw, 1280px)`, `max-inline-size: 100%`, `margin-inline: auto` |
| Clip and fade | `overflow: clip`, `overflow-clip-margin: 6px`, the identical four-stop `mask-image` |
| The chosen lift | `transform: translateY(-24px)` under `.chosen`, the same 260 ms curve, both dropped under `prefers-reduced-motion` exactly as `Coverflow.svelte:1036-1047` drops them |
| Contents | Layer R (one `<div>`) and Layer T (`::after`). Nothing else, ever |
| Semantics | `aria-hidden="true"`, `pointer-events: none` |

**The five literals source-scan 7 compares string-equal**, read out of `Coverflow.svelte`'s `.band`
rules and `FrontDoor.svelte`'s `.crt-band` rules after `identity.spec.ts`'s `normalise()`:
`clamp(260px, 52vmin, 560px)`, `min(100vw, 1280px)`, `overflow-clip-margin: 6px`, the `mask-image`
linear-gradient, `translateY(-24px)`. **A change to one that is not made to the other goes red and
names both files.**

**The three switches.**

| # | Switch | Reaches |
|---|---|---|
| 1 | `prefers-reduced-motion: reduce` | R's and T's `animation`. **Not** G or S — they do not move. Read in CSS **and** in JS through the host's existing live `matchMedia` subscription (`host.ts:93-94,160-167,200`), never a second one-off |
| 2 | `SCREEN: TEXTURED · FLAT`, footer, every route, persisted at `hangar.screen.v1` | **all four layers.** `--crt: 0`. Default `TEXTURED`, except `FLAT` when `prefers-reduced-motion: reduce` at first paint |
| 3 | `navigator.hardwareConcurrency <= 4` | Layer R does not mount at all. Feature-detected, never browser-sniffed |

**Switch 3 is also the second way the reduced-motion test could pass on nothing** — a four-core CI
runner would mount no roll bar — so tests 1 and 2 force `hardwareConcurrency` to 8 through
`addInitScript` **before** `goto`, and test 4 asserts Switch 3 in its own right at a forced 4.

**Where the tear fires, and where it must not.** One surface, one event: `.crt-band::after`, on
`navigator.serial`'s `connect` — a permitted ZONA physically plugged in, the one thing on this site
the visitor did not start with a click. 180 ms, seven `step-end` states over `clip-path: inset()` and
`transform: translateX(±1.6%..3.2%)`. **No `filter`. `hue-rotate`, `saturate`, `contrast` and
`brightness` are removed from the ported keyframes entirely** — `paint.ts:17-35` makes a filter over a
pad a fidelity violation, not a style choice. Never on a product surface, never on the element it
decorates, never on a failure state, never on a pad canvas, never on a recurring idle timer.

**Layer S's two backgrounds.** `repeating-linear-gradient` scanlines at a **5px period** (a declared
non-token texture metric — §6 exception 5) reading `var(--crt-scanline)` from `app.css`, inside a
layer at `opacity: 0.18`; and a 240px `feTurbulence` SVG data-URI tile painted at 220px, layer opacity
≤ 0.10. **The data-URI lives in `PadFrame.svelte` and never in `src/app.css`**, because
`identity.spec.ts` reads every hex in that file and a `%23`-encoded colour inside a data-URI would
read as a fourth hue. Source-scan 6 asserts the tile declares no `fill` attribute other than the
filter's own output.

**`PadFrame.svelte`'s header contract is amended by name.** Its "there is no drop-shadow, no blur, no
hue-rotate, no sepia and no invert anywhere near a pad face" gains a fourth clause admitting one
`pointer-events: none` pseudo-element carrying a repeating gradient and a noise tile at
`opacity ≤ 0.18`, which adds no colour and only darkens. *A comment that quietly stopped being true is
how a header stops being read.*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-04-01: Measure Layer S first, then ship Layers G and S behind the --crt gate</name>
  <files>src/app.css, src/lib/ui/PadFrame.svelte, src/lib/ui/aesthetic.spec.ts</files>

  <read_first>
    - `10-UI-SPEC.md` §7.1, §8.1, §8.3, §8.5, §8.7 and §6 exception 5
    - `10-RESEARCH.md` §3.1 (the reference's five techniques) and §3.3
    - `src/lib/ui/PadFrame.svelte` in full; `src/lib/sim/paint.ts:17-37`
  </read_first>

  <action>
**1. The measurement, before Layer S's selector is written (V-02).** Against the shipped `/browse/`
at thirty-six entries, in `chromium` **and** `webkit-phone`, with a throwaway Playwright script in
the scratchpad — **not a committed test**:

- Arm A: `page.addStyleTag` carrying the exact two background layers this task is about to write,
  applied to `[data-testid^="pad-"]::after`.
- Arm B: the same run with no injection (this is the `SCREEN: FLAT` arm).
- For each arm and each engine: first-paint time, and the **95th-percentile frame time** during a
  full scroll of the grid, sampled from `requestAnimationFrame` deltas.

**Record all eight numbers.** §8.5's rule: *if the delta exceeds 2 ms at the 95th percentile, Layer S
is scoped to the front door's seven frames only and the browse grid keeps Layer G alone.* That is a
one-line selector change and it is declared here so it is a decision rather than a discovery. State
the verdict explicitly in the SUMMARY, whichever way it goes.

**2. `src/app.css` — the `--crt` gate and Layer G.**

```css
html { --crt: 1; }
html[data-screen="flat"] { --crt: 0; }
```

Layer G: `body::before`, `position: fixed; inset: 0; pointer-events: none; z-index: -1`, carrying the
ground halftone `rgb(214 255 78 / 0.04)` at `background-size: 3px 3px`, the noise tile, and the
`box-shadow: inset` vignette. Gate its `background-image` and `box-shadow` on `--crt` so `FLAT` turns
it off. **Two constraints:** the halftone's `rgb()` must match the widened regex (it does — it is an
alpha of the accent); and the vignette must use `rgb(0 0 0 / …)`, which the widened regex now admits
and which 10-02 widened it *for*. Any third set of channels is still red.

**Do not put the `feTurbulence` data-URI in this file.** Layer G's noise tile is the same tile Layer S
uses; declare it **once**, as a custom property `--crt-noise` at `:root` — and check by running
`identity.spec.ts` immediately: if the tile's encoding contains a `%23` or a `#`, the hex loop goes
red and the tile must move into a component instead, with Layer G losing its noise and keeping the
halftone and the vignette. **Record which of the two happened.**

**3. `PadFrame.svelte` — Layer S.** One `::after` on `.pad`, `pointer-events: none`,
`aria-hidden`-irrelevant (a pseudo-element has no accessibility node), inside the 10px radius,
`inset: 6px` like its siblings so it does not paint over the frame border. Two background layers: the
5px-period `repeating-linear-gradient` reading `var(--crt-scanline)`, and `var(--crt-noise)` at 220px.
Layer opacity 0.18. `content: ""` gated on `--crt`: `content: none` when `--crt` is 0, which is
exactly what browser gate 2 reads. No `will-change`, no promotion, no `shadowBlur`, no `filter`.

Amend the header comment by name, with the fourth clause from `<interfaces>`.

**4. `aesthetic.spec.ts` scans 1, 2, 5 and 6** — the four that gate G and S. Written **red first**;
observe and record the red before the layers land, or write them after and perturb. Either way the
SUMMARY carries the red.

1. **The four layer selectors appear only in an explicit allowlist of files**, with a floor so the
   walk cannot pass vacuously. Allowlist today: `src/app.css`, `src/lib/ui/PadFrame.svelte`. `.crt-band`
   joins in task 2.
2. **Every layer declares `pointer-events: none`**; Layers R and T additionally `aria-hidden="true"`
   (asserted in task 2).
5. **No CRT selector names a `canvas`.** Scan the allowlisted files' `<style>` and `app.css` for any
   rule whose selector contains both a CRT class/pseudo and `canvas`.
6. **The noise data-URI declares no `fill` attribute other than the filter's own output**, so the tile
   cannot smuggle in a hue.

Every one carries the house non-vacuity assertion first —
`expect(count, "the walk was not empty").toBeGreaterThan(0)`.

**Negative check:** move `--crt-scanline` from `src/app.css` into `PadFrame.svelte`'s `<style>`.
Expect `identity.spec.ts` **green** and `aesthetic.spec.ts` scan 1 **red**, naming the file. That pair
is the finding §7.1 predicts and 10-02 recorded half of; record the other half here. Revert and
confirm `git diff --quiet`.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/aesthetic.spec.ts src/lib/ui/identity.spec.ts && npm run build</automated>
    Expect `aesthetic.spec.ts` **5 passed** (scan 4 plus four new), `identity.spec.ts` **7 passed**.
    Plus the eight measured numbers and the 2 ms verdict recorded.
  </verify>

  <done>
The Layer S A/B measurement is eight numbers with an explicit verdict. Layers G and S ship behind
`--crt`. The noise tile's home is decided by running `identity.spec.ts`, not by assumption.
`PadFrame.svelte`'s header carries its fourth clause. `aesthetic.spec.ts` is 5 tests. The
scanline-placement negative check is recorded in both directions.
`git diff --stat HEAD -- src/lib/ui/Coverflow.svelte` is empty.
  </done>
</task>

<task type="auto">
  <name>Task 10-04-02: The .crt-band shell, Layers R and T, the SCREEN toggle, and scans 3, 4 and 7</name>
  <files>src/lib/ui/FrontDoor.svelte, src/lib/ui/ScreenToggle.svelte, src/routes/+layout.svelte, src/lib/device/session.svelte.ts, src/lib/ui/aesthetic.spec.ts</files>

  <read_first>
    - `10-UI-SPEC.md` §8.2, §8.3, §8.4, §8.6, §14
    - `src/lib/ui/Coverflow.svelte:942-1048` — the `.band` rules to be duplicated verbatim
    - `src/lib/ui/FrontDoor.svelte` — `.row` at `:278`, the markup at `:181`
    - `src/lib/ui/Knob.svelte` and `BrowseToolbar.svelte`'s `SORT` — the word-row widget being reused
    - `src/lib/device/session.svelte.ts` — where `navigator.serial`'s `connect` is already handled
  </read_first>

  <action>
**1. `.crt-band` in `FrontDoor.svelte`.** `.row` gains `position: relative` — the one line the file
already owns. Add a sibling `<div class="crt-band" aria-hidden="true">` containing one
`<div class="crt-roll">` and nothing else; Layer T is `.crt-band::after`. Copy the five literals from
`Coverflow.svelte`'s `.band` **verbatim**, and put a comment above them saying the duplication is
real, is deliberate, and is gated by scan 7 rather than trusted. `.crt-band` follows `.chosen` with
the same `translateY(-24px)` on the same 260 ms curve, and drops it under `prefers-reduced-motion`
exactly as `:1036-1047` does.

Layer R: `transform: translateY(-130% → 520%)` over `6.5s linear infinite`, a 22%-tall bar at
`rgb(214 255 78 / 0.05)`, `will-change: transform`, `position: absolute`, `pointer-events: none`.
Mounted only when `--crt` is 1, only when `navigator.hardwareConcurrency > 4`, and **not at all while
a pad is chosen**. `animation: none` under `prefers-reduced-motion`.

Layer T: `.crt-band::after`, `content: ""` gated on `--crt`, `pointer-events: none`, seven `step-end`
states over 180 ms of `clip-path: inset()` and `transform: translateX(…)`. **No `filter` property
appears anywhere in the keyframes** — grep the file for `filter:` and state the count (expected: 0).
Triggered by a class toggled for 180 ms on the `connect` event.

**2. The `connect` wiring.** `session.svelte.ts` already listens at the `navigator.serial` level.
Expose the event, or a derived "a permitted ZONA was just plugged in" signal, and let `FrontDoor`
toggle the class. **Do not add a second `navigator.serial` listener**, and do not fire on
`disconnect`, on a click-driven connect, or on any failure. One surface, one event.

**3. `ScreenToggle.svelte`.** Phase 5's word-row widget, unchanged in markup and behaviour: a
`role="radiogroup"` of real `<input type="radio">` inside `<label>`s, one tab stop, arrow keys move
and select. Caption `SCREEN` (6), Micro, `--color-ink-quiet`. Options `TEXTURED` (8) · `FLAT` (4),
Micro, selected in `--color-accent` — reserved-list item 8, so **no accent is added**. Persist at
`hangar.screen.v1`, a single string, with a **guarded read** exactly as the snapshot store is: a
throwing store must not break the page, and it never touches `hangar.snapshot.v1`. Default `TEXTURED`
except `FLAT` when `prefers-reduced-motion: reduce` at first paint. Sets `data-screen` on
`<html>`. **`min-block-size: 44px` and `min-inline-size: 44px` per option** — `FLAT` is four
characters at 12px/0.18em, about 38px wide, so the inline floor is load-bearing here rather than free.

Mount it in `+layout.svelte`'s footer beside the existing links, on every route.

**4. Scans 3, 4 and 7.** Scan 4 already exists from 10-01; extend nothing, but confirm it still
passes and that its file is byte-unchanged apart from the header note.

3. **No text-bearing element is a descendant of a CRT container.** Expressed as: no CRT selector
   appears in the same component as a rendered `<p>`, `<h*>`, `<label>`, `<button>` or `{…}` text
   expression, **except through the allowlist**. `FrontDoor.svelte` is on that allowlist and its entry
   **carries its condition in the assertion message**: `.crt-band` is `aria-hidden`,
   `pointer-events: none`, and geometrically bounded to a box that contains no rendered text.
7. **`.crt-band`'s geometry is string-equal to `.band`'s.** Read the five literals out of both files,
   `normalise()` both sides, compare. On failure the message names **both** files and the literal.

**Three negative checks:**

- Change `.crt-band`'s `clamp(260px, 52vmin, 560px)` to `50vmin`. Expect scan 7 red naming both files.
- Add a `<p>Hello</p>` inside `.crt-band`. Expect scan 3 red naming `FrontDoor.svelte` and quoting its
  allowlist condition.
- Set `opacity: 0.99` on `.stage` in `Coverflow.svelte`. Expect scan 4 red naming `.stage` and
  `opacity`. **Revert with `git checkout -- src/lib/ui/Coverflow.svelte` and re-prove the file is
  byte-untouched.**
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/aesthetic.spec.ts && npm run check 2>&1 | grep -Ei "error|warning"</automated>
    Expect `aesthetic.spec.ts` **7 passed**. Plus `grep -c "filter:" src/lib/ui/FrontDoor.svelte`
    reporting zero inside the CRT rules, and
    `git diff --stat HEAD -- src/lib/ui/Coverflow.svelte` empty.
  </verify>

  <done>
`.crt-band` exists in `FrontDoor.svelte`, bounded to `.band`'s box, containing Layers R and T and
nothing else. `ScreenToggle` is in the footer on every route with both 44px axes and a guarded store.
The tear fires on `connect` and on nothing else, with no `filter` anywhere in its keyframes.
`aesthetic.spec.ts` is 7 tests. Three negative checks recorded. `Coverflow.svelte` byte-untouched.
  </done>
</task>

<task type="auto">
  <name>Task 10-04-03: e2e/aesthetic.e2e.ts - four browser gates the canvas assertions cannot make</name>
  <files>e2e/aesthetic.e2e.ts</files>

  <read_first>
    - `10-UI-SPEC.md` §8.7 in full
    - `e2e/browse.e2e.ts:486-520` — how this suite arms reduced motion, and why `test.use` alone was
      not enough
    - `e2e/first-experience.e2e.ts:87` — the twin assertion that reads the canvas backing store
  </read_first>

  <action>
**Why this file exists, in the header, verbatim in substance:** `browse.e2e.ts:140-148` and
`first-experience.e2e.ts:87` read the **9×9 canvas backing store**. A CSS overlay painted above the
canvas by the compositor never touches that backing store. **A sweeping scanline bar over the pads
would leave every existing reduced-motion assertion green.** The tests would lie.

Four tests, both projects, **each carrying the house non-vacuity assertion first** —
`expect(count, "the walk was not empty").toBeGreaterThan(0)` — because each reads a computed style off
an element that a default or a capability check can legitimately keep out of the DOM, and
`getComputedStyle` of nothing asserts nothing.

1. **Reduced motion stops both moving layers.** `emulateMedia({ reducedMotion: "reduce" })` called
   **before** `goto` (`test.use` alone was observed insufficient — `browse.e2e.ts:493-500` records
   why), with `matchMedia("(prefers-reduced-motion: reduce)").matches` asserted true. **Two gates
   opened first or the test is vacuous:** `SCREEN: TEXTURED` selected explicitly, because §8.6's own
   default under reduced motion is `FLAT` and `FLAT` mounts no roll bar; and
   `navigator.hardwareConcurrency` forced to **8** through `addInitScript` before `goto`, because
   Switch 3 removes Layer R on a four-core runner. Then: the roll bar and the tear-bearing element are
   each found and counted `> 0`, and `getComputedStyle(el).animationName === "none"` on both.
2. **`SCREEN: FLAT` turns off all four layers, not two.** `--crt` is `0`;
   `getComputedStyle(document.body, "::before").backgroundImage` is `none` (**G**);
   `getComputedStyle(padFrame, "::after").content` is `none` (**S**); the roll bar is absent from the
   DOM (**R**); `getComputedStyle(crtBand, "::after").content` is `none` (**T**). The same four
   selectors are asserted **present** first with `SCREEN: TEXTURED` and `hardwareConcurrency` forced to
   8. **The test proves a change rather than an absence.**
3. **The `SCREEN` choice survives** a navigation from `/` to `/browse/` and a reload.
4. **Switch 3 asserted in its own right.** `hardwareConcurrency` forced to **4** before `goto`,
   `SCREEN: TEXTURED` selected: the roll bar is **absent** from the DOM while Layers G and S are still
   **present**. Without this, Switch 3 is a sentence no test can tell from a typo.

Tag whichever titles need the `@webkit` grep so both projects run all four; `webkit-phone` greps on
`/@webkit/`, so a title without it runs only in chromium. **Four titles × two projects = 8 e2e
titles.**

**Re-take the Layer S measurement** against the real control now that `SCREEN: FLAT` exists: the same
two arms, the same two engines, the same 95th-percentile method. Reconcile against task 1's injected
figures and state whether the verdict changed. If the real measurement crosses 2 ms where the injected
one did not, apply the declared one-line fallback — Layer S scoped to the front door's seven frames —
in this task, and say so.

**Negative check:** delete the `hardwareConcurrency` forcing from test 1 and run it with a forced 4
instead. Expect the test's own non-vacuity assertion red, naming the roll bar. Revert.
  </action>

  <verify>
    <automated>npx playwright test e2e/aesthetic.e2e.ts --workers 3</automated>
    Expect **8 passed** across both projects. Then the full suite:
    `npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright PREV_E2E+8`,
    with `test-results/` removed by hand and `git status --porcelain` empty.
  </verify>

  <done>
Four browser gates exist in both projects, each non-vacuous by construction, each opening the two
gates that would otherwise make it pass on nothing. `PREV_E2E` is re-measured at `+8`. The Layer S
measurement is re-taken against the real control and reconciled with the injected one, with the
verdict stated. The negative check on the forcing is recorded.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+6`.
- `npx playwright test --workers 3` at `PREV_E2E + 8`; `test-results/` removed; tree clean.
- `git diff --stat HEAD -- src/lib/ui/Coverflow.svelte src/vendor/` **empty**.
- `npm run build` green, then `npm run test:quick` again.
- `identity.spec.ts` still 7 and still green after every CSS edit.
</verification>

<success_criteria>
1. Four layers ship; none is set on `.stage` or between `.stage` and a `.slot`; none covers a text
   node; every one carries `pointer-events: none`.
2. `Coverflow.svelte` is byte-untouched and its three rules are asserted in **both** directions.
3. The off switch is one attribute plus one visible control, and the control reaches the two layers
   no motion preference can.
4. The reduced-motion claim is asserted at the compositor level in both projects, with both vacuity
   holes explicitly opened first.
5. The tear is one surface, one event, and carries no `filter` — grep-proved.
6. The Layer S cost is a measured number twice, with an explicit verdict against the 2 ms threshold.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-04-SUMMARY.md` with the eleven-name block
carried, both Layer S measurement tables and their verdicts, the noise-tile placement decision and
which gate decided it, the seven scans listed with what each one caught, the six negative checks, and
the `git diff --stat` proving `Coverflow.svelte` untouched.
</output>
