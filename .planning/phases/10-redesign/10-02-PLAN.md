---
phase: 10-redesign
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app.css
  - static/fonts/GRIFTER-Bold.woff2
  - static/fonts/README.md
  - .gitattributes
  - src/lib/ui/identity.spec.ts
  - src/lib/ui/font-assets.spec.ts
  - scripts/gen-licenses.mjs
  - scripts/deploy.mjs
  - package.json
  - package-lock.json
  - THIRD-PARTY.md
autonomous: true
requirements: [IDENT-01, FOUND-02]

must_haves:
  truths:
    - "The display face is reachable through exactly one custom property and exactly one @font-face block, so the licence answer changing costs two lines and nothing else moves."
    - "A token edited without its face, or a face edited without its token, is red rather than silently falling through to system-ui."
    - "The palette is still nine tokens, three hexes and no fourth hue after a font swap and after a black scanline value enters the file."
    - "The Grifter binary is served from the site and is absent from the public source archive, with a note in its place naming the family and where to obtain it - and both halves are asserted rather than trusted."
    - "Quicksand is gone from the tree, from node_modules, from licenses/ and from THIRD-PARTY.md's hand-written paragraph, in one commit."
  artifacts:
    - path: "src/app.css"
      provides: "Two @font-face blocks, --font-display, --font-sans retargeted, --crt-scanline declared, the header comment amended in the same breath"
      contains: "--font-display"
    - path: "static/fonts/README.md"
      provides: "The note that stands in the archive where the binary does not: family, licensee, licence name, where to obtain it"
    - path: "src/lib/ui/identity.spec.ts"
      provides: "G-01 - seven tests: the ninth token still ninth, the widened rgb() regex, and --font-display equal to EVERY display @font-face family"
  key_links:
    - from: "src/app.css"
      to: "src/lib/ui/identity.spec.ts"
      via: "--font-display's first family string-equal to every display @font-face's font-family"
      pattern: "font-display"
    - from: ".gitattributes"
      to: "scripts/deploy.mjs"
      via: "export-ignore honoured by git archive HEAD, asserted two-sided in deploy step 5"
      pattern: "export-ignore"
---

<objective>
The type settlement: two faces, one swappable token, and a licence pipeline that can see a file npm
cannot.

Wave 2. Blocked on 10-01, whose `FONT_SRC` string this plan pastes verbatim and whose `CH_PER_LINE`
it does **not** yet spend — the reservations are 10-03's.

Three pieces:

**The stylesheet.** `src/app.css` stops `@import`ing two Quicksand statics and starts declaring two
`@font-face` blocks: Inter Variable at the proved specifier, and **the one swappable block** — one
static Grifter Bold, served from `/fonts/`, reached only through `--font-display`. No component, no
utility class and no other rule ever writes a display family name. That is D-14's engineering
requirement stated as a structure rather than as a hope.

**The gate.** G-01 widens `identity.spec.ts` in three named ways and in no others: `--font-sans`'s
first family becomes `"Inter Variable"`; a new assertion pins `--font-display`'s first family to the
family named by **every** display `@font-face` block in the file; and the `rgb()`-arguments regex
widens from `^214 255 78 / [0-9.]+$` to `^(214 255 78|0 0 0) / [0-9.]+$` so `--crt-scanline` can live
where the gate can see it. **The hex set stays at three. The token count stays at nine. The favicon's
two-hue regex is untouched.** This is X-27's shape applied a second time.

**The licence pipeline.** Quicksand is uninstalled and its hard-coded paragraph at
`gen-licenses.mjs:165-167` is rewritten in the same commit. Grifter is recorded the way D-13 requires
— **family, licensee and licence name, not an SPDX identifier and not a redistributable file** — its
path is `export-ignore`d, a note stands in the archive where it does not, and `deploy.mjs` step 5
learns to check **both** halves: the binary absent, the note present.

**The Open-for-the-user fork, planned as recommended with its reversal costed.** `10-UI-SPEC.md`
Open item 2 is the one place the contract's position (D-14: Grifter ships) and the document's own
recommendation (A-02: Archivo) differ. **This plan ships D-14 as written.** The reversal, if the user
takes it, is: `npm i -D @fontsource-variable/archivo@5.3.0`; two lines in `src/app.css` (the block's
`font-family`/`src` and `--font-display`'s first family); one line removed from `.gitattributes`; one
allowlist row removed from `font-assets.spec.ts`; the Grifter paragraph removed from
`gen-licenses.mjs`; `static/fonts/` deleted; `npm run licenses`. **Seven edits, no component touched,
no test rewritten** — because `identity.spec.ts`'s new assertion says "every display `@font-face`",
not "the second one". Write that list into the SUMMARY as a numbered reversal recipe.

Purpose: IDENT-01 extended through a font swap without growing the ladder; FOUND-02's licence gate
given eyes it did not have.
Output: one rewritten stylesheet, one new gate assertion, one tracked binary with its exclusion and
its note, one dependency removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/10-redesign/10-01-SUMMARY.md
@src/app.css
@src/lib/ui/identity.spec.ts
@src/lib/ui/font-assets.spec.ts
@scripts/gen-licenses.mjs
@scripts/deploy.mjs
@scripts/postbuild.mjs

<standing_rules>
- **Never edit anything under `src/vendor/`.**
- **Never touch a sibling repository.** `C:\Users\sabot\Documents\Claude\gridstrument-landing\fonts\grifter\`
  is a read-only source: copy the file out of it, never write into it.
- **No agent connects to or writes to a device, and no agent deploys.** `npm run deploy` is never run
  by an agent; its step 5 is edited and exercised by reading, not by deploying.
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- **State every count as a carried name plus a delta**, never as an absolute total. This plan carries
  10-01's eleven names.
- The project spends no money.
</standing_rules>

<interfaces>
**`FONT_SRC`** — the exact `src:` string 10-01 proved, and its four-step evidence. Paste it; do not
re-derive it. If 10-01 recorded candidate (c), this plan's allowlist gains a **second** row with an
OFL-1.1 record and `.gitattributes` gains a second `export-ignore` line.

**The Grifter file.** `C:\Users\sabot\Documents\Claude\gridstrument-landing\fonts\grifter\GRIFTER-Bold.woff2`,
**13,752 bytes**, verified on 2026-09-08. Copy it to `static/fonts/GRIFTER-Bold.woff2` and assert the
byte size on arrival. **Only the Bold.** The shipped family is five statics and 900 is not among them
(A-03), which is why the display family ships one weight and the roles separate by size and tracking.

**`src/app.css`'s target state**, structurally:

| Block | Contract |
|---|---|
| `@import "tailwindcss";` | first, unchanged |
| The two Quicksand `@import`s at `:9-10` | **deleted** |
| `@font-face` — Inter Variable | `font-weight: 100 900`, `font-style: normal`, `font-display: swap`, `src: FONT_SRC`, and the **valid** `unicode-range` list 10-01 quoted |
| `@font-face` — the one swappable block | `font-family: "Grifter"`, `font-weight: 700`, `font-style: normal`, `font-display: swap`, `src: url("/fonts/GRIFTER-Bold.woff2") format("woff2")`, **no `unicode-range`** — the shipped file is not subset and an incomplete range would silently fall through to `system-ui` |
| `@theme` | nine `--color-*` tokens **byte-unchanged**; `--font-display: "Grifter", ui-sans-serif, system-ui, "Segoe UI", sans-serif`; `--font-sans: "Inter Variable", ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`; `--font-mono` **byte-unchanged** |
| `--crt-scanline` | `rgb(0 0 0 / 0.50)`, declared **outside** `@theme` (it is not a `--color-*` and must not enter the ladder), at `:root`. §7.1: *"a `rgb(0 0 0 / 0.50)` written inside `PadFrame.svelte`'s `<style>` would be invisible to every colour gate the site has"* |
| The header comment | amended in the same breath, so the file and `identity.spec.ts` never disagree. Say: two faces, one swappable, one token; the scanline is the ground at an alpha, not a hue; the ladder is still nine |

**Three things that must NOT enter `src/app.css` in this plan**, because `identity.spec.ts` reads the
whole file: the `feTurbulence` noise data-URI (a `%23`-encoded hex would read as a fourth hue — it
belongs in `PadFrame.svelte`, 10-04); Layer G's `body::before` rule (10-04); the `--crt` gate (10-04).
`--crt-scanline` alone comes early because G-01's widened regex is meaningless without it, and a
widened regex with nothing to widen for is a hole.

**G-01, the three changes and the four non-changes.** `identity.spec.ts` goes 6 → 7 tests.

Changed:
1. Test 3's `expect(sansFamilies[0]).toBe('"Quicksand"')` → `'"Inter Variable"'`. The
   `>= 4` named-fallback floor stays; Inter's stack has six.
2. **New test 7**: read every `@font-face` block out of the stripped source; partition into "display"
   (any block whose `font-family` is not `"Inter Variable"`) and body; assert the display set is
   non-empty; assert **every** member's `font-family` string-equals `--font-display`'s first family
   after `normalise()`. *Every*, not *the second* — so it survives the D-14 route (one Grifter face),
   the A-02 reversal (one Archivo face) and any later two-face arrangement without itself being
   rewritten.
3. Test 5's `rgb()`-arguments regex → `/^(214 255 78|0 0 0) \/ [0-9.]+$/`.

**Not changed, and each is asserted to still fail on its own violation:** the nine-token ladder (a
tenth still red); the three-hex set (a fourth still red); `alphaOf()`'s four AA members (`0 0 0` is
not an alpha of the accent and never enters that loop); the favicon's two-hue regex.

**`gen-licenses.mjs`, three edits:**

| Where | Edit |
|---|---|
| `:165-167` | the Quicksand paragraph **rewritten**, not deleted: Inter Variable (`@fontsource-variable/inter`, OFL-1.1, conveyed as separate static font files beside the program rather than linked into it) |
| beside it | a **new** paragraph for Grifter recording **the family, the licensee and the licence name** — no SPDX identifier, no `licenses/` file — and naming the archive exclusion and the note that stands in its place |
| the allowlist at `:41` | **no change.** OFL-1.1 is already there, and its own contract is that it is extended deliberately. Say so in the SUMMARY rather than touching it |

**`scripts/deploy.mjs` step 5, two-sided.** It already lists the archive and refuses on a missing
lockfile or any `.planning/` leak. Add, in the same shape:

- the archive **carries no** path matching `/\.(woff2?|ttf|otf|eot)$/i` — the D-13 exclusion working;
- the archive **carries** `static/fonts/README.md` — the note standing in its place. *A one-sided
  check would go green on a deploy that dropped the note as well as the binary, which is a weaker
  Corresponding Source, not a stronger one.*

**Note the correction to §16 and §18:** the archive is written by `scripts/postbuild.mjs:82-89`
(`git archive HEAD`), not by `deploy.mjs`. `deploy.mjs` is where it is **verified**. Both files are
in `files_modified` for exactly that reason; record the correction in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-02-01: The two faces, the one swappable block, and the token that names it</name>
  <files>src/app.css, static/fonts/GRIFTER-Bold.woff2, static/fonts/README.md, .gitattributes</files>

  <read_first>
    - `10-01-SUMMARY.md` — `FONT_SRC` and its `unicode-range` block, verbatim
    - `10-UI-SPEC.md` §1, §5.1, §5.2, §7.1
    - `src/app.css` in full, including the header comment
    - `.gitattributes` in full
  </read_first>

  <action>
**1. Copy the binary.** From the read-only sibling to `static/fonts/GRIFTER-Bold.woff2`. Assert
**13,752 bytes** on arrival and record the number. Nothing is written into the sibling.

**2. Rewrite `src/app.css`** to the structure in `<interfaces>`. Delete the two Quicksand `@import`s.
Write the Inter block with `FONT_SRC` pasted verbatim and the valid `unicode-range` list. Write the
swappable Grifter block with the comment §5.1 gives it, verbatim, because that comment is the
instruction the reversal follows. Add `--font-display` and retarget `--font-sans` inside `@theme`.
Add `--crt-scanline: rgb(0 0 0 / 0.50);` at `:root`, **outside** `@theme`, with a two-line comment
saying why it is here rather than in the component that uses it.

Amend the header comment. It currently opens with a paragraph about Quicksand at `:3-8`; that
paragraph is **rewritten, not deleted** — same rule, two families, and the "never a CDN, the site
works offline, makes no third-party request, asks no `font-src` question of a future CSP" sentence
survives verbatim because it is still true of both faces.

**3. `static/fonts/README.md` — the note that stands in the archive.** Under 60 lines. It says:
which family the bundle needs (`Grifter Bold`, one static `.woff2`), that the binary is deliberately
absent from this archive, why (it is served, not redistributed — `git archive HEAD` publishes this
tree at an unauthenticated URL and that is a different permission from displaying a face on your own
site), who holds the licence, and where to obtain the file. **No SPDX identifier and no licence text.**
State plainly that a build without it falls through `--font-display`'s named fallbacks to
`ui-sans-serif`, which is a weight change and not a layout break, because `font-display: swap` and a
black ground make the flash a weight change rather than a jump.

**4. `.gitattributes`.** One line, in the existing "keep out of the source archive" block, with a
comment naming D-13:

```
static/fonts/GRIFTER-Bold.woff2 export-ignore
```

**5. Build and prove both faces are served.** `npm run build`, then `npm run preview`, then request
`/fonts/GRIFTER-Bold.woff2` and the Inter asset and record **both status codes**. Then
`node scripts/postbuild.mjs` has already run; list the archive with
`tar -tzf build/source-*.tar.gz | grep -Ei "woff|fonts/"` and record that the binary is **absent** and
`static/fonts/README.md` is **present**. This is the D-13 mechanism observed rather than asserted, and
task 3 turns the observation into a gate.
  </action>

  <verify>
    <automated>npm run build && npm run check 2>&1 | grep -Ei "error|warning"</automated>
    Plus recorded: both font assets served 200 by `npm run preview`; `tar -tzf` showing the binary
    absent and the note present; the copied file at exactly 13,752 bytes.
  </verify>

  <done>
`src/app.css` declares exactly two `@font-face` blocks and exactly one `--font-display`. The Quicksand
imports are gone. `--crt-scanline` is at `:root` outside `@theme`. The nine colour tokens and
`--font-mono` are byte-unchanged (prove with `git diff` on those lines). The binary is in
`static/fonts/`, is `export-ignore`d, is served 200, and is absent from the archive while its note is
present. The header comment tells the same story the file does.
  </done>
</task>

<task type="auto">
  <name>Task 10-02-02: G-01 - the identity gate widened in three ways and in no others</name>
  <files>src/lib/ui/identity.spec.ts</files>

  <read_first>
    - `src/lib/ui/identity.spec.ts` in full, especially the X-27 header block that records the last
      time this gate was deliberately widened
    - `10-UI-SPEC.md` §3.2 G-01 and §7.1
  </read_first>

  <action>
Make the three changes in `<interfaces>` and **write the amendment note in the file's header, in
X-27's own shape**, because that is how this repository records a signed-off gate being widened:

> **G-01 — A DELIBERATE AMENDMENT TO A SIGNED-OFF PHASE'S GATE.** This file shipped in Phase 4 with
> eight tokens and two hexes, was widened once in Phase 5 to nine and three, and is widened here in
> three named ways: `--font-sans`'s first family, a new assertion over `--font-display`, and the
> `rgb()`-arguments regex, which now admits `0 0 0` so `--crt-scanline` can live in this file where
> this gate can see it. What the amendment did NOT do: the ladder is still nine and a tenth token
> still fails; the hex set is still three and a fourth hue still fails; `alphaOf()`'s AA loop keeps
> its four members, because `rgb(0 0 0 / 0.5)` is not an alpha of the accent; and the favicon's
> two-hue regex is untouched, so no black and no red enters the mark. **All four non-changes were
> observed red before this note was written.**

Test 7's exact shape, because "every display face" is the wording that must survive the reversal:

```ts
it("--font-display names the family of every display @font-face in the file", () => {
  const faces = [...css.matchAll(/@font-face\s*\{([^}]*)\}/g)].map((m) =>
    normalise(/font-family\s*:\s*([^;]+);/.exec(m[1])?.[1] ?? ""),
  );
  expect(faces.length, "the file declares @font-face blocks").toBeGreaterThan(1);

  const body = normalise((declared("--font-sans") as string).split(",")[0]);
  const display = normalise((declared("--font-display") as string).split(",")[0]);
  const displayFaces = faces.filter((f) => f !== body);

  expect(displayFaces.length, "at least one face is a display face").toBeGreaterThan(0);
  for (const face of displayFaces) {
    expect(face, `every display @font-face is the family --font-display names`).toBe(display);
  }
  expect(faces, "the body face is declared too").toContain(body);
});
```

**Four negative checks**, each reverted with `git checkout -- src/app.css` and each confirmed with
`git diff --quiet -- src/app.css`:

1. `--font-display`'s first family → `"Archivo"` while the `@font-face` stays `"Grifter"`. Expect
   test 7 red, naming both strings. **This is the assertion that makes the D-14 swap two lines
   instead of a hunt.**
2. Add `--color-crt: rgb(0 0 0 / 0.5);` **inside** `@theme`. Expect test 1 red on the tenth token —
   proving the widened `rgb()` regex did not widen the ladder.
3. Add `#0a0a0b` anywhere in the file (the ZONA landing's ground, Open item 4). Expect test 5 red on
   the hex set — proving the third hue is still the last one.
4. Move `--crt-scanline` out of `app.css` into a component `<style>`. Expect `identity.spec.ts`
   **green** — and record that as the finding §7.1 predicts, because it is exactly the silent-green
   hole the placement rule exists to close. (`aesthetic.spec.ts` will catch it from 10-04; say so.)
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/identity.spec.ts</automated>
    Expect **7 passed**. Then `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+1`.
  </verify>

  <done>
`identity.spec.ts` is 7 tests. Three widenings landed, four non-changes observed red, and the X-27
shaped amendment note is in the header. Negative check 4's **green** result is recorded as a finding
with the sentence naming which later gate closes it.
  </done>
</task>

<task type="auto">
  <name>Task 10-02-03: Quicksand out, Grifter recorded, and the archive checked from both sides</name>
  <files>scripts/gen-licenses.mjs, scripts/deploy.mjs, src/lib/ui/font-assets.spec.ts, package.json, package-lock.json, THIRD-PARTY.md</files>

  <read_first>
    - `scripts/gen-licenses.mjs` in full
    - `scripts/deploy.mjs:130-180` — step 5 as it stands
    - `src/lib/ui/font-assets.spec.ts` from 10-01 — the empty allowlist and its five tests
    - `10-UI-SPEC.md` §18 and `10-CONTEXT.md` D-13
  </read_first>

  <action>
**1. Uninstall Quicksand.** `npm rm @fontsource/quicksand`. Confirm no source file references it:
`grep -rn "quicksand" src scripts docs .planning/phases/10-redesign` — the only survivors should be
prose in earlier phases' documents, which are history and are not edited.

**2. `gen-licenses.mjs`, the two paragraph edits** from `<interfaces>`. The allowlist at `:41` is
**not** touched; state that in the SUMMARY, because that allowlist's own contract is that it changes
deliberately and a silent addition would be exactly the wrong precedent.

**3. `npm run licenses`.** `:191-192` does `rmSync` on `licenses/` first, so one rerun clears the
stale Quicksand file. Confirm `licenses/@fontsource/quicksand@5.3.0-LICENSE.txt` is gone,
`licenses/@fontsource-variable/inter@5.3.0-LICENSE.txt` is there, and `THIRD-PARTY.md` names Inter and
Grifter and no longer names Quicksand. **Commit the regenerated `licenses/` and `THIRD-PARTY.md`.**

**4. The allowlist row.** In `font-assets.spec.ts`, one entry:

```ts
const ALLOWED_FONTS: readonly FontRecord[] = [
  {
    path: "static/fonts/GRIFTER-Bold.woff2",
    family: "Grifter Bold",
    licensee: "Botond Sandor / Intech Studio",
    licence: "Hanson Method commercial licence, held by the licensee",
    // NOT an SPDX id, NOT a redistributable file. D-13: the binary is served
    // from the site and export-ignored from `git archive HEAD`; the note at
    // static/fonts/README.md stands in the archive in its place.
    exportIgnored: true,
  },
];
```

Test 4 stops being vacuous the moment this row exists — it now reads `.gitattributes` and asserts the
path is `export-ignore`d. Test 3 asserts the licence name is not SPDX-shaped. Test 5 asserts
`gen-licenses.mjs` names the family. **All five tests were written in 10-01 to accept this row without
being rewritten**; if any of them needs editing beyond adding the row, say so, because that is the
gate having been written to the tree rather than to the contract.

**5. `deploy.mjs` step 5, both directions.** Add the two checks in `<interfaces>`, in the file's own
`console.error` + `process.exit(1)` idiom, with the archive name in each message. Extend the step's
success line to name what it verified: entries, lockfile, no `.planning/`, no font binary, note
present.

**Do not run `npm run deploy`.** Exercise the new code by listing the archive `npm run build` already
produced (`tar -tzf build/source-*.tar.gz`) and confirming by inspection that the two predicates
evaluate as intended on that listing. Record the listing's font-shaped lines (expected: only
`hangar-<sha>/static/fonts/README.md`).

**Two negative checks:**

- Remove the `export-ignore` line from `.gitattributes`, commit nothing, run `npm run build`, and list
  the archive: the binary appears. Expect `font-assets.spec.ts` test 4 red naming the path. Restore
  and confirm `git diff --quiet -- .gitattributes`. *Note in the SUMMARY that `export-ignore` is read
  from `HEAD`'s `.gitattributes` by `git archive`, so this check must be run against a committed
  state or it proves nothing — say which you did.*
- Change the allowlist row's `licence` to `"OFL-1.1"`. Expect test 3 red, saying a font recorded this
  way is one we are not claiming redistribution rights to.
  </action>

  <verify>
    <automated>npm run licenses && npx vitest run --project server src/lib/ui/font-assets.spec.ts src/lib/ui/identity.spec.ts</automated>
    Expect `font-assets.spec.ts` **5 passed**, `identity.spec.ts` **7 passed**, `npm run licenses`
    exiting 0 with no stale file. Then `npm run build`, `npm run test:quick`, `npm run lint`.
  </verify>

  <done>
Quicksand is uninstalled, its `licenses/` file is gone and its hand-written paragraph is rewritten.
Inter and Grifter are both recorded, Grifter without an SPDX identifier. One allowlist row exists and
is `export-ignore`d and named in `gen-licenses.mjs`. `deploy.mjs` step 5 checks the archive from both
sides. The reversal recipe for Open item 2 is written out as a numbered list with its seven edits.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+1`, arithmetic written out.
- `npm run build` green; both font assets served 200 by `npm run preview`; the archive omits the
  binary and carries the note.
- `npm run licenses` exits 0 and `licenses/` holds no Quicksand file.
- `git diff --stat HEAD -- src/vendor/` empty; `git status --porcelain` empty after both negative
  checks are reverted.
</verification>

<success_criteria>
1. Exactly one `--font-display` and exactly one display `@font-face`, asserted equal by a test whose
   wording survives the reversal.
2. Nine tokens, three hexes, no fourth hue — after a font swap and after `rgb(0 0 0 / 0.50)` entered
   the file. All four non-changes observed red.
3. The Grifter binary is served, `export-ignore`d, recorded by family/licensee/licence-name, and
   absent from the archive with its note present — checked from both sides.
4. Quicksand is gone from the tree, `node_modules`, `licenses/` and `THIRD-PARTY.md`, in one commit.
5. The A-02 reversal is written out as seven edits, none of them a component and none of them a test
   rewrite.
6. Every string count in this plan was produced by a script and its number stated.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-02-SUMMARY.md` carrying the eleven-name
block updated, `FONT_SRC` as shipped, the byte size of both font assets, the archive listing's
font-shaped lines, the four G-01 negative checks with their messages (including the deliberately
green one and the gate that closes it), and the numbered A-02 reversal recipe.
</output>
