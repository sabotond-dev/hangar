---
phase: 10-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/routes/dev/type/+page.svelte
  - src/lib/ui/font-assets.spec.ts
  - src/lib/ui/aesthetic.spec.ts
autonomous: true
requirements: [IDENT-01, IDENT-02, FOUND-02]

must_haves:
  truths:
    - "Every number this phase spends fourteen plans arithmetic-ing from was measured on this machine, on a clean tree, before a single component was written - not carried from a document."
    - "The one font specifier the whole type settlement rests on is proved to resolve in a production build and to be fetched with a 200 from a preview server, so a stylesheet that works in dev and 404s in production is impossible."
    - "A font binary committed anywhere in the tracked tree outside licenses/ fails a test, so a file npm run licenses cannot see can no longer reach the public source archive."
    - "Coverflow.svelte's three load-bearing rules are asserted in BOTH directions before anything in this phase touches the page it lives on: nothing grouping is added to the 3D context, and nothing is deleted from the band that the row is built on."
  artifacts:
    - path: "src/lib/ui/font-assets.spec.ts"
      provides: "The Wave 0 font-asset gate: no tracked font binary outside licenses/ without an allowlist entry carrying a licence record. 5 tests, fixed count."
    - path: "src/lib/ui/aesthetic.spec.ts"
      provides: "Source scan 4 only - Coverflow.svelte asserted in both directions. 1 test in this plan, 7 after 10-04."
    - path: "src/routes/dev/type/+page.svelte"
      provides: "The type probe: a 372px content column at 16px/1.5 that CH_PER_LINE is measured against, and the surface the font specifier is proved on."
  key_links:
    - from: "src/lib/ui/aesthetic.spec.ts"
      to: "src/lib/ui/Coverflow.svelte"
      via: "the .band / .stage / .slot rules read out of the source and asserted present as well as absent"
      pattern: "overflow-clip-margin: 6px"
    - from: "src/lib/ui/font-assets.spec.ts"
      to: "git ls-files"
      via: "the tracked tree walked for font-shaped paths, not the working directory"
      pattern: "ls-files"
---

<objective>
Measure everything the phase's arithmetic depends on, prove the one unproven specifier, and land the
two gates that must exist before a component is written.

Wave 0. Nothing here changes a pixel. Three kinds of work:

**The block.** Phase 9 closed on 2026-09-07 and nothing has re-measured the tree since. Eleven names
are measured on a clean tree — the seven Phase 9 carried, plus `CH_PER_LINE`, `FONT_SRC`,
`BASE_SWEEP_WALL` and `PREV_SWEEP_WALL`. Every later plan asserts a carried name plus a stated delta
and never an absolute total.

**The two measurements the spec calls provisional.** `CH_PER_LINE` is 46 in `10-UI-SPEC.md` §12.1 and
the document says so out loud: *"Wave 0 measures it in both engines and pins it… A wrong number here
reflows a device panel mid-install."* Five reservations and four caps are that number's arithmetic.
And the Inter `@font-face`'s `src: url(…)` is, in the spec's own words, **unproven**: a bare package
specifier inside `url()` is not a resolution Vite documents, so it can ship verbatim into the built
stylesheet and 404 in production while `vite dev` happens to work. Three candidates are named; this
plan proves exactly one, under `vite build` **and** `vite preview`.

**The two gates.** A font binary is invisible to `npm run licenses`, which inspects the npm tree only
— so a hand-committed `.woff2` sails through the licence gate, the build and the deploy and lands in
the public source tarball. And `Coverflow.svelte` is the one file this phase promises not to touch;
its three header rules become a test **now**, in both directions, so every later plan works with a
net under it.

**What this plan deliberately does NOT do.** It does not edit `src/app.css`, does not uninstall
Quicksand, does not write a `@font-face` into the shipped stylesheet, and does not author a CRT
layer. It adds `@fontsource-variable/inter` and one `/dev/` route, and everything else it writes is a
test or a number.

**V-01, stated here because this is the plan it changes.** `10-UI-SPEC.md` §17 asks for
`aesthetic.spec.ts`'s **seven** source scans in Wave 0. Six of them scan for `.crt-band`,
`body::before`, the pad-frame `::after` and the noise tile, none of which exists yet, and each carries
a non-vacuity floor — so six would be red on arrival, which is a scheduled failure rather than a
gate. **Scan 4 lands here, green, because it is the one that protects the fragile thing before
anything touches it.** Scans 1, 2, 3, 5, 6 and 7 land in 10-04, in the same commit as the layer each
one gates. Never later.

Purpose: IDENT-01's font swap made safe to attempt; IDENT-02's protected file protected first.
Output: two new specs (6 tests), one dev route, one dependency, eleven measured names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-CONTEXT.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/09-twenty-configurations/09-10-SUMMARY.md
@.planning/phases/09-twenty-configurations/09-VERIFICATION.md
@src/app.css
@src/lib/ui/Coverflow.svelte
@src/lib/ui/identity.spec.ts
@scripts/gen-licenses.mjs

<standing_rules>
- **Never edit anything under `src/vendor/`.**
- **Never touch a sibling repository.** `gridstrument-landing` and `fulfilment-dashboard` are
  read-only sources; copy files out of them, never write into them.
- **No agent connects to or writes to a device, and no agent deploys.**
- No Claude or Anthropic attribution in any file, comment or commit message, and no co-author
  trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults per `.prettierrc`. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`, the message file in the scratchpad.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- e2e at `--workers 3`; remove `test-results/` by hand and confirm `git status --porcelain` empty.
- **State every count as a carried name plus a delta**, never as an absolute total. This plan is the
  one that measures the carried names.
- The project spends no money.
</standing_rules>

<interfaces>
**The three `url()` candidates, verbatim from `10-UI-SPEC.md` §5.1**, in the spec's order of
preference. Prove **one**; record which and why in the SUMMARY as `FONT_SRC`.

| # | Candidate | Cost if it works | How it fails |
|---|---|---|---|
| (a) | `@import "@fontsource-variable/inter/wght.css";` | six unused subsets ship (all seven `unicode-range` blocks, latin plus six) | none — bare specifiers **are** resolved in `@import`. The cost is bytes, not correctness |
| (b) | `src: url("../node_modules/@fontsource-variable/inter/files/inter-latin-wght-normal.woff2")` | none; Vite fingerprints it into `_app/immutable/assets/` | a relative path out of `src/` is unusual; confirm the emitted asset exists and is fetched |
| (c) | a build-time copy into `static/fonts/`, `url("/fonts/inter-latin-wght-normal.woff2")` | a second tracked binary — **which the font-asset gate in task 3 will then refuse unless allowlisted** | it makes the OFL file a tracked artefact for no reason; prefer (a) or (b) |

**(b) is the one to try first** and (a) is the fallback, because (b) keeps the latin-only subset the
spec measured at 48,256 B and (a) does not. **(c) is a last resort** and, if taken, its path needs an
allowlist row with an OFL-1.1 record in task 3.

**What "proved" means, exactly.** Not "the page looks right in `vite dev`". All four:

1. `npm run build` completes.
2. The built stylesheet under `build/_app/immutable/assets/*.css` contains a `url(...)` whose target
   is a real path under `build/` — `grep -o "url([^)]*)"` the built CSS and resolve each target.
3. `npm run preview` (which runs `wrangler dev` on `./build`) serves that target with **HTTP 200 and
   a `font/woff2` or `application/octet-stream` content type**, checked with a real request.
4. The `unicode-range` declaration in the block is **valid CSS** — the spec's own draft carries
   `unicode-range: /* … */;`, which is a syntax error that silently drops the whole descriptor.

**The measurement `CH_PER_LINE` is, exactly.** Characters of Body per line, where Body is
`font-family: var(--font-sans)` at `font-size: 16px; line-height: 1.5; font-weight: 400`, inside a
**372px** content column — the panel's, recorded in `06-UI-SPEC.md` and used by every twin in §12.
Measure it the way the twins consume it: render a long lowercase-heavy sentence, read
`element.getClientRects().length` for the line count and divide the code-point count by it, at
several lengths, and take the **floor** of the smallest ratio observed. Do it in `chromium` **and**
`webkit-phone`; **the smaller of the two is `CH_PER_LINE`**, because a reservation that is right in
one engine and one line short in the other reflows a device panel mid-install.

**The nine numbers that fall out of it** (`10-UI-SPEC.md` §12.2), stated as formulas so this plan
records outputs rather than opinions. The Body line box is `16 × 1.5 = 24px`, so a reservation is
`ceil(longest / CH_PER_LINE) × 24`.

| Name | Formula | At the provisional 46 |
|---|---|---|
| header note | `ceil(37 / CH) × 24` | **24px** |
| honesty slot | `ceil(90 / CH) × 24` | **48px** |
| `PUT BACK` cell | `ceil(101 / CH) × 24` | **72px** |
| `KEEP` cell | `ceil(82 / CH) × 24` | **48px** |
| `CLEAR` cell | `ceil(86 / CH) × 24` | **48px** |
| `HONESTY_CAP` | `2 × CH` | **92** |
| `PUT_BACK_CAP` | `3 × CH` | **138** |
| `KEEP_CAP` | `2 × CH` | **92** |
| `CLEAR_CAP` | `2 × CH` | **92** |

**If the measurement is not 46**, recompute all nine and record both columns. Plan 10-03 applies them
and asserts them; this plan only measures and tabulates. **No component is written against a
provisional number.**

**`Coverflow.svelte`'s five geometry literals**, at the lines they sit on today. Confirm each with
`grep -n` before writing the scan; a moved line number means somebody edited the file and this plan
should say so rather than proceed.

| Literal | Line | Rule |
|---|---|---|
| `clamp(260px, 52vmin, 560px)` | `:949` | `.band { --pad-hero: … }` |
| `min(100vw, 1280px)` | `:951` | `.band { inline-size: … }` |
| `overflow-clip-margin: 6px` | `:963` | `.band` |
| the four-stop `mask-image` linear-gradient | `:964-970` | `.band` |
| `translateY(-24px)` | `:979` | `.band.chosen` |

**The permitted set scan 4 asserts PRESENT, by name** (§8.2): `.band`'s `overflow: clip` and
`mask-image`; a `.slot`'s inline `opacity` and `filter: brightness(`. And asserts **ABSENT** on
`.stage`: `filter`, `mix-blend-mode`, an `opacity` below 1, `mask-image`, `contain: paint`.
*A scan that only forbade would go green on a tidy-up that deleted the band's mask.*

**The font census, as it stands today**, verified by the planner on 2026-09-08:
`git ls-files | grep -Ei "\.(woff2?|ttf|otf)$"` returns **nothing**. The only font-shaped tracked path
is `licenses/@fontsource/quicksand@5.3.0-LICENSE.txt`, which is a `.txt`. §17's phrasing is loose;
state the census correctly in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-01-01: The eleven-name block, measured on a clean tree before anything grows</name>
  <files>(no source file is modified; this task writes numbers into the SUMMARY)</files>

  <read_first>
    - `.planning/phases/09-twenty-configurations/09-10-SUMMARY.md` — its closing block
    - `.planning/phases/09-twenty-configurations/09-VERIFICATION.md` — the gates table it observed
    - `.planning/phases/10-redesign/10-VALIDATION.md` — the eleven-name table
  </read_first>

  <action>
**Precondition first.** Confirm Phase 9 has closed: `09-10-SUMMARY.md` and `09-VERIFICATION.md` on
disk, `.planning/ROADMAP.md` marking Phase 10 as the current phase, and `git status --porcelain`
empty. If the tree is dirty, stop and say so — every number below is a statement about a clean tree.

**Measure, in this order, recording free memory beside each:**

1. `npm run check 2>&1 | grep -Ei "error|warning"` → must print nothing. Record `BASE_CHECK` (the
   file count) as provenance only.
2. `npm run lint` → clean.
3. `npm run test:quick 2>&1 | node scripts/check-counts.mjs 74 780` → this is the reconciliation
   against 09-VERIFICATION. **If it disagrees, the observation is this phase's**: record what was
   actually observed and set `BASE_FILES` / `BASE_TESTS` to it, with a sentence naming the
   disagreement. Set `PREV_FILES = BASE_FILES` and `PREV_TESTS = BASE_TESTS`.
4. `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` → `BASE_SWEEP`. **Time it with a
   wall clock** and record `BASE_SWEEP_WALL` in seconds, with free memory before and after. This is
   the number the whole of 10-08 is compared against and it must be taken **before the sweep grows**.
   Set `PREV_SWEEP_WALL = BASE_SWEEP_WALL`.
5. `npm run build` → record the wall time and confirm `build/source-<sha>.tar.gz`,
   `build/THIRD-PARTY.md` and `build/licenses/` exist.
6. `npm run test:quick` again (three specs read `build/`) → confirm the same counts.
7. `npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright 89` →
   `BASE_E2E`, with the wall time and free memory. Set `PREV_E2E = BASE_E2E`, named as measured here.
   Then remove `test-results/` by hand and confirm `git status --porcelain` empty.

**Record three provenance facts the phase will lean on:**

- `git ls-files | grep -Ei "\.(woff2?|ttf|otf)$"` → state the exact output (expected: empty) and, as a
  separate line, the one font-shaped `licenses/` path with its extension named. **Do not restate
  §17's phrasing; state what the command printed.**
- `ls ~/.claude/skills/impeccable` → record the file list. Then
  `git status --porcelain -- src static` → must be empty, and `npm ls impeccable` → must be empty.
  **D-01 is satisfied by the skill being present and by `10-UI-SPEC.md` having been authored through
  it. No plan runs `npx impeccable install`**, and this line is the record of why.
- `git diff --stat 08c285e..HEAD -- src/vendor/` → empty.

**Record the catalog's shape** so later plans can quote it: `CATALOG.length`, the preset/lua split,
`LISTING.length`, `FRONT_DOOR.length`, `EXCLUDED_FROM_ROW.length`, `KNOWN_TAGS.length`, and the count
of `kind: "colour"` occurrences in `src/lib/catalog/entries/` broken down by file. The planner
measured 45 across 25 files with three/two/one splits of 3/14/8; reconcile and say so.
  </action>

  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs BASE_FILES BASE_TESTS</automated>
    Plus: `npm run test:sweep` prints `BASE_SWEEP`; `npx playwright test --workers 3` prints
    `BASE_E2E`; `git status --porcelain` is empty at the end.
  </verify>

  <done>
Eleven names measured and written into the SUMMARY with free memory and wall clocks beside each:
`BASE_FILES`, `BASE_TESTS`, `PREV_FILES`, `PREV_TESTS`, `BASE_SWEEP`, `BASE_SWEEP_WALL`,
`PREV_SWEEP_WALL`, `BASE_E2E`, `PREV_E2E`, `BASE_CHECK` (provenance), and `CH_PER_LINE` /
`FONT_SRC` marked **pending task 2**. The font census, the `impeccable` record and the catalog shape
recorded. Any disagreement with 09-VERIFICATION named rather than smoothed.
  </done>
</task>

<task type="auto">
  <name>Task 10-01-02: FONT_SRC proved under build and preview; CH_PER_LINE measured in both engines</name>
  <files>package.json, package-lock.json, src/routes/dev/type/+page.svelte</files>

  <read_first>
    - `10-UI-SPEC.md` §5.1 (the three candidates and the comment that names them) and §12 in full
    - `src/app.css:1-10` — how Quicksand is imported today, and the "never a CDN" rule
    - `src/routes/dev/tune/+page.svelte` — the house shape of a `/dev/` probe route
    - `src/lib/ui/ChosenPanel.svelte` — where the 372px content column is declared
  </read_first>

  <action>
**1. Install Inter, and only Inter.**

```
npm i -D @fontsource-variable/inter@5.3.0
```

Exact version, no caret drift beyond what the lockfile records. **Do not uninstall Quicksand** — that
is 10-02's, and this plan must leave the shipped site rendering exactly as it does today. Confirm
`@fontsource-variable/inter` resolves and that `node_modules/@fontsource-variable/inter/files/`
contains `inter-latin-wght-normal.woff2`; record its byte size (the spec says 48,256).

**2. Build the probe route, `src/routes/dev/type/+page.svelte`.**

A sixth `/dev/` route beside the five that exist. It must:

- declare its own `@font-face` for Inter Variable using **candidate (b)** first, with the
  `unicode-range` block from §5.1 written out as **valid CSS** (the spec's draft carries
  `unicode-range: /* … */;`, which is a syntax error that drops the descriptor silently — write the
  real list);
- render a `<div data-testid="measure-column">` at exactly `inline-size: 372px`, `font-size: 16px`,
  `line-height: 1.5`, `font-weight: 400`, `font-family: "Inter Variable", …`, containing several
  paragraphs of the **real strings the twins reserve for** — `PUT BACK`'s 101-character line,
  `KEEP ON DEVICE`'s 82, the 90-character honesty form, `RECONNECT_OFFER`'s 37 and CLEAR's 86 — each
  in its own `<p data-testid="measure-{n}">`;
- render the same column a second time in Quicksand, `data-testid="measure-column-quicksand"`, so the
  43-vs-46 comparison is observed rather than assumed;
- be `export const prerender = true` like its siblings, and reach nothing under `src/vendor/`.

**3. Prove the specifier. All four steps, in order, and record each.**

```
npm run build
```

Then, on the built output:

- `grep -ro "url([^)]*)" build/_app/immutable/assets/*.css` — list every `url()` target the built
  stylesheets carry. For each, resolve it against `build/` and state whether the file exists.
  **A target that is still a bare package specifier is the failure this step exists to catch.**
- `npm run preview` in the background, then request the resolved target and record the **status code
  and content type**. A 200 with a woff2 body is the pass. A 404 is candidate (b) failing; fall back
  to (a) and repeat from `npm run build`.
- Stop the preview server and confirm nothing is left on 4173/4174.

Record `FONT_SRC` as the exact string that worked, plus the built asset path and its byte size. If
(c) was taken, say so loudly: it adds a tracked binary and task 3's gate will need an allowlist row
with an OFL-1.1 record.

**4. Measure `CH_PER_LINE`.**

With `npm run preview` serving `/dev/type/`, in **both** Playwright projects, for each measure
paragraph: read `el.getClientRects().length` and `[...el.textContent].length`, compute
`floor(chars / lines)`, and take the **minimum across all paragraphs and both engines**. That
minimum is `CH_PER_LINE`. Record the full table — per string, per engine, chars, lines, ratio — and
the Quicksand column beside it.

A one-off Playwright script under the scratchpad is the right instrument here; **do not add a test
file for it.** This is a measurement, not a gate — the gate on these numbers is 10-03's twins.

**5. Re-derive the nine dependent numbers** from the measured `CH_PER_LINE` using the formulas in
`<interfaces>`, and tabulate them beside the provisional column. If any differs from the provisional,
say which, by how much, and which component it moves.
  </action>

  <verify>
    <automated>npm run build && npm run check 2>&1 | grep -Ei "error|warning" ; npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS</automated>
    Plus, recorded in the SUMMARY rather than asserted: the built `url()` target resolving to a real
    file under `build/`, that file served **200** by `npm run preview`, and the two-engine
    `CH_PER_LINE` table. `npm run test:quick` must be unchanged — this task adds no test.
  </verify>

  <done>
`FONT_SRC` is one exact string, proved under `vite build` **and** `vite preview` with the asset's
status code stated. The `unicode-range` block is valid CSS and is quoted in the SUMMARY ready for
10-02 to paste. `CH_PER_LINE` is a single integer, the minimum over both engines, with its full
measurement table and the Quicksand comparison. The nine dependent numbers are re-derived and
tabulated against the provisional column. `/dev/type/` prerenders and `npm run check` is silent.
  </done>
</task>

<task type="auto">
  <name>Task 10-01-03: The font-asset gate, and Coverflow protected in both directions</name>
  <files>src/lib/ui/font-assets.spec.ts, src/lib/ui/aesthetic.spec.ts</files>

  <read_first>
    - `src/lib/ui/identity.spec.ts` in full — the `strip`, `normalise`, `declared` and non-vacuity
      idiom every scan in this phase copies
    - `src/lib/catalog/front-door.spec.ts` — the import-scan idiom
    - `scripts/gen-licenses.mjs:30-45` (the allowlist and its contract) and `:160-200`
    - `src/lib/ui/Coverflow.svelte:1-30` (the three header rules) and `:942-1048` (the whole `<style>`)
  </read_first>

  <action>
**1. `src/lib/ui/font-assets.spec.ts` — five tests, fixed count.**

The gate `npm run licenses` cannot be: `license-checker-rseidelsohn --production` inspects the npm
tree and has **no visibility into `static/` or `src/lib/assets/`**, so a hand-committed `.woff2`
sails through the licence gate, the build and the deploy and lands in the public source tarball.

The walk is over **`git ls-files`**, not the working directory: an untracked font is not in the
archive and is not this gate's business, and a `.gitignore`d one is not either.

```ts
const tracked = execFileSync("git", ["ls-files"], { encoding: "utf8" })
  .split(/\r?\n/)
  .filter(Boolean);
const FONT_SHAPED = /\.(woff2?|ttf|otf|eot)$/i;
```

Declare the allowlist **in this file**, as data, one row per permitted path:

```ts
/**
 * Every font binary permitted in the tracked tree, with the record that
 * permits it. A row is added deliberately, in a commit, with a reason - the
 * same contract gen-licenses.mjs:41's SPDX allowlist carries.
 *
 * A row is NOT an SPDX identifier. D-13's mechanism records the FAMILY, the
 * LICENSEE and the LICENCE NAME for a font that is served but not
 * redistributed, and names the archive exclusion that keeps it out of
 * `git archive HEAD`. An SPDX id here would be a claim we may not make.
 */
const ALLOWED_FONTS: readonly FontRecord[] = [];  // empty today; 10-02 adds one
```

The five tests:

1. **Every tracked font-shaped path outside `licenses/` is allowlisted.** Non-vacuity first:
   `expect(tracked.length, "the tracked tree was actually read").toBeGreaterThan(100)`. Then, for each
   font-shaped path, assert a matching allowlist row, with the offending path in the message.
2. **Every allowlist row points at a path that exists and is tracked.** A retired row is red too —
   the same both-directions rule `copy.spec.ts` uses on `KNOWN_TAGS`.
3. **Every allowlist row carries a non-empty family, licensee and licence name**, and its licence
   name is **not** an SPDX identifier (assert it does not match `/^[A-Z][A-Za-z0-9.+-]*-[0-9]/`),
   because a font recorded this way is precisely one we are *not* claiming redistribution rights to.
4. **Every allowlisted path is `export-ignore`d** in `.gitattributes`, read from the file, so the
   record and the exclusion cannot drift apart. Vacuously true today with an empty allowlist —
   assert `ALLOWED_FONTS.length === 0 || …` and state in the message that the vacuous branch is
   closed by 10-02.
5. **`scripts/gen-licenses.mjs` names every allowlisted family**, read from that file's text, so the
   `THIRD-PARTY.md` record cannot be dropped while the file stays.

**2. `src/lib/ui/aesthetic.spec.ts` — one test today, seven after 10-04.**

Write the file's header so a later reader knows why it is one and not seven:

> Scan 4 is here in Wave 0 because it is the only one of the seven that can be true on a tree with no
> CRT, and it is the one that protects the file this phase promises not to touch. Scans 1, 2, 3, 5, 6
> and 7 land in 10-04, in the same commit as the layer each one gates — never afterwards, because
> a gate that arrives after the thing it gates has already shipped is a comment.

**Scan 4 — `Coverflow.svelte`, both directions.** Parse the component's `<style>` into
`{ selector, body }` rules with `identity.spec.ts`'s `strip` and `normalise` applied first.

- Non-vacuity: the parse found more than twenty rules, and found rules whose selector contains
  `.band`, `.stage` and `.slot`.
- **ABSENT on `.stage`**: no `filter`, no `mix-blend-mode`, no `mask-image`, no `contain: paint`, and
  no `opacity` whose value parses below 1. Check every rule whose selector contains `.stage`,
  including the reduced-motion block.
- **PRESENT on `.band`**: `overflow: clip` and a `mask-image:` declaration. Message names both the
  declaration and the file.
- **PRESENT on a slot**: the component's markup carries an inline `opacity` and an inline
  `filter: brightness(` — these are inline style attributes at `:841-846`, not `<style>` rules, so
  scan the template text, not the parsed rules.
- One sentence in the failure message for each direction saying which it is, so a red test does not
  leave a reader guessing whether something was added or deleted.

**3. The two negative checks.** `git add` both new files first — on an untracked path
`git checkout --` fails outright and `git diff --quiet` passes vacuously.

- Create an empty `static/fonts/probe.woff2`, `git add` it, run `font-assets.spec.ts`. Expect test 1
  red, naming the path. Then `git rm --cached` it, delete it, and confirm
  `git status --porcelain` empty.
- Delete the `mask-image` declaration from `Coverflow.svelte`'s `.band` rule, run
  `aesthetic.spec.ts`. Expect scan 4 red, naming `.band` and `mask-image`. Restore with
  `git checkout -- src/lib/ui/Coverflow.svelte` and confirm `git diff --quiet -- src/lib/ui/Coverflow.svelte`.

Record both exit codes and both failing messages verbatim in the SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/font-assets.spec.ts src/lib/ui/aesthetic.spec.ts</automated>
    Expect `font-assets.spec.ts` **5 passed** and `aesthetic.spec.ts` **1 passed**. Then
    `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES+2 PREV_TESTS+6`, with the
    arithmetic written out, and `npm run lint` clean.
  </verify>

  <done>
`font-assets.spec.ts` is 5 tests with an empty allowlist and both directions asserted; it goes red on
a planted `.woff2` with the path named, and green again after it is removed. `aesthetic.spec.ts` is
1 test asserting `Coverflow.svelte` in both directions; it goes red on a **deleted** band mask, which
is the direction the spec's own first pass had backwards. `PREV_FILES` and `PREV_TESTS` are
re-measured and the `+2 / +6` arithmetic is written out. `git status --porcelain` empty.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing.
- `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS` with the `+2 / +6`
  arithmetic written out against the block measured in task 1.
- `npm run build` green; `/dev/type/` prerenders; the built `url()` target exists under `build/` and
  is served 200 by `npm run preview`.
- `git diff --stat HEAD -- src/vendor/` empty.
- `git status --porcelain` empty after both negative checks are reverted.
</verification>

<success_criteria>
1. Eleven names measured on a clean tree, with free memory and wall clocks, and any disagreement with
   09-VERIFICATION named rather than smoothed.
2. `BASE_SWEEP_WALL` taken **before** the sweep grows — the one number that cannot be recovered later.
3. `FONT_SRC` is one exact string, proved under `vite build` **and** `vite preview`, with the asset's
   status code stated, and its `unicode-range` block is valid CSS.
4. `CH_PER_LINE` is one integer, the minimum over both engines, with its full table and the nine
   dependent numbers re-derived beside the provisional column.
5. Two gates exist and are green: five font-asset tests and one both-directions `Coverflow` scan.
6. Both negative checks observed red on the intended test, with the message recorded, and both files
   restored byte-identical.
7. Nothing under `src/vendor/` moved; no component was written; `src/app.css` is byte-unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-01-SUMMARY.md` carrying the eleven-name
block in full — every later plan in this phase reads it — plus the `CH_PER_LINE` table, the
`FONT_SRC` proof with its four steps' outputs, the nine re-derived numbers, the font census, the
`impeccable` record, the catalog shape, and both negative checks with their exit codes and messages.
</output>
