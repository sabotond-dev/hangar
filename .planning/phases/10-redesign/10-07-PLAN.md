---
phase: 10-redesign
plan: 07
type: execute
wave: 7
depends_on: ["10-06"]
files_modified:
  - src/lib/browse/sort.ts
  - src/lib/browse/sort.spec.ts
  - src/lib/browse/query.ts
  - src/lib/browse/query.spec.ts
  - src/lib/browse/filter.ts
  - src/lib/browse/filter.spec.ts
  - src/lib/catalog/listing.ts
  - src/lib/catalog/listing.spec.ts
  - src/lib/ui/BrowseToolbar.svelte
  - src/lib/ui/FacetRow.svelte
  - src/lib/ui/TagChip.svelte
  - src/lib/ui/CatalogCard.svelte
  - src/lib/ui/FrontDoor.svelte
  - src/lib/ui/browse-ui.spec.ts
  - src/routes/browse/+page.svelte
  - e2e/browse.e2e.ts
  - e2e/browse-webkit.e2e.ts
autonomous: true
requirements: [CAT-02, CAT-03, CAT-04]

must_haves:
  truths:
    - "The toolbar shows two captioned facet rows of sixteen chips, and every one of them fits above the grid at every width with no disclosure."
    - "Newest is gone from the sort control, the comparator, the URL's accepted values and the browse e2e walk, and addedAt has left the browse projection while staying on the entry as provenance."
    - "No link already shared lands on nothing: a mapped legacy tag becomes its facet chip, an unmapped one becomes the search query, and a retired sort falls back silently."
    - "The front door is the entry to browse rather than its rival: the ring stays at eight and gains a FOR link row that keeps the page prerendered and import-free."
    - "Pressing two chips inside one facet widens the grid and pressing one in each facet narrows it, and the browse e2e asserts both - 05.1's AND-only walk is rewritten by name, not left to pass by luck."
    - "Every e2e site that names the retired sort, the old filterListing arity or the written ?tag= address is repaired inside its own title - six sites across two files, and no test( or test.describe( line is added, removed or renamed."
  artifacts:
    - path: "src/lib/ui/FacetRow.svelte"
      provides: "One captioned facet row - a checkbox group on /browse/, a link row on / - with its own role=group and aria-labelledby"
      contains: "role=\"group\""
    - path: "src/lib/browse/query.ts"
      provides: "?for= and ?feels=, the legacy ?tag= migration, and G-10's ruling that an unmapped value lands in q"
  key_links:
    - from: "src/lib/ui/BrowseToolbar.svelte"
      to: "src/lib/browse/facets.ts"
      via: "the standing rows are the facet members, not a count-derived list"
      pattern: "FOR_TERMS"
    - from: "src/lib/ui/FrontDoor.svelte"
      to: "/browse/"
      via: "ten <a href=\"/browse/?for={term}\"> links, so the front door stays prerendered and import-free"
      pattern: "browse/\\?for="
---

<objective>
Browse at sixteen chips and two sorts, and a front door that is the entry to it rather than its rival.

Wave 7. Blocked on 10-06, which closed the vocabulary this plan renders.

Four pieces, and three of them are removals:

**D-11 — Newest goes.** `addedAt` has three distinct values across thirty-six entries and one of them
covers twenty, so ordering by date says nothing. `sort.spec.ts:125` **self-adjusts** — it already
reads `BROWSE_SORTS.length * LISTING.length * (LISTING.length - 1)` and follows the constant. What
needs editing is `:82`, whose literal `["featured", "newest", "name"]` and the words "the three
sorts" are hand-written, and the `NEWEST` date-block test at `:179`, which 09-02 restructured and
which is **retired by name**. That asymmetry is worth a negative check of its own.

**And the e2e delta is 0. Measured across six sites, not two.** D-11 and A-19 reach seven declarations
in `e2e/` — `browse.e2e.ts:268`, `:121`, `:332-404`, `:852-888`, `:1029-1120` and `:437`, plus
`browse-webkit.e2e.ts:79` — and **not one of them is a `test(` or `test.describe(` line**. `:268` is a
single title whose body walks FEATURED, NAME, NEWEST at `:289-311` and FEATURED again, so removing
Newest deletes assertions *inside* it; `:1029-1120` is a whole test built on the retired sort and on
`filterListing`'s old arity, and it is **restated inside its own title** rather than dropped, because
D-19's one-slot-two-labels proof lives in its second half. So this plan's e2e delta is **0**, and the
phase total is **`BASE_E2E + 12`**, not `+10`.

> **Retracted, 2026-09-08.** Revision 1 of this plan asserted here that `browse-webkit.e2e.ts` never
> names `newest`. It does, at `:79`, in its own copy of `orderOf` — see task 10-07-03 item 5b. The
> delta was right; the evidence offered for it was two sites out of six and one of the two was a false
> claim about a file that was already in `files_modified`.

**A-19 — the toolbar's tag row becomes two facet rows.** `FOR` first, ten chips; `FEELS` second, six.
The count-derived row and the outsider chip retire together, because the vocabulary is closed and no
active facet member can fall outside the standing rows. `disabledTags()` stays and becomes rare.

**A-20 — no shared link is dropped on the floor.** `?tag=` is accepted for one release. A **mapped**
value becomes its facet chip. An **unmapped** value becomes `?q=`. Dropping it silently would break
exactly the links most likely to exist, *because a singleton tag is what somebody sends when they mean
this one card*.

**A-22 — the ring stays at eight and gains a `FOR` link row**, answering Open item 3. The ten workflow
terms render on `/` as **links**, not checkboxes: a link keeps the front door prerendered, keeps its
`<head>` intact, and keeps the page import-free. The reversal — growing the ring — is blocked by
`front-door.spec.ts:110-112`'s `preview === "padsim"` requirement, which twenty-seven of the
thirty-six entries fail; record that as a deferred item rather than pre-empting it.

Purpose: CAT-02 amended to two sorts; CAT-03 and CAT-04 rendered.
Output: one sort removed, two facet rows, one URL migration, one link row, one AND→OR e2e rewrite,
and **no e2e title lost**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/10-redesign/10-06-SUMMARY.md
@src/lib/browse/sort.ts
@src/lib/browse/query.ts
@src/lib/browse/filter.ts
@src/lib/browse/facets.ts
@src/lib/ui/BrowseToolbar.svelte
@src/lib/ui/CatalogCard.svelte
@src/lib/catalog/front-door.ts

<standing_rules>
- **Never edit anything under `src/vendor/`.** **Never touch a sibling repository.**
- **`src/lib/ui/Coverflow.svelte` is not edited.** Prove it with `git diff --stat`.
- **No agent connects to or writes to a device, and no agent deploys.**
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- e2e at `--workers 3`; remove `test-results/` by hand and confirm `git status --porcelain` empty.
- **`front-door.ts` gains no import**, and `facets.ts`, `sort.ts`, `filter.ts`, `query.ts` and
  `listing.ts` each keep their single `import type` and no other specifier. Their source scans say so.
- **State every count as a carried name plus a delta.**
- The project spends no money.
</standing_rules>

<interfaces>
**D-11's edit sites, each named:**

| What | To |
|---|---|
| `BrowseSort` | `"featured" \| "name"` |
| `BROWSE_SORTS` | `["featured", "name"]` |
| `newestOrder`, `orderFor`'s middle branch | **deleted** |
| `sort.spec.ts:125` | **no edit** — it follows the constant, `3 × n × (n−1)` becomes `2 × n × (n−1)` on its own |
| `sort.spec.ts:82` | its literal `["featured", "newest", "name"]` and the string "the three sorts, in the toolbar's order" are hand-written and both move |
| `sort.spec.ts:179` | the `NEWEST` date-block test, **retired by name** with a comment saying 09-02 built it and D-11 retired it |
| `BrowseToolbar.svelte` | options `FEATURED` (8) · `NAME` (4) |
| `?sort=` accepted values | `featured`, `name`. `?sort=newest` falls back to the default **silently** — a link carrying a sort nobody ships opens on the catalog, not on an error. That rule already exists in `query.ts` and needs no code change; assert it |
| `ListingEntry.addedAt` | **removed from the browse projection**, kept in the catalog entries as provenance (D-b). `listing.spec.ts`'s equality loop drops one field and **no entry file is touched** |

**G-10 — the tenth gate amendment, which `10-UI-SPEC.md` §3.2 does not list.** `query.ts:104-110`
drops an unknown `?tag=` **on purpose**, under 05.1's W-12, with a written argument beside it. §9.4
reverses that. Rewrite W-12 by name — its paragraph stays and gains its amendment — and rule the
precedence the spec leaves open:

> **An explicit `?q=` always wins.** An unmapped `?tag=` becomes `q` only when `q` is empty. Two
> unmapped tags join with a single space, in address order. A **mapped** value becomes its facet chip
> and never touches `q`. It is never an error, never a 404 and never a message: *a stale shared link
> should land on a working catalog, not on an apology.*

`BrowseQuery` becomes `{ sort, q, for: string[], feels: string[] }`. `serialiseBrowseQuery` writes
`sort`, then `q`, then one `for` per active term, then one `feels` per active term, each in activation
order — chips are individually removable, so the address has to reproduce the sequence the visitor
built.

**The two facet rows.** `FOR` first, `FEELS` second, each a `role="group"` with its own Micro caption
and its own `aria-labelledby`. Chip treatment, box, states, the 44px floor and "removing an active
chip is clicking it again" are 05.1's, **unchanged**. Sixteen chips in two labelled rows fit above the
grid at every width — which is what A-20 is really claiming — so **there is no disclosure and there
never was one**.

**What retires with the derivation:** `chipTags()`'s standing row and the **outsider chip** branch in
`BrowseToolbar.svelte:49-52`. Nothing can produce an outsider any more, because the vocabulary is
closed and an unmapped legacy value lands in the search field rather than as a chip — and the
outsider's job (making a `/browse/?tag=looper` link removable) passes to the search field's own
`CLEAR`. **`disabledTags()` stays, unchanged**, and its meaning narrows: a chip is disabled only when
it would return zero given the *other* facet's active set.

**The card.** Everything in 05.1's "Screen 2c — the card" holds: the reserved 16px `FEATURED` band,
the arrowless name plate as the one link, the whole-card `::after` overlay, `aria-hidden` on the pad
wrapper, the never-truncated description, the quiet non-interactive chips at 24.4px, the
`SIDE_INTERVAL_MS = 50` paint cadence, the two off-nominal states. **Two things change:** the
resting-black note is already gone (10-05), and each card now carries **three** quiet chips instead of
three or four.

**The `FOR` row on `/`.** Ten `<a href="/browse/?for={term}">`, caption `FOR` (3), between the name
plate and the fidelity line, at the 32px rhythm §6 gives it. **Links, not checkboxes** — a link keeps
the front door prerendered, keeps its `<head>` intact, and keeps the page import-free. It is the same
`FacetRow.svelte` in its link mode.

**Front-door invariants that must still hold afterwards**, asserted by `front-door.spec.ts` and not
edited here: `FRONT_DOOR.length === 8`, `EXCLUDED_FROM_ROW.length === 28`, the partition exact with
zero overlap and zero extras, `front-door.ts` declaring **no imports**, and every row member
`preview === "padsim"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-07-01: D-11 - Newest removed, and addedAt out of the projection</name>
  <files>src/lib/browse/sort.ts, src/lib/browse/sort.spec.ts, src/lib/catalog/listing.ts, src/lib/catalog/listing.spec.ts</files>

  <read_first>
    - `src/lib/browse/sort.ts` in full, including the comment explaining why `addedAt` is not a
      tie-break inside the featured group
    - `sort.spec.ts:82`, `:125`, `:179` — confirm all three line numbers with `grep -n` first
    - `src/lib/catalog/listing.ts:38-50` — `ListingEntry`'s four extra fields
  </read_first>

  <action>
**1. `sort.ts`.** Narrow `BrowseSort`, shorten `BROWSE_SORTS`, delete `newestOrder` and `orderFor`'s
middle branch. Rewrite the module header's opening line — "The three orders a visitor can put the
catalog in: Featured, Newest, Name" — to two, with a sentence recording **why** Newest went: three
distinct dates over thirty-six entries, twenty sharing one. Keep the `byNameThenId` comment about
locale-independence verbatim; it is untouched and it is load-bearing.

**2. `sort.spec.ts`.** `:82`'s literal and its message move to two. `:179`'s date-block test is
**retired by name**, not deleted silently: replace it with a short comment block saying 09-02 built it
as date blocks descending, D-11 retired the sort it tested, and the question it answered — "does the
catalog have a meaningful arrival order" — is now answered by nothing, deliberately. **Do not touch
`:125`.** File goes 6 → **5** tests.

**3. `addedAt` out of `ListingEntry`.** Remove the field from the type and from all thirty-six
restatements in `listing.ts`. **Do not touch a single entry file** — `addedAt` stays on the catalog
entry as provenance and `catalog.spec.ts` still sees it. `listing.spec.ts`'s equality loop drops one
field name from its list; the file stays at **5** tests.

**Two negative checks, and the first is the interesting one:**

- Restore `"newest"` to `BROWSE_SORTS` without restoring `newestOrder`. Expect `:125`'s comparator
  sweep to **self-adjust and pass** (it multiplies by `BROWSE_SORTS.length`), while `:82`'s list and
  the toolbar walk go **red**. **Record the asymmetry** — it is exactly what the spec predicted at
  §9.5 and it is why `:82` needed a hand edit and `:125` did not.
- Add `addedAt` back to one `LISTING` entry only. Expect `listing.spec.ts`'s field walk red or the
  type check red; say which caught it first, because if neither does, the field left the projection
  without a gate and that is worth knowing.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/browse/sort.spec.ts src/lib/catalog/listing.spec.ts src/lib/catalog/catalog.spec.ts && npm run check 2>&1 | grep -Ei "error|warning"</automated>
    Expect `sort.spec.ts` **5**, `listing.spec.ts` **5**, `catalog.spec.ts` **10** passed.
  </verify>

  <done>
`BROWSE_SORTS` is two, `newestOrder` is gone, `:82` and `:179` are edited and retired by name, and
`:125` is byte-untouched. `addedAt` has left `ListingEntry` and no entry file was touched. The
self-adjusting-versus-hand-written asymmetry is recorded from an observed run.
  </done>
</task>

<task type="auto">
  <name>Task 10-07-02: ?for= and ?feels=, and G-10 - no shared link dropped</name>
  <files>src/lib/browse/query.ts, src/lib/browse/query.spec.ts, src/lib/browse/filter.ts, src/lib/browse/filter.spec.ts</files>

  <read_first>
    - `src/lib/browse/query.ts` in full — especially the three rules in its header and the W-12
      paragraph at `:96-110`
    - `src/lib/browse/facets.ts` — `LEGACY_TAG_MAP` and `matchesFacets`
    - `10-UI-SPEC.md` §9.4's URL-parameters ruling
  </read_first>

  <action>
**1. `BrowseQuery` and the two parameters.** `tags: string[]` becomes `for: string[]` and
`feels: string[]`. `parseBrowseQuery` still takes its vocabulary as an **argument** — the module
imports nothing but `./sort` today and `facets.ts` must not become a second runtime specifier unless
`query.spec.ts`'s scan is amended to permit exactly it. **Prefer the argument**: it is the shape the
file's own header defends and it keeps the scan unchanged.

**2. G-10, the migration.** Rewrite W-12's paragraph by name — keep it, amend it — and implement:

- each `?tag=` value looked up in `LEGACY_TAG_MAP`;
- a **mapped** value appended to its facet's list (deduplicated, activation order preserved);
- an **unmapped** value collected, and — **only if `q` is empty** — joined with single spaces in
  address order and used as `q`;
- an unknown `?for=` or `?feels=` value **still dropped silently**, because that is a term the visitor
  did not author and `filterListing` would intersect it to nothing. W-12's original argument survives
  for the new parameters; it is only the *legacy* parameter that redirects.

**3. `serialiseBrowseQuery`** writes `sort`, `q`, then `for`s, then `feels`, and **never writes
`tag`** — the address a visitor builds is always the new shape, and the old one is read-only.

**4. `filter.ts`.** `filterListing` takes the two facet lists and applies **OR within, AND across**
through `matchesFacets`. `chipTags()` is deleted here or in task 3 — wherever its last caller goes —
and its deletion is recorded. **`disabledTags()` stays**, with its predicate narrowed to "would return
zero given the other facet's active set"; its comment records the narrowing and names why it is now
rare.

**5. `query.spec.ts`** gains the G-10 cases and loses none:

- `?tag=drums` → `for: ["drums"]`, `q: ""`;
- `?tag=looper` → `for: []`, `feels: []`, `q: "looper"`;
- `?tag=looper&q=aurora` → `q: "aurora"` and `looper` **gone** — the explicit query wins;
- `?tag=looper&tag=arpeggio` → `q: "looper arpeggio"`, in address order;
- `?tag=drums&tag=looper` → `for: ["drums"]`, `q: "looper"`;
- `?sort=newest` → the default, silently;
- a repeated `?for=` is one term; an unknown `?for=` is dropped.

**Negative check:** make an unmapped `?tag=` drop instead of landing in `q`. Expect the G-10 test red,
naming `looper`. Revert. **Then, separately, record the answer to the question the spec left open**:
run `?tag=looper&q=aurora` and state which value survived, because a plan that guessed here would
have broken a link in the least visible way available.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/browse/query.spec.ts src/lib/browse/filter.spec.ts src/lib/browse/facets.spec.ts</automated>
    Expect `filter.spec.ts` **6** and `facets.spec.ts` **4** passed, `query.spec.ts` green with its new
    cases and its import scan unchanged.
  </verify>

  <done>
`?for=` and `?feels=` are the written address; `?tag=` is read-only for one release. A mapped legacy
value becomes a chip, an unmapped one becomes `q` unless `q` is already set, and `?sort=newest` falls
back silently. W-12 is amended by name as G-10 and `10-VALIDATION.md`'s tenth gate amendment is
discharged. `disabledTags()` survives with a narrowed predicate; `chipTags()` is gone.
  </done>
</task>

<task type="auto">
  <name>Task 10-07-03: Two facet rows, three chips per card, and the FOR link row on the front door</name>
  <files>src/lib/ui/FacetRow.svelte, src/lib/ui/BrowseToolbar.svelte, src/lib/ui/TagChip.svelte, src/lib/ui/CatalogCard.svelte, src/lib/ui/FrontDoor.svelte, src/lib/ui/browse-ui.spec.ts, src/routes/browse/+page.svelte, e2e/browse.e2e.ts, e2e/browse-webkit.e2e.ts</files>

  <read_first>
    **`grep -n` every line number in this task before acting on it.** The citations below were measured
    on the shipped tree, but this plan is line-exact and a stale number is how a rewrite silently
    misses its target. Confirm each one, and if it has moved, record the new number in the SUMMARY.

    - `src/lib/ui/BrowseToolbar.svelte` in full, especially `:40-60`'s account of the standing row,
      the absent `MORE TAGS` and the outsider chip
    - `05.1-UI-SPEC.md` "Screen 2c — the card" and "The tag chips"
    - `src/lib/ui/FrontDoor.svelte` — the headline and `.row` from 10-03 and 10-04

    **The five `browse.e2e.ts` sites and the one `browse-webkit.e2e.ts` site D-11 and A-19 reach.**
    Revision 1 measured two of them and asserted the delta from those two; it is measured across all
    six here, and the answer is still 0 because none of them is a title.

    - `e2e/browse.e2e.ts:268` and `:289-311` — one title, four arms; the NEWEST arm is one of them
    - `e2e/browse.e2e.ts:121` — `const orderOf = (sort: "featured" | "newest" | "name")`. The literal
      union still names the retired sort
    - `e2e/browse.e2e.ts:332-404` in full — it presses `tag-playable` and `tag-generative` **by name**,
      cites `05.1-UI-SPEC W-04` at `:364`, computes the **intersection** as `both` at `:379`, asserts
      `toBeLessThan` at `:383`, and requires at least one chip disabled at `:394-398`
    - `e2e/browse.e2e.ts:852-888` — `test("the browser Back button returns the filtered view it left")`
      asserts the **written** address three times, at `:856`, `:867` and `:885`, each
      `toHaveURL(/\/browse\/\?tag=playable$/)`
    - `e2e/browse.e2e.ts:1029-1120` — `test("browse, open a configuration, and come back to the same
      view")`, built end to end on the retired sort **and** on `filterListing`'s three-argument form.
      It is the only `filterListing` call site in `e2e/` besides the import at `:42`
    - `e2e/browse.e2e.ts:437` — a cold arrival on `/browse/?sort=name&tag=playable`. Read it to
      confirm what task item 5e says about it rather than to change it
    - `e2e/browse-webkit.e2e.ts:79` — its own copy of `orderOf`, with the same three-member literal
      union. **This file is edited for D-11**, contrary to what revision 1 of this plan claimed
  </read_first>

  <action>
**1. `FacetRow.svelte`, two modes.** A `role="group"` with a Micro caption and `aria-labelledby`.
In **checkbox mode** (`/browse/`) each member is a real `<input type="checkbox">` inside a `<label>`,
its own tab stop, `Space` toggles, `disabled` when `disabledTags()` says so, **no adjacent reason
line** — the count line already tells the truth. In **link mode** (`/`) each member is an
`<a href="/browse/?for={term}">`. Both modes: `min-block-size: 44px` **and** `min-inline-size: 44px`
per member. `data-testid` `facet-for` and `facet-feels`; the individual chips keep 05.1's
`tag-{term}` testids so the shipped e2e presses still resolve.

**2. `BrowseToolbar.svelte`.** Replace the single derived tag row with two `FacetRow`s, `FOR` then
`FEELS`, at the 8px gap §6 gives them. Delete the outsider-chip branch at `:49-52` and rewrite the
`:40-60` header block **by name**: the standing row is no longer count-derived, the outsider retires
with the derivation that needed it, `MORE TAGS` was never built and still is not, and `disabledTags()`
stays. Sort options become `FEATURED` · `NAME`.

**3. `CatalogCard.svelte`** renders **three** quiet non-interactive chips. Everything else in
Screen 2c is untouched; say so in the SUMMARY, item by item, because "the card is unchanged except"
is a claim worth itemising.

**4. `FrontDoor.svelte`** gains the `FOR` link row between the name plate and the fidelity line, in
link mode. **`front-door.ts` gains no import** and the page stays prerendered — confirm with
`npm run build` and by checking the ten `<a>` elements are in `build/index.html` as static markup.
The ring stays at **8**; `front-door.spec.ts` is untouched and green.

**5. The e2e walks. Six sites, one of them inverts, and not one of them is a title.**

Revision 1 of this plan measured two sites and asserted `0` from those two. The delta was right and
the measurement was not wide enough; the six sites below are the whole of what D-11 and A-19 reach in
`e2e/`, found by `grep -n "newest\|sortListing\|filterListing\|orderOf\|tag=playable"` across both
files. **Re-run that grep first** and reconcile it against this list before editing anything.

**5a. The sort walk loses assertions, not a title.** `browse.e2e.ts:268` is a single
`test("sorting reorders the grid and the address")` whose body walks FEATURED, NAME, NEWEST
(`:289-311`) and FEATURED again. Delete the NEWEST arm — the `option("NEWEST").click()`, the
`radio("newest")` check, the `?sort=newest` URL assertion and the whole `addedAt` block-boundary loop,
which stops type-checking anyway the moment `addedAt` leaves `ListingEntry` — and rewrite the comment
block above it **by name**: 09-02 wrote it as a block boundary rather than a sequence, D-11 retired
the sort it tested, and the title keeps its other three arms.

**5b. Both copies of `orderOf`, and the false claim this plan carried.** The three-member literal
union `(sort: "featured" | "newest" | "name")` exists **twice**: at `browse.e2e.ts:121` and again at
`browse-webkit.e2e.ts:79`. Narrow both to `BrowseSort`, or to the two surviving members, and let the
type flow from `sortListing`'s own signature rather than restating it.

> **Retraction, dated 2026-09-08.** Revision 1 of this plan said at this point that
> "`browse-webkit.e2e.ts` never names `newest` and is not edited for D-11". **That is false.** The
> file imports `sortListing` at `:39` and declares its own `orderOf` at `:79` with `newest` in the
> literal union. The moment `BrowseSort` narrows, `npm run check` goes red there. The file is already
> in this plan's `files_modified`; it was the *instruction* that was wrong, not the file list. The
> same false claim was propagated verbatim to `10-14:112-113` and `10-VALIDATION:222` and is retracted
> in all three places. Its two call sites at `:217-218` ask for `featured` and `name` only, so the
> edit is one line and no assertion in that file changes.

**5c. `:332-404` encodes 05.1's AND semantics and A-19 supersedes it — this is a rewrite, not a
survival check.** The block presses `tag-playable` then `tag-generative`, computes
`both = playable ∩ generative` at `:379`, asserts
`expect(both.length, "the intersection is smaller than either chip alone").toBeLessThan(playable.length)`
at `:383`, then `toHaveCount(both.length)` and `expectCount(page, both.length, total)` at `:386-388`.
In the new vocabulary **`playable` and `generative` are both `FEELS`**, so pressing both yields the
**union** — and every one of those three assertions inverts. Rewrite the block:

- **Two chips within one facet → union.** Recompute the expectation as `union = playable ∪ generative`
  and flip the guard to
  `expect(union.length, "the union is larger than either chip alone").toBeGreaterThan(playable.length)`
  — non-vacuous in the same way the old guard was, in the opposite direction. From 10-06's assignment
  table the union is **21** of 36 and `playable` alone is **13**, so the guard has real slack; state
  both observed numbers rather than only the assertion's verdict.
- **A second press across facets → AND, on a term that is actually clickable.** Press the `FOR` chip
  **`modulation`** on top of the two active `FEELS` chips and assert the **intersection**: entries
  carrying `modulation` **and** at least one of `playable` / `generative`, which 10-06's table makes
  **4** — `arc`, `ghost`, `radar`, `shuttle` — against the union's 21. Do **not** reach for `mixing`,
  `shortcuts` or `pointing`: at `feels = [playable, generative]` all three have an empty intersection
  and render `disabled`, so the press would silently do nothing and the assertion would pass on the
  union's own count. (`sequencing` also works, at **3** — `euclid`, `sonar`, `steps` — if `modulation`
  has moved facet by the time this runs.) Without this arm the file proves OR and never proves the AND
  half of "OR within, AND across", which is the whole of CAT-03's amendment.
- **Amend `:364`'s comment by name.** The parenthetical `(05.1-UI-SPEC W-04)` is the cited reason the
  chips intersect. W-04 is superseded by **A-19**. Rewrite the paragraph — keep it, amend it, date it —
  rather than deleting the citation, which is the same rule this phase applies to every other retired
  assertion. Its opening sentence, "playable is carried by four entries and generative by three", is
  also stale after 10-06: re-derive both numbers and write them in.
- **The disabled-chip assertion at `:394-398`** reads
  `[data-testid="browse-tags"] input:disabled` and requires at least one. `disabledTags()` survives
  with a narrowed predicate and becomes **rare**, and the testid now sits inside the two facet rows.
  **Re-derive whether any chip is still disabled at sixteen terms.** By 10-06's table it is: at
  `feels = [playable, generative]` the three `FOR` terms named above are all empty, so the assertion
  is expected to survive with the selector widened to the two facet rows. **Verify that rather than
  trusting it** — if none is disabled it has become vacuous by inversion, and it is then replaced by
  the narrowed predicate's own proof (press a `FOR` chip, assert the `FEELS` chips that would return
  zero carry a real `disabled`) or dropped **inside the title** with the reason recorded. Either way
  **no title moves**.

**5d. The three written-address assertions at `:852-888`, inside
`test("the browser Back button returns the filtered view it left")`.** The test presses
`tag-playable` and then asserts the *composed* address three times — at `:856` after the press, at
`:867` after `goBack()`, and at `:885` after the cold-arrival leg — each
`await expect(page).toHaveURL(/\/browse\/\?tag=playable$/)`. Under 10-07-02 item 3
`serialiseBrowseQuery` never writes `?tag=` again, so all three become
**`/\/browse\/\?feels=playable$/`**. The cold `goto` at `:880` keeps `?tag=playable` **unchanged and
deliberately** — it is the read path, and leaving one legacy address in the suite is what makes `:885`
a migration proof: entered as `?tag=`, re-written as `?feels=`. Say that in the comment, because a
future reader will otherwise "fix" the inconsistency. The chip testid `tag-playable` is unchanged by
item 1, so nothing else in the block moves.

**5e. `:1029-1120`, `test("browse, open a configuration, and come back to the same view")` — an
entire test built on the retired sort.** It breaks five ways and every one is inside the title:

1. `:1048-1051` calls `sortListing(filterListing(LISTING, QUERY, [TAG]), "newest")` — the retired
   comparator **and** `filterListing`'s three-argument signature, which 10-07-02 item 4 replaces with
   two facet lists. This is the only `filterListing` call site in `e2e/` besides the import at `:42`,
   so it is also the only place the new arity is exercised end to end from a browser.
2. `:1059-1060` clicks `NEWEST` by text and asserts `input[value="newest"]` checked.
3. `:1070` asserts `toHaveURL(/sort=newest/)`.
4. `:1072` asserts `toHaveURL(new RegExp("tag=" + TAG))` — the written address, retired by
   10-07-02 item 3.
5. `:1105-1106` re-asserts `input[value="newest"]` checked after Back.

Restate it **inside its existing title**: `TAG = "playable"` stays and becomes a `feels` term, the
sort becomes `"name"` (`"featured"` would leave the ordering assertion at `:1065` weaker, since
featured order is the shelf's own), the `filterListing` call takes the new two-list arity, both
`input[value=...]` assertions take the surviving sort's value, and `:1072` becomes `feels=${TAG}`.
The non-vacuity guards at `:1052-1056` — more than one result, fewer than the whole shelf — must
still hold under the new filter; **state the observed `expectedIds.length`**, because `QUERY = "pad"`
intersected with a `feels` term is a different set than it was with a tag, and a guard that happens to
hold is not the same as a guard that was checked. Everything after `:1074` — the scroll offset,
`BACK TO BROWSE`, the restore tolerance — is untouched, and D-19's one-slot-two-labels proof is the
reason this test is repaired rather than deleted.

**5f. `:437`'s cold arrival survives, and here is why rather than by luck.** The test at `:428-450`
does `coldGoto(page, "/browse/?sort=name&tag=playable")` and then computes its own expectation with
`LISTING.filter(...)` and `sortListing(..., "name")` — no `filterListing`, no `newest`. `?sort=name`
is a surviving sort and `playable` is a **mapped** legacy term, so 10-07-02's read path turns it into
a `FEELS` chip and the rendered set is identical. **And nothing in the test asserts a URL after the
arrival**, so the read/write asymmetry cannot reach it. It is left untouched deliberately, and it is
the second half of the migration proof `:885` starts: a shared `?tag=` link still lands on the right
shelf. Assert nothing new here; record in the SUMMARY that it was examined and left alone.

**5g. The two search anchors.** The `?q=aurora` single-result proof at `:719-734` and
`filter.spec.ts`'s `ghost` single result must both still hold at three tags per entry: **re-verify
both and state the result counts**, because the tag re-cut changed the haystack `matches()` folds.

**Expected e2e delta: 0 — measured across all six sites, not two.** 5a and 5c edit assertions inside
`:268` and inside `:332-404`'s title; 5b edits a type annotation in each file; 5d edits three regexes
inside `:852-888`'s title; 5e restates a body inside `:1029-1120`'s title; 5f edits nothing. **No
`test(` or `test.describe(` line is added, removed or renamed in either file** — assert that directly
with `grep -c "test(" e2e/browse.e2e.ts e2e/browse-webkit.e2e.ts` before and after, and state both
numbers. The phase total after this plan is **`BASE_E2E + 12`**. Run the full suite, state the
observed total beside `PREV_E2E`, and if it is anything but unchanged, name the title that moved.

**Four negative checks:**

- Remove `FEELS`'s `aria-labelledby`. Expect `browse-ui.spec.ts`'s group test red.
- Give a `FacetRow` member `min-inline-size: auto`. Expect `browse-ui.spec.ts`'s 44px floor red,
  naming the class — the same both-axes derivation `device-ui.spec.ts:278-306` uses.
- Restore `:379-388`'s **intersection** expectation on the two `FEELS` chips — the shipped
  `toBeLessThan` form. Expect the rewritten union assertion red, naming both terms and both counts.
  This is the one negative check that proves the inversion was *understood* rather than merely
  edited: on the old vocabulary the intersection assertion was true, and if it is still green after
  the re-cut then `playable` and `generative` did not both land in `FEELS` and 10-06's assignment
  table is wrong. **Record which.**
- Revert `browse-webkit.e2e.ts:79`'s `orderOf` to the three-member literal union. Expect
  `npm run check` red **in that file**, naming `newest`. This is the check that retires revision 1's
  false claim by observation rather than by assertion: if `check` stays green, the narrowing in
  10-07-01 did not actually reach `BrowseSort` and the sort is only half retired.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -Ei "error|warning" ; npm run build && npx playwright test e2e/browse.e2e.ts e2e/browse-webkit.e2e.ts --workers 3</automated>
    `npm run check` prints nothing — the gate that catches `browse-webkit.e2e.ts:79` — and both e2e
    files are green. Then the full suite:
    `npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright PREV_E2E`
    — **unchanged**, because all six sites are edited inside existing titles. The `grep -c "test("`
    before/after counts for both files are stated, the arithmetic is written out and the observed
    total given. `test-results/` removed by hand; `git status --porcelain` empty.
  </verify>

  <done>
Two captioned facet rows of sixteen chips, both 44px axes, no disclosure and no outsider. Three chips
per card and every other Screen 2c property itemised as unchanged. The `FOR` link row is static markup
in `build/index.html` and `front-door.ts` is import-free with the ring still at eight. The two facet
presses assert **union** within `FEELS` (21 against `playable`'s 13) and **intersection** across
`FOR` × `FEELS` on `modulation` (4), and `:364`'s W-04 citation is amended to A-19 by name. All six
D-11/A-19 sites are edited or explicitly examined and left: `:268`, `:121`, `:332-404`, `:852-888`,
`:1029-1120`, `:437`, plus `browse-webkit.e2e.ts:79`. The two search anchors are re-verified with
their result counts stated. `PREV_E2E` is re-measured and **unchanged**: this plan's e2e delta is
**0**, and the `grep -c "test("` counts prove it rather than the reasoning alone.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS-1` — the −1 is
  `sort.spec.ts` 6 → 5 and nothing else; no other spec file in this plan moves and none is created.
- `npx playwright test --workers 3` at `PREV_E2E`, **unchanged**; `test-results/` removed.
- `grep -c "test(" e2e/browse.e2e.ts e2e/browse-webkit.e2e.ts` equal to the pre-edit counts.
- `npm run build` green, then `npm run test:quick` again.
- `grep -rn "newest\|newestOrder\|chipTags" src e2e` returns only deliberate history in comments —
  **`e2e/` is in the scan**, because two of this plan's six sites live there and one of them was
  missed by a scan that looked only at `src/`.
- `git diff --stat HEAD -- src/vendor/ src/lib/ui/Coverflow.svelte src/lib/catalog/front-door.ts` empty.
</verification>

<success_criteria>
1. Two sorts, sixteen chips in two labelled rows, three chips per card, no disclosure and no outsider.
2. No shared link lands on nothing: mapped, unmapped and retired-sort cases each have a test and the
   `?q=` precedence is ruled and asserted.
3. `addedAt` left the projection without an entry file being touched.
4. `:125` self-adjusted and `:82` did not — observed, not assumed.
5. The ring is still eight, `front-door.ts` still has no imports, and the `FOR` row is static markup in
   the prerendered HTML.
6. The two search anchors the shipped suites depend on still return one result each, stated.
7. The AND→OR inversion at `browse.e2e.ts:332-404` is **rewritten, not survived**: union within one
   facet, intersection across two on a term that is not disabled, `:364`'s `W-04` citation amended to
   A-19 by name.
8. **All six D-11/A-19 sites are accounted for by name** — `browse.e2e.ts:268`, `:121`, `:332-404`,
   `:852-888`, `:1029-1120` and `:437`, plus `browse-webkit.e2e.ts:79` — each either edited or
   recorded as examined and left, and `npm run check` green, which `:79` alone would break. The e2e
   total is **unchanged**, proved by the `grep -c "test("` counts rather than by argument.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-07-SUMMARY.md` with the eleven-name block
carried, the measured e2e delta with the before/after `grep -c "test("` counts, the `?tag=` migration
matrix with all six cases and their outputs, the two search-anchor result counts, the rewritten
`:332-404` block with its union and intersection counts and the fate of the disabled-chip assertion,
the six-site table recording what each site became, the `expectedIds.length` observed at `:1052`, the
itemised list of Screen 2c properties that did not change, and the deferred item recording why the
ring cannot grow today.
</output>
