---
phase: 10-redesign
plan: 13
type: execute
wave: 13
depends_on: ["10-12"]
files_modified:
  - src/lib/ui/Clear.svelte
  - src/lib/ui/InstallState.svelte
  - src/lib/ui/ChosenPanel.svelte
  - src/lib/ui/DeviceDetails.svelte
  - src/lib/ui/device-ui.spec.ts
  - e2e/install.e2e.ts
  - docs/INSTALL-RUNBOOK.md
  - docs/TESTING.md
autonomous: true
requirements: [SAFE-02, SAFE-03, DEGR-02]

must_haves:
  truths:
    - "Three controls read as one sequence without any of them changing weight: a caption, DOM order and enablement carry it, and SAFE-02's two tiers are exactly where Phase 7 left them."
    - "CLEAR is in the Quiet tier beside KEEP ON DEVICE and is distinguished from it on three channels - the words, the enablement set, and the confirmation KEEP has and CLEAR does not - two of which are behaviour rather than appearance. At rest the two look the same, and the plan says so."
    - "The panel says a true sentence about what a clear leaves behind: the module is running the firmware's own default configuration, and PUT BACK is on the screen and enabled while that sentence names it."
    - "On a browser that cannot write, CLEAR is present and disabled with its reason inline - taught, not hidden - and PUT BACK is absent, and the difference is one assertion."
    - "The fourth click is walked end to end in a real browser in both projects against the fake, and the hardware half joins runbook row C rather than adding a row H."
  artifacts:
    - path: "src/lib/ui/Clear.svelte"
      provides: "The Quiet tier: no border, no background, no radius, both 44px axes, its 48px sizing-twin cell, its three reasons, CLEARING… and aria-busy"
      contains: "min-inline-size"
    - path: "src/lib/ui/InstallState.svelte"
      provides: "The FACTORY DEFAULT block and CLEAR's details on the three reused failure states"
      contains: "FACTORY DEFAULT"
    - path: "docs/INSTALL-RUNBOOK.md"
      provides: "Row C extended with the clear, still seven rows, still awaiting the user"
  key_links:
    - from: "src/lib/ui/ChosenPanel.svelte"
      to: "src/lib/ui/Clear.svelte"
      via: "CLEAR joins the existing column under the one hairline Phase 7 gave it, at the 16px rhythm, with no hairline added"
      pattern: "Clear"
    - from: "e2e/install.e2e.ts"
      to: "e2e/fake-zona.ts"
      via: "the clear walk driven against the scripted module, never hardware"
      pattern: "clear"
---

<objective>
CLEAR, part two: one control, one block, one browser walk — and the sequence D-04 asked for, without
flattening SAFE-02.

Wave 13. Blocked on 10-12, which built the machine this plan renders.

**REWRITTEN 2026-09-08 for D-19, D-20 and D-21.** The plan this replaces built a second confirmation
component, a fifth control tier, a second hairline and a runbook row H. **All four are gone.** What is
left is a control, a block, a caption and a browser walk — and the honest statement that `CLEAR` and
`KEEP ON DEVICE` now look the same at rest. **Nothing renumbers**: this is still plan 13 at wave 13,
still blocked on 10-12, and 10-13.1 and 10-14 keep their numbers and their `depends_on`.

**D-04's tension is exact.** SAFE-02's whole content is that `TRY ON DEVICE` and `KEEP ON DEVICE` are
never equal-weight, and today they are three tiers apart with a hairline, a tuning region and another
control between them. **"One natural sequence" must not become "three equal buttons in a row".**

> **A sequence is made of a caption, an order and an enablement. It is not made of equal weights.**

| Channel | How it carries the sequence |
|---|---|
| **A caption** | region 6 gains one: **`NEXT`** (4), Micro, `--color-ink-quiet`, directly under the existing hairline. One word, inside the two-word uppercase rule. Before a try-on it still reads right: it is what comes next |
| **DOM and visual order** | unchanged from Z-03: `PUT BACK` → `KEEP ON DEVICE` → **`CLEAR`** → `COPY LINK`, at a 16px rhythm, one layout at every width |
| **Enablement choreography** | the strongest channel, and **it already exists**. Before a try-on, everything under `NEXT` is disabled with its reason in its own cell. The instant `settled` lands, `PUT BACK` and `KEEP ON DEVICE` go live **together**. That transition *is* the sequence, and it is behaviour rather than decoration |
| **The honesty slot above** | already says what the primary does before the click. **Nothing is added** |

**Explicitly not done:** no step numerals, no connecting rule or bracket, no progress indicator, no
shared background, no equalised widths, **no accent on any sequence marker**, and — new in this
revision — **no second hairline** (A-46 retired it with the tier it was separating).

**A-46 — CLEAR is Quiet, not Bare, and the ladder is three tiers.** The fifth tier declared for one
control is retired before it ships. `CLEAR` takes the Quiet treatment verbatim: borderless, shapeless,
no radius, no background, Micro label at the site's ordinary `letter-spacing: 0.18em` in
`--color-ink-quiet`. **The 0.28em tracking is gone, and with it the grep-checkable uniqueness the old
plan leaned on.** What replaces it as a gate is the opposite claim, and it is more useful: `CLEAR`
declares **no** border, background, radius or non-standard tracking — the same assertion A-41 already
makes about `KEEP ON DEVICE`, now protecting two controls instead of one.

**A-47 — recount the channels honestly, and do not pad to keep the word "five".** The approved §10.4
claimed five: tracking, position below a second hairline, its own line, its confirmation caption, and
a different set of disabled states. **Three are gone** (A-45 and A-46) and **a fourth was never a
channel**: `KEEP ON DEVICE` and `PUT BACK` each have their own line cell too, so having one
distinguishes `CLEAR` from nothing. **The honest number is three:**

1. **The words.** `CLEAR` / `Reset the current page to factory default` (41) against `KEEP ON DEVICE` /
   `Stores this configuration in your ZONA’s own memory, so it survives a power cycle.` (82).
2. **The enablement set.** The two are rarely both live; the difference is asserted over 10-12's
   fifteen-row table.
3. **The ceremony, inverted.** `KEEP ON DEVICE` opens a confirmation; `CLEAR` sends on the click.

**So at rest, after a try-on, on a browser that can write, `CLEAR` and `KEEP ON DEVICE` look the
same.** Same tier, same weight, same colour, same tracking. **State that in the SUMMARY as a fact
rather than burying it.** The argument that it is acceptable is not aesthetic: **`CLEAR` is the least
consequential of the four writes** — RAM only, undone by the control directly above it, undone by a
power cycle, and what it writes is a working configuration the firmware itself ships. The write that
needed setting apart was the irreversible one, and that one still has its confirmation.

**A-25 stands and is now cheaper to hold.** No `--color-over` on CLEAR. The reversal Open item 6
described — the token as a confirmation block's 1px border — has nothing to attach to, because there
is no block. If red is still wanted it would have to be a border on the control, which puts `CLEAR`
back in a tier of its own and reverses A-46 too. **State that cost.**

Purpose: SAFE-02 kept verbatim; SAFE-03's `PUT BACK`-after-a-clear half rendered; DEGR-02 extended.
Output: one component, one caption, one `FACTORY DEFAULT` block, two e2e titles, one widened runbook
row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-CONTEXT.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/10-redesign/10-03-SUMMARY.md
@.planning/phases/10-redesign/10-12-SUMMARY.md
@src/lib/ui/KeepOnDevice.svelte
@src/lib/ui/PutBack.svelte
@src/lib/ui/ChosenPanel.svelte
@src/lib/ui/InstallState.svelte
@src/lib/ui/device-ui.spec.ts
@docs/INSTALL-RUNBOOK.md

<standing_rules>
- **Never edit anything under `src/vendor/`.** **Never touch a sibling repository.**
- **No agent connects to or writes to a device, and no agent deploys.** Every walk in this plan runs
  against `e2e/fake-zona.ts`. **The runbook is written for the user and is not performed by any agent.**
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- e2e at `--workers 3`; remove `test-results/` by hand and confirm `git status --porcelain` empty.
- **No new icon and no new SVG ships this phase** (X-22).
- **Every user-facing string is counted by script and its number stated.**
- **State every count as a carried name plus a delta.**
- The project spends no money.
</standing_rules>

<interfaces>
**The tier table, Phase 7's, unchanged — three tiers, not four (A-46):**

| Tier | Controls | Treatment |
|---|---|---|
| **Primary** — one per panel | `TRY ON DEVICE` | Phase 7's, **verbatim**, plus A-41's radius |
| **Secondary** — bordered | `PUT BACK`, `KEEP ON DEVICE`'s confirmation affirmative, `COPY LINK`, `TURN IT DOWN`, `MIX TWO` | Phase 7's, **verbatim**, plus A-41's pill outline |
| **Quiet** — borderless | `KEEP ON DEVICE` in the install row, **`CLEAR`**, `NOT NOW`, `DISCONNECT ZONA`, `BROWSE ALL`, `HOLD` / `HELD` | Phase 7's, **verbatim**. `CLEAR` joins with no exception: `min-block-size: 44px` **and `min-inline-size: 44px`** — both axes, because `device-ui.spec.ts:278-306` derives its list from the presence of a control and requires both on every interactive class it finds, **so an `auto` width fails G-03**. `padding-inline: 0`, no border, no background, no fill, no radius, label Micro in `--color-ink-quiet` at the site's ordinary `letter-spacing: 0.18em`. Hover: label to `--color-ink`, 140 ms. Disabled: `--color-ink-dim`, a real `disabled` attribute |

**One name left the Secondary row.** It read "the two confirmations' affirmatives". There is one
confirmation now (A-45), so it reads `KEEP ON DEVICE`'s. Check nothing in `device-ui.spec.ts` counted
two.

**G-03's three changes to `device-ui.spec.ts`, as amended:**

1. the honesty slot's `min-block-size: 72px` becomes the number 10-03 measured;
2. `PUT_BACK_CAP`, `KEEP_CAP` and the new `CLEAR_CAP` are the measured arithmetic;
3. **the control walk gains `Clear.svelte`** — one component, not two — and it must declare the 44px
   floor on **both axes**.

File goes 11 → **13** tests, **+2** rather than the +3 the old plan carried: the tracking-uniqueness
test is retired with the Bare tier, and what replaces it is a shapelessness test that reads two
controls instead of one.

**`Clear.svelte`'s line cell.** The measured height at `CH_PER_LINE` **43** is **48px** — two lines —
and **that is deliberately one line more than the formula's output** (A-52). The four candidates are
the enabled line at **41** and the three reasons at **43 / 26 / 36**, so `ceil(43 / 43) × 24` is 24px.
**Do not take it.** A one-line cap of 43 puts a shipped string exactly on its own cap, which is the
zero-headroom defect 10-01 flagged against the old 86-character `CLEAR_LINE`, reintroduced at a
different number. `CLEAR_CAP` stays **86** and the cell stays **48px**, and the second line is
declared **headroom rather than occupancy** in a comment, so a later reader does not "correct" it.

**There is no confirmation** (A-45). No `ClearConfirm.svelte`, no `REMOVES` caption, no 101-character
sentence, no affirmative, no `NOT NOW`. **Assert the absence**, not just the design: scan
`src/lib/ui/` for a `clear-confirm` testid and for the retired literals and assert none exists, with
D-19's reason in the message. A component that is not built leaves no trace, and a plan that removed
one should be able to prove it.

**Region 3's `FACTORY DEFAULT` block**: caption `FACTORY DEFAULT` (**15**), body `Your ZONA is running
the firmware’s own default configuration. PUT BACK restores what was there when you connected.`
(**115**). The caption names the **state**, not the button, exactly as `PLAYING NOW` names the state
`TRY ON DEVICE` leaves behind. **The body names `PUT BACK`, which is on the screen** — the copy rule
is "no string names a control that is not on the screen", and in `cleared` `PUT BACK` is enabled.
Assert that pairing, because it is the one place this phase's copy could break that rule.

**There is no rig sentence.** SAFE-06's "the store reaches every module at once" is a property of
`PAGESTORE`, and **CLEAR does not store. Saying it would be false.** Assert its absence.

**DEGR-02.** CLEAR is **present-but-disabled** on browsers that cannot write, with its reason inline.
It is **not** absent: unlike `PUT BACK` (Z-12), **CLEAR does something meaningful on any module, so
there is a real capability to teach.**

**`docs/INSTALL-RUNBOOK.md` — no row H (A-51). The clear folds into row C.** Row C is `PUT BACK`
(SAFE-03) and its subject is already *"the pad is doing exactly what it was doing before row B"*. A
clear before the `PUT BACK` makes the same row a stronger test of the same claim at no extra rows and
no extra half hour. **The section still says "Seven rows"** — do not edit that sentence, and say in
the SUMMARY that you did not.

**Row C's "Passes when" gains the honest observation, and it is not "the pad goes dark".** The
firmware's default Setup zeroes all 81 cells and installs a touch callback, so the pad is **dark at
rest and lights a soft white bloom under a finger**, brightest under the fingertip and falling off by
distance. That is the thing to look for, and it is exactly the thing a fake cannot show: the scripted
module acknowledges what it is told to acknowledge, and only a real ZONA can show that two
`CONFIG/EXECUTE` frames carrying the package's own `defaultConfig` actually *run* rather than merely
being acknowledged. Row C's "Why a machine cannot" gains that sentence.

**Add the caution the other write rows carry**: do not pull the cable while the label reads
`CLEARING…`.

**`docs/TESTING.md:840`** says `install.spec.ts` proves "the three clicks". It becomes **four**. That
is G-06's documentary half; the assertion itself did not change and 10-12 recorded why, and A-53
recorded that the approved §10.7 said the opposite of its own G-06 row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-13-01: Clear.svelte in the Quiet tier, and G-03's widened control walk</name>
  <files>src/lib/ui/Clear.svelte, src/lib/ui/device-ui.spec.ts</files>

  <read_first>
    - `src/lib/ui/KeepOnDevice.svelte` in full — the Quiet tier as it actually ships, and the
      sizing-twin cell mechanism (grid-area 1/1, `visibility: hidden`, `aria-hidden`)
    - `device-ui.spec.ts:63-71` (`DEVICE_COMPONENTS`), `:199` (its length assertion) and `:270-310`
      (the both-axes control walk), read before writing a control
    - `10-03-SUMMARY.md` — the measured `CH_PER_LINE` and the cell heights that follow from it
    - `10-12-SUMMARY.md` — `CLEAR_CAP`, the four candidate strings and their counts
  </read_first>

  <action>
**1. `Clear.svelte`.** The Quiet tier exactly as `<interfaces>` gives it. `data-testid` `clear` and
`clear-line`. Its line cell is **48px**, built the same way its two neighbours are: **one grid cell,
every candidate at `grid-area: 1 / 1`, the inactive ones `visibility: hidden` and
`aria-hidden="true"`.** Four candidates: the enabled line (41) and the three reasons (43 / 26 / 36).

**Write A-52's reasoning into a comment beside the 48px.** The longest candidate is 43 and the formula
gives 24px; the second line is headroom so the cap stays a promise. Without that comment the next
reader deletes 24px and thinks they tidied something.

`CLEARING…` with `aria-busy` through its one leg. **Nothing animates** — assert no `transition` and no
`animation` on the busy state, exactly as Z-09 rules for every other write. The 2000 ms
`Still writing.` line applies unchanged and is not re-declared here.

**Declare both 44px axes explicitly.** `padding-inline: 0` means the label's own width sets the box,
and `CLEAR` at 12px/0.18em is well under 44px — so `min-inline-size` is not decoration here, it is
what makes `device-ui.spec.ts:278-306` green **and** what keeps the target reachable. Say that in a
comment so a later reader does not remove it as redundant.

**No border, no background, no radius, no non-standard tracking.** `CLEAR` is Quiet and A-41 forbids
pilling Quiet; that prohibition now protects two controls.

**2. `DEVICE_COMPONENTS` gains one name.** `device-ui.spec.ts:63-71` declares the list by hand and
`:277-306`'s 44px walk loops `for (const name of DEVICE_COMPONENTS)`. **Omitting `Clear.svelte` makes
the walk pass silently on a component it never read**, which is the exact failure mode the file's own
comment at `:81-84` says the list exists to prevent. So: add the one name, and move the length
assertion at `:199` from its current value to **that value + 1** — read it with `grep -n` first and
state both numbers, because 10-13.1 records the shipped literal as **6** while an earlier revision of
this plan asserted **7 → 9**. **Assert the observed number, not either plan's figure**, and name the
disagreement in the SUMMARY. `SENTENCE_COMPONENTS` is **not** touched: `Clear.svelte`'s whole content
is not Body prose.

**3. G-03.** `device-ui.spec.ts` 11 → **13** tests:

- **`CLEAR_CAP` and the 48px cell** join the cap and reservation assertions in the same shape as
  `KEEP_CAP` at `:782,797-798` and `PUT_BACK_CAP` at `:642,657-658`. One new test.
- **Shapelessness, over two controls.** `KeepOnDevice.svelte` and `Clear.svelte` declare **no**
  `border`, no `background`, no `border-radius`, and no `letter-spacing` other than the site's Micro
  0.18em. One new test, and it is the replacement for the retired tracking-uniqueness test: instead of
  proving `CLEAR` is unlike everything, it proves `CLEAR` and `KEEP ON DEVICE` are alike, which is
  what A-46 actually decided and what A-41 needs held.
- **`0.28em` appears nowhere.** Fold this into the shapelessness test as one more expectation rather
  than a third test: the Bare tier was retired before it shipped, so this is a guard against it
  arriving by copy-paste from the old plan.

**4. Prove the component that is not built.** Assert no file in `src/lib/ui/` contains the testid
`clear-confirm` and no source file contains `REMOVES` or `This empties the Setup and Timer`. Put
D-19's reason in the message: CLEAR is recoverable by its neighbour and by a power cycle, so it needs
no confirmation, and `KEEP ON DEVICE`'s is the site's only one. **Fold this into the shapelessness
test too** if it fits cleanly; the file count above assumes it does not add a third.

**Three negative checks:**

- Give `Clear.svelte` an `auto` inline size. Expect the walk red, naming `Clear.svelte -> .clear`.
- Give `Clear.svelte` a `border: 1px solid`. Expect the shapelessness test red, naming the
  declaration and A-41.
- Remove `Clear.svelte` from `DEVICE_COMPONENTS`. Expect the length assertion red. **This is the check
  that proves the hand list is guarded rather than merely long.**
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/device-ui.spec.ts src/lib/device/install-copy.spec.ts</automated>
    Expect `device-ui.spec.ts` **13 passed**, `install-copy.spec.ts` **6 passed**. Plus a script
    printing 41 / 43 / 26 / 36 and a `grep -rc "0.28em" src/lib/ui/` of **0**.
  </verify>

  <done>
`Clear.svelte` is Quiet tier with no border, background, radius or non-standard tracking, both 44px
axes declared with their reason in a comment, and a 48px cell whose second line is documented as
headroom. `DEVICE_COMPONENTS` names it and its length assertion moved with it, with the observed
before-and-after stated and any disagreement with 10-13.1's figure named. `device-ui.spec.ts` is 13
with a two-control shapelessness test replacing the retired tracking-uniqueness test, and the
confirmation that was not built is proved absent. Three negative checks recorded.
  </done>
</task>

<task type="auto">
  <name>Task 10-13-02: The NEXT caption, the FACTORY DEFAULT block, and DEGR-02</name>
  <files>src/lib/ui/ChosenPanel.svelte, src/lib/ui/InstallState.svelte, src/lib/ui/DeviceDetails.svelte, src/lib/ui/device-ui.spec.ts</files>

  <read_first>
    - `src/lib/ui/ChosenPanel.svelte` — region 6, the existing hairline, and `:217`'s untouched 152px
    - `src/lib/ui/InstallState.svelte` — how a phase selects a block, and how `lastAction` is read
    - `10-UI-SPEC.md` §10.2, §10.3, §10.4, §10.6, §13.3 as amended by A-45 to A-52
  </read_first>

  <action>
**1. The `NEXT` caption.** One word, Micro, `--color-ink-quiet`, directly under the existing hairline
in region 6. **No accent, no rule, no bracket, no numeral.** Count it: **4**.

**2. No second hairline.** `CLEAR` joins the existing column at the 16px rhythm, under the one hairline
Phase 7 gave region 6. **A-46 retired the second hairline with the tier it was separating**, so this
plan adds no spacing usage at all and §6's `lg` row is amended rather than extended. **Confirm with
`git diff` that `ChosenPanel.svelte` gained a caption and a control and no `border-block-start`.**

**3. Order and weight, asserted.** DOM order `PUT BACK` → `KEEP ON DEVICE` → `CLEAR` → `COPY LINK` at
a 16px rhythm, one layout at every width. Then assert what did **not** change: `TRY ON DEVICE` is
still the one accent fill, `KEEP ON DEVICE` is still Quiet, and their computed weights are **not**
equalised. That assertion is SAFE-02 surviving D-04 and it is the one most worth having.

**4. The `FACTORY DEFAULT` block** in `InstallState.svelte`: caption `FACTORY DEFAULT` (15), body
(115). Plus CLEAR's own details on the **three reused** failure states, selected by `lastAction` —
`partial`, `lost`, `nothing-landed`. The `nothing-landed` detail is Phase 7's `PUT BACK` form reused
**verbatim and imported** (96).

**Assert the on-screen-control rule**: the body names `PUT BACK`, and in phase `cleared` `PUT BACK` is
**enabled** — so the pairing holds. Write it as a test over the phase→controls table from 10-12: for
every string that names a control, that control is enabled or at least present in that phase. This is
the one place this phase's copy could break the rule and it deserves a gate rather than a reading.

**5. Control states in `cleared`**, asserted against 10-12's table: `TRY ON DEVICE` enabled, `PUT BACK`
enabled, `KEEP ON DEVICE` disabled with `Available after a try-on.` — **the closed set of six is
unchanged** — and `CLEAR` enabled.

**6. DEGR-02.** On a browser that cannot write, `CLEAR` is **present and disabled** with
`This browser cannot write to a ZONA.` inline. Contrast it with `PUT BACK`, which Z-12 makes **absent**
— put both behaviours in one assertion so the difference is deliberate and visible.

**7. `device-ui.spec.ts` stays at 13.** Every assertion in this task lands **inside an existing test**,
named in the SUMMARY: the order and weight assertions inside the tier test, the phase→controls pairing
inside the existing states test, DEGR-02 inside the existing capability test. **If any of them will not
fit inside an existing test, say so and state the new count** — this plan's arithmetic then changes and
10-13.1 and 10-14's chains change with it, which is a thing to name rather than absorb.

**Two negative checks:**

- Make `CLEAR` absent under DEGR-02 instead of disabled. Expect the DEGR-02 assertion red, naming the
  difference from `PUT BACK`.
- Give `KEEP ON DEVICE` the same visual weight as `TRY ON DEVICE`. Expect the SAFE-02 weight assertion
  red. **If it does not fire, add it** — D-04 is exactly the change that would make that regression
  attractive, and with `CLEAR` now in Quiet beside `KEEP ON DEVICE` there is one more control that
  would be dragged up with it.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/device-ui.spec.ts src/lib/device/install-copy.spec.ts src/lib/ui/tune-ui.spec.ts</automated>
    Expect `device-ui.spec.ts` **13 passed**, `install-copy.spec.ts` **6 passed** with 9/7/7/13/4.
    Plus `git diff --stat HEAD -- src/lib/ui/ChosenPanel.svelte` showing the caption and the control
    only — `:217`'s `min-block-size: 152px` **untouched** and no `border-block-start` added.
  </verify>

  <done>
`NEXT` is one word under the existing hairline, `CLEAR` is in the column at the 16px rhythm, and no
hairline was added. SAFE-02's two weights are asserted unchanged. The `FACTORY DEFAULT` block exists
and the control-naming rule has a gate. DEGR-02 teaches rather than hides, and the contrast with
`PUT BACK` is one assertion. `device-ui.spec.ts` is still 13, with every new assertion named inside the
existing test that absorbed it. Region 4's 152px is byte-untouched.
  </done>
</task>

<task type="auto">
  <name>Task 10-13-03: The browser walk, row C's fold, and the docs</name>
  <files>e2e/install.e2e.ts, docs/INSTALL-RUNBOOK.md, docs/TESTING.md</files>

  <read_first>
    - `e2e/install.e2e.ts` in full — its six untagged probe walks, its four untagged titles on
      `/c/aurora/`, and its one `@webkit` title
    - `e2e/fake-zona.ts` and `e2e/fake-serial.ts` — the scripted module
    - `docs/INSTALL-RUNBOOK.md:143-172` — the seven rows' exact column shape, and row C in particular
    - `docs/TESTING.md:840`
  </read_first>

  <action>
**1. Two e2e titles**, tagged so both projects run them (**+2 × 2 = +4**):

1. **The clear walk.** Connect against the fake, choose a card, `TRY ON DEVICE` to `settled`, then
   `CLEAR` → **no confirmation appears and the click sends** → `CLEARING…` with `aria-busy` →
   `FACTORY DEFAULT` with its body → `PUT BACK` enabled → click it → `restored`. Assert the live
   region said `The page is reset to factory default.` exactly once. **Assert no `clear-confirm`
   element ever appears** — that is the cheapest possible proof that A-45 shipped rather than being
   merely planned.
2. **The degrade walk.** With `navigator.serial` deleted via `addInitScript`, `CLEAR` is **present and
   disabled** with its reason inline, beside `PUT BACK` being **absent**. This is the branch a large
   share of visitors hit and the one no manual tester remembers to check.

**Then re-run the two never-writes browser assertions unchanged**: `install.e2e.ts` test 1 and
`session.e2e.ts`'s seven cable tests. **Their wording does not change and their fact is stronger** —
"zero writes of any class" now covers four clicks. Record that they are unchanged in wording and were
re-run, because a phase that adds a write and does not re-run them has not finished.

**2. `docs/INSTALL-RUNBOOK.md` — extend row C. Do not add a row H (A-51).**

Row C is `PUT BACK` (SAFE-03) and its "Do this" is currently *"Click `PUT BACK`. Do not touch anything
else."* It becomes: click `PUT BACK` and check the pad as it is today; then click `CLEAR`, **watch the
pad**, then click `PUT BACK` again and watch it come back.

Its "Passes when" gains, in the same voice as the rest of the column:

- `CLEAR` sends **with no confirmation** — one click, the label reads `CLEARING…` for an instant, and
  the block reads `FACTORY DEFAULT` over its body;
- **the pad goes dark at rest and lights a soft white bloom under a finger**, brightest under the
  fingertip and falling off by distance. **Not "the pad goes dark" full stop** — the firmware's
  default Setup installs a proximity-weighted touch callback, so a dark *resting* pad that stays dark
  under a finger is a **failure**, and that is the single most valuable observation in the row;
- `PUT BACK` is enabled throughout, and after the second click the pad is doing what it did before.

Its "Why a machine cannot" gains: the fake acknowledges what it is told to acknowledge, so only a
module can show that two `CONFIG/EXECUTE` frames carrying the pinned package's own `defaultConfig`
actually **run** rather than merely being acknowledged — and that the firmware default behaves the way
its Lua reads.

**Add the caution the other write rows carry**: do not pull the cable while the label reads
`CLEARING…`.

**Do not edit the section's "Seven rows" sentence or its time estimate.** State in the SUMMARY that
you did not, and why: the fold adds an observation, not a row.

**3. `docs/TESTING.md`.** `:840`'s "the three clicks" → four. Move the per-file counts this phase has
changed so far — `identity.spec.ts` 7, `device-ui.spec.ts` **13**, `session.spec.ts` 21 (ten needles),
`install.spec.ts` 21, `constants.spec.ts` **7**, `install-copy.spec.ts` 6 with its four enumerations —
and add the new suites' rows. **Do not write the phase totals here**; 10-14 re-measures and writes them.

**Negative check:** delete row C's clear paragraph and run whatever gates the runbook (`audition.spec.ts`
gates the audition, not the runbook — **check whether anything gates `INSTALL-RUNBOOK.md` at all and
say so**). If nothing does, record it as a finding: the runbook is prose with no gate, which is why its
row-count sentence has to be trusted by hand and why a later phase might want a `ROW_COUNT` of its own.
**This is a stronger finding after A-51 than before it**, because the fold means the row count did not
change and nothing would have caught it if it had.
  </action>

  <verify>
    <automated>npx playwright test e2e/install.e2e.ts e2e/session.e2e.ts --workers 3</automated>
    Then the full suite:
    `npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright PREV_E2E+4`,
    with the arithmetic written out — `PREV_E2E` carried in is **97**, leaving at **101** — and
    `test-results/` removed by hand, and `git status --porcelain` empty.
  </verify>

  <done>
Two new titles run in both projects and walk the fourth click end to end against the fake, including
the degrade branch, and one of them proves no confirmation element exists. The two never-writes browser
assertions are unchanged in wording and were re-run. Row C carries the clear with the honest
observation — dark at rest, a proximity bloom under a finger — its cable caution, and its widened "why
a machine cannot"; the section still says seven rows and that is stated deliberately.
`docs/TESTING.md:840` says four. Whether anything gates the runbook is recorded either way.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+2` — **and the +2 is
  the sum of this plan's own tasks, written out**: `device-ui.spec.ts` 11 → **13** (+2, task 01),
  `install-copy.spec.ts` **6, unchanged**, `install.spec.ts` **21, unchanged**, task 02 **+0** with
  every assertion named inside the existing test that absorbed it, task 03 **+0** in node, and **no
  spec file is created**.
  **The old +3 is retired by name, not renumbered**: it was `device-ui.spec.ts` +3 for the cap test,
  the tracking-uniqueness test and the two-component walk. A-46 retired the tracking test with the
  Bare tier and A-45 removed the second component, so the term is **+2**: the cap test and a
  shapelessness test that reads two controls instead of one.
- `PREV_FILES` carried in is **80** and leaves at **80**; `PREV_TESTS` carried in is **819** and leaves
  at **821**; `PREV_E2E` carried in is **97** and leaves at **101**.
- `npx playwright test --workers 3` at `PREV_E2E + 4`; `test-results/` removed; tree clean.
- A script prints `grep -rc "0.28em" src/lib/ui/` of **0** and finds no `clear-confirm` testid.
- Every string counted by script with its number stated and matched against §13.3.
- `git diff --stat HEAD -- src/vendor/ src/lib/ui/Coverflow.svelte` empty; `ChosenPanel.svelte:217`
  unchanged; `docs/INSTALL-RUNBOOK.md`'s "Seven rows" sentence unchanged.
</verification>

<success_criteria>
1. Three controls read as one sequence through a caption, DOM order and enablement — and SAFE-02's
   two weights are asserted unchanged, with no hairline added.
2. `CLEAR` is Quiet tier, shapeless, with both 44px axes and a 48px cell whose second line is
   documented as headroom — and the SUMMARY says plainly that it looks the same as `KEEP ON DEVICE` at
   rest, with A-47's three channels named.
3. The confirmation that was cut is **proved absent**, in node and in the browser.
4. DEGR-02 teaches rather than hides, and the contrast with `PUT BACK`'s absence is one assertion.
5. The fourth click is walked end to end in both projects against the fake, and the two never-writes
   browser assertions were re-run unchanged.
6. Runbook row C carries the clear with the right thing to look for, the section still says seven
   rows, and no agent touched a device.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-13-SUMMARY.md` with the eleven-name block
carried, every counted string, the `0.28em` count of zero, the phase→controls table as rendered,
`DEVICE_COMPONENTS`'s observed before-and-after length and any disagreement with 10-13.1's recorded
figure, the eight negative checks including whether the SAFE-02 weight assertion had to be added, the
e2e delta, the plain statement that `CLEAR` and `KEEP ON DEVICE` are visually identical at rest with
A-47's three channels, and whether anything gates `INSTALL-RUNBOOK.md`.
</output>
