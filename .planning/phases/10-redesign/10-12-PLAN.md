---
phase: 10-redesign
plan: 12
type: execute
wave: 12
depends_on: ["10-11"]
files_modified:
  - src/lib/protocol/constants.ts
  - src/lib/protocol/constants.spec.ts
  - src/lib/device/install.svelte.ts
  - src/lib/device/install.spec.ts
  - src/lib/device/install-copy.ts
  - src/lib/device/install-copy.spec.ts
  - src/lib/device/session.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SAFE-01, SAFE-03, SAFE-05, SAFE-07, DEGR-02]

must_haves:
  truths:
    - "A fourth device write exists, it writes RAM and never flash, and it is called done only when both CONFIG/ACKNOWLEDGE frames have arrived."
    - "What it writes is the firmware's own default configuration - Setup 641, Timer 22 - read from the pinned package rather than hand-authored, so CLEAR's bytes track firmware exactly as every other wire fact in this project does."
    - "The pad is not dead after a clear: the default Setup is a proximity-weighted touch highlight, and the panel says a true sentence about it."
    - "CLEAR refuses to run without a snapshot, so SAFE-03 is satisfied by construction rather than by a check somebody remembered to write."
    - "The never-writes-without-a-click proof got wider, not weaker: four classes of click, counted by class, with the source scan's needles grown so a clear.bind-shaped export cannot slip past."
    - "The machine went from fourteen states to fifteen, not eighteen, because a clear that half-lands is already partial, one whose link drops is already lost, and one where neither script got through is already nothing-landed."
    - "The number of write clicks stopped being a word in prose and became a constant of length four, asserted equal to the four control labels."
    - "The 131,101-byte protocol chunk did not reach the front door's static graph, because the two default strings are read through the lazily resolved protocol module and never from install-copy.ts."
  artifacts:
    - path: "src/lib/protocol/constants.ts"
      provides: "TOUCH_DEFAULT_SETUP and TOUCH_DEFAULT_TIMER, selected from the existing TOUCH_EVENTS by event number and never by array position"
      contains: "TOUCH_DEFAULT_SETUP"
    - path: "src/lib/device/install.svelte.ts"
      provides: "InstallAction widened to four, the cleared phase, WRITABLE_PHASES gaining it, and the clear sequencer"
      contains: "\"clear\""
    - path: "src/lib/device/install-copy.ts"
      provides: "Nine control labels, thirteen utterances, seven failure builders unchanged, four caps, and WRITE_CLICKS"
      contains: "WRITE_CLICKS"
  key_links:
    - from: "src/lib/device/install.svelte.ts"
      to: "src/lib/protocol/constants.ts"
      via: "the clear sequencer reads the two defaultConfig strings through the lazily resolved HeavyModules.P, never from copy"
      pattern: "TOUCH_DEFAULT_"
    - from: "src/lib/device/install.svelte.ts"
      to: "src/lib/device/snapshot.ts"
      via: "CLEAR is enabled only when a snapshot exists; the record is read and never written"
      pattern: "snapshot"
    - from: "src/lib/device/install-copy.ts"
      to: ".planning/REQUIREMENTS.md"
      via: "WRITE_CLICKS replaces the word three in the SAFE-01 closure record"
      pattern: "WRITE_CLICKS"
---

<objective>
CLEAR, part one: the machine and the copy. A fourth click that extends the never-writes proof rather
than denting it.

Wave 12. Blocked on 10-11. The control, the `FACTORY DEFAULT` block, the browser walk and the runbook
are 10-13's; this plan is everything that can be proved in node.

**REWRITTEN 2026-09-08 for D-19, D-20 and D-21.** The plan this replaces was written against a design
the user has since simplified in three messages, and it was wrong in enumerable ways: it wrote *empty*
Setup and Timer, it said "the copy says exactly that, twice", and it built toward a confirmation block
that no longer exists. What follows is the corrected version. **Nothing renumbers**: this is still
plan 12 at wave 12, still blocked on 10-11, and 10-13, 10-13.1 and 10-14 keep their numbers and their
`depends_on`.

**A-48 — CLEAR writes the firmware's own default configuration, not emptiness.** Read from the
sibling source rather than assumed: `grid-editor/src/renderer/runtime/operations.ts:167`
`clearElement()` is `resetDefault()` followed by `sendToGrid()`, and `runtime.ts:1352` shows
`resetDefault()` walking `grid.get_element_events(type)` and taking each event's **`defaultConfig`**.
Measured from the pin this repository already holds (`1.20260825.1135`), `ElementType.TOUCH`:

| Event | Raw | Compressed | Of 908 |
|---|---|---|---|
| 0, Setup | **641** | **641** | inside |
| 6, Timer | **22** | **22** | inside |

Both are canonical — raw equals compressed — so **nothing has to be fitted**. The Setup default zeroes
all 81 cells on layer 1, sets a dim white base, and installs a `touch_cb` that lights the cells around
a finger by true Euclidean distance. **The pad is not dead after a clear; it does something sensible,
and the panel gets to say so.**

**A-26 — CLEAR writes RAM and never flash, and this survives D-20 intact.** The Editor calls
`sendToGrid()`, not `store()`. Two `CONFIG/EXECUTE` frames, **no `PAGESTORE/EXECUTE`**, and a power
cycle brings back whatever is stored. **The copy no longer says so** — D-21 fixed the line at 41
characters and it does not mention the power cycle — so this fact is held by an assertion by class
rather than by a sentence. That is a real trade and it is recorded in §10.5 rather than hidden.

**A-45 — no confirmation.** The block, its `REMOVES` caption, its 101-character sentence, its
affirmative and its `NOT NOW` are dropped before they ship. `ClearConfirm.svelte` is not built.
`KEEP ON DEVICE` keeps its confirmation, because that one is genuinely irreversible.

**A-50 — `cleared` stays a phase of its own, and that was the one open question D-19 left.** Settled
**yes**, and D-20 is what settles it. After a clear the module is running a real, nameable
configuration. `settled` would claim *this* configuration is on the pad. `restored` would claim the
visitor's own is back — and that one is not merely inaccurate, it is **unsafe**, because a panel
reading `RESTORED` tells a visitor not to click `PUT BACK`, which is the one control that actually
would restore them. **Fifteen states, not fourteen and not eighteen.** The confirmation was the
extravagance; the state is one union member, one `WRITABLE_PHASES` entry, one table row and one block.

**A-28 — three failure states reused, not three invented.** `partial` already means "one of two
landed", `lost` "the connection went", `nothing-landed` "zero of two" — which is exactly how a clear
fails. The copy is selected by `lastAction`, which the store already tracks.

**G-06 is a no-op on the assertion, and that is the finding.** `install.spec.ts` test 4 counts writes
**by class**, and CLEAR writes `CONFIG/EXECUTE` — the class already counted — so the enumeration needs
no widening and its reach is already total. What actually widens is `InstallAction`, and the compiler
enforces that for free. **The amendment is documentary**: the test's comment and `docs/TESTING.md:840`
become four so a later reader does not think the fourth click was missed. (The doc line is 10-13's.)
A-53 records that the approved §10.7 said the opposite of its own G-06 row, and that G-06 was right.

**A staleness this plan corrects by name.** `REQUIREMENTS.md:169` says `session.spec.ts` test 15 has
**eight** needles. It has **nine** today — `.write(`, `RequestQueue`, `hostHeartbeat`, `sendConfig`,
`storePage`, `fetchConfig`, `storeToFlash`, `writeBack`, `setInterval`. That was stale before this
phase. It becomes **ten** here, and the record is corrected in the same named amendment.

Purpose: SAFE-03 and SAFE-07 extended to a fourth click; SAFE-01's proof widened; **SAFE-05 recorded
as NOT extended**, which is the honest outcome of A-45.
Output: two protocol exports, one new action, one new phase, one sequencer, two more labels, one more
utterance, one more cap, one new constant, one corrected record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redesign/10-CONTEXT.md
@.planning/phases/10-redesign/10-VALIDATION.md
@.planning/phases/10-redesign/10-UI-SPEC.md
@.planning/phases/10-redesign/10-03-SUMMARY.md
@.planning/phases/10-redesign/10-11-SUMMARY.md
@src/lib/protocol/constants.ts
@src/lib/protocol/constants.spec.ts
@src/lib/protocol/write-guard.ts
@src/lib/device/install.svelte.ts
@src/lib/device/install-copy.ts
@src/lib/device/install.spec.ts
@src/lib/device/session.spec.ts
@src/lib/device/snapshot.ts
@src/lib/transport/fixtures/synthetic-zona.json

<standing_rules>
- **Never edit anything under `src/vendor/`.** **Never touch a sibling repository** — `grid-editor` is
  read only, and the two line references above are to be *read*, never checked out or restored.
- **No agent connects to or writes to a device, and no agent deploys.** Every clear in this plan runs
  against `FakeTransport` and the scripted ZONA. **The hardware half is runbook row C and it is the
  user's; it is 10-13's to write and nobody's to perform.**
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults. `npm run format`, then `npm run lint`.
- **PowerShell is the shell.** Long content goes through the Write tool, never a heredoc. Commit with
  `git commit -F <scratch message file> --only -- <paths>`.
- Read `npm run check` with `npm run check 2>&1 | grep -Ei "error|warning"`.
- **`hangar.snapshot.v1` is READ by CLEAR and never written by it.** A returning visitor's record
  predates this feature and `PUT BACK` must still work from it.
- **Every user-facing string is counted by script and its number stated.**
- **State every count as a carried name plus a delta.**
- The project spends no money.
</standing_rules>

<interfaces>
**Where the payload lives, and this is the load-bearing decision of the plan.**

`install-copy.ts` **imports nothing** — zero specifiers, `import type` included — because Phase 4's
chunk guard matches specifier text and the header's device disclosure paints on the first frame of
`/`. The pinned package is the **131,101-byte chunk** `config-shape.spec.ts` test 13 keeps out of that
graph. **So the two default strings are not copy and do not go in `install-copy.ts`.**

They are read from `$lib/protocol`, which the store already resolves lazily as `HeavyModules.P`.
`constants.ts` already exports `TOUCH_EVENTS` (`grid.get_element_events(ElementType.TOUCH)`); this
plan adds two derived exports beside it:

```ts
/** The firmware's own Setup for the touch element - what the Editor's clear writes back (A-48). */
export const TOUCH_DEFAULT_SETUP = defaultFor(EVENT_SETUP);
/** The firmware's own Timer for the touch element. */
export const TOUCH_DEFAULT_TIMER = defaultFor(EVENT_TIMER);
```

`defaultFor` selects **by event number**, never by array position, and **throws at module load** if
the event is absent — so a pin bump that drops or renumbers an event fails loudly instead of writing
`undefined` to somebody's module.

**The machine's changes, each named:**

| Change | Detail |
|---|---|
| `InstallAction` | `"try" \| "put-back" \| "keep" \| "clear"` — **four**. Every `switch` over it is re-checked for exhaustiveness, which the compiler does for free once the union widens. **Run `npm run check` immediately after widening it and record every site the compiler named** — that list is the audit, and any file on it that is not in `files_modified` is added to the SUMMARY by name |
| New phase **I14 `cleared`** | entered when **both** `CONFIG/ACKNOWLEDGE` frames for the two default scripts arrived. **SAFE-07 holds verbatim: `cleared` means an ACK, never a resolved writer promise** |
| `WRITABLE_PHASES` | gains `"cleared"`, so CLEAR after CLEAR is possible (idempotent and harmless) and `TRY ON DEVICE` works from it |
| Failure states | **reused, not invented**: `partial`, `lost`, `nothing-landed`, each rendering CLEAR's own detail selected by `lastAction` |
| Control states in `cleared` | `TRY ON DEVICE` **enabled** · `PUT BACK` **enabled** (a snapshot exists by construction) · `KEEP ON DEVICE` **disabled**, reason `Available after a try-on.` — **the closed set of six is unchanged** · `CLEAR` **enabled** |
| Busy label | `CLEARING…` (**9**) through its one leg, with `aria-busy`. **Nothing animates**, exactly as Z-09 rules for every other write; the 2000 ms `Still writing.` line and its single utterance apply unchanged |
| Header write lock | engaged during a clear exactly as during every other write (Z-15) |
| **No confirmation** | there is no `pendingClear` flag, no block state and no second affirmative. The click sends (A-45) |

**Enablement — one rule, and SAFE-03 is satisfied by construction:**

> `phase ∈ WRITABLE_PHASES && snapshot != null && capability.canWrite`

Enabled in `ready`, `settled`, `restored`, `kept`, `partial`, `nothing-landed`, `unconfirmed`,
`kept-mismatch`, `restored-unconfirmed` and the new `cleared`. Disabled in `idle`, `snapshotting`,
`writing`, `lost`, `snapshot-failed`. **No snapshot, no clear.**

**Three disabled reasons, and only one is new:**

| Cause | Reason | n |
|---|---|---|
| No snapshot yet | `Needs a copy of what is on your ZONA first.` | **43** — new |
| No session | `Needs your ZONA connected.` | 26 — Phase 7's `PUT BACK` string, **reused verbatim** |
| Cannot write here (DEGR-02) | `This browser cannot write to a ZONA.` | 36 — Phase 7's `KEEP ON DEVICE` reason, **reused verbatim** |

**The counts that change in `install-copy.spec.ts` (G-04)**, each an arithmetic the existing tests
already do — only the literals move:

| Assertion | Was | Becomes |
|---|---|---|
| control labels (`:358`, counted by `_LABEL` suffix) | 7 | **9** (`CLEAR`, `CLEARING…`) |
| failure builders (`:403`) | 7 | **7 — unchanged, and that is the point of reusing the three** |
| distinct failure titles (`:416`) | 7 | **7** |
| utterances (`:428`) | 12 | **13** (`The page is reset to factory default.`, **37**) |
| character caps | 3 | **4** (`HONESTY_CAP`, `PUT_BACK_CAP`, `KEEP_CAP`, `CLEAR_CAP`) — `CLEAR_CAP` is `2 × CH_PER_LINE` = **86**, exactly as `install-copy.ts`'s shipped header comment already announces |

**`CLEAR_CAP` is 86 and the cell is 48px even though the longest string is now 43 (A-52).** After D-21
the four candidates in the cell are the line at **41** and the three reasons at **43 / 26 / 36**, so
§12's formula (`ceil(longest / 43) × 24`) would give one line and 24px. **Do not take it.** A one-line
cap of 43 would put a shipped string exactly on its own cap — the zero-headroom defect 10-01 flagged
against the old `CLEAR_LINE` at 86, reintroduced at a different number. Two lines is the smallest
reservation that leaves the cap a promise about strings not yet written. **The second line is declared
headroom rather than occupancy, and the comment beside it says so** so a later reader does not
"correct" it to 24px.

**`WRITE_CLICKS`, and why it is a constant.**

```ts
export const WRITE_CLICKS = [
  "TRY ON DEVICE", "PUT BACK", "KEEP ON DEVICE", "CLEAR",
] as const;
```

Asserted to have **length 4** and asserted **equal to the four control labels**. *The number has now
changed once and will change again; a constant moves, a sentence rots.*

**The never-writes proof, extended and never weakened:**

| Assertion | Extension |
|---|---|
| `install.spec.ts` test 4 — zero config writes across connect, snapshot and every knob move, **counted by class** | the class enumeration already covers `CONFIG/EXECUTE`. **The wording is unchanged and its reach is wider** (G-06). Amend its comment to say four clicks |
| `e2e/install.e2e.ts` test 1, `e2e/session.e2e.ts`'s seven cable tests | unchanged in wording, stronger in fact — 10-13's to run |
| `session.spec.ts` test 15's source scan | **nine needles become ten.** A `clear.bind`-shaped export must not slip past the scan that exists to catch exactly that shape |
| `src/lib/protocol/write-guard.ts:24-58` | **unchanged.** `canWriteBack` gates the snapshot fetch and its verdict *is* `capability.canWrite`, which is a term of CLEAR's own enablement rule, so CLEAR is behind the same guard by construction. **Assert that routing** rather than trusting it. **Note the path**: A-53 records that the approved spec and revision 1 of this plan both wrote `write-guard.ts` as if it lived in `src/lib/device/`; it does not |
| `REQUIREMENTS.md:169` | "one of three clicks" → **four**, amended by name and dated; and "eight needles" → **ten**, with a sentence recording that eight was already stale at nine before this phase |

**`SAFE-05` is NOT extended, and this plan is where that is written down.** The approved §4 ruling read
*"Extended. CLEAR's confirmation names what is removed and what a power cycle brings back."* There is
no CLEAR confirmation (A-45), and the requirement should not reach it anyway: SAFE-05's subject is the
control that **stores to flash**, and CLEAR writes RAM only. **`REQUIREMENTS.md`'s SAFE-05 row is left
exactly as Phase 7 wrote it** — nothing was extended, so nothing is claimed. Say so in the SUMMARY;
an unchanged requirement that a plan claimed to extend is how a traceability table starts lying.

**What this plan does NOT do:** it renders nothing. `Clear.svelte`, the `NEXT` caption, the
`FACTORY DEFAULT` block, DEGR-02's present-but-disabled rendering, the e2e walk,
`INSTALL-RUNBOOK.md` row C's fold and `docs/TESTING.md:840` are all **10-13's**. And it does **not**
touch `src/lib/protocol-pin.spec.ts`: 10-VALIDATION lists that file among the standing gates that are
green at the phase gate and **not edited** this phase, so the canonicity assertion goes in
`constants.spec.ts` beside the export it pins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-12-01: The two firmware defaults, the fourth action, the fifteenth phase, and the clear sequencer</name>
  <files>src/lib/protocol/constants.ts, src/lib/protocol/constants.spec.ts, src/lib/device/install.svelte.ts, src/lib/device/install.spec.ts</files>

  <read_first>
    - `src/lib/protocol/constants.ts` — `TOUCH_EVENTS`, `EVENT_SETUP`, `EVENT_TIMER`, `CONFIG_MAX`
    - `src/lib/protocol/constants.spec.ts` — its five tests and how they read the package
    - `src/lib/device/install.svelte.ts` in full — `:199-260` (the phases and `WRITABLE_PHASES`),
      the try-on and put-back sequencers, `#classify` at `:1021`
    - `src/lib/device/install.spec.ts` — especially test 4's by-class enumeration
    - `src/lib/transport/fixtures/synthetic-zona.json` — what the scripted module acknowledges
    - `10-UI-SPEC.md` §10.5, §10.6, §10.7 as amended by A-45 to A-53
  </read_first>

  <behavior>
    - `TOUCH_DEFAULT_SETUP` is 641 characters and `TOUCH_DEFAULT_TIMER` is 22, both printable ASCII,
      both under `CONFIG_MAX`, both selected by event number.
    - `GridScript.compressScript(s) === s` for both, after `initLuaFormatter()` — they are canonical.
    - A clear from a phase with a snapshot writes exactly two `CONFIG/EXECUTE` and zero `PAGESTORE`.
    - `cleared` is entered only when both `CONFIG/ACKNOWLEDGE` frames have arrived; one ACK is
      `partial`.
    - A clear with `snapshot === null` performs zero writes of any class.
  </behavior>

  <action>
**1. The two exports, first, in `src/lib/protocol/constants.ts`.** A local `defaultFor(event: number)`
that finds the entry in `TOUCH_EVENTS` whose `value` matches and returns its `defaultConfig`, throwing
with the event number in the message if it is absent. Two exports, each with a one-line doc comment
naming A-48 and saying what the string *is* — the Setup is a proximity-weighted touch highlight, the
Timer is a debug print that never fires unless something else started a timer.

**2. `constants.spec.ts` goes 5 → 7 tests:**

- **the shape**: both defaults are defined, `TOUCH_DEFAULT_SETUP.length` is **641** and
  `TOUCH_DEFAULT_TIMER.length` is **22**, both match `PRINTABLE_ASCII`, both are `< CONFIG_MAX`, and
  both are selected by number — assert `defaultFor` throws for an event the element does not declare;
- **the canonicity**: with `await initLuaFormatter()` in a `beforeAll`,
  `GridScript.compressScript(s) === s` for both. **This is the gate that would go red on a pin bump**,
  which is the entire reason it is worth a test rather than a comment. Write the reason in the test's
  own comment, naming the datestamp versioning.

**3. Widen `InstallAction`, then run `npm run check`.** Record every site the compiler names. That
list **is** the exhaustiveness audit and it belongs in the SUMMARY; do not fix the sites before
recording them.

**4. `cleared` as the fifteenth phase**, and `WRITABLE_PHASES` gains it. Update the type's header
comment, which enumerates the phases and their causes. **Write A-50's reasoning into that comment in
one sentence** — `restored` would tell a visitor not to click the control that would actually restore
them — because the next reader's first instinct will be to collapse this state into an existing one.

**5. The sequencer.** Two `CONFIG/EXECUTE` frames carrying `TOUCH_DEFAULT_SETUP` and
`TOUCH_DEFAULT_TIMER`, in the same order and through the same writer every other write uses. **No
`PAGESTORE/EXECUTE`** — assert its absence by class in the test, not by reading the code. `cleared` is
set **only** when both `CONFIG/ACKNOWLEDGE` frames have arrived; a resolved writer promise is not
enough and `install.spec.ts` gets a test that drives exactly that distinction (SAFE-07's shape, copied
from the try-on's).

Failure routing through `#classify`: one ACK → `partial`; connection dropped → `lost`; neither →
`nothing-landed`. `lastAction` is `"clear"` in each, which is what selects the copy in 10-13.

**No compiler, and say so in a comment.** The try-on path compiles a configuration; a clear does not —
both defaults are already canonical, `sendConfig` takes a plain string, and nothing on this path needs
`initLuaFormatter()`. A future reader adding an `await padCompilerReady()` here would be adding a
628 KB dependency to the cheapest write on the site.

**6. Enablement.** One expression, in one place:
`phase ∈ WRITABLE_PHASES && snapshot != null && capability.canWrite`. Assert the whole
enabled/disabled partition over all fifteen phases — a table test, fifteen rows — so the list cannot
drift. **SAFE-03 by construction**: a test that clears with `snapshot === null` and observes **zero
writes of any class**.

**7. `install.spec.ts` goes 18 → 21 tests:**

- the clear sequencer's happy path: two `CONFIG/EXECUTE` **carrying the two default strings verbatim**,
  zero `PAGESTORE`, `cleared` from both ACKs;
- SAFE-07: a resolved writer promise with one ACK missing lands `partial`, not `cleared`;
- SAFE-03: no snapshot → refused, zero writes, and the reason is the 43-character one.

Amend test 4's comment to say **four** clicks and record that its assertion did not change (G-06's
documentary finding, and A-53's correction of the spec text that said otherwise).

**8. `write-guard`.** Assert CLEAR is routed through it — a session whose snapshot fetch failed
`canWriteBack` has `capability.canWrite` false, so CLEAR is disabled and a clear attempted anyway
performs zero writes. **Read the path from `src/lib/protocol/write-guard.ts`**, not `src/lib/device/`.

**Three negative checks:**

- Set `cleared` from the writer promise instead of from the ACKs. Expect the SAFE-07 test red.
- Add a `PAGESTORE/EXECUTE` to the clear sequence. Expect the by-class assertion red, naming the
  class. **This is the check that proves A-26 is enforced rather than intended.**
- Select the defaults by array position (`TOUCH_EVENTS[0]`, `TOUCH_EVENTS[1]`) instead of by event
  number. **State whether anything goes red.** Today the order happens to match, so this check
  probably passes — which is the finding, and it is why `defaultFor` throws on a missing event
  instead: the position is a coincidence and the number is the contract.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/protocol/constants.spec.ts src/lib/device/install.spec.ts src/lib/device/snapshot.spec.ts && npm run check 2>&1 | grep -Ei "error|warning"</automated>
    Expect `constants.spec.ts` **7 passed**, `install.spec.ts` **21 passed**, `snapshot.spec.ts`
    **7 passed and unchanged** — the record is read, never written, and this is how that is said.
  </verify>

  <done>
`TOUCH_DEFAULT_SETUP` (641) and `TOUCH_DEFAULT_TIMER` (22) are exported from `$lib/protocol`, selected
by event number, and pinned at their lengths and their canonicity by two new tests. `InstallAction` is
four and the compiler's exhaustiveness list is recorded. `cleared` is the fifteenth phase, entered only
from both ACKs, with A-50's reasoning in its comment. The clear writes two `CONFIG/EXECUTE` carrying
the two defaults and no `PAGESTORE`, proved by class. Enablement is one expression asserted over a
fifteen-row table, and a clear without a snapshot performs zero writes. `constants.spec.ts` is 7 and
`install.spec.ts` is 21. Three negative checks recorded, including whether the by-position check went
red.
  </done>
</task>

<task type="auto">
  <name>Task 10-12-02: Nine labels, thirteen utterances, four caps, WRITE_CLICKS, and a tenth needle</name>
  <files>src/lib/device/install-copy.ts, src/lib/device/install-copy.spec.ts, src/lib/device/session.spec.ts, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `src/lib/device/install-copy.ts` — the cap comment 10-03 rewrote, its `CLEAR_CAP` paragraph, and
      the `_LABEL` exports
    - `install-copy.spec.ts:355-440` — how each of the four enumerations is counted
    - `session.spec.ts:1141-1151` — the nine needles, confirmed with `grep -n` first
    - `10-UI-SPEC.md` §10.5, §10.6, §13.3 as amended by A-45 to A-52
  </read_first>

  <action>
**1. The strings, each counted by script with its number stated:**

| Export | Literal | n |
|---|---|---|
| `CLEAR_LABEL` | `CLEAR` | **5** |
| `CLEAR_BUSY_LABEL` | `CLEARING…` | **9** (U+2026) |
| `CLEAR_LINE` | `Reset the current page to factory default` | **41** — D-21, verbatim, against a cap of 86 |
| reason — no snapshot | `Needs a copy of what is on your ZONA first.` | **43** — new |
| reason — no session | `Needs your ZONA connected.` | 26 — **reused, imported not retyped** |
| reason — cannot write | `This browser cannot write to a ZONA.` | 36 — **reused, imported not retyped** |
| `cleared` caption | `FACTORY DEFAULT` | **15** |
| `cleared` body | `Your ZONA is running the firmware’s own default configuration. PUT BACK restores what was there when you connected.` | **115** |
| live region | `The page is reset to factory default.` | **37** |
| `nothing-landed` detail after a clear | `Neither script got through. Nothing on the module changed, so what was playing is still playing.` | 96 — **Phase 7's `PUT BACK` form, reused verbatim because it is exactly true of a clear** |

**Three copy rules this table exists to enforce**, and each is checkable:

- **Nothing beside CLEAR says "clears", "empties" or "removes"** (A-48). Scan the four new literals for
  those three stems and assert their absence, with the reason in the message. A control that restores
  a factory default and says it empties something is the same class of lie as the never-writes
  sentence this phase already retired.
- **The caption names the state, not the button.** `FACTORY DEFAULT`, as `PLAYING NOW` names the state
  `TRY ON DEVICE` leaves behind. It is two words, so §5.2's uppercase rule needs no exception.
- **The body names `PUT BACK`, which is enabled in `cleared`**, so the no-string-names-an-absent-control
  rule holds. The gate for that pairing is 10-13's, over the phase→controls table.

**The two reused reasons are imported, not retyped.** `device-ui.spec.ts` already forbids a retyped
install sentence; assert the reuse rather than duplicating the literal.

**2. `CLEAR_CAP`** = `2 × CH_PER_LINE` = **86**, which is exactly what `install-copy.ts`'s header
comment already announces, so that arithmetic needs no edit. **Two sentences in that header do:**

- the cap block's "THREE CAPS - THREE BECOMING FOUR IN PLAN 10-12" becomes four, with all four values
  and their formulas;
- *"CLEAR_LINE is exactly 86 - it fits with zero headroom, and one added character breaks it"* is
  **retired by name and dated**, replaced by the A-52 statement: `CLEAR_LINE` is **41**, the longest
  candidate in the cell is the **43**-character reason, and the second reserved line is **headroom
  rather than occupancy** — kept deliberately so the cap stays a promise about strings not yet
  written, rather than being cut to 43 and landing exactly on a shipped string.

**Never raise a cap to admit its own string.** Nothing in this plan needs it — every literal above is
inside 86 — but the rule stands and the header says so.

**3. `WRITE_CLICKS`**, exported, `as const`, asserted **length 4** and asserted **equal to the four
control labels** (`TRY_ON_LABEL`, `PUT_BACK_LABEL`, `KEEP_LABEL`, `CLEAR_LABEL`) — the equality is what
stops the constant and the labels drifting.

**4. The four enumerations move as the table in `<interfaces>` says.** Labels 7 → **9**, builders
**7**, titles **7**, utterances 12 → **13**, caps 3 → **4**. `install-copy.spec.ts` stays at **6**
tests; only the literals move, because each of those assertions is already an arithmetic over exports.
**If any of them needs restructuring rather than a literal change, say so** — it would mean the
enumeration was written to the tree rather than to the contract.

**5. `session.spec.ts` test 15's tenth needle.** Confirm the nine at `:1141-1151` with `grep -n`
first, then add a `clear`-shaped needle in the same style — the scan exists to catch a
`clear.bind`-shaped export slipping past. **The file's test count does not move** (21).

**6. `REQUIREMENTS.md:169`.** Two clauses, both amended by name and dated 2026-09-08:

- "every write attributable to one of **three** clicks" → **four**, with `WRITE_CLICKS` named as the
  constant that now holds the number, replacing the word in prose;
- "session.spec.ts test 15, **eight** needles" → **ten**, with one sentence recording that **eight was
  already stale at nine before this phase** and that the correction is deliberate rather than a
  renumbering.

**And leave SAFE-05's row alone.** Do not add a CLEAR clause to it. Record in the SUMMARY that the
approved §4 ruling said SAFE-05 was extended by CLEAR's confirmation, that A-45 removed the
confirmation, and that the honest outcome is an unchanged requirement rather than a reworded one.
**Touch no tick-mark in `REQUIREMENTS.md`.**

**Three negative checks:**

- Export `clear` as a `clear.bind`-shaped view. Expect `session.spec.ts` test 15 red on the tenth
  needle.
- Set `WRITE_CLICKS` to length 3. Expect the equality assertion red, naming which label is missing.
- Change `CLEAR_LINE` to say `Empties the current page` and expect the three-stem scan red. **If the
  scan does not fire, say so** — it would mean the rule is a comment rather than a gate.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/device/install-copy.spec.ts src/lib/device/session.spec.ts src/lib/ui/device-ui.spec.ts</automated>
    Expect `install-copy.spec.ts` **6 passed** with 9 / 7 / 7 / 13 / 4, `session.spec.ts` **21 passed**
    with ten needles, `device-ui.spec.ts` **11 passed** (its own growth is 10-13's). Plus a script
    printing every count above: 5 / 9 / 41 / 43 / 26 / 36 / 15 / 115 / 37 / 96.
  </verify>

  <done>
Nine labels, thirteen utterances, seven builders and seven titles unchanged, four caps. `CLEAR_LINE` is
D-21's 41 characters and no string beside CLEAR says clears, empties or removes. The two reused reasons
are imported rather than retyped. `install-copy.ts`'s header carries the four-cap arithmetic and A-52's
headroom statement, with the zero-headroom sentence retired by name. `WRITE_CLICKS` is a constant of
length four asserted equal to the labels. `session.spec.ts` has ten needles. `REQUIREMENTS.md:169`
carries both amendments, dated, including the correction of a figure that was stale before this phase,
and SAFE-05's row is untouched with the reason recorded. Three negative checks recorded.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` prints nothing; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+5` — **and the
  +5 is the sum of this plan's own tasks, written out**: `install.spec.ts` 18 → 21 (**+3**, task 01),
  `constants.spec.ts` 5 → 7 (**+2**, task 01), `install-copy.spec.ts` **6, unchanged**,
  `session.spec.ts` **21, unchanged** (test 15's needles go 9 → 10 *inside* it), `device-ui.spec.ts`
  **11, unchanged** (its growth is 10-13's), `snapshot.spec.ts` **7, unchanged**, and **no spec file
  is created**, so `PREV_FILES` does not move.
  **Two retired terms, both named rather than renumbered:** revision 1 asserted `+4` on a
  `session.spec.ts +1` that this plan's own task 02 denies twice; revision 2 asserted `+3` on a
  payload that did not exist. The `+2` added here is `constants.spec.ts`, and it exists because A-48
  made the payload a protocol fact that needs pinning.
- `PREV_FILES` carried in is **80** and leaves at **80**; `PREV_TESTS` carried in is **814** and leaves
  at **819**. `PREV_E2E` **97**, not run by this plan.
- Every string counted by script with its number stated and matched against §13.3.
- `grep -n "PAGESTORE" src/lib/device/install.svelte.ts` shows it only in the keep path.
- `grep -rn "empt\|removes\|clears" src/lib/device/install-copy.ts` names no CLEAR string.
- `git diff --stat HEAD -- src/vendor/ src/lib/protocol/write-guard.ts src/lib/device/snapshot.ts
  src/lib/protocol-pin.spec.ts` empty.
</verification>

<success_criteria>
1. A fourth write exists, writes RAM only, writes the pinned package's own `defaultConfig`, and is
   called done only from two ACKs.
2. The payload is 641 and 22, canonical, selected by event number, and read through the lazily
   resolved protocol module — never from `install-copy.ts`, whose zero-import rule is intact.
3. CLEAR without a snapshot performs **zero writes of any class**, proved rather than promised.
4. Fifteen phases, not eighteen; three failure states reused; the closed set of six `KEEP` reasons
   unchanged; `cleared`'s reason for existing written into the code that declares it.
5. The never-writes proof is wider on four fronts and weaker on none, and the source scan has ten
   needles.
6. `WRITE_CLICKS` replaced a word in prose with a constant asserted equal to the labels.
7. `REQUIREMENTS.md:169` carries both amendments, dated, including a correction of a pre-existing
   staleness — and SAFE-05 is recorded as **not extended** rather than reworded.
</success_criteria>

<output>
After completion, create `.planning/phases/10-redesign/10-12-SUMMARY.md` with the eleven-name block
carried, the compiler's exhaustiveness site list from widening `InstallAction`, the fifteen-row
enablement table, every counted string with its number, the four enumerations before and after, the
measured lengths and canonicity of the two defaults, and the six negative checks — including whether
the by-position selection went red and whether the three-stem copy scan fired.
</output>
