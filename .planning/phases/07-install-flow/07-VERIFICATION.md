---
phase: 07-install-flow
verified: 2026-09-05T18:10:00Z
status: human_needed
score: 5/5 success criteria have every automatable half verified; 0/5 close outright, because all five carry a (hardware) half that only the user's runbook can answer. Requirements: 5/10 closed outright (SAFE-01, SAFE-07, SAFE-08, SAFE-09, DEGR-02), 5/10 human_needed (SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06)
human_verification:
  - test: "INSTALL-RUNBOOK row A - the module names itself and is remembered (SAFE-04; D-19 timing 1)"
    expected: "After connect the header disclosure's snapshot line reads the DURABLE form (`...is saved in this browser, so it can be put back even in a new tab.`); after a full tab close and reopen the header offers `ZONA detected`, the same line shows again, PUT BACK is enabled. If the SESSION-ONLY form shows (`...is held until this tab closes...`), record it: the module did not answer SERIALNUMBER/FETCH within three attempts and SAFE-04 closes as partially verified"
    why_human: "SERIALNUMBER/FETCH is source-verified and wire-unproven - no Phase 2 capture holds a frame of that class and the desktop editor never sends one; the scripted ZONA answers because it was told to. Everything durable hangs off this answer"
  - test: "INSTALL-RUNBOOK row B - the first RAM write (SAFE-02, SAFE-08; D-19 timing 3)"
    expected: "On /c/aurora/, connected and ZONA IDENTIFIED, one click on TRY ON DEVICE: the pad plays Aurora within about a second and the block reads PLAYING NOW from the two acknowledgements. Record the wall time click-to-PLAYING NOW, whether the border LEDs flashed white, whether it ever timed out, and whether pacing escalated (first click landing `Nothing reached your ZONA` or `Only one of the two scripts landed` with no refusal, then the retry landing; or the probe's `pacing escalated` line)"
    why_human: "Only a module can prove a write landed; a fake acknowledgement proves HANGAR believed one. Two back-to-back 957-byte CONFIG/EXECUTE frames at 0 ms pacing have never met the module's 2,048-byte receive ring. This is the first write from this site to a real ZONA"
  - test: "INSTALL-RUNBOOK row C - PUT BACK (SAFE-03)"
    expected: "One click, no confirmation; PUTTING BACK... for an instant; RESTORED over `Your own Setup and Timer are back on your ZONA's touch element, exactly as they were when you connected.`; the pad is doing exactly what it did before row B; KEEP ON DEVICE disabled again with `Available after a try-on.`"
    why_human: "The suite proves the snapshot's bytes went back out verbatim (install.spec test 5, wire-pin.spec); only a person can say the pad is doing what it did"
  - test: "INSTALL-RUNBOOK row D - a power cycle brings the original back (SAFE-02)"
    expected: "After a TRY ON DEVICE, unplug and replug: the module comes up running its OWN configuration, not Aurora; header flips to ZONA unplugged then offers ZONA detected; one click reconnects; panel reads ZONA IDENTIFIED with PUT BACK enabled"
    why_human: "RAM-only is the whole safety argument for the primary control and RAM is not readable after it is gone; the scripted ZONA's powerCycle() copies flash over RAM because the firmware source says so"
  - test: "INSTALL-RUNBOOK row E - KEEP ON DEVICE survives a power cycle (SAFE-05, D-12; D-19 timing 2)"
    expected: "The confirmation reads PERMANENT / `This replaces the Setup and Timer scripts on your ZONA's touch element, and it survives a power cycle.` / `PUT BACK still restores what was there when you connected.` before anything is sent, with the row's KEEP ON DEVICE gone while the block is open. On commit: KEEPING..., yellow-dim border, the pad goes dark for an instant, then KEPT with the proof line. After unplug/replug the pad runs Aurora. After reconnect and PUT BACK, RESTORED gains `They are stored too, so they stay after a power cycle.` Record the time commit-to-KEPT (or the probe's refetch-setup count after `store ok`: 1, 2 or 3). Keep the cable in while KEEPING... or PUTTING BACK... reads"
    why_human: "Flash is the definition of survives and only a power cycle tests it; the PAGESTORE acknowledgement is sent by the callback that STARTS the page reload, so the number of re-fetch rounds a real store needs has never been measured"
  - test: "INSTALL-RUNBOOK row F - fresh-tab PUT BACK (SAFE-04)"
    expected: "After a memory-only TRY ON DEVICE, quit the browser completely, reopen, connect (one click), choose the same card, PUT BACK: the pad returns to the owner's own configuration, which can only have come from localStorage because the module's memory held Aurora at connect. If row A read the session-only form this row is expected to fail honestly (PUT BACK restores Aurora; a power cycle brings the original back) - record it as row A's consequence"
    why_human: "localStorage surviving a browser restart is profile state; the record being consulted ahead of a fresh copy is proven only against a Map (install.spec test 6). Neither is simulable end to end"
  - test: "INSTALL-RUNBOOK row G (optional) - a rig (SAFE-06)"
    expected: "With a second Grid module attached the slot gains `- with <TYPE>`; after a try-on, KEEP ON DEVICE opens a confirmation carrying a fourth sentence naming the module as the header spells it (`Your EN16 is on the same cable. Its current page is stored too, because the store reaches every module at once.`); its KEEP ON DEVICE is enabled; NOT NOW closes it and nothing was sent. Do not confirm unless willing to store that module's page"
    why_human: "Every one of the 1,550 frames Phase 2 recorded carried SX 0, SY 0; the rig path has met only synthetic heartbeats and a fan-out fake that answers one store three times"
---

# Phase 7: Install Flow Verification Report

**Phase Goal:** A visitor can put a configuration on their own ZONA in about a second, get their original back with one click at any time, and only commit it to flash on purpose.
**Verified:** 2026-09-05T18:10:00Z
**Status:** human_needed
**Re-verification:** No - initial verification

A note on the brief: it asked for success criteria 1 to 6. ROADMAP.md's Phase 7 carries five, all five marked *(hardware)* in full or in part; the report covers those five.

## Gates, run by the verifier (observed, not copied)

| Command | Expected (07-13) | Observed |
| ------- | ---------------- | -------- |
| `npm run check 2>&1 \| grep -Ei "error\|warning"` | 545 files, 0 errors | `COMPLETED 545 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`, exit 0 |
| `npm run lint` | clean | `All matched files use Prettier code style!`, eslint silent, exit 0 |
| `npm run test:quick 2>&1 \| node scripts/check-counts.mjs 73 776` | 73 / 776 | **Twice**, at 1.18 GB free of 15.26 GB with six node processes resident: `Test Files 1 failed \| 72 passed (73)`, `Tests 1 failed \| 775 passed \| 1 todo (777)`, Duration 34.54 s then 36.73 s. The one failure both times: `src/lib/catalog/lua-entries.spec.ts` > "stays canonical and in budget across the whole knob cross-product", `Test timed out in 5000ms` - deferred items 12 and 21 exactly. `check-counts` refuses the summary (no `N passed` line to match) and exits 1. The file alone: `6 passed (6)` in 2.68 s. `git log 145f85d..HEAD -- src/lib/catalog/lua-entries.spec.ts` is empty - no Phase 7 commit touched it. 775 + 1 = 776 is the expected total; the machine, not the tree, is what did not read green. 07-13 observed 73 / 776 with no timeout at 1.73 GB free |
| `npm run test:sweep 2>&1 \| node scripts/check-counts.mjs 3 13` | 3 / 13 | `observed 3 files, 13 tests passed` - matches; Duration 141.28 s |
| `npx playwright test --workers 3 --reporter list` (nothing on 4173/4174 beforehand; `build/` present; `npm run preview` rebuilt and served) | 89 = 78 + 11 | `89 passed (2.1m)`, exit 0; tally 78 `[chromium]` + 11 `[webkit-phone]`; `install.e2e.ts` 12 runs; zero failures, zero retries, no `ProxyController`, no `Network connection lost`; `check-counts --playwright 89` matches; `test-results/` removed afterwards; no listener on 4173/4174 and no workerd/wrangler process after |

Four of five gates match 07-13's numbers first time. The quick gate matches in total and in every file but the load-sensitive cross-product test, whose timeout is the item the phase already carries; it was rerun once as the brief permits and reported as observed.

Console lines from the e2e run worth keeping: `test 8: KEPT after 1 heartbeat(s)`; `test 10: slow line at +2419 ms, KEPT at +2695 ms after 1 heartbeat(s)`; `test 10: RESTORED after 2 heartbeat(s)`; `degrade panel on webkit-phone: document 393 = client 393`; `degrade panel on chromium: document 1281 vs client 1280` (deferred item 20's one pixel, bounded by the test).

## Goal Achievement

### The five success criteria (ROADMAP)

| # | Criterion | Status | Evidence |
| --- | --------- | ------ | -------- |
| 1 | *(hardware)* The connect screen says out loud that nothing is written without an explicit click; at connect, before any write is possible, HANGAR snapshots the touch element's Setup and Timer; every write control stays disabled until the snapshot is present and non-empty | HUMAN_NEEDED (automated half VERIFIED) | `SAFE_PROMISE` = "HANGAR never writes to your ZONA on its own. Nothing reaches the module without a click." (session-copy.ts:221, 88 chars, present tense - the "cannot write at all" clause retired) renders on the pre-connect note (`DeviceNote.svelte:191`) and in the S4 disclosure (`DeviceDetails.svelte:305`). Snapshot order is the gate: `#snapshot` runs fetch-serial, fetch-setup, fetch-timer, `canWriteBack`, memory, then storage, and only then `phase = "ready"` (install.svelte.ts 573-645); `install.spec.ts` test 1 pins the step order and `phases` passing through `snapshotting` before `ready`, with zero `CONFIG/EXECUTE` and `writeLock` false; `install.e2e.ts` test 1 asserts `idle > snapshotting > ready` and the wire by class. Controls: TRY ON DEVICE `disabled` includes `install.phase === "snapshotting"` (TryOnDevice.svelte 260-268); PUT BACK is `absent` with no snapshot (`putBackState`); KEEP ON DEVICE `never-tried`. Non-empty: `P.canWriteBack` refuses an empty string before the record is consulted (D-03 over Z-16; install.spec test 3; deferred items 7/27 record the open ruling). **Hardware half - row A, the module answering SERIALNUMBER/FETCH - unanswered** |
| 2 | *(hardware)* TRY ON DEVICE is primary and writes to RAM only; the pad plays in about a second; the site states that speed honestly and confirms a settled state rather than animating a progress bar; a power cycle brings the original back | HUMAN_NEEDED (automated half VERIFIED) | `.primary`: `inline-size: 100%`, `background: var(--color-accent)` - the one accent fill on the panel. `HONESTY_READY` "...in about a second. A power cycle brings your own back." shown once connected; "about a second" exists in exactly two literals (`HONESTY_NO_SESSION`, `HONESTY_READY`), both pre-click honesty-slot strings of which one is visible at a time, and nowhere else in `src` (Z-08). No bar, no spinner during a write (`PadSpinner` renders only while `connecting`); `settled` is set only after `writeBoth` resolved both ACKs and `restorePageChange` went out (install.svelte.ts 793-800; install.spec test 4's `steps` = write-timer ok, write-setup ok, restore-page-change sent). The only escape hatch is one `setTimeout` at 2000 ms (`#armSlow`), rendering `STILL_WRITING_LINE` once and speaking `LIVE_STILL_WRITING` once (e2e test 10: slow line at +2419 ms on a held store leg). RAM-only in code: `sendConfig` is CONFIG/EXECUTE, never PAGESTORE. **Hardware half - rows B and D - unanswered** |
| 3 | *(hardware)* PUT BACK restores the snapshotted configuration with one click at any time, including in a fresh tab after the browser was closed, because the snapshot persists in localStorage keyed by module identity | HUMAN_NEEDED (automated half VERIFIED) | `putBack()` has no confirmation and writes `this.snapshot` through the same `writeBoth` (install.svelte.ts 813-844); install.spec test 5: the snapshot's strings, Timer then Setup, `restored`, the module's RAM back to what it was. Key: `fetchModuleKey` -> `SERIALNUMBER/FETCH` addressed to the ZONA's SX/SY (descriptors.ts 247-259) -> `moduleKeyOf` 32 lowercase hex; record under `hangar.snapshot.v1` keyed module then page; `persistIfAbsent` returns `kept` on an existing valid entry and no function calls `removeItem` (snapshot.ts; snapshot.spec tests 3, 5). An existing record WINS over a fresh fetch (install.svelte.ts 614-619; install.spec test 6 - a record different from the module's RAM is what PUT BACK writes, and the entry is untouched). Session-only fallback when the serial is unanswered: `snapshotDurable=false`, `SNAPSHOT_SESSION_LINE` (install.spec test 2; DeviceDetails picks the form from the store). `rememberedModule` renders PUT BACK `needs-zona` in a fresh tab before any session. **Hardware halves - rows C, F and A - unanswered.** Recorded limitation: deferred item 29 - after a KEEP in an earlier page load, a fresh tab's PUT BACK restores memory only |
| 4 | *(hardware)* KEEP ON DEVICE is visibly secondary, separate, never equal-weight; requires a confirmation naming what is replaced and stating it survives a power cycle; on a rig it names the other modules, states their pages are stored too, and remains allowed | HUMAN_NEEDED (automated half VERIFIED) | Quiet tier: `.control { border: 0; background: transparent; padding-inline: 0; inline-size: fit-content }` (KeepOnDevice.svelte 151-169) against the primary's full-width accent fill; in the install column below a hairline, the tuning region and PUT BACK (ChosenPanel.svelte 173-181, `.install-row { flex-direction: column }`); device-ui.spec test 10 asserts the border, the padding, the column and PUT BACK first. Confirmation: `CONFIRM_REPLACES` = "This replaces the Setup and Timer scripts on your ZONA's touch element, and it survives a power cycle."; `role="group"`, `tabindex="-1"`, focus on the container, no dialog/aria-modal, affirmative bordered never filled (KeepConfirm.svelte); e2e test 8 asserts all of it and `PAGESTORE/EXECUTE` 0 until the commit. Rig: `confirmRig(["EN16","BU16"])` renders as the fourth sentence (e2e test 8's rig page, install.spec test 16); `storeToFlash` no longer throws on `storeAllowed` (sequence.ts 356-361 - the only `throw` left in the file is `runNoOpCycle`'s canWriteBack guard at 392); sequence.spec test 3 rewritten to `resolves` with three ACKs to one request and `storeAllowed` informational. **Hardware halves - rows E and G - unanswered** |
| 5 | *(happy path on hardware; failure paths against recorded frames)* "Installed" means an ACK per event write; one-event-landed is detected and reported with retry and PUT BACK offered; retries bounded; connection lost mid-write ends in a named failure state; on browsers without Web Serial the install controls are present but disabled with the reason inline | HUMAN_NEEDED for the happy path; failure paths VERIFIED | ACK per event: the queue resolves each `request` on its ACK and `writeBoth` awaits them sequentially (sequence.ts 285-296); `#classify` reads `partial` off the recorded steps, never a message (install.svelte.ts 1021-1040). `partial`: install.spec test 13 (Timer ok, Setup timeout after 3 attempts, `landed`/`failed` named, `putBackState` enabled, `keepReason` after-partial); e2e test 4. Bounded: `RETRY_ATTEMPTS = 3`, `retryBackoffMs` 120/240/360 (constants.ts 116-118; queue.ts 235-304); a NACK is one attempt and never escalates (install.spec test 14 part one); the pacing escalation fires once on a no-NACK timeout and moves no attempt count (test 14 part two). Lost: `AbortedError` -> `lost`, PUT BACK `needs-zona` present and disabled, `snapshot` survives, the session's "Nothing was written" suppressed under `writeLock` (install.spec test 15; e2e tests 5 and 10). DEGR-02: e2e test 11 on BOTH projects (`delete Navigator.prototype.serial`): TRY ON DEVICE and KEEP ON DEVICE `toBeDisabled()` with `HONESTY_INCAPABLE` and `KEEP_REASONS.incapable`, PUT BACK count 0 by decision (Z-12), the body names Chrome/Edge/Firefox 151 and never "Chromium"; `first-experience.e2e.ts` "with no Web Serial..." extended with the same three assertions. Nuance: the failure paths run against the synthetic scripted module (`synthetic.ts`, real encoder/decoder, source-modelled) with Phase 2's hardware capture for identification - no recorded failure frame exists, which the phase's own documents say. **Happy path on hardware - row B - unanswered** |

**Score:** 0/5 close outright; 5/5 human_needed with every automatable half green. No criterion failed.

### The particular checks the verifier was asked for

| # | Check | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | SAFE-01: no sentence on the connect surface or the panel promises HANGAR never writes | VERIFIED (with one deliberate qualified sentence) | `grep -rni "never writes\|next release\|cannot write at all" src`: "next release" 0, "cannot write at all" 0. "never writes" appears in one visitor-facing literal - `SAFE_PROMISE` "HANGAR never writes to your ZONA **on its own**. Nothing reaches the module without a click." - which IS the UI-SPEC's post-approval amendment (the sentence the criterion asks for, qualified) - and otherwise only in comments (`install.svelte.ts:10`, `session.svelte.ts:93`, `TryOnDevice.svelte:87,278` naming the retired sentence in prose) and in unrelated Phase 5 URL-hash comments. `TryOnDevice.svelte`'s code carries neither Phase 4 literal: device-ui.spec test 10 asserts 0 occurrences of both needles on comment-stripped code, and asserts the header still names it in prose so the strip stays load-bearing |
| 2 | SAFE-01: zero writes without a click, by class, in node and in a browser | VERIFIED | Node: install.spec test 4 - two `observeConfig` calls then `writesOf("CONFIG","EXECUTE") === 0`, then exactly 2 after the click plus 1 restore heartbeat; test 1 at connect (0 config writes, 0 stores). Browser: install.e2e test 1 - `zona.seen` CONFIG/EXECUTE 0, PAGESTORE/EXECUTE 0, HEARTBEAT/EXECUTE 0 over connect + snapshot, and `writesOf(page).length === 3` (the three reads); session.e2e `onlyReads()` (lines 262-273) asserts the same three zeros plus `SERIALNUMBER/FETCH === connects` and `CONFIG/FETCH === 2 * connects`, called from 8 sites. Every install.e2e test ends on the wire counted by class (37 `zona.seen` assertions) |
| 3 | SAFE-02: TRY ON DEVICE primary/accent; KEEP ON DEVICE quiet tier; never equal weight | VERIFIED | Criterion 4 evidence; the confirmation's affirmative `.secondary` is `border: 1px solid var(--color-line); background: transparent` (KeepConfirm.svelte 246-268) - never a second fill |
| 4 | SAFE-03: every write control disabled until the snapshot is present and non-empty (D-03 over Z-16, deferred 7 recorded) | VERIFIED (design nuance noted) | TRY ON DEVICE disabled in `snapshotting`; `#tryRefusal` refuses `not-writable` phases; PUT BACK absent without a snapshot; KEEP `never-tried`. One deliberate exception per UI-SPEC I9 cause 4: in `snapshot-failed` TRY ON DEVICE is enabled and its click re-reads the module first, writing only if that lands - install.spec test 3 asserts zero `CONFIG/EXECUTE` through that click on a still-wrong page. `canWriteBack` refuses the empty string BEFORE the record is consulted; deferred items 7 and 27 carry the Z-16 question to the user |
| 5 | SAFE-04: durable record keyed by the serial, never overwritten, session-only fallback sentence | VERIFIED (key wire-unproven -> row A) | snapshot.ts: `SNAPSHOT_KEY = "hangar.snapshot.v1"`, `modules[moduleId].pages[page]`, `persistIfAbsent` returns `kept` when a valid entry exists, zero imports, no `removeItem` call (snapshot.spec test 1 scans); install.spec test 6 - the record wins and its `takenAt` is untouched after PUT BACK; test 15 - the record survives an unplug and a reconnect. `SNAPSHOT_SESSION_LINE` (session-copy.ts 268, 114 chars) rendered when `snapshotDurable` is false; install.spec test 2 - three bounded `fetch-serial` attempts, `ready` all the same, `storage.map.size === 0` |
| 6 | SAFE-05/06: confirmation names the touch element, the power cycle and the rig's modules; store stays allowed on a rig; sequence.spec test 3 rewritten, not deleted | VERIFIED | `CONFIRM_REPLACES` contains "touch element" and "survives a power cycle" (e2e test 8 asserts both substrings); `confirmRig` singular/plural (install-copy.ts 315-320); `storeToFlash` body is one `q.request(storePage(), "store")` (sequence.ts 356-361); sequence.spec.ts still has 11 tests with test 3 titled "a second module is named, and the store is a broadcast that stays allowed" asserting `resolves.toBeUndefined()`, one PAGESTORE on the wire, `storeAllowed === false` informational |
| 7 | SAFE-07: installed only on both ACKs; partial detected; kept only after the re-fetch proof | VERIFIED | Criterion 5 evidence; `#storeLeg`: ACK, then `#nextHeartbeat()`, then up to `REFETCH_ROUNDS = 3` `fetchBoth(...,"refetch")` rounds byte-compared to `sent`, `mismatch` when exhausted (install.svelte.ts 890-948); install.spec tests 9-10, e2e test 3 (a lying read-back is `kept-mismatch`) |
| 8 | SAFE-08: "about a second"; the 2000 ms line once | VERIFIED | Two pre-click honesty literals only (see criterion 2); `STILL_WRITING_LINE` rendered at exactly one site (InstallState.svelte:169 under `{#if install.slow}`), `LIVE_STILL_WRITING` announced from exactly one `setTimeout` (`#armSlow`), cleared in every leg's `finally`; install.spec test 18; e2e test 10 |
| 9 | SAFE-09: lost mid-write is a named state with PUT BACK offered | VERIFIED | `lost` phase with `LOST_TITLE` "The ZONA was unplugged mid-write" in both RAM and store forms (`lostBlock(storeLeg, label)`); `putBackState()` returns `needs-zona` when a snapshot exists and the session is not connected, rendered disabled with `PUT_BACK_NEEDS_ZONA`; install.spec test 15, e2e tests 5 and 10 (`LOST_ON_PAGE` spoken, PUT BACK disabled with its line) |
| 10 | DEGR-02: controls present, disabled, explained on WebKit | VERIFIED | e2e test 11 ran on `webkit-phone` (iPhone 15 viewport, no `navigator.serial` ever) and on `chromium` with the slot deleted; `first-experience.e2e.ts` degrade test on `/`. Both assert real `disabled`, the capability sentence adjacent, PUT BACK absent |
| 11 | D-10/D-17: written bytes equal the meters (wire-pin.spec against the landed meter) | VERIFIED | wire-pin.spec.ts (4 tests): test 1 for every compiler-driven entry `config.setup.length === cost.setup.used && config.timer.length === cost.timer.used` and the meters' `used`; test 2 every Lua entry canonical; test 3 `writeBoth` puts `ACTIONSTRING === config.timer/setup` and `ACTIONLENGTH === length` on the wire, with the negative that `compressScript(config.setup).length < setupOnWire.length`; test 4 `undefined` builds no request. `model.ts` emits `onconfig` inside `land()` (430) and `undefined` on stale (485, 609); `TuningRegion` threads it, `Coverflow.svelte:910,935` hands `configStrings` to `TryOnDevice`'s `config` prop, whose `$effect` calls `install.observeConfig` |
| 12 | D-16: a second raw onData registration refused | VERIFIED | `session.svelte.ts` `get transport()` returns a view whose `onData()` throws "The session owns the byte stream; subscribe with session.onClass() instead" (849-853); session.spec test "the transport view writes, and refuses a raw onData"; install.spec test 8 asserts `.onData(` absent from the store's stripped source and `session.onClass(` present; the store's queue is fed from `#buildQueue`'s `onClass` subscription (install.svelte.ts 490-493) |
| 13 | D-18: `storeToFlash` has no throw; TryOnDevice has no never-writes literal | VERIFIED | Items 6 and 1 above |
| 14 | `install.svelte.ts` exactly three static specifiers; the session exactly four | VERIFIED | `grep -n '^import \|^} from'` install.svelte.ts -> `./install-copy` (173), `./snapshot` (181), `./session.svelte` (182); heavy modules only via `await import("$lib/protocol")` / `("$lib/transport")` at 274-275 inside `loadHeavy()`. session.svelte.ts -> `./session-copy`, `$lib/protocol/usb`, `$lib/transport/ports`, `$lib/transport/transport` (148-160); dynamic at 291-293. install.spec test 8 asserts the three by exact array |
| 15 | config-shape 13/14 walk the install path; the /c/[id] cold load unchanged in shape | VERIFIED | `PERMITTED_SPECIFIERS` has eight entries: the session's five plus `$lib/device/install.svelte`, `$lib/device/install-copy`, `$lib/device/snapshot`; test 13 "the front door never reaches the compiler at module scope" and test 14 "the built front door does not preload the protocol chunk" over `build/index.html`, `build/browse/index.html`, `build/c/aurora/index.html`, `build/c/euclid/index.html`; both passed in this verifier's quick runs with `build/` present (14 of 14 in config-shape.spec.ts). install-copy.spec test 1 and snapshot.spec test 1 assert zero import specifiers |
| 16 | No `setInterval` in the device path | VERIFIED | `grep -rn setInterval src/lib/device src/lib/transport src/lib/protocol src/lib/ui src/routes`: only comments naming the prohibition (`session.svelte.ts:957`, `BrowseToolbar.svelte:71`, `TuningRegion.svelte:115`, `tune-ui.spec.ts:13`, `dev/tune/+page.svelte:24`); install.spec test 8 and session.spec test 15 scan for the assembled needle; the store's two timers are `setTimeout` (`#armSlow`, `defaultSleep`) |
| 17 | No attribution text | VERIFIED | `grep -rli "claude\|anthropic" src e2e docs`: `firmware-oracle.ts` (a clone path under `Documents\Claude` and a `.claude\projects` exclusion path), `format-parity.spec.ts` (a `.claude/worktrees` path comment), `licence-notices.spec.ts` (the Phase 1 gate asserting `CLAUDE.md export-ignore`), `e2e/artifacts.e2e.ts` (same gate) - all pre-existing paths and gates, none attribution. `.planning/phases/07-install-flow`: the standing-rule sentence "No Claude or Anthropic attribution..." in all 13 PLANs and the GSD template heading `### Claude's Discretion` in 07-RESEARCH.md:89. Zero in any SUMMARY, the CONTEXT, the UI-SPEC, the VALIDATION, deferred-items, or any shipped source, e2e or doc |
| 18 | Engine names only in comments, if anywhere | VERIFIED (report) | `src/lib/device`: `session.svelte.ts:10,13,947` and `session.spec.ts:869` (Chromium/Blink, Phase 6 comments citing browser source). `src/lib/ui`: `BrowseToolbar.svelte:222`, `DeviceDetails.svelte:13`, `KnobRack.svelte:69`, `TuningRegion.svelte:69` (comments, pre-existing). `src/lib/transport`: `web-serial.ts:28` (comment), `transport.spec.ts` (the test that asserts copy never says Chromium), three hardware-capture JSON `userAgent` fields. **Phase 7 added no engine name anywhere; none in any Phase 7 copy** - install-copy.spec test 4 and session-copy.spec test 6 walk every export; e2e tests 11 and the degrade test assert `body` never contains "Chromium"; `docs/INSTALL-RUNBOOK.md` engine scan 0 lines (07-13) |
| 19 | Every SUMMARY commit exists | VERIFIED | `gsd-tools verify commits` over the 42 distinct 7-char hashes in the thirteen SUMMARYs: `all_valid: true, invalid: []` |
| 20 | Files the phase said it never edited | VERIFIED | `git log 145f85d..HEAD -- playwright.config.ts vite.config.ts e2e/catalog.e2e.ts src/lib/catalog/lua-entries.spec.ts docs/SESSION-RUNBOOK.md` is empty. 60 files changed across the phase (+15,951 / -431) |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/lib/device/install.svelte.ts` | The install store: 14 phases, the snapshot gate, three clicks, taxonomy, bounds | VERIFIED | 1,097 lines; `InstallPhase` union of 14; `WRITABLE_PHASES`; `#snapshot`, `tryOnDevice`, `putBack`, `keepOnDevice`, `#storeLeg`, `#ramLeg`, `#classify`, `#escalatePacing`, `#armSlow`; wired from `+layout.svelte` (`install.start()` in onMount) and consumed by five components and the probe |
| `src/lib/device/install-copy.ts` | Every install sentence, zero imports, the caps | VERIFIED | 393 lines, zero specifiers; install-copy.spec test 2 holds every literal over 40 chars against 07-UI-SPEC read from disk (`status: approved`) |
| `src/lib/device/snapshot.ts` | The durable record, keyed serial then page, never overwritten or deleted | VERIFIED | 284 lines, zero specifiers, five exported functions, `REFUSED` sentinel so a throwing `getItem` is `unavailable` rather than a blind write |
| `src/lib/protocol/descriptors.ts` | `fetchSerialNumber(sx, sy)` addressed, `moduleKeyOf` | VERIFIED | 247-273; descriptors.spec tests 11-12; forbidden-instructions.spec test 4 names five allowed classes including SERIALNUMBER |
| `src/lib/transport/sequence.ts` | `writeBoth` as the one writer, `fetchModuleKey`, `storeToFlash` without the throw | VERIFIED | 285-296, 320-329, 356-361; `writeBack` an adapter |
| `src/lib/ui/TryOnDevice.svelte` | Writes on a click through the store; five honesty twins; three labels; no never-writes literal | VERIFIED | `install.tryOnDevice(config, entry.name)` at 315; `session.connect()` first statement when not connected (312); five `class:twin` lines; `WRITING…`/`KEEPING…`/resting; `aria-busy` |
| `src/lib/ui/KeepOnDevice.svelte`, `KeepConfirm.svelte`, `PutBack.svelte`, `InstallState.svelte` | The three install leaves and region 3 | VERIFIED | Each reads the store and renders install-copy's strings only (device-ui.spec test 9 "every install sentence on screen comes from the copy modules"); 44px floors; reserved twin cells 72/48/72 px; `role="group"` not dialog |
| `src/lib/ui/ChosenPanel.svelte` | The install column, the confirmation in the control's place, the focus rule | VERIFIED | 173-181; `returnFocus()` to KEEP ON DEVICE else region 3 |
| `src/lib/ui/DeviceDetails.svelte` | The header lock on both controls, the snapshot line in two forms, the amended forget explanation | VERIFIED | `disabled={session.writeLock}` twice, `WRITE_LOCK_REASON` once, `snapshot-line` only with a snapshot; device-ui.spec test 11 |
| `src/routes/dev/install/+page.svelte` | The probe: plain-text readout, trace, steps, pacing | VERIFIED | 264 lines; testids `install-phase`, `install-trace`, `install-snapshot`, `install-module`, `install-pacing`, `install-steps`, `install-put-back`; linked from nowhere (config-shape test "every probe route under /dev/ is linked from nowhere") |
| `e2e/fake-zona.ts`, `e2e/fake-serial.ts` | The node suite's responder exposed into the page; the shim's write hook | VERIFIED | `fake-zona.ts` wraps `zonaResponder`/`rigResponder` from `synthetic.ts` with five layered faults; `fake-serial.ts` exposes `writes()` and `unplugAfterWrites(i, n)` on `window.__hangarSerial` |
| `e2e/install.e2e.ts` | 11 titles / 12 runs: six probe walks, four real-page titles, one `@webkit` degrade | VERIFIED | Titles at 348, 424, 484, 552, 649, 728, 1148, 1292, 1424, 1538, 1684; 12 runs observed |
| `docs/INSTALL-RUNBOOK.md` | Seven rows A-G with Do this / Passes when / Why a machine cannot / Pass; the three measurements in bold; recovery; hand-back | VERIFIED (present, unanswered) | 233 lines; every quoted string matches install-copy.ts and session-copy.ts literals read here; the Pass column is empty in every row |
| `docs/TESTING.md`, `docs/HARDWARE-AUDITION.md` | The install test surface; item 2 in the present tense | VERIFIED | TESTING.md 632-725 with the thirteen per-file counts (each matching the titles counted here); HARDWARE-AUDITION item 2 names TRY ON DEVICE / PUT BACK / KEEP ON DEVICE and the runbook, "cannot install" absent |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `+layout.svelte` | `install.start()` | onMount after `session.start()` | WIRED | Lines 24-28; idempotent; subscribes `onConnection` |
| `session.onConnection("connected")` | `#attach` -> `#buildQueue` -> `#snapshot` | generation-guarded | WIRED | install.svelte.ts 419-469; install.spec test 1; e2e test 1's trace |
| `session.onClass` | `queue.deliver` + `#onClassSeen` (heartbeat waiters, page check) | the one class pump | WIRED | 490-519; never a raw `onData` |
| `TuningRegion` -> `Coverflow` -> `TryOnDevice.config` -> `install.observeConfig` | the tuner's verbatim pair | `onconfig` in `land()` | WIRED | model.ts 430; Coverflow.svelte 910, 935; TryOnDevice.svelte 241-246; wire-pin.spec |
| `TryOnDevice` click | `install.tryOnDevice` / `session.connect` | onclick, nothing awaited before `connect()` | WIRED | 310-316 |
| `install.tryOnDevice` / `putBack` | `T.writeBoth(q, targetOf(id), strings)` | the one RequestQueue | WIRED | 978; `restorePageChange` in `finally` |
| `KeepOnDevice` click | `install.openConfirm` -> `KeepConfirm` in its place -> `install.keepOnDevice` -> `P.storePage()` | `confirmOpen` | WIRED | KeepOnDevice 104-106; ChosenPanel 175-179; KeepConfirm 131-133; install.svelte.ts 855-877, 908 |
| `install.snapshotDurable` / `session.writeLock` | `DeviceDetails` snapshot line and lock | direct reads | WIRED | DeviceDetails 173, 302-336 |
| `install.*` announcements | the one `SessionAnnouncer` live region | `session.announce` | WIRED | `#session.announce(...)` at 644, 800, 843, 869, 1074, 1089; device-ui.spec test 11 asserts three live regions site-wide and the announcer imports nothing from the store |
| `writeBoth` bytes | the meters' `used` | `ACTIONSTRING === config.*`, `ACTIONLENGTH === length` | WIRED | wire-pin.spec tests 1 and 3 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `InstallState.svelte` | `install.phase`, `install.name`, `install.landed/failed`, `install.leg`, `install.slow`, `session.identity` | The store's actions, fed from decoded ACK/REPORT/HEARTBEAT classes off the real wire (in tests, the scripted module answering through the real codec) | Yes | FLOWING |
| `TryOnDevice.svelte` honesty slot | `shown` from `session.phase`, `install.phase`, `budgetReason`, `connected` | Session capability + store phase + tuner | Yes | FLOWING |
| `PutBack.svelte` | `install.putBackState()`, `install.keptThisSession` | `snapshot` set only after both fetches pass `canWriteBack`; `keptThisSession` set on a proved keep | Yes | FLOWING |
| `KeepOnDevice.svelte` | `install.keepReason(capable)` | Seven-row table over `phase` and `armed` | Yes | FLOWING |
| `KeepConfirm.svelte` rig sentence | `session.identity.otherModules` | Phase 6's heartbeat fold, sx-then-sy | Yes | FLOWING |
| `DeviceDetails.svelte` snapshot line | `install.snapshot`, `install.snapshotDurable` | `persistIfAbsent` outcome | Yes | FLOWING |
| `/dev/install/` probe | `install.lastSteps`, `install.pacingEscalated` | The queue's `onStep`; `#escalatePacing` | Yes | FLOWING (the `refetchRounds` field is recorded on the store and rendered nowhere - deferred item 28) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| The whole unit suite | `npm run test:quick` (twice) | 775 passed, 1 failed (lua-entries cross-product timeout, item 12/21), 1 todo, 73 files | PARTIAL - matches in total; the one failure is a Phase 8 load-sensitive test untouched by Phase 7 |
| The load-sensitive file alone | `npx vitest run --project server src/lib/catalog/lua-entries.spec.ts` | 6 passed in 2.68 s | PASS |
| The sweep | `npm run test:sweep` | 3 files / 13 tests | PASS |
| The full e2e against the production build | `npx playwright test --workers 3` | 89 passed = 78 chromium + 11 webkit-phone, 12 install.e2e runs, no retries | PASS |
| Type check / lint | `npm run check`, `npm run lint` | 545 / 0 / 0; clean | PASS |
| The written bytes are the meters' bytes | wire-pin.spec (inside test:quick) | 4/4 | PASS |
| Commit provenance | `gsd-tools verify commits <42 hashes>` | all valid | PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| SAFE-01 | 07-04, 07-06, 07-08, 07-10, 07-12 | Nothing written without an explicit click; the connect screen says so | SATISFIED | Checks 1-2; `SAFE_PROMISE` on both header surfaces; zero writes by class in node and browser |
| SAFE-02 | 07-09, 07-10, 07-12 | TRY ON DEVICE RAM-only primary; KEEP ON DEVICE secondary, separate | NEEDS HUMAN (automated half satisfied) | Criteria 2 and 4; runbook row D |
| SAFE-03 | 07-06, 07-08, 07-12 | Snapshot at connect before any write; PUT BACK one click at any time | NEEDS HUMAN (automated half satisfied) | Criteria 1 and 3; runbook row C. Deferred 7/27 (D-03 vs Z-16) open |
| SAFE-04 | 07-01, 07-03, 07-04, 07-06, 07-11 | Snapshot persists in localStorage keyed by module identity | NEEDS HUMAN (automated half satisfied, key wire-unproven) | Check 5; runbook rows A and F. If row A reads session-only, closes as partially verified. Deferred 29 recorded |
| SAFE-05 | 07-09, 07-10, 07-12 | Confirmation names the touch element and the power cycle | NEEDS HUMAN (automated half satisfied) | Criterion 4; runbook row E |
| SAFE-06 | 07-02, 07-07, 07-09, 07-12 | Rig: other modules named, pages stored too, action allowed | NEEDS HUMAN (automated half satisfied) | Criterion 4; sequence.spec test 3; runbook row G (optional) |
| SAFE-07 | 07-06, 07-07, 07-08, 07-12 | Installed = ACK per event; half-written detected and recovered | SATISFIED | Criterion 5; check 7 |
| SAFE-08 | 07-10, 07-12 | "about a second"; settled state, no progress bar | SATISFIED (the wall time on hardware is row B) | Check 8 |
| SAFE-09 | 07-06, 07-07, 07-08, 07-12 | Bounded retries; lost mid-write a named state with PUT BACK | SATISFIED | Check 9 |
| DEGR-02 | 07-10, 07-11, 07-12 | Install controls present but disabled with the reason inline on unsupported browsers | SATISFIED | Check 10 |

No orphaned requirements: REQUIREMENTS.md maps exactly these ten to Phase 7 and every one appears in a plan's `requirements` and in 07-13's `requirements-completed`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `src/lib/catalog/lua-entries.spec.ts` | test 6 | Whole cross-product under Vitest's 5000 ms default; times out under memory pressure (deferred items 12, 21) | Warning | The quick gate did not read green on this machine at 1.18 GB free; passes alone; not a Phase 7 file. Owner: the next plan that edits the file |
| `src/lib/device/install.svelte.ts` | 598-606 | `canWriteBack` refuses an empty fetch BEFORE the durable record is consulted (D-03 over Z-16; deferred 7/27) | Info | A remembered module whose RAM reads empty is not offered its own record; named in the header; awaits the user's ruling |
| `src/lib/device/install.svelte.ts` | 822 | `putBack()` returns silently when `id.activePage !== this.snapshotPage` - no state, no copy | Info | The page-change re-snapshot keeps them equal in practice; a silent refusal is still a refusal with no sentence. Not covered by a test as a rendered outcome |
| `src/lib/device/install.svelte.ts` | 941-942 | A `NackError` on the PAGESTORE leg falls through to `"unconfirmed"` and `#fail(..., "timeout", ...)` records cause `timeout` for a refusal | Info | `cause` is rendered nowhere (deferred item 22 records that a refusal has no copy of its own); firmware never NACKs a PAGESTORE per the research, so unreachable on modelled hardware |
| `src/lib/device/install.svelte.ts` | 351 | `refetchRounds` recorded and rendered nowhere (deferred item 28) | Info | Runbook row E reads the count off the probe's `steps` list instead |
| Store semantics | - | `keptThisSession` is per page load; a fresh tab's PUT BACK after a keep is memory-only (deferred item 29) | Warning (product fact, recorded) | Flash keeps HANGAR's configuration until a power cycle unless the runbook's recovery step 4 is followed; a `kept` mark beside the record is a schema change awaiting the user |
| `src/lib/device/install-copy.ts:62`, `src/lib/protocol/descriptors.ts:101` | - | The word "placeholder" in comments | Info (false positive) | Descriptive uses (the contract's `{Setup\|Timer}` template; the desktop's filter placeholder); no stub |
| TODO / FIXME / `return null` / `console.log` in the store and the five leaves | - | none | - | Clean |

### Human Verification Required

The seven rows of `docs/INSTALL-RUNBOOK.md`, presented by 07-13 and unanswered; reproduced in the frontmatter with the exact expected strings. Rows A to F run in one tab (item 29); row G is optional. The results belong in a dated `## Results` heading in a results document beside the runbook. No agent has connected to, written to or deployed anything during this verification.

### Gaps Summary

No gap blocks the phase goal as far as a machine can see. Every automatable half of every criterion is implemented in substantive, wired code with real data flowing through it, and each is pinned by a node test against the scripted module and by a browser test against the production build - counted by protocol class on the wire, never by a resolved promise. The five *(hardware)* halves are exactly what the roadmap said they would be: a checklist for a person, and the runbook that carries it is on disk with its Pass column empty.

Three things to carry forward rather than close:

1. **The quick gate on this machine.** 775 + 1 timeout twice, the file alone in 2.68 s, the machine at 1.18 GB free. The phase's own item 21 predicted this run. It is a Phase 8 test's missing `{ timeout }`, not a Phase 7 regression, and the total reconciles at 776.
2. **The user's decisions the store is waiting on**: D-03 versus Z-16 (items 7/27 - whether an empty fetch may be a valid snapshot and whether the record should be consulted first), and item 29 (whether a fresh tab's PUT BACK should store after a keep it cannot remember). Both are a few lines once decided; neither is code until then.
3. **SAFE-04's honesty clause.** The key is read with an instruction no hardware has ever answered. The code degrades correctly to a session-only copy with its own sentence, and the runbook's row A is written to record which form appears. If it is the session-only form, SAFE-04 is partially verified and the roadmap's criterion 3 needs a decision, not a lie.

---

_Verified: 2026-09-05T18:10:00Z_
_Verifier: Claude (gsd-verifier)_
