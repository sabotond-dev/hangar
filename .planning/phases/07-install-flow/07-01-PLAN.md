---
phase: 07-install-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/protocol/descriptors.ts
  - src/lib/protocol/descriptors.spec.ts
  - src/lib/protocol/forbidden-instructions.spec.ts
  - src/lib/transport/capture.ts
autonomous: true
requirements: [SAFE-01, SAFE-04]

must_haves:
  truths:
    - "Phase 7 starts on a tree Phase 6 has closed, and every count this phase asserts is that closed tree's measured baseline plus a stated delta - never a number copied from a document."
    - "HANGAR can ask a ZONA for its own factory serial number, addressed to that ZONA and never broadcast, and turn the answer into one stable 32-character key."
    - "The set of instructions HANGAR can put on a wire grows from four to five and no further, and the gate that counts them says five."
  artifacts:
    - path: "src/lib/protocol/descriptors.ts"
      provides: "fetchSerialNumber(sx, sy) - the fifth and last outbound descriptor - and moduleKeyOf(cls)"
      contains: "SERIALNUMBER"
    - path: "src/lib/transport/capture.ts"
      provides: "STEP_IDS gains fetch-serial"
      contains: "fetch-serial"
    - path: "src/lib/protocol/forbidden-instructions.spec.ts"
      provides: "test 4 widened from four builders to five; the class set gains SERIALNUMBER and the instr set is unchanged"
      contains: "SERIALNUMBER"
  key_links:
    - from: "src/lib/protocol/descriptors.ts"
      to: "@intechstudio/grid-protocol"
      via: "encode_packet on a SERIALNUMBER/FETCH descriptor with DX/DY set to the module's own address"
      pattern: "class_name: \"SERIALNUMBER\""
    - from: "src/lib/protocol/descriptors.ts"
      to: "WORD0..WORD3"
      via: "moduleKeyOf reads the four decoded words and pads each to eight lowercase hex characters"
      pattern: "padStart\\(8"
---

<objective>
Measure the ground Phase 6 left, then add the one instruction this phase needs that has never existed.

Wave 1. Nothing visible ships here. Three things land:

1. **The five baselines**, re-measured on the clean tree Phase 6 closed. Phase 6 was executing while
   this phase was planned, so no number written in `07-VALIDATION.md` may be trusted: `BASE_FILES`,
   `BASE_TESTS`, `BASE_SWEEP`, `BASE_E2E` and `PREV_E2E` are observed here, reconciled against
   `06-14-SUMMARY.md`'s closing block, and carried forward by every later SUMMARY. `BASE_E2E` is frozen
   at this measurement so 07-13's gate asserts against the clean tree rather than against a number that
   has already absorbed this phase's own additions.
2. **The fifth descriptor.** `SERIALNUMBER/FETCH` -> `SERIALNUMBER/REPORT` returns `WORD0..WORD3`, the
   ESP32-S3 eFuse MAC - the same bytes as the USB `iSerialNumber` the browser keys its permission on
   and refuses to expose through `getInfo()`. It is the durable-snapshot key D-04 (amended) asks for.
   It is **addressed to the ZONA's SX/SY, never broadcast**: firmware accepts the FETCH with
   `IS_ME | IS_GLOBAL` and builds the REPORT at the global position, so a broadcast on a rig would make
   every module answer with indistinguishable frames.
3. **`fetch-serial` joins `STEP_IDS`**, so the sequence, the capture, the probe and every gate name the
   same step.

Purpose: SAFE-04's key exists as one pure function pair before anything is built on top of it, and
SAFE-01's forbidden-instructions gate says five rather than being quietly outgrown.
Output: one descriptor, one helper, one step id, two tests, five numbers.

**Phase 7 execution starts only after Phase 6 closes.** Task 1 asserts that and stops if it is not so.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-install-flow/07-CONTEXT.md
@.planning/phases/07-install-flow/07-RESEARCH.md
@.planning/phases/07-install-flow/07-VALIDATION.md
@.planning/phases/06-device-session/06-14-SUMMARY.md
@src/lib/protocol/descriptors.ts
@src/lib/protocol/descriptors.spec.ts
@src/lib/protocol/forbidden-instructions.spec.ts
@src/lib/protocol/constants.ts
@src/lib/transport/capture.ts
@scripts/check-counts.mjs

<standing_rules>
- **Never edit anything under `src/vendor/`.** Never touch a sibling repository. Never deploy.
- No Claude or Anthropic attribution, in any file, comment or commit. No emojis. No exclamation marks.
  **No new dependency** - this phase installs nothing.
- Every new file carries `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults; `npm run format` then `npm run lint`; `npm run check 2>&1 | grep -Ei "0 errors"`.
- **NO AGENT WRITES TO A DEVICE.** Nothing in this phase sends a byte to hardware from an executor's
  hands. Every gate runs against `FakeTransport` in node or the fake serial shim in Playwright. The
  first write to a real ZONA is the user's click at plan 07-13's checkpoint.
- **`descriptors.ts` stays the ONLY shipped module that calls `encode_packet`**
  (`forbidden-instructions.spec.ts` test 5). The fifth descriptor is added there and nowhere else.
- **The page-change class name never appears in `descriptors.ts`, not even in a comment**
  (`forbidden-instructions.spec.ts` test 2). The SERIALNUMBER comment must not name it.
- **No user-agent read anywhere. No `.svelte.spec.ts`. No `setInterval`, anywhere.**
- **Strip comments before every structural scan.** House helper: `stripComments` in
  `src/lib/config-shape.spec.ts`. Needles assembled from fragments.
- **No backslashes inside `node -e "…"`.** Scratch scripts go in the session scratch directory.
  **Large TypeScript files go through the editor tool, never a Bash heredoc.**
- Counts: whole-suite totals are the previous SUMMARY's numbers plus this plan's stated delta through
  `scripts/check-counts.mjs`. **Per-file counts are exact and asserted absolutely.**
- **This plan's SUMMARY records Phase 7's five-name block** - `BASE_FILES`, `BASE_TESTS`, `BASE_SWEEP`,
  `BASE_E2E`, `PREV_E2E` - and every later plan carries it forward whether or not it moved it.
  `BASE_E2E` **never moves after this plan**; only `PREV_E2E` does.
</standing_rules>

<interfaces>
**`fetchSerialNumber`**, the fifth and last outbound instruction HANGAR will ever have:

```ts
// src/lib/protocol/descriptors.ts
/**
 * Ask the module for its factory serial number (07-CONTEXT D-04 amended).
 *
 * ADDRESSED, NEVER BROADCAST. Firmware accepts this FETCH with
 * GRID_DESTINATION_IS_ME | GRID_DESTINATION_IS_GLOBAL (grid_decode.c:839-869)
 * and builds its REPORT at the global position, so a broadcast on a rig makes
 * EVERY module answer with frames that cannot be told apart. That is the whole
 * reason this takes sx and sy. The REPORT sets no LASTHEADER, so correlateById
 * is false, and the filter names no address, for the same reason storePage()'s
 * does not.
 *
 * WORD0..WORD3 are the ESP32-S3 eFuse MAC (grid_esp32_platform.c:139-181) -
 * the same six bytes the module's USB iSerialNumber is built from
 * (grid_esp32_usb.c:20-24), which the browser keys its permission on and
 * getInfo() refuses to expose. WORD2 and WORD3 are always zero on this chip;
 * the key still spans all four so a future chip cannot collide with this one.
 *
 * Source-verified, wire-unproven: no hardware capture holds a frame of this
 * class and the desktop editor never sends one. docs/INSTALL-RUNBOOK.md row A
 * is where it meets a module.
 */
export function fetchSerialNumber(sx: number, sy: number): GridRequest {
  return {
    label: "fetch-serial",
    descr: {
      brc_parameters: { DX: sx, DY: sy },
      class_name: "SERIALNUMBER",
      class_instr: "FETCH",
      class_parameters: {},
    },
    filter: { class_name: "SERIALNUMBER", class_instr: "REPORT" },
    timeoutMs: TIMEOUTS.fetchMs,
    correlateById: false,
  };
}

/** 32 lowercase hex characters: WORD0..WORD3, each unsigned, each padded to eight. */
export function moduleKeyOf(cls: DecodedClass): string {
  const word = (name: string) =>
    (Number(cls.class_parameters[name]) >>> 0).toString(16).padStart(8, "0");
  return word("WORD0") + word("WORD1") + word("WORD2") + word("WORD3");
}
```

`DecodedClass` is imported as a type from `./decode`, which `descriptors.ts` does not import today;
that is one type import and it costs nothing. `>>> 0` is load-bearing: a word above `2^31` decodes
as a negative number through `Number()` on some paths, and `toString(16)` of a negative carries a
sign, which would put a `-` in a key that is supposed to be hex.

**`STEP_IDS`** gains one member, placed after `fetch-timer`:

```ts
export const STEP_IDS = [
  "identify",
  "fetch-serial",   // NEW (Phase 7): the module's own key, before the snapshot
  "fetch-setup",
  ...
```

**The measured facts these tests pin** (07-RESEARCH, verified against the pinned package on
2026-09-05): `encode_packet({class_name:"SERIALNUMBER", class_instr:"REPORT", class_parameters:
{WORD0:0x12345678, WORD1:0x9abcdef0, WORD2:0, WORD3:0}})` decodes back to
`{"WORD0":305419896,"WORD1":2596069104,"WORD2":0,"WORD3":0}` with `brc {DX:-127, DY:-127, SX:-127,
SY:-127}`. The offsets of `SERIALNUMBER_WORD0..3` in the package are 5/13/21/29, length 8, identical
to `grid_protocol.h:952-965`. Re-run the round trip in the spec rather than trusting the document.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 07-01-01: Phase 6 has closed, and the five baselines are re-measured</name>
  <files>.planning/phases/07-install-flow/07-01-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/06-device-session/06-14-SUMMARY.md` - its five-name block, its per-file counts,
      and its statement that the hardware checkpoint was presented
    - `.planning/STATE.md` - the current position
    - `07-VALIDATION.md` § Test Infrastructure and § The five-name carry-forward block
  </read_first>

  <action>
**Precondition, and it stops the phase if it fails.** Confirm that Phase 6 has closed:
`.planning/phases/06-device-session/06-14-SUMMARY.md` exists on disk, carries the five-name block,
and records the hardware checkpoint as presented. If it does not exist, **stop** and report that
Phase 7 cannot start on a tree Phase 6 is still editing - the session seams this phase adds
(plan 07-04) are edits to files Phase 6 owns until then. Do not improvise around it.

Then, on a clean tree (`git status --short` empty), before any edit, measure and record all five:

```
npm run check      2>&1 | tail -5          # provenance only: files + "0 errors"
npm run test:quick 2>&1 | tail -6          # BASE_FILES / BASE_TESTS
npm run test:sweep 2>&1 | tail -6          # BASE_SWEEP - expected "3 13"
npm run build && npm run test:e2e 2>&1 | tail -6   # BASE_E2E, and PREV_E2E starts equal to it
```

Reconcile against `06-14-SUMMARY.md`: `BASE_FILES` and `BASE_TESTS` must equal that SUMMARY's
closing quick numbers, and the e2e total must equal the number 06-14's gate asserted
(`its BASE_E2E + 16`). If any disagrees, do not adjust anything - record the discrepancy, find which
commit moved it, and carry the **measured** number. The e2e total is written down **twice**, under
two names, from the one run. `BASE_E2E` is frozen here; 07-13's gate asserts `BASE_E2E + 12`.

Also record, from the same tree, the per-file counts Phase 7 grows, because every later plan
asserts them absolutely: `descriptors.spec.ts` (expected **10**), `forbidden-instructions.spec.ts`
(**5**), `sequence.spec.ts` (**9**), `synthetic.spec.ts` (**3**), `session.spec.ts` (**17**),
`session-copy.spec.ts` (**6**), `model.spec.ts` (**8**), `device-ui.spec.ts` (**7**),
`config-shape.spec.ts` (**14**), and `e2e/session.e2e.ts` (**14**). If any number differs from the
expectation in parentheses, the measured number is the phase's and every later plan's arithmetic
starts from it - say so in the SUMMARY by name.

**No spike is needed in this phase.** 06-01 proved that a `.svelte.ts` runes module is compiled and
collected by the `server` project and that a `$state` field arrives in node as a plain own property;
`install.svelte.ts` (plan 07-06) rests on that measurement and the same rule against `$derived`.
Quote 06-01's finding in one line so the reader knows why there is no spike here.

Kill the whole wrangler process tree afterwards (npx-cli -> wrangler.js -> cli.js -> workerd.exe); a
survivor holds 4173 and answers nothing.
  </action>

  <verify>
    <automated>npm run test:quick 2>&1 | tail -6</automated>
  </verify>

  <acceptance_criteria>
    - `06-14-SUMMARY.md` was found and its five-name block quoted; the phase did not start otherwise.
    - All five numbers are recorded as Phase 7's baseline, each with the command that produced it and
      the date. `BASE_E2E` and `PREV_E2E` are present and equal.
    - `BASE_SWEEP` is quoted as the literal the sweep printed.
    - The ten per-file counts are recorded, with any deviation from the expected value named.
    - No wrangler or workerd process is listening on 4173 at the end of the task.
  </acceptance_criteria>

  <done>Every later plan in this phase has a number to assert against, measured on the tree Phase 6 closed.</done>
</task>

<task type="auto">
  <name>Task 07-01-02: the fifth descriptor, the key, and the step id</name>
  <files>src/lib/protocol/descriptors.ts, src/lib/transport/capture.ts</files>

  <read_first>
    - `src/lib/protocol/descriptors.ts` in full - the header's "four instructions" sentence, which
      becomes five, and `storePage()`'s comment about a filter that names no address
    - `07-RESEARCH.md` § Pattern 1 - the firmware citations and the round-trip measurement
    - `src/lib/transport/capture.ts` - `STEP_IDS` and the comment above it
  </read_first>

  <action>
Add `fetchSerialNumber` and `moduleKeyOf` exactly as the interfaces block gives them, with the
comment's four paragraphs in prose: why it is addressed and not broadcast, what the words are, why
`>>> 0`, and that it is source-verified and wire-unproven with the runbook row named.

Amend the module header: "The four outbound instructions" becomes five, and the sentence naming
which plan added the fifth and why (Phase 7, the durable-snapshot key). **Do not name the page-change
class anywhere in the file**, comment included - `forbidden-instructions.spec.ts` test 2 reads the raw
source.

Amend `storePage()`'s comment: its last clause says D-12 disables the button on a rig. That rule is
superseded by SAFE-06 (07-CONTEXT D-05): the store stays allowed and the confirmation names the other
modules. Rewrite the clause to say so; leave the descriptor's body untouched.

Then `STEP_IDS` gains `fetch-serial` after `fetch-timer`, with a one-line comment naming Phase 7.
`StepId` widens by itself. `fixtures.spec.ts` test 4 asserts every recorded step id is in `STEP_IDS`,
which is a superset check, so no capture changes and no spec is edited here.

`npm run check` must stay clean. `DecodedClass` is a type import from `./decode`; confirm nothing
else changes in the import list.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -Ei "0 errors"</automated>
  </verify>

  <acceptance_criteria>
    - `npm run check 2>&1 | grep -Ei "0 errors"` matches; `npm run lint` exits 0.
    - `npx vitest run --project server src/lib/protocol/ src/lib/transport/fixtures/fixtures.spec.ts`
      is green with **no spec edited** - the existing ten descriptor tests, five forbidden tests and
      four fixture tests all pass against the widened module.
    - The raw source of `descriptors.ts` does not contain the page-change class name; quote the scan
      (fragment-assembled needle).
    - `STEP_IDS` has eleven members and `fetch-serial` is the second; quote the array.
  </acceptance_criteria>

  <done>HANGAR has a fifth thing it can ask a module, and it asks the module by name.</done>
</task>

<task type="auto">
  <name>Task 07-01-03: two tests on the descriptor, and the gate that counts to five</name>
  <files>src/lib/protocol/descriptors.spec.ts, src/lib/protocol/forbidden-instructions.spec.ts</files>

  <read_first>
    - `src/lib/protocol/descriptors.spec.ts` tests 5 and 6 - the round-trip helper and the measured
      frame lengths
    - `src/lib/protocol/forbidden-instructions.spec.ts` test 4 - the four builders and the two sets
  </read_first>

  <action>
Two tests in `descriptors.spec.ts`, taking it from **10 to 12**:

- *"the serial-number fetch is addressed to the module, and its report decodes to four words"* -
  `fetchSerialNumber(0, 0)` has `DX 0, DY 0` (never `-127`), class `SERIALNUMBER`, instr `FETCH`,
  empty class parameters, `correlateById === false`, `timeoutMs === TIMEOUTS.fetchMs`, and a filter
  that names no `brc_parameters` and no `class_parameters`. Then the round trip: encode a REPORT
  through the package's own encoder with `WORD0 0x12345678, WORD1 0x9abcdef0, WORD2 0, WORD3 0`,
  decode it, and assert the four numbers come back as `305419896, 2596069104, 0, 0`. Record the
  encoded frame length on the wire in the SUMMARY (the fetch's and the report's).
- *"moduleKeyOf is 32 lowercase hex characters, stable, and distinct for distinct words"* - the
  decoded report above keys to `123456789abcdef00000000000000000`; two different `WORD0` values give
  two different keys; a word above `2^31` (use `0xfffffff0`) keys with no `-` and exactly 32
  characters; `/^[0-9a-f]{32}$/` matches; calling it twice on the same class returns the same string.

Add `fetchSerialNumber(0, 0)` to test 5's round-trip list, so every descriptor still round-trips.

Then widen `forbidden-instructions.spec.ts` test 4 from four builders to five: the built list gains
`fetchSerialNumber(0, 0)`, the class-name set becomes `["CONFIG", "HEARTBEAT", "PAGESTORE",
"SERIALNUMBER"]` and the instr set stays `["EXECUTE", "FETCH"]`. Rename the test title from "four" to
"five". The amendment carries its note in the file's own header, naming Phase 7 and the reason, per
the house rule for touching a shipped gate. **The count stays 5** - this is a widening, not a new test.

Negative checks, observed red then restored:

1. Change the fetch's `DX` to `-127` and watch the addressing assertion go red naming `-127`.
2. Remove `padStart(8, "0")` and watch the key assertion go red on length.
3. Remove `fetchSerialNumber` from test 4's built list and watch the class-name set assertion go red
   with the set missing `SERIALNUMBER` - which proves the widening is asserted, not decorative.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/protocol/descriptors.spec.ts src/lib/protocol/forbidden-instructions.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/protocol/descriptors.spec.ts` reports **12 passed**.
    - `npx vitest run --project server src/lib/protocol/forbidden-instructions.spec.ts` reports
      **5 passed**, and test 4's title says five.
    - All three negative checks were observed red and restored; the SUMMARY names what each said.
    - The two encoded frame lengths are recorded.
    - `npm run test:quick 2>&1 | node scripts/check-counts.mjs $BASE_FILES $((BASE_TESTS + 2))` exits 0.
    - `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 3 13` exits 0 (or the literal task 01
      recorded).
    - `npm run lint` exits 0; `npm run check 2>&1 | grep -Ei "0 errors"` matches.
    - The SUMMARY carries the five-name block, with `BASE_E2E` and `PREV_E2E` equal and this plan
      named as the one that measured both.
  </acceptance_criteria>

  <done>The one new instruction of this phase is pinned by its address, its words and its key, and the gate over all five says five.</done>
</task>

</tasks>

<verification>
```
npm run check 2>&1 | grep -Ei "0 errors"
npm run lint
npx vitest run --project server src/lib/protocol/ src/lib/transport/fixtures/fixtures.spec.ts
npm run test:quick 2>&1 | node scripts/check-counts.mjs $BASE_FILES $((BASE_TESTS + 2))
npm run test:sweep 2>&1 | node scripts/check-counts.mjs 3 13
```
Negative checks named: the broadcast address (red on `-127`), the missing pad (red on length), the
builder removed from test 4 (red on the class set).
</verification>

<success_criteria>
- Phase 6 confirmed closed; five baselines measured on the clean tree and recorded, `BASE_SWEEP`
  quoted as printed, `BASE_E2E` equal to `PREV_E2E`, ten per-file counts recorded.
- `fetchSerialNumber` and `moduleKeyOf` exist; `STEP_IDS` has eleven members.
- `descriptors.spec.ts` 12, `forbidden-instructions.spec.ts` 5 with test 4 widened to five builders.
- Quick at baseline +0 files / +2 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-install-flow/07-01-SUMMARY.md`, recording the five
baselines with their commands, the reconciliation against 06-14, the ten per-file counts, the two
encoded frame lengths, and the three negative checks with what they said when red.
</output>
