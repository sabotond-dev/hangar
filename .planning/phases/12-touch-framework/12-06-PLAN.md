---
phase: 12-touch-framework
plan: 06
type: execute
wave: 6
depends_on: ["12-05"]
files_modified:
  - src/lib/catalog/presets.ts
  - src/lib/catalog/front-door.ts
  - src/lib/catalog/front-door.spec.ts
  - src/lib/catalog/listing.ts
  - src/lib/catalog/frames.json
  - src/lib/fidelity/golden-frames.json
  - static/og/joystick.png
  - docs/HARDWARE-AUDITION.md
autonomous: false
requirements: [CONT-01, TUNE-01, PREV-01, SHARE-04]

must_haves:
  truths:
    - "JOYSTICK's visual is decided by the user from costed options with each one's consequence for the front door stated, and not chosen by the planner or the executor."
    - "Whatever is chosen is implemented at the cost the option quoted, re-measured at the picker corner, with frames.json, golden-frames.json, the OG image and the front-door derivation moved together and listed."
    - "If the answer is the trail, the front door's no-two-quiet-pads-adjacent rule and the no-dark-pad-in-the-opening-window property are both re-derived on the record, never weakened silently."
    - "The TRACKPAD beside-or-replace decision is shown to the user at the same stop as a reversible default, so 12-10 builds on an answer rather than an assumption."
  artifacts:
    - path: "src/lib/catalog/presets.ts"
      provides: "JOYSTICK's declaration per the answer, with the option not taken costed in the comment"
      contains: "joystick"
    - path: "src/lib/catalog/front-door.ts"
      provides: "The derivation table's row for joystick, and the ring order if the geometry had to move"
      contains: "joystick"
  key_links:
    - from: "src/lib/catalog/front-door.ts"
      to: "src/lib/fidelity/golden-frames.json"
      via: "motion is derived from the golden frames, never declared - so a look layer moves the fixture first and the row second"
      pattern: "motion"
---

<objective>
JOYSTICK's visuals are a user decision, restated with numbers and asked - not picked.

> `JOYSTICK: didn't improve the visuals`

11-06 measured why the first bench note's two halves fight: `springLed` returns `comet` the moment
`touch.kind` is `comet`, so the trail the note asked for costs both the parked dot and the
lit-from-power-on centre cell. Zero lit bytes at rest - a black square - which flips `restsBlack`,
moves `motion` to `dark`, and takes JOYSTICK out of the front-door row for a note that asked to
*improve* the visual. The alternatives that keep the dot are look layers behind the stick - **shimmer
603, wave 621, swirl 641, ripple 652** of 908 - each animating, each fitting, and each moving JOYSTICK
to `animated`, which moves the front door's quiet-pad geometry (`front-door.ts:150-176`: the three
quiet pads land on 2, 4 and 6 so no two are adjacent, and JOYSTICK is one of them).

11-16 restated it as open item 1; the second bench round answered *"didn't improve the visuals"*,
which is a verdict on 11-06's centre-from-power-on and nothing else. Nobody has chosen, and nothing in
this repository is allowed to choose for the user. The stop is here rather than at the gate because
the implementation is cheap and the fixtures it moves (`frames.json`, `golden-frames.json`,
`static/og/`) are regenerated by later plans anyway.

**The same stop carries one reversible default.** 12-10 builds TRACKPAD's edge flash as a new
hand-authored entry **beside** the `tpad` preset rather than replacing it; the reason is in 12-10's
objective, and the user can reverse it here in one word.

Purpose: one decision the user owns, taken with its costs in front of them, then implemented at the
quoted cost.
Output: one checkpoint, one implementation, fixtures moved together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-touch-framework/12-VALIDATION.md
@.planning/phases/12-touch-framework/12-RESEARCH.md
@.planning/phases/11-bench-corrections/11-06-SUMMARY.md
@.planning/phases/11-bench-corrections/deferred-items.md
@src/lib/catalog/presets.ts
@src/lib/catalog/front-door.ts
@src/lib/catalog/front-door.spec.ts
@src/lib/fidelity/golden-frames.spec.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.**
- **No agent connects to or writes to a device, and no agent deploys.**
- **`src/vendor/` is not touched.** The look layers are the vendored compiler's own; choosing one is a
  `presets.ts` edit under `INTENDED_DIVERGENCE`'s rules (`presets.spec.ts`), never a vendored edit.
  `firmware-oracle.spec.ts` green and unedited.
- **Never touch a sibling repository.** **Do not edit `.planning/ROADMAP.md`.**
- **`gsd-tools state advance-plan` and `record-session` destroy lines.** Copy `STATE.md` first, repair
  by inverse edit, skip `roadmap update-plan-progress`.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- Commit with `git commit --only <paths> -F <message-file>`. Do not push.
- **State every count as a carried name plus a delta.** This plan carries `PREV_FILES 84` /
  `PREV_TESTS 876` from 12-05 and asserts `+0 / +0` **written out** - the test count does not move
  in any branch; the fixtures do.
- **No new knob, no change to any knob's value count.** A look layer is a state edit, not a knob.
- **`golden-frames.json` and `frames.json` are regenerated through `UPDATE_GOLDEN=1` /
  `UPDATE_FRAMES=1`, never hand-edited**; the moved hashes and rows are listed.
- **Every cost at the RGB444 picker corner.**
</standing_rules>

<interfaces>

## The options, as 11-06 and 11-16 costed them (picker corner, Setup)

| Option | Setup | What it keeps | What it moves |
|---|---|---|---|
| **trail** (`touch.kind = "comet"`) | 468 | the trail the note named | loses the parked dot AND the power-on centre; `restsBlack` flips; `motion` -> `dark`; **leaves the front-door row** (no dark pad in the opening window) |
| **shimmer** look layer | 603 | the dot and the centre | `motion` -> `animated`; quiet pads become ninepads and faders only; the "2, 4, 6" geometry must be re-derived |
| **wave** | 621 | same | same |
| **swirl** | 641 | same | same |
| **ripple** | 652 | same | same |
| **as is** | 532 | everything | nothing; the note stays a named non-delivery with the reason |

Every figure is a defaults-plus-substitution measurement from 11-06; **the executor re-measures the
chosen one at the picker corner before writing the header.**

## The front door under each branch

`front-door.ts:150-176` asserts three properties and `front-door.spec.ts:174-260` holds them: the
declared motion equals the golden-frames derivation (`:174`), `restsBlack` agrees with it (`:202`),
and no two quiet pads are adjacent on the ring (`:246`). Under a look layer the row's `motion` becomes
`animated`, the three-quiet-pads sentence becomes two, and the adjacency rule still passes (ninepads
at 2, faders at 6). Under the trail, JOYSTICK leaves the ring: seven entries, the radius-three window,
and the opening geometry all need re-deriving - **and that is a second decision the checkpoint must
surface**, not one the executor makes (11-14's ring question is the precedent: it went to the user).

</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 12-06-01: JOYSTICK's visual, and the TRACKPAD default, put to the user with numbers</name>

  <action>
Before presenting the options, **re-measure the four look-layer figures and the trail** at the picker
corner against the tree as 12-05 left it (a `presets.ts` state edit compiled through `compileState`,
never committed), so the numbers in front of the user are today's and not 11-06's; if any moved by
more than a character, say so in the option. Then present the decision below and stop. Nothing is
edited until the answer is recorded verbatim in the SUMMARY draft.
  </action>

  <decision>Which visual JOYSTICK gets, from the six costed options; and whether TRACKPAD's edge-flash entry is built beside the preset (the default) or replaces it.</decision>
  <context>
Your second bench note on JOYSTICK reads *"didn't improve the visuals"*. The first asked for the pad
to start from the middle (done in 11-06) and for *"more led animation, trail or something"*. The trail
and the centre dot cannot coexist: the compiler drops the parked dot and the power-on centre the moment
the touch kind is a comet, so a trail makes the card a black square at rest and takes it off the
front page. Four look layers keep the dot and add motion behind the stick; each costs between 603 and
652 of 908 and each makes JOYSTICK an animated card on the front page, which changes where the quiet
cards sit on the ring.

Separately: TRACKPAD's edge flash cannot be tuned into the preset (the compiler strips every look from a
trackpad state, and the preset is at 907 of 908). 12-10 builds it as a **new entry beside** the
trackpad preset unless you say otherwise. Beside keeps the preset's two-finger scroll, tap-to-click and
right-click, which your note did not ask to lose; replacing would remove a preset (nine become eight)
and those three gestures with it.
  </context>
  <options>
    <option id="trail">
      <name>Trail (comet), 468</name>
      <pros>The word your first note used. Cheapest.</pros>
      <cons>No parked dot and no lit centre at rest - a black card; JOYSTICK leaves the front page and the ring is re-derived at seven.</cons>
    </option>
    <option id="shimmer">
      <name>Shimmer behind the stick, 603</name>
      <pros>Keeps the dot and the centre; the whole field breathes.</pros>
      <cons>JOYSTICK becomes an animated front-page card; the quiet cards move.</cons>
    </option>
    <option id="wave">
      <name>Wave behind the stick, 621</name>
      <pros>Same as shimmer, with a direction that can follow the stick's axis.</pros>
      <cons>Same as shimmer.</cons>
    </option>
    <option id="swirl">
      <name>Swirl behind the stick, 641</name>
      <pros>Rotational; reads as a stick being pushed.</pros>
      <cons>Same as shimmer; closest to PINWHEEL and DIAL, which already swirl.</cons>
    </option>
    <option id="ripple">
      <name>Ripple behind the stick, 652</name>
      <pros>Rings from the centre; the most stick-like of the four.</pros>
      <cons>Same as shimmer; the most expensive, 256 free.</cons>
    </option>
    <option id="as-is">
      <name>Leave it, 532</name>
      <pros>Nothing moves.</pros>
      <cons>The note stays open and is recorded as a named non-delivery with this reason.</cons>
    </option>
  </options>
  <resume-signal>Select one of: trail, shimmer, wave, swirl, ripple, as-is. Then, for TRACKPAD: "beside" (default) or "replace".</resume-signal>
</task>

<task type="auto">
  <name>Task 12-06-02: The answer implemented at the quoted cost, with every fixture it moves listed</name>
  <files>src/lib/catalog/presets.ts, src/lib/catalog/front-door.ts, src/lib/catalog/front-door.spec.ts, src/lib/catalog/listing.ts, src/lib/catalog/frames.json, src/lib/fidelity/golden-frames.json, static/og/joystick.png, docs/HARDWARE-AUDITION.md</files>

  <read_first>
    - the recorded answer, verbatim, in this plan's SUMMARY draft
    - `src/lib/catalog/presets.ts:160-200` - JOYSTICK's declaration and 11-06's comment
    - `src/lib/catalog/presets.spec.ts` - `INTENDED_DIVERGENCE` and how a state edit is declared
    - `src/lib/catalog/front-door.ts:60-176` and `front-door.spec.ts:170-260`
    - `src/lib/fidelity/golden-frames.spec.ts` header - the `UPDATE_GOLDEN=1` path
  </read_first>

  <action>
**Record the answer verbatim first**, both halves, in the SUMMARY, before touching a file. Hand the
TRACKPAD half to 12-10 by name (`beside` or `replace`); nothing in this plan acts on it.

**Under `as-is`:** write the option table and the answer into JOYSTICK's comment in `presets.ts`
and into `deferred-items.md`'s successor list (12-12 owns the file; note the row for it). Term `+0`.
Nothing regenerates; say so.

**Under a look layer:** set `d.look.kind` and its parameters in JOYSTICK's declaration with the
declared divergence row (`presets.spec.ts`), re-measure at the picker corner and quote the option's
figure beside the measurement; `front-door.ts`'s joystick row `motion: "animated"` and the header's
"three quiet pads" paragraph rewritten to two with the adjacency still holding; regenerate
`golden-frames.json` (`UPDATE_GOLDEN=1`, list the moved hash and `nonZeroBytes`) **before**
`frames.json` (`UPDATE_FRAMES=1`), because motion is derived from the golden frames; `npm run build`
for `static/og/joystick.png`; the audition row's expectation sentence. `listing.ts`'s sentence if it
describes the rest state.

**Under `trail`:** `d.touch.kind = "comet"`, the divergence row, the measurement; `restsBlack` and
`motion: "dark"`; **JOYSTICK out of `FRONT_DOOR` and into `EXCLUDED_FROM_ROW` with the reason** -
and stop: the seven-entry ring and its opening geometry are 11-14's ring question again. Re-derive
the three properties at seven, write the derivation into `front-door.ts`'s header, and if the
no-dark-pad and three-largest-move properties cannot both hold at seven, **say so in the SUMMARY as a
finding and leave the ring at seven with the property that failed named** rather than moving a
second card to fix it.

**In every branch:** `presets.spec.ts` green with any divergence declared; `front-door.spec.ts` green
on the data, no assertion weakened; the negative check below.

**Negative check:** under a look layer, leave the row's `motion` at `static`; expect
`front-door.spec.ts:174` red naming joystick. Under `as-is`, the negative check is that
`git diff --stat` is empty of fixtures.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/catalog/ src/lib/fidelity/ && npm run build && npm run test:quick 2>&1 | node scripts/check-counts.mjs 84 876</automated>
    Expect every catalog and fidelity spec green on the regenerated fixtures, `PREV_TESTS + 0`, and
    the OG image rebuilt with its bytes recorded.
  </verify>

  <done>
The answer recorded verbatim and implemented at the quoted cost re-measured at the picker corner,
with the fixtures moved in the right order and every moved row and hash listed; the front door's
three properties re-derived on the record in the branch that moves them; the TRACKPAD half handed to
12-10 by name; term `+0` written out.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs 84 876` - `PREV_FILES + 0` /
  `PREV_TESTS + 0`. **This plan's term is `+0`, written out.**
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - unchanged; under a look layer the
  compiler-route sweep re-walks JOYSTICK's states and stays inside 908 - state the worst.
- `npm run build`; `static/og/` at `26 + 0` files, `joystick.png`'s bytes before and after.
- `git diff --quiet -- src/lib/fidelity/firmware-oracle.spec.ts src/lib/fidelity/preset-baseline.json`.
- `git diff --stat HEAD -- src/vendor/` unchanged; `git diff --quiet -- .planning/ROADMAP.md`.
</verification>

<success_criteria>
1. The user chose JOYSTICK's visual from six costed options, with the front-door consequence of each
   in front of them; the answer is recorded verbatim.
2. The TRACKPAD beside-or-replace default was shown and answered at the same stop.
3. The chosen option is implemented at the quoted cost, re-measured, with `golden-frames.json`
   regenerated before `frames.json` and every moved row listed.
4. The front door's three properties are re-derived on the record in any branch that moves them; no
   assertion is weakened.
5. Test count unchanged and stated as `+0`; nothing under `src/vendor/` moved; no device touched.
</success_criteria>

<output>
After completion, create `.planning/phases/12-touch-framework/12-06-SUMMARY.md` carrying: the
carried counts plus `+0 / +0`; **both answers verbatim**; the measured cost beside the quoted one;
the front-door derivation under the chosen branch; the moved golden hash, `frames.json` rows and OG
bytes (or the sentence that nothing moved); the negative check with both exit codes; and the
sentence handing the TRACKPAD answer to 12-10.
</output>
