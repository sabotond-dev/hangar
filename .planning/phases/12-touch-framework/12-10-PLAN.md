---
phase: 12-touch-framework
plan: 10
type: execute
wave: 10
depends_on: ["12-09"]
files_modified:
  - src/lib/catalog/entries/glide.ts
  - src/lib/catalog/index.ts
  - src/lib/catalog/listing.ts
  - src/lib/catalog/front-door.ts
  - src/lib/sim/demo.ts
  - src/lib/sim/lua-smoke.spec.ts
  - src/lib/catalog/frames.json
  - src/lib/catalog/audition.spec.ts
  - src/lib/browse/filter.spec.ts
  - src/lib/browse/sort.spec.ts
  - docs/HARDWARE-AUDITION.md
autonomous: true
requirements: [CONT-02, CONT-03, CAT-01, CAT-04, SHARE-04, PREV-01]

must_haves:
  truths:
    - "A trackpad whose edges flash, rounded, in the direction the finger moves, exists as a hand-authored entry beside the preset - because the compiler strips every look from a trackpad state and the preset sits at 907 of 908, so no tune could ever have produced it."
    - "The new entry is a single-finger pointer that unlocks both axes to ten bits, sends relative motion the way the preset does, and drops nothing the user asked for: two-finger scroll, tap-to-click and right-click stay on the preset, which is why the preset stays."
    - "Every edge flash lands on phase 0, through D where D fits and through the inlined idiom where it does not, and the catalog's decay gate proves it."
    - "The catalog chain gains a +1 term here, stated as BASE_CATALOG - 3 + 1 with the split 9 + 18, and the audition checklist a row - never a restated literal."
  artifacts:
    - path: "src/lib/catalog/entries/glide.ts"
      provides: "A new hand-authored entry with both costs at the picker corner in its header and the beside-not-replace decision recorded"
      min_lines: 120
    - path: "src/lib/sim/demo.ts"
      provides: "A demo path for glide so its OG image and frames show the flash, not a black square"
      contains: "glide"
  key_links:
    - from: "src/lib/catalog/entries/glide.ts"
      to: "src/lib/catalog/library.ts"
      via: "the edge cells decay through D(n,1,w), the library's phase-0 helper"
      pattern: "D\\("
    - from: "src/lib/catalog/entries/glide.ts"
      to: "src/lib/sim/lua-host.ts"
      via: "self:txma(1023) and self:tyma(1023) - both axes, avoiding D-11-13-a's one-axis trap"
      pattern: "tyma"
---

<objective>
TRACKPAD's animation, from a blank page, beside the preset.

> `Trackpad: still no animation. edgek villanjanak fel lekerekítve amelyik irányba a mozgás történik`
> - the edges should flash, rounded, in the direction the movement goes.

**Why it cannot be a tune.** 11-05 and 11-06 measured it: `normalisePadState` strips every look from a
trackpad state, and the compiled preset is 901 at the defaults and **907 at worst, one free**. There
was never a knob that could have produced this, and 11-16 named it non-delivery 3.

**Why beside, not replacing - decided here, reversible at 12-06's stop.** The preset is the only
trackpad in the catalog that scrolls with two fingers, clicks on a tap and right-clicks on two; the
user's note asked for animation and did not ask to lose those. A replacement would also drop the
vendored shelf from nine to eight and move `catalog.spec.ts:172-198`, `presets.spec.ts`, CONT-01's
wording and `tpad`'s OG image for a card whose exclusion from the front-door row would not change
(`front-door.ts:60-66`: tpad is already excluded). Beside costs a catalog `+1`, a new id, a listing
row, an audition row and an OG image - the same shape as WHEELS in 11-15. **If 12-06 recorded
`replace`, this plan's task 02 is different and the SUMMARY says so; read the answer first.**

**The entry.** A single-finger pointer: both axes unlocked to ten bits (`txma(1023)`, `tyma(1023)` -
both, because D-11-13-a's one-axis trap is real and STRIP pays fifteen characters to avoid it),
relative motion through `gmms` the way the preset sends it, and on every move five edge cells on the
side the pointer moves toward - centred on the finger's other coordinate, brightness 252 / 192 / 132
falling off with distance ("rounded") - each landing on phase 0 through the library's `D`. The
research's sketch is 553 with `D` and 603 with the pair inlined; the executor measures.

**The name.** The card is a glide across a surface with light at its edges; `glide` is the id and
GLIDE the name, in the house's one-word register (ARC, CULL, LUMEN, STRIP). If the executor finds a
truer word, the id must still be lower-case ASCII and not already a catalog id or a retired one.

Purpose: the one bench line Phase 11 could not fit, fitted from scratch on the library.
Output: one entry, one demo path, one test, `BASE_CATALOG - 3 + 1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-touch-framework/12-VALIDATION.md
@.planning/phases/12-touch-framework/12-RESEARCH.md
@.planning/phases/12-touch-framework/12-06-SUMMARY.md
@.planning/phases/12-touch-framework/12-07-SUMMARY.md
@.planning/phases/11-bench-corrections/11-15-PLAN.md
@.planning/phases/11-bench-corrections/11-15-SUMMARY.md
@.planning/phases/11-bench-corrections/deferred-items.md
@src/lib/catalog/library.ts
@src/lib/catalog/entries/wheels.ts
@src/lib/catalog/entries/strip.ts
@src/lib/catalog/listing.ts
@src/lib/sim/demo.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.**
- **No agent connects to or writes to a device, and no agent deploys.**
- **`src/vendor/` is not touched.** `firmware-oracle.spec.ts` green and unedited.
- **Never touch a sibling repository.** **Do not edit `.planning/ROADMAP.md`.**
- **`gsd-tools state advance-plan` and `record-session` destroy lines.** Copy `STATE.md` first, repair
  by inverse edit, skip `roadmap update-plan-progress`.
- Every HANGAR-authored file opens with
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- Commit with `git commit --only <paths> -F <message-file>`. Do not push.
- **State every count as a carried name plus a delta.** This plan carries `PREV_FILES 85` /
  `PREV_TESTS 887` from 12-09 and asserts `+0 / +1`; catalog `BASE_CATALOG - 3 + 1`, split
  **9 + 18**; `static/og/` `26 + 1`; audition `22 + 1`; e2e `87 / 106` and asserts `+0 / +0` with the
  suite run (a new route and a new OG image).
- **Every cost at the RGB444 picker corner**, at defaults and free at worst; the knobs' whole
  cross-product inside 908 is the sweep's job and the header quotes the worst.
- **Three to six knobs (TUNE-01)**, every colour knob through the picker; no knob may reach an
  over-budget state (D-10: a Lua entry has no runtime ladder).
- **Both Phase 11 gates apply**; `D`'s ceiling is 42 ticks, so every decay `w` here is at most 252.
- `frames.json` regenerated, never hand-edited; this is the one plan in this wave that regenerates it.
</standing_rules>

<interfaces>

## The sketch (12-RESEARCH §5 TRACKPAD), to be measured, not copied

```lua
--[[@cb]]self:txma(1023)self:tyma(1023)for a=0,80 do glc(a,1,@EDGEC,1)glp(a,1,0)end self.touch_cb=function(s,i,e,x,y)if i>0 then return end local c=s.c if e==3 or e>=5 and e<9 then s.c=nil return end if e==4 or e>8 or not c then s.c={x,y}return end local u,v=x-c[1],y-c[2]c[1]=x c[2]=y gmms(1,glim(u//@GAIN,-63,63))gmms(2,glim(v//@GAIN,-63,63))local h=math.abs(u)>math.abs(v)local q=x*9//1024 local r=y*9//1024 for k=-2,2 do local n if h then n=(u>0 and 8 or 0)+glim(r+k,0,8)*9 else n=glim(q+k,0,8)+(v>0 and 8 or 0)*9 end D(n,1,252-math.abs(k)*60)end end
```

553 with `D` and `@EDGEC` at `255,255,255`, `@GAIN` two digits. `252-|k|*60` gives 252 / 192 / 132 -
all multiples of 6, so `D` lands each on phase 0. **Both `txma` and `tyma`**; the cell arithmetic
divides by 1024 accordingly. What it deliberately drops against the preset: two-finger scroll,
tap-to-click, the 24-deep drain loop - and the preset keeps them.

**Knobs (three to six):** `@EDGEC` (colour, picker), `@GAIN` (an `amount` of two-digit values, so the
worst corner is the widest literal), and at least one more that is true of the card - the flash's
reach (five cells is the sketch; three / five / seven as a `size`, costing a `for k=-@R,@R` and a
divisor in the brightness that keeps every value a multiple of 6) or the flash length. **State the
shape character at birth**; a new entry has no stamps to demote, so this is the one moment the knob
set is free.

**Everything a new entry is added to** (11-15's list): `index.ts` (import, `CATALOG`, re-export);
`listing.ts` (a full `ListingEntry`: description, `motion`, `quiet`, tags, `featured`, `restsBlack`,
`preview: "lua"`); `front-door.ts`'s `EXCLUDED_FROM_ROW` with a `why` (hand-authored; a `lua` row
would dynamic-import the VM on the front page - the reason 11-16 wrote into `front-door.spec.ts`);
`frames.json`; `static/og/`; `docs/HARDWARE-AUDITION.md` and `audition.spec.ts`'s `ROW_COUNT` 22 ->
23; `filter.spec.ts` and `sort.spec.ts`'s `RECORDED` (entries 26 -> 27, and the tag census);
`wild-stamps.json` **only if** `stamp.spec.ts` requires a record per entry - read it and say.

**Tags, per CONT-03.** FOR `pointing` (tpad, quadrant -> 3) is what it is. FEELS: it moves when
touched and rests dark-lit-nothing - **read `frames.json`'s `animating` and `restsBlack` for the card
before choosing** between `expressive` / `readable` / `precise`; `still` is false of it (it flashes)
and `generative` is false (nothing moves untouched). State the histogram; no term may fall to one.

**`restsBlack` and the demo path.** All 81 cells are `glp(a,1,0)` at rest, so the card rests black
and is dark until a finger moves - like GHOST and MORPH, which have paths in `demo.ts:248`
(`DEMO_PATHS`), **not** like tpad, which is in `DARK_BY_CONSTRUCTION` because nothing can light it.
Give GLIDE a `DemoPath` (a diagonal drag, then a horizontal one, so the OG frame catches an edge lit)
and `restsBlack: false` or `true` **as the fixture reports it**, with `front-door.spec.ts:202`'s
agreement rule read first.

## Under `replace` (only if 12-06 recorded it)

The `tpad` preset leaves `presets.ts`, `PRESETS.length` 9 -> 8 in `catalog.spec.ts:172-198` and
`presets.spec.ts`, `EXCLUDED_FROM_ROW` loses tpad and gains glide, `DARK_BY_CONSTRUCTION` empties,
`static/og/tpad.png` goes, the `/c/tpad/` address dies (a fourth dead link this phase, recorded), and
CONT-01's "nine" is amended at 12-12. Catalog `-3 +0`. The SUMMARY names every one.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-10-01: The entry, costed before it is designed, on both axes and on D</name>
  <files>src/lib/catalog/entries/glide.ts, src/lib/catalog/index.ts</files>

  <read_first>
    - `12-06-SUMMARY.md` - the recorded TRACKPAD answer, first
    - `src/lib/catalog/entries/wheels.ts` and `strip.ts` - the house shape of an entry header, and
      STRIP's `txma`/`tyma` paragraph on D-11-13-a
    - `src/lib/catalog/library.ts` - `D`'s contract and ceiling
    - `src/lib/catalog/types.ts` - `CatalogEntry`, knob kinds
    - `src/lib/tune/knobs.lua.ts` - how a Lua knob's token, kind and values are declared and swept
    - `deferred-items.md` A.3 - TRACKPAD's non-delivery as 11-16 stated it
  </read_first>

  <action>
**1. Measure the sketch first** at `@EDGEC 255,255,255` (the widest colour literal) and the widest
`@GAIN`; quote 553 beside the measurement. Then design the knobs (three to six) and measure the worst
corner of the whole cross-product; if a knob's widest value takes the entry past 908, narrow the
value list and say so - a Lua entry has no ladder.

**2. Write `glide.ts`** in the house shape: the header carries the bench line in Hungarian and
English, the cannot-be-a-tune measurement (901 / 907, one free, looks stripped), the beside-not-replace
decision with its reasons, what the preset keeps that this drops, both axes and D-11-13-a, `D`'s use
and the 252 / 192 / 132 ladder as multiples of 6, both costs at the picker corner, the shape character
at birth, and the sentence that nothing here is hardware-verified. `id: "glide"`, `name: "GLIDE"`,
`preview: "lua"`, a one-line description a visitor reads, and the knob table with every colour knob
on the picker.

**3. `index.ts`**: import, `CATALOG`, re-export - in the file's own order.

**Negative check:** set one brightness to `250` (not a multiple of 6); expect `decay-idiom.spec.ts`
red naming glide and the frozen phase - `D`'s arithmetic is in the library, so check the gate sees
through the call (if it does not, that is a finding for 12-12: the gate reads literal `glpfs` pairs
and a `D(` call hides one; record it and add the clause or the row).
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/catalog/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts</automated>
    Expect `catalog.spec.ts` green at `BASE_CATALOG - 3 + 1` (or `-3 +0` under `replace`),
    `host-surface.spec.ts` resolving `D(`, `txma` and `tyma`, the sweep walking glide's whole
    cross-product inside 908 with the worst stated.
  </verify>

  <done>
`glide.ts` exists with both costs at the picker corner, the worst corner of its knob cross-product
inside 908, both axes unlocked, every flash landing on phase 0 through `D`, and the header carrying
the decision, the measurement that forced it, and the shape character at birth. The decay gate's
sight through a `D(` call established either way.
  </done>
</task>

<task type="auto">
  <name>Task 12-10-02: Into the catalog, with a demo path, a test, a row and a picture</name>
  <files>src/lib/catalog/listing.ts, src/lib/catalog/front-door.ts, src/lib/sim/demo.ts, src/lib/sim/lua-smoke.spec.ts, src/lib/catalog/frames.json, src/lib/catalog/audition.spec.ts, src/lib/browse/filter.spec.ts, src/lib/browse/sort.spec.ts, docs/HARDWARE-AUDITION.md</files>

  <read_first>
    - `src/lib/sim/demo.ts:170-300` - `DemoPath`, `DEMO_PATHS`, `DARK_BY_CONSTRUCTION`, `demoPathFor`
    - `src/lib/catalog/front-door.spec.ts:195-215` - the `restsBlack`-agrees-with-motion rule
    - `src/lib/catalog/listing.ts:219-237` - tpad's row, whose `quiet` explains why it is dark
    - `src/lib/catalog/audition.spec.ts:52`, `:207-211`
    - `src/lib/browse/filter.spec.ts:176-200`, `sort.spec.ts:81`
    - `src/lib/share/stamp.spec.ts:390-470` - whether a record per entry is required
  </read_first>

  <action>
**1. `listing.ts`**: the full row; tags per the interfaces block with the histogram stated; `featured`
false (a new card earns it on the bench); `restsBlack` as the fixture reports after step 3.

**2. `front-door.ts`**: `EXCLUDED_FROM_ROW` with the hand-authored `why`.

**3. `demo.ts`**: a `DemoPath` for glide (a diagonal drag then a horizontal one, ending with the finger
still down so the OG tick catches lit edges - read how GHOST's and MORPH's paths choose their ticks).
`DARK_BY_CONSTRUCTION` untouched (tpad only).

**4. `lua-smoke.spec.ts` +1**: a horizontal drag rightward lights the five right-edge cells centred on
the finger's row at 252 / 192 / 132 / 192 / 252-pattern brightness (read the phases) and nothing on
the left edge; a vertical drag lights the top or bottom edge; every lit cell reaches phase 0 within
42 ticks; `gmms` carries the deltas with the sign of the motion; a second finger does nothing.

**5. Regenerate `frames.json`**; read `animating` and `restsBlack` for glide and write them into
`listing.ts`. **`npm run build`** for `static/og/glide.png`; record its bytes and that it is not the
black square tpad's is.

**6. `audition.spec.ts`** `ROW_COUNT` 22 -> 23 and `docs/HARDWARE-AUDITION.md`'s row: drag right, the
right edge flashes and fades to black; drag up, the top edge; the pointer moves on the computer.
`filter.spec.ts` and `sort.spec.ts` `RECORDED` recounted. `wild-stamps.json` per `stamp.spec.ts`'s
contract, stated.

**7. The suite**: `npm run test:quick` again after the build, `npx playwright test --workers 3` at
`106 + 0` / grep `87 + 0` (a new route and OG image; `artifacts.e2e.ts` and `og/build.spec.ts` see
27). Remove `test-results/`.

**Negative check:** leave glide out of `EXCLUDED_FROM_ROW`; expect `front-door.spec.ts` red on
row-plus-exclusions-equals-CATALOG naming glide.
  </action>

  <verify>
    <automated>npm run build && npm run test:quick 2>&1 | node scripts/check-counts.mjs 85 888 && npx playwright test --workers 3</automated>
    Expect `lua-smoke.spec.ts` **+1**; `frames.spec.ts` green at 27; `audition.spec.ts` at 23
    contiguous rows; `facets.spec.ts` with no singleton; e2e at `106`.
  </verify>

  <done>
GLIDE is in the catalog, the listing, the exclusion list, the fixture, the OG set and the audition
checklist, with a demo path that lights its edges for the picture; one test proving the directional
flash, its fade to zero, its deltas and its single-finger rule; `RECORDED` recounted; the catalog
stated as `BASE_CATALOG - 3 + 1`, 9 + 18; the suite run at `106 + 0`.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs 85 888` - `PREV_FILES + 0` /
  `PREV_TESTS + 1`. **This plan's term is `+1`.**
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - glide's cross-product inside 908,
  worst stated; the lua-entries combination total rises and is recorded.
- `npm run build`, `npm run test:quick` again; `static/og/` at **`26 + 1`** with `glide.png`'s bytes;
  `npx playwright test --workers 3` at `106 + 0` proved by `grep -c "test("`; `test-results/` removed.
- `decay-idiom.spec.ts` green with glide's flashes seen (or the gate's blindness to `D(` recorded as a
  finding); `touch-guard.spec.ts` green, no new row.
- `git diff --stat HEAD -- src/vendor/` unchanged; `git diff --quiet -- .planning/ROADMAP.md`.
</verification>

<success_criteria>
1. A directional, rounded edge flash exists as a hand-authored entry beside the preset, with the
   decision and the measurement that forced it in its header.
2. Both axes unlocked; every flash lands on phase 0; the worst knob corner inside 908.
3. The catalog is `BASE_CATALOG - 3 + 1` with the split 9 + 18, the OG set `26 + 1`, the audition
   checklist `22 + 1`, every one stated as a chain.
4. The OG image shows lit edges, not a black square, because the card has a demo path.
5. Whether the decay gate sees through `D(` is established and recorded.
6. Nothing under `src/vendor/` moved; no device touched.
</success_criteria>

<output>
After completion, create `.planning/phases/12-touch-framework/12-10-SUMMARY.md` carrying: the carried
counts plus `+0 / +1`; the catalog term `+1` and the split; both costs at the picker corner and the
worst corner; the knob table and the shape character at birth; the tag histogram; `animating` and
`restsBlack` as the fixture reports them; the OG bytes; the decay gate's sight through `D(`; the
`wild-stamps.json` decision; the e2e zero; the two negative checks with both exit codes; and the
sentence that no device was touched.
</output>
