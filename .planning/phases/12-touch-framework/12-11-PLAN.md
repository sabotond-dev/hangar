---
phase: 12-touch-framework
plan: 11
type: execute
wave: 11
depends_on: ["12-10"]
files_modified:
  - src/lib/catalog/entries/lumen.ts
  - src/lib/catalog/listing.ts
  - src/lib/sim/lua-smoke.spec.ts
  - src/lib/catalog/frames.json
  - static/og/lumen.png
  - docs/HARDWARE-AUDITION.md
autonomous: true
requirements: [CONT-02, TUNE-01, TUNE-05, PREV-01, PREV-02, SHARE-01]

must_haves:
  truths:
    - "LUMEN's depth knob reaches exact black on the bottom row at its deepest setting, so the deepest ramp is unmistakable on a desk, and every one of the four values still decodes at its own index - no link is demoted."
    - "The card says, where a visitor reads it, that the top row is the anchor and never moves, so the next person who turns the knob compares the bottom row - the sentence 11-09.2 owed and Probe B confirmed was the missing piece."
    - "12-01's verdict is consumed by name: if the wiring was found broken there, this plan says it was fixed there; if sound, this plan is the honest deliverable and says what it is not - hardware-verified."
    - "LUMEN's cursor cell has the same one-unit boundary the sequencers had, and takes the same answer from the library, measured."
  artifacts:
    - path: "src/lib/catalog/entries/lumen.ts"
      provides: "d = 32 - row*@DEPTH with anchor*d//32; the cursor through Q; the header's two costed routes resolved on the record"
      contains: "32-n//9*@DEPTH"
    - path: "src/lib/catalog/listing.ts"
      provides: "LUMEN's description or quiet line naming the anchor row"
      contains: "lumen"
  key_links:
    - from: "src/lib/catalog/entries/lumen.ts"
      to: "src/lib/sim/lua-smoke.spec.ts"
      via: "the 11-09.2 depth test, extended with the exact-black clause at index 3"
      pattern: "lumen"
---

<objective>
LUMEN, with the verdict attached.

> `LUMEN: seems like nothing changed`

**What Probe B settled.** Four distinct brightness levels at 196 / 139 / 84 / 27; 27 clearly lit
against dark; the halving ladder 128 / 64 / 32 / 16 all visible. The LEDs were never the suspect and
the research's sRGB-gamma hypothesis is retired. **What 12-01 settled** is read from its SUMMARY, not
restated here: either the knob-to-wire path was found broken and fixed there - in which case the
user's report was HANGAR's bug and this plan's re-cut is a bonus - or it was sound, in which case the
report is the second candidate the probe named: **the top row cannot move** (`d = 36 - row*@DEPTH`, row
0 is 36 at every value) and whichever rows the user compared did not carry the change, or the
one-step-from-default difference (84 against 139) did not read as "changed" on a pad that was also
lit at row 0 at 253.

**The honest deliverable, under either verdict.** 11-09.2 costed two routes and shipped neither; the
header at `lumen.ts:186-222` records both. This plan ships the **subtrahend 32** route: `d = 32 -
row*@DEPTH` with `anchor*d//32`, the same four values, character-neutral (11-09.2 measured 608
against 608 that day; 746 today), and the bottom row reaches **exact black at @DEPTH 4** - the one
picture that cannot be mistaken for "nothing changed". The default stays at index 2: index 3 at 32/32
turns a row of the card off at rest, and that is the visitor's choice to make, not the card's. The
never-black guarantee in the header becomes `8*max(@DEPTH) <= 32` with equality allowed.

**And the row-0 sentence goes where a visitor reads it.** The card's description or quiet line says
the top row is the anchor and the ramp deepens below it, so a bench tester compares the bottom row.

**LUMEN's cursor is also a cell test.** `if n~=s.c or e>3` on `x*9//128+y*9//128*9` re-sends the
colour over sysex and moves the cursor on every cell change - a finger on a boundary sends sysex
alternately. `Q` answers it at the same cost as the sequencers; measure and take it.

Purpose: the last "nothing changed" answered with a picture that cannot be misread, and the
explanation in the card.
Output: one entry re-cut, its copy amended, its test extended, its picture regenerated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-touch-framework/12-VALIDATION.md
@.planning/phases/12-touch-framework/PROBE-RESULTS-2026-09-10.md
@.planning/phases/12-touch-framework/12-01-SUMMARY.md
@.planning/phases/12-touch-framework/12-07-SUMMARY.md
@.planning/phases/11-bench-corrections/11-09.2-SUMMARY.md
@.planning/phases/11-bench-corrections/11-10-SUMMARY.md
@src/lib/catalog/entries/lumen.ts
@src/lib/catalog/library.ts
@src/lib/catalog/listing.ts

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.**
- **No agent connects to or writes to a device, and no agent deploys.** Probe B is the only
  hardware observation this plan rests on, and it was the user's.
- **`src/vendor/` is not touched.** `firmware-oracle.spec.ts` green and unedited.
- **Never touch a sibling repository.** **Do not edit `.planning/ROADMAP.md`.**
- **`gsd-tools state advance-plan` and `record-session` destroy lines.** Copy `STATE.md` first, repair
  by inverse edit, skip `roadmap update-plan-progress`.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- Commit with `git commit --only <paths> -F <message-file>`. Do not push.
- **State every count as a carried name plus a delta.** This plan carries `PREV_FILES 85` /
  `PREV_TESTS 888` from 12-10 and asserts `+0 / +0` **written out** - the depth test is extended, not
  added.
- **Every cost at the RGB444 picker corner**; `@DEPTH` keeps **four** values, so no LUMEN link is
  demoted - measure `shapeOf` before and after and say so.
- **Both Phase 11 gates apply.** LUMEN has no decay; its onset stays `(e==4 or e>8)` inside `Q`.
- `frames.json` regenerated, never hand-edited (eighty of eighty-one cells move at the default);
  this is the one plan in this wave that regenerates it.
</standing_rules>

<interfaces>

## The re-cut (`lumen.ts:343`)

```
today:  local d=36-n//9*@DEPTH local r=H[i+1]*d//36 local g=H[i+2]*d//36 local b=H[i+3]*d//36
after:  local d=32-n//9*@DEPTH local r=H[i+1]*d//32 local g=H[i+2]*d//32 local b=H[i+3]*d//32
```

Four literals move (36 -> 32 at four sites, the research says two; count them in the string). Bottom
row at @DEPTH 1..4: `d` = 24 / 16 / 8 / **0**. At the default (index 2, @DEPTH 3) the bottom-left cell
goes from `84,29,0` to `62,21,0` (11-09.2's figure - re-measure through the emitted bytes). Non-zero
bytes at @DEPTH 4 fall from 171 to 152 (re-measure).

## The cursor through `Q`

```
today:  local n=x*9//128+y*9//128*9 if n~=s.c or e>3 then <restore s.c, paint n, sysex, cursor> s.c=n end s:gms(@CH,176,@CC,x,0)s:gms(@CH,176,@CC+1,y,0)
after:  local n=Q(s,i,e,x,y)if n then <same> s.c=n end s:gms(...)s:gms(...)
```

`Q` returns the cell on onset and on change and nil otherwise - the same two conditions. The entry's
own end test (`if e~=1 and e~=4 and e<9 then return end`) stays before it so the x/y CCs are not sent
on a lift; `Q`'s expiry path runs on that end code only if the call is reached - decide whether the
end test comes before or after `Q` (after means `Q` forgets the contact on a lift, which is right;
before means it never does, and a stale `H[i]` waits for the next press) and say which. **LUMEN's
`local function F` and `local function D` shadow the library's `F` and `D` inside its own chunk** -
harmless, because LUMEN calls neither library function, and `E`'s `F(i)` resolves in the library's
chunk; write that sentence in the header so nobody "fixes" the names.

## The test (`lua-smoke.spec.ts:3357`, 11-09.2's)

Extend, not add: at index 3 (@DEPTH 4) the bottom row's emitted bytes are exactly `0,0,0` in every
column; at every index row 0 is byte-identical; the relation is still monotonic downwards; and a
boundary finger on the cursor (x 71/72 wobble) sends **one** sysex, counted from `sysexLog`.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-11-01: The 32/32 re-cut, the cursor on Q, and the verdict written into the header</name>
  <files>src/lib/catalog/entries/lumen.ts, src/lib/catalog/listing.ts, src/lib/sim/lua-smoke.spec.ts, src/lib/catalog/frames.json, static/og/lumen.png, docs/HARDWARE-AUDITION.md</files>

  <read_first>
    - `12-01-SUMMARY.md` - the verdict, first
    - `src/lib/catalog/entries/lumen.ts` in full, especially `:100-230` (the ramp's arithmetic, the two
      costed routes, the never-black guarantee) and `:343`
    - `src/lib/sim/lua-smoke.spec.ts:3357-3590` - the depth test; `:3588-3660` - the sysex test
    - `src/lib/catalog/listing.ts:355-366`
    - `docs/HARDWARE-AUDITION.md` - LUMEN's row
  </read_first>

  <action>
**1. The re-cut**, all four literals; measure at the picker corner before and after (746 expected
both ways; quote the observation). `shapeOf` before and after, equal.

**2. The cursor on `Q`**, measured; the end-test ordering decided and stated.

**3. The header.** Resolve the two costed routes on the record: `32/32` shipped, the default kept
with the reason, the default-move route not taken with the reason; the never-black guarantee restated
as `8*max(@DEPTH) <= 32` with equality; **12-01's verdict quoted** with the sentence that follows
from it; Probe B's four levels quoted as the reason the LEDs were never the suspect and the gamma
hypothesis retired; and the sentence that nothing here is hardware-verified.

**4. The copy.** `listing.ts`'s LUMEN description or quiet line gains the anchor-row sentence, one
line, in the house register (read the neighbouring rows). `copy.spec.ts` may pin lengths; run it.

**5. The test, extended** per the interfaces block; term `+0`.

**6. Regenerate `frames.json`** (list how many of the eighty-one cells moved at the default - the
research says eighty) and **`npm run build`** for `static/og/lumen.png`; bytes before and after.

**7. `docs/HARDWARE-AUDITION.md`.** LUMEN's row rewritten as the observation that settles it: install
at @DEPTH 1, install again at @DEPTH 4, **compare the bottom row** - at 4 it is off; the top row is
the same both times, by design.

**Negative check:** keep one `36` of the four; expect the exact-black assertion red with the residual
bytes named (the divisor and the subtrahend disagree, and the bottom row is not 0).
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/ src/lib/share/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts && npm run build</automated>
    Expect the depth test green with the exact-black clause and the one-sysex count; `frames.spec.ts`
    green after regeneration; `stamp.spec.ts`'s LUMEN literals still `restored`; LUMEN inside 908 at
    every knob position with its margin stated.
  </verify>

  <done>
LUMEN's ramp reaches exact black at its deepest value with the default unmoved and every link still
`restored`; the cursor comes from `Q`; the header resolves both costed routes and quotes 12-01's
verdict and Probe B's levels; the card tells the visitor which row is the anchor; the test is extended
with the exact-black and one-sysex clauses; the fixture and the OG image regenerated with their
numbers; the audition row is the bottom-row comparison.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs 85 888` - `PREV_FILES + 0` /
  `PREV_TESTS + 0`. **This plan's term is `+0`, written out.**
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - LUMEN inside 908 at every knob
  position.
- `npm run build`, then `npm run test:quick` again; `static/og/` at `27 + 0` files with `lumen.png`'s
  bytes before and after.
- `git diff --stat HEAD -- src/vendor/` unchanged; `git diff --quiet -- .planning/ROADMAP.md`.
</verification>

<success_criteria>
1. At @DEPTH 4 the bottom row is exactly black in every column and row 0 is byte-identical at every
   index, proved on emitted bytes.
2. `@DEPTH` keeps four values; `shapeOf` unchanged; every LUMEN stamp still `restored`.
3. The cursor cell holds under a boundary wobble, one sysex counted.
4. 12-01's verdict is quoted in the header and the plan's deliverable follows from it.
5. The visitor is told which row is the anchor, in the card.
6. Nothing under `src/vendor/` moved; no device touched.
</success_criteria>

<output>
After completion, create `.planning/phases/12-touch-framework/12-11-SUMMARY.md` carrying: the carried
counts plus `+0 / +0`; the cost before and after at the picker corner; the emitted bottom-row bytes at
all four indices; the number of `frames.json` cells that moved; the OG bytes; the end-test ordering
chosen for `Q`; 12-01's verdict as quoted; the negative check with both exit codes; and the sentence
that nothing here is hardware-verified.
</output>
