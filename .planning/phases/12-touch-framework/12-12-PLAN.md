---
phase: 12-touch-framework
plan: 12
type: execute
wave: 12
depends_on: ["12-11"]
files_modified:
  - docs/TESTING.md
  - docs/HARDWARE-AUDITION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/12-touch-framework/deferred-items.md
autonomous: false
requirements: [SAFE-03, SAFE-05, CONT-01, CONT-02, CONT-03, CAT-01, CAT-03, CAT-04, PREV-01, PREV-02, PREV-06, TUNE-01, TUNE-05, SHARE-01, SHARE-03, SHARE-04]

must_haves:
  truths:
    - "Every projection this phase planned against is replaced by an observation taken on this machine, on a clean tree, against a fresh production build - and every wrong projection is named as wrong, including any of 12-VALIDATION's per-plan deltas and the planner's own library figure."
    - "The phase total is a chain from 11-16's carried block in twelve terms, one per plan with every +0 written out, and the count of terms is itself asserted against every prose statement of it in 12-VALIDATION and in this plan."
    - "Every entry this phase touched is re-measured at the RGB444 picker corner at the gate, and any header that disagrees with the measurement is corrected and named."
    - "SAFE-03 and SAFE-05 are amended by name and dated - the snapshot and the KEEP confirmation now cover a third script - and every other touched requirement carries a qualifier saying what was proved and what was not."
    - "The bench rows are handed over unanswered by an agent that never touched a device, ordered so the first ten minutes re-observe the two probe findings the whole phase rests on: the boundary finger and the lost lift."
    - "Everything this phase could not do is named with what would close it, and the two hardware findings the probe recorded for the production unit are written where the user will find them."
  artifacts:
    - path: "docs/TESTING.md"
      provides: "Every count and cost re-measured at the gate; the twelve-term chains; the three-write install shape; the library row"
    - path: ".planning/phases/12-touch-framework/deferred-items.md"
      provides: "The phase's work list: non-deliveries with what closes each, the hardware findings, the open decisions, the state of every item the twelve SUMMARYs recorded"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "src/lib/device/snapshot.ts"
      via: "SAFE-03 amended to name the system element's setup as the third string snapshotted and restored"
      pattern: "system"
---

<objective>
The phase gate: every number re-measured, two requirements amended, the non-deliveries named, the
hardware findings recorded, and the bench rows handed over.

**What a gate is for in this repository** (11-16's words, kept): not a victory lap. Eleven plans have
each asserted a carried name plus a delta; this plan is the only one that asserts against 11-16's
carried block, and its number is the phase total **written as a chain** so the block and the total
are visibly the same arithmetic seen from two ends. Phase 11's gate found twelve rows the planner got
wrong and named every one; this gate expects to find some and names them the same way.

**Two requirements are falsified by this phase, and this plan amends them rather than leaving them
standing.** SAFE-03 says HANGAR *"snapshots the touch element's Setup and Timer configs"*; since 12-03
it snapshots and restores three strings, one of them the system element's setup. SAFE-05's
confirmation names *"the Setup and Timer scripts on your ZONA's touch element"* as what KEEP replaces;
a KEEP stores the page, and the page now carries the library. Both are amended by name and dated,
neither deleted nor softened.

**This phase's entire premise is that the bench is the only instrument that sees touch correctly.**
The probe answered seven questions on the desk this morning; every plan in the phase was written
against those answers; and nothing in any of them has been seen on the desk since. The checkpoint
below is where that is said plainly and the rows are handed over, ordered by what re-observes the
probe's own findings first.

Purpose: the phase closed honestly, with everything it could not do named and the desk given what it
needs to say whether the framework works.
Output: one re-measured document, two amended requirements, one deferred-items file, the audition
rows, one checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-touch-framework/12-VALIDATION.md
@.planning/phases/12-touch-framework/PROBE-RESULTS-2026-09-10.md
@.planning/phases/12-touch-framework/BENCH-2026-09-10.txt
@.planning/phases/12-touch-framework/12-01-SUMMARY.md
@.planning/phases/12-touch-framework/12-06-SUMMARY.md
@.planning/phases/12-touch-framework/12-07-SUMMARY.md
@.planning/phases/11-bench-corrections/11-16-SUMMARY.md
@.planning/phases/11-bench-corrections/11-16-PLAN.md
@.planning/phases/11-bench-corrections/deferred-items.md
@.planning/phases/11-bench-corrections/TOUCH-CODE-9.md
@docs/TESTING.md
@docs/HARDWARE-AUDITION.md
@docs/INSTALL-RUNBOOK.md

<standing_rules>
- **No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.**
- **No agent connects to or writes to a device, and no agent deploys.** `npm run deploy` is not run.
- **`src/vendor/` is not touched**, and its diff must equal `upstream-manifest.json`'s
  `intendedDivergence` rows exactly - the sixteen-plus-twelve consecutive waves with no vendored byte
  moved; `firmware-oracle.spec.ts` green and unedited; `preset-baseline.json` byte-unchanged.
- **Never touch a sibling repository.**
- **Do not edit `.planning/ROADMAP.md`.** Its Phase 12 block stays as it stands; the row for whoever
  updates it goes in the SUMMARY.
- **`gsd-tools state advance-plan` and `record-session` destroy lines.** Copy `STATE.md` first, repair
  by inverse edit, skip `roadmap update-plan-progress`. `requirements mark-complete` is not run: CAT-04
  stays unticked for the fifth time and the reason is restated.
- **A shipped SUMMARY is a record of what was observed and believed.** A superseded number in one -
  including `TOUCH-CODE-9.md`'s framing - is **not** edited; it is left standing and pointed at.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- Commit with `git commit --only <paths> -F <message-file>`. Do not push.
- **This plan asserts against 11-16's carried block. Every other plan asserted against `PREV_*`.**
- e2e at `--workers 3`; `test-results/` removed by hand; no commit while the suite runs.
</standing_rules>

<interfaces>

## The chains to write out, twelve terms each, every zero written

```
tests   869  +1 (12-01)  +4 (12-02)  +3 (12-03)  -3 (12-04)  +2 (12-05)  +0 (12-06)
             +8 (12-07)  +1 (12-08)  +2 (12-09)  +1 (12-10)  +0 (12-11)  +0 (12-12)   = 869 + 19 = 888
files   84   +0 +0 +0 +0 +0 +0  +1 (12-07: library.spec.ts)  +0 +0 +0 +0 +0          = 85
e2e     86 titles  +1 (12-01)  +0 x 11                                                  = 87
        105 runs   +1 (12-01, chromium only)  +0 x 11                                   = 106
catalog 29   +0 +0 +0  -3 (12-04)  +0 +0 +0 +0 +0  +1 (12-10)  +0 +0                    = 27, 9 + 18
og      29   -3 (12-04)  +1 (12-10), zeros elsewhere                                    = 27
audition 25  -3 (12-04)  +1 (12-10), zeros elsewhere                                    = 23
sweep   4 19 in every plan, unchanged, by 12-07's choice
```

**Twelve terms for twelve plans**, each checked against its plan's own `check-counts` line, with a
disagreement against a plan named as a plan defect and a disagreement against `12-VALIDATION.md`
named as a document defect. **The catalog and e2e chains change under two branches**: `replace` at
12-06 makes 12-10's catalog term `+0` and adds a dead link; a wiring fix at 12-01 adds no test. Read
both SUMMARYs before writing a number.

Per file, projected (reconciled here): `model.spec` +2 (12-01, 12-07); `constants.spec` +1;
`descriptors.spec` +1; `sequence.spec` +1; `synthetic.spec` +1; `install.spec` +2; `snapshot.spec` +1;
`lua-smoke` 25 -> 22 -> 23 (12-05) -> 25 (12-07) -> 26 (12-08) -> 28 (12-09) -> 29 (12-10); `view.spec`
+1; `library.spec` 3 (created); `lua-host.spec` +1; `host-surface.spec` +1:
`2+1+1+1+1+2+1-3+1+2+1+2+1+1+3+1+1 = 19`.

## The re-measurement at the picker corner

Every entry this phase touched: ARC, CONSOLE, EUCLID, STEPS, RADAR POINTS, CHORUS, MORPH, LUMEN, GLIDE,
plus the library string - at defaults and at the picker corner, Setup and Timer, free at worst; each
header's figure beside the measurement; any header that disagrees corrected and named. WHEELS, STRIP
and the untouched entries re-read from `docs/TESTING.md`'s four-corner table, not re-measured.

## The two amendments

- **SAFE-03**: quote the current text; the new text names three strings - the system element's setup
  (the page init) and the touch element's Setup and Timer - snapshotted before any write and restored
  by PUT BACK in the order system, Timer, Setup; a snapshot taken before this phase restores the
  firmware's system default; dated 2026-09-10 with the plan (12-03).
- **SAFE-05**: quote the current text; the confirmation's amended sentence from 12-03 if it was
  amended, or the qualifier that KEEP stores the whole page including the library and the copy says so
  where 12-03 decided it must; dated.

**Qualifiers** (proved / not proved) for CONT-01 (nine presets unchanged, or eight under `replace`),
CONT-02 (every hand-authored entry inside 908 at every corner; the library at its cost; **nothing on
a ZONA since the probe**), CONT-03 (seven FOR terms, thirteen in all, zero singletons, `still` at six
by re-homing, the thirteenth removal's consequence), CAT-01 (three more dead links, twelve in all),
CAT-03, CAT-04 (unticked, fifth time), PREV-01 / PREV-02 (the host runs the system setup; the preview
can call the library; **the preview still cannot show a wobbling finger, a lost lift or a phantom** -
the three things the probe found, none of which `touch.ts` produces), PREV-06 (oracle unedited),
TUNE-01 / TUNE-05 (GLIDE's knobs; NINE PADS' default; no value count moved), SHARE-01 / SHARE-03 (no
link demoted this phase - say it, because Phase 11 could not), SHARE-04 (27 OG images).

## The deferred-items file, sections

A. **Could not do**: clock sync (still blocked twice); SNAKE (still deferred); the `_coordMax`
two-axis fix (D-11-13-a: 12-07 touched `lua-host.ts` and chose not to widen its scope - say so);
anything a plan recorded as a finding (a `D(` call the decay gate cannot see through, if 12-10 found
it; a library that did not fit with `D`, if 12-07 dropped it).
B. **Hardware findings for the production unit**, from the probe: dropped single taps (Q3 - a
threshold matter), palm ghosting (Q7 - phantoms that keep reporting; PUT BACK and power-cycle).
Recorded for the user, not for a plan.
C. **Retired by the probe**: `TOUCH-CODE-9.md`'s framing (code 9 does not occur for a human tap;
`touch.ts` is faithful here; the class-B fixes were correct and harmless); the sRGB-gamma hypothesis;
the fast-tap reading of "not precise". Pointed at, never edited.
D. **Open for the user**: the expiry window (bench row 2 decides it); CHORUS's two seconds; whether
GLIDE earns `featured`; JOYSTICK if `as-is`; STRIP as three (still 1,088); the twelve dead links.
E. **Every item the twelve SUMMARYs recorded**, with its state at the gate - the table 11-16 kept.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-12-01: The gate against a fresh production build, the twelve-term chains, and the picker-corner re-measurement</name>
  <files>docs/TESTING.md, docs/HARDWARE-AUDITION.md</files>

  <read_first>
    - all twelve SUMMARYs, for each plan's own `check-counts` line and its recorded branch
    - `11-16-SUMMARY.md` - the carried block and the shape of the chain
    - `12-VALIDATION.md` - the per-plan delta table, every row of which is replaced here
    - `docs/TESTING.md` in full - every count, the four-corner table, the install shape paragraph
    - `docs/HARDWARE-AUDITION.md` in full
  </read_first>

  <action>
**1. The gate**, on a clean tree, free memory beside every run:
`npm run check` (file count reported, `0 ERRORS 0 WARNINGS` asserted); `npm run lint`;
`npm run test:quick 2>&1 | node scripts/check-counts.mjs 85 888` with the chain written out and
**the term count asserted at twelve** against every prose statement of it in `12-VALIDATION.md` and
this plan; `npm run test:sweep` at `4 19` with the wall clock against 11-16's pair; `npm run build`;
`npm run test:quick` again; `npx playwright test --workers 3` at **`106`** with `grep -c "test("`
at **87** and the reason they differ by the same one; the catalog at **27** as a twelve-term chain
with the split; `static/og/` at 27 with its byte total against 172,699 B / 29; the audition rows at
23; `git diff --stat HEAD -- src/vendor/` beside the manifest's rows; `firmware-oracle.spec.ts` and
`preset-baseline.json` unedited (`git diff --quiet`). Run the e2e suite twice; report both.

**2. Every disagreement named.** A term that disagrees with its plan's line is a plan defect; one
that disagrees with `12-VALIDATION.md` is a document defect; the planner's library figure (885) beside
12-07's measurement; the planner's savings for the three sequencers beside 12-08's; the research's
figures (643, 550, MORPH's call shape) as 12-05, 12-07 and 12-09 corrected them. Write them in
`docs/TESTING.md`'s "Where the planner was wrong" the way 11-16 did.

**3. The picker-corner re-measurement** per the interfaces block, into the four-corner table; every
header corrected that disagrees, named.

**4. `docs/TESTING.md`** rewritten where counts and shapes live: twenty-seven configurations, eighteen
hand-authored; the three-write install; the library row; the twelve-term chains; the e2e numbers;
the sweep totals as observed.

**5. `docs/HARDWARE-AUDITION.md`** at 23 contiguous rows, every cost row re-measured from the entry,
and **two rows added to the existing entries' expectations rather than as new rows**: WHEELS under two
fingers (pitch and mod together, both wheels move, neither disturbs the other - Q6 makes this the
one entry that earns a two-finger row), and CHORUS's expiry (hold a chord still for ten seconds).
Every row still the user's and unanswered.

**Negative check:** raise `facets.spec.ts:110`'s floor to `> 27`; expect red naming 27 (11-16 found
the raise-by-one arm green at the old floor; state which arm goes red at 27 and why). Restore from a
scratch copy, sha256 both sides.
  </action>

  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs 85 888 && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && npm run build && npx playwright test --workers 3 && git diff --stat HEAD -- src/vendor/</automated>
    Every gate green at the stated arithmetic; the vendored diff equal to the manifest's rows; the
    tree clean after `test-results/` is removed.
  </verify>

  <done>
The gate green against a fresh build at `84 + 1` / `869 + 19` in twelve terms with the term count
asserted and every disagreement named as a plan or document defect; e2e `87` / `106` with the reason;
catalog 27 as a chain with 9 + 18; every touched entry re-measured at the picker corner and every
header that disagreed corrected; `docs/TESTING.md` carrying observations where the validation
document carried projections; the audition checklist at 23 with the two-finger and the expiry
expectations; one negative check red.
  </done>
</task>

<task type="auto">
  <name>Task 12-12-02: SAFE-03 and SAFE-05 amended, every touched requirement qualified, and the deferred-items file</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/12-touch-framework/deferred-items.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` SAFE-03, SAFE-05 verbatim, and every requirement in this plan's list
    - `12-03-SUMMARY.md` - the amended copy before and after; D-11-08.1-a's closing line
    - `11-16-SUMMARY.md` - the amendment shape and the qualifier table
    - `11-bench-corrections/deferred-items.md` in full - the items this phase closes, moves or leaves
    - `PROBE-RESULTS-2026-09-10.md` "What this file retires" and "What this file opens"
  </read_first>

  <action>
**1. Amend SAFE-03 and SAFE-05** per the interfaces block, by name and dated, current text quoted,
new text stated, plan named. Neither deleted nor softened.

**2. Qualifiers** for every requirement in this plan's list: proved / not proved, dated
`PHASE 12 GATE QUALIFIER, 2026-09-10`, with the sentence that **nothing in this phase has been
hardware-verified since the probe** wherever a hardware claim would otherwise be implied. CAT-04
stays `[ ]` with the reason for the fifth time.

**3. `deferred-items.md`** for this phase, sections A-E per the interfaces block. Section E walks
Phase 11's file: D-11-08.1-a **closed** by 12-03 (the line named); D-11-13-a **open**, with 12-07's
choice; D-11-16-a open; C.3 JOYSTICK **answered** (12-06, verbatim); C.5 the code-9 gap **retired by
the probe** (option 2 chosen by evidence: record and ship; `touch.ts` is faithful); A.3 TRACKPAD
**delivered** as GLIDE beside the preset (or replaced); A.9 LUMEN **delivered** with 12-01's verdict;
every other row carried with its state.

**4. The bench-note trace** for `BENCH-2026-09-10.txt`, every line: the user's snippet (12-07's `A`);
ARC (12-05); CHORUS (12-09); EUCLID (12-08); LATTICE (12-04); LUMEN (12-01, 12-11); MORPH (12-09);
NINE PADS (12-01, 12-05); SNAKE (deferred); CONSOLE (12-05, 12-09); FORGE (12-04); GHOST (good,
untouched); JOYSTICK (12-06, the answer); RADAR POINTS (12-08); POMODORO (good); SHUTTLE (12-04);
STEPS (12-08); Trackpad (12-10); the bigger-scope decision (12-07). No line closes on a claim.

**5. The ROADMAP row, for the SUMMARY only**: requirements, twelve plans, final counts, status
"complete pending the checkpoint answer; nothing hardware-verified since the probe".

**Negative check:** change one `intendedDivergence` row's `reason` to an empty string; expect
`vendored-diff.spec.ts` red naming the file and the field; restore from a scratch copy, sha256 both
sides.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/fidelity/ && git diff --quiet -- .planning/ROADMAP.md src/lib/fidelity/firmware-oracle.spec.ts src/lib/fidelity/preset-baseline.json</automated>
    The fidelity suite green; the roadmap, the oracle and the baseline untouched.
  </verify>

  <done>
SAFE-03 and SAFE-05 amended by name and dated; every touched requirement qualified with its unproved
half; `deferred-items.md` carrying the non-deliveries, the two hardware findings, the three retired
readings, the open decisions and the state of every Phase 11 item; every bench line traced to a plan,
a removal, a deferral or an answer; the ROADMAP row written for whoever updates it; one negative
check red.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12-12-03: The bench rows - the probe's two findings re-observed first, then every re-fitted entry</name>

  <files>docs/HARDWARE-AUDITION.md, docs/INSTALL-RUNBOOK.md</files>

  <read_first>
    - `docs/HARDWARE-AUDITION.md` at 23 rows, every row still the user's
    - `docs/INSTALL-RUNBOOK.md` rows C and E as 12-03 left them
    - this phase's `deferred-items.md` as task 02 wrote it
  </read_first>

  <action>
Present the sections below and stop. **Do not run any part of the bench list. No agent in this phase
connected to a device, performed a write, or deployed**, and this checkpoint is where that promise is
handed over. Order the rows so the first ten minutes re-observe the two findings the whole phase
rests on.
  </action>

  <what-built>
Your probe answered seven questions this morning and every plan in this phase was written against
those answers. The framework is now a library your ZONA loads before any touch configuration - a cell
test that holds a finger on the line between two cells, a lift that is never trusted on its own, the
finger lighting its cell, and your own per-axis snippet - and HANGAR writes it, snapshots it, restores
it and clears it as a third script beside Setup and Timer. Three configurations were removed. The
sequencers, CONSOLE, MORPH and CHORUS sit on the library; ARC stays stopped under a still finger;
NINE PADS starts at sixteen and its knob can be seen; LUMEN reaches black at its deepest and tells you
which row to compare; TRACKPAD's edge flash exists as GLIDE beside the preset; JOYSTICK is whatever
you chose. State the catalog from the chain: twenty-seven, nine presets and eighteen hand-authored.

**Nothing in any of it has touched a ZONA since the probe.** Every claim above was measured in a real
Lua VM, in the simulator, or read out of firmware C - and the probe is the reason none of that is
enough on its own.
  </what-built>

  <how-to-verify>
**Install first, once** - TRY ON DEVICE on any hand-authored card. You will see one white flash of
about half a second (the firmware paints white on every write). Then, in this order:

1. **The boundary finger, on EUCLID** (two minutes). Rest a fingertip on the line between two cells
   and hold it for five seconds. One cell should arm and stay armed; nothing should flicker. Then
   slide slowly across the line - the neighbour should arm once, and sliding back should arm the
   first once. If the two cells flicker as they did in the probe, the margin is wrong and the number
   to report is how far you have to move before it switches.

2. **The lost lift, on CHORUS** (three minutes). Press five fingers on five pads and lift them
   together. Every chord should stop within two seconds of the lift, whatever the module reported.
   Then hold one chord perfectly still for ten seconds: **does it release on its own?** If it does,
   the expiry window is shorter than a real hold and the number to report is how many seconds it
   lasted. Then wipe a flat palm across the pad and lift: report whether any chord sounds afterwards,
   and whether it stops within a few seconds once the pad settles or keeps going until PUT BACK.

3. **STEPS and RADAR POINTS**, the same boundary finger as row 1, one minute each.

4. **WHEELS under two fingers** - pitch with one hand, mod with the other; both move, neither
   disturbs the other, pitch springs home on lift. Two fingers is the one case the probe said is
   lossy; this is the card that earns the row.

5. **ARC**: tap the centre to stop it, then leave your finger resting on the centre. The swirl must
   stay stopped. Tap again; it resumes at the rate you last dragged.

6. **CONSOLE**: mute a column, move its fader - the picture moves, nothing sounds; unmute - the level
   you moved to is sent.

7. **MORPH**: the corners are now nine cells each; a finger a cell and a half from a corner already
   reads full; hold a finger still in the middle - no flicker.

8. **NINE PADS** starts at sixteen pads; the Pads control reads as two words, `9` and `16`.

9. **LUMEN**: install at the lightest depth, then at the deepest, and compare the **bottom** row - at
   the deepest it is off. The top row is the same both times, by design; that is the anchor.

10. **GLIDE**: drag right - the right edge flashes and fades to black; drag up - the top edge.

11. **JOYSTICK**, per your answer at 12-06.

12. **Runbook row C** again: CLEAR then KEEP then PUT BACK. Three frames now go out per click, and a
    KEEP after a CLEAR leaves nothing of HANGAR's on the module.

**Two hardware findings recorded for the production unit, not for a plan:** dropped single taps
(the probe's Q3) and palm ghosting (Q7). **Retired by your probe:** the code-9 gap in the preview - a
human tap never produces a 9 - and the gamma reading of LUMEN.

**Still open, each with what closes it, in `deferred-items.md`:** clock sync; SNAKE; the two-axis
coordinate maximum in the host; STRIP as three; the twelve dead `/c/<id>/` links.
  </how-to-verify>

  <resume-signal>
Type "approved" to close the phase, or describe what the bench found - the boundary finger's switch
distance and the chord's hold time are the two numbers that move constants.
  </resume-signal>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs 85 888` - `84 + 1` / `869 + 19` in twelve
  terms, the term count asserted at twelve.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19`, wall clock recorded.
- `npm run build`, `npm run test:quick` again, `npx playwright test --workers 3` at `106` with
  `grep -c "test("` at 87, run twice; `test-results/` removed; `git status --porcelain` empty.
- `git diff --stat HEAD -- src/vendor/` pasted beside the manifest's rows and asserted equal.
- `git diff --quiet -- src/lib/fidelity/firmware-oracle.spec.ts src/lib/fidelity/preset-baseline.json`.
- `git diff --quiet -- .planning/ROADMAP.md` - untouched, deliberately.
</verification>

<success_criteria>
1. Every projection in `12-VALIDATION.md` is replaced by an observation, every wrong one named as
   wrong and by how much, including the planner's own figures.
2. The phase total is `84 + 1` / `869 + 19` in twelve terms with the term count asserted; the catalog
   27 as a chain with its split; e2e 87 / 106 with the reason.
3. Every touched entry re-measured at the picker corner and every wrong header corrected.
4. SAFE-03 and SAFE-05 amended by name and dated; every other touched requirement qualified; CAT-04
   unticked with its reason.
5. Every bench line traced; every non-delivery named with what closes it; the two hardware findings
   and the three retired readings recorded; `TOUCH-CODE-9.md` pointed at, not edited.
6. The bench rows handed over unanswered, the probe's two findings first, by an agent that never
   touched a device.
</success_criteria>

<output>
After completion, create `.planning/phases/12-touch-framework/12-12-SUMMARY.md` carrying: the
re-measured block against 11-16's carried block; the twelve-term chains with every term reconciled and
every disagreement named as a plan or document defect; the term count asserted; the picker-corner
table with every corrected header; both suite runs; the vendored diff beside the manifest; the two
amendments before and after; every qualifier's unproved half; the bench-note trace; the deferred
items by section; the user's checkpoint answer verbatim; and the ROADMAP row for whoever updates it,
with the note that the roadmap was deliberately not edited.
</output>
