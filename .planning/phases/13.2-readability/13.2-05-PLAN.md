---
phase: 13.2-readability
plan: 05
type: execute
wave: 5
depends_on: ["13.2-04"]
# Execution order (13.2-VALIDATION "Why the phase is serial"): SERIAL - 01 -> 02 -> 03 -> 04 -> 05 -> 06.
# FIFTH: forty-eight components and app.css - the <!-- --> block, the script's leading comment, the comments
# in <style>. Not Knob.svelte, not ColourPicker.svelte (D-03). No file renamed, no rule moved, no
# <style> reflow beyond comments: the built CSS hash is the proof.
files_modified:
  - src/app.css
  - src/lib/ui/BrowseGrid.svelte
  - src/lib/ui/BrowseLink.svelte
  - src/lib/ui/BrowseToolbar.svelte
  - src/lib/ui/BudgetMessage.svelte
  - src/lib/ui/BudgetMeter.svelte
  - src/lib/ui/CatalogCard.svelte
  - src/lib/ui/Clear.svelte
  - src/lib/ui/CopyLink.svelte
  - src/lib/ui/DestinationZone.svelte
  - src/lib/ui/DeviceActions.svelte
  - src/lib/ui/DeviceDetails.svelte
  - src/lib/ui/DeviceMark.svelte
  - src/lib/ui/DeviceNote.svelte
  - src/lib/ui/DeviceSlot.svelte
  - src/lib/ui/FacetRow.svelte
  - src/lib/ui/FailureBlock.svelte
  - src/lib/ui/FidelityLine.svelte
  - src/lib/ui/KeepConfirm.svelte
  - src/lib/ui/KnobRack.svelte
  - src/lib/ui/MidiField.svelte
  - src/lib/ui/MidiMonitor.svelte
  - src/lib/ui/MotionControl.svelte
  - src/lib/ui/PadCanvas.svelte
  - src/lib/ui/PadFrame.svelte
  - src/lib/ui/PadSpinner.svelte
  - src/lib/ui/SessionAnnouncer.svelte
  - src/lib/ui/StampNotice.svelte
  - src/lib/ui/Swatch.svelte
  - src/lib/ui/TagChip.svelte
  - src/lib/ui/TuningRegion.svelte
  - src/lib/ui/Wordmark.svelte
  - src/lib/ui/intro/HeroSurface.svelte
  - src/lib/ui/intro/Intro.svelte
  - src/lib/ui/intro/StartCard.svelte
  - src/lib/ui/library/LibraryTable.svelte
  - src/lib/ui/library/ResumeBanner.svelte
  - src/lib/ui/sandbox/ElementList.svelte
  - src/lib/ui/sandbox/Palette.svelte
  - src/lib/ui/sandbox/RegionInspector.svelte
  - src/lib/ui/sandbox/SurfaceActions.svelte
  - src/lib/ui/sandbox/SurfaceEditor.svelte
  - src/lib/ui/shell/ConnectionControl.svelte
  - src/lib/ui/shell/ContextBar.svelte
  - src/lib/ui/shell/Footer.svelte
  - src/lib/ui/shell/Header.svelte
  - src/lib/ui/shell/Inspector.svelte
  - src/lib/ui/shell/Nav.svelte
  - src/lib/ui/shell/Rail.svelte
  - src/lib/ui/aesthetic.spec.ts
  - src/lib/ui/browse-ui.spec.ts
  - src/lib/ui/device-ui.spec.ts
  - src/lib/ui/font-assets.spec.ts
  - src/lib/ui/identity.spec.ts
  - src/lib/ui/instrument.spec.ts
  - src/lib/ui/intro.spec.ts
  - src/lib/ui/sandbox-ui.spec.ts
  - src/lib/ui/shell.spec.ts
  - src/lib/ui/tune-ui.spec.ts
  - src/lib/tune/colour-picker.spec.ts
  - docs/CODE-STYLE.md
  - .planning/phases/13.2-readability/gate/
  - .planning/STATE.md
autonomous: true
requirements: [] # none - a refactor claims nothing

must_haves:
  truths:
    - "Every component in the list opens with a <!-- --> block of at most ten lines saying what it renders, what it takes and what rule it obeys, with one pointer; the script's leading comment and every $effect latch keep one line each; comments inside <style> are gone or one line; no rule moved, no selector reordered."
    - "The SCOPED built-CSS hash (the two Tailwind layers removed) is the baseline's after a fresh build (Svelte hashes the filename; Vite strips <style> comments), every markup-named utility is still emitted, the utilities that left with the comments are printed by name, the normalised built JS is equal (comments only), and the radius gate is EMPTY with the six circles at their lines, the two circle files unedited."
    - "The four titles that pin phrases in TuningRegion.svelte and MidiMonitor.svelte are green with zero body edits: THERE IS NO ADVANCED SECTION, Use actual parameter names, NOT GENERAL UNDO, 13-16, Not a history, not a stack, not a tree, D-14 Q4b, src/vendor/, midiLog, history.ts; and the four raw prose pins in device-ui.spec.ts hold: aria-live in TuningRegion.svelte prose (:681), writeLock in DeviceDetails.svelte prose (:1173), and the summary-versus-button sentence in DeviceSlot.svelte and ConnectionControl.svelte (:1646)."
    - "No user-facing literal, no data-testid, no filename moved: the census, the testid hash and the copy-exports hash are the baseline's; every e2e title green in all five chunks."
  artifacts:
    - path: "src/lib/ui/TuningRegion.svelte"
      provides: "The region's header at ten lines with the two pinned decisions as one line each; the five-latch comments kept"
      contains: "NOT GENERAL UNDO"
    - path: "src/lib/ui/MidiMonitor.svelte"
      provides: "The monitor's header at ten lines with D-14 Q4b, src/vendor/ and midiLog named"
      contains: "midiLog"
    - path: "src/app.css"
      provides: "The site's tokens and roles with the essays gone; every @theme value byte-identical"
  key_links:
    - from: "src/lib/ui/tune-ui.spec.ts"
      to: "src/lib/ui/TuningRegion.svelte"
      via: "regionRaw.toContain"
      pattern: "THERE IS NO ADVANCED SECTION"
    - from: "src/lib/ui/sandbox-ui.spec.ts"
      to: "src/lib/ui/TuningRegion.svelte"
      via: "the other header points back"
      pattern: "history\\.ts"
---

<objective>
Forty-eight components (the fifty under `src/lib/ui/` less the two circle files) and the stylesheet:
the `<!-- -->` block a reader lands on first, brought to ten lines; the script's essays to facts; the
`<style>` comments out. Nothing that would move a byte of the SCOPED built CSS — no rename, no rule
moved between files, no selector reordered — and nothing that would move a string or a test id. The
Tailwind utilities layer WILL move (a comment word is a utility candidate under `source(".")`,
13.2-PLAN-CHECK H-1); the gate prints which classes left and asserts the markup-named ones stayed.

Purpose: the components are where the Bible's decisions were argued at length in 13-01 to 13-20 and
13.1; the arguments are in the SUMMARYs, and a component should say what it renders, what it takes
and the one rule it obeys.
Output: forty-eight components and `app.css` under the rule; eleven ui specs' headers under the rule;
the scoped CSS hash and the normalised JS hash equal; the four wording-pin titles and the four raw
prose pins green. Term **+0 / +0; e2e +0 / +0; check 655
(+0)**; the sweep by declaration; all five chunks.

**Execution order.** FIFTH, after 13.2-04 (and after 13.2-03, whose `tune/copy.ts` `TuningRegion`
cites).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-readability/13.2-CONTEXT.md
@.planning/phases/13.2-readability/13.2-VALIDATION.md
@.planning/phases/13.2-readability/13.2-04-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@docs/CODE-STYLE.md
@src/lib/ui/TuningRegion.svelte
@src/lib/ui/MidiMonitor.svelte
@src/lib/ui/DestinationZone.svelte
@src/lib/ui/BrowseGrid.svelte
@src/lib/ui/MidiField.svelte
@src/lib/ui/tune-ui.spec.ts
@src/lib/ui/sandbox-ui.spec.ts
@src/lib/ui/instrument.spec.ts
@src/lib/ui/identity.spec.ts
@src/lib/ui/radius.spec.ts
@src/lib/ui/radius-allowlist.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution and no co-author trailer (the
  repository's rule wins; the SUMMARY reports the conflict). No emojis.
- **No device, no deploy; sibling repositories read-only; the dev server on 5173 left alone.**
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state`.** STATE.md by script against a copy, asserting `status: executing`,
  `completed_phases: 11`, `percent: 100` unchanged. ROADMAP not edited. **CAT-04 `[ ]`.**
- **Never `prettier --write .`** — prettier (with `prettier-plugin-svelte`) on every file this plan
  touches, by path; a `<style>` block prettier reflows after a comment cut is a reflow the tree's
  formatter makes and is allowed — the built CSS hash is the judge, not the source bytes.
- **`git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on the four docs — none is this plan's.
- **`Knob.svelte` and `ColourPicker.svelte` are not opened for editing** (13.2-CONTEXT D-03); `git
  diff --stat 500e33c -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte` is empty at the end.
  **No `.svelte` file renamed, moved, split or created**; no rule moved between components; no
  selector reordered inside a file (the cascade); no `border-radius` above zero; the allowlist EMPTY;
  the six circles at `ColourPicker.svelte:840, :867, :882` and `Knob.svelte:730, :785, :807`.
- **The refuse-list, absolute**: every user-facing literal (the templates are made of them — the
  census), every `data-testid` (320; the testid hash), every `class` name a spec reads.
- **Four raw prose pins in `device-ui.spec.ts` must survive the header cuts** (13.2-PLAN-CHECK
  section 4): `:676-683` - raw `TuningRegion.svelte` counts more `aria-live` than its code (the
  prescribed header carries it); `:1170-1175` - raw `DeviceDetails.svelte` counts more `writeLock`
  than its code (today `:75` in the header and `:352` in a body comment - keep one prose mention,
  e.g. "the writeLock the store raises while it writes"); `:1640-1650` - the lowercased raw
  `DeviceSlot.svelte` (`:13-15` today) and `shell/ConnectionControl.svelte` (`:15-17`) each match
  `summary[^.]*whenever it does not` - keep the sentence "a button whenever there is something to press,
  a summary whenever it does not" (or its current wording) in each header. And `tune-ui.spec.ts:1216`
  - raw `MidiMonitor.svelte` contains `midiLog`.
- **The `$effect` latches stay with their one comment each** (`DestinationZone.svelte:203-205,
  :246-248`, `MidiField.svelte:106-112`, `TuningRegion.svelte:465`, `BrowseGrid.svelte:388-394`;
  13.2-RESEARCH A.8): a `$derived` cannot hold a previous value, and the comment above each is the
  one comment in the file a reader needs. The Sandbox header's box arithmetic in
  `[draftId]/+page.svelte` is 06's and is not moved (13.1 deferred-items row 12).
- **Every count a carried name plus a delta**: `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4
  19 / allowlist 0`. Term **+0 / +0; +0 / +0; check +0**. `check-counts.mjs 94 966`.
- **The gate brackets the plan**: `--before 05` first, `--after 05 --against 05 --check 655` last.
- **Quick at `--maxWorkers=2`; the ui specs (`npx vitest run --project server --maxWorkers=2
  src/lib/ui src/lib/tune/colour-picker.spec.ts`) after every component** (13.2-RESEARCH pitfall 10
  — the wording pins are per file; `instrument.spec.ts`, `identity.spec.ts`, `device-ui.spec.ts`
  and `radius.spec.ts` walk every component). Chunks **c1 c2 c3 c4 c5** on fresh detached servers.
- **Re-runnable (D-17)**: the remaining list is `node scripts/gate/comment-lines.mjs --todo src/lib/ui/**/*.svelte src/app.css` less the two circle files.
</standing_rules>

<interfaces>
The pinned phrases in this plan's files, kept verbatim inside the ten-line headers (13.2-CONTEXT
D-14; zero re-aims expected):

`TuningRegion.svelte` (header `:1-152`; `tune-ui.spec.ts:1476-1477` and `:1962-1964`;
`sandbox-ui.spec.ts:727`):
```
<!--
  The tuning region: the workspace's inspector - the knobs, the MIDI fields,
  the colour block, Randomize and its one-value Undo, and ONE aria-live region
  that never fires on a value change (device-ui.spec.ts:681 counts the word here).
  Reflow width and inset are layout.ts's and written in no component (tune-ui.spec.ts).
  THERE IS NO ADVANCED SECTION: section 7's boundary says "Use actual parameter
  names, limits, units, defaults, and dependencies from the configuration schema";
  the entries declare none of the advanced properties, so no disclosure is drawn.
  UNDO RANDOMIZE IS ONE VALUE AND ONE CLICK, and IT IS EXPLICITLY NOT GENERAL UNDO:
  Not a history, not a stack, not a tree - the Sandbox's draft history is
  src/lib/sandbox/history.ts (13-16), a different thing with a different owner.
  Decided at 13-09 / 13-10 / 13-16; see .planning/phases/13-gui-overhaul/13-10-SUMMARY.md
-->
```
(`13-16` and `history.ts` are in that text; `THERE IS NO ADVANCED SECTION`, `Use actual parameter
names`, `NOT GENERAL UNDO`, `Not a history, not a stack, not a tree` are verbatim; `aria-live` is the
prose occurrence `device-ui.spec.ts:676-683` requires - the raw file must count more `aria-live` than
its code does, and today the only prose occurrence is `:121`, inside the header this plan cuts.) The Randomize-scope
paragraph (`isMidiDestination` is ONE predicate doing two jobs) becomes one line beside the code
that uses it; the five-latch comment at `:465` stays.

`MidiMonitor.svelte` (header `:1-57`; `tune-ui.spec.ts:1212-1216` - `D-14 Q4b`, `src/vendor/` AND
`midiLog`, the last asserted at `:1216` "the header does not name the host's log" on the raw file,
whose only occurrence today is header line 11):
```
<!--
  The MIDI monitor: PDF page 5's collapsed bar under the surface, expanded into
  section 10's six-column log - a RENDER of the midiLog lua-host.ts already keeps
  (monitor.ts is the arithmetic; the host is read and never edited).
  Absent on the preset-backed entries, not present and empty: the vendored
  pad-sim.ts records no send, and D-14 Q4b chose Lua entries only for v1 over
  adding a log to src/vendor/. Mounted inside {#if listed.preview === "lua"} only.
  Decided at 13-10 (D-14 Q4b); see .planning/phases/13-gui-overhaul/13-10-SUMMARY.md
-->
```

The other `<!-- -->` headers over 90 lines (13.2-RESEARCH A.1 item 6): `DestinationZone.svelte:1-117`,
`DeviceDetails.svelte:1-104`, `SurfaceEditor.svelte:1-103`, `CatalogCard.svelte:1-100`,
`Clear.svelte:1-98`, `BrowseGrid.svelte:1-92`, `DeviceSlot.svelte:1-91`, `BrowseToolbar.svelte:1-90`,
`BudgetMeter.svelte:1-81`, `KeepConfirm.svelte:1-76`; the twelve `<style>` blocks over 150 lines
(`[draftId]` and `my-configs` are 06's): `SurfaceEditor` 236, `LibraryTable` 218, `Intro` 204,
`CatalogCard` 194, `BrowseToolbar` 175, `MidiMonitor` 140 — comment cleanup and blank-line grouping
only.

`app.css` (363 lines, 213 comment): the `@theme` block's eleven tokens and the `.type-*` roles are
read by `identity.spec.ts` (values, not comments) and by `instrument.spec.ts`, `intro.spec.ts`,
`radius.spec.ts`, `browse-ui.spec.ts`, `font-assets.spec.ts`, `og/render.spec.ts`; the comments go to
one line per block (the token's Bible section and D-16 / D-17 as pointers); no declaration moves.

Rules a spec enforces that the headers keep as one line each (so the next editor knows before
editing): `BrowseLink.svelte:120-125`'s and `TuningRegion.svelte:619`'s "Rule 1 in the header is the
whole reason" — the one-line rule replaces the cross-reference; the 44px floor every control
declares (`tune-ui.spec.ts`); `{#if manyKnobs}`, `{#if onresult}` (`colour-picker.spec.ts:614, :730`
— in `ColourPicker.svelte`, untouched); `{#if changed}\s*<span class="changed"` and `function
resetKnob(id: string)` (`tune-ui.spec.ts:1830, :1849` — in `Knob.svelte`, untouched); `{#snippet
clear()} <Clear /> {/snippet}` (`shell.spec.ts:954`, in `+layout.svelte`, 06's); `function
onpointerdown … onclick(at.col, at.row)` (`sandbox-ui.spec.ts:320`, `SurfaceEditor.svelte` — code, not
touched).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.2-05-01: --before 05; the tune and device components (the two pinned files first)</name>
  <files>src/lib/ui/TuningRegion.svelte, src/lib/ui/MidiMonitor.svelte, src/lib/ui/DestinationZone.svelte, src/lib/ui/DeviceActions.svelte, src/lib/ui/DeviceDetails.svelte, src/lib/ui/DeviceMark.svelte, src/lib/ui/DeviceNote.svelte, src/lib/ui/DeviceSlot.svelte, src/lib/ui/FailureBlock.svelte, src/lib/ui/KeepConfirm.svelte, src/lib/ui/Clear.svelte, src/lib/ui/SessionAnnouncer.svelte, src/lib/ui/KnobRack.svelte, src/lib/ui/MidiField.svelte, src/lib/ui/Swatch.svelte, src/lib/ui/BudgetMessage.svelte, src/lib/ui/BudgetMeter.svelte, src/lib/ui/CopyLink.svelte, src/lib/ui/StampNotice.svelte, src/lib/ui/FidelityLine.svelte, src/lib/ui/MotionControl.svelte</files>
  <action>
    1. `scripts/13.2-gate.sh --before 05` (the CSS hash at this HEAD's fresh build recorded — it is
       the term this plan is most likely to move, and it must not).
    2. `TuningRegion.svelte` and `MidiMonitor.svelte` first, to the headers prescribed in
       `<interfaces>`; then `npx vitest run --project server --maxWorkers=2 src/lib/ui/tune-ui.spec.ts
       src/lib/ui/sandbox-ui.spec.ts` — green at the four pinned titles with no body edit; a red
       means a phrase was paraphrased — put it back verbatim.
    3. Then each of the others, one at a time: (a) the `<!-- -->` block to at most ten lines — what
       it renders, its props by name, the one rule it obeys and the spec that enforces it, one
       pointer; (b) the script's leading comment and inline essays to the fact (the `$effect`
       latches keep their one line; the "measured rather than assumed" figures keep the number and
       the plan id as the pointer); (c) inside `<style>`: comments out or one line per group; no
       declaration, selector or rule order touched; (d) the template untouched — not a literal, not
       an attribute, not a `data-testid`. After each file: `npx vitest run --project server
       --maxWorkers=2 src/lib/ui src/lib/tune/colour-picker.spec.ts` green.
    4. Every three files: `node scripts/gate/hash-strings.mjs --diff gate/05.strings.json` silent
       (the census, the testids).
    5. Prettier by path (`npx prettier --write <file>`; the svelte plugin). Commit: `refactor(13.2-05):
       the tune and device components under the ten-line rule - <n> files, <before> comment lines to
       <after>; TuningRegion's and MidiMonitor's pinned decisions kept as one line each; no rule
       moved`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 src/lib/ui src/lib/tune/colour-picker.spec.ts 2>&1 | grep -E "Test Files.*passed" && node scripts/gate/hash-strings.mjs --diff .planning/phases/13.2-readability/gate/05.strings.json | grep -cE "^[-+] " | grep -qx 0</automated>
  </verify>
  <done>Twenty-one components under the rule; the four wording-pin titles green with no body edit; the ui specs green; no literal or testid moved.</done>
</task>

<task type="auto">
  <name>Task 13.2-05-02: The browse, intro, library, sandbox, shell and pad components, and app.css</name>
  <files>src/lib/ui/BrowseGrid.svelte, src/lib/ui/BrowseLink.svelte, src/lib/ui/BrowseToolbar.svelte, src/lib/ui/CatalogCard.svelte, src/lib/ui/FacetRow.svelte, src/lib/ui/TagChip.svelte, src/lib/ui/intro/HeroSurface.svelte, src/lib/ui/intro/Intro.svelte, src/lib/ui/intro/StartCard.svelte, src/lib/ui/library/LibraryTable.svelte, src/lib/ui/library/ResumeBanner.svelte, src/lib/ui/sandbox/ElementList.svelte, src/lib/ui/sandbox/Palette.svelte, src/lib/ui/sandbox/RegionInspector.svelte, src/lib/ui/sandbox/SurfaceActions.svelte, src/lib/ui/sandbox/SurfaceEditor.svelte, src/lib/ui/shell/ConnectionControl.svelte, src/lib/ui/shell/ContextBar.svelte, src/lib/ui/shell/Footer.svelte, src/lib/ui/shell/Header.svelte, src/lib/ui/shell/Inspector.svelte, src/lib/ui/shell/Nav.svelte, src/lib/ui/shell/Rail.svelte, src/lib/ui/PadCanvas.svelte, src/lib/ui/PadFrame.svelte, src/lib/ui/PadSpinner.svelte, src/lib/ui/Wordmark.svelte, src/app.css</files>
  <action>
    1. The same per-file loop as task 01 over the twenty-eight, largest header first
       (`SurfaceEditor` 103, `CatalogCard` 100, `BrowseGrid` 92, `BrowseToolbar` 90, `Intro` 70,
       `FacetRow` 65, `ContextBar` 63, `HeroSurface` 59, `PadFrame` 57, …). The
       intro's cq-unit arithmetic keeps its numbers with the plan id (`intro.spec.ts:465` reads the
       units); `PadFrame.svelte`'s header — half a diagnosis since Phase 4, closed at 10-13.2 — to the
       fact and the pointer; `BrowseGrid.svelte`'s `untrack` latch keeps its line. After each file:
       the ui specs plus `src/lib/ui/intro.spec.ts` and `browse-ui.spec.ts` (in `src/lib/ui`).
    2. `app.css`: comments to one line per block; no declaration moves; `npx vitest run --project
       server --maxWorkers=2 src/lib/ui/identity.spec.ts src/lib/ui/instrument.spec.ts
       src/lib/ui/intro.spec.ts src/lib/ui/radius.spec.ts src/lib/og/render.spec.ts
       src/lib/ui/font-assets.spec.ts src/lib/ui/browse-ui.spec.ts` green.
    3. `npm run build` then `node scripts/gate/css-terms.mjs --against
       .planning/phases/13.2-readability/gate/05.utilities.txt` — the SCOPED CSS hash equal to
       `gate/05.txt`'s, every markup-named utility (`block grid outline ring sr-only` today) still
       emitted, and the classes that left printed by name (expected: words that lived only in the
       comments this plan cut - `ordinal` from `FacetRow.svelte:48` is one). If the SCOPED hash moves:
       `diff <(git show <before-HEAD>:src/app.css) src/app.css` and the component diffs read for a
       moved declaration; the source is put right, never the scoped hash explained away - the
       utilities layer is the one layer that may move, and only by subtraction of comment words. `npx
       vitest run --project server --maxWorkers=2 src/lib/ui/radius.spec.ts` (layer B against the
       fresh build).
    4. The eleven ui specs' headers (`device-ui.spec.ts` 94, `instrument.spec.ts` 76, `identity.spec.ts`
       54, `shell.spec.ts` 42, `browse-ui.spec.ts` 37, `intro.spec.ts`, `aesthetic.spec.ts`,
       `font-assets.spec.ts`, `tune-ui.spec.ts` 23, `sandbox-ui.spec.ts` 20, `colour-picker.spec.ts`
       18) and comments under the rule; bodies untouched; `radius.spec.ts`'s header is NOT touched
       (the gate's own record of D-01 - named as the exception).
    5. Prettier by path; the whole quick suite through `94 966`; `npm run check` (655); `npm run
       lint`. Commit: `refactor(13.2-05): the browse, intro, library, sandbox, shell and pad
       components and app.css under the ten-line rule - <n> files, <before> comment lines to <after>;
       the built CSS <8 chars> unmoved; eleven ui spec headers under the rule`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966 && npm run build > /dev/null 2>&1 && node scripts/gate/css-terms.mjs --against .planning/phases/13.2-readability/gate/05.utilities.txt</automated>
  </verify>
  <done>All forty-eight components and app.css under the rule; the quick suite green at the carried pair; the scoped CSS hash equal to the plan's --before with the markup-named utilities intact.</done>
</task>

<task type="auto">
  <name>Task 13.2-05-03: --after 05, all five chunks, the record</name>
  <files>docs/CODE-STYLE.md, .planning/phases/13.2-readability/gate/, .planning/STATE.md</files>
  <action>
    1. `scripts/13.2-gate.sh --after 05 --against 05 --check 655 --js-equal` — every compared term
       equal, the SCOPED CSS hash and the NORMALISED JS hash first among them (a comments-only plan
       moves neither), the utilities delta printed; `git diff --stat 500e33c -- src/lib/ui/Knob.svelte
       src/lib/ui/ColourPicker.svelte` empty.
    2. All five chunks on fresh detached servers (`E2E_LOG_DIR=<scratchpad> bash scripts/gate/e2e-chunks.sh` whole), memory beside each,
       `test-results/` removed, every red rerun alone and named; layer C (`radius.e2e.ts` in c5)
       STRICT with `knob:thumb` in its unreachable list as before.
    3. `docs/CODE-STYLE.md` "Appended": one dated line if the components taught the rule something
       (e.g. "a $effect that holds a value across a phase keeps its one comment; a $derived cannot
       hold"); STATE by script against a copy. The SUMMARY: before / after pasted (the CSS hash
       stated twice), the per-file table (forty-eight components, app.css, eleven specs), the four
       titles' pinned phrases and the four raw prose pins with `kept` beside each, the utilities that
       left by name, the chunk lines. Commit the docs: `docs(13.2-05):
       SUMMARY with the gate's before and after, the per-file table over forty-eight components, the
       four wording-pin titles' and four prose pins' state and the utilities that left; STATE by script against a copy with status, completed_phases
       11 and percent 100 asserted`.
  </action>
  <verify>
    <automated>bash scripts/13.2-gate.sh --after 05 --against 05 --check 655 --js-equal && git diff --stat 500e33c -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte | wc -l | grep -qx 0</automated>
  </verify>
  <done>The gate's --after equals its --before on every compared term including the scoped CSS and the normalised JS; the two circle files unedited; all five chunks green with reruns named; STATE recorded.</done>
</task>

</tasks>

<verification>
- `gate/05*.txt`: every compared term equal; the scoped CSS hash, the normalised JS hash and the OG
  bytes first among them; the markup-named utilities intact and the departed ones named; the
  allowlist 0; the six circles by file and line.
- `check-counts.mjs 94 966` green; all five chunks green at 101, reruns named.
- `git diff --stat 500e33c -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte` empty; `git
  status --porcelain src/lib/ui | grep -E "^(R|A|D)"` prints nothing (no rename, add or delete).
- `grep -c "THERE IS NO ADVANCED SECTION\|NOT GENERAL UNDO\|Not a history, not a stack, not a tree\|Use actual parameter names" src/lib/ui/TuningRegion.svelte` = 4; `grep -c "D-14 Q4b\|src/vendor/\|midiLog" src/lib/ui/MidiMonitor.svelte` = 3; `grep -ci "whenever it does not" src/lib/ui/DeviceSlot.svelte src/lib/ui/shell/ConnectionControl.svelte` = 1 / 1; `grep -c writeLock src/lib/ui/DeviceDetails.svelte` greater than its comment-stripped count.
- `git diff --quiet -- .planning/ROADMAP.md` exits 0.
</verification>

<success_criteria>
- Forty-eight components and `app.css` under the rule with no rule moved and no file renamed.
- The scoped CSS and the normalised JS byte-identical; the radius gate empty; every wording pin and
  every raw prose pin kept.
- Term `+0 / +0; +0 / +0; check 655` observed; every hash equal.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-readability/13.2-05-SUMMARY.md` with the observed
count line, the gate's before and after pasted, the per-file table, the four titles' pinned phrases
with their state, the chunk lines with memory and reruns, any component carried, and the trailer
conflict. CAT-04 stays `[ ]`.
</output>
