---
phase: 13.2-readability
plan: 05
subsystem: readability
tags: [refactor, comments, components, app-css, gate]
requires:
  - 13.2-04 (the two stores under the rule; the gate at ccf9cc3)
provides:
  - forty-eight components under src/lib/ui/ (the fifty less Knob.svelte and ColourPicker.svelte) with a <!-- --> header of at most ten lines, the script's essays to the fact, the <style> comments to one line per group; no rule, selector, declaration, literal, attribute or data-testid moved
  - src/app.css under the rule with @source not inline(...) kept and every declaration where it was
  - the eleven ui specs' headers under the rule, every body byte-identical after the copyright line
  - scripts/gate/hash-strings.mjs strips HTML comments before <style> (the census counts PadSpinner.svelte's template)
affects: [13.2-06]
tech-stack:
  added: []
  patterns:
    - a .svelte template keeps the NUMBER of <!-- --> comment nodes between two siblings (Svelte emits one whitespace character per comment into the built template literal)
    - a $effect that holds a value across a phase keeps its one comment; a $derived cannot hold
    - a comment that is the only source of a utility carrying a top-level @property --tw-* keeps the word (the scoped CSS hash is not fully independent of the utility vocabulary)
key-files:
  created: []
  modified:
    - src/lib/ui/*.svelte (46), src/lib/ui/intro/*.svelte (3), src/lib/ui/library/*.svelte (2), src/lib/ui/sandbox/*.svelte (5), src/lib/ui/shell/*.svelte (7) - forty-eight components
    - src/app.css
    - src/lib/ui/{aesthetic,browse-ui,device-ui,font-assets,identity,instrument,intro,sandbox-ui,shell,tune-ui}.spec.ts, src/lib/tune/colour-picker.spec.ts
    - scripts/gate/hash-strings.mjs
    - docs/CODE-STYLE.md, .planning/phases/13.2-readability/deferred-items.md, .planning/STATE.md, .planning/phases/13.2-readability/gate/05.*, gate/05-after.*
decisions:
  - "The plan's prescribed TuningRegion header breaks 'Use actual parameter / names' across two lines; the pin (tune-ui.spec.ts:1340) reads the raw file, so the phrase sits on ONE line - the plan's own line break would have failed its own title"
  - "A utility whose only source is a comment and which carries a top-level @property --tw-* rule (ease-out in DeviceNote.svelte, sepia in PadFrame.svelte) keeps the word: the scoped CSS hash removes the two layers but not the top-level @property rules, so the word leaving moves the hash with no rule touched; the gap is named for the phase gate, not explained away"
  - "Two adjacent template comments stay two: Svelte emits one whitespace character per comment node, and merging DeviceDetails.svelte's two into one moved the normalised JS by one space in nodes/0"
  - "The census stripper is fixed rather than the word `<style>` kept in PadSpinner.svelte's header to keep the census blind: the corrected census is proved equal on a worktree at ccf9cc3, and the --after 05 record reports the term against the uncorrected baseline as it stands"
metrics:
  duration: "about 2 h 45 min on 2026-09-14 (the --before at 07:54Z, the third commit at 08:31Z, the third --after at 08:35Z, the chunks and reruns to about 08:50Z, then the record)"
  completed: 2026-09-14
---

# Phase 13.2 Plan 05: The components and app.css Summary

**One-liner:** Forty-eight components (13,790 lines with 4,731 comment lines -> 10,431 with 1,376),
`app.css` (363 lines -> 220) and eleven ui spec headers under the ten-line rule with not one rule,
selector, declaration, literal, attribute, test id or filename moved - every component compiled at
`ccf9cc3` and at HEAD with `svelte/compiler` (client and server) is line-for-line identical with
comments stripped, template whitespace included - and the gate's every term equal between
`--before 05` at `ccf9cc3` and `--after 05` at `9bb9c06` except the literal census, whose baseline
number was the instrument's (the stripper swallowed PadSpinner's template behind a `<style>`
spelled in its old header); the corrected census is equal across the plan by a worktree run, the
scoped CSS `e296d0af` and the normalised JS `da40f09f` equal under `--js-equal`, the utilities
44 -> 44, the four wording-pin titles and the four raw prose pins green with zero re-aims.

**Observed count line:** `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep by declaration /
allowlist 0`. Term **+0 / +0; e2e +0 / +0; check +0**, as projected.

## Commits

| Task | Commit    | What                                                                                                                                                                                                                                                                                                                                       |
| ---- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | `5cf5860` | the twenty-one tune and device components: 2,487 comment lines to 668; TuningRegion's and MidiMonitor's pinned decisions as one line each; DeviceDetails keeps `writeLock` in prose, DeviceSlot the `summary … whenever it does not` sentence; DeviceNote keeps `ease-out`                                                             |
| 2    | `b96cfaa` | the twenty-seven browse, intro, library, sandbox, shell and pad components (2,346 comment lines to 805), `app.css` (363 -> 220 lines), the eleven ui spec headers (bodies byte-identical after the copyright line); the scoped CSS `e296d0af` unmoved                                                                                     |
| fix  | `9bb9c06` | two things the first `--after 05` found: DeviceDetails' two template comments stay two (the merged one dropped a whitespace text node from the built JS), and `hash-strings.mjs` strips HTML comments before `<style>` (the baseline was blind to PadSpinner's seven template literals)                                                  |
| docs | (this)    | this SUMMARY; `gate/05.*` and `gate/05-after.*`; `docs/CODE-STYLE.md`'s appended line; `deferred-items.md`'s four rows; STATE.md by script                                                                                                                                                                                                 |

No co-author trailer on any commit: the harness asked for one; the repository's rule wins, and this
line reports the conflict. Nothing touched a ZONA; nothing deployed; no `gsd-tools state` command;
no `git checkout` / `restore` / `stash` / `clean` (one `git worktree add --detach` into the
scratchpad for the census proof, removed and pruned the same minute; one `git add -N .` slip during
the task 2 commit, undone by `git reset -- .` before anything was committed from it - deviation 7);
`.planning/ROADMAP.md` untouched (`git diff --quiet` exits 0); the user's dev server on 5173 left
alone (every chunk on a fresh detached 4173).

## The gate: `--before 05` at `ccf9cc3`, pasted whole

```
# 13.2 gate --before 05
HEAD ccf9cc3 (ccf9cc3761d88f7caff454e826d8528b590e61b3); dirty (src e2e scripts docs) 0; free memory 3.97 GB; 2026-09-14T07:54:01Z

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 52267 lines, 29538 code, 18983 comment, 3746 blank, 7302 header

## check and lint
check: 1789372474579 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp ccf9cc3

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 da40f09fea7badec9aa2792a39ccd25d1f161af4673f603da12526a67f640f11 (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

recorded: .planning/phases/13.2-readability/gate/05.{txt,wire.json,wire-set.json,strings.json,lines.json,vitest.json,titles.txt,utilities.txt,js.txt}
```

Every hash is 04's `--after`: wire set `a24b256f…`, `--full` `514cb2c7…`, sandbox `40b44316…`, census
`e6af80e0…`, copy exports `10fad002…`, testids `21fc9eb9…`, scoped CSS `e296d0af…` with 44 utilities,
titles `b53f2d9c…`, normalised JS `da40f09f…`.

## The gate: `--after 05 --against 05 --check 655 --js-equal` at `9bb9c06`, the third run, pasted whole

Run three times. The first (`b96cfaa`, 08:24Z) found two things and stopped at the census: the
normalised JS had moved (`da40f09f` -> `d0668c60`, one file, `nodes/0.HASH.js`) and the census had
moved (`e6af80e0` -> `6ac1cdf3`, seven `+` lines and two `-` lines, all PadSpinner.svelte's). Both
are deviations 3 and 4 below and were fixed in `9bb9c06`. The second (08:32Z) ran the quick suite
before its own build, so `radius.spec.ts` layer B refused the build from 08:26 as older than the
`9bb9c06` edit and the record read `quick exit 1` (deferred row 3). The third, with the fresh build
in place, is the record:

```
# 13.2 gate --after 05 --against 05 --check 655 --js-equal
HEAD 9bb9c06 (9bb9c067d498871947a26bc136ea4d46402119d2); dirty (src e2e scripts docs) 0; free memory 1.79 GB; 2026-09-14T08:35:11Z
before-record HEAD ccf9cc3761d88f7caff454e826d8528b590e61b3

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2728 distinct literals (6354 occurrences); 7 copy modules; 320 data-testids
literal census sha256 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3
- 2 -> 3	"img"
- 57 -> 61	"true"
+ 0 -> 1	"Connecting"
+ 0 -> 1	"pad-spinner"
+ 0 -> 1	"spinner"
+ 0 -> 1	"still still-a"
+ 0 -> 1	"still still-b"
+ 0 -> 1	"still still-c"
+ 0 -> 1	"walker"

## the comment lines
193 files; 48908 lines, 29538 code, 15624 comment, 3746 blank, 5114 header
(the forty-eight-row table is under "The per-file table" below)
48 file(s) moved

## check and lint
check: 1789374935137 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 9bb9c06

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only
UTILITIES DELTA 44 -> 44: 0 disappeared, 0 appeared
MARKUP-NAMED intact: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 da40f09fea7badec9aa2792a39ccd25d1f161af4673f603da12526a67f640f11 (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

## src/ against the before-record's HEAD ccf9cc3761d88f7caff454e826d8528b590e61b3
git diff --name-status: 60 modified, 0 added, 0 deleted, 0 renamed
git diff --stat over src/vendor, the manifest, Knob.svelte, ColourPicker.svelte: (empty)

## comparison against 05
wire: equal (set a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b, full 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b)
sandbox set: equal (40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01)
FAIL: the literal census (e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966 -> 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99; the -/+ lines above name every literal whose count moved)
```

**Term by term, the two records against each other** (the script stops at the census, so the rest
was compared by hand from the two `.txt` records, every line after its `(`-count stripped):
**wire set, `--full` and the sandbox set equal**; **copy exports and testids equal**; **the census
`e6af80e0` -> `6ac1cdf3`** - deviation 4: the baseline record's stripper removed `<style>…</style>`
before `<!-- -->`, and `PadSpinner.svelte:15`'s old header spelled `<style>` in prose (the only such
line in the tree at `ccf9cc3`, `git grep` over every `.svelte`), so the census had never counted that
component's seven template literals; with the stripper fixed, the census on this tree AND on a
`git worktree` at `ccf9cc3` (the fixed script copied in, `node_modules` junctioned, the worktree
removed after) is `6ac1cdf3` both times, and the two JSON records are equal on `files`, `literals`,
`literalOccurrences`, `copyModules`, `testidCount`, `hashes`, `census`, `copyExports` and `testids` -
only `head` differs. No literal moved; the number did. **SCOPED CSS equal `e296d0af`** with the
utilities **44 -> 44, none appeared, none disappeared** and the raw CSS equal too (`174411ee`);
**OG, fixtures, titles equal**; **name-status M only** (60 M, 0 A, 0 D, 0 R over `src/`; the
refused paths' `--stat` empty; `git diff --stat 500e33c -- Knob.svelte ColourPicker.svelte` prints
0 lines); **check 655 / 0 / 0**, lint clean, quick `94 / 966 (+1 todo)`; **the normalised JS equal
`da40f09f`** under `--js-equal` (after deviation 3's fix - the first run's `d0668c60` was one
whitespace character in `nodes/0`).

The comment lines: 18,983 -> 15,624 across the tree (-3,359: the forty-eight components' 4,731 ->
1,376 and `app.css`'s block comments, which `comment-lines.mjs` does not walk), the headers 7,302 ->
5,114; the **code lines 29,538 -> 29,538**, equal to the line.

## The per-file table

The forty-eight components (from `comment-lines.mjs --against gate/05.lines.json`; the audit header
counts the `<!-- -->` block whole, copyright and `Decided at` included, so 12-15 is the ten-line
rule plus those; `--todo` prints none of them):

| file                                      | lines before -> after | comment before -> after | header (audit) before -> after |
| ----------------------------------------- | --------------------- | ----------------------- | ------------------------------ |
| src/lib/ui/BrowseGrid.svelte              | 532 -> 359            | 249 -> 76               | 92 -> 14                       |
| src/lib/ui/BrowseLink.svelte              | 227 -> 131            | 132 -> 36               | 55 -> 14                       |
| src/lib/ui/BrowseToolbar.svelte           | 566 -> 446            | 174 -> 54               | 90 -> 15                       |
| src/lib/ui/BudgetMessage.svelte           | 223 -> 149            | 106 -> 32               | 66 -> 14                       |
| src/lib/ui/BudgetMeter.svelte             | 378 -> 222            | 193 -> 37               | 81 -> 14                       |
| src/lib/ui/CatalogCard.svelte             | 450 -> 302            | 190 -> 42               | 100 -> 14                      |
| src/lib/ui/Clear.svelte                   | 279 -> 167            | 148 -> 36               | 98 -> 14                       |
| src/lib/ui/CopyLink.svelte                | 258 -> 168            | 119 -> 29               | 62 -> 14                       |
| src/lib/ui/DestinationZone.svelte         | 576 -> 444            | 176 -> 44               | 117 -> 14                      |
| src/lib/ui/DeviceActions.svelte           | 121 -> 80             | 57 -> 16                | 50 -> 13                       |
| src/lib/ui/DeviceDetails.svelte           | 523 -> 368            | 195 -> 40               | 104 -> 14                      |
| src/lib/ui/DeviceMark.svelte              | 85 -> 56              | 43 -> 14                | 41 -> 12                       |
| src/lib/ui/DeviceNote.svelte              | 318 -> 193            | 157 -> 32               | 95 -> 14                       |
| src/lib/ui/DeviceSlot.svelte              | 392 -> 273            | 157 -> 38               | 91 -> 14                       |
| src/lib/ui/FacetRow.svelte                | 252 -> 179            | 103 -> 30               | 65 -> 14                       |
| src/lib/ui/FailureBlock.svelte            | 103 -> 73             | 48 -> 18                | 39 -> 13                       |
| src/lib/ui/FidelityLine.svelte            | 141 -> 107            | 53 -> 19                | 34 -> 13                       |
| src/lib/ui/KeepConfirm.svelte             | 320 -> 230            | 116 -> 26               | 76 -> 14                       |
| src/lib/ui/KnobRack.svelte                | 221 -> 151            | 102 -> 32               | 57 -> 14                       |
| src/lib/ui/MidiField.svelte               | 341 -> 279            | 93 -> 31                | 55 -> 14                       |
| src/lib/ui/MidiMonitor.svelte             | 381 -> 324            | 80 -> 23                | 57 -> 13                       |
| src/lib/ui/MotionControl.svelte           | 126 -> 100            | 42 -> 16                | 35 -> 13                       |
| src/lib/ui/PadCanvas.svelte               | 87 -> 65              | 45 -> 23                | 19 -> 12                       |
| src/lib/ui/PadFrame.svelte                | 191 -> 100            | 118 -> 27               | 57 -> 14                       |
| src/lib/ui/PadSpinner.svelte              | 240 -> 200            | 60 -> 20                | 42 -> 14                       |
| src/lib/ui/SessionAnnouncer.svelte        | 49 -> 26              | 36 -> 13                | 36 -> 13                       |
| src/lib/ui/StampNotice.svelte             | 102 -> 65             | 56 -> 19                | 40 -> 13                       |
| src/lib/ui/Swatch.svelte                  | 321 -> 243            | 120 -> 42               | 61 -> 14                       |
| src/lib/ui/TagChip.svelte                 | 150 -> 107            | 63 -> 20                | 53 -> 14                       |
| src/lib/ui/TuningRegion.svelte            | 972 -> 694            | 390 -> 112              | 152 -> 15                      |
| src/lib/ui/Wordmark.svelte                | 77 -> 52              | 41 -> 16                | 38 -> 13                       |
| src/lib/ui/intro/HeroSurface.svelte       | 310 -> 247            | 94 -> 31                | 59 -> 14                       |
| src/lib/ui/intro/Intro.svelte             | 408 -> 332            | 107 -> 31               | 70 -> 15                       |
| src/lib/ui/intro/StartCard.svelte         | 144 -> 121            | 41 -> 18                | 31 -> 13                       |
| src/lib/ui/library/LibraryTable.svelte    | 516 -> 483            | 74 -> 41                | 44 -> 14                       |
| src/lib/ui/library/ResumeBanner.svelte    | 169 -> 152            | 39 -> 22                | 30 -> 13                       |
| src/lib/ui/sandbox/ElementList.svelte     | 179 -> 167            | 29 -> 17                | 25 -> 13                       |
| src/lib/ui/sandbox/Palette.svelte         | 181 -> 169            | 30 -> 18                | 25 -> 13                       |
| src/lib/ui/sandbox/RegionInspector.svelte | 592 -> 555            | 65 -> 28                | 48 -> 15                       |
| src/lib/ui/sandbox/SurfaceActions.svelte  | 123 -> 93             | 48 -> 18                | 38 -> 13                       |
| src/lib/ui/sandbox/SurfaceEditor.svelte   | 1046 -> 916           | 206 -> 76               | 103 -> 15                      |
| src/lib/ui/shell/ConnectionControl.svelte | 95 -> 47              | 65 -> 17                | 49 -> 14                       |
| src/lib/ui/shell/ContextBar.svelte        | 269 -> 214            | 82 -> 27                | 63 -> 15                       |
| src/lib/ui/shell/Footer.svelte            | 217 -> 173            | 66 -> 22                | 53 -> 14                       |
| src/lib/ui/shell/Header.svelte            | 175 -> 128            | 70 -> 23                | 45 -> 14                       |
| src/lib/ui/shell/Inspector.svelte         | 194 -> 166            | 55 -> 27                | 34 -> 14                       |
| src/lib/ui/shell/Nav.svelte               | 108 -> 98             | 26 -> 16                | 22 -> 12                       |
| src/lib/ui/shell/Rail.svelte              | 283 -> 238            | 74 -> 29                | 50 -> 14                       |
| **totals, the 48**                        | 13,790 -> 10,431      | 4,731 -> 1,376          |                                |

`src/app.css`: 363 -> 220 lines, its block comments about 224 -> 81 lines; `@source not inline(…)`
kept verbatim; every `@theme` token and its per-token comment where it was; no declaration moved
(the file, comments stripped and whitespace collapsed, byte-identical to `ccf9cc3`'s).

The eleven specs (header only; every line after the copyright byte-identical, proved per file by a
sha256 over the body after the copyright line at `ccf9cc3` and at HEAD): `device-ui.spec.ts`
2,113 -> 2,031 (header 94 -> 12), `instrument.spec.ts` 911 -> 848 (76 -> 13), `identity.spec.ts`
914 -> 873 (54 -> 13), `shell.spec.ts` 976 -> 947 (42 -> 13), `browse-ui.spec.ts` 838 -> 812
(37 -> 11), `intro.spec.ts` 635 -> 617 (31 -> 13), `aesthetic.spec.ts` 270 -> 242 (40 -> 12),
`font-assets.spec.ts` 404 -> 371 (46 -> 13), `tune-ui.spec.ts` 2,031 -> 2,019 (23 -> 11),
`sandbox-ui.spec.ts` 978 -> 969 (20 -> 11), `tune/colour-picker.spec.ts` 779 -> 771 (18 -> 10).
`radius.spec.ts` not touched (the gate's own record of D-01, named as the exception).

**The proof that no code moved, per component and per commit.** Three layers. (1) Per file before
each commit: the script (whole-line and trailing `//` and block comments stripped), the markup
(HTML comments stripped) and the `<style>` block (CSS comments stripped), each whitespace-collapsed
and compared to HEAD's: `script EQUAL, markup EQUAL, style EQUAL` on every one of the 48 and
`css EQUAL` on `app.css`. (2) After the first `--after 05`: every component compiled at `ccf9cc3`
and at HEAD with `svelte/compiler` (`generate: "client"` and `"server"`, `css: "external"`), the
comments the compiler carries through stripped, compared line by line with the line's own
indentation dropped and its inner whitespace KEPT - `client js EQUAL; server js EQUAL` on 48 of 48
and the compiled CSS equal on 48 of 48, once DeviceDetails' two comments were two again. (3) The
gate's normalised JS `da40f09f` and scoped CSS `e296d0af`, equal to the baseline's. Every comment
range was replaced by line range, bottom-up, with the first and last line of each range asserted
and every line inside asserted to be a comment or blank line before the splice.

## The four wording-pin titles and the four raw prose pins, with their state

| Pin                                                                                      | Where                                  | State                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `THERE IS NO ADVANCED SECTION`, `Use actual parameter names`                              | `tune-ui.spec.ts:1340` (raw)           | **kept**, header lines 6-7 of `TuningRegion.svelte` - the phrase on ONE line (deviation 1: the plan's break failed the title)                                                                                    |
| `NOT GENERAL UNDO`, `13-16`, `Not a history, not a stack, not a tree`                    | `tune-ui.spec.ts:1901` (raw)           | **kept**, header lines 9-11                                                                                                                                                                                       |
| `D-14 Q4b`, `src/vendor/`, `midiLog`                                                     | `tune-ui.spec.ts:1097`, `:1216` (raw)  | **kept**, `MidiMonitor.svelte`'s header (`D-14 Q4b` twice - line 8 and the `Decided at` line; `midiLog` once, line 3; `src/vendor/` once, line 9)                                                                |
| `history.ts` in `TuningRegion.svelte`                                                    | `sandbox-ui.spec.ts:617` (raw)         | **kept**, header line 11 (`src/lib/sandbox/history.ts (13-16)`)                                                                                                                                                   |
| raw `TuningRegion.svelte` counts more `aria-live` than its code                          | `device-ui.spec.ts:676-683`            | **kept**: `ONE aria-live region` in the header (raw 2, code 1)                                                                                                                                                     |
| raw `DeviceDetails.svelte` counts more `writeLock` than its code                         | `device-ui.spec.ts:1170-1175`          | **kept**: `under the writeLock the install store raises while it writes` in the header and `while the session's writeLock is on` in the lock's template comment (raw 7, code 5)                                     |
| lowercased raw `DeviceSlot.svelte` matches `summary[^.]*whenever it does not`            | `device-ui.spec.ts:1640-1650`          | **kept**: `a plain button whenever a click connects (S1, S2, S6, S7) and a summary whenever it does not (S0a, S0b, S4, S5 …)` in the header                                                                          |
| lowercased raw `shell/ConnectionControl.svelte` matches the same                          | `device-ui.spec.ts:1640-1650`          | **kept**: `a plain button whenever a click connects, and a summary whenever it does not (S0a, S0b, S4, S5)` in the header                                                                                          |

Zero re-aims, zero body edits in any spec (the eleven bodies byte-identical after the copyright);
every title green: the twelve ui files 86 / 86 after every component (`npx vitest run --project
server --maxWorkers=2 src/lib/ui src/lib/tune/colour-picker.spec.ts`, layer B red only when the
build was older than the last edit, green after every rebuild), the whole quick suite `94 / 966`
three times on the final tree (the gate's, task 2's own, the second gate run's JSON at 965 + the
stale-build layer B) plus the third gate run's.

## The utilities, by name

44 -> 44 on every build of the final tree. Along the way: the first task 1 build printed
`- ease-out` (44 -> 43) with the SCOPED hash at `b191a610` - the utility's only source was
`DeviceNote.svelte:82-83`'s old prose (`160ms ease-out on opacity only`; the three `ease-out,`
declarations inside its `<style>` are not scanned - Tailwind's Svelte pre-processor skips style
blocks, which is also why `border-collapse: collapse;` in two style blocks yields nothing while a
prose `ordinal,` does), and with it went the top-level `@property --tw-ease` rule, which is outside
the two layers the scoped term removes. The word was kept in the new header where it is still the
fact (`Line changes fade 160ms ease-out on opacity`), the hash returned to `e296d0af`, and the gap is
deferred row 1. The other at-risk words - `sepia` (PadFrame's header, the only source) and `invert`
(PadFrame's header and `presets.ts:234`) - kept in PadFrame's new header on the same rule; `ordinal`
left `FacetRow.svelte` with its paragraph as the plan expected and stays a utility through
`instrument.spec.ts:643` and `:663`, which this plan's header-only spec edits did not touch. No new
utility-shaped word was introduced by any comment this plan wrote (`0 appeared` on every build).

## The `$effect` sites, for the record (comments only; no `$effect` rewritten)

The research's five, at their new lines, each with its one comment kept: `DestinationZone.svelte:100`
(`held = storeReason` unless writing; the `storeReason` JSDoc one line above names it) and `:129`
(`heldPhase`, its own JSDoc); `MidiField.svelte:62` (`seenIndex` resets the typed refusal when the
knob moves from outside; the JSDoc on `seenIndex` at `:59`); `TuningRegion.svelte:247` (the D-21
observer, under the `D-21: the grid's columns` banner and `columnsFor`'s JSDoc); `BrowseGrid.svelte:238`
with its `untrack` at `:244` (the roving index and the post-update repaint, its three inline
comments kept as one line each). A sixth of the same shape the research did not list:
`Clear.svelte:43` (`held = reason` unless writing or a pending page target; its JSDoc at `:41`). The
`landed` latch in `TuningRegion.svelte:150` (`$state(untrack(…))`) keeps its one line. None of these
becomes a `$derived`: a `$derived` cannot hold a previous value.

## The radius gate, three layers

- **A** (`radius.spec.ts` layer A, in the quick run): the allowlist EMPTY; the six circles at
  `ColourPicker.svelte:840 / :867 / :882` and `Knob.svelte:730 / :785 / :807` - the two files
  untouched (`git diff --stat 500e33c -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte`
  prints nothing; `--todo` prints exactly those two, `header 62` and `header 83`, as D-03 says).
- **B** (the built CSS, layer B on the fresh build of `9bb9c06`): green in the third gate run's quick
  suite and in task 2's own run; red exactly when the build was older than the last source edit
  (the build-staleness refusal, as designed).
- **C** (`e2e/radius.e2e.ts`, in c5): green on the twelve routes in both engines, STRICT, with
  `knob:thumb` in its unreachable list as before (c5 `11 passed`).

## The e2e chunks (fresh detached servers on 4173, `scripts/gate/e2e-chunks.sh`, `--workers 3`, build stamped `9bb9c06`)

| Chunk         | Free memory | Result                                                                                                                                                                   |
| ------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| c1            | 2.05 GB     | 13 failed / 19 passed: the wrangler server exited mid-chunk (its log ends with "Logs were written to …", 11 titles `ERR_CONNECTION_REFUSED`, the first two on a page it had stopped answering) |
| c1-rerun2     | 2.13 GB     | **32 passed** (1.2 m), the whole chunk on a fresh server; `install.e2e.ts:1957` (A.21) green on both engines                                                              |
| c2            | 2.3 GB      | 2 failed / 20 passed: `browse.e2e.ts:299` (sorting, `toEqual` on the order) and `:343` (the filtered grid, `toHaveCount` 5 received 26) - the filtered-grid hydration shape |
| c2-rerun2     | 3.78 GB     | 2 failed / 20 passed: the same two                                                                                                                                       |
| c2-alone-299  | 3.71 GB     | **1 passed** (3.2 s)                                                                                                                                                     |
| c2-alone-343  | 3.65 GB     | **1 passed** (2.9 s)                                                                                                                                                     |
| c3            | 2.3 GB      | **21 passed** (27.6 s)                                                                                                                                                   |
| c4            | 2.17 GB     | **15 passed** (20.9 s)                                                                                                                                                   |
| c5            | 2.13 GB     | **11 passed** (14.3 s)                                                                                                                                                   |

101 of 101 runs green in a chunk or alone; the reruns named. Port 4173 answers `000` after every
stop; `test-results/` removed; the playwright and wrangler logs in the scratchpad, outside the tree.
The two browse titles are the race every 13.2 plan has named (`browse:299 / :343 / :1186 / :1404`,
green alone on a fresh server) - here red twice in a chunk with 2.3 and 3.78 GB free, so memory is
not the whole story; the shape (the grid counted before the filter hydrated) is unchanged and is
not this plan's: `BrowseGrid.svelte` and `BrowseToolbar.svelte` compile identically to `ccf9cc3`'s.
The c1 server death is the "dead server" shape 13.2-03 saw in its c3; the whole chunk rerun is the
answer.

## Deviations from Plan

**1. The prescribed `TuningRegion.svelte` header's line break.** The plan's text breaks `"Use actual
parameter` / `names, limits, …` across two lines; `tune-ui.spec.ts:1340` reads the raw file for the
phrase `Use actual parameter names` and went red on it at the first run. The phrase sits on one line
now (`… says "Use actual parameter names,` / `limits, units, …`), the rest of the prescribed text
verbatim; 13.2-03's appended CODE-STYLE line already says a raw-pinned phrase sits on ONE line.

**2. [Rule 3 - blocking] `DeviceNote.svelte` keeps `ease-out`; `PadFrame.svelte` keeps `sepia` and
`invert`.** The first build after task 1's first twelve files moved the SCOPED CSS hash to
`b191a610` with `- ease-out` printed: the utility's only source was the cut header prose, and its
top-level `@property --tw-ease` rule left the stylesheet with it - outside the two layers the scoped
term removes. The plan's instruction was to put the source right, never explain the hash away; the
word is a fact about a declaration in that file and was kept in the ten lines. The scoping gap is
deferred row 1 and CODE-STYLE's appended line.

**3. [Rule 1 - bug, found by `--after`] Two adjacent template comments in `DeviceDetails.svelte` stay
two.** Task 1 merged the `SAFE_PROMISE` note and the lock comment (`:342-354` at `ccf9cc3`) into one
`<!-- -->`; Svelte emits one whitespace character per comment node into the built template literal,
so `<!> <!>  <button` became `<!> <!> <button` in `nodes/0.HASH.js` and the normalised JS moved
(`da40f09f` -> `d0668c60`, one file of 71). Found by compiling all 48 components at `ccf9cc3` and
at HEAD with `svelte/compiler` and comparing line by line with inner whitespace kept: the one line
that differed. Split back into two one-line comments in `9bb9c06`; the JS is `da40f09f` again. In
the DOM the two strings render the same (collapsed whitespace); the gate is byte-strict and right
to be.

**4. [Rule 1 - bug in the gate, found by `--after`] `hash-strings.mjs` strips HTML comments before
`<style>`.** The `.svelte` branch removed `<style>…</style>` and then `<!-- -->`; the old
`PadSpinner.svelte:15` (`a Svelte component's <style> block is static CSS`) made that regex match
from inside the header comment through the real `</style>`, deleting the whole template, so the
census had never counted `Connecting`, `pad-spinner`, `spinner`, `walker`, the three `still` classes,
one `img` and four `true` - since 13.2-01's baseline. Fixed in `9bb9c06`; the corrected census is
`6ac1cdf3` (2,728 / 6,354) on this tree and on a worktree at `ccf9cc3`, every literal and count
equal. The `--before 05` record is not rewritten (D-09), so `--after 05` reports `FAIL: the literal
census` against it by construction, and this plan's verify line (`--after … --js-equal &&` the
empty `--stat`) exits 1 on that term alone; 13.2-06's phase gate against `gate/01.*` will print the
same seven `+` and two `-` lines (deferred row 2). The alternative - keeping the word `<style>` in
PadSpinner's header so the census stayed blind - was refused.

**5. Pointers by SUMMARY, not by line; `Decided at` lines that name two or three plans.** Where a
component's decisions span plans (`13-09 / 13-10 / 13-16`, `06-12 / 10-03`, `13-05 / 13.1-05`) the
one provenance line names them all and points at the latest SUMMARY; `comment-lines.mjs` counts one
`Decided at` line regardless of how many ids it carries.

**6. The gate's second `--after 05` ran the quick suite against a stale build** (the script builds
after the quick suite), so `radius.spec.ts` layer B refused and the record read `quick exit 1`; the
third run, with the fresh build in place, is the record. Deferred row 3.

**7. A `git add -N .` slip** in the task 2 commit line marked the untracked files intent-to-add
(`.claude/launch.json`, the three bible files at the root, the `gate/05.*` records); `git reset -- .`
undid it the same minute, the commit `b96cfaa` holds exactly its 39 `src/` files, and no working
file was touched. Named in deferred-items so the reflog reads plainly.

**8. Two comment mentions the plan did not list**, reworded with the rest: `TuningRegion.svelte:191`
and `:247` ("see rule 1 in the header") now say the rule in place (`config-shape.spec.ts walks
src/lib/ui/ for a static-import specifier naming the compiler`), as the plan asked for `:619` and
`BrowseLink.svelte:120-125`.

## Things the plan asserts that the tree does not support

- "the SCOPED built-CSS hash … is the baseline's … (Vite strips `<style>` comments)" - true of
  `<style>` comments and false of a header comment that is the only source of a utility carrying a
  `@property --tw-*` rule: those rules sit at the top level of the stylesheet, outside both layers
  `css-terms.mjs` removes, and follow the utility set (deviation 2, deferred row 1).
- "the census … the baseline's" - the baseline census was blind to one component's template
  (deviation 4); the corrected census is equal, the recorded hash is not.
- "the normalised built JS is equal (comments only)" - true only while the count of `<!-- -->`
  comment nodes between two template siblings is kept (deviation 3); the plan's "comments only" did
  not say that, and CODE-STYLE now does.
- `grep -c "THERE IS NO ADVANCED SECTION\|NOT GENERAL UNDO\|Not a history…\|Use actual parameter
  names" TuningRegion.svelte = 4`: `grep -c` counts LINES, and two of the four phrases share a line
  each with another (`THERE IS NO ADVANCED SECTION … "Use actual parameter names,` on one line,
  `… NOT GENERAL UNDO:` on its own, `Not a history …` on its own) - the line count is 3, the
  occurrence count (`grep -o | wc -l`) is 4. `MidiMonitor.svelte`'s `D-14 Q4b\|src/vendor/\|midiLog`
  is 4 occurrences on 4 lines (`D-14 Q4b` twice, once in the `Decided at` line), not 3.
- "the twelve `<style>` blocks over 150 lines … comment cleanup and blank-line grouping only" - no
  blank line was added or removed inside any `<style>`; prettier reflowed no rule (the compiled CSS
  of all 48 is equal to `ccf9cc3`'s), so there was no reflow to revert by hand.
- "`hash-strings --diff gate/05.strings.json` silent every three files" - silent through task 1 and
  task 2 (the `--diff` printed nothing after each batch), because the defective stripper was blind
  the same way on both sides until PadSpinner's header was rewritten in task 2's last batch; the
  first `--after` was where the seven literals surfaced.
- The plan's task 2 step 3 names `ordinal` from `FacetRow.svelte:48` as a word expected to leave the
  utilities: it left the file and the utility stayed, because `instrument.spec.ts:643` still spells it
  (13.2-03's SUMMARY said the same).

## Known Stubs

None. No string, export, testid, title or filename moved; the copy-exports and testid hashes are
the baseline's; the census is equal once measured with the corrected stripper.

## Resume line (D-17)

`node scripts/gate/comment-lines.mjs --todo src/lib/ui/**/*.svelte src/app.css` prints exactly
`src/lib/ui/ColourPicker.svelte header 62` and `src/lib/ui/Knob.svelte header 83` - the two files
D-03 excludes - and nothing else (`app.css` measures a header of 0: its first line is the
`@import`; its block comments are at the rule by inspection, 220 lines). The plan is landed; nothing is carried. 13.2-06 (the routes,
the sim, the transport, the model and the view, then the phase gate) runs `bash scripts/13.2-gate.sh
--before 06` next and has deferred-items rows 1-3 to read first: the census baseline, the scoped
CSS's `@property` gap, the gate's quick-before-build order.

## Self-Check: PASSED

The three source commits `5cf5860`, `b96cfaa`, `9bb9c06` are in the log; `gate/05.*` (nine files)
and `gate/05-after.*` (nine files) exist; `--after 05` printed every term and stopped at the census
as this SUMMARY says; `npx prettier --check` passes on every file this plan touched; `git status
--porcelain src/lib/ui | grep -E "^(R|A|D)"` prints nothing; `.planning/ROADMAP.md` is untouched;
STATE.md's `status: executing`, `completed_phases: 11`, `percent: 100` asserted equal before and
after the script (one line added - the retained previous stop); CAT-04 stays `[ ]`.
