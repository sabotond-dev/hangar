---
phase: 13.2-readability
plan: 06
type: execute
wave: 6
depends_on: ["13.2-05"]
# Execution order (13.2-VALIDATION "Why the phase is serial"): SERIAL - 01 -> 02 -> 03 -> 04 -> 05 -> 06.
# SIXTH AND LAST: the routes, the sim, the transport, the model, the view and the editor - comments only -
# then the phase gate: every hash from 01's --before equal at HEAD, all five chunks, the sweep,
# TESTING.md's section, and a checkpoint that asks the user to read three files.
files_modified:
  - src/routes/+layout.svelte
  - src/routes/+layout.ts
  - src/routes/+page.svelte
  - src/routes/+page.ts
  - src/routes/playground/+page.svelte
  - src/routes/playground/+page.ts
  - src/routes/playground/[id]/+page.svelte
  - src/routes/playground/[id]/+page.ts
  - src/routes/sandbox/+page.svelte
  - src/routes/sandbox/+page.ts
  - src/routes/sandbox/[draftId]/+page.svelte
  - src/routes/sandbox/[draftId]/+page.ts
  - src/routes/my-configs/+page.svelte
  - src/routes/my-configs/+page.ts
  - src/routes/dev/catalog/+page.svelte
  - src/routes/dev/fidelity/+page.svelte
  - src/routes/dev/install/+page.svelte
  - src/routes/dev/session/+page.svelte
  - src/routes/dev/skeleton/+page.svelte
  - src/routes/dev/tune/+page.svelte
  - src/routes/dev/type/+page.svelte
  - src/lib/sim/demo.ts
  - src/lib/sim/engine.ts
  - src/lib/sim/host.ts
  - src/lib/sim/lua-host.ts
  - src/lib/sim/lua-pad-sim.ts
  - src/lib/sim/monitor.ts
  - src/lib/sim/motion.svelte.ts
  - src/lib/sim/paint.ts
  - src/lib/sim/ready.ts
  - src/lib/sim/schedule.ts
  - src/lib/sim/touch.ts
  - src/lib/transport/capture.ts
  - src/lib/transport/fake.ts
  - src/lib/transport/fixtures/synthetic.ts
  - src/lib/transport/index.ts
  - src/lib/transport/ports.ts
  - src/lib/transport/queue.ts
  - src/lib/transport/sequence.ts
  - src/lib/transport/transport.ts
  - src/lib/transport/web-serial.ts
  - src/lib/tune/model.ts
  - src/lib/tune/view.ts
  - src/lib/sandbox/editor.ts
  - src/lib/sim/demo.spec.ts
  - src/lib/sim/host.spec.ts
  - src/lib/sim/lazy.spec.ts
  - src/lib/sim/lua-host.spec.ts
  - src/lib/sim/lua-smoke.spec.ts
  - src/lib/sim/paint.spec.ts
  - src/lib/sim/schedule.spec.ts
  - src/lib/sim/touch.spec.ts
  - src/lib/transport/capture.spec.ts
  - src/lib/transport/fake.spec.ts
  - src/lib/transport/fixtures/fixtures.spec.ts
  - src/lib/transport/fixtures/synthetic.spec.ts
  - src/lib/transport/queue.spec.ts
  - src/lib/transport/sequence.spec.ts
  - src/lib/transport/transport.spec.ts
  - src/lib/tune/model.spec.ts
  - src/lib/tune/view.spec.ts
  - src/lib/tune/ladder.spec.ts
  - src/lib/tune/reachability.sweep.spec.ts
  - src/lib/share/stamp-roundtrip.sweep.spec.ts
  - docs/TESTING.md
  - docs/CODE-STYLE.md
  - .planning/phases/13.2-readability/gate/
  - .planning/STATE.md
autonomous: false
requirements: [] # none - a refactor claims nothing; the gate re-proves what the tree already ticks; CAT-04 stays [ ]

must_haves:
  truths:
    - "The seven routes' headers, the sim, the transport, buildTuner's file, the view and the editor are under the rule with no code moved: buildTuner is still one function; runRequest and #snapshot are whole; synthetic.ts's fixture is byte-identical; every route's import shape is what config-shape.spec.ts scans."
    - "At the phase gate every hash 13.2-01's --before recorded is equal at HEAD: the wire set and --full, the literal census, the copy exports, the testids, the sorted test titles, the three fidelity fixtures and frames.json, the OG bytes, the SCOPED built CSS (the two Tailwind layers removed) with every markup-named utility still emitted and the departed utilities listed by name; git diff --name-status 500e33c -- src shows one A and no R or D; check reads 655 = 654 + 1 (src/test-support/source.ts) and nothing else; the quick suite 94 / 966 (+1 todo); the sweep 4 19; all five chunks green at 86 titles / 101 runs; the allowlist 0 and the six circles by file and line with both files unedited across the phase."
    - "docs/TESTING.md carries a Phase 13.2 section inserted whole: the six-term chain with the term count asserted at six on every row, the per-file before / after comment-line table for the whole phase, every hash before and after, the runs with memory, where the planner was wrong, the gate holes."
    - "The user has read three files of their choosing beside their 500e33c form and said whether they read better; no requirement row moved; CAT-04 [ ]."
  artifacts:
    - path: "docs/TESTING.md"
      provides: "A Phase 13.2 section, inserted whole before the vendored-tree section, zero deletions"
      contains: "Phase 13.2"
    - path: ".planning/phases/13.2-readability/gate/06-phase.txt"
      provides: "The whole-phase --after against 01's --before, every term equal"
  key_links:
    - from: "scripts/13.2-gate.sh"
      to: ".planning/phases/13.2-readability/gate/01.wire.json"
      via: "--after 06-phase --against 01"
      pattern: "against"
---

<objective>
The last of the source: the seven routes (three of them over 980 lines, functions all under 50),
`sim/host.ts` and `lua-host.ts` (the firmware-fidelity record in their bodies), the transport's
state machines that read better whole, `buildTuner`'s 644 lines that stay 644, the view's four
concerns that stay in one file, the editor whose shape is already the best in the tree — comments
only, every one. Then the gate: the phase measured against its own first record, the suite whole,
the section in TESTING.md, and three files read by the user.

Purpose: a refactor claims nothing and must prove it — the gate's `--against 01` is the phase's one
sentence: every byte a visitor, a module or a test can see is where it was.
Output: about forty-five modules and sixteen spec headers under the rule; `gate/06-phase.txt`;
TESTING.md's section; STATE; the checkpoint. Term **+0 / +0; e2e +0 / +0; check 655 (+0)**; the sweep
run; all five chunks.

**Execution order.** SIXTH AND LAST, after 13.2-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-readability/13.2-CONTEXT.md
@.planning/phases/13.2-readability/13.2-VALIDATION.md
@.planning/phases/13.2-readability/13.2-01-SUMMARY.md
@.planning/phases/13.2-readability/13.2-02-SUMMARY.md
@.planning/phases/13.2-readability/13.2-03-SUMMARY.md
@.planning/phases/13.2-readability/13.2-04-SUMMARY.md
@.planning/phases/13.2-readability/13.2-05-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-08-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/deferred-items.md
@docs/TESTING.md
@docs/CODE-STYLE.md
@src/lib/tune/model.ts
@src/lib/sim/lua-host.ts
@src/lib/transport/queue.ts
@src/lib/transport/fixtures/synthetic.ts
@src/routes/playground/[id]/+page.svelte
@src/routes/+page.svelte
@src/lib/config-shape.spec.ts
@src/lib/ui/intro.spec.ts
@src/lib/tune/model.spec.ts
@src/lib/tune/view.spec.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution and no co-author trailer (the
  repository's rule wins; the SUMMARY reports the conflict). No emojis.
- **No device, no deploy; sibling repositories read-only; the dev server on 5173 left alone.**
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state`.** STATE.md by script against a copy, asserting `status: executing`,
  `completed_phases: 11`, `percent: 100` unchanged; Phase 13.2 recorded as 6 of 6 landed, gate
  landed, the checkpoint's answer recorded when given. `.planning/ROADMAP.md` is NOT edited (the row
  for whoever updates it goes in this SUMMARY). `requirements mark-complete` is not run;
  `.planning/REQUIREMENTS.md` is not edited — a refactor claims nothing; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on every file this plan touches, by path;
  `docs/TESTING.md` is written prettier-clean and `prettier --check docs/TESTING.md` passes — if
  `--write` would touch a line outside the new section, that is a reflow of an append-only file and
  the section is rewritten instead.
- **`git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md` (a section inserted whole), `docs/INSTALL-RUNBOOK.md`,
  `docs/HARDWARE-AUDITION.md`, `.planning/REQUIREMENTS.md` (the last three not this plan's).
- **Comments only in every file of this plan.** `buildTuner` stays one function (`model.spec.ts` and
  `wire-pin.spec.ts` await a fixed microtask chain; `model.spec.ts:394` "needsLadder is the only door
  to the ladder"); `runRequest`, `#snapshot`'s kin in the transport and `zonaResponder` stay whole;
  `synthetic.ts` leaves `synthetic-zona.json` byte-identical (`fixtures.spec.ts`,
  `synthetic.spec.ts:286`); `view.ts` keeps "imports nothing at all" (`view.spec.ts:450`) and its
  `EVENT_BUDGET = 908` restatement with one line saying why; no route gains or loses an import
  (`config-shape.spec.ts:251` on the skeleton page; `:267`, `:333` on the front door); **no route
  comment names a probe directory outside that probe** - `config-shape.spec.ts:304-320` scans every
  file under `src/routes/` RAW for `dev/catalog`, `dev/fidelity`, `dev/install`, `dev/session`,
  `dev/skeleton`, `dev/tune`, `dev/type`, and only a probe's own directory is exempt, so a workspace
  header that says "see /dev/install/" is red (write "the install probe" instead);
  `routes/+page.svelte` keeps `try { return window.localStorage;` inline (`intro.spec.ts:345`), and
  the other six `localStorage` guards stay where they are with one line each (13.2-CONTEXT D-15);
  `{#snippet clear()} <Clear /> {/snippet}` in `+layout.svelte` (`shell.spec.ts:954`); the Sandbox
  header's box arithmetic in `[draftId]/+page.svelte` is not moved (13.1 deferred-items row 12);
  `lazy.spec.ts:106/109`'s dynamic-import spelling in `engine.ts`; `ready.ts` is the one module that
  names the VM package.
- **The refuse-list, absolute**: `src/vendor/`, the manifest, the fixtures, the OG images, the two
  circle files, every persisted key, every literal and `data-testid` (`my-configs/+page.svelte`'s
  forty inline strings stay inline — D-05 / D.7), every title, every filename.
- **Every count a carried name plus a delta**: `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4
  19 / allowlist 0`. Term **+0 / +0; +0 / +0; check +0**. `check-counts.mjs 94 966`; `4 19`.
- **The gate brackets the plan and then the phase**: `--before 06` first; `--after 06 --against 06
  --check 655` after the last source commit; then `--after 06-phase --against 01 --check 655` — the
  whole phase against 13.2-01's `--before`.
- **Quick at `--maxWorkers=2`.** All five chunks on fresh detached servers (`scripts/gate/e2e-chunks.sh`, `E2E_LOG_DIR` the scratchpad),
  the free memory beside each, `test-results/` removed after, every red rerun alone and named; a
  rerun red again reruns its whole chunk once on a fresh server, and a red that survives that is a
  finding, not a transient — named as such.
- **Re-runnable (D-17)**: the remaining list is `node scripts/gate/comment-lines.mjs --todo <this
  plan's routes, sim, transport, model, view, editor>`; the gate tasks run only when it prints nothing.
</standing_rules>

<interfaces>
The big files by line (13.2-RESEARCH A.9, Appendix 1): `my-configs/+page.svelte` 1,361 (82 header,
~730 script, 200 template, 314 style), `sandbox/[draftId]/+page.svelte` 1,177 (96 / ~520 / 245 /
316), `playground/[id]/+page.svelte` 984 (120 / ~480 / 220 / 167), `playground/+page.svelte` 618
(79), `+layout.svelte` 431 (0 header, 171 comment), `dev/skeleton/+page.svelte` 679 (22 header;
`open` at 142 lines is a dev probe), `dev/install/+page.svelte` 422 (69), `dev/tune/+page.svelte` 242
(100), `dev/session/+page.svelte` 208 (69), `dev/type/+page.svelte` 157 (70); `tune/model.ts` 1,168
(58 header; `buildTuner` 525-1168, its first thirty lines `:528-556` are three paragraphs on why the
library import is dynamic — to one line: "Read once here so `land()` stays synchronous" and a pointer
to 12.1-08b); `sim/lua-host.ts` 1,058 (22 header; 477 comment lines in the body — the
firmware-fidelity record: `f2i`, the FIFO depth, the tap coalescing; trim to the fact with a
`docs/` pointer where one exists, keep every firmware `file:line` citation); `sim/host.ts` 785 (112
header); `sandbox/editor.ts` 990 (103 header; the best-shaped large file - header cut only);
`tune/view.ts` 862 (43 header, 403 comment, 54 exports - the over-exports `DOT_RAIL_MAX`,
`DIRECTION_WORDS`, `MODE_WORDS`, `BEND_WORDS`, `SPRING_WORDS`, `NOTE_NAMES`, `HUE_NAMES` lose their
`export` keyword under D-11 — `npm run check` proves nothing imports them; `colour-picker.spec.ts:103`
reads the lattice arithmetic by text and is run after); `transport/sequence.ts` 653 (23 header, 250
comment), `transport/fixtures/synthetic.ts` 647 (`pagediscardAckFrame`, `pageCountReportFrame` lose
`export`; the file's comments only otherwise), `transport/queue.ts` 390 (15 header), `sim/demo.ts`
391 (66 header, 255 comment), `sim/monitor.ts` 256 (54), `sim/motion.svelte.ts` 159 (50),
`sim/paint.ts` 80 (41), `sim/ready.ts` 72 (35), `sim/engine.ts` 137 (40), `sim/schedule.ts`,
`sim/touch.ts`, `sim/lua-pad-sim.ts` 231 (`:74-83`'s `renderLua` paragraph to "Plain-string
`replaceAll`: a token is data, not a pattern"; `:211`'s `landLua` ghost reworded to `measureLuaRoute`
+ `land`), `transport/transport.ts` 200 (`failureCopy` is a table and stays; `transport.spec.ts:106`
scans it for "Chromium" as a negative and for "the browsers that work" - the strings, not the
comments), `transport/capture.ts`, `fake.ts`, `web-serial.ts`, `ports.ts`, `index.ts`.

The ghost names in this plan's files: `lua-pad-sim.ts:211 landLua`, `model.ts:637 askForecast` (the
forecast's caller is gone since 13.1-07 - deferred-items A.7; the comment names what exists),
`model.ts:434 SWATCH_ROW_MAX` (a retirement note - the fact is that the swatch rows went with 13-09;
one line), `browse/query.ts:25 update_url` and `[draftId]/+page.ts:12 handleUnseenRoutes` are Kit's
and stay.

The routes' `<!-- -->` headers say what the page is, which stores it mounts, which snippets it hands
to the shell, and the one rule each obeys (the front door never reaches the compiler at module scope
- `config-shape.spec.ts:333`; the skeleton imports the protocol and transport surfaces and nothing
else - `:251`; the workspace mounts `MidiMonitor` inside `{#if listed.preview === "lua"}` only). The
"measured rather than assumed (05.1-09: …)" lines keep the number and the plan id.

The TESTING.md section, inserted whole before `## Why the vendored tree is excluded from
type-checking but not from the test run` (where 13's and 13.1's sections are), headed `## Phase
13.2's suites, measured at the gate` with these subsections in this order: the block re-measured
against the round-4c line (`94 / 966 (+1 todo) / 86 / 101 / check 654 / sweep 4 19 / allowlist 0`);
**the six-term chain, every zero written out, the term count asserted at six on every row** (files,
tests, e2e titles, e2e runs, check with its one +1 named, allowlist, circles, catalog, sweep); the
hashes before and after — the wire set and `--full` and the sandbox set, the census / exports /
testids, the four fixture hashes, the OG count and bytes, the built CSS — each as 01's `--before`
figure and the gate's figure side by side; the per-file before / after comment-line table for the
whole phase (`comment-lines.mjs --against gate/01.lines.json`, every file whose numbers moved, plus
totals: the research's 27,480 comment / 14,072 header against the gate's); the docs created
(`docs/CODE-STYLE.md`, the twenty under `docs/entries/`, the six harness files, the gate script) and
the one source file (`src/test-support/source.ts`); the spec bodies edited across the phase, by
title, and the re-aims (expected zero beyond 04's eleven `storedThisSession` lines and the
`stripComments` swap) - stated as observed; the two circle files and `src/vendor/` unedited across
the phase (`git diff --stat 500e33c -- …` empty); the runs with the memory beside each and every
rerun named; **"Where this phase's planner was wrong"** — every figure 13.2-VALIDATION or a PLAN
stated that a SUMMARY observed differently (the 33-not-35 strips, the six-not-ten deletions, the
ten-not-three spec lines, the CSS hash of a build stamped at a different commit, whatever the plans
found); the gate holes, as observed (13.2-PLAN-CHECK H-1, H-4, H-5, H-6): the Tailwind utilities
layer is a function of the comment vocabulary (`app.css:1` `source(".")`), so the CSS term is the
scoped hash plus the utilities list with its markup-named subset, and the classes that left across
the phase are listed by name; test titles are held by the sorted-titles hash from 01 on and by count
before it; the normalised built JS was asserted equal on the two comments-only plans (03, 05) and
recorded, not asserted, on the four that drop `export`s or rename; the census does not see backtick
literals in markup (six today, three user-facing in `RegionInspector.svelte`), text nodes that mix
prose with `{expr}` (thirteen, e.g. `BrowseToolbar.svelte`'s `Showing {showing} of {total}
configurations.`) or the four `data-testid={expr}` attributes - those are rule-only ("the template
untouched") and named as such; `PadUserCode` is uncovered and unused; the stamp codec is the sweep's;
the requirement rows: none moved, none claimed, CAT-04 `[ ]` a ninth time; the ROADMAP row for
whoever updates it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.2-06-01: --before 06; the routes, the model, the view and the editor</name>
  <files>the twenty-one route files, src/lib/tune/model.ts, src/lib/tune/view.ts, src/lib/sandbox/editor.ts, src/lib/tune/model.spec.ts, src/lib/tune/view.spec.ts, src/lib/tune/ladder.spec.ts, src/lib/tune/reachability.sweep.spec.ts</files>
  <action>
    1. `scripts/13.2-gate.sh --before 06`.
    2. The routes, largest first: the `<!-- -->` header to ten lines per `<interfaces>`; the script's
       essays to facts; `<style>` comments out with no rule moved; the template untouched; the
       `localStorage` guard's comment to one line naming D-15 and, in `routes/+page.svelte`,
       `intro.spec.ts:345`. `dev/skeleton/+page.svelte`'s `open` (142 lines) is a probe and stays
       whole. After each route: `npx vitest run --project server --maxWorkers=2
       src/lib/config-shape.spec.ts src/lib/ui/intro.spec.ts src/lib/ui/device-ui.spec.ts
       src/lib/ui/tune-ui.spec.ts src/lib/ui/sandbox-ui.spec.ts src/lib/ui/browse-ui.spec.ts
       src/lib/ui/shell.spec.ts` green; `hash-strings --diff gate/06.strings.json` silent.
    3. `tune/model.ts`: header to ten lines; `buildTuner`'s opening paragraphs to one line and a
       pointer; every inner function's essay to the fact; the two ghost names; no code moved.
       `npx vitest run --project server --maxWorkers=2 src/lib/tune src/lib/device/wire-pin.spec.ts`
       green.
    4. `tune/view.ts`: header to ten lines with "imports nothing at all" and why as one line; the
       seven over-exports demoted; comments to facts; `view.spec.ts` and `colour-picker.spec.ts`
       green; `npm run check` (655).
    5. `sandbox/editor.ts`: header cut only (103 → ten); `sandbox-ui.spec.ts` and `geometry.spec.ts`
       green.
    6. The four specs' headers under the rule (`model.spec.ts` 20, `view.spec.ts`, `ladder.spec.ts`,
       `reachability.sweep.spec.ts`); bodies untouched.
    7. Prettier by path; the quick suite through `94 966`. Commit: `refactor(13.2-06): the seven
       routes' headers to ten lines, buildTuner's file, the view and the editor under the rule with
       no code moved - <n> files, <before> comment lines to <after>; seven view.ts over-exports
       demoted; the localStorage guards stay by D-15`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966 && npm run check 2>&1 | tail -1 | grep -E "655 files, 0 errors, 0 warnings"</automated>
  </verify>
  <done>The routes, the model, the view and the editor under the rule; the quick suite green at the carried pair; check 655; no import shape moved.</done>
</task>

<task type="auto">
  <name>Task 13.2-06-02: The sim and the transport</name>
  <files>the eleven src/lib/sim/*.ts, the nine src/lib/transport/**/*.ts, the fifteen sim and transport specs, src/lib/share/stamp-roundtrip.sweep.spec.ts</files>
  <action>
    1. `sim/lua-host.ts` first (the largest body of comment): every fidelity paragraph to the fact
       and the firmware citation, a `docs/` pointer where one exists (`CALIBRATION-PROBE.md`,
       `MIDI-IN-PROBE.md`); `registerGlobals` stays whole (one binding per block; the golden frames
       are the proof). Then `sim/host.ts` (header 112 → ten; the shared rAF loop's rules - one
       loop, the 10 ms accumulator with `MAX_CATCHUP_MS`, `RENDER_INTERVAL_MS`, the
       IntersectionObserver gate, the self-cancelling loop, reduced motion to tick 64 - as one line
       each: they are the file's contract), `demo.ts`, `monitor.ts`, `motion.svelte.ts`,
       `lua-pad-sim.ts` (the two named sites), `engine.ts` (the dynamic-import spelling untouched),
       `ready.ts` (the VM package named once), `paint.ts`, `schedule.ts`, `touch.ts`. After each:
       `npx vitest run --project server --maxWorkers=2 src/lib/sim src/lib/fidelity` green (the
       golden frames, the parity, the smoke).
    2. The transport: `sequence.ts` (23 header, 250 comment — the SLOTS order and its three reasons
       stay as the list they are; the rest to facts), `queue.ts` (`runRequest` whole), `transport.ts`
       (`failureCopy` untouched), `synthetic.ts` (comments; the two over-exports demoted; then
       `npx vitest run --project server --maxWorkers=2 src/lib/transport` and `git status
       --porcelain src/lib/transport/fixtures/synthetic-zona.json` empty), `capture.ts`, `fake.ts`,
       `web-serial.ts`, `ports.ts`, `index.ts`.
    3. The fifteen sim and transport specs' headers (`lua-smoke.spec.ts` 66, `host.spec.ts` 16,
       `lua-host.spec.ts`, …) and `stamp-roundtrip.sweep.spec.ts`'s under the rule; bodies untouched.
    4. Prettier by path; `npm run check` (655); `npm run lint`; the quick suite through `94 966`;
       `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19`. Commit: `refactor(13.2-06):
       the sim and the transport under the rule - the fidelity record to facts with their firmware
       citations, the state machines whole, synthetic-zona.json byte-identical; <n> files, <before>
       comment lines to <after>; sweep 4 19`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966 && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && git status --porcelain src/lib/transport/fixtures/ src/lib/fidelity/ src/lib/catalog/frames.json | wc -l | grep -qx 0</automated>
  </verify>
  <done>The sim and the transport under the rule with every fixture byte-identical; the quick suite and the sweep at the carried counts.</done>
</task>

<task type="auto">
  <name>Task 13.2-06-03: The phase gate - the plan's --after, the phase's --after against 01, all five chunks, TESTING.md's section, STATE</name>
  <files>docs/TESTING.md, docs/CODE-STYLE.md, .planning/phases/13.2-readability/gate/, .planning/STATE.md</files>
  <action>
    1. On a clean tree after task 02's commit: `scripts/13.2-gate.sh --after 06 --against 06 --check
       655`, then `scripts/13.2-gate.sh --after 06-phase --against 01 --check 655` — the whole phase
       against 13.2-01's `--before`: the wire set `a24b256f…` and `--full` `514cb2c7…` (the sandbox
       set against 02's), the census `e6af80e0…`, the copy exports `10fad002…`, the testids
       `21fc9eb9…`, the sorted titles, the fixtures, the OG 26 at 154,136 B, the SCOPED CSS equal to
       01's fresh-build figure with the markup-named utilities intact and the whole phase's departed
       utilities printed by name (the raw CSS hash and the normalised JS hash recorded beside them,
       moved, and said so); `git diff --stat 500e33c -- src/vendor src/lib/fidelity/upstream-manifest.json
       src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte` empty; `git diff --name-status 500e33c
       -- src | grep -E "^(R|A|D)"` shows exactly one `A` (`src/test-support/source.ts`) and nothing
       else. The quick suite three times at `--maxWorkers=2` through `94 966` and a fourth
       for the per-file JSON; `npm run check` 655 / 0 / 0; `npm run lint`; the sweep `4 19`; the
       memory beside each.
    2. The radius gate, three layers: layer A in the quick run (`ALLOWLIST` `[]`, the six circles
       named by file and line, both files unedited); layer B against the fresh build; layer C in c5.
    3. All five chunks on fresh detached servers (`scripts/gate/e2e-chunks.sh` whole, `E2E_LOG_DIR`
       set to the scratchpad), memory beside each,
       `test-results/` removed after; every red rerun alone and named; a rerun red again reruns its
       chunk once; expected `32 + 22 + 21 + 15 + 11 = 101` (state the observed sums). The build is
       the one from step 1 (stamped at task 02's commit; no commit between).
    4. `docs/TESTING.md`: the section per `<interfaces>`, inserted whole, zero deletions, prettier-clean;
       `node -e` asserting every line of the file before the insertion survives as a prefix (13-20's
       shape). `docs/CODE-STYLE.md` "Appended": one dated line pointing at the TESTING.md section.
    5. STATE by script against a copy: Phase 13.2 at 6 of 6 landed, gate landed, the checkpoint
       open; the three assertions held. Commit the docs and the gate records: `docs(13.2-06): the
       phase gate - every hash from 01's --before equal at HEAD (wire a24b256f / 514cb2c7, census
       e6af80e0, CSS <8>), 94 / 966 (+1 todo) / 86 / 101 / check 655 (654 + source.ts) / sweep 4 19 /
       allowlist 0 / six circles unedited; TESTING.md's Phase 13.2 section inserted whole with the
       six-term chain and the per-file table; STATE by script against a copy with status,
       completed_phases 11 and percent 100 asserted`.
  </action>
  <verify>
    <automated>bash scripts/13.2-gate.sh --after 06-phase --against 01 --check 655 && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && git diff --stat 500e33c -- src/vendor src/lib/fidelity/upstream-manifest.json src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte | wc -l | grep -qx 0</automated>
  </verify>
  <done>The whole phase measured against its first record with every term equal; the suite whole and green with every rerun named; the TESTING.md section inserted whole; STATE recorded; nothing under the refuse-list moved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 13.2-06-04: Three files, read by the user</name>
  <what-built>
    Six plans of comments, names and dead code: every source file under src/ (not src/vendor/, not
    Knob.svelte, not ColourPicker.svelte) opens with at most ten header lines and one pointer; the
    eighteen entries read MECHANISM / WHAT IT SENDS / TRAPS with their history in docs/entries/; the
    two stores' essays are gone and three private names say what they are; one stripComments serves
    33 specs; six dead exports are gone. Nothing a visitor, a module or a test can see moved: the wire
    hash, the string census, the fixtures, the OG bytes and the built CSS are equal to the record taken
    before the first edit, every count is the carried pair, every e2e title is green, the sweep reads
    4 19, the allowlist is empty and the six circles are where they were. No device was touched.
  </what-built>
  <how-to-verify>
    Name any three files under src/ (a store, an entry and a component make a fair sample - for
    instance src/lib/device/install.svelte.ts, src/lib/catalog/entries/lumen.ts and
    src/lib/ui/TuningRegion.svelte, but the choice is yours; the per-file table in this plan's SUMMARY,
    from `comment-lines.mjs --against gate/01.lines.json`, lists every file by how much it lost, and
    the largest cuts are the best sample). Saying "approved" records the phase as gate landed and
    reviewed; it ticks no requirement (CAT-04 stays [ ]) and edits no ROADMAP line - that is the
    orchestrator's. For each:
    1. Open it at HEAD.
    2. Open the same file as it was before the phase: `git show 500e33c:<path>` (or
       `git diff 500e33c -- <path>` for the two side by side).
    3. Say whether the HEAD form reads better - whether you can tell what the file is, what it owns
       and how to use it from the top, and whether anything you relied on in the old comments is now
       missing (if it is, say which line: it is in the plan SUMMARY that moved it, and in
       docs/entries/ for an entry).
    Then, if you wish, the six questions in 13.2-CONTEXT.md ("Questions this phase could not
    decide"): placeInWords, DISCARD_LABEL, the localStorage guards, ColourPicker.svelte:66's stale
    line, the public "keep" API, buildTuner's helpers.
  </how-to-verify>
  <resume-signal>Type "approved" or name the files that read worse and what is missing.</resume-signal>
</task>

</tasks>

<verification>
- `gate/06-phase.txt`: every term equal to `gate/01.txt`'s; the sandbox set equal to `gate/02.txt`'s;
  check 655; the quick suite `94 / 966` three times; the sweep `4 19`; lint clean.
- All five chunks green at 101 runs (86 titles), every rerun named.
- `git diff --stat 500e33c -- src/vendor src/lib/fidelity/upstream-manifest.json src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte` empty; `git diff --name-status 500e33c -- src | grep -E "^(R|D)"` empty; one `A`.
- `docs/TESTING.md`: every pre-existing line survives as a prefix; the new section carries the
  six-term chain with "six" asserted on every row.
- `git diff --quiet -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` exits 0.
</verification>

<success_criteria>
- The last of the source under the rule with no code moved; every fixture byte-identical.
- The phase proved against its own first record on every term; the suite whole; the section in
  TESTING.md.
- The user has read three files and answered.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-readability/13.2-06-SUMMARY.md` with: the observed
count line, the plan's and the phase's gate texts pasted whole, the six-term chain as observed
against 13.2-VALIDATION's projection with every difference named, the per-file table for the whole
phase with its totals, the runs with memory and every rerun, the spec bodies edited across the phase
by title, "where this phase's planner was wrong", the gate holes, the ROADMAP row for whoever updates
it (6 plans; the phase is gate landed, checkpoint pending until the user answers; no requirement
claimed; CAT-04 `[ ]`), the six questions restated, and the trailer conflict reported.
</output>
