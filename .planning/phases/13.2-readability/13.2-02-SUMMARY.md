---
phase: 13.2-readability
plan: 02
subsystem: readability
tags: [refactor, comments, catalog-entries, library, sandbox, gate]
requires:
  - 13.2-01 (docs/CODE-STYLE.md, scripts/13.2-gate.sh, scripts/gate/*, the 01 baseline)
provides:
  - docs/entries/<id>.md - twenty markdown files holding the moved history verbatim
  - scripts/gate/sandbox-fixtures.mjs and hash-wire --sandbox - the Sandbox set, recorded from 02 on
  - the eighteen entries under MECHANISM / WHAT IT SENDS / TRAPS with one pointer each
  - library.ts as a ten-line header over a ten-heading table of contents
affects: [13.2-03, 13.2-04, 13.2-05, 13.2-06]
tech-stack:
  added: []
  patterns:
    - a catalog entry's three headings, the history in docs/entries/<id>.md
    - the docs' fenced-block shape (D-16) - prettier-clean by construction
key-files:
  created:
    - docs/entries/arc.md
    - docs/entries/chorus.md
    - docs/entries/console.md
    - docs/entries/cull.md
    - docs/entries/euclid.md
    - docs/entries/ghost.md
    - docs/entries/library.md
    - docs/entries/lumen.md
    - docs/entries/morph.md
    - docs/entries/pomodoro.md
    - docs/entries/quadrant.md
    - docs/entries/radar-points.md
    - docs/entries/sandbox-runtime.md
    - docs/entries/snake.md
    - docs/entries/sonar.md
    - docs/entries/stage.md
    - docs/entries/steps.md
    - docs/entries/strip.md
    - docs/entries/trackpad.md
    - docs/entries/wheels.md
    - scripts/gate/sandbox-fixtures.mjs
  modified:
    - src/lib/catalog/entries/*.ts (nineteen), src/lib/catalog/library.ts, src/lib/sandbox/runtime.ts, src/lib/sandbox/emit.ts
    - src/lib/catalog/library.spec.ts, lua-entries.sweep.spec.ts, decay-idiom.spec.ts, host-surface.spec.ts, copy.spec.ts, audition.spec.ts, src/lib/sandbox/runtime.spec.ts, emit.spec.ts
    - scripts/gate/hash-wire.mjs, scripts/gate/comment-lines.mjs, scripts/13.2-gate.sh, docs/CODE-STYLE.md
decisions:
  - "The whole former header of every entry goes to its doc verbatim (a superset of the costings and verdicts the plan names), so nothing is dropped; the new header is written fresh from the facts"
  - "comment-lines.mjs --todo stops at a catalog entry's MECHANISM heading: CODE-STYLE section 4's three-heading block is not counted against the ten"
  - "library.ts keeps its ten numbered banners (the tree has ten, not the plan's eleven) as a table of contents, so --todo prints it by construction; the ten-line header proper is the ten lines above the first banner"
  - "Every comment between library.ts:494 and :645 is left untouched, including the ones inside the G concatenation, so no hunk starts at or after 494"
  - "The gate's normalised-JS term folds kit's per-build __sveltekit_<hash(version)> global, found when 01's --after and 02's --before disagreed on an unchanged src/"
metrics:
  duration: "about 3 h 40 min wall clock on 2026-09-13 (a first session ended before any commit; the second started from the clean tree at ec868c9)"
  completed: 2026-09-13
---

# Phase 13.2 Plan 02: The entries and the library Summary

**One-liner:** The eighteen hand-authored entries and `ported.ts` under `MECHANISM / WHAT IT SENDS /
TRAPS` with a ten-line header and one `History:` pointer each, their former headers moved verbatim
into twenty `docs/entries/*.md` files (5,667 entry comment lines to 1,533); `library.ts` as a
ten-line header over its ten numbered banners with section 5's contract and section 9's
`842 / 66 + 873 / 35`, the stale `705 / 781` gone; `runtime.ts` and `emit.ts` under the rule with six
`export` keywords dropped; `hash-wire --sandbox` over the eleven Sandbox fixtures the two specs
build - and `--before 02` equal to `--after 02` on every compared term including the new sandbox
set, with not one byte of any Lua literal moved.

**Observed count line:** `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4 19 / allowlist 0`.
Term **+0 / +0; e2e +0 / +0; check +0**, as projected.

## Commits

| Task | Commit    | What                                                                                                                                                      |
| ---- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | `69c99f1` | `scripts/gate/sandbox-fixtures.mjs`; `hash-wire.mjs --sandbox` (154 records, `SANDBOX SET 40b44316…`); `13.2-gate.sh` passes `--sandbox` and compares the set |
| 1    | `b0c033c` | `13.2-gate.sh`: the normalised-JS term also folds `__sveltekit_<hash>` (deviation 2)                                                                       |
| 1    | `d6551ed` | `runtime.ts` (202-line header to 10) and `emit.ts` (138 to 10) under the rule; `TIMER_PERIOD_MS`, `DEFAULT_REST_PHASE`, `renderPaint`, `renderPullIn`, `renderInlineCallback`, `SPLIT_CALLBACK` demoted; the two spec headers; `docs/entries/sandbox-runtime.md` |
| 2    | `c956424` | the eighteen entries and `ported.ts`; eighteen docs; five catalog spec headers; `comment-lines.mjs`'s MECHANISM stop                                        |
| 3    | `ceb9bd8` | `library.ts` (479-line header to a ten-line header and ten banners); `library.spec.ts` header; `docs/entries/library.md`                                     |
| docs | (this)    | this SUMMARY; `gate/02-after.*`; `docs/CODE-STYLE.md`'s one appended line; STATE.md by script                                                              |

No co-author trailer on any commit: the harness asked for one; the repository's rule wins, and this
line reports the conflict. Nothing touched a ZONA; nothing deployed; no `gsd-tools state` command;
`.planning/ROADMAP.md` and `REQUIREMENTS.md` untouched (`git diff --quiet -- .planning/ROADMAP.md`
exits 0); **CAT-04 stays `[ ]`**.

## The gate: `--before 02` at `69c99f1`, pasted whole

Equal to 01's `--after` on every term it shares - the wire set `a24b256f…` (1,735), `--full`
`514cb2c7…` (1,761), the census `e6af80e0…`, the copy exports `10fad002…`, the testids `21fc9eb9…`,
the SCOPED CSS `e296d0af…`, the utilities 44 with the five markup-named, the titles `b53f2d9c…`,
check 655, the fixtures, the OG - and recording for the first time the **SANDBOX SET
`40b44316…`** over 154 records from eleven fixtures. One term moved between 01's `--after` at
`c86de8b` and this record with nothing under `src/` changed: the normalised JS `69ca1e25…` ->
`e754d5f2…`, in one chunk (`C1U6IK0Q.js`) - deviation 2 below.

```
# 13.2 gate --before 02
HEAD 69c99f1 (69c99f1b0d09d2548f2816df6c02fa5337a8dba0); dirty (src e2e scripts docs) 0; free memory 0.8 GB; 2026-09-13T14:32:00Z

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 58384 lines, 29520 code, 25118 comment, 3746 blank, 12241 header

## check and lint
check: 1789309943827 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 69c99f1

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 e754d5f290e12a0a252b0393164f1c9068c5de90158d8f163dabbda1d3f7b2fc (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

recorded: .planning/phases/13.2-readability/gate/02.{txt,wire.json,wire-set.json,strings.json,lines.json,vitest.json,titles.txt,utilities.txt,js.txt}
```

## The gate: `--after 02 --against 02 --check 655` at `ceb9bd8`, pasted whole

Term by term: **wire set, `--full` and the sandbox set equal** (`a24b256f…`, `514cb2c7…`,
`40b44316…`); **census, copy exports and testids equal**; **SCOPED CSS equal** at `e296d0af…` with
the **utilities delta 44 -> 44, none disappeared, none appeared** - the plan expected classes to
leave with the entries' prose, and none did: no word cut from the 5,600 lines was one Tailwind
emits (the five markup-named intact); **OG, fixtures, titles equal**; **name-status 30 `M`, 0 `A`,
0 `D`, 0 `R`**; the refuse-list paths' `--stat` empty; **check 655 / 0 / 0**; lint, quick and build
green. Recorded, not compared: the raw CSS unmoved at `174411ee…` (nothing left the utilities
layer), the normalised JS `e754d5f2…` -> `dacda4eb…` (the six `export` drops in `runtime.ts` /
`emit.ts` change the emitted chunks, as the plan says). The comment-line record: **58,384 ->
53,649 lines, 25,118 -> 20,383 comment lines, the headers 12,241 -> 8,040**; the twenty-two files
that moved are the table.

```
# 13.2 gate --after 02 --against 02 --check 655
HEAD ceb9bd8 (ceb9bd820e1d167708c4c47a7aec7a2cb7c9ac4e); dirty (src e2e scripts docs) 0; free memory 0.93 GB; 2026-09-13T15:27:31Z
before-record HEAD 69c99f1b0d09d2548f2816df6c02fa5337a8dba0

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 53649 lines, 29520 code, 20383 comment, 3746 blank, 8040 header

| file | lines before | after | comment before | after | header before | after |
|---|--:|--:|--:|--:|--:|--:|
| src/lib/catalog/entries/arc.ts | 397 | 183 | 293 | 79 | 264 | 65 |
| src/lib/catalog/entries/chorus.ts | 330 | 192 | 217 | 79 | 158 | 62 |
| src/lib/catalog/entries/console.ts | 385 | 146 | 313 | 74 | 276 | 59 |
| src/lib/catalog/entries/cull.ts | 249 | 141 | 184 | 76 | 144 | 61 |
| src/lib/catalog/entries/euclid.ts | 373 | 185 | 266 | 78 | 226 | 62 |
| src/lib/catalog/entries/ghost.ts | 442 | 194 | 338 | 90 | 282 | 71 |
| src/lib/catalog/entries/lumen.ts | 639 | 167 | 576 | 104 | 530 | 86 |
| src/lib/catalog/entries/morph.ts | 535 | 185 | 440 | 90 | 376 | 69 |
| src/lib/catalog/entries/pomodoro.ts | 356 | 161 | 281 | 86 | 215 | 66 |
| src/lib/catalog/entries/ported.ts | 191 | 149 | 89 | 47 | 44 | 12 |
| src/lib/catalog/entries/quadrant.ts | 261 | 144 | 191 | 74 | 128 | 55 |
| src/lib/catalog/entries/radar-points.ts | 469 | 193 | 365 | 89 | 317 | 68 |
| src/lib/catalog/entries/snake.ts | 285 | 159 | 210 | 84 | 167 | 67 |
| src/lib/catalog/entries/sonar.ts | 417 | 186 | 313 | 82 | 278 | 65 |
| src/lib/catalog/entries/stage.ts | 389 | 151 | 323 | 85 | 291 | 70 |
| src/lib/catalog/entries/steps.ts | 384 | 165 | 300 | 81 | 252 | 64 |
| src/lib/catalog/entries/strip.ts | 392 | 152 | 320 | 80 | 265 | 62 |
| src/lib/catalog/entries/trackpad.ts | 312 | 142 | 246 | 76 | 225 | 58 |
| src/lib/catalog/entries/wheels.ts | 508 | 185 | 424 | 101 | 354 | 76 |
| src/lib/catalog/library.ts | 709 | 401 | 603 | 295 | 479 | 188 |
| src/lib/sandbox/emit.ts | 442 | 325 | 188 | 71 | 138 | 12 |
| src/lib/sandbox/runtime.ts | 393 | 217 | 238 | 62 | 202 | 12 |
| **totals (193 -> 193 files)** | 58384 | 53649 | 25118 | 20383 | 12241 | 8040 |

22 file(s) moved

## check and lint
check: 1789313274387 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp ceb9bd8

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only
UTILITIES DELTA 44 -> 44: 0 disappeared, 0 appeared
MARKUP-NAMED intact: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 dacda4ebb17999b08f57edd59d98e1b093466c88a63732accbad844c0baf72ea (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

## src/ against the before-record's HEAD 69c99f1b0d09d2548f2816df6c02fa5337a8dba0
git diff --name-status: 30 modified, 0 added, 0 deleted, 0 renamed
git diff --stat over src/vendor, the manifest, Knob.svelte, ColourPicker.svelte: (empty)

## comparison against 02
wire: equal (set a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b, full 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b)
sandbox set: equal (40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01)
strings: equal (census e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966, copy exports 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef, testids 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3)
scoped CSS: equal (e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237); the utilities delta is printed above by name
OG: equal (26 files, 154136 B, 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57)
fixtures: equal
titles: equal (b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b)
name-status: no R, no D, A only for src/test-support/source.ts
check: 655 files, 0 errors, 0 warnings
lint, quick, build: green
normalised JS: recorded (dacda4ebb17999b08f57edd59d98e1b093466c88a63732accbad844c0baf72ea), not compared (no --js-equal)

GATE --after 02 against 02: every compared term equal
```

## The Sandbox set (task 1)

`scripts/gate/sandbox-fixtures.mjs` copies the two specs' fixtures by hand under each spec's own
helper - the two `region()` functions differ (runtime.spec's takes a controller and uses channel 1
and colour `15,15,15`; emit.spec's pins cc 102, channel 16 and the picker corner), so one shared
helper would have hashed strings neither spec measures. Eleven fixtures: runtime.spec's PAGE3, Two
(Filter + Cut), Latch, Wide, One; emit.spec's PAGE3, ONE, EIGHT, TWELVE, SIXTEEN, FOUR_FADERS. Per
fixture: `S/<name>/landed/*` (five, through `landSurface`), `S/<name>/emitted-2-slots/*` and
`emitted-3-slots/*` (three each), `S/<name>/emitted-at-corner/*` (three) - 14 x 11 = **154 records,
`SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01`**, hashed apart from
the base set, which still reads `a24b256f…` / `514cb2c7…` with or without the flag. `13.2-gate.sh`
passes `--sandbox` on its `--full` line, logs `WIRE SANDBOX SET`, and compares it; a before-record
without one (01's) reads "not recorded on the before side ... recorded now, not compared".

## The twenty docs, by name and line count

`docs/entries/`: `arc.md` 278, `chorus.md` 172, `console.md` 290, `cull.md` 156, `euclid.md` 240,
`ghost.md` 296, `library.md` 495, `lumen.md` 543, `morph.md` 389, `pomodoro.md` 229, `quadrant.md`
141, `radar-points.md` 330, `sandbox-runtime.md` 358, `snake.md` 181, `sonar.md` 292, `stage.md` 304,
`steps.md` 265, `strip.md` 278, `trackpad.md` 240, `wheels.md` 367. Every one carries the dated
heading `## Moved from <path> on 2026-09-13 (13.2-02)` (`sandbox-runtime.md` two, one per file) and
the moved text in a ` ```text ` block with the `//` prefixes removed and nothing else changed -
verified for `lumen.md` and `library.md` by `diff` against the former header (the only difference
the trailing empty `//` before the copyright line, dropped). What moved is the WHOLE former header,
a superset of the costings and verdicts the plan names, so nothing is dropped; `docs/` is outside
every walker, count and the Tailwind scan, and `npx prettier --check docs/entries` passes with the
fenced content untouched (each doc was `--check`ed on creation; none needed a `--write`).

## The entries (task 2)

Each entry's header is at most ten text lines ending in `History: docs/entries/<id>.md (...)`, then
exactly `MECHANISM`, `WHAT IT SENDS`, `TRAPS`, then the copyright line; the `SETUP` / `TIMER`
literals and the `knobs` tables are byte-identical (the diff of every commit read by eye for a hunk
inside a template literal - none; `git diff ec868c9..HEAD -U0 -- src/lib/catalog/entries
src/lib/catalog/library.ts | grep '^[-+]' | grep -vE '^[-+]{3}|^[-+]\s*(//|\*|/\*)'` prints
nothing). The body comments on tags, knobs, defaults and `restsBlack` were trimmed to the fact under
D-01 (they are not in the docs; their history is in the plan SUMMARYs). Kept whole under TRAPS:
LUMEN's `DO NOT "FIX" THE NAMES` (the `D` / `F` shadowing) and `THE CURSOR RESTORES, IT DOES NOT
REPAINT`; MORPH's `DO NOT ADD THE STANDARD KEEPER`; ARC's `THE TWO glt(a,2,65535) CALLS ARE CORRECT
AND MUST NOT BE REMOVED`; STAGE's `THE KEEPER IS 30000 ... DO NOT FIX IT` and its declared class-B
exception; POMODORO's `THE 655 SECOND RE-ISSUE, DO NOT REMOVE`; CULL's and QUADRANT's `DO NOT
"TIDY" IT` bit tests; WHEELS's `NEVER gtt(0,0)`; SNAKE's `DO NOT REMOVE A BOUND`; GHOST's clear;
the two gks arity rules; every divisor-of-252 rule. Cost figures corrected to the shipped values:
LUMEN's library figure `781 / 705` -> `842 / 873 since 12.1-08b` (cited). `comment-lines.mjs --todo
src/lib/catalog/entries/*.ts` prints `0 file(s) not yet at the rule`. Quick suite green at
`--maxWorkers=2` after entries 3, 6, 9, 12, 15 and 18; `hash-wire` (quick) `a24b256f…` at each.

`ported.ts`: header 44 -> 10, the three body comments to the fact, no doc (as the plan says).

The five catalog spec headers, comments only, no title touched: `lua-entries.sweep.spec.ts` 71 ->
13 (the prose still names none of test 4's constructs); `decay-idiom.spec.ts` 132 -> 52 - the four
numbered sections kept because the tests' messages cite "this file's header" and "section 4 of the
header" and the idiom exists nowhere else (D-20), the 11-01 history to one `Decided at 11-01`
pointer; `host-surface.spec.ts` 28 -> 12; `copy.spec.ts` 39 -> 13; `audition.spec.ts` 36 -> 13.

## library.ts (task 3)

The 479-line header is a ten-line header (two strings, thirteen functions, one convention; which
half holds what; 842 / 66 and 873 / 35; the `History:` pointer) over the ten numbered banners,
kept with their numbers and titles as a table of contents, each followed by the fact: section 1's
firmware `file:line` citations as citations; section 2's slot table, the split rule in the words
`library.spec.ts` test 4 asserts ("255/0 holds STATE and THE MAP, 255/6 holds THE PAINTERS AND THE
SENDERS"), the join and the install order, the page-load doubt as a pointer to D-04's runbook row,
and the pointer to `SLOT-ARITHMETIC.md` / `12.1-02-SUMMARY.md` / `12.1-08b-SUMMARY.md`; section 3's
five alert-layer call sites; section 4's six probe rules and where each lives; section 5's contract
as a compact list (name, signature, unit, callers); section 6's 45/64 band; section 7's self-expiry;
section 8's caller-owned window; section 9's `842 / 66 + 873 / 35` with both part sums; section 10's
names. **The stale `705` / `781` at `:68`, `:80` and `:453` went with their narrative**; the only
library figures in the file are the shipped 842 / 873. `LIBRARY_VERSION`'s doc comment (after the
strings) to the fact. Every comment between `:494` and `:645` is untouched, including the two inside
the `G` concatenation, so `git diff -U0`'s hunk headers end at `@@ -465,13 +183,4 @@`; the proof is
`hash-wire --full --sandbox`, equal.

`library.spec.ts:17` ("see `library.ts` section 5") and `:603` ("THE SPLIT RULE (`library.ts`
section 2)") re-read: both headings exist with those numbers and titles - **left as they are**. Its
header 25 -> 13.

**The citations `grep -rn "library.ts.\{0,12\}section [0-9]"` finds outside `library.ts`, fourteen
in `src/`, every one re-read against the kept banners:** `entries/chorus.ts:32` (section 5, the
window is the caller's - true), `entries/lumen.ts:9` (section 1 - true), `entries/radar-points.ts:17`
(section 5, `R` idempotent - true), `library.spec.ts:17` (5) and `:603` (2), `sandbox/runtime.ts:30`
(section 8, `X` - true), `sandbox/runtime.spec.ts:216` (section 7, the same-id expiry - true),
`sim/lua-host.spec.ts:584` (section 2, `N` moved to the map's slot - true), `sim/lua-smoke.spec.ts:6598`,
`:7289` (section 5, the window - true), `:7817`, `:8148`, `:8175` (section 7 - true; `:8175` is inside
an assertion message and is untouched), and **`sim/lua-smoke.spec.ts:6531` ("`library.ts` section 3
says so" about `R`'s idempotency) - stale before this plan: idempotency is section 5's, section 3 is
the alert layer.** Not this plan's file; named for the plan that owns `lua-smoke.spec.ts`. Five more
in `docs/entries/*.md` are the moved history, verbatim by design (including chorus.md's former
"section 3" for the same fact).

## The e2e chunks (fresh detached servers, `scripts/gate/e2e-chunks.sh <name> <files...>`, `--workers 3`, build stamped `ceb9bd8`)

| Chunk    | Free memory | Result                                                                                                                                                                                                   |
| -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (full)   | 0.89 GB     | an accidental run of all 101 - `bash scripts/gate/e2e-chunks.sh c3` with no file list runs `npx playwright test` whole: 98 passed, 3 failed (2.3 m): `browse.e2e.ts:343`, `browse.e2e.ts:1404`, `install.e2e.ts:1957` (webkit-phone) |
| c3       | 1.4 GB      | 21 passed (27.8 s)                                                                                                                                                                                       |
| c4       | 1.15 GB     | 15 passed (21.2 s)                                                                                                                                                                                       |
| c5       | 1.64 GB     | 11 passed (17.9 s)                                                                                                                                                                                       |
| rerun    | 1.09 GB     | `browse.e2e.ts:343`, `browse.e2e.ts:1404`, `install.e2e.ts:1957` alone on a fresh server: 4 passed (14.3 s) - the two browse titles on chromium, the install title on chromium and webkit-phone       |

**21 + 15 + 11 = 47**, the three chunks the plan names, all green; the accidental full run's three
reds are the two browse hydration titles the standing rules name (`:343`, `:1404`) and round 4c's
webkit install title, every one green alone. `test-results/` removed after; port 4173 free. The
sweep: `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` - **4 / 19**, green. The quick
suite through `check-counts.mjs 94 966`: green after task 1, after entries 3 / 6 / 9 / 12 / 15 / 18,
after `library.ts`, and inside `--after` - nine runs at `--maxWorkers=2`.

## Deviations from Plan

**1. [Rule 2 - Missing] `comment-lines.mjs --todo` stops at a catalog entry's `MECHANISM` heading.**
The counter counted the whole leading run up to the copyright line, so every entry under the three
headings CODE-STYLE section 4 requires would have read as 60 to 90 lines "not at the rule".
`ENTRY_HEADINGS` ends the count at `// MECHANISM`; `docs/CODE-STYLE.md`'s appended line says so.
Landed in the task-2 commit.

**2. [Rule 2 - Missing] The gate's normalised-JS term folds `__sveltekit_<hash>`.** 01's `--after`
(`c86de8b`) and 02's `--before` (`69c99f1`) built the same `src/` and the term moved in one chunk,
`C1U6IK0Q.js`: SvelteKit names its client global `__sveltekit_${hash(kit.version.name)}`
(`@sveltejs/kit/src/exports/vite/index.js:247`) and the version name is the build time, so the term
could never be equal across two builds. `s/__sveltekit_[a-z0-9]+/__sveltekit_HASH/g` added
(`b0c033c`); 03 and 05 compare under `--js-equal` with the rule on both sides. This plan's `--before`
record was written before the fix, so its JS hash and the `--after`'s are not comparable anyway
(not compared, as the plan says).

**3. [Rule 3 - Blocking] The e2e chunk runner's `<name>` form.** `bash scripts/gate/e2e-chunks.sh
c3` (the plan's spelling) passes `c3` as a chunk name with no file list, and `npx playwright test`
with no files runs the whole suite. The three chunks were run with their file lists from the
script's own default block. The accidental full run is reported in the table, not hidden.

**4. `library.ts` has ten numbered sections, not eleven**, and `--todo` prints it at 162 by
construction: the plan's shape keeps the banners as a table of contents, and the counter has no
exception for them (adding one would let any file keep a long header behind a numbered banner). The
ten-line header proper is the ten lines above the first banner; named here so the phase gate reads
it as intended rather than as carried.

**5. `decay-idiom.spec.ts` is 52 header lines, not ten** (D-20's stated exception: the header
explains the assertions, the tests cite its sections by number, and the idiom exists nowhere else);
`--todo` prints it at 39.

**6. The whole former header goes to each doc**, not only the costing and verdict paragraphs the
plan enumerates - a superset, so "nothing dropped" is mechanical (`diff` against the former header)
rather than a judgement; the traps therefore appear both in the entry and, as history, in the doc.

**7. Body comments trimmed under D-01 are not in the docs** (the knob glosses, the tag histories,
the `restsBlack` paragraphs): the docs are the plan's shape for the header, and the body facts kept
are the facts; the histories cut there live in the plan SUMMARYs each named.

**8. The utilities delta is empty**, where the plan expected classes to leave with the entries'
prose: 44 -> 44, none appeared, none disappeared, the raw CSS hash unmoved at `174411ee…`.

## Things the plan asserts that the tree does not support

- "`library.ts` keeps its eleven numbered section headings": the file has ten (1 to 10); all ten
  are kept.
- "sixteen by a strict regex, eighteen by a loose one": the strict regex finds fourteen in `src/`
  outside `library.ts` (plus five in `docs/entries/`, the moved history); the plan's list of citing
  files (`runtime.ts`, `chorus.ts`, `lumen.ts`, `radar-points.ts`, `library.spec.ts`,
  `runtime.spec.ts`, `lua-host.spec.ts`, `lua-smoke.spec.ts`) is exactly the set found.
- "the utilities list prints the classes that left with the entries' prose (expected)": none left.
- `lua-smoke.spec.ts:6531` cites `library.ts` section 3 for `R`'s idempotency, which is section 5 -
  stale before this plan, in a file this plan does not own.
- `bash scripts/gate/e2e-chunks.sh c3` does not run chunk c3 (deviation 3).
- The plan's task-2 verify counts `ls docs/entries | wc -l` = 20 before task 3 writes
  `library.md`; the count is 19 at that point and 20 after task 3.

## Known Stubs

None. `docs/entries/*.md` are documentation; every `History:` pointer resolves to a file that
exists with the dated heading; `scripts/gate/sandbox-fixtures.mjs` is read by `hash-wire.mjs`
under `--sandbox` and by nothing else, by design.

## Resume line (D-17)

`node scripts/gate/comment-lines.mjs --todo src/lib/catalog/entries/*.ts src/lib/sandbox/runtime.ts
src/lib/sandbox/emit.ts` prints `0 file(s) not yet at the rule`; over `src/lib/catalog/library.ts`
and `src/lib/catalog/decay-idiom.spec.ts` it prints the two named exceptions (deviations 4 and 5);
every entry carries the three headings and its pointer (`grep -c` = 1 each, eighteen of eighteen).
The plan is landed; nothing is carried. 13.2-03 runs `bash scripts/13.2-gate.sh --before 03` next
(the gate now records the sandbox set and folds the kit hash into the JS term).

## Self-Check: PASSED

The twenty files under `docs/entries/` exist with the dated heading (`grep -L` prints none);
`scripts/gate/sandbox-fixtures.mjs` exists and `hash-wire --sandbox` prints `SANDBOX SET
40b44316…`; the five commits `69c99f1`, `b0c033c`, `d6551ed`, `c956424`, `ceb9bd8` are in the log;
`gate/02.*` and `gate/02-after.*` exist; `--after 02` printed `every compared term equal`;
`npx prettier --check` passes on every file this plan touched; `.planning/ROADMAP.md` is untouched;
STATE.md's `status: executing`, `completed_phases: 11`, `percent: 100` asserted equal before and
after the script (copy sha256 `dec7ac0c…`, one line added - the retained previous stop).
