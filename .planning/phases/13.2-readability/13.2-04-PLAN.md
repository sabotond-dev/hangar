---
phase: 13.2-readability
plan: 04
type: execute
wave: 4
depends_on: ["13.2-03"]
# Execution order (13.2-VALIDATION "Why the phase is serial"): SERIAL - 01 -> 02 -> 03 -> 04 -> 05 -> 06.
# FOURTH: the two runes stores - comments and three private names; no method moved, no import added.
# After 03 because the store's header cites the copy modules' retirement lines.
files_modified:
  - src/lib/device/install.svelte.ts
  - src/lib/device/session.svelte.ts
  - src/lib/device/install.spec.ts
  - src/lib/device/session.spec.ts
  - src/lib/device/wire-pin.spec.ts
  - src/lib/tune/model.spec.ts
  - docs/CODE-STYLE.md
  - .planning/phases/13.2-readability/gate/
  - .planning/STATE.md
autonomous: true
requirements: [] # none - a refactor claims nothing

must_haves:
  truths:
    - "install.svelte.ts opens with at most ten header lines plus the rules a spec enforces, one line each (four specifiers, no $derived, no interval, the write order, the page target's click rule with 13.1-CONTEXT D-05 and THE KEYBOARD FACT, NAMED), then a table of contents made of the section banners already in the file; the class's shape - every method, every field, every import - is where it was."
    - "The three substitutions are one private method, #systemStringOr(config, slot, protocolLib?), and the wire it produces is byte-identical: install.spec.ts's substitution titles, wire-pin.spec.ts and model.spec.ts are green with only their comments reworded."
    - "P and T read protocolLib and transportLib in both stores; keptThisSession reads storedThisSession in the store and in the ten install.spec.ts assertions; install.spec.ts:1286 still counts exactly four static specifiers and session.spec.ts:1108 still finds none of its eleven needles."
    - "No comment in either store spells the erase / clear instruction names or the two page classes (forbidden-instructions.spec.ts green)."
  artifacts:
    - path: "src/lib/device/install.svelte.ts"
      provides: "The install store at roughly 1,150 lines from 1,938 with no code moved; #systemStringOr; protocolLib / transportLib; storedThisSession"
      contains: "#systemStringOr("
    - path: "src/lib/device/session.svelte.ts"
      provides: "The session at roughly 800 lines from 1,238; protocolLib / transportLib; per-field JSDoc to the fact"
      contains: "protocolLib"
  key_links:
    - from: "src/lib/device/install.spec.ts"
      to: "src/lib/device/install.svelte.ts"
      via: "the four-specifier scan over stripped source"
      pattern: "exactly four static specifiers"
    - from: "src/lib/device/install.svelte.ts"
      to: "src/lib/device/install-copy.ts"
      via: "one of the four static from specifiers"
      pattern: "from \"\\./install-copy\""
---

<objective>
The two files a hardware-facing reader opens first: 1,938 and 1,238 lines of which 870 and 534 are
code. The essays between the methods go to one line each with a pointer; the banners already in the
files become a table of contents at the top; three private names say what they are. Nothing moves:
no method, no field order, no import, no export, no phase name, no string.

Purpose: `install.svelte.ts` is the one file that writes to a ZONA; a reader should be able to find
the write order, the four rules and the click that sends in the first screen, and the class straight
after.
Output: both stores under the rule; `#systemStringOr`; `protocolLib` / `transportLib`;
`storedThisSession` with its eleven `install.spec.ts` lines; seven spec comments reworded; the two
specs' headers under the rule. Term **+0 / +0; e2e +0 / +0; check 655 (+0)**; the sweep by
declaration (nothing under catalog / tune / share / vendor moves); chunks c1 c4.

**Execution order.** FOURTH, after 13.2-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-readability/13.2-CONTEXT.md
@.planning/phases/13.2-readability/13.2-VALIDATION.md
@.planning/phases/13.2-readability/13.2-03-SUMMARY.md
@docs/CODE-STYLE.md
@src/lib/device/install.svelte.ts
@src/lib/device/session.svelte.ts
@src/lib/device/install.spec.ts
@src/lib/device/session.spec.ts
@src/lib/device/wire-pin.spec.ts
@src/lib/tune/model.spec.ts
@src/lib/protocol/forbidden-instructions.spec.ts
@src/lib/ui/device-ui.spec.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution and no co-author trailer (the
  repository's rule wins; the SUMMARY reports the conflict). No emojis.
- **No device, no deploy; sibling repositories read-only; the dev server on 5173 left alone.**
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state`.** STATE.md by script against a copy, asserting `status: executing`,
  `completed_phases: 11`, `percent: 100` unchanged. ROADMAP not edited. **CAT-04 `[ ]`.**
- **Never `prettier --write .`** — prettier on every file this plan touches, by path.
- **`git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on the four docs — none is this plan's.
- **No method moved, no import added, no export renamed** (13.2-CONTEXT D-04): `install.spec.ts:1286`
  counts exactly four `from "` in the stripped source — `./install-copy`, `./page-target`,
  `./snapshot`, `./session.svelte` — and `session.spec.ts:1108` scans the session for eleven
  write-shaped needles; a "helper extraction" that imports is red on the first and a rename that
  spells `RequestQueue`, `storePage`, `clearToDefault`, `pageActive` … in the session is red on the
  second. The public API (`keepOnDevice`, `keepReason`, `tryOnDevice`, `putBack`, `clearToDefault`,
  `switchPage`, the phase and reason unions) is not touched.
- **A rewritten comment never spells the two erase / clear instruction names or the two page
  classes** — `forbidden-instructions.spec.ts` scans both stores raw; the round-4c note says the
  store "names it by number, not by name" — keep that habit (255/4, page-next, the class by number).
- **The refuse-list, absolute**: no string, no persisted field (`system`, `systemTimer`,
  `systemUtility`, `hangar.snapshot.v4`), no `data-testid`.
- **Every count a carried name plus a delta**: `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4
  19 / allowlist 0`. Term **+0 / +0; +0 / +0; check +0**. `check-counts.mjs 94 966`.
- **The gate brackets the plan**: `--before 04` first, `--after 04 --against 04 --check 655` last.
- **Quick at `--maxWorkers=2`; `install.spec.ts` alone after every section of the store** (its 3,558
  lines read the store as text at 301 needles). Chunks **c1 c4** on fresh detached servers.
- **Re-runnable (D-17)**: a section already at the rule is skipped.
</standing_rules>

<interfaces>
The store's shape by line today (13.2-RESEARCH A.9): header 1-225; type imports and the unions
265-350; env and heavy-module loading 352-437; the class 440-1937 with its banners at `:441` reactive,
`:539` not reactive, `:634` start, `:658` the connection lifecycle, `:816` the snapshot, `:965` the
tuner's pair and `armed`, `:1056` the two closed decisions, `:1169` the inline confirmation, `:1185`
the page target, `:1334` the two RAM clicks, `:1440` CLEAR, `:1588` the flash store and the proof,
`:1899` the 2000 ms line; the singleton 1938. The session's banners: `:309, :368, :463, :659, :802,
:857, :960, :1010, :1110, :1156`.

The header after this plan (install.svelte.ts):
```
// The install store: the snapshot at connect, the two RAM clicks, the way
// back, the flash store and its proof, and every way a write can go wrong
// (Phase 7, SAFE-01, SAFE-03 to SAFE-09). Every write on the site lives here
// and never in the session (session.spec.ts:1108 scans the session for eleven
// write-shaped needles).
//
// RULES A SPEC ENFORCES, one line each:
//   four static specifiers and no more (install.spec.ts:1286): ./install-copy,
//     ./page-target, ./snapshot, ./session.svelte; protocol and transport
//     arrive by await import() in heavyModules()
//   no $derived (the same title): derived answers are methods, or `armed`
//   no setInterval, no raw onData, no direct write (the same title)
//   the write order is SLOTS (sequence.ts): 255/6, 255/0, 255/4, 0/6, 0/0
//   the page target is folded into Phase 7's rules (13-CONTEXT D-06; 13.1-CONTEXT
//     D-05 struck clause 2): a switch is the Target select's change and nothing
//     else; THE KEYBOARD FACT, NAMED (13.1-PLAN-CHECK W-03): a closed <select>
//     fires change on every arrow press, so each press is a switch until it disables
//
// CONTENTS: reactive fields · not-reactive record · start · the connection
// lifecycle · the snapshot · the tuner's pair and armed · the two closed
// decisions · the inline confirmation · the page target · the two RAM clicks
// · CLEAR · the flash store and the proof · the 2000 ms line · the singleton.
// Decided at 07-04 (D-16) and 13-12 (D-06); see the plans' SUMMARYs.
//
// Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.
```
(That is the ten-line rule plus the enforced rules — the rule allows the rules. `13.1-CONTEXT
D-05` and `THE KEYBOARD FACT, NAMED` are `device-ui.spec.ts:2106-2111`'s needles and stay verbatim.)

The merged substitution (replacing `:1017-1054` and its three doc comments; called at `:991-993`
and `:1376-1378`):
```ts
  /**
   * The system string for one slot, or the firmware's own default where the
   * tuner published "" - the one substitution point, read by the write path
   * and by `armed` alike (12.1-07 for 255/6, 13-17 for 255/4). Reads a FIELD,
   * never a kind: the store cannot tell a surface from an entry (13-17).
   */
  #systemStringOr(
    config: ConfigStrings,
    slot: "system" | "systemTimer" | "systemUtility",
    protocolLib?: Protocol,
  ): string {
    if (config[slot] !== "") return config[slot];
    const lib = protocolLib ?? this.#modules?.protocolLib;
    if (slot === "system") return lib?.SYSTEM_DEFAULT_SETUP ?? "";
    if (slot === "systemTimer") return lib?.SYSTEM_DEFAULT_TIMER ?? "";
    return lib?.SYSTEM_DEFAULT_UTILITY ?? "";
  }
```
Call sites: `this.#pageTimer(current) === written.systemTimer` becomes
`this.#systemStringOr(current, "systemTimer") === written.systemTimer` (three lines at `:991-993`),
and `systemTimer: this.#pageTimer(config, P)` becomes `systemTimer: this.#systemStringOr(config,
"systemTimer", protocolLib)` (three lines at `:1376-1378`). The order of the three keys in the
`strings` object literal at `:1375-1380` is not changed (an object literal's key order is not the
wire, but the diff should show three renamed calls and nothing else).

The module handles: `interface HeavyModules { P: Protocol; T: Transport }` → `{ protocolLib:
Protocol; transportLib: Transport }`; in `heavyModules()` `const P = await import("$lib/protocol")` →
`const protocolLib = …`, `T` → `transportLib`; every `P.` / `T.` / `modules.P` / `modules.T` / `{ P }`
destructure (35 sites in the store, 9 in the session at `:292-293`, `:304-305`, `:727-728`, `:831-850`)
follows. `install.spec.ts:1286`'s needles do not name `P` or `T`; `grep -nE "\b[PT]\." src/lib/device/*.svelte.ts`
prints nothing afterwards.

`keptThisSession` → `storedThisSession`: the field at `:465`, the reads at `:1300, :1421, :1432,
:1563, :1608`, the comments at `:146, :1400, :1494`; `install.spec.ts` `:1398, :1500, :1541, :1604,
:1655, :2228, :2323, :2469, :2517, :3147` (assertions) and `:3138` (a comment). `device-ui.spec.ts`
and the components do not read it (grepped 2026-09-13).

The seven spec comments naming the three old methods: `install.spec.ts:2823, :2835`;
`wire-pin.spec.ts:431, :524`; `model.spec.ts:525, :594-595, :614` — reworded to `#systemStringOr`
(comments; no assertion).

The session (`session.svelte.ts`): header `:1-134` to ten lines plus its enforced rules (four light
specifiers - `./session-copy`, `$lib/protocol/usb`, `$lib/transport/ports`,
`$lib/transport/transport` - and the eleven needles it must never contain, named as "eleven
write-shaped needles" without spelling them); `:451-461`'s `#started` paragraph to "Per instance:
every test constructs its own session; a second start() attaches nothing (two call sites, 06-06 and
06-09)"; per-field JSDoc to the fact; the listener-pair and fold sections keep their banners.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.2-04-01: --before 04; the install store's names - #systemStringOr, protocolLib / transportLib, storedThisSession - with the spec lines</name>
  <files>src/lib/device/install.svelte.ts, src/lib/device/install.spec.ts, src/lib/device/wire-pin.spec.ts, src/lib/tune/model.spec.ts</files>
  <action>
    1. `scripts/13.2-gate.sh --before 04`.
    2. Names first, comments second, so the rename diff is readable on its own: (a) `HeavyModules`,
       `heavyModules()` and every `P` / `T` use → `protocolLib` / `transportLib` (the `<interfaces>`
       block; the `P?: Protocol` parameters of the three substitutions go with step b); (b) the
       three substitutions → `#systemStringOr` as prescribed, the six call sites updated, nothing
       else in `armed`'s recompute or `tryOnDevice`'s `strings` literal touched; (c)
       `keptThisSession` → `storedThisSession` at every site listed, and the eleven `install.spec.ts`
       lines; (d) the seven spec comments reworded.
    3. `npm run check` (655 / 0 / 0 — a missed site is a type error here); `npx vitest run --project
       server --maxWorkers=2 src/lib/device/install.spec.ts src/lib/device/wire-pin.spec.ts
       src/lib/tune/model.spec.ts src/lib/protocol/forbidden-instructions.spec.ts` green; `grep -nE
       "\b[PT]\.|keptThisSession|#page(Init|Timer|Utility)" src/lib/device/install.svelte.ts
       src/lib/device/*.spec.ts src/lib/tune/model.spec.ts` prints nothing.
    4. `node --import ./scripts/gate/ts-ext-register.mjs scripts/gate/hash-wire.mjs` — the set
       `a24b256f…` (the store is not on the harness's import list, but the run is 0.8 s and the
       habit is the point); `hash-strings --diff gate/04.strings.json` silent.
    5. Prettier by path. Commit: `refactor(13.2-04): the install store's three substitutions are one
       #systemStringOr(config, slot, protocolLib?) with the wire byte-identical; P / T read
       protocolLib / transportLib; keptThisSession reads storedThisSession with its ten assertions
       and one comment in install.spec.ts; seven spec comments reworded; check 655`.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -1 | grep -E "655 files, 0 errors, 0 warnings" && npx vitest run --project server --maxWorkers=2 src/lib/device/install.spec.ts src/lib/device/wire-pin.spec.ts src/lib/tune/model.spec.ts src/lib/protocol/forbidden-instructions.spec.ts 2>&1 | grep -E "Test Files.*passed"</automated>
  </verify>
  <done>Three private names say what they are; the substitution is one method with the wire unchanged; check 655; the four specs green; no old name survives in code or spec.</done>
</task>

<task type="auto">
  <name>Task 13.2-04-02: The install store's prose - the header, the rules, the contents, the essays between the methods</name>
  <files>src/lib/device/install.svelte.ts, src/lib/device/install.spec.ts</files>
  <action>
    1. The header (`:1-225`) to the shape in `<interfaces>`: the ten lines, the enforced rules one
       line each, the CONTENTS line built from the fourteen banners, one pointer. `13.1-CONTEXT D-05`
       and `THE KEYBOARD FACT, NAMED` verbatim.
    2. Section by section down the class, in banner order: every JSDoc and inline essay to the fact
       with a pointer — `:202-209`'s `WHY THERE IS NO $derived` is now the header's one line and
       goes; the `WHY …` paragraphs (twenty before the first type) each to one line or to the
       header's rule they restate; the enablement answers (`keepReason` "eight rows, first match
       wins" - keep the row list, cut the argument); the five actions' JSDoc to what the action
       writes, in which order, and what it lands (the phase names), one pointer each (12-03, 12.1-07,
       13-17, 13.1-06, round 4c's `61ba376`); the classifier's rules kept as the list they are; the
       2000 ms line's Z-09 kept as one line; `:339`'s ghost `landLua` reworded to `measureLuaRoute`
       + `land`; `:1444-1445`'s `resetDefault()` / `clearElement()` are the Editor's names, cited on
       purpose, and stay. The banners stay where they are. **No line of code
       moves.** After each section: `npx vitest run --project server --maxWorkers=2
       src/lib/device/install.spec.ts src/lib/ui/device-ui.spec.ts src/lib/protocol/forbidden-instructions.spec.ts`.
       A red names a comment a spec read: the phrase goes back as one line and the SUMMARY names it
       (none is expected outside the header's two, which are kept).
    3. `install.spec.ts`'s header (71 lines) and its comments under the rule; bodies untouched beyond
       task 01's eleven lines and two comments.
    4. `node scripts/gate/comment-lines.mjs --against gate/04.lines.json | grep install.svelte` — state
       the before / after (the research projected roughly 1,938 → 1,150 lines; report what it is).
       Prettier by path; the whole quick suite through `94 966`. Commit: `refactor(13.2-04): the
       install store's prose - the header at ten lines plus the rules a spec enforces and a contents
       line, every essay between the methods to the fact and a pointer, no code moved; <before> lines
       to <after>; 94 / 966 (+1 todo)`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966 && grep -c "13.1-CONTEXT D-05" src/lib/device/install.svelte.ts | grep -vqx 0 && grep -c "THE KEYBOARD FACT, NAMED" src/lib/device/install.svelte.ts | grep -vqx 0</automated>
  </verify>
  <done>The store opens with the rules and the contents; every section's prose is the fact with a pointer; the two pinned phrases are present; the quick suite is green at the carried pair.</done>
</task>

<task type="auto">
  <name>Task 13.2-04-03: The session's prose; --after 04; c1 c4; the record</name>
  <files>src/lib/device/session.svelte.ts, src/lib/device/session.spec.ts, docs/CODE-STYLE.md, .planning/phases/13.2-readability/gate/, .planning/STATE.md</files>
  <action>
    1. `session.svelte.ts`: the `P` / `T` rename first (nine sites; `session.spec.ts:1108`'s eleven
       needles are not among the new names — `transportLib` contains none of `RequestQueue`,
       `storePage`, `pageActive` …); `npm run check`; `npx vitest run --project server
       --maxWorkers=2 src/lib/device/session.spec.ts src/lib/device/install.spec.ts` green.
    2. The header (`:1-134`) and the body per `<interfaces>`: ten lines plus the enforced rules (the
       four light specifiers; the eleven needles named as a count and a pointer, never spelled),
       the banners kept, `#started` to its fact, every per-field JSDoc to the fact; the `disconnect`
       / `connect` listener-pair section keeps its one rule ("attached at navigator.serial, not the
       port - 06-RESEARCH"). No code moved. `session.spec.ts` after each section; its header (53
       lines) under the rule.
    3. Prettier by path; `npm run check` (655); `npm run lint`; the quick suite through `94 966`.
       Commit: `refactor(13.2-04): the session's prose - the header at ten lines plus its four
       specifiers and eleven-needle rule, every field's JSDoc to the fact, no code moved; P / T read
       protocolLib / transportLib; <before> lines to <after>`.
    4. `scripts/13.2-gate.sh --after 04 --against 04 --check 655`; chunks **c1** (`install.e2e.ts`,
       `session.e2e.ts` — the store's every path on the fake) and **c4** (`sandbox.e2e.ts`'s loop
       applies through the store) on fresh detached servers, memory noted, `test-results/` removed,
       reds rerun alone and named (`install:1434`, `install:1977`, `session:851`, `session:1009` are
       13-20's known transients).
    5. `docs/CODE-STYLE.md` "Appended": one dated line if the stores taught the rule something (e.g.
       "a file whose shape a spec scans lists the enforced rules under its header, one line each");
       STATE by script against a copy. The SUMMARY: before / after pasted, the per-file table (two
       stores, four specs), the rename sites counted, the eleven `install.spec.ts` lines listed, the
       chunk lines. Commit the docs: `docs(13.2-04): SUMMARY with the gate's before and after, the
       per-file table, the renames by site and the eleven spec lines; STATE by script against a copy
       with status, completed_phases 11 and percent 100 asserted`.
  </action>
  <verify>
    <automated>bash scripts/13.2-gate.sh --after 04 --against 04 --check 655</automated>
  </verify>
  <done>Both stores under the rule with no code moved; the gate's --after equals its --before on every term; c1 and c4 green with reruns named; STATE recorded.</done>
</task>

</tasks>

<verification>
- `gate/04*.txt`: every term equal; check 655.
- `check-counts.mjs 94 966` green; c1 and c4 green, reruns named; the sweep by declaration (no file
  under `src/lib/catalog/`, `src/lib/tune/` (specs' comments aside), `src/lib/share/`, `src/vendor/` moved).
- `grep -cE "^import|^} from" src/lib/device/install.svelte.ts` unchanged; `install.spec.ts:1286`
  green; `session.spec.ts:1108` green; `forbidden-instructions.spec.ts` green.
- `git diff 13.2-03's-HEAD..HEAD -U0 -- src/lib/device/install.svelte.ts | grep '^[-+]' | grep -vE '^[-+]\s*(//|\*|/\*)'`
  shows only the rename and merge hunks named in this plan.
- `git diff --quiet -- .planning/ROADMAP.md` exits 0.
</verification>

<success_criteria>
- The two stores read in ten lines plus their rules, with the essays gone and the code unmoved.
- Three private names renamed with every site and every spec line named; the wire byte-identical.
- Term `+0 / +0; +0 / +0; check 655` observed; every hash equal.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-readability/13.2-04-SUMMARY.md` with the observed
count line, the gate's before and after pasted, the per-file table, the rename sites by count and
the eleven `install.spec.ts` lines by number, the seven reworded spec comments, the chunk lines with
memory and reruns, any section carried, and the trailer conflict. CAT-04 stays `[ ]`.
</output>
