---
phase: 13.2-readability
plan: 04
subsystem: readability
tags: [refactor, comments, renames, install-store, session, gate]
requires:
  - 13.2-03 (the copy modules and page-target.ts under the rule; the gate at 100b632)
provides:
  - install.svelte.ts and session.svelte.ts under the ten-line rule with a RULES A SPEC ENFORCES block and a contents line after the copyright, no method / field / import / export moved
  - "#systemStringOr(config, event: 0 | 4 | 6, protocolLib?) in place of #pageInit / #pageTimer / #pageUtility, keyed by the event number, no literal added"
  - protocolLib / transportLib (and deviceLib in the session) in place of P / T / D; storedThisSession in place of keptThisSession
  - the four specs' comments re-aimed; install.spec.ts's and session.spec.ts's headers under the rule; no title touched
affects: [13.2-05, 13.2-06]
tech-stack:
  added: []
  patterns:
    - a file a spec scans lists the rules it obeys after the copyright line, one line each, citing the spec by title
    - a rename or merge is proved by the comment-stripped source (byte-identical for a prose task) and by the wire hash (for a code task)
key-files:
  created:
    - .planning/phases/13.2-readability/deferred-items.md
  modified:
    - src/lib/device/install.svelte.ts, src/lib/device/session.svelte.ts
    - src/lib/device/install.spec.ts, src/lib/device/session.spec.ts, src/lib/device/wire-pin.spec.ts, src/lib/tune/model.spec.ts
    - docs/CODE-STYLE.md, .planning/STATE.md, .planning/phases/13.2-readability/gate/04.*, gate/04-after.*
decisions:
  - "The RULES A SPEC ENFORCES block and the CONTENTS line sit after the copyright line, as 13.2-03 placed the retirement ledgers, so the header is the ten lines and comment-lines.mjs --todo prints nothing (the plan's prescribed header put them above it and would have read as 22 lines)"
  - "A rule line cites the spec by title (install.spec.ts \"the file's shape\", session.spec.ts \"writes nothing across a whole visit, and cannot\"), not by line number - this plan's own eleven install.spec.ts numbers were six lower at HEAD than in the plan, and the header trim moved them by 59 more"
  - "page-target.spec.ts's own `import * as P` alias is not the store's handle and is left alone, though the plan's grep prints it"
  - "The six old-name comment lines in tune/model.ts and one in routes/dev/install/+page.svelte are deferred by declaration (the plan's sweep: nothing under tune/ or the routes moves), not fixed"
metrics:
  duration: "about 40 min of edits on 2026-09-13 (the --before at 16:27Z to the third commit at 16:53Z) plus the resumed gate, quick and chunks on 2026-09-14 (07:39Z to 08:05Z) after the session ended mid-record"
  completed: 2026-09-14
---

# Phase 13.2 Plan 04: The two runes stores Summary

**One-liner:** `install.svelte.ts` 1,939 -> 1,462 lines and `session.svelte.ts` 1,239 -> 975 with
no method, field, import or export moved (the comment-stripped source byte-identical across each
prose commit), the three substitutions one `#systemStringOr(config, event: 0 | 4 | 6, protocolLib?)`
keyed by the event number with no string literal added, `P` / `T` / `D` reading `protocolLib` /
`transportLib` / `deviceLib`, `keptThisSession` reading `storedThisSession` with its ten
`install.spec.ts` assertions and one comment; seven spec comments reworded, no title touched; every
compared gate term equal between `--before 04` at `100b632` and `--after 04` at `1fa39a8`, the
normalised JS recorded and moved by the renames as the plan said it would.

**Observed count line:** `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep by declaration /
allowlist 0`. Term **+0 / +0; e2e +0 / +0; check +0**, as projected.

## Commits

| Task | Commit    | What                                                                                                                                                                                                                                                                                                      |
| ---- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | `cba1b44` | `#pageInit` / `#pageTimer` / `#pageUtility` -> `#systemStringOr`; `P` / `T` -> `protocolLib` / `transportLib` in the store; `keptThisSession` -> `storedThisSession` in the store and `install.spec.ts`; the seven spec comments and two in the store's `ConfigStrings` JSDoc; check 655; four specs green |
| 2    | `35b46b5` | the install store's prose: the header, the rules block, the contents line, 86 comment ranges; `install.spec.ts`'s header; 1,939 -> 1,462                                                                                                                                                                  |
| 3    | `1fa39a8` | the session: `P` / `T` / `D` -> `protocolLib` / `transportLib` / `deviceLib`; the header, the rules block, 48 comment ranges; `session.spec.ts`'s header; 1,239 -> 975                                                                                                                                    |
| docs | (this)    | this SUMMARY; `gate/04.*` and `gate/04-after.*`; `deferred-items.md`; `docs/CODE-STYLE.md`'s one appended line; STATE.md by script                                                                                                                                                                       |

No co-author trailer on any commit: the harness asked for one; the repository's rule wins, and this
line reports the conflict. Nothing touched a ZONA; nothing deployed; no `gsd-tools state` command;
no `git checkout` / `restore` / `stash` / `clean`; `.planning/ROADMAP.md` untouched (`git diff
--quiet` exits 0); the user's dev server on 5173 left alone (every chunk on a fresh detached 4173).

**One thing the record shows that is not this plan's:** `2d292ed` (`chore: a launcher for the bench
- scripts/launch-hangar.cmd`, the user's, 2026-09-14 09:39:30 +0200) landed one second before the
resumed `--after 04` wrote its first line, so that record reads `HEAD 1fa39a8`, `dirty (src e2e
scripts docs) 1` (the untracked launcher at that instant) and `build stamp 2d292ed`. The file is
under `scripts/`, outside every hashed term; the six-file name-status over `src/` against `100b632`
is exactly this plan's six files.

## The session ended between the third commit and the gate

The first `--after 04` was cut after its `check` line (the coordinator found `04-after.{lines,
strings, txt, wire-set, wire}` and no `js / titles / utilities / vitest`). Re-run whole on
2026-09-14 - `--after` overwrites its own partial record; only `--before` refuses - and every
transcript edit was verified on disk at HEAD before the re-run: `#systemStringOr` 9 mentions in the
store, `storedThisSession` 8 / 11 (store / spec), zero old names in the six plan files, `deviceLib` 4,
`--todo` empty over the two stores and the two specs, the seven reworded spec comments present by
line. Nothing was redone.

## The gate: `--before 04` at `100b632`, pasted whole

```
# 13.2 gate --before 04
HEAD 100b632 (100b632f74acc36aede8d5b2dec9c2930ea3a901); dirty (src e2e scripts docs) 0; free memory 0.18 GB; 2026-09-13T16:27:50Z

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 53008 lines, 29520 code, 19740 comment, 3748 blank, 7602 header

## check and lint
check: 1789316899875 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 100b632

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 dacda4ebb17999b08f57edd59d98e1b093466c88a63732accbad844c0baf72ea (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

recorded: .planning/phases/13.2-readability/gate/04.{txt,wire.json,wire-set.json,strings.json,lines.json,vitest.json,titles.txt,utilities.txt,js.txt}
```

Every hash is 03's `--after`: wire set `a24b256f…`, `--full` `514cb2c7…`, sandbox `40b44316…`, census
`e6af80e0…`, copy exports `10fad002…`, testids `21fc9eb9…`, scoped CSS `e296d0af…` with 44 utilities,
titles `b53f2d9c…`, normalised JS `dacda4eb…`.

## The gate: `--after 04 --against 04 --check 655` at `1fa39a8`, pasted whole

Term by term: **wire set, `--full` and the sandbox set equal**; **census, copy exports and testids
equal** (`hash-strings --diff gate/04.strings.json` silent after task 1 - the merged method is keyed
by number, so no literal joined the census); **SCOPED CSS equal** with the **utilities delta 44 -> 44,
none appeared, none disappeared** (the raw CSS hash equal too, at `174411ee…` - no comment word this
plan cut or wrote was the only source of a utility); **OG, fixtures, titles equal**; **name-status M
only** (6 M, 0 A, 0 D, 0 R over `src/`); **check 655 / 0 / 0**; **the normalised JS moved, recorded
and not compared**: `dacda4eb…` -> `da40f09f…`, because the renames are in the built JS - a class's
private method and field names and the two module-handle property names survive minification as
strings and as property keys (`#systemStringOr`, `storedThisSession`, `{ protocolLib, transportLib }`
are in the bundle where `#pageTimer`, `keptThisSession`, `{ P, T }` were). The plan said this would
happen and told the gate not to compare.

```
# 13.2 gate --after 04 --against 04 --check 655
HEAD 1fa39a8 (1fa39a8e1d1ecd51eacadf0e0804f91a44dbbf91); dirty (src e2e scripts docs) 1; free memory 4.85 GB; 2026-09-14T07:39:31Z
before-record HEAD 100b632f74acc36aede8d5b2dec9c2930ea3a901

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 52267 lines, 29538 code, 18983 comment, 3746 blank, 7302 header

| file | lines before | after | comment before | after | header before | after |
|---|--:|--:|--:|--:|--:|--:|
| src/lib/device/install.svelte.ts | 1939 | 1462 | 988 | 498 | 225 | 31 |
| src/lib/device/session.svelte.ts | 1239 | 975 | 635 | 368 | 134 | 28 |
| **totals (193 -> 193 files)** | 53008 | 52267 | 19740 | 18983 | 7602 | 7302 |

2 file(s) moved

## check and lint
check: 1789371606456 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 2d292ed

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only
UTILITIES DELTA 44 -> 44: 0 disappeared, 0 appeared
MARKUP-NAMED intact: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 da40f09fea7badec9aa2792a39ccd25d1f161af4673f603da12526a67f640f11 (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

## src/ against the before-record's HEAD 100b632f74acc36aede8d5b2dec9c2930ea3a901
git diff --name-status: 6 modified, 0 added, 0 deleted, 0 renamed
git diff --stat over src/vendor, the manifest, Knob.svelte, ColourPicker.svelte: (empty)

## comparison against 04
wire: equal (set a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b, full 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b)
sandbox set: equal (40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01)
strings: equal (census e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966, copy exports 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef, testids 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3)
scoped CSS: equal (e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237); the utilities delta is printed above by name
OG: equal (26 files, 154136 B, 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57)
fixtures: equal
titles: equal (b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b)
name-status: no R, no D, A only for src/test-support/source.ts
check: 655 files, 0 errors, 0 warnings
lint, quick, build: green
normalised JS: recorded (da40f09fea7badec9aa2792a39ccd25d1f161af4673f603da12526a67f640f11), not compared (no --js-equal)

GATE --after 04 against 04: every compared term equal
```

The comment-line totals: 19,740 -> 18,983 across the tree (-757, all of it these two files), the
headers 7,602 -> 7,302. The 29,520 -> 29,538 code lines are task 1's prettier reflow of renamed lines
(the merged method's five-line signature and three ternaries, `const cause: InstallCause =`, two
`(s) => s.label,` wraps, the session's `port.open({ … })`), not a code addition: the code term of the
prose commits is byte-identical (below).

## The per-file table

| File                                | Lines before -> after | Comment lines | Header (audit / rule) | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------- | --------------------- | ------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/lib/device/install.svelte.ts`  | 1,939 -> 1,462        | 988 -> 498    | 225 / - -> 31 / 7     | the header (7 lines, `Decided at 07-04 (07-CONTEXT D-16)`, the copyright) then RULES A SPEC ENFORCES (six rules, one line each, `13.1-CONTEXT D-05` and `THE KEYBOARD FACT, NAMED` verbatim) and CONTENTS from the thirteen banners; 86 comment ranges rewritten; the merge and the two renames; every banner where it was; the WRITABLE_PHASES block (pinned raw with its two inner comment lines) untouched; the requestPage JSDoc that sat above `#page()` now above `requestPage()` |
| `src/lib/device/session.svelte.ts`  | 1,239 -> 975          | 635 -> 368    | 134 / - -> 28 / 9     | the header (9 lines, `Decided at 06-04 (06-CONTEXT D-05, D-07)`, the copyright) then RULES A SPEC ENFORCES (seven rules); 48 comment ranges rewritten, `#started` to CODE-STYLE's worked example, the listener pair and the fold keep their banners, both eslint directives kept; `P` / `T` / `D` renamed                                                                                                                                                                                              |
| `src/lib/device/install.spec.ts`    | 3,552 -> 3,493        | -             | 71 -> 10 (+copyright) | the eleven `storedThisSession` lines, the two `#systemStringOr` comments, the header; no title, no body line otherwise                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/device/session.spec.ts`    | 1,627 -> 1,586        | -             | 53 -> 10 (+copyright) | the header; nothing else                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `src/lib/device/wire-pin.spec.ts`   | 712 -> 712            | -             | -                     | two comments (`:431`, `:524`)                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/lib/tune/model.spec.ts`        | 977 -> 977            | -             | -                     | three comments (`:519`, `:588-589`, `:608-609`)                                                                                                                                                                                                                                                                                                                                                                                                                           |

`node scripts/gate/comment-lines.mjs --todo src/lib/device/install.svelte.ts
src/lib/device/session.svelte.ts src/lib/device/install.spec.ts src/lib/device/session.spec.ts`
prints `0 file(s) not yet at the rule` (the audit header of 31 / 28 counts the rules block after the
copyright; the rule header is 7 / 9 and the copyright breaks the count, as 13.2-03's ledgers do).

**The proof that no code moved, per prose commit:** a comment stripper (whole-line `//`, block
comments, blank lines dropped) applied to the file before and after each prose task, compared as
text: `install.svelte.ts` **CODE IDENTICAL (885 code lines)** against `cba1b44`; `session.svelte.ts`
**CODE IDENTICAL (537 code lines)** against its own renamed copy (the rename is the one code diff of
that commit, shown whole below). Each comment range was replaced by line range, bottom-up, with the
first and last line of every range asserted before the splice and every line inside asserted to be a
comment line (one asserted code line, `requestPage(page: number): boolean {`, where a JSDoc was
inserted above it).

## The renames, by site

**`#pageInit` / `#pageTimer` / `#pageUtility` -> `#systemStringOr(config, event: 0 | 4 | 6,
protocolLib?)`.** The three bodies at `:1017`, `:1033`, `:1051` (at `100b632`) proved identical
modulo the slot before the merge - each normalised by `#page(Init|Timer|Utility)` -> `#M`,
`config.(system|systemTimer|systemUtility)` -> `config.FIELD`, `SYSTEM_DEFAULT_(SETUP|TIMER|UTILITY)`
-> `SYSTEM_DEFAULT_X`, then `diff`ed pairwise, both empty:

```
  #M(config: ConfigStrings, protocolLib?: Protocol): string {
    if (config.FIELD !== "") return config.FIELD;
    return (protocolLib ?? this.#modules?.protocolLib)?.SYSTEM_DEFAULT_X ?? "";
  }
```

The merged method is the plan's text at the first's place (`lib` resolved once, then `event === 6`,
`event === 4`, else the Setup - the same field-or-default per slot; the one evaluation-order
difference, `this.#modules?.protocolLib` read before the field test instead of after, is a
side-effect-free optional read). Six call sites: `#recomputeArmed`'s three `&&` terms (`(current,
6)`, `(current, 0)`, `(current, 4)`, in that order) and `tryOnDevice`'s `strings` literal
(`systemTimer: … 6`, `system: … 0`, `systemUtility: … 4`, keys in that order); nothing else in
either method touched. Nothing awaits inside; the write order is `writeAll` over `SLOTS` and the wire
set is `a24b256f…` before and after; `install.spec.ts`'s order and substitution titles green.
`hash-strings --diff` silent: no `"system"` / `"systemTimer"` / `"systemUtility"` literal joined the
census (13.2-PLAN-CHECK H-2's reason for the number).

**`P` / `T` -> `protocolLib` / `transportLib` in the store: 77 tokens (42 `P`, 35 `T`), all code,
none in a comment** - `HeavyModules`'s two fields, `loadHeavy`'s two locals and its return,
`#targetWith`'s parameter, `#buildQueue`'s parameter, `#rejectHeartbeatWaiters`'s local, the
destructures in `#snapshot`, `confirmPage`, `revertToStored`, `tryOnDevice`, `clearToDefault`,
`#storeLeg`, `#ramLeg`, `#classify`, and every `modules.P` / `modules.T` in `#attach` and
`#escalatePacing`. (The task 1 commit message says "60 tokens" - an eyeball count; 77 is the grep.)
**In the session: 22 tokens on the code lines** `:179-181`, `:292-295`, `:301-305`, `:727-728`,
`:741`, `:831-850` - `HeavyModules`, `loadHeavy`, `openWithWebSerial`, `#openAdopted`, `#startFold`
- with the third handle `D` -> `deviceLib` in the same move; the rename ran on those lines only,
because `\bD\b` also matches `D-05`, `D-06`, `D-07` … in the comments, which stay. (The commit says
"18"; 22 is the grep.) `grep -nE "\b[PT]\." src/lib/device/*.svelte.ts` prints nothing;
`session.spec.ts` "writes nothing across a whole visit, and cannot" finds none of its eleven needles
in `transportLib` / `protocolLib` / `deviceLib`; `install.spec.ts` "the file's shape" still counts
exactly four specifiers.

**`keptThisSession` -> `storedThisSession`: nine sites in the store** (the field at `:465`, the reads
at `:1300`, `:1421`, `:1432`, `:1563`, `:1608`, the comments at `:146`, `:1400`, `:1494` - all at
`100b632`, as the plan listed them) **and eleven in `install.spec.ts`**, which at HEAD sat six lines
above the plan's numbers and now sit 59 lower again after the header trim:

| The plan's line | At `100b632` | At HEAD now | Kind                                          |
| --------------- | ------------ | ----------- | --------------------------------------------- |
| `:1398`         | `:1392`      | `:1333`     | assertion                                     |
| `:1500`         | `:1494`      | `:1435`     | assertion ("not called kept")                 |
| `:1541`         | `:1535`      | `:1476`     | assertion                                     |
| `:1604`         | `:1598`      | `:1539`     | assertion ("cleared by a put-back that stored") |
| `:1655`         | `:1649`      | `:1590`     | assertion (`second.store.…`)                  |
| `:2228`         | `:2222`      | `:2163`     | assertion ("nothing stored yet this session") |
| `:2323`         | `:2317`      | `:2258`     | assertion                                     |
| `:2469`         | `:2463`      | `:2404`     | assertion ("not called stored")               |
| `:2517`         | `:2511`      | `:2452`     | assertion                                     |
| `:3138`         | `:3132`      | `:3073`     | comment (`Z-04, storedThisSession`)           |
| `:3147`         | `:3141`      | `:3082`     | assertion                                     |

No component, e2e file or other spec reads the field (grepped again at HEAD).

## The seven spec comments (and two more), reworded

| File:line (at `100b632` -> HEAD) | Was                                                                   | Now                                                                                             |
| -------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `install.spec.ts:2817` -> `:2758` | "own in ONE place each (#pageInit, #pageTimer)"                       | "own in ONE place (#systemStringOr, keyed by the event number)"                                 |
| `install.spec.ts:2829` -> `:2770` | "and #pageUtility substitutes"                                        | "and #systemStringOr substitutes"                                                               |
| `wire-pin.spec.ts:431`           | "the store's #pageTimer substitution stops firing the same way"       | "the store's #systemStringOr substitution for 255/6 stops firing the same way"                 |
| `wire-pin.spec.ts:524`           | "(in one place, #pageUtility), which install.spec.ts pins"            | "(in one place, #systemStringOr for 255/4), which install.spec.ts pins"                        |
| `model.spec.ts:519`              | "`#pageInit` substitution stays in its one place"                     | "`#systemStringOr` substitution for 255/0 stays in its one place"                              |
| `model.spec.ts:588-589`          | "`#pageUtility` substitutes … as `#pageInit` did for every entry's page init" | "`#systemStringOr` substitutes … for 255/4 in one place, as it did for every entry's page init (255/0)" |
| `model.spec.ts:608-609`          | "`#pageInit` and `#pageTimer`, side by side"                          | "`#systemStringOr`, one method keyed by the event number"                                      |
| `install.svelte.ts:326`, `:334`  | "in ONE place (`#pageTimer`)" / "(`#pageUtility`)"                    | "(`#systemStringOr`)" both - the store's own `ConfigStrings` JSDoc, not on the plan's list      |

Comments only; no assertion, no title. The plan's line numbers for `wire-pin.spec.ts` and
`model.spec.ts` matched the tree; `install.spec.ts`'s were six high.

## The header, as landed (install.svelte.ts)

```
// The install store: the snapshot at connect, the two RAM clicks, the way
// back, the flash store and its proof, and every way a write can go wrong
// (Phase 7, SAFE-01, SAFE-03 to SAFE-09). Every write on the site lives here
// and never in the session (session.spec.ts "writes nothing across a whole
// visit, and cannot" scans the session for eleven write-shaped needles).
// Components read the singleton `install`; every node test constructs its
// own `new InstallStore(session)` and never touches it.
//
// Decided at 07-04 (07-CONTEXT D-16); see .planning/phases/07-install-flow/07-04-SUMMARY.md
//
// Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.
//
// RULES A SPEC ENFORCES, one line each:
//   four static specifiers and no more (install.spec.ts "the file's shape"):
//     ./install-copy, ./page-target, ./snapshot, ./session.svelte; protocol
//     and transport arrive by await import() in heavyModules()
//   no $derived (the same title): derived answers are methods, or `armed`
//   no setInterval, no raw onData, no direct write (the same title)
//   the write order is SLOTS (sequence.ts): 255/6, 255/0, 255/4, 0/6, 0/0
//   the page target is folded into Phase 7's rules (13-CONTEXT D-06; 13.1-CONTEXT
//     D-05 struck clause 2): a switch is the Target select's change and nothing
//     else; THE KEYBOARD FACT, NAMED (13.1-PLAN-CHECK W-03): a closed <select>
//     fires change on every arrow press, so each press is a switch until it disables
//   no comment here spells an erase, clear or page class by name
//     (forbidden-instructions.spec.ts reads this file raw): the event numbers
//
// CONTENTS, the class's banners in order: reactive fields - NOT reactive
// record - start - the connection lifecycle - the snapshot - the tuner's pair
// and armed - the two closed decisions - the inline confirmation - the page
// target - the two RAM clicks - the fourth click, CLEAR - the flash store and
// the proof - the 2000 ms line - the singleton.
```

The plan's prescribed text with three changes: the copyright line moved up so the counter's header
is the ten lines (deviation 1); the two spec citations by title, not `install.spec.ts:1286` /
`session.spec.ts:1108` (deviation 2); a sixth rule line for the by-number habit
(`forbidden-instructions.spec.ts`), which the plan's standing rules ask this file to keep. The plan's
`·` separators are ` - ` (ASCII, like the rest of the tree). `device-ui.spec.ts`'s two raw needles
(`13.1-CONTEXT D-05`, `THE KEYBOARD FACT, NAMED`) are present once each; the spec is green.

## What each store's prose kept, by the plan's list

- `:202-209`'s `WHY THERE IS NO $derived` is the header's one rule line and is gone from the body;
  `#recomputeArmed`'s JSDoc says "never derived (the header's rule)".
- The twenty `WHY …` paragraphs before the first type: gone; each one's fact is in the rule line or
  in the method it was about (the onClass-not-onData fact in `#buildQueue` / the `transport` getter,
  the one-queue-per-connection fact in `#buildQueue`, the in-memory-first and record-wins facts in
  `#snapshot`'s inline comments, the heartbeat-first proof in `#storeLeg`, the error-type taxonomy in
  `#classify`, the pacing rule in `#escalatePacing`, the 2000 ms line in `#armSlow`).
- `keepReason`'s eight rows kept as the list; the argument under it to two sentences.
- The five actions' JSDoc: what is written, in which order, what lands, one pointer each -
  `tryOnDevice` (12-03, 12.1-07, 13-17), `putBack` (13.1-06, D-07, Z-04), `clearToDefault` (A-48,
  D-20, D-21, round 4c `61ba376`, `resetDefault()` / `clearElement()` as the Editor's names on
  purpose), `keepOnDevice` (D-12), `revertToStored` (13-12, runbook row I).
- `#classify`'s two tables kept as they are; the "cannot happen" essay to one sentence with its
  pointer (12-RESEARCH Pitfall 6).
- The 2000 ms line's Z-09 arithmetic kept as one paragraph in `#armSlow`.
- `:339`'s ghost `landLua` -> "the tuner's measureLuaRoute and land".
- The session: `#started` to CODE-STYLE's worked example verbatim; the listener pair's one rule
  ("attached at navigator.serial, not the port - 06-RESEARCH") in the header's rules and in
  `#attachListeners`; the replug identity trap in `#onSerialConnect` (one sentence with the
  `serial_service.cc` pointer); the watchdog's two load-bearing halves kept.

Words a rewritten comment must not spell: `NVMERASE`, `PAGECLEAR`, `PAGEACTIVE`, `PAGEDISCARD`
(`forbidden-instructions.spec.ts`'s needles) - grep over both stores prints nothing; the two page
classes are "the page-discard class" and "the class by number" as before. No new Tailwind-shaped
word: the utilities delta is empty.

## The e2e chunks (fresh detached servers, `scripts/gate/e2e-chunks.sh <name> <files...>`, `--workers 3`, build stamped `2d292ed`)

| Chunk | Free memory | Result                    |
| ----- | ----------- | ------------------------- |
| c1    | 4.77 GB     | **32 passed** (1.2 m)     |
| c4    | 4.88 GB     | **15 passed** (24.2 s)    |

47 of 47 runs green in their chunk; no rerun. `install.e2e.ts:1957` - the CLEAR title A.21 carries as
a 50 ms harness margin, red on one engine in every 13.2-03 run that had both engines on it - was
green on both engines this time, with 4.8 GB free where 03 had 0.13. Port 4173 answers `000` after
each stop; `test-results/` removed; the playwright and wrangler logs in the scratchpad, outside the
tree.

The quick suite: three runs at `--maxWorkers=2` on the final tree (the gate's, then two more), each
`94 / 966 (+1 todo)` through `check-counts.mjs 94 966`; plus one per section during the edits.

## Deviations from Plan

**1. The RULES and CONTENTS blocks sit after the copyright line.** The plan's prescribed header
ends with the copyright after the contents; `comment-lines.mjs --todo` (D-17's resume test) counts
the leading comment run up to the copyright and would have printed the store at 22 lines by
construction. 13.2-03's ledgers set the pattern (its deviation 2, CODE-STYLE's appended line): the
header is the ten lines, the copyright closes it, the rules and contents follow as a section. Same in
the session. Landed in `35b46b5` and `1fa39a8`; CODE-STYLE's appended line for this plan says so.

**2. Spec citations by title, not line.** The plan's rule lines cite `install.spec.ts:1286` and
`session.spec.ts:1108`; both numbers were already stale at HEAD (the shape test's scan is at `:1284`,
the needle loop at `:1148`) and this plan's own header trims moved them again by 59 and 41. The
lines cite the titles - "the file's shape" and "writes nothing across a whole visit, and cannot" -
which the titles hash holds fixed.

**3. Two more `#page*` comment mentions in the store itself** (`ConfigStrings`'s JSDoc, `:326` and
`:334`) were not on the plan's list of seven; reworded in task 1 so the task's clean grep held.

**4. [Rule 3 - blocking, not in the tree] `hash-wire.mjs` writes `gate/hash-wire.<head>-dirty.json`
beside the record when run by hand** (task 1 step 4's habit run); removed before the commit, never
tracked. 13.2-03 named the same for `hash-strings.mjs` (`--out` into the scratchpad avoids it there;
`hash-wire.mjs` has no `--out`).

**5. The commit messages' token counts** (`cba1b44` "60 tokens", `1fa39a8` "18 tokens") were eyeball
counts; the grep says 77 and 22. STATE.md and this SUMMARY carry the grep.

**6. The session's `port.open({ … })` reflowed to three lines by prettier** after the rename made
the line too long - a formatting change on a renamed line, inside the rename commit.

## Things the plan asserts that the tree does not support

- "`keptThisSession` … `install.spec.ts` `:1398, :1500, :1541, :1604, :1655, :2228, :2323, :2469,
  :2517, :3147` (assertions) and `:3138` (a comment)": at HEAD every one was six lines lower
  (`:1392 … :3141`, `:3132`) - the brief said to read them myself, and the table above has all three
  numberings. The `wire-pin.spec.ts` and `model.spec.ts` numbers matched.
- "`grep -nE "\b[PT]\.|keptThisSession|#page(Init|Timer|Utility)" src/lib/device/install.svelte.ts
  src/lib/device/*.spec.ts src/lib/tune/model.spec.ts` prints nothing": it prints
  `page-target.spec.ts:117-119`, that spec's own `import * as P from "$lib/protocol"` alias, which is
  not the store's handle and not this plan's file. Left alone; named in `deferred-items.md`.
- "no old name survives in code or spec": true of the plan's six files; the old names   six comment lines of `src/lib/tune/model.ts` (`:234`, `:251`, `:252`, `:261`, `:326`, `:337`) and
  one of `src/routes/dev/install/+page.svelte` (`:91`), outside the plan's file list and under its
  "sweep by declaration - nothing under `src/lib/tune/` moves". Logged in `deferred-items.md` for
  13.2-06 (the route is its file) or a quick task; comments only, no assertion reads them.
- "P and T … (35 sites in the store, 9 in the session)": 77 tokens in the store and 22 in the
  session, on the sites the plan lists - the plan counted lines, not tokens.
- "`install.svelte.ts` at roughly 1,150 lines from 1,938": 1,462 (1,461 by `wc`). The classifier's
  two tables (10 rows), `keepReason`'s eight rows, the WRITABLE_PHASES block pinned raw, the rules
  and contents block (19 lines), the merged method's prettier shape and the JSDoc the plan's own
  list keeps (five actions with their pointers, the snapshot's four gates, the confirm and the page
  target) are the difference; every remaining comment is a fact with a pointer. The session's
  "roughly 800" is 975 on the same terms.
- The plan's task 3 step 4 names `install:1434`, `install:1977`, `session:851`, `session:1009` as
  known transients; none fired.
- The prescribed header's `·` separators and its `session.spec.ts:1108` / `install.spec.ts:1286`
  citations (deviations 1, 2).

## Known Stubs

None. No string, export, testid or title moved; the three string hashes and the copy-exports hash are
the baseline's.

## Resume line (D-17)

`node scripts/gate/comment-lines.mjs --todo src/lib/device/install.svelte.ts
src/lib/device/session.svelte.ts` prints `0 file(s) not yet at the rule`. The plan is landed; nothing
is carried. 13.2-05 (the components) runs `bash scripts/13.2-gate.sh --before 05` next; 13.2-06 has
`deferred-items.md`'s seven old-name comment lines to take with its route.

## Self-Check: PASSED

The three source commits `cba1b44`, `35b46b5`, `1fa39a8` are in the log; `gate/04.*` (nine files) and
`gate/04-after.*` (nine files) exist; `--after 04` printed `every compared term equal` with the
normalised JS recorded; `npx prettier --check` passes on every file this plan touched;
`.planning/ROADMAP.md` is untouched; STATE.md's `status: executing`, `completed_phases: 11`,
`percent: 100` asserted equal before and after the script (one line added - the retained previous
stop); CAT-04 stays `[ ]`.
