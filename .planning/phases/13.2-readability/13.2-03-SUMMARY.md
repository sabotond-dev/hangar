---
phase: 13.2-readability
plan: 03
subsystem: readability
tags: [refactor, comments, copy-modules, retirement-ledger, gate]
requires:
  - 13.2-02 (the gate with the sandbox set and the normalised-JS term folding the kit hash)
provides:
  - the eight copy modules and page-target.ts under the ten-line rule
  - a retirement ledger per module, as its own section after the copyright line, every RETIRED BY NAME heading kept as its first line
  - the four copy specs' headers under the rule, no title and no body line touched
affects: [13.2-04, 13.2-05, 13.2-06]
tech-stack:
  added: []
  patterns:
    - the retirement ledger sits after the copyright line, so the ten-line header stays the header and comment-lines.mjs --todo reads it
    - a spec-pinned phrase sits on ONE comment line
key-files:
  created: []
  modified:
    - src/lib/device/install-copy.ts, src/lib/device/session-copy.ts, src/lib/device/page-target.ts
    - src/lib/tune/copy.ts, src/lib/tune/inspector-copy.ts, src/lib/sandbox/copy.ts
    - src/lib/ui/library/words.ts, src/lib/browse/labels.ts
    - src/lib/device/install-copy.spec.ts, src/lib/device/session-copy.spec.ts, src/lib/device/page-target.spec.ts, src/lib/tune/copy.spec.ts
    - docs/CODE-STYLE.md
decisions:
  - "The retirement ledger is a section after the copyright line, not part of the header: the counter breaks at the copyright line, so the header is the ten lines and the ledger keeps every heading and every pinned phrase without a counter exception"
  - "A pinned phrase is kept on one comment line even where the plan's prescribed block wrapped it (MIX_TWO 7, MIX_LINE 75, MIX_THIS / MIX_THAT 8 / 8) - the phrase went back verbatim, no spec line re-aimed"
  - "pageName's three copies say why without a spec line number (the plan's install-copy.spec.ts:431 would have gone stale the moment that spec's header was trimmed)"
metrics:
  duration: "about 45 min wall clock on 2026-09-13, from the --before at 15:44Z to the docs commit"
  completed: 2026-09-13
---

# Phase 13.2 Plan 03: The copy modules and page-target.ts Summary

**One-liner:** The seven copy modules (`install-copy.ts`, `session-copy.ts`, `tune/copy.ts`,
`inspector-copy.ts`, `sandbox/copy.ts`, `words.ts`, `labels.ts`) and `page-target.ts` under the
ten-line rule, each retirement ledger compact as its own section after the copyright line with every
`RETIRED BY NAME, <date>` heading kept as its first line and every spec-pinned phrase carried on one
line (the twelve `install-copy.ts` names, the five MIX names, `CH_PER_LINE the 43`, `D-05 changed
the register`, `86` / `129`, `HONESTY_CAP (86)` … `forecastExpansion's 44`), zero spec needles
re-aimed, no title touched; comments only, so `--before 03` and `--after 03` are equal on EVERY term,
the normalised built JS included under `--js-equal` - not a string, not an export, not a testid
moved; the tree's comment lines 20,383 -> 19,740, the headers 8,040 -> 7,602.

**Observed count line:** `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4 19 / allowlist 0`.
Term **+0 / +0; e2e +0 / +0; check +0**, as projected.

## Commits

| Task | Commit    | What                                                                                                                                                                                             |
| ---- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | `4c73296` | `install-copy.ts` (header 205 -> 10, the five retirement blocks compact after the copyright), `page-target.ts` (96 -> 10 + one provenance line, the two blocks compact), the two spec headers          |
| 2    | `0147762` | `tune/copy.ts` (131 -> 10, three blocks compact), `session-copy.ts` 73 -> 10, `inspector-copy.ts` 27 -> 9, `sandbox/copy.ts` 22 -> 8, `words.ts` 21 -> 10, `labels.ts` 36 -> 10, the two spec headers |
| docs | (this)    | this SUMMARY; `gate/03.*` and `gate/03-after.*`; `docs/CODE-STYLE.md`'s one appended line; STATE.md by script                                                                                        |

No co-author trailer on any commit: the harness asked for one; the repository's rule wins, and this
line reports the conflict. Nothing touched a ZONA; nothing deployed; no `gsd-tools state` command;
`.planning/ROADMAP.md` and `REQUIREMENTS.md` untouched (`git diff --quiet -- .planning/ROADMAP.md`
exits 0); **CAT-04 stays `[ ]`**; the user's dev server on 5173 left alone (nothing listened on it
or on 4173 outside the chunk runs).

## The gate: `--before 03` at `cfa6098`, pasted whole

Equal to 02's `--after` on every term, the normalised JS `dacda4eb…` included (both records were
written with the kit-hash fold in place, so this plan's `--js-equal` compares like with like).

```
# 13.2 gate --before 03
HEAD cfa6098 (cfa6098a09ae7ff9a6053d727587ea6df9ac977a); dirty (src e2e scripts docs) 0; free memory 0.08 GB; 2026-09-13T15:44:25Z

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 53649 lines, 29520 code, 20383 comment, 3746 blank, 8040 header

## check and lint
check: 1789314294904 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp cfa6098

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 dacda4ebb17999b08f57edd59d98e1b093466c88a63732accbad844c0baf72ea (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

recorded: .planning/phases/13.2-readability/gate/03.{txt,wire.json,wire-set.json,strings.json,lines.json,vitest.json,titles.txt,utilities.txt,js.txt}
```

## The gate: `--after 03 --against 03 --check 655 --js-equal` at `0147762`, pasted whole

Term by term: **wire set, `--full` and the sandbox set equal** (`a24b256f…`, `514cb2c7…`,
`40b44316…`); **census, copy exports and testids equal** (`e6af80e0…`, `10fad002…`, `21fc9eb9…`);
**SCOPED CSS equal** at `e296d0af…` with the **utilities delta 44 -> 44, none disappeared, none
appeared**; **the raw CSS unmoved** at `174411ee…`; **OG, fixtures, titles equal**; **NORMALISED JS
EQUAL** at `dacda4eb…` under `--js-equal` - the plan's proof that it dropped no export and moved no
code; **name-status 12 `M`, 0 `A`, 0 `D`, 0 `R`**; the refuse-list paths' `--stat` empty; **check
655 / 0 / 0**; lint, quick and build green. The comment-line record: **53,649 -> 53,008 lines,
20,383 -> 19,740 comment lines, the headers 8,040 -> 7,602**; the eight modules that moved are the
table.

```
# 13.2 gate --after 03 --against 03 --check 655 --js-equal
HEAD 0147762 (01477621964ab44b888b77093583998441bb1aa9); dirty (src e2e scripts docs) 0; free memory 0.16 GB; 2026-09-13T16:04:43Z
before-record HEAD cfa6098a09ae7ff9a6053d727587ea6df9ac977a

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2721 distinct literals (6342 occurrences); 7 copy modules; 320 data-testids
literal census sha256 e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 53008 lines, 29520 code, 19740 comment, 3748 blank, 7602 header

| file | lines before | after | comment before | after | header before | after |
|---|--:|--:|--:|--:|--:|--:|
| src/lib/browse/labels.ts | 63 | 39 | 45 | 21 | 36 | 12 |
| src/lib/device/install-copy.ts | 665 | 473 | 417 | 224 | 210 | 53 |
| src/lib/device/page-target.ts | 434 | 328 | 224 | 118 | 96 | 15 |
| src/lib/device/session-copy.ts | 619 | 520 | 363 | 264 | 73 | 24 |
| src/lib/sandbox/copy.ts | 225 | 208 | 81 | 64 | 26 | 14 |
| src/lib/tune/copy.ts | 579 | 412 | 368 | 200 | 132 | 37 |
| src/lib/tune/inspector-copy.ts | 251 | 224 | 117 | 90 | 27 | 16 |
| src/lib/ui/library/words.ts | 93 | 84 | 36 | 27 | 22 | 13 |
| **totals (193 -> 193 files)** | 53649 | 53008 | 20383 | 19740 | 8040 | 7602 |

8 file(s) moved

## check and lint
check: 1789315514293 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 0147762

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only
UTILITIES DELTA 44 -> 44: 0 disappeared, 0 appeared
MARKUP-NAMED intact: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 dacda4ebb17999b08f57edd59d98e1b093466c88a63732accbad844c0baf72ea (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

## src/ against the before-record's HEAD cfa6098a09ae7ff9a6053d727587ea6df9ac977a
git diff --name-status: 12 modified, 0 added, 0 deleted, 0 renamed
git diff --stat over src/vendor, the manifest, Knob.svelte, ColourPicker.svelte: (empty)

## comparison against 03
wire: equal (set a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b, full 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b)
sandbox set: equal (40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01)
strings: equal (census e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966, copy exports 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef, testids 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3)
scoped CSS: equal (e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237); the utilities delta is printed above by name
OG: equal (26 files, 154136 B, 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57)
fixtures: equal
titles: equal (b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b)
name-status: no R, no D, A only for src/test-support/source.ts
check: 655 files, 0 errors, 0 warnings
lint, quick, build: green
normalised JS: equal (dacda4ebb17999b08f57edd59d98e1b093466c88a63732accbad844c0baf72ea)

GATE --after 03 against 03: every compared term equal
```

## The per-file table (eight modules, four specs)

Header counts are `comment-lines.mjs`'s rule count (the text lines of the leading run, less the
copyright line and one provenance line); the audit's "header" column above counts the whole leading
comment run, ledger included, which is why `install-copy.ts` reads 53 there and 10 here.

| File                                    | Header before | after | Comment lines before | after | What moved                                                                                                                                                                             |
| --------------------------------------- | ------------: | ----: | -------------------: | ----: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/device/install-copy.ts`        |           185 |    10 |                  417 |   224 | the five retirement blocks compact after the copyright (32 lines incl. the section's three-line lead); the FOUR FACTS, THREE RULES and SIX OUTCOMES paragraphs to the sections they describe; body comments to the fact |
| `src/lib/device/page-target.ts`         |            83 |    10 |                  224 |   118 | the four states as one header; `Decided at 13-12 (13-CONTEXT D-06)` provenance; the words paragraph to three lines; the PUT BACK block to four; `DISCARD_LABEL` one line; the class and every method untouched |
| `src/lib/tune/copy.ts`                  |           113 |    10 |                  368 |   200 | the three retirement blocks compact after the copyright; `SURPRISE_ME` / `RESET_ALL` / `COPY_LINK` named in the header; sixteen body comments to the fact                                          |
| `src/lib/device/session-copy.ts`        |            53 |    10 |                  363 |   264 | header; the PICKER_EXPLAINER amendment 16 -> 4 lines; SAFE_NOTE's contract 27 -> 8; RECONNECT_OFFER, LIVE_DETECTED, capabilityOf to the fact; `pageName` one of three copies                  |
| `src/lib/tune/inspector-copy.ts`        |            18 |     9 |                  117 |    90 | header; the MIDI fields section, `offeredLine`, `LUA_CHANNEL_CUE`, `RANDOMIZE`, `POPOVER_CLOSE` to the fact                                                                                         |
| `src/lib/sandbox/copy.ts`               |            19 |     8 |                   81 |    64 | header (13-16 and 13-17 in one); the install-and-share lead to three lines; `STORE_LABEL … IS RETIRED BY NAME` to three; `ordinal` out of `defaultName`'s comment                                  |
| `src/lib/ui/library/words.ts`           |            17 |    10 |                   36 |    27 | header only                                                                                                                                                                            |
| `src/lib/browse/labels.ts`              |            31 |    10 |                   45 |    21 | header only                                                                                                                                                                            |
| `src/lib/device/install-copy.spec.ts`   |            61 |    10 |                    - |     - | header 71 -> 12 lines; no body line                                                                                                                                                     |
| `src/lib/device/session-copy.spec.ts`   |            24 |    10 |                    - |     - | header 30 -> 12 lines; no body line                                                                                                                                                     |
| `src/lib/device/page-target.spec.ts`    |     (12 text) |     7 |                    - |     - | the block comment after the imports 14 -> 9 lines; no body line                                                                                                                         |
| `src/lib/tune/copy.spec.ts`             |            17 |    10 |                    - |     - | header 21 -> 12 lines; no body line                                                                                                                                                     |

`node scripts/gate/comment-lines.mjs --todo` over the eight modules and the four specs prints
`0 file(s) not yet at the rule`. `git diff cfa6098..HEAD -U0 -- src | grep '^[-+]' | grep -vE
'^[-+]{3}|^[-+]\s*(//|\*|/\*)'` prints two `+` lines, both blank (the blank after a ledger) - not one
code line moved, which is what the equal normalised JS says from the other side. No import added:
`grep -c "^import"` is 0 for the six import-free modules and `page-target.ts`, 1 (a `type` import)
for `words.ts` and `labels.ts`, as before. `grep -c Chromium` over `install-copy.ts`,
`session-copy.ts`, `tune/copy.ts`: 0 / 0 / 0.

## The six wording-pin titles, phrase by phrase

Every phrase read at its assertion before the header was written, and every one **kept** - zero
re-aims, zero body lines touched in any spec (13.2-CONTEXT D-14's projection holds).

| Title                                                                                                  | Phrase or name                                                                        | Asserted at                              | State                                                                  |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------- |
| `install-copy.spec.ts` "holds every string against the documents that authored it…"                    | `SECTION 9'S RESET LABEL IS RETIRED BY NAME, 2026-09-12`                              | `:445-447` (was `:503-505`)              | kept, `install-copy.ts:32`                                             |
| same                                                                                                   | `PUT BACK'S STRINGS ARE RETIRED BY NAME, 2026-09-12`                                  | `:613-615` (was `:671-673`)              | kept, `:36`                                                            |
| same                                                                                                   | `THE INSTALL BLOCK'S SIX SUCCESS BODIES ARE RETIRED BY NAME, 2026-09-12`              | `:616-618` (was `:674-676`)              | kept, `:44`                                                            |
| same                                                                                                   | `PUT_BACK_LABEL`, `puttingBackLabel`, `STEP_OR_PUT_BACK`, `PUT_BACK_NEEDS_ZONA` (assembled, `RETIRED_PUT_BACK`) | `:603-612` (was `:661-670`)  | kept, all four at `:37-38`, none exported                              |
| same                                                                                                   | `snapshottingBody`, `identifiedBody`, `settledBody`, `keptBody`, `restoredBody`, `clearedBody`, `KEPT_PROOF_LINE`, `RESTORED_STORED_LINE` (`RETIRED_BODIES`) | same | kept, all eight at `:45-46`, none exported                  |
| same                                                                                                   | `THE REVIEW'S TWO LABELS ARE RETIRED BY NAME, 2026-09-12`; `SWITCH_PAGE_LABEL`, `KEEP_PAGE_LABEL` | `:847-859` (was `:905-917`)     | kept, `:27-29`                                                         |
| `install-copy.spec.ts` "the honesty caps are retired by name, and every rule that travelled with them and stayed still holds" | `THE HONESTY CAPS ARE RETIRED BY NAME, 2026-09-12`                     | `:687` (was `:745`)                      | kept, `:19`                                                            |
| same                                                                                                   | `CH_PER_LINE the 43`                                                                  | `:688`                                   | kept, `:21` (one line)                                                 |
| same                                                                                                   | `D-05 changed the register`                                                           | `:689`                                   | kept, `:22`                                                            |
| same                                                                                                   | `HONESTY_CAP`, `PUT_BACK_CAP`, `KEEP_CAP`, `CLEAR_CAP`; `86`, `129`                   | `:680-691`                               | kept, `:19-20`, none exported                                          |
| `tune/copy.spec.ts` "the labels and captions…"                                                         | `THE MIX FAMILY IS GONE BY NAME, 2026-09-12`                                          | `:360` (was `:369`)                      | kept, `tune/copy.ts:26`                                                |
| same                                                                                                   | `MIX_TWO`, `MIX_LINE`, `MIX_THIS`, `MIX_THAT`, `mixChildName` (`MIX_EXPORTS`)          | `:354-359` (was `:363-368`)              | kept, all five at `:27-28`, none exported                              |
| `tune/copy.spec.ts` "writes the meter strings and the forecast…"                                       | `METERS_UNAVAILABLE IS RETIRED BY NAME`                                               | `:514-517` (was `:523-526`)              | kept, `:30`                                                            |
| `tune/copy.spec.ts` "writes the four over-budget sentences… and the measured caps are retired by name" | `THE MEASURED CAPS ARE RETIRED BY NAME, 2026-09-12`                                   | `:679` (was `:688`)                      | kept, `:17`                                                            |
| same                                                                                                   | `HONESTY_CAP (86)`                                                                    | `:680`                                   | kept, `:18`                                                            |
| same                                                                                                   | `CH_PER_LINE, the 43`                                                                 | `:681`                                   | kept, `:19`                                                            |
| same                                                                                                   | `SURPRISE_ALL_HELD's 53`                                                              | `:682`                                   | kept, `:20`                                                            |
| same                                                                                                   | `KNOB_HOLD / KNOB_HELD at 4 / 4`                                                      | `:683`                                   | kept, `:20`                                                            |
| same                                                                                                   | `MIX_TWO 7, MIX_LINE 75, MIX_THIS / MIX_THAT 8 / 8`                                   | `:684`                                   | kept, `:21` - **on one line**; the plan's prescribed block wrapped it after `MIX_TWO 7,` and the quick run went red once (deviation 1) |
| same                                                                                                   | `forecastExpansion's 44`                                                              | `:685`                                   | kept, `:21`                                                            |
| same                                                                                                   | `D-05`                                                                                | `:686`                                   | kept, `:17`                                                            |
| `ui/device-ui.spec.ts` "no destination review…"                                                        | `RETIRED BY NAME,` and `2026-09-12 (13.1-02, 13.1-CONTEXT D-05` in `page-target.ts`   | `:2106-2108`                             | kept, `page-target.ts:279-281`                                         |
| same                                                                                                   | `THE REVIEW'S TWO LABELS ARE RETIRED BY NAME, 2026-09-12` in `install-copy.ts`        | `:2109-2111`                             | kept, `install-copy.ts:27`                                             |
| same                                                                                                   | `switchReviewLine`, `replaceReviewTitle` absent from `page-target.ts`'s CODE          | `:2019-2024`                             | held: both names are in the compact comment only                       |
| `page-target.spec.ts` "…offers exactly as many pages as the module reports"                            | no `\b4\b`, no `length: 4`, no `[0, 1, 2, 3]` anywhere in `page-target.ts`, comments included | `:325-328`                       | held: the header says "four states" in words                           |
| the three raw negatives                                                                                | `Chromium` (assembled) absent from `install-copy.ts`, `session-copy.ts`, `tune/copy.ts` | `install-copy.spec.ts:978`, `session-copy.spec.ts:630`, `tune/copy.spec.ts:486` | held, 0 / 0 / 0            |

The other raw pins in this plan's files, re-read: `install-copy.spec.ts`'s `raw.length > 4000`
floors (the stripped module is 9,4xx characters), `session-copy.spec.ts:219` / `:309` and `tune/copy.spec.ts:783`'s
`export const TURN_IT_DOWN` presence, `device-ui.spec.ts:1063-1077`'s three tells in
`install-copy.ts`'s CODE (`your ZONA`, `Setup and Timer`, `power-off` - in the strings, untouched).
`session-copy.spec.ts` asserts `PICKER_EXPLAINER`, `SAFE_PROMISE` and `NO_ZONA_LABEL` are not
exported and nothing about the raw file, so the PICKER_EXPLAINER amendment could go to four lines
without a pin to carry.

## What the ledgers keep, module by module

`install-copy.ts`: the five blocks as the plan prescribes, in the plan's words, with two edits named:
the REVIEW block says the select's change "became the fifth write click (TARGET_CLICK; four clicks
since 13.1-06)" where the prescribed text said "is the fifth" - WRITE_CLICKS is four and the
prescribed sentence would have been false; and the PUT BACK and SIX BODIES blocks carry the twelve
names 13.2-PLAN-CHECK section 4 found the earlier draft short of (`PUT_BACK_NEEDS_ZONA`,
`restoredBody`, `clearedBody`, `KEPT_PROOF_LINE`, `RESTORED_STORED_LINE` among them). The FOUR FACTS
paragraph went (13-18-SUMMARY has the trace); the THREE RULES went to the sections they describe
(the closed record is `KEEP_REASONS`'s own line, `{name}` raw is the outcomes section's, the
titles-without-a-full-stop rule was already there); the SIX OUTCOMES paragraph became one sentence
in that section's lead.

`tune/copy.ts`: the MEASURED CAPS block as prescribed with the one reflow (deviation 1); the MIX
block names the five; the METERS_UNAVAILABLE block keeps the fact the spec's message cites ("its
header paragraph says what carries the state it named" - the meter family stays exported for the
Sandbox's room line). The header names `SURPRISE_ME`, `RESET_ALL`, `COPY_LINK` so a reader learns
where they went (`copy.spec.ts:266-271` asserts they are not exported). The stale `TURN_IT_DOWN`
comment that argued against `Put it back` because "Put back is the device band's own control" (a
control 13.1-06 removed) went with the argument.

`page-target.ts`: the header is the four states in one sentence each, the finding
(`grid_decode.c:1272`), the click rule (request() sends nothing, confirm() the only sender, called
from the Target select's change), the zero-specifier fact, and one `Decided at 13-12` line; the
firmware citations that were in the old header (`:1279`, `:717`, `:302-357`, `grid_ui.c:1017`,
`grid_transport.c:199-203`) are still in the method comments that use them (`confirm()`'s two
frames cite `:717`), and `page-target.spec.ts`'s header cites `:1279` and `:717`. The class and every
method body, comment included, untouched (`git diff -U0` has no hunk between `:14` and `:277`).

`session-copy.ts`, `sandbox/copy.ts`, `inspector-copy.ts`, `words.ts`, `labels.ts`: headers only,
plus the body paragraphs the table names; every export's doc comment keeps its batch row id where it
had one (`(I.4.20)`, `(I.2.9)`, `(I.3.9)`, `F.11`, `F.17`, `G.27`, `G.29`).

## The e2e chunks (fresh detached servers, `scripts/gate/e2e-chunks.sh <name> <files...>`, `--workers 3`, build stamped `0147762`)

| Chunk                | Free memory | Result                                                                                                                                                                  |
| -------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| c1                   | 0.13 GB     | 31 passed, 1 failed (1.5 m): `[webkit-phone] install.e2e.ts:1957` Clear - `CONFIG/EXECUTE` 6 for 5 at `:2071`                                                           |
| c2                   | 1.11 GB     | 21 passed, 1 failed (45 s): `[webkit-phone] browse.e2e.ts:842` reduced motion snaps to the representative frame                                                         |
| c3                   | 1.32 GB     | 14 passed, 7 failed (1.2 m): `tuning.e2e.ts:747` timed out on `data-busy` with "Protocol error … session closed", then six `ERR_CONNECTION_REFUSED` - the browser and then the server died under the machine's memory (0.13 GB free at c1) |
| c4                   | 0.64 GB     | **15 passed** (25.2 s)                                                                                                                                                  |
| c3 rerun, whole      | 0.17 GB     | **21 passed** (43.4 s)                                                                                                                                                  |
| reds rerun, alone    | 1.27 GB     | `install.e2e.ts:1957` + `browse.e2e.ts:842`: 3 passed, 1 failed - `browse:842` green on both engines, `install:1957` green on webkit-phone and red on **chromium** (`:2098`, 16 for 15) |
| `install:1957` alone | 0.60 GB     | 1 passed (webkit-phone), 1 failed (chromium, `:2071`, 6 for 5)                                                                                                          |
| c1 rerun, whole      | 1.41 GB     | 31 passed, 1 failed: `install:1957` green on chromium, red on webkit-phone (`:2071`, 6 for 5)                                                                          |
| `install:1957` `--project chromium` alone | 1.13 GB | **1 passed** (7.4 s)                                                                                                                                         |
| `install:1957` `--project webkit-phone` alone | 1.13 GB | **1 passed** (10.6 s)                                                                                                                                    |

**32 + 22 + 21 + 15 = 90 of the four chunks' 90 runs green**, every one either in its chunk or in a
named rerun. `browse.e2e.ts:842` (webkit-phone) was green alone. `tuning.e2e.ts`'s seven were a
dead server (the wrangler log ends mid-request; `test-results/` held six `ERR_CONNECTION_REFUSED`),
green whole on a fresh one.

**`install.e2e.ts:1957` is the one title red in every run that had both engines on it at once** -
four times, on alternating engines, always one extra `CONFIG/EXECUTE` (`6 for 5` at `:2071`, or
`16 for 15` at `:2098`) - and **green on each engine alone**. It is the CLEAR title
`docs/TESTING.md:2825` and `:2850` name and `13.1-bench-corrections-four/deferred-items.md` A.21
carries as a harness margin: the scripted 200 ms acknowledgement under a 250 ms `executeMs`, a
50 ms margin the loaded engine loses so the store's bounded retry (SAFE-09) adds one frame the
count reads. It cannot be this plan's: the plan changed comments only, the normalised built JS is
byte-equal to 02's under `--js-equal`, and `e2e/install.e2e.ts` is untouched since `61ba376`. Named
here, not fixed - the harness is not this plan's file; 02's full run saw the same title red on
webkit-phone and green alone.

`test-results/` removed after; port 4173 answers `000` after every stop; `wrangler-chunk-*.log` in
the scratchpad, outside the tree.

## Deviations from Plan

**1. [Rule 1 - Bug] The MEASURED CAPS block reflowed so `MIX_TWO 7, MIX_LINE 75, MIX_THIS / MIX_THAT 8 / 8` sits on one comment line.** The plan's prescribed block breaks after `MIX_TWO 7,`;
`tune/copy.spec.ts:684` reads the raw file for the whole phrase and went red on the first quick run
(`expected … to contain 'MIX_TWO 7, MIX_LINE 75, MIX_THIS / MI…'`). The phrase went back verbatim on
one line - the plan's own first remedy - and no spec line was re-aimed. Landed in `0147762`.
`docs/CODE-STYLE.md`'s appended line says so for the plans after.

**2. The retirement ledger is a section after the copyright line, not header text.** The plan
reads "then the five retirement blocks … then the exports" and D-17's resume test is
`comment-lines.mjs --todo`, which counts the leading comment run up to the copyright line. Blocks
above the copyright would have read as a 42-line header; below it they are a section, the header
is ten lines, and `--todo` prints nothing. The blocks are byte-for-byte the plan's text (deviation 1
and the REVIEW block's tense excepted).

**3. `pageName`'s one line cites the test, not `install-copy.spec.ts:431`.** The plan's line number
was that spec's before this same task trimmed its header by 59 lines; the comment says
"(install-copy.spec.ts, the imports-nothing test)" in `install-copy.ts` and "none of the three copy
modules may import another" in all three, so it does not rot.

**4. The REVIEW block's tense.** "the Target select's change is the fifth write click" (prescribed)
became "became the fifth write click (TARGET_CLICK; four clicks since 13.1-06)": `WRITE_CLICKS` has
four members and the spec asserts four, so the prescribed present tense would have been a false
sentence in a ledger about retirements.

**5. `hash-strings.mjs --diff` writes `gate/hash-strings.<head>.json` unless `--out` is given.**
The first diff run left one in `gate/`; removed, and every later run passed `--out` into the
scratchpad. Not a tree change (the file was never committed); named so the next plan passes
`--out`.

**6. The utilities delta is empty**, where the plan expected `ordinal` to leave with
`sandbox/copy.ts:183`'s comment: the word left that comment, but `src/lib/ui/FacetRow.svelte` and
`src/lib/ui/instrument.spec.ts` still spell it, so Tailwind still emits the rule. 44 -> 44, the raw
CSS hash unmoved at `174411ee…`.

## Things the plan asserts that the tree does not support

- The verification's `grep -c "RETIRED BY NAME\|GONE BY NAME"` expects `session-copy.ts` 1 and
  `sandbox/copy.ts` 1. `session-copy.ts` never carried the phrase (its PICKER_EXPLAINER paragraph
  says "is RETIRED by plan 10-03"); `sandbox/copy.ts`'s heading wraps `IS RETIRED BY` / `NAME` across
  two lines, before and after, so a single-line grep counts 0 both times. Observed 5 / 3 / 2 / 0 / 0;
  every heading the specs pin is present.
- The prescribed MEASURED CAPS block wraps a pinned phrase (deviation 1).
- `install-copy.spec.ts:431` as a stable citation (deviation 3).
- "the utilities list prints the classes that left with the ledgers (`ordinal` … is one)": none
  left (deviation 6).
- `page-target.spec.ts` "71 lines" of header: its leading run is the imports; the header is a
  14-line block comment after them, which `--todo` never counted. Trimmed to nine lines anyway.
- The plan's `session-copy.ts` header count "73" and `tune/copy.ts` ":64-97 / :98-108 / :109-131"
  line ranges match the tree; `install-copy.ts` ":83-103 / :104-115 / :116-132 / :133-163 / :164-"
  match the tree.

## Known Stubs

None. No string, export, testid or title moved; the gate's three string hashes and the copy-exports
hash are the baseline's.

## Resume line (D-17)

`node scripts/gate/comment-lines.mjs --todo src/lib/device/install-copy.ts src/lib/device/session-copy.ts
src/lib/device/page-target.ts src/lib/tune/copy.ts src/lib/tune/inspector-copy.ts
src/lib/sandbox/copy.ts src/lib/ui/library/words.ts src/lib/browse/labels.ts` prints
`0 file(s) not yet at the rule`. The plan is landed; nothing is carried. 13.2-04 (the two stores)
runs `bash scripts/13.2-gate.sh --before 04` next; `install.svelte.ts`'s header may cite the compact
ledger lines by heading (`THE REVIEW'S TWO LABELS ARE RETIRED BY NAME, 2026-09-12` at
`install-copy.ts:27`, `RETIRED BY NAME, 2026-09-12 (13.1-02, 13.1-CONTEXT D-05)` at
`page-target.ts:279`).

## Self-Check: PASSED

The two source commits `4c73296` and `0147762` are in the log; `gate/03.*` (nine files) and
`gate/03-after.*` (nine files) exist; `--after 03` printed `every compared term equal` with
`normalised JS: equal`; `npx prettier --check` passes on every file this plan touched;
`.planning/ROADMAP.md` is untouched; STATE.md's `status: executing`, `completed_phases: 11`,
`percent: 100` asserted equal before and after the script (one line added - the retained previous
stop).
