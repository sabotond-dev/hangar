---
phase: 13.2-readability
plan: 03
type: execute
wave: 3
depends_on: ["13.2-02"]
# Execution order (13.2-VALIDATION "Why the phase is serial"): SERIAL - 01 -> 02 -> 03 -> 04 -> 05 -> 06.
# THIRD: the copy modules, whose headers are the retirement ledger ten spec titles read as text, and
# page-target.ts. Before 04 (the store's header cites these lines) and before 05 (TuningRegion cites
# tune/copy.ts). Strings: the census does not move by one occurrence.
files_modified:
  - src/lib/device/install-copy.ts
  - src/lib/device/session-copy.ts
  - src/lib/device/page-target.ts
  - src/lib/tune/copy.ts
  - src/lib/tune/inspector-copy.ts
  - src/lib/sandbox/copy.ts
  - src/lib/ui/library/words.ts
  - src/lib/browse/labels.ts
  - src/lib/device/install-copy.spec.ts
  - src/lib/device/session-copy.spec.ts
  - src/lib/device/page-target.spec.ts
  - src/lib/tune/copy.spec.ts
  - docs/CODE-STYLE.md
  - .planning/phases/13.2-readability/gate/
  - .planning/STATE.md
autonomous: true
requirements: [] # none - a refactor claims nothing

must_haves:
  truths:
    - "Every copy module opens with at most ten lines saying what it is, that it imports nothing and why, and where the register and the ledger are; every RETIRED BY NAME paragraph is its first line verbatim plus at most three lines of names, numbers and a pointer."
    - "The six titles that pin phrases in these files - install-copy.spec.ts:454 and :738, tune/copy.spec.ts:250, :506 and :641, device-ui.spec.ts:1969 - are green with zero body edits, or with the re-aimed lines named by title and line."
    - "The literal census, the copy-exports hash and the testid hash are the baseline's: not a string, not an export, moved."
    - "pageName stays in three places, each with one line saying why (D-05 / D.6)."
  artifacts:
    - path: "src/lib/device/install-copy.ts"
      provides: "The five retirement blocks compact, every pinned phrase carried; the header at ten lines"
      contains: "THE HONESTY CAPS ARE RETIRED BY NAME, 2026-09-12"
    - path: "src/lib/tune/copy.ts"
      provides: "The three retirement blocks compact; the register's rules as a short list"
      contains: "THE MEASURED CAPS ARE RETIRED BY NAME, 2026-09-12"
    - path: "src/lib/device/page-target.ts"
      provides: "The four-state envelope in ten lines; the two retirement lines"
      contains: "2026-09-12 (13.1-02, 13.1-CONTEXT D-05"
  key_links:
    - from: "src/lib/device/install-copy.spec.ts"
      to: "src/lib/device/install-copy.ts"
      via: "raw.includes(cap) for each RETIRED_CAPS name, and the four pinned phrases"
      pattern: "CH_PER_LINE the 43"
    - from: "src/lib/ui/device-ui.spec.ts"
      to: "src/lib/device/page-target.ts"
      via: "targetRaw.toContain"
      pattern: "RETIRED BY NAME,"
---

<objective>
The eight modules that hold the site's words, and whose headers hold the site's retirement ledger:
brought to the rule without moving a string, an export or a pinned phrase. Every `RETIRED BY NAME,
<date>` heading survives as its first line; every number a spec asserts survives in a compact block
beside its name; the paragraphs that re-argue the register go to the SUMMARYs that already hold them.

Purpose: a reader opening `install-copy.ts` should meet the register's rules in ten lines and the
strings straight after, not two hundred lines of what used to be exported.
Output: eight modules under the rule; four copy specs' headers under the rule; zero expected re-aims
(13.2-CONTEXT D-14) and every actual one named. Term **+0 / +0; e2e +0 / +0; check 655 (+0)**; the
sweep run (`tune/copy.ts`, `inspector-copy.ts`); chunks c1 c2 c3 c4.

**Execution order.** THIRD, after 13.2-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-readability/13.2-CONTEXT.md
@.planning/phases/13.2-readability/13.2-VALIDATION.md
@.planning/phases/13.2-readability/13.2-02-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@docs/CODE-STYLE.md
@src/lib/device/install-copy.ts
@src/lib/device/session-copy.ts
@src/lib/device/page-target.ts
@src/lib/tune/copy.ts
@src/lib/tune/inspector-copy.ts
@src/lib/sandbox/copy.ts
@src/lib/ui/library/words.ts
@src/lib/browse/labels.ts
@src/lib/device/install-copy.spec.ts
@src/lib/tune/copy.spec.ts
@src/lib/ui/device-ui.spec.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution and no co-author trailer (the
  repository's rule wins; the SUMMARY reports the conflict). No emojis.
- **No device, no deploy; sibling repositories read-only; the dev server on 5173 left alone.**
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state`.** STATE.md by script against a copy, asserting `status: executing`,
  `completed_phases: 11`, `percent: 100` unchanged. ROADMAP not edited. **CAT-04 `[ ]`.**
- **Never `prettier --write .`** — prettier on every file this plan touches, by path.
- **`git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on the four docs — none is this plan's.
- **The refuse-list, absolute**: every user-facing literal — these files are made of them; `node
  scripts/gate/hash-strings.mjs --diff gate/03.strings.json` prints no `-` or `+` line after every
  file; the copy-exports hash is equal (no export added, removed or renamed — `DISCARD_LABEL` stays,
  `overLine` stays, `CUSTOM_SURFACE` keeps its `export`).
- **The "imports nothing at all" rule** (`install-copy.spec.ts:431`, `session-copy.spec.ts:235`,
  `copy.spec.ts:726`): no import is added to any of the eight; `pageName` stays three times.
- **No test title touched; the ten wording-pin titles hold** — the quick suite after EVERY file in
  this plan (13.2-RESEARCH pitfall 10), not after the plan.
- **No comment in `install-copy.ts`, `session-copy.ts` or `tune/copy.ts` spells the engine name
  `Chromium`** — three raw negatives assembled from fragments (`install-copy.spec.ts:171` `ENGINE`,
  `session-copy.spec.ts:79` / `:654`, `tune/copy.spec.ts:146` / `:501`); the browsers-that-work fact
  is named without the engine's word, as the strings are.
- **Comments only**: this plan drops no `export` and moves no code, so the gate's NORMALISED JS hash
  is asserted equal (`--after 03 … --js-equal`); a moved JS hash on this plan is a code change to find.
- **Every count a carried name plus a delta**: `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4
  19 / allowlist 0`. Term **+0 / +0; +0 / +0; check +0**. `check-counts.mjs 94 966`.
- **The gate brackets the plan**: `--before 03` first, `--after 03 --against 03 --check 655` last.
- **Quick at `--maxWorkers=2`.** Chunks **c1 c2 c3 c4** on fresh detached servers, reds rerun alone.
- **Re-runnable (D-17)**: the remaining list is `node scripts/gate/comment-lines.mjs --todo <the eight modules>`.
</standing_rules>

<interfaces>
The pinned phrases in this plan's files, by title (13.2-RESEARCH A.7; 13.2-CONTEXT D-14), every one of
which must survive verbatim in the compact text below or be re-aimed and named:

| Title | Phrases pinned |
|---|---|
| `install-copy.spec.ts:454` holds every string against the documents that authored it… | `SECTION 9'S RESET LABEL IS RETIRED BY NAME, 2026-09-12`; `PUT BACK'S STRINGS ARE RETIRED BY NAME, 2026-09-12`; `THE INSTALL BLOCK'S SIX SUCCESS BODIES ARE RETIRED BY NAME, 2026-09-12`; `THE REVIEW'S TWO LABELS ARE RETIRED BY NAME, 2026-09-12` |
| `install-copy.spec.ts:738` the honesty caps are retired by name, and every rule that travelled with them and stayed still holds | `THE HONESTY CAPS ARE RETIRED BY NAME, 2026-09-12`; `CH_PER_LINE the 43`; `D-05 changed the register`; the names `HONESTY_CAP`, `PUT_BACK_CAP`, `KEEP_CAP`, `CLEAR_CAP`; the numbers `86` and `129` |
| `install-copy.spec.ts:454` (same title), asserted at `:667-676` from fragments the literal audit could not see | `RETIRED_PUT_BACK` (`:208-213`): `PUT_BACK_LABEL`, `puttingBackLabel`, `STEP_OR_PUT_BACK`, `PUT_BACK_NEEDS_ZONA`; `RETIRED_BODIES` (`:215-224`): `snapshottingBody`, `identifiedBody`, `settledBody`, `keptBody`, `restoredBody`, `clearedBody`, `KEPT_PROOF_LINE`, `RESTORED_STORED_LINE` — each must be present in the raw file and absent from the exports |
| `tune/copy.spec.ts:250` the labels and captions… | `THE MIX FAMILY IS GONE BY NAME, 2026-09-12`; and at `:369-373` every name of `MIX_EXPORTS` (`:167-173`): `MIX_TWO`, `MIX_LINE`, `MIX_THIS`, `MIX_THAT`, `mixChildName` — present in the raw file, absent from the exports |
| `tune/copy.spec.ts:506` writes the meter strings and the forecast… | `METERS_UNAVAILABLE IS RETIRED BY NAME` |
| `tune/copy.spec.ts:641` writes the four over-budget sentences… and the measured caps are retired by name | `THE MEASURED CAPS ARE RETIRED BY NAME, 2026-09-12`; `HONESTY_CAP (86)`; `CH_PER_LINE, the 43`; `SURPRISE_ALL_HELD's 53`; `KNOB_HOLD / KNOB_HELD at 4 / 4`; `MIX_TWO 7, MIX_LINE 75, MIX_THIS / MIX_THAT 8 / 8`; `forecastExpansion's 44`; `D-05` |
| `ui/device-ui.spec.ts:1969` no destination review… | in `page-target.ts`: `RETIRED BY NAME,` and `2026-09-12 (13.1-02, 13.1-CONTEXT D-05`; in `install-copy.ts`: `THE REVIEW'S TWO LABELS ARE RETIRED BY NAME, 2026-09-12` |

The compact blocks, prescribed (the executor writes these, not a paraphrase; the first line of each
is the heading as it stands today up to its first full stop):

`install-copy.ts` (replacing `:83-103`):
```
// THE HONESTY CAPS ARE RETIRED BY NAME, 2026-09-12 (13-18, D-05). HONESTY_CAP
// (86), PUT_BACK_CAP (129), KEEP_CAP (86) and CLEAR_CAP (86) were `lines x
// CH_PER_LINE`, with CH_PER_LINE the 43 characters of one Body line in Phase
// 10's 372px column. D-05 changed the register and the column is gone; the
// rules that travelled with the caps - the register, the punctuation, no
// paraphrase, Z-08, A-48 - still hold over every export (install-copy.spec.ts).
// See .planning/phases/13-gui-overhaul/13-18-SUMMARY.md.
```
(replacing `:104-115`):
```
// THE REVIEW'S TWO LABELS ARE RETIRED BY NAME, 2026-09-12 (13.1-02,
// 13.1-CONTEXT D-05; ledgered in 13.1-COPY-NEW.md): SWITCH_PAGE_LABEL and
// KEEP_PAGE_LABEL, the destination review's pair; the Target select's change
// is the fifth write click (TARGET_CLICK).
```
(replacing `:116-132`):
```
// SECTION 9'S RESET LABEL IS RETIRED BY NAME, 2026-09-12 (13.1-05,
// 13.1-CONTEXT D-04; ledgered): CLEAR_LABEL is `Clear`, the user's word; the
// fact section 9's label carried is clearLine's.
```
(replacing `:133-163`):
```
// PUT BACK'S STRINGS ARE RETIRED BY NAME, 2026-09-12 (13.1-06, 13.1-CONTEXT
// D-06 and D-07; ledgered): PUT_BACK_LABEL, puttingBackLabel, STEP_OR_PUT_BACK
// (stepOrClear replaces it) and PUT_BACK_NEEDS_ZONA (NEEDS_ZONA is the same
// sentence); WRITE_CLICKS is FOUR; every way-back sentence names Clear. The
// snapshot and the store's putBack() stay for the /dev/install/ probe;
// restoredCaption and RESTORED_UNCONFIRMED_TITLE are its words.
// See .planning/phases/13.1-bench-corrections-four/13.1-06-SUMMARY.md.
```
(replacing `:164-` to the end of that paragraph):
```
// THE INSTALL BLOCK'S SIX SUCCESS BODIES ARE RETIRED BY NAME, 2026-09-12
// (13.1-06, D-06; ledgered): snapshottingBody, identifiedBody, settledBody,
// keptBody, restoredBody, clearedBody, KEPT_PROOF_LINE and RESTORED_STORED_LINE;
// STILL_WRITING_LINE is NOT retired (SAFE-08's visible half, the zone's); the
// bar's device clause (device-clause.ts) carries every success caption.
```

`tune/copy.ts` (replacing `:64-97`):
```
// THE MEASURED CAPS ARE RETIRED BY NAME, 2026-09-12 (13-19, D-05), in the
// same move 13-18 made for install-copy.ts: HONESTY_CAP (86) held
// tryOnBudgetReason from outside (2 x CH_PER_LINE, the 43 of Phase 10's
// column); SURPRISE_ALL_HELD's 53, KNOB_HOLD / KNOB_HELD at 4 / 4, MIX_TWO 7,
// MIX_LINE 75, MIX_THIS / MIX_THAT 8 / 8 and forecastExpansion's 44 were
// counts of sentences, not rules - the lock's width is Knob.svelte's and
// ColourPicker.svelte's .lock rule now. See
// .planning/phases/13-gui-overhaul/13-19-SUMMARY.md.
```
(`:98-108` to three lines keeping `THE MIX FAMILY IS GONE BY NAME, 2026-09-12` and the FIVE names
`tune/copy.spec.ts:167-173` assembles - `MIX_TWO`, `MIX_LINE`, `MIX_THIS`, `MIX_THAT`, `mixChildName`;
`:109-131` to three lines keeping `METERS_UNAVAILABLE IS RETIRED BY NAME, 2026-09-12` and the fact
that the meter family stays exported for the Sandbox's room line). The header's "exactly one copy"
line keeps the three moved names `SURPRISE_ME`, `RESET_ALL`, `COPY_LINK` as the names whose Bible
lines live in `inspector-copy.ts` (`copy.spec.ts:281` asserts they are not exported; naming them is
how a reader learns where they went). The header above `:64` (the
"imports nothing", "strings in consts", "exactly one copy", "the register is the Bible's", the
register's rules, U+2212's scope) to at most ten lines: one line each for the rule and the spec that
enforces it, the register's rules as one dense line, the fifth character's scope as one line.

`page-target.ts` (replacing `:359-368`):
```
// The words: D-05's register, ledgered in 13-COPY-NEW.md. RETIRED BY NAME,
// 2026-09-12 (13.1-02, 13.1-CONTEXT D-05): switchReviewLine and
// replaceReviewTitle, the destination review's two lines.
```
(the `:403` `PUT BACK'S TWO PAGE LINES ARE RETIRED BY NAME, 2026-09-12 (13.1-06, …)` block to its first
line plus the two names; the header `:1-96` to ten lines: the four states as four one-line rows -
`reported / requested / switching / unverified` - the finding (`grid_decode.c:1272` NACKs a write off
the active page; one citation line), the click rule (request() sends nothing, confirm() is the only
sender) and the pointer to 13-12 / 13.1-02.)

`session-copy.ts` (header 73 → ten; the `:250` retirement to its first line plus names),
`sandbox/copy.ts` (`:198`'s `STORE_LABEL … IS RETIRED BY NAME` to its first line plus the fact),
`inspector-copy.ts` (27 → ten), `words.ts` (22 → ten), `labels.ts` (36 → ten). Every export's own doc
comment keeps ONE line with the batch row id it landed under (`(I.4.20)`) — that is the ledger key a
reader needs; the paragraph beneath goes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.2-03-01: --before 03; install-copy.ts and page-target.ts</name>
  <files>src/lib/device/install-copy.ts, src/lib/device/page-target.ts, src/lib/device/install-copy.spec.ts, src/lib/device/page-target.spec.ts</files>
  <action>
    1. `scripts/13.2-gate.sh --before 03` (every hash equal to 02's `--after`).
    2. `install-copy.ts`: the header (`:1-82`) to ten lines — what it is, "imports nothing at all"
       and the spec that pins it, D-05's register in one line, where the ledgers are
       (`13-18-BATCH.md`, `13-COPY-NEW.md`, `13.1-COPY-NEW.md`), `WRITE_CLICKS` is four; then the
       five retirement blocks exactly as prescribed in `<interfaces>`; then the exports, each with
       one line and its row id. `pageName` at `:277` keeps one line: "one of three copies — none of
       the three copy modules may import another (install-copy.spec.ts:431)".
    3. `npx vitest run --project server --maxWorkers=2 src/lib/device/install-copy.spec.ts
       src/lib/ui/device-ui.spec.ts` — green; `node scripts/gate/hash-strings.mjs --diff
       .planning/phases/13.2-readability/gate/03.strings.json` — no `-` / `+` line. If `:454` or
       `:738` is red, the phrase the assertion names goes back verbatim into the compact block (it
       fits); only if it cannot is the spec's body line re-aimed, and then the SUMMARY lists it by
       title and line.
    4. `page-target.ts`: the header to ten lines and the two retirement blocks as prescribed. The
       class and every method body untouched; the exports untouched (`DISCARD_LABEL` stays, its
       comment to one line: "ledgered I.3.9; ships on the /dev/install/ probe until bench row I").
       `npx vitest run --project server --maxWorkers=2 src/lib/device/page-target.spec.ts
       src/lib/ui/device-ui.spec.ts src/lib/device/install.spec.ts` — green.
    5. The two specs' headers (`install-copy.spec.ts` 71 lines, `page-target.spec.ts`) and comments
       under the rule; bodies untouched except a named re-aim.
    6. Prettier by path. Commit: `refactor(13.2-03): install-copy.ts and page-target.ts under the
       ten-line rule; the six retirement ledgers compact with every pinned phrase carried (or: <n>
       needles re-aimed, named); the census unmoved`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 src/lib/device src/lib/ui/device-ui.spec.ts 2>&1 | grep -E "Test Files.*passed" && node scripts/gate/hash-strings.mjs --diff .planning/phases/13.2-readability/gate/03.strings.json | grep -cE "^[-+] " | grep -qx 0</automated>
  </verify>
  <done>Both modules open in ten lines; the retirement ledgers are compact with the pinned phrases inside them; the device and device-ui specs are green; no literal moved.</done>
</task>

<task type="auto">
  <name>Task 13.2-03-02: tune/copy.ts, inspector-copy.ts, session-copy.ts, sandbox/copy.ts, words.ts, labels.ts</name>
  <files>src/lib/tune/copy.ts, src/lib/tune/inspector-copy.ts, src/lib/device/session-copy.ts, src/lib/sandbox/copy.ts, src/lib/ui/library/words.ts, src/lib/browse/labels.ts, src/lib/tune/copy.spec.ts, src/lib/device/session-copy.spec.ts</files>
  <action>
    1. `tune/copy.ts`: the header to ten lines and the three retirement blocks as prescribed; every
       export one line with its row id; `EVENT_BUDGET` is `view.ts`'s (06's) — not here. Quick:
       `npx vitest run --project server --maxWorkers=2 src/lib/tune/copy.spec.ts
       src/lib/ui/tune-ui.spec.ts` green; `hash-strings --diff` silent. A red on `:250`, `:506` or
       `:641`: the phrase back verbatim first; a re-aim only if it cannot fit, named.
    2. `inspector-copy.ts`, `session-copy.ts`, `sandbox/copy.ts`, `words.ts`, `labels.ts`: headers to
       ten lines, the retirement paragraphs to their first line plus names, `pageName` (session-copy)
       with its one line. After each: the quick suite over its spec and the ui spec that reads it
       (`session-copy.spec.ts` + `device-ui.spec.ts`; `sandbox-ui.spec.ts`; `browse-ui.spec.ts`;
       `instrument.spec.ts` reads `words.ts`? — check `grep -l words.ts src/lib/ui/*.spec.ts` and run
       what it prints); `hash-strings --diff` silent after each.
    3. The specs' headers (`copy.spec.ts` 21, `session-copy.spec.ts`) under the rule; bodies untouched
       except a named re-aim.
    4. Prettier by path; the whole quick suite through `94 966`; `npm run test:sweep 2>&1 | node
       scripts/check-counts.mjs 4 19` (`tune/` moved). Commit: `refactor(13.2-03): the six other
       copy modules under the ten-line rule, the retirement ledgers compact and every pinned phrase
       carried; pageName's three copies each say why; the census and the copy exports unmoved;
       sweep 4 19`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966 && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19</automated>
  </verify>
  <done>Eight modules under the rule; the quick suite green at the carried pair; the sweep 4 19; the three string hashes the baseline's.</done>
</task>

<task type="auto">
  <name>Task 13.2-03-03: --after 03, four chunks, the record</name>
  <files>docs/CODE-STYLE.md, .planning/phases/13.2-readability/gate/, .planning/STATE.md</files>
  <action>
    1. `npm run check` (655), `npm run lint`; `scripts/13.2-gate.sh --after 03 --against 03 --check
       655 --js-equal` — every compared term equal, the normalised JS among them; the utilities list
       prints the classes that left with the ledgers (`ordinal` from `sandbox/copy.ts:183` is one),
       the markup-named subset intact.
    2. Chunks **c1 c2 c3 c4** on fresh detached servers (`E2E_LOG_DIR=<scratchpad> bash scripts/gate/e2e-chunks.sh c1 e2e/install.e2e.ts
       e2e/session.e2e.ts` and so on per the script's own list), memory beside each,
       `test-results/` removed, reds rerun alone and named (the browse race is c2's known one).
    3. `docs/CODE-STYLE.md` "Appended": one dated line if the ledgers taught the rule something (e.g.
       "a retirement keeps its first line, the names and the numbers a spec asserts, and one
       pointer"); STATE by script against a copy. The SUMMARY: before / after pasted, the per-file
       table (eight modules, four specs), the six titles with `kept` or `re-aimed (line)` beside each
       pinned phrase, the chunk lines. Commit the docs: `docs(13.2-03): SUMMARY with the gate's
       before and after, the per-file table and the six wording-pin titles' state; STATE by script
       against a copy with status, completed_phases 11 and percent 100 asserted`.
  </action>
  <verify>
    <automated>bash scripts/13.2-gate.sh --after 03 --against 03 --check 655 --js-equal</automated>
  </verify>
  <done>The gate's --after equals its --before on every term; c1 c2 c3 c4 green with reruns named; the six titles' state recorded; STATE recorded.</done>
</task>

</tasks>

<verification>
- `gate/03*.txt`: every compared term equal, the normalised JS included; census / copy exports /
  testids equal to the baseline's; `grep -c Chromium src/lib/device/install-copy.ts src/lib/device/session-copy.ts src/lib/tune/copy.ts` = 0 / 0 / 0.
- `check-counts.mjs 94 966` green; the sweep `4 19`; c1 c2 c3 c4 green, reruns named.
- `grep -c "RETIRED BY NAME\|GONE BY NAME" src/lib/device/install-copy.ts src/lib/tune/copy.ts src/lib/device/page-target.ts src/lib/device/session-copy.ts src/lib/sandbox/copy.ts` — every heading still present (5 / 3 / 2 / 1 / 1).
- No import added to any of the eight (`grep -c "^import" <file>` unchanged, 0 for the copy modules).
- `git diff --quiet -- .planning/ROADMAP.md` exits 0.
</verification>

<success_criteria>
- Eight modules under the rule with the ledgers compact and every pinned phrase carried.
- Zero titles touched; zero re-aims expected and every actual one named.
- Term `+0 / +0; +0 / +0; check 655` observed; the three string hashes equal.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-readability/13.2-03-SUMMARY.md` with the observed count
line, the gate's before and after pasted, the per-file table, the six titles with each pinned phrase's
state (kept / re-aimed at line), the chunk lines with memory and reruns, any file carried, and the
trailer conflict. CAT-04 stays `[ ]`.
</output>
