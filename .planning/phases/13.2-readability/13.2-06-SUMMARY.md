---
phase: 13.2-readability
plan: 06
subsystem: readability
tags: [refactor, comments, routes, sim, transport, tune, sandbox, gate, checkpoint]
requires:
  - 13.2-05 (the components and app.css under the rule; the gate at 681b1e8; the census instrument fixed)
provides:
  - the twenty-one route files, the ten sim modules, the eight transport files, tune/model.ts, tune/view.ts and sandbox/editor.ts under the ten-line rule with no code moved
  - seven view.ts and two synthetic.ts over-exports demoted; the seven stale #page* comment lines and the askForecast / landLua ghosts reworded
  - nine spec headers under the rule, every body byte-identical after the copyright line
  - the phase gate - --after 06-phase --against 01 - with every term a visitor, a module or a test can see equal to 13.2-01's --before, the census proved on a worktree at the baseline, the JS move counter-built
  - docs/TESTING.md's Phase 13.2 section (543 lines, inserted whole); CODE-STYLE's dated line; deferred-items closed
affects: [the checkpoint (13.2-06 task 04); the ROADMAP row for whoever updates it]
tech-stack:
  added: []
  patterns:
    - a route's header describes a sibling probe and never spells its directory (config-shape.spec.ts's raw dev/<probe> scan)
    - a body comment that pointed at a numbered section of its own header is reworded when the header is cut (editor.ts's seven "(section N)" pointers)
    - a demoted export on a module-scope const moves the normalised built JS; the move is proved by a counter-build with the keyword restored, not explained away
    - a --before record whose instrument was later fixed is compared by re-measuring the baseline on a worktree with the fixed instrument, never by rewriting the record
key-files:
  created: []
  modified:
    - src/routes/**/*.svelte, src/routes/**/*.ts (the twenty-one route files; +layout.ts untouched at two lines)
    - src/lib/sim/{demo,engine,host,lua-host,lua-pad-sim,monitor,motion.svelte,paint,ready,touch}.ts (schedule.ts already at the rule, untouched)
    - src/lib/transport/{capture,fake,ports,queue,sequence,transport,web-serial}.ts, src/lib/transport/fixtures/synthetic.ts (index.ts already at the rule, untouched)
    - src/lib/tune/model.ts, src/lib/tune/view.ts, src/lib/sandbox/editor.ts
    - src/lib/tune/{model,ladder,reachability.sweep}.spec.ts, src/lib/sim/{demo,host,lua-host,lua-smoke}.spec.ts, src/lib/share/stamp-roundtrip.sweep.spec.ts (headers only)
    - docs/TESTING.md (a section inserted whole), docs/CODE-STYLE.md (one dated line), .planning/phases/13.2-readability/deferred-items.md, .planning/STATE.md, .planning/phases/13.2-readability/gate/06.*, 06-after.*, 06-phase-after.*
decisions:
  - "The seven view.ts over-exports are demoted as the plan says even though it moves the normalised JS by one chunk; the move is proved to be exactly that by restoring the keywords on a scratch copy and rebuilding to the baseline hash - the orchestrator's --js-equal on a plan that drops exports is a term to attribute, not a reason to keep dead exports"
  - "The census is compared against the phase baseline by re-measuring 21c5ff8 on a worktree with the fixed instrument (equal on every JSON field but head), and the --after reports FAIL on that term by construction; no --before record is rewritten (D-09)"
  - "editor.ts's header is cut only, as the plan says, and its seven body pointers to that header's sections are reworded to the Bible sections and the spec they meant - a pointer to a paragraph that no longer exists is a comment that lies"
  - "schedule.ts, transport/index.ts, view.spec.ts and +layout.ts are on the plan's file list and already at the rule; they are not touched, because the mechanical test (--todo) is the rule and an edit with no reason is a diff with no reason"
  - "The route headers say what the page is, which stores it mounts, which snippets it hands to the shell and the one rule each obeys; the measured-rather-than-assumed line (05.1-09) keeps its number and plan id in the beforeNavigate comment where it is read"
metrics:
  duration: "about 1 h 45 min on 2026-09-14 (the --before at 08:53Z, the first commit at 09:13Z, the second at 09:23Z, the two --after runs 09:25Z to 09:37Z, the chunks to 09:44Z, the docs commit at 10:07Z)"
  completed: 2026-09-14
---

# Phase 13.2 Plan 06: The routes, the sim, the transport, the model, the view and the editor, then the phase gate Summary

**One-liner:** The last forty-nine source files under the ten-line rule with no code moved
(48,908 -> 45,774 lines, 15,624 -> 12,490 comment lines, the headers 5,114 -> 3,830, the code lines
29,538 -> 29,538; `buildTuner` one function, `runRequest` and the responder whole,
`synthetic-zona.json` byte-identical, every route's import shape what `config-shape.spec.ts` scans),
seven `view.ts` and two `synthetic.ts` over-exports demoted, nine spec headers cut with every body
byte-identical - and the phase gate: `--after 06-phase --against 01` measured the whole phase against
13.2-01's record at `21c5ff8` and every term a visitor, a module or a test can see is equal (the
wire set `a24b256f…` and `--full` `514cb2c7…` on 1,761 records, the copy exports, the testids, the
four fixtures, the 26 OG images, the scoped CSS `e296d0af…` with the utilities 45 -> 44 by `row-0`
alone, the titles `b53f2d9c…`, name-status 234 M / 1 A / 0 D / 0 R, check 655 = 654 + 1); the
census `6ac1cdf3…` proved equal on a worktree at the baseline with the fixed instrument (the
recorded `e6af80e0…` was the instrument's), the raw CSS and the normalised JS recorded and
attributed (06's JS move is the seven demotions, counter-built back to `da40f09f…`); `94 / 966 (+1
todo)` four times, `4 19`, 101 e2e runs in five chunks with one rerun named, the allowlist empty
and the six circles unedited; TESTING.md's Phase 13.2 section inserted whole. The phase is gate
landed, checkpoint pending: the user reads three files.

**Observed count line:** `94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4 19 / allowlist 0`.
Term **+0 / +0; e2e +0 / +0; check +0**, as projected. The whole phase's six-term chain, every zero
written out, is in `docs/TESTING.md` ("Phase 13.2's suites, measured at the gate") and restated below.

## Commits

| Task | Commit    | What                                                                                                                                                                                                                                                                                                                                             |
| ---- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | `df69cd1` | the twenty-one route files, `tune/model.ts` (1,169 -> 887), `tune/view.ts` (863 -> 639, seven exports demoted), `sandbox/editor.ts` (991 -> 901), three tune spec headers; 26 files, the tree's comment lines 15,624 -> 13,948; the six browser-store guards each with one D-15 line                                                               |
| 2    | `4b1f6e7` | the ten sim modules (`lua-host.ts` 1,059 -> 785 with every firmware citation, `host.ts` 786 -> 533), the eight transport files (`sequence.ts` 654 -> 510 with SLOTS' three reasons as the list they are; `synthetic.ts` 648 -> 540, two exports demoted, the fixture byte-identical), five spec headers; 23 files, 13,948 -> 12,490; sweep `4 19` |
| 3    | `7a3850a` | `gate/06.*`, `gate/06-after.*`, `gate/06-phase-after.*`; `docs/TESTING.md`'s section; `docs/CODE-STYLE.md`'s line; `deferred-items.md`'s rows; STATE by script                                                                                                                                                                                   |
| docs | (this)    | this SUMMARY                                                                                                                                                                                                                                                                                                                                     |

No co-author trailer on any commit: the harness asked for one; the repository's rule wins, and this
line reports the conflict. Nothing touched a ZONA; nothing deployed; no `gsd-tools state` command;
no `git checkout` / `restore` / `stash` / `clean` (one `git worktree add --detach` into the
scratchpad for the census proof, `node_modules` junctioned and un-junctioned, the worktree removed
and pruned the same minute, `node_modules` intact; one `git commit --amend` on `4b1f6e7`'s own
message to correct a count I had typed before measuring - the tree of the commit unchanged);
`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` untouched (`git diff --quiet` exits 0); the
user's dev server on 5173 left alone (every chunk on a fresh detached 4173).

## The gate: `--before 06` at `681b1e8`, pasted whole

```
# 13.2 gate --before 06
HEAD 681b1e8 (681b1e83ecd1806bc7e88eb8d609c47a3b6a6b08); dirty (src e2e scripts docs) 0; free memory 3.38 GB; 2026-09-14T08:53:25Z

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2728 distinct literals (6354 occurrences); 7 copy modules; 320 data-testids
literal census sha256 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 48908 lines, 29538 code, 15624 comment, 3746 blank, 5114 header

## check and lint
check: 1789376022914 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 681b1e8

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 da40f09fea7badec9aa2792a39ccd25d1f161af4673f603da12526a67f640f11 (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

recorded: .planning/phases/13.2-readability/gate/06.{txt,wire.json,wire-set.json,strings.json,lines.json,vitest.json,titles.txt,utilities.txt,js.txt}
```

Every hash is 05's `--after`: wire set `a24b256f…`, `--full` `514cb2c7…`, sandbox `40b44316…`,
census `6ac1cdf3…` (the corrected instrument), copy exports `10fad002…`, testids `21fc9eb9…`,
scoped CSS `e296d0af…` with 44 utilities, titles `b53f2d9c…`, normalised JS `da40f09f…`.

## The gate: `--after 06 --against 06 --check 655 --js-equal` at `4b1f6e7`, pasted whole

Run once, after `npm run build` on the final tree (deferred row 3: the script's quick suite runs
before its own build, and `radius.spec.ts` layer B refuses a stale one).

```
# 13.2 gate --after 06 --against 06 --check 655 --js-equal
HEAD 4b1f6e7 (4b1f6e7bf1103df5e3e997ff0a82169be9e7a386); dirty (src e2e scripts docs) 0; free memory 1.74 GB; 2026-09-14T09:27:59Z
before-record HEAD 681b1e83ecd1806bc7e88eb8d609c47a3b6a6b08

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2728 distinct literals (6354 occurrences); 7 copy modules; 320 data-testids
literal census sha256 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3

## the comment lines
193 files; 45774 lines, 29538 code, 12490 comment, 3746 blank, 3830 header

| file | lines before | after | comment before | after | header before | after |
|---|--:|--:|--:|--:|--:|--:|
| src/lib/sandbox/editor.ts | 991 | 901 | 202 | 112 | 103 | 13 |
| src/lib/sim/demo.ts | 392 | 211 | 255 | 74 | 66 | 13 |
| src/lib/sim/engine.ts | 138 | 87 | 80 | 29 | 40 | 12 |
| src/lib/sim/host.ts | 786 | 533 | 375 | 122 | 112 | 13 |
| src/lib/sim/lua-host.ts | 1059 | 785 | 477 | 203 | 22 | 12 |
| src/lib/sim/lua-pad-sim.ts | 232 | 167 | 103 | 38 | 17 | 9 |
| src/lib/sim/monitor.ts | 257 | 184 | 129 | 56 | 54 | 13 |
| src/lib/sim/motion.svelte.ts | 160 | 95 | 89 | 24 | 50 | 12 |
| src/lib/sim/paint.ts | 81 | 42 | 55 | 16 | 41 | 12 |
| src/lib/sim/ready.ts | 73 | 44 | 47 | 18 | 35 | 13 |
| src/lib/sim/touch.ts | 190 | 141 | 85 | 36 | 22 | 9 |
| src/lib/transport/capture.ts | 271 | 239 | 57 | 25 | 11 | 11 |
| src/lib/transport/fake.ts | 240 | 229 | 29 | 18 | 19 | 8 |
| src/lib/transport/fixtures/synthetic.ts | 648 | 540 | 233 | 125 | 33 | 13 |
| src/lib/transport/ports.ts | 62 | 40 | 41 | 19 | 20 | 9 |
| src/lib/transport/queue.ts | 391 | 382 | 67 | 58 | 15 | 6 |
| src/lib/transport/sequence.ts | 654 | 510 | 250 | 106 | 23 | 10 |
| src/lib/transport/transport.ts | 201 | 168 | 64 | 31 | 10 | 10 |
| src/lib/transport/web-serial.ts | 178 | 158 | 59 | 39 | 15 | 8 |
| src/lib/tune/model.ts | 1169 | 887 | 504 | 222 | 58 | 13 |
| src/lib/tune/view.ts | 863 | 639 | 403 | 179 | 43 | 18 |
| src/routes/+layout.svelte | 432 | 320 | 171 | 59 | 0 | 11 |
| src/routes/+page.svelte | 131 | 104 | 49 | 22 | 35 | 13 |
| src/routes/+page.ts | 25 | 12 | 19 | 6 | 19 | 6 |
| src/routes/dev/catalog/+page.svelte | 87 | 72 | 28 | 13 | 25 | 12 |
| src/routes/dev/fidelity/+page.svelte | 91 | 50 | 59 | 18 | 19 | 10 |
| src/routes/dev/install/+page.svelte | 423 | 328 | 145 | 50 | 69 | 15 |
| src/routes/dev/session/+page.svelte | 209 | 137 | 99 | 27 | 69 | 14 |
| src/routes/dev/skeleton/+page.svelte | 680 | 647 | 77 | 44 | 22 | 11 |
| src/routes/dev/tune/+page.svelte | 243 | 147 | 136 | 40 | 100 | 15 |
| src/routes/dev/type/+page.svelte | 158 | 103 | 70 | 15 | 70 | 15 |
| src/routes/my-configs/+page.svelte | 1362 | 1274 | 177 | 89 | 82 | 15 |
| src/routes/my-configs/+page.ts | 23 | 17 | 12 | 6 | 12 | 6 |
| src/routes/playground/+page.svelte | 619 | 529 | 172 | 82 | 79 | 15 |
| src/routes/playground/+page.ts | 32 | 19 | 21 | 8 | 21 | 8 |
| src/routes/playground/[id]/+page.svelte | 985 | 840 | 272 | 127 | 120 | 15 |
| src/routes/playground/[id]/+page.ts | 69 | 42 | 44 | 17 | 38 | 11 |
| src/routes/sandbox/+page.svelte | 111 | 102 | 23 | 14 | 22 | 12 |
| src/routes/sandbox/+page.ts | 27 | 20 | 14 | 7 | 14 | 7 |
| src/routes/sandbox/[draftId]/+page.svelte | 1178 | 1068 | 185 | 75 | 96 | 15 |
| src/routes/sandbox/[draftId]/+page.ts | 51 | 25 | 37 | 11 | 37 | 11 |
| **totals (193 -> 193 files)** | 48908 | 45774 | 15624 | 12490 | 5114 | 3830 |

41 file(s) moved

## check and lint
check: 1789378104067 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 4b1f6e7

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only
UTILITIES DELTA 44 -> 44: 0 disappeared, 0 appeared
MARKUP-NAMED intact: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 07f50f2af06978a48a7825a670050dee63ecb722fe341d08c684efec05af2530 (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

## src/ against the before-record's HEAD 681b1e83ecd1806bc7e88eb8d609c47a3b6a6b08
git diff --name-status: 49 modified, 0 added, 0 deleted, 0 renamed
git diff --stat over src/vendor, the manifest, Knob.svelte, ColourPicker.svelte: (empty)

## comparison against 06
wire: equal (set a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b, full 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b)
sandbox set: equal (40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01)
strings: equal (census 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99, copy exports 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef, testids 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3)
scoped CSS: equal (e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237); the utilities delta is printed above by name
OG: equal (26 files, 154136 B, 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57)
fixtures: equal
titles: equal (b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b)
name-status: no R, no D, A only for src/test-support/source.ts
check: 655 files, 0 errors, 0 warnings
lint, quick, build: green
  < 7f37422d34e16fddbb4b7480baef2a06ea65665795d3319e89ca98487bc4a67b build/_app/immutable/chunks/HASH.js
  > c403478cad3e58e08e85dfc7456cef0b577027b53fa4bec2ed4b7ea4d6eaf6fe build/_app/immutable/chunks/HASH.js
FAIL: the normalised JS (da40f09fea7badec9aa2792a39ccd25d1f161af4673f603da12526a67f640f11 -> 07f50f2af06978a48a7825a670050dee63ecb722fe341d08c684efec05af2530)
```

**Every compared term equal; the normalised JS moved on the last line.** The move is one file of 71,
`build/_app/immutable/chunks/HASH.js` (`7f37422d…` -> `c403478c…`), the chunk that carries
`view.ts`'s word tables (`C#`, `Magenta` inside it). The plan demotes seven `view.ts` over-exports
(`DOT_RAIL_MAX`, `DIRECTION_WORDS`, `MODE_WORDS`, `BEND_WORDS`, `SPRING_WORDS`, `NOTE_NAMES`,
`HUE_NAMES`) from `export const` to `const`; an unexported module-scope name is one the minifier
may mangle. **The counter-build:** the committed blob of `view.ts` recorded (`4fc1a534…`), the
seven `export` keywords put back by `sed`, `npm run build`, and the gate's own normalisation
pipeline (the script's `find | sed | sha256sum` lines, copied verbatim into a scratch file) gives
**`da40f09f…`** - the `--before 06` figure to the byte; the keywords taken off again, the file is
blob `4fc1a534…` and `git status` over `src/` is empty. So the whole of the JS move is the seven
demotions, and the JS is otherwise byte-equal across a plan that touched 49 files. The final build
(`postbuild: 4b1f6e7…`) was then rebuilt on the real tree and its normalised JS `07f50f2a…` is
equal to both gate runs' - it is the build the chunks ran against.

## The gate: `--after 06-phase --against 01 --check 655` at `4b1f6e7`, pasted whole (the 179-row table elided once, named)

```
# 13.2 gate --after 06-phase --against 01 --check 655
HEAD 4b1f6e7 (4b1f6e7bf1103df5e3e997ff0a82169be9e7a386); dirty (src e2e scripts docs) 0; free memory 1.16 GB; 2026-09-14T09:30:56Z
before-record HEAD 21c5ff8b945a7ac27a874ca74d4280fb6767e95c

## the wire
1735 strings from 18 Lua entries + 8 presets + library + defaults + Sandbox page 3
WIRE SET a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE FULL 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b
154 Sandbox strings from 11 fixtures (runtime.spec.ts and emit.spec.ts) through landSurface, emitSurface under 2 and 3 slots, and at the picker corner
WIRE SANDBOX SET 40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01

## the strings
192 files; 2728 distinct literals (6354 occurrences); 7 copy modules; 320 data-testids
literal census sha256 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99
copy exports   sha256 10fad0020ce89919548d2dcb586c2160aa01d415d644e27dec0419f13840ffef
data-testids   sha256 21fc9eb96531a7161ea6e00facb69c1d792a1e534a57b34893225043bf774ce3
- 2 -> 3	"img"
- 57 -> 61	"true"
+ 0 -> 1	"Connecting"
+ 0 -> 1	"pad-spinner"
+ 0 -> 1	"spinner"
+ 0 -> 1	"still still-a"
+ 0 -> 1	"still still-b"
+ 0 -> 1	"still still-c"
+ 0 -> 1	"walker"

## the comment lines
193 files; 45774 lines, 29538 code, 12490 comment, 3746 blank, 3830 header

| file | lines before | after | comment before | after | header before | after |
|---|--:|--:|--:|--:|--:|--:|
| (the 179 rows are docs/TESTING.md's "The per-file table for the whole phase" and this record's lines 33-211) |
| **totals (192 -> 193 files)** | 60539 | 45774 | 27267 | 12490 | 14072 | 3830 |

179 file(s) moved

## check and lint
check: 1789378287949 COMPLETED 655 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS
lint exit 0

## the quick suite (--maxWorkers=2, JSON reporter for the titles)
check-counts: observed 94 files, 966 tests passed, 1 todo (todo is reported, never asserted)
check-counts: matches the expected counts
quick exit 0

## the build
build exit 0; stamp 4b1f6e7

## the fixtures
git status --porcelain over the fixture paths: (empty)
hash-object a581ef4ce6aae2427e69503f9aaa543a02e33561 src/lib/catalog/frames.json
hash-object 3a1d71dabfb6f056b625863bb269ad1967b2065d src/lib/fidelity/golden-frames.json
hash-object eca808d23b1e57b6e34db93523dfa89300b21ca6 src/lib/fidelity/preset-baseline.json
hash-object 8b78c396d7d72ca8609e0cb0522451143c7a039b src/lib/transport/fixtures/synthetic-zona.json
OG 26 files, 154136 B, sha256 2a9ccf80cda87bdb3db133d52eb961246e726636c288c00311d9d9f077992f57

## the built CSS
cat build/_app/immutable/assets/*.css | sha256sum: 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177 (14 stylesheets)
14 stylesheets under build/_app/immutable/assets/
RAW CSS sha256 174411ee5293df366ee05becf716a5494ae56d0a1a313d6c46d462f4ebfc3177
SCOPED CSS sha256 e296d0af8e89b880cee457fa2bfc5f7ac1af8abf190efd0ceae22ddd4e4f1237
UTILITIES 44 classes, 5 named by markup: block grid outline ring sr-only
- row-0
UTILITIES DELTA 45 -> 44: 1 disappeared, 0 appeared
MARKUP-NAMED intact: block grid outline ring sr-only

## the normalised built JS
NORMALISED JS sha256 07f50f2af06978a48a7825a670050dee63ecb722fe341d08c684efec05af2530 (71 files)

## the titles
TITLES sha256 b53f2d9c9deaaebb833a0d350c2383d2d199db14906196e7c12ca4a38db0f76b (967 vitest titles incl. todo, 101 playwright runs)

## src/ against the before-record's HEAD 21c5ff8b945a7ac27a874ca74d4280fb6767e95c
git diff --name-status: 234 modified, 1 added, 0 deleted, 0 renamed
  A	src/test-support/source.ts
git diff --stat over src/vendor, the manifest, Knob.svelte, ColourPicker.svelte: (empty)

## comparison against 01
wire: equal (set a24b256f7f075b7ba811a251545905480d327fba683cc5e40e43aedb8ddb769b, full 514cb2c7c3a43228b76beb056630180dbd3b3556602a33374bae11752c719f9b)
sandbox set: not recorded on the before side (01 predates --sandbox); recorded now (40b44316545f91a2cb4d8d7c5d2c859db904916e823b564ed568bc4055a88f01), not compared
FAIL: the literal census (e6af80e09d3ddf133752816b38e70bc5ab7293ab9f5536341ed58ae6b4b8c966 -> 6ac1cdf373b4c86f29af1eed0ddc3733f875150d6ce1aa57c4a7a6f834377f99; the -/+ lines above name every literal whose count moved)
```

**Term by term against `gate/01.txt` (`21c5ff8`, 2026-09-13T11:41Z, the phase baseline), the
script stopping at the census by construction and the rest compared by hand from the two `.txt`
records and the JSON beside them:**

| Term                                 | `--before 01`                              | `--after 06-phase`                  | State                                                                                                                                                  |
| ------------------------------------ | ------------------------------------------ | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| wire set / `--full`                  | `a24b256f…` / `514cb2c7…`                  | `a24b256f…` / `514cb2c7…`           | **equal**; the `--full` JSON's 1,761 records vs 1,761, 0 moved                                                                                         |
| sandbox set                          | not recorded (predates `--sandbox`)        | `40b44316…`                         | **equal to 02's `--before`** and to every record since                                                                                                 |
| literal census                       | `e6af80e0…` (2,721 / 6,342)                | `6ac1cdf3…` (2,728 / 6,354)         | **the instrument's, proved equal** (below)                                                                                                             |
| copy exports / testids               | `10fad002…` / `21fc9eb9…`                  | the same                            | **equal**                                                                                                                                              |
| the four fixtures                    | `a581ef4c… 3a1d71da… eca808d2… 8b78c396…`  | the same                            | **equal**; `git status` over the fixture paths empty after the build                                                                                   |
| OG                                   | 26 files, 154,136 B, `2a9ccf80…`           | the same                            | **equal**                                                                                                                                              |
| SCOPED CSS                           | `e296d0af…`                                | `e296d0af…`                         | **equal**; utilities 45 -> 44, `- row-0` (13.2-01), `block grid outline ring sr-only` intact, 0 appeared                                               |
| RAW CSS                              | `f89f1f4e…`                                | `174411ee…`                         | recorded; moved once, at 01, with `row-0`; equal on every record from `01-after` on                                                                    |
| NORMALISED JS                        | `7f4e4d7a…`                                | `07f50f2a…`                         | recorded; moved at 01 (six deletions, 28 demotions), 02's normaliser fix `b0c033c`, 04 (renames), 06 (seven demotions, counter-built); equal on 03, 05 |
| TITLES                               | `b53f2d9c…` (967 + 101)                    | `b53f2d9c…`                         | **equal**; `diff gate/01.titles.txt gate/06-phase-after.titles.txt` empty                                                                              |
| check                                | 654 / 0 / 0                                | 655 / 0 / 0                         | **+1**, `src/test-support/source.ts`                                                                                                                   |
| name-status (`21c5ff8..HEAD -- src`) | -                                          | 234 M, 1 A, 0 D, 0 R                | **the one A is `source.ts`**; the same four numbers against `500e33c`                                                                                  |
| refused paths' `--stat`              | -                                          | (empty)                             | `src/vendor`, the manifest, `Knob.svelte`, `ColourPicker.svelte` untouched                                                                             |
| lint / quick / build                 | clean / `94 / 966` / 0                     | clean / `94 / 966` / 0              | green on both `--after` runs                                                                                                                           |

**The census, proved another way.** `git worktree add --detach <scratchpad>/wt-baseline 21c5ff8`;
`scripts/gate/hash-strings.mjs` copied in (sha256 `cc1ffb2a…`, the same bytes as HEAD's - the
directory did not exist at the baseline); `node_modules` junctioned; `node scripts/gate/hash-strings.mjs
--out census-21c5ff8.json` in the worktree prints **`192 files; 2728 distinct literals (6354
occurrences); 7 copy modules; 320 data-testids` and `literal census sha256 6ac1cdf3…`**, with the
copy exports `10fad002…` and the testids `21fc9eb9…`; its JSON against `gate/06-phase-after.strings.json`
is **equal on `files`, `literals`, `literalOccurrences`, `copyModules`, `testidCount`, `hashes`,
`census`, `copyExports` and `testids`** - only `head` differs (`21c5ff8` vs `4b1f6e7`); the
junction removed with `rmdir`, the worktree removed and pruned, `node_modules/typescript` present
after. The seven `+` and two `-` lines the script prints are the ones 05 named (PadSpinner's
template: `Connecting`, `pad-spinner`, `spinner`, `walker`, the three `still` classes, one `img`,
four `true`), and they are what the old stripper never counted, on both sides. No literal moved.

## The six-term chain as observed, against 13.2-VALIDATION's projection

```
order            1     2     3     4     5     6
plan            01    02    03    04    05    06
files     94    +0    +0    +0    +0    +0    +0   =  94   (6 terms)   projected +0 x6: equal
tests    966    +0    +0    +0    +0    +0    +0   = 966   (6 terms)   projected +0 x6: equal (+1 todo reported)
e2e ttl   86    +0    +0    +0    +0    +0    +0   =  86   (6 terms)   projected +0 x6: equal
e2e run  101    +0    +0    +0    +0    +0    +0   = 101   (6 terms)   projected +0 x6: equal
check    654    +1    +0    +0    +0    +0    +0   = 655   (6 terms)   projected +1 +0 x5: equal (source.ts)
allowlist  0    +0    +0    +0    +0    +0    +0   =   0   (6 terms)   equal
circles    6    +0    +0    +0    +0    +0    +0   =   6   (6 terms)   equal
catalog   26    +0    +0    +0    +0    +0    +0   =  26   (6 terms)   equal
sweep   4 19   4 19  4 19  4 19    -     -   4 19  = 4 19  (run where D-18 says)   equal
```

Every difference between the projection and the observation is a hash, not a count, and each is
named in the table above and under "Where this phase's planner was wrong": the census (the
instrument), the raw CSS (expected to move, and it moved once), the normalised JS (recorded on four
plans, asserted on two, attributed on all four).

## The per-file table for the whole phase, and this plan's

The whole phase's 179-row table (`comment-lines.mjs --against gate/01.lines.json`) is in
`docs/TESTING.md` under "The per-file table for the whole phase"; its totals: **192 -> 193 files,
60,539 -> 45,774 lines, 27,267 -> 12,490 comment, 14,072 -> 3,830 header, 29,525 -> 29,538 code,
3,747 -> 3,746 blank**; the fourteen unmoved files are `Knob.svelte`, `ColourPicker.svelte` and
twelve already at the rule; the twelve largest cuts by comment lines are `install.svelte.ts` 490,
`entries/lumen.ts` 472, `entries/morph.ts` 350, `entries/wheels.ts` 323, `library.ts` 308,
`tune/model.ts` 282, `TuningRegion.svelte` 278, `entries/radar-points.ts` 276, `sim/lua-host.ts`
274, `session.svelte.ts` 267, `sim/host.ts` 253, `entries/ghost.ts` 248. This plan's 41 rows are the
`--after 06` record's table above (`--against gate/06.lines.json`): the routes, the sim, the
transport, the model, the view and the editor, 48,908 -> 45,774 lines and 15,624 -> 12,490 comment
lines, the code lines 29,538 -> 29,538.

The nine spec headers (header only; every line after the copyright byte-identical, proved per file
by the splicer's stripped-code equality and by `--todo`): `model.spec.ts` 20 -> 6,
`ladder.spec.ts` 26 -> 7, `reachability.sweep.spec.ts` 57 -> 10, `demo.spec.ts` 14 -> 5,
`host.spec.ts` 12 -> 5, `lua-host.spec.ts` 17 -> 6, `lua-smoke.spec.ts` 60 -> 10,
`stamp-roundtrip.sweep.spec.ts` 43 -> 10. `view.spec.ts` (8) and the other sim and transport specs
were under the rule already and were not touched.

**The proof that no code moved, per file.** Every comment range was replaced by line range with a
scratch splicer that refuses a code line inside the range, asserts the first and last line of each
range, and compares the file's stripped code (whole-line comments dropped by the audit's own
classifier, `<!-- -->` and `/* */` blocks and trailing `//` comments removed, whitespace collapsed)
before and after the splice - `code equal` on every one of the 49 files - and the same comparison
against `HEAD`'s blob after prettier: `EQUAL` on every file but `view.ts` and `synthetic.ts`, whose
only differences are the nine `export` keywords (`git diff -U0 | grep -v comment` lists exactly
those lines). Then the gate's terms: the scoped CSS `e296d0af…` and the raw CSS `174411ee…` equal to
`--before 06` on a fresh build after each task (utilities `44 -> 44, 0 disappeared, 0 appeared`),
and the normalised JS equal but for the one chunk the counter-build attributes.

## The e2e chunks (fresh detached servers on 4173, `scripts/gate/e2e-chunks.sh`, `--workers 3`, build stamped `4b1f6e7`)

| Chunk        | Free memory | Result                                                                                                                                                                                                             |
| ------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| c1           | 2.13 GB     | **32 passed** (1.2 m); `install.e2e.ts:1957` (A.21) green on both engines                                                                                                                                          |
| c2           | 2.30 GB     | 1 failed / 21 passed (32.7 s): `browse.e2e.ts:343` "searching and tag chips narrow the grid, and CLEAR FILTERS brings it back" - `toHaveCount(5)` received 26, the filtered grid counted before the filter hydrated |
| c2-alone-343 | 2.38 GB     | **1 passed** (2.7 s)                                                                                                                                                                                               |
| c3           | 2.37 GB     | **21 passed** (22.4 s)                                                                                                                                                                                             |
| c4           | 2.42 GB     | **15 passed** (16.9 s)                                                                                                                                                                                             |
| c5           | 2.40 GB     | **11 passed** (11.8 s); layer C STRICT on the twelve routes, `knob:thumb` unreachable as before                                                                                                                    |

101 of 101 runs green in a chunk or alone (32 + 22 + 21 + 15 + 11); the one rerun named. Port 4173
answered `000` after every stop; `test-results/` removed; the logs in the scratchpad. The one red
is the browse hydration race every plan of the phase has named (`browse:299 / :343 / :1186 /
:1404`, green alone on a fresh server), and `BrowseGrid.svelte` and `BrowseToolbar.svelte` compile
identically to their pre-phase form (13.2-05's proof) - not this plan's shape and not this phase's.

## The quick suite, the sweep and the radius gate

Quick `94 / 966 (+1 todo)` at `--maxWorkers=2` four times on the final tree: task 2's own, the two
gate runs (JSON reporter, `gate/06-after.vitest.json` and `gate/06-phase-after.vitest.json`), and a
fourth plain run at 2.34 GB free. Check 655 / 0 / 0 on both gate runs and task 2's; lint clean
three times (the two gate runs and after the docs). The sweep `4 19` twice on the final tree (task
2; the gate, 127 s wall: reachability 20,270 + 24,576 = 44,846 states in 120.1 s, laddered 8, over
budget 0, Pass B colours excluded 0; the kind cross-product 1,296 in 3.3 s, worst 906 of 908 at
`none/none/trackpad/hi=false/grid=false`). The radius gate, three layers: **A** (in the quick run
and alone, verbose) `34 declarations in 65 files scanned; 0 above zero remaining in 0 allowlisted
files (); 6 circles (D-15): ColourPicker.svelte:840, :867, :882, Knob.svelte:730, :785, :807; 28
exempt` - the two files untouched across the phase (`git diff --stat 500e33c..HEAD` prints nothing;
`--todo` prints exactly those two, `header 62` and `header 83`); **B** on the fresh build of
`4b1f6e7` `36 radius declarations in 14 built stylesheets; 6 of them 50%; tolerated values from
the allowlist: none; build 09:34:56Z, newest source 09:04:07Z`; **C** in c5, `11 passed`.

## The utilities, by name

`44 -> 44` on every build of this plan (after task 1, after task 2, the two gate builds, the final
build), `0 disappeared, 0 appeared`, the raw CSS `174411ee…` unmoved - no comment this plan cut was
the sole source of a utility, and no comment it wrote spells one (the at-risk `@property` words
`blur`, `ease-out`, `ring-1` / `ring-4` (in `lua-smoke.spec.ts`'s body, untouched), `outline`,
`transform`, `border`, `filter` all keep sources outside this plan's files, and the build is the
proof). Across the phase: **`row-0`** left at 13.2-01 (its only source was `calibration.ts:31`'s
old comment), 45 -> 44 -> 44 -> 44 -> 44 -> 44 -> 44; the markup-named five (`block grid outline
ring sr-only`) intact on every record.

## The refuse-list, proved

`src/vendor/`, the manifest, `Knob.svelte`, `ColourPicker.svelte`: `git diff --stat 500e33c..HEAD`
prints nothing. `synthetic-zona.json` `8b78c396…` after the build with two of `synthetic.ts`'s
exports demoted (`fixtures.spec.ts` regenerates and compares; `synthetic.spec.ts` green). Every
Lua literal: the wire. Every persisted key, literal and testid: the census (proved), the exports,
the testids. Every title: the titles hash. Every `.svelte` filename: the scoped CSS and the
name-status. No comment under `src/lib/**/*.ts` spells an erase, clear or page class
(`forbidden-instructions.spec.ts` green; grepped before each commit; `synthetic.ts` is under
`fixtures/`, excluded, and its own spellings predate the phase). `transport.ts` names no engine
(`transport.spec.ts` reads the raw file). No route names another probe's directory (`for p in
dev/catalog … dev/type; grep -rl $p src/routes | grep -v ^src/routes/$p/` prints nothing).

## Deviations from Plan

**1. [Rule 3 - blocking] `editor.ts`'s seven body pointers to its own header's sections were
reworded.** The plan says "header cut only"; the header's four numbered sections were what
`(section 1)`, `(section 2)`, `(section 3)` at `:58`, `:77`, `:352`, `:463`, `:484`, `:512`, `:533`
and `(section 8's own requirement)` at `:768` pointed at. A pointer to a paragraph that no longer
exists is a comment that lies, so each now names the Bible section or the spec it meant ("the two
creation paths, neither needing a drag (Bible sections 2, 8, 14)", "Play locks every structural
method and keeps the selection and the history (Bible section 8)"). Comments only. The same rule
fixed `view.ts:324`'s "see the header" to "(13-09)".

**2. The normalised JS moved under `--js-equal` on `--after 06`.** The orchestrator's line asked
for `--js-equal`; the plan's own task 3 and 13.2-CONTEXT D-09 say the JS is recorded, not asserted,
on a plan that drops `export`s. The term is attributed by counter-build rather than explained away
(above): one chunk, the seven `view.ts` demotions, the baseline hash returned with the keywords
restored. The plan's verify line (`--after 06 … --js-equal && …`) therefore exits 1 on that term
alone, as 05's did on the census.

**3. The census term reports `FAIL` on `--after 06-phase --against 01` by construction** (05
deviation 4, deferred row 2). Proved equal on the worktree; the record is not rewritten (D-09).

**4. The `--after` runs were preceded by `npm run build` on the final tree** (deferred row 3: the
script runs the quick suite before its own build, and layer B refuses a stale build). The script
is unchanged - a gate script edit at the last plan would itself be unbracketed.

**5. Four files on the plan's list were already at the rule and were not edited**: `schedule.ts`
(its constants' `pad-sim-host.ts:N` citations are what `schedule.spec.ts` reads and every comment
is the fact), `transport/index.ts`, `view.spec.ts` (8 lines) and `+layout.ts` (2 lines, no
comment). The mechanical test is the rule; an edit with no reason is a diff with no reason.

**6. `+layout.svelte` had no header at all** (the plan lists it at "0 header, 171 comment"): the
ten-line header now sits as the leading comment inside `<script>` rather than as a new `<!-- -->`
block before it, because a new top-level comment node before `<script>` is a change to the
template's node list that 05's lesson says to leave alone; the 90-line essay before `fill` is six
lines; the built JS of that route is in the equal set (the only moved chunk is `view.ts`'s).

**7. The six browser-store guards gained one D-15 line each** (13.2-CONTEXT D-15: "the seven copies
each keep a one-line comment saying so"): four of the six route copies had no comment at all
(`my-configs`, `sandbox/+page`, `[draftId]`, `[id]`), so one `/** The route's edge to the browser
store, one per route (13.2-CONTEXT D-15). */` line was added above each; `+page.svelte`'s and
`playground/+page.svelte`'s existing comments were reworded to name D-15 (the front door's also
`intro.spec.ts`, which pins that copy inline). The seventh is the store's own (`motion.svelte.ts`,
its one line kept). Comments only; `intro.spec.ts` blanks comments before it counts `localStorage`.

**8. `4b1f6e7`'s commit message was amended once** (`git commit --amend -F`, nothing staged, the
tree unchanged): the first message said 12,829 comment lines where the tree measured 12,490 - a
figure I had typed before running the counter. The SHA `5fd9e32` in the reflog is that first
message; `4b1f6e7` is the commit.

**9. The gate's `hash-strings.mjs --diff` run without `--out` wrote `gate/hash-strings.681b1e8.json`**
(its default output name) during task 1's route check; deleted the same minute, never staged.

## Things the plan asserts that the tree does not support

- "`sim/host.ts` 785 (112 header)": the audit counts 94 header lines under the rule (112 is the
  audit's leading run with its blank lines); "`sim/demo.ts` 391 (66 header, 255 comment)": 56 under
  the rule; "`sandbox/editor.ts` 990 (103 header)": 85. The figures moved, not the files.
- "`tune/model.ts` … its first thirty lines `:528-556` are three paragraphs on why the library
  import is dynamic": at `681b1e8` they were `:528-532` (the page-init string), `:536-538` (the
  fifth) and `:540-550` (the touch library) - three comments, one of them the paragraph the plan
  meant; all three are now one line each with the 12.1-08b pointer.
- "the `localStorage` guard's comment to one line naming D-15": four of the six route guards had no
  comment to cut (deviation 7).
- "`sim/lua-pad-sim.ts:211`'s `landLua` ghost": at `681b1e8` the line was `:211` inside the
  `createLuaPadSim` JSDoc, as the plan says; reworded to "`measureLuaRoute` + `land` publish the
  library"; `grep -rn landLua src` now prints nothing (the research's "cited in four files" were
  reworded by 01 and 02 before this plan).
- "the fifteen sim and transport specs' headers (`lua-smoke.spec.ts` 66, `host.spec.ts` 16,
  `lua-host.spec.ts`, …)": four sim specs were over the rule (`demo` 14, `host` 12, `lua-host` 17,
  `lua-smoke` 60 under the rule's count; 66 and 16 are the audit's) and no transport spec was; the
  other eleven were not touched.
- "`dev/skeleton/+page.svelte`'s `open` (142 lines)": 142 at `681b1e8` and at HEAD - the probe's
  function stays whole, its five inline comments to the fact.
- The plan's task 2 verify (`git status --porcelain src/lib/transport/fixtures/ … | wc -l | grep -qx
  0`) reads 1 before task 2's commit because `synthetic.ts` itself is under `fixtures/`; 0 after it.
- The plan's task 3 step 1 says "the quick suite three times … and a fourth for the per-file JSON";
  the two gate runs both carry the JSON reporter, so it was two JSON runs and two plain.
- 13.2-VALIDATION's "the built CSS … 01's fresh-build figure" is the SCOPED hash `e296d0af…`, equal
  on every record; the raw hash is 01's `f89f1f4e…` before the first edit and `174411ee…` from
  01-after on (the `row-0` departure), which the VALIDATION's row "expected to move" allows.

## Where this phase's planner was wrong, the gate holes and the six questions

Both lists are in `docs/TESTING.md`'s section ("Where this phase's planner was wrong": the 33-not-35
strips, the six-not-ten deletions, the eleven-not-three `keptThisSession` lines, the research's
totals including `app.css`, the SCOPED CSS and the `@property` rules, the census instrument, the
comment-node count, the gate's build order, `e2e-chunks.sh c3`, `library.ts`'s ten headings, this
plan's `--js-equal`; "The gate holes": H-1 with the utilities that left by name, H-4, H-5, H-6, the
node count, the `dev/<probe>` scan, the rule-only refuse items). The six questions 13.2-CONTEXT
could not decide are restated there and at the checkpoint below: `placeInWords`, `DISCARD_LABEL`,
the seven `localStorage` guards, `ColourPicker.svelte:66`'s stale `<dialog>` line, the public "keep"
API (`keepOnDevice`, `keepReason`, `KEEP_LABEL`, `KeepConfirm.svelte`), `buildTuner`'s six pure
helpers (declined this phase: `model.spec.ts` and `wire-pin.spec.ts` await a fixed microtask
chain; `view.spec.ts:450` pins import shapes) - and the seventh, Tailwind's scanner reach
(`source(none)` in `app.css`).

## The ROADMAP row, for whoever updates it

**Phase 13.2 Readability - 6 plans, 6 of 6 landed; the gate landed at `4b1f6e7` on 2026-09-14
(the docs commit `7a3850a`); the checkpoint (13.2-06 task 04, three files read by the user)
PENDING; not complete until the user answers; no requirement claimed; CAT-04 `[ ]`, the ninth
decline.** `.planning/ROADMAP.md` is not edited by this plan.

## Known Stubs

None. No string, export a consumer names, testid, title or filename moved; the nine demoted
exports had zero references outside their files (`grep` over `src`, `e2e`, `scripts`; `npm run
check` 655 / 0 / 0).

## The checkpoint (task 04): three files, read by the user

The per-file table in `docs/TESTING.md` is the menu. The three I propose, one entry, the install
store, one component - the largest cuts of their kinds:

1. `src/lib/device/install.svelte.ts` (490 comment lines gone across 13.2-04: 1,939 -> 1,462 lines;
   the header at ten lines plus the rules a spec enforces and a contents line);
2. `src/lib/catalog/entries/lumen.ts` (472 gone across 13.2-02: MECHANISM / WHAT IT SENDS / TRAPS,
   its history in `docs/entries/lumen.md`);
3. `src/lib/ui/TuningRegion.svelte` (278 gone across 13.2-05: 972 -> 694 lines, the four pinned
   phrases on one line each).

Or any three of your own - `src/lib/tune/model.ts` (282, this plan), `src/lib/sim/lua-host.ts`
(274, this plan, every firmware citation kept) and `src/routes/playground/[id]/+page.svelte` (145,
this plan) are this plan's largest. For each: open it at HEAD; open the same file as it was before
the phase (`git show 500e33c:<path>`, or `git diff 500e33c -- <path>` for the two side by side);
say whether the HEAD form reads better - whether you can tell what the file is, what it owns and
how to use it from the top - and whether anything you relied on in the old comments is now missing
(if it is, say which line: it is in the plan SUMMARY that moved it, and in `docs/entries/` for an
entry). "approved" records the phase as gate landed and reviewed; it ticks no requirement (CAT-04
stays `[ ]`) and edits no ROADMAP line. No hardware.

## Resume line (D-17)

`node scripts/gate/comment-lines.mjs --todo` over this plan's routes, sim, transport, model, view
and editor prints nothing; over every non-spec file under `src/` it prints exactly
`ColourPicker.svelte header 62`, `Knob.svelte header 83` (D-03) and `library.ts header 162` (the
table of contents, by construction). The plan is landed; nothing is carried; the phase waits on the
checkpoint's answer.

## Self-Check: PASSED

The three commits `df69cd1`, `4b1f6e7`, `7a3850a` are in the log; `gate/06.*`, `gate/06-after.*`
and `gate/06-phase-after.*` (27 files) exist and are committed; `docs/TESTING.md` carries "Phase
13.2's suites, measured at the gate" with every prior line a prefix and every later line a suffix
(asserted by `node -e` after prettier), `prettier --check` passes on it, on `CODE-STYLE.md` and on
`deferred-items.md`; `npm run lint` exit 0 on the docs tree; `git status --porcelain src` is empty;
`git diff --name-status 21c5ff8..HEAD -- src | grep -E "^(R|D)"` prints nothing and the one `A` is
`src/test-support/source.ts`; `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` untouched;
STATE.md's `status: executing`, `completed_phases: 11`, `percent: 100` asserted equal before and
after the script (one line added - the retained previous stop); CAT-04 stays `[ ]`.
