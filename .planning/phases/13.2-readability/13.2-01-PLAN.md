---
phase: 13.2-readability
plan: 01
type: execute
wave: 1
depends_on: []
# Execution order (13.2-VALIDATION "Why the phase is serial"): SERIAL - 01 -> 02 -> 03 -> 04 -> 05 -> 06.
# This plan runs FIRST: it writes the rule, seeds the gate, records the phase's --before, and takes the
# leaf modules. One tree, one build/, one port 4173, one gate/ directory, one STATE.md.
files_modified:
  - docs/CODE-STYLE.md
  - scripts/13.2-gate.sh
  - scripts/gate/hash-wire.mjs
  - scripts/gate/hash-strings.mjs
  - scripts/gate/ts-ext-register.mjs
  - scripts/gate/ts-ext-hooks.mjs
  - scripts/gate/comment-lines.mjs
  - src/test-support/source.ts
  - src/lib/browse/facets.ts
  - src/lib/browse/filter.ts
  - src/lib/browse/grid.ts
  - src/lib/browse/query.ts
  - src/lib/browse/rail.ts
  - src/lib/browse/return.ts
  - src/lib/browse/sort.ts
  - src/lib/browse/typographic.ts
  - src/lib/catalog/calibration.ts
  - src/lib/catalog/divergence.ts
  - src/lib/catalog/front-door.ts
  - src/lib/catalog/index.ts
  - src/lib/catalog/listing.ts
  - src/lib/catalog/presets.ts
  - src/lib/catalog/touch-guard.ts
  - src/lib/catalog/types.ts
  - src/lib/protocol/constants.ts
  - src/lib/protocol/decode.ts
  - src/lib/protocol/descriptors.ts
  - src/lib/protocol/framing.ts
  - src/lib/protocol/index.ts
  - src/lib/protocol/match.ts
  - src/lib/protocol/usb.ts
  - src/lib/protocol/write-guard.ts
  - src/lib/protocol-pin.ts
  - src/lib/share/stamp.ts
  - src/lib/share/url.ts
  - src/lib/og/png.ts
  - src/lib/og/render.ts
  - src/lib/store/collections.ts
  - src/lib/store/drafts.ts
  - src/lib/store/favorites.ts
  - src/lib/store/intro.ts
  - src/lib/store/library.ts
  - src/lib/store/local.ts
  - src/lib/store/motion.ts
  - src/lib/store/recent.ts
  - src/lib/store/schema.ts
  - src/lib/store/transfer.ts
  - src/lib/fidelity/firmware-oracle.ts
  - src/lib/pad/index.ts
  - src/lib/pad/ready.ts
  - src/lib/sandbox/colour-knob.ts
  - src/lib/sandbox/cost.ts
  - src/lib/sandbox/draft.ts
  - src/lib/sandbox/geometry.ts
  - src/lib/sandbox/history.ts
  - src/lib/sandbox/land.ts
  - src/lib/sandbox/model.ts
  - src/lib/sandbox/preview.ts
  - src/lib/tune/idle.ts
  - src/lib/tune/knobs.lua.ts
  - src/lib/tune/knobs.preset.ts
  - src/lib/tune/state.ts
  - src/lib/tune/surprise.ts
  - src/lib/device/snapshot.ts
  - src/lib/device/try-on.ts
  - src/lib/ui/device-drawer.svelte.ts
  - src/lib/ui/fidelity-line.ts
  - src/lib/ui/intro/card.ts
  - src/lib/ui/radius-allowlist.ts
  - src/lib/ui/shell/device-clause.ts
  - src/lib/ui/shell/layout.ts
  - src/lib/ui/shell/shell.svelte.ts
  - src/lib/index.ts
  - src/app.d.ts
  - src/lib/browse/facets.spec.ts
  - src/lib/browse/filter.spec.ts
  - src/lib/browse/query.spec.ts
  - src/lib/browse/return.spec.ts
  - src/lib/browse/sort.spec.ts
  - src/lib/catalog/front-door.spec.ts
  - src/lib/catalog/listing.spec.ts
  - src/lib/config-shape.spec.ts
  - src/lib/device/install-copy.spec.ts
  - src/lib/device/install.spec.ts
  - src/lib/device/session-copy.spec.ts
  - src/lib/device/session.spec.ts
  - src/lib/device/snapshot.spec.ts
  - src/lib/device/try-on.spec.ts
  - src/lib/og/png.spec.ts
  - src/lib/share/url.spec.ts
  - src/lib/sim/demo.spec.ts
  - src/lib/sim/lazy.spec.ts
  - src/lib/store/collections.spec.ts
  - src/lib/store/local.spec.ts
  - src/lib/tune/colour-picker.spec.ts
  - src/lib/tune/copy.spec.ts
  - src/lib/tune/idle.spec.ts
  - src/lib/tune/ladder.spec.ts
  - src/lib/tune/model.spec.ts
  - src/lib/tune/view.spec.ts
  - src/lib/ui/aesthetic.spec.ts
  - src/lib/ui/browse-ui.spec.ts
  - src/lib/ui/device-ui.spec.ts
  - src/lib/ui/identity.spec.ts
  - src/lib/ui/instrument.spec.ts
  - src/lib/ui/sandbox-ui.spec.ts
  - src/lib/ui/tune-ui.spec.ts
  - src/lib/browse/grid.spec.ts
  - src/lib/browse/typographic.spec.ts
  - src/lib/catalog/calibration.spec.ts
  - src/lib/catalog/catalog.spec.ts
  - src/lib/catalog/frames.spec.ts
  - src/lib/catalog/presets.spec.ts
  - src/lib/catalog/touch-guard.spec.ts
  - src/lib/protocol/constants.spec.ts
  - src/lib/protocol/decode.spec.ts
  - src/lib/protocol/descriptors.spec.ts
  - src/lib/protocol/framing.spec.ts
  - src/lib/protocol/match.spec.ts
  - src/lib/protocol/write-guard.spec.ts
  - src/lib/protocol-pin.spec.ts
  - src/lib/share/stamp.spec.ts
  - src/lib/og/build.spec.ts
  - src/lib/og/render.spec.ts
  - src/lib/store/transfer.spec.ts
  - src/lib/fidelity/firmware-oracle.spec.ts
  - src/lib/pad/ready.spec.ts
  - src/lib/sandbox/geometry.spec.ts
  - src/lib/tune/knobs.lua.spec.ts
  - src/lib/tune/knobs.preset.spec.ts
  - src/lib/tune/state.spec.ts
  - src/lib/tune/surprise.spec.ts
  - src/lib/format-parity.spec.ts
  - src/lib/skeleton-results.spec.ts
  - .planning/phases/13.2-readability/gate/
  - .planning/STATE.md
autonomous: true
requirements: [] # none - a refactor claims nothing (ROADMAP: "the gate re-proves every requirement the tree already ticks")

must_haves:
  truths:
    - "The rule exists once, in docs/CODE-STYLE.md, and every later plan can point at it: ten-line headers, one provenance line with a pointer, the fact without the argument, the refuse-list, the gate command."
    - "scripts/13.2-gate.sh --before 01 records the phase's baseline at HEAD before any source edit, and --after 01 --against 01 proves the wire (set and --full), the three string hashes, the fixtures, the OG bytes and the built CSS equal, with check at 655 and the quick suite at 94 / 966."
    - "One stripComments lives in src/test-support/source.ts; 33 specs import it; every one of their needles matches exactly as it did (the quick suite green at the carried count)."
    - "Six dead exports are gone by name and no user-facing literal went with them; four dead-looking exports are kept by name with the reason."
    - "Every leaf .ts module in this plan's list opens with a header of at most ten lines plus one pointer, cites no name that does not exist, and re-argues nothing in its body."
  artifacts:
    - path: "docs/CODE-STYLE.md"
      provides: "The header rule, the pointer format, the three entry headings, the fact-not-argument test, the refuse-list, the gate command"
    - path: "scripts/13.2-gate.sh"
      provides: "--before <tag> / --after <tag> --against <tag> over the five harnesses, check, lint, the quick suite, the build, the fixture and CSS/OG hashes"
    - path: "scripts/gate/hash-wire.mjs"
      provides: "The wire hash (set and --full) resolving the repo root from import.meta.url"
    - path: "src/test-support/source.ts"
      provides: "stripComments (the 24-spec variant verbatim), readSource(rel)"
      exports: ["stripComments", "readSource"]
  key_links:
    - from: "scripts/13.2-gate.sh"
      to: "scripts/gate/hash-wire.mjs"
      via: "node --import ./scripts/gate/ts-ext-register.mjs scripts/gate/hash-wire.mjs --full"
      pattern: "hash-wire\\.mjs --full"
    - from: "src/lib/device/install.spec.ts"
      to: "src/test-support/source.ts"
      via: "import { stripComments } from \"../../test-support/source\""
      pattern: "test-support/source"
---

<objective>
The rule written once, the gate seeded and run before anything moves, the one `stripComments` for 33
specs, the dead exports gone by name, and the leaf modules — the tier with behavioural unit specs and
no positive source scan — brought to the ten-line rule. Smallest blast radius first: nothing in this
plan is read by an e2e title as text, nothing in it is a Lua string, and nothing in it is a store.

Purpose: every later plan brackets itself with a script that exists, reads a rule that exists, and
imports a helper that exists; the tree's readability gain starts where a mistake would cost least.
Output: `docs/CODE-STYLE.md`; `scripts/13.2-gate.sh` and `scripts/gate/`'s five harnesses; the
`--before 01` and `--after 01` records under `gate/`; `src/test-support/source.ts`; 33 specs importing
it; six exports deleted, about 44 `export` keywords dropped, six ghost names reworded; about sixty
leaf modules under the rule; the headers of their specs under the same rule. Term **+0 / +0; e2e +0 /
+0; check 655 (+1, `source.ts`)**; the sweep run; all five chunks.

**Execution order.** FIRST in the serial order 01 -> 02 -> 03 -> 04 -> 05 -> 06. Its `--before` is
the phase's baseline and 13.2-06 compares the whole phase against it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-readability/13.2-CONTEXT.md
@.planning/phases/13.2-readability/13.2-VALIDATION.md
@.planning/phases/13.2-readability/13.2-RESEARCH.md
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-08-SUMMARY.md
@docs/TESTING.md
@scripts/check-counts.mjs
@src/lib/ui/radius-allowlist.ts
@src/lib/protocol/forbidden-instructions.spec.ts
@src/lib/sim/lazy.spec.ts
@src/lib/ui/shell/layout.ts
@src/lib/sandbox/model.ts
@src/lib/tune/surprise.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer — the
  harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
  The user's dev server on 5173 is left alone.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged, adding a "Concurrent, Phase 13.2 (recorded,
  not reconciled)" line with this plan's observed count line. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on every file this plan touches, by path (a comment cut
  can leave a reflow; `npm run lint` is `prettier --check .`).
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's.
- **The refuse-list (13.2-CONTEXT D-06), absolute**: `src/vendor/`; every Lua literal; the manifest;
  the fixtures and OG images; `CIRCLES` and the two circle files; every persisted key and field; every
  user-facing literal and `data-testid`; every test title; every `.svelte` filename; no new spec file.
- **A rewritten comment under `src/lib/**/*.ts` never spells the two erase / clear instruction names
  or the two page classes outside `descriptors.ts`** — `forbidden-instructions.spec.ts` scans raw
  source, comments included (round 4c was red on this once).
- **Every count a carried name plus a delta** from `94 / 966 (+1 todo) / 86 / 101 / check 654 / sweep
  4 19 / allowlist 0`. Term **+0 / +0; +0 / +0; check +1**. `check-counts.mjs` is handed `94 966`.
- **The gate brackets the plan**: `scripts/13.2-gate.sh --before 01` before the first source edit
  (task 1 writes the script, then runs it, then edits nothing else until it has passed);
  `--after 01 --against 01` after the last source commit; both pasted whole in the SUMMARY.
- **Quick at `--maxWorkers=2`.** Playwright chunks **c1 c2 c3 c4 c5** on fresh detached servers
  (`e2e-chunks-1208.sh`, scratchpad), stopped through PowerShell, the free memory beside each; every
  red rerun alone and named (`browse:299`, `:343`, `:1404` are the hydration race — green alone).
- **Re-runnable (13.2-CONTEXT D-17)**: a file already at the rule is skipped; an executor past 50%
  context commits at a file boundary, runs `--after`, and lists the files not reached under
  "carried" in the SUMMARY.
</standing_rules>

<interfaces>
The scratchpad harnesses to seed from, at
`C:\Users\sabot\AppData\Local\Temp\claude\C--Users-sabot-Documents-Claude-hangar\9e0a83a0-9e73-4b17-922e-5ea9c879910b\scratchpad\`:
`hash-wire.mjs` (217 lines; hard-codes `H` and `SP` at the top; `--full`, `--out F`), `hash-strings.mjs`
(93 lines; the same two constants; `--out F`, `--diff OLD.json`), `ts-ext-register.mjs` (2 lines) and
`ts-ext-hooks.mjs` (13 lines; resolves extensionless relative imports to `.ts`), `audit-13.2.mjs` (the
per-file line / code / comment / header counter; seed `comment-lines.mjs` from its counting only).
`e2e-chunks-1208.sh` beside them is the chunk runner and is not copied.

The seeded copies differ from the scratchpad's in exactly this: `H` becomes
`fileURLToPath(new URL("../../", import.meta.url))` (the repo root from `scripts/gate/`), `SP` becomes
`<H>/.planning/phases/13.2-readability/gate`, and both are prettier- and eslint-clean (`npm run lint`
walks `scripts/`; `typescript-eslint`'s recommended set applies — no unused locals, no empty catch).
Their output lines are unchanged so a record made in the scratchpad and one made in the tree read alike.

`src/test-support/source.ts` (new; the copyright line at its foot like the tree's other files):
```ts
import { readFileSync } from "node:fs";
import { fileURLToPath } from "node:url";

/** The repository root, resolved from this file: src/test-support/ is two levels down. */
export const REPO_ROOT: string = fileURLToPath(new URL("../../", import.meta.url));

/** Read a repo-relative file as text. */
export function readSource(rel: string): string {
  return readFileSync(new URL(rel, new URL("../../", import.meta.url)), "utf8");
}

/**
 * Remove whole-line // comments, block comments and HTML comments before a
 * spec scans source for a needle. REMOVES, so line numbers do not hold - the
 * radius gate's line-preserving blankComments (ui/radius-allowlist.ts) is a
 * different contract and stays where it is. Twenty-four specs carried this
 * exact body since Phase 4; nine carried it without the HTML clause.
 */
export const stripComments = (source: string): string =>
  source
    .replace(/^[ ]*[/][/].*$/gm, "")
    .replace(/[/][*][^]*?[*][/]/g, "")
    .replace(/<!--[^]*?-->/g, "");
```
The 24 specs whose local body is exactly that (13.2-VALIDATION lists them; `strip-variants.mjs` in the
scratchpad prints the two groups): `catalog/front-door`, `catalog/listing`, `config-shape`,
`device/install-copy`, `device/install`, `device/session-copy`, `device/session`, `device/try-on`,
`og/png`, `share/url`, `sim/lazy`, `tune/colour-picker`, `tune/copy`, `tune/idle`, `tune/ladder`,
`tune/model`, `tune/view`, `ui/aesthetic`, `ui/browse-ui`, `ui/device-ui`, `ui/identity`,
`ui/instrument`, `ui/sandbox-ui`, `ui/tune-ui`. The 9 without the HTML clause: `browse/facets`,
`browse/filter`, `browse/query`, `browse/return`, `browse/sort`, `device/snapshot`, `sim/demo`,
`store/collections`, `store/local`.

The six dead exports to delete (13.2-CONTEXT D-10), with their doc comments: `sandbox/model.ts:325
cellAt`, `:341 isOnSurface`; `tune/surprise.ts:125 SURPRISE_BUDGET_MS`; `ui/shell/layout.ts:75
RAIL_FR` (and the `:71` comment's "RAIL_FR is its provenance"), `:203 INTRO_STRIP_H` (and the `:200`
paragraph), `:281 TYPE_SCALE` (and the `:45-46` claim that two specs read it — neither does; D-17's
36 / 30 / 17 stay as one line pointing at `app.css`'s `.type-*` roles). Kept by name with the reason
in the SUMMARY: `pad/ready.ts:37 resetPadReadyForTests`, `page-target.ts:433 DISCARD_LABEL`,
`sandbox/copy.ts:175 overLine`, `sandbox/geometry.ts:448 placeInWords` (not this plan's files, not
touched).

The over-exports whose `export` keyword goes (13.2-CONTEXT D-11; `audit-dead.mjs`'s second list,
re-run by the executor at HEAD and the list pasted in the SUMMARY): `browse/rail.ts MADE_FOR`;
`catalog/touch-guard.ts NE`; `og/render.ts FRAME_BYTES, CELL_PX, CELL_GUTTER_PX, CELL_PAINT_PX,
FACE_PX, DOT_PX, FRAME_PX, FRAME_RADIUS_PX, FRAME_STROKE_PX`; `sandbox/colour-knob.ts latticeValues`;
`sandbox/draft.ts surfaceDraftId, surfaceRecord`; `sandbox/geometry.ts offSurfaceField,
edgeAdjacent`; `sandbox/model.ts DEFAULT_MINIMUM_SIZES, minimumSizeOf`; `store/collections.ts
isCollection`; `store/schema.ts isRegion, isSurface`; `store/transfer.ts READABLE_OLDER_SCHEMAS,
EXPORT_EXTENSION, rackOf`; `ui/radius-allowlist.ts EXEMPT_VALUES, normaliseValue, declarationsIn`;
`ui/shell/layout.ts INSPECTOR_W`. NOT in this plan (their files are later plans'): `sandbox/editor.ts`,
`sandbox/emit.ts`, `sandbox/runtime.ts`, `sim/*`, `transport/*`, `tune/view.ts` rows — each later
plan takes its own. NEVER: `ui/library/words.ts CUSTOM_SURFACE` (a copy module; the copy-exports hash)
and the three export-list / type rows.

The ghost names to reword (comments only): `landLua` at `land.ts:7`, `lua-pad-sim.ts:211` (this
plan's? no — `lua-pad-sim.ts` is 06's; reword `land.ts:7` here and leave the other three to their
plans, named), `syncAddress` at `share/url.ts:20` (Coverflow's retired accessor — the fact is that
another entry drops the stamp), `askForecast` at `tune/model.ts:637` (06's), `SWATCH_ROW_MAX` at
`model.ts:434` (06's). `radius-allowlist.ts:31-34`: the gloss names `:829 / :857 / :872` — lines that
moved to `:840 / :867 / :882` at 13-09; correct the three numbers in the prose, touch nothing in
`CIRCLES`.

`docs/CODE-STYLE.md`'s content, in this order and no more: (1) what a header is and the ten-line
rule, with one good header quoted from this plan's work; (2) the provenance line — `// Decided at
13-12 (D-06); see .planning/phases/13-gui-overhaul/13-12-SUMMARY.md` — at most one per header and
one per section; (3) the fact-not-argument test ("a rule a spec enforces stays as one line; the
alternative that lost goes to the SUMMARY"), with `session.svelte.ts:455`'s `#started` as the worked
example (keep "Per instance: every test constructs its own session"; cut the module-scope-flag
argument); (4) a catalog entry's three headings `MECHANISM`, `WHAT IT SENDS`, `TRAPS` and where the
rest goes (`docs/entries/<id>.md`); (5) `.svelte` files: the `<!-- -->` block, the script's leading
comment, comments inside `<style>` (Vite strips them), and the two things a `.svelte` file may never
do (rename; move a rule between components); (6) the refuse-list verbatim from 13.2-CONTEXT D-06; (7)
the gate: `scripts/13.2-gate.sh --before <tag>` / `--after <tag> --against <tag>`, what it holds, where
its records go, and `strip-variants`-style proof that a comment edit changed no behaviour; (8)
"Appended" — a dated list later plans add to, empty at creation. Prettier-clean; no emoji; the
copyright footer the docs carry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.2-01-01: The rule, the gate script and the phase's --before</name>
  <files>docs/CODE-STYLE.md, scripts/13.2-gate.sh, scripts/gate/hash-wire.mjs, scripts/gate/hash-strings.mjs, scripts/gate/ts-ext-register.mjs, scripts/gate/ts-ext-hooks.mjs, scripts/gate/comment-lines.mjs, .planning/phases/13.2-readability/gate/</files>
  <action>
    1. Before any other edit: `netstat -ano | grep -w LISTENING | grep ":4173"` is empty (kill the
       tree per `docs/TESTING.md` "Before an e2e run" if not); `git status --porcelain -- src e2e
       scripts docs` is empty apart from nothing (the user's three untracked root files and
       `.claude/` are not the tree's); note HEAD and the free memory.
    2. Seed `scripts/gate/` from the scratchpad (the `<interfaces>` block says what changes: the two
       path constants become `import.meta.url` resolution; nothing in the output format moves).
       Write `comment-lines.mjs`: for every `.ts` / `.svelte` under `src/` outside `src/vendor/`
       and outside `*.spec.ts`, the audit's line classes (a comment line starts with `//`, `*`, `/*`
       or `<!--` or sits inside a block; the header is the leading comment run — for `.svelte` the
       `<!-- -->` before `<script>` plus the leading comment inside it), `--out F` writing `{ files,
       totals: { lines, code, comment, blank, header }, perFile: [...] }`, and `--against OLD.json`
       printing a markdown table `file | lines before | after | comment before | after | header
       before | after` for every file whose numbers moved plus a totals row. Every `.mjs` carries
       the tree's copyright line; `npx prettier --write scripts/gate/*.mjs` then `npx eslint
       scripts/gate` clean.
    3. Write `scripts/13.2-gate.sh` (bash; `set -u`; `export MSYS_NO_PATHCONV=1`; `cd` to the repo
       root from its own location). `--before <tag>` writes `gate/<tag>.wire.json` (`--full`),
       `gate/<tag>.strings.json`, `gate/<tag>.lines.json` and `gate/<tag>.txt` — HEAD, dirty flag,
       free memory, the wire set hash and record count, the three string hashes with their counts,
       `npm run check 2>&1 | tail -1`, `npm run lint`'s exit, the quick suite through
       `check-counts.mjs 94 966` (its last three lines), then `npm run build`'s exit and stamp, `git
       status --porcelain` over the fixture paths (must print nothing), `cat
       build/_app/immutable/assets/*.css | sha256sum` with the stylesheet count, `ls static/og | wc
       -l` and `cat static/og/*.png | sha256sum`, and `git hash-object` of the three fidelity
       fixtures and `frames.json`. `--after <tag> --against <before-tag>` does the same into
       `gate/<tag>.txt` and then compares: wire set equal (else print every record whose sha moved
       from the two JSONs), the three string hashes equal (else `hash-strings.mjs --diff`'s lines),
       CSS hash, OG count and hash, the four fixture hashes equal, `git diff --stat
       <before-HEAD>..HEAD -- src/vendor src/lib/fidelity/upstream-manifest.json
       src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte` empty, check's file count equal to the
       figure passed as `--check <n>` (655 for this plan), the quick line green; exit 1 on the first
       inequality with the term named. The `.txt` is what the SUMMARY pastes.
    4. Run `scripts/13.2-gate.sh --before 01` on the untouched tree. Every figure it prints is
       compared by eye against 13.2-VALIDATION's baseline block (the wire `a24b256f…`, `--full`
       `514cb2c7…`, census `e6af80e0…`, exports `10fad002…`, testids `21fc9eb9…`, check 654, the
       fixtures, the OG bytes; the CSS hash is measured against a build at THIS HEAD and stated —
       the research's `f89f1f4e…` was `71dbb4f`'s build, and a docs-only commit changes the stamp,
       not the CSS). Any difference is reported in the SUMMARY before the first edit; none is
       expected under `src/`.
    5. Write `docs/CODE-STYLE.md` per the `<interfaces>` block. `npx prettier --check
       docs/CODE-STYLE.md`.
    6. Commit `docs/CODE-STYLE.md`, `scripts/13.2-gate.sh`, `scripts/gate/*.mjs` and
       `gate/01.*` together: `docs(13.2-01): the readability rule written once in CODE-STYLE.md; the
       gate script seeded from the research's harnesses with --before 01 recorded at <HEAD> - wire
       <8 chars>, --full <8>, census <8>, check 654`.
  </action>
  <verify>
    <automated>bash scripts/13.2-gate.sh --before 01 && test -s .planning/phases/13.2-readability/gate/01.wire.json && npx prettier --check docs/CODE-STYLE.md scripts/gate && npx eslint scripts/gate</automated>
  </verify>
  <done>The rule is one file; the gate script and its five harnesses are in the tree, lint-clean; `gate/01.*` records the baseline at HEAD with every hash equal to the research's (the CSS hash stated against a fresh build); nothing under src/ has moved yet.</done>
</task>

<task type="auto">
  <name>Task 13.2-01-02: One stripComments for 33 specs; six dead exports gone; the over-exports demoted</name>
  <files>src/test-support/source.ts, the 33 specs named in the interfaces block, src/lib/sandbox/model.ts, src/lib/tune/surprise.ts, src/lib/ui/shell/layout.ts, src/lib/browse/rail.ts, src/lib/catalog/touch-guard.ts, src/lib/og/render.ts, src/lib/sandbox/colour-knob.ts, src/lib/sandbox/draft.ts, src/lib/sandbox/geometry.ts, src/lib/store/collections.ts, src/lib/store/schema.ts, src/lib/store/transfer.ts, src/lib/ui/radius-allowlist.ts</files>
  <action>
    1. Create `src/test-support/source.ts` as the `<interfaces>` block spells it (the body of
       `stripComments` byte-identical to the 24-spec variant — copy it from `device-ui.spec.ts:234`,
       do not retype it).
    2. The 24: replace the local `const strip = …` / `const stripComments = …` definition with
       `import { stripComments } from "<relative>/test-support/source";` (relative from the spec's
       directory — `../../test-support/source` from `src/lib/<dir>/`, `../test-support/source` from
       `src/lib/`), and where the local name was `strip`, rename its uses in that file to
       `stripComments` (a body edit, no title). Keep every other local helper (`sourceOf`, `raw`,
       `repo`, `occurrences`, `read`) as it is — this plan unifies one function, not six.
    3. The 9 without the HTML clause: for each, list the files the spec reads (its `SOURCE_PATH`,
       `readFileSync` and `repo(...)` arguments) and assert in the log, with one node line, that
       `oldStrip(text) === stripComments(text)` for every one — true whenever the file contains no
       `<!--`; then swap as in step 2. A spec for which the assertion is false keeps its local copy
       and is named in the SUMMARY (none is expected: all nine read `.ts` files).
    4. `npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966`
       — green at the carried pair. A red here is a needle that spanned a comment: restore that
       spec's local copy and name it.
    5. Delete the six dead exports with their doc comments (the `<interfaces>` block, by file and
       line); reword `layout.ts:45-46` and `:71` so no line claims a reader that does not exist.
       Re-run `node <scratchpad>/audit-dead.mjs` at HEAD and paste its first list into the SUMMARY
       with the decision beside each of the ten (six deleted, four kept by name — the reasons are
       13.2-CONTEXT D-10).
    6. Drop the `export` keyword on the over-exports listed for THIS plan's files (the `<interfaces>`
       block); `words.ts CUSTOM_SURFACE` is not touched. `npm run check` — a dropped `export` that
       something imported after all is a type error here, and the answer is to put it back and
       name it, not to leave the import broken.
    7. `node scripts/gate/hash-strings.mjs --diff .planning/phases/13.2-readability/gate/01.strings.json`
       prints no `-` or `+` line (no literal moved; the six deleted exports carried none).
    8. Prettier on every file touched, by path. Quick suite again through `94 966`; `npm run lint`.
       Commit the source files and the specs together: `refactor(13.2-01): one stripComments in
       src/test-support/source.ts for 33 specs (24 + 9, the nine proved equal over their sources);
       cellAt, isOnSurface, SURPRISE_BUDGET_MS, RAIL_FR, INTRO_STRIP_H, TYPE_SCALE deleted by name
       and four dead-looking exports kept by name; <n> export keywords dropped on names nothing
       imports; 94 / 966 (+1 todo), check 655`.
  </action>
  <verify>
    <automated>npx vitest run --project server --maxWorkers=2 2>&1 | node scripts/check-counts.mjs 94 966 && npm run check 2>&1 | tail -1 | grep -E "655 files, 0 errors, 0 warnings" && node scripts/gate/hash-strings.mjs --diff .planning/phases/13.2-readability/gate/01.strings.json | grep -cE "^[-+] " | grep -qx 0</automated>
  </verify>
  <done>33 specs import stripComments from one file and the quick suite is green at 94 / 966; check reads 655; the census, copy-exports and testid hashes are the baseline's; six exports are gone and four are kept, each by name.</done>
</task>

<task type="auto">
  <name>Task 13.2-01-03: The leaf modules under the rule, their specs' headers with them, and --after 01</name>
  <files>the leaf .ts modules in files_modified (browse, catalog helpers, protocol, share, og, store, fidelity, pad, sandbox leaves, tune leaves, device leaves, the ui .ts files, src/lib/index.ts, src/app.d.ts), the spec headers named in files_modified, .planning/phases/13.2-readability/gate/, .planning/STATE.md</files>
  <action>
    1. In the order of 13.2-RESEARCH Appendix 1's line counts (largest first, so the biggest gain
       lands if the list is not finished): for each leaf module, (a) the header to at most ten
       lines — what it is, what it owns, how it is used — plus one provenance line with a pointer
       where the header carried a decision; the copyright line stays; (b) in the body, every
       paragraph that re-argues (13.2-RESEARCH A.2's pattern: fact, alternative, why it lost, the
       plan that measured) to the fact and a pointer; a rule a spec enforces stays as one line and
       says which spec; (c) `WHY …` and `IT WAS N UNTIL PLAN …` paragraphs to one line each; (d) the
       ghost names of this plan's files reworded (`land.ts:7`, `url.ts:20`; the `radius-allowlist.ts`
       gloss corrected to `:840 / :867 / :882`); (e) `history.ts`'s `2. THE COALESCING BOUNDARY`
       heading kept verbatim (`sandbox-ui.spec.ts:723-724` reads it); (f) `firmware-oracle.ts`'s
       firmware `file:line` citations kept — they are the oracle's provenance and the one kind of
       history this rule keeps; (g) `descriptors.ts` may name the four instruction classes; no other
       file's comment may; (h) `presets.ts` is data — header only; (i) `snapshot.ts` and
       `store/local.ts`: "imports nothing, names no window" stays as one line each (`snapshot.spec.ts`,
       `local.spec.ts` scan for it); (j) `constants.ts`: the five firmware defaults' comments keep the
       lengths a spec asserts (`constants.spec.ts`, `skeleton-results.spec.ts` read the file).
       Skip a file already at the rule.
    2. After each directory (browse, catalog, protocol, …): the quick suite through `94 966`. A red
       names a comment a spec read; put the phrase back as one line and note it — do not re-aim a
       spec in this plan (none of these files is on the wording-pin list; a red here is a needle
       the research classed as runtime output and got wrong, and the SUMMARY says so).
    3. The specs beside these modules whose headers run over 25 lines (`facets.spec.ts`,
       `decode.spec.ts`, `constants.spec.ts`, `install-copy.spec.ts` is 03's — not here;
       `firmware-oracle.spec.ts`, `stamp.spec.ts`, `transfer.spec.ts`, `local.spec.ts`,
       `snapshot.spec.ts`, `config-shape.spec.ts`, …): the same rule on the header and the comments;
       a header that explains the assertions beneath it stays (13.2-CONTEXT D-20); bodies and titles
       untouched.
    4. Prettier on every file touched, by path. `npm run check` (655), `npm run lint`, the quick
       suite, then `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` (the tune and
       catalog leaves moved). Commit the modules and the spec headers: `refactor(13.2-01): the leaf
       modules under the ten-line rule - <n> files, <before> comment lines to <after>, provenance as
       one pointer each, the ghost names reworded, the allowlist gloss corrected; 94 / 966 (+1
       todo), sweep 4 19, check 655`.
    5. `scripts/13.2-gate.sh --after 01 --against 01 --check 655` — every hash equal (the build is
       fresh at this HEAD). Then the five chunks: `bash <scratchpad>/e2e-chunks-1208.sh` whole, the
       free memory beside each, `test-results/` removed after; every red rerun alone and named;
       expected `32 + 22 + 21 + 15 + 11 = 101` (the 13.1 gate's `32 + 21 + 21 + 15 + 11 = 100` plus
       the browse title the A.4 quick task added to c2; state the observed chunk sums).
    6. STATE.md by script against a copy (the "Concurrent, Phase 13.2 (recorded, not reconciled)"
       line created with this plan's observed count line, the three assertions held). The SUMMARY
       with the `--before` and `--after` texts pasted whole, the per-file table from
       `comment-lines.mjs --against`, the 33 specs listed, the ten exports with their decisions, the
       over-exports list, the ghost names, every chunk line, and — if the list was not finished —
       "carried" (D-17). Commit the docs: `docs(13.2-01): SUMMARY with the gate's before and after
       pasted, the per-file comment-line table, the 33 swaps and the ten exports by name; STATE by
       script against a copy with status, completed_phases 11 and percent 100 asserted`.
  </action>
  <verify>
    <automated>bash scripts/13.2-gate.sh --after 01 --against 01 --check 655 && npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19</automated>
  </verify>
  <done>Every leaf module in the list opens with at most ten header lines and one pointer, or is named under "carried"; the gate's --after equals its --before on the wire, the strings, the fixtures, the OG bytes and the CSS with check at 655; the sweep 4 19; all five chunks green with every rerun named; STATE recorded.</done>
</task>

</tasks>

<verification>
- `gate/01.txt` (before) and `gate/01-after.txt`: wire set and `--full` equal; census / copy exports
  / testids equal; fixtures, OG bytes, CSS equal; `git diff --stat` over `src/vendor`, the manifest
  and the two circle files empty.
- `check-counts.mjs 94 966` green on the quick suite; `npm run check` 655 / 0 / 0; `npm run lint`
  clean; the sweep `4 19`.
- All five chunks green at 101 runs, reruns named.
- `grep -rn "test-support/source" src --include=*.spec.ts | wc -l` = 33; `grep -rnE "^const (strip|stripComments) = " src --include=*.spec.ts | wc -l` = 0 (or the named exceptions).
- `git diff --quiet -- .planning/ROADMAP.md` exits 0.
</verification>

<success_criteria>
- The rule exists once; the gate exists and has recorded the phase's baseline before any edit.
- One `stripComments`; six dead exports gone by name; the leaf tier under the rule.
- Term `+0 / +0; +0 / +0; check 655` observed and stated; every hash equal.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-readability/13.2-01-SUMMARY.md` with: the observed
count line (`94 / 966 (+1 todo) / 86 / 101 / check 655 / sweep 4 19 / allowlist 0`), the gate's
`--before 01` and `--after 01` texts pasted whole, the per-file before / after comment-line table, the
33 specs swapped (and any kept local, named), the ten exports with the decision beside each, the
over-exports demoted, the ghost names reworded, the chunk lines with the memory beside each and every
rerun, the files carried if any, and the trailer conflict reported. CAT-04 stays `[ ]`.
</output>
