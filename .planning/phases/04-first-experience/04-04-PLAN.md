---
phase: 04-first-experience
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/transport/transport.ts
  - src/lib/transport/transport.spec.ts
  - src/lib/device/try-on.ts
  - src/lib/device/try-on.spec.ts
  - docs/TESTING.md
autonomous: true
requirements: [DEGR-02]

must_haves:
  truths:
    - "Phase 2's failure copy names the button that is actually on the screen: failureCopy gains a third, trailing controlLabel parameter defaulting to \"Connect\", so every existing caller and every existing assertion is untouched."
    - "TRY ON DEVICE connects and identifies and cannot write: no RequestQueue, no heartbeat keeper, and no call to the transport's write method anywhere on the path."
    - "\"It never writes\" is a test, not a promise: a full connect, identify and close cycle against FakeTransport records zero writes."
    - "A module that is not a ZONA, and a port that answers nothing at all, are two different named states with two different sentences."
    - "The capability check is a capability check: no user-agent string is read anywhere on the path."
  artifacts:
    - path: "src/lib/transport/transport.ts"
      provides: "failureCopy(f, raw?, controlLabel = \"Connect\") with the label interpolated at its six Connect sites"
      contains: "controlLabel"
    - path: "src/lib/device/try-on.ts"
      provides: "capabilityOf, the TryOnState union, and identifyOnly - the identify-only device path"
      exports: ["capabilityOf", "identifyOnly", "TRY_ON_LABEL", "TryOnState", "IdentifyOutcome"]
    - path: "src/lib/device/try-on.spec.ts"
      provides: "The never-writes invariant, the two silent states, and the capability branches"
  key_links:
    - from: "src/lib/device/try-on.ts"
      to: "src/lib/transport/sequence.ts"
      via: "newIdentifyState, absorbFrame, identify and identifyTimedOut fold inbound heartbeats"
      pattern: "absorbFrame"
    - from: "src/lib/device/try-on.ts"
      to: "src/lib/protocol/framing.ts"
      via: "FrameScanner and decodeFrame turn chunks into classes"
      pattern: "FrameScanner"
    - from: "src/lib/device/try-on.spec.ts"
      to: "src/lib/transport/fake.ts"
      via: "FakeTransport.writes is asserted empty after a full cycle"
      pattern: "writes"
---

<objective>
Build the device half of the front door: a path that opens a port, listens, names the module, and is
structurally incapable of writing to it.

Purpose: 04-CONTEXT D-13 and D-22 (`TRY ON DEVICE` is connect plus identify in this phase, and the
"it never writes" claim is asserted against `FakeTransport` rather than promised), D-23 and UI-SPEC
W-18 (the control label the copy names), and the Phase 4 half of DEGR-02 — the install control is
present and disabled with its reason on a browser that cannot install, never hidden.
Output: one additive parameter on a Phase 2 function, one new pure module, 7 new tests, and a
corrected row in `docs/TESTING.md`.

This plan writes no Svelte. Plan 04-08 renders these states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-experience/04-CONTEXT.md
@.planning/phases/04-first-experience/04-UI-SPEC.md
@.planning/phases/04-first-experience/04-VALIDATION.md
@.planning/phases/04-first-experience/04-01-SUMMARY.md
@docs/SKELETON-RESULTS.md

<standing_rules>
- **Nothing on this path may reach a write.** `RequestQueue`, `hostHeartbeat`, `sendConfig`,
  `storePage`, `fetchConfig` and `canWriteBack` are all out of scope — starting the keeper would
  itself be a write. `src/lib/protocol/forbidden-instructions.spec.ts` must stay green.
- **`requestPort()` is the first statement in the click handler, with nothing awaited before it.**
  That rule binds plan 04-08, which writes the handler; this plan must not make it impossible by
  requiring an `await` before the call — `identifyOnly` therefore takes an already-open transport.
- **Never edit anything under `src/vendor/`.**
- **Do not change any existing assertion in `src/lib/transport/transport.spec.ts`.** The parameter is
  additive and trailing precisely so that nothing existing moves; if an existing test needs editing,
  the parameter went in the wrong position.
- Files a task creates are `git add`ed before any negative check is run against them.
- `expect: { requireAssertions: true }` is on.
- No Claude or Anthropic attribution anywhere. No emojis. Real apostrophes (U+2019) and a real
  ellipsis (U+2026) in visitor-facing copy.
- Every new source file carries the house header ending
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- **Counting convention (shared tree).** Phase 8 plan 08-01 lands `scripts/check-counts.mjs`, which
  reads a Vitest or Playwright summary on stdin and compares it against an expected file/test count,
  **reporting the todo count without asserting it**. Whole-suite gates in this phase use it with
  **baseline plus delta**: take the observed totals recorded in the previous plan's SUMMARY (for this
  phase's first plan, capture them once before touching anything and write them down), add this task's
  own delta, and pipe `npm run test:quick 2>&1 | node scripts/check-counts.mjs $EXPECTED_FILES $EXPECTED_TESTS`.
  Never hard-code an absolute total in a criterion and never assert a todo count — the tree is shared
  with Phase 8. If `scripts/check-counts.mjs` is absent, fall back to the pair
  `npm run test:quick 2>&1 | grep -E "Tests +[0-9]+ passed"` matching **and**
  `npm run test:quick 2>&1 | grep -qE "Tests +[0-9]+ failed"` exiting non-zero, and say so in the SUMMARY.
  Every SUMMARY records the observed `test:quick` file and test totals so the next plan has a baseline.
- **Every structural scan strips comments first, with one uniform expression.** Six of these checks
  look for a token the same task deliberately *names* in a comment (the painter's header lists the
  forbidden calls; the host cites the vendored file; the spinner explains that it is the loading
  state). A scan over raw source would then fail on correct code. Use this form everywhere, and note
  that it is deliberately **backslash-free** — inside `node -e "…"` both bash and PowerShell eat a
  backslash, which silently turns a pattern into one that matches nothing:

  ```js
  const strip = (t) => t
    .replace(/^[ ]*[/][/].*$/gm, "")      // line comments
    .replace(/[/][*][^]*?[*][/]/g, "")    // block comments
    .replace(/<!--[^]*?-->/g, "");        // markup comments
  ```

  If inline quoting gets fragile, write the check into a scratch `.mjs` under the system temp
  directory and run it with `node` — never into the repository.
</standing_rules>

<interfaces>
**The six sites in `failureCopy` that currently hard-code `Connect`** (`src/lib/transport/transport.ts`):

1. `insecure-context` → `detail`, the clause ending "which is why the Connect button does nothing there."
2. `insecure-context` → `steps[1]`, "Click Connect again"
3. `cancelled` → `steps[0]`, "Click Connect again and pick the ZONA"
4. `port-busy` → `steps[5]`, "Click Connect again"
5. `unplugged` → `steps[2]`, "Click Connect again"
6. `unknown` → `steps[0]`, "Unplug the ZONA, plug it back in, and click Connect again"

No other string in the function mentions a control. `no-web-serial` mentions none at all, and that is
correct — there is no button to click on a browser that cannot do it.

**The three existing positional callers** (do not touch any of them):
`src/lib/transport/transport.spec.ts:72` (`failureCopy("unknown", "boom")`), `:83`
(`failureCopy(failure, "raw text")`), and `src/routes/dev/skeleton/+page.svelte:122-125` (the caught
error's message). This is why the parameter is appended **after** `raw` and never in its place — an
options object would rewrite the same three call sites the trailing parameter exists to leave alone.

**What Phase 2 already provides, and what identification actually costs** (`docs/SKELETON-RESULTS.md`):
the module sends a `HEARTBEAT TYPE 1` carrying its hardware config, its firmware version and its
active page **four times a second, unprompted**, and the host heartbeat is **not required** — arm B
ran with it off for 271.5 s and received 1,086 inbound heartbeats. Identification is therefore a
purely passive fold over inbound frames.

```ts
// src/lib/transport (barrel)
newIdentifyState(startedAt?: number): IdentifyState
absorbFrame(classes: DecodedClass[], state: IdentifyState): void
identify(state: IdentifyState): Identity | undefined      // needs a ZONA heartbeat AND an active page
identifyTimedOut(state: IdentifyState, at?: number): boolean   // IDENTIFY_WINDOW_MS = 1500
interface Identity { zona: ModuleSeen; activePage: number; otherModules: ModuleSeen[]; storeAllowed: boolean }
interface ModuleSeen { sx; sy; rot; hwcfg; moduleType?: string; revision?: string; heartbeatType; firmware: {major;minor;patch}; lastSeen }
// src/lib/protocol (barrel)
class FrameScanner { push(chunk: Uint8Array): number[][] }
decodeFrame(bytes: number[]): { ok: true; classes: DecodedClass[] } | { ok: false; reason: string }
ZONA_HWCFG = 161, IDENTIFY_WINDOW_MS = 1500, ZONA_USB, BAUD_RATE, READ_BUFFER_SIZE
```

**Why `insecure` is its own state even though Chromium cannot produce it.** `webSerialAvailable()`
folds both conditions, so on `file://` the `no-web-serial` branch fires and
`failureCopy("insecure-context")` is unreachable through it. UI-SPEC §Screen 4 nevertheless specifies
two distinct states, keyed on two distinct conditions. Both are built here as a **pure** function over
an explicit environment record, so both branches are reachable in a test and the component reads its
environment once and asks. `navigator.serial` is `[SecureContext]` in Chromium, so in practice the
`insecure` branch is unreachable there; say so in a comment rather than deleting the branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4-04-01: failureCopy names the button that is on the screen</name>
  <files>src/lib/transport/transport.ts, src/lib/transport/transport.spec.ts, docs/TESTING.md</files>

  <read_first>
    - `src/lib/transport/transport.ts` — the whole `failureCopy` function
    - `src/lib/transport/transport.spec.ts` — all six existing tests, to confirm none needs editing
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §"Failure copy comes from Phase 2, with the
      control label interpolated" and W-18
    - `docs/TESTING.md` §"The walking skeleton's test surface" — the per-file count table
  </read_first>

  <behavior>
    - `failureCopy("cancelled")` is byte-identical to what it returns today.
    - `failureCopy("unknown", "boom")` still carries `boom` in its detail — the second parameter is
      still `raw`.
    - `failureCopy(f, undefined, "TRY ON DEVICE")` names `TRY ON DEVICE` and never `Connect`, for
      every failure whose copy names a control.
    - No message in any failure, at any label, says `Chromium`.
  </behavior>

  <action>
Change the signature to:

```ts
export function failureCopy(
  f: OpenFailure,
  raw?: string,
  controlLabel = "Connect",
): FailureCopy
```

and interpolate `controlLabel` at the six sites listed in `<interfaces>`, and nowhere else. Titles,
details and step ordering are Phase 2's words verbatim; only the control's name changes.

Add a short comment recording why the parameter is third rather than second — three callers already
pass `raw` positionally, and inserting the label second would silently retarget all three onto the new
parameter and only fail at the moment a visitor reads the copy. **Put that comment above the function,
never inside the parameter list.** A comment between the parameters would land inside the captured
signature and break the arity probe below (which strips comments defensively anyway, but a signature
is not the place for prose).

**Add exactly one test to `src/lib/transport/transport.spec.ts`** — a seventh, after the existing six,
changing none of them:

> "the copy names the control that is actually on the screen"

For each of `insecure-context`, `cancelled`, `port-busy`, `unplugged` and `unknown`:
- `failureCopy(f, "raw text", "TRY ON DEVICE")` joined across title, detail and steps **contains**
  `TRY ON DEVICE` and **does not contain** the default label;
- `failureCopy(f, "raw text")` joined the same way still contains the default label — the Phase 2
  page's copy is unchanged.

Then assert the same for `no-web-serial` in the honest direction: its copy names no control under
either label, so assert it contains neither. Assert the loop ran over five failures before the
per-failure assertions, so an empty list cannot pass.

Write the default label as a `const` at the top of the test (`const DEFAULT_LABEL = "Connect";`) and
compare against it, rather than repeating the literal in six assertions.

Update `docs/TESTING.md`: the row for `src/lib/transport/transport.spec.ts` moves from **6** to **7**,
and its description gains "and the control label the copy interpolates". A stale count in that table
is worse than no count.

**Negative check.** Interpolate the label at only five of the six sites (drop the `unknown` step), run
the spec, confirm test 7 goes red naming `unknown`, then restore. Record the red line in the SUMMARY.

Run `npx prettier --write src/lib/transport/transport.ts src/lib/transport/transport.spec.ts docs/TESTING.md`, then `npm run lint` and `npm run check`.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/transport/transport.spec.ts 2>&1 | grep -E "Tests +7 passed \(7\)" && npx vitest run --project server src/lib/config-shape.spec.ts 2>&1 | grep -E "Tests +12 passed \(12\)" && npm run check 2>&1 | grep -Ei "0 errors" && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/transport/transport.spec.ts 2>&1 | grep -E "Tests +7 passed \(7\)"` matches.
    - The parameter is trailing and defaulted, checked over a comment-stripped signature: `node -e "const raw=require('node:fs').readFileSync('src/lib/transport/transport.ts','utf8');const src=raw.replace(/^[ ]*[/][/].*$/gm,'').replace(/[/][*][^]*?[*][/]/g,'');const m=/export function failureCopy[(]([^)]*)[)]/.exec(src);if(!m){console.error('signature not found - the matcher is broken, fix it before trusting it');process.exit(1)}const args=m[1].split(',').map(a=>a.trim()).filter(Boolean);if(args.length!==3){console.error('arity: '+args.length);process.exit(1)}if(!/^raw[?]/.test(args[1])){console.error('second parameter is not raw: '+args[1]);process.exit(1)}if(!/^controlLabel = /.test(args[2])){console.error('third parameter is not a defaulted controlLabel: '+args[2]);process.exit(1)}"` exits 0.
    - No existing caller changed: `git diff -- src/routes/dev/skeleton/+page.svelte` produces no output, and `git diff -U0 -- src/lib/transport/transport.spec.ts | grep -c "^-[^-]" ` prints `0` (the spec only gained lines).
    - The Phase 2 page and its e2e still pass: `npx playwright test e2e/skeleton.e2e.ts 2>&1 | grep -E "2 passed"` matches, with the run's output captured under `.tmp-e2e/` and the word `failed` absent from it. Kill any stale wrangler tree on port 4173 first — `docs/TESTING.md` §"Before an e2e run".
    - `docs/TESTING.md` names 7 for that file: `grep -E "transport\.spec\.ts.*\| +7 +\|" docs/TESTING.md` matches.
    - The negative check was observed red and the SUMMARY quotes the failing line; afterwards `git diff --quiet -- src/lib/transport/transport.ts` reports only the intended change (re-run the spec green).
    - `npm run test:quick` is green at the expected count (counting convention above: baseline from the previous SUMMARY plus this task's delta, through `scripts/check-counts.mjs`); `npm run check 2>&1 | grep -Ei "0 errors"` matches; `npm run lint` exits 0.
  </acceptance_criteria>

  <done>Failure copy can name any control, Phase 4 gets `TRY ON DEVICE` in the recovery steps, and Phase 2's page, specs and end-to-end test are all untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4-04-02: The identify-only path, with zero writes by construction</name>
  <files>src/lib/device/try-on.ts, src/lib/device/try-on.spec.ts</files>

  <read_first>
    - `src/routes/dev/skeleton/+page.svelte` — `connect()`, the frame pump, and the ordering rule in
      its comments; this is the proven shape being narrowed
    - `src/lib/transport/sequence.ts` — `newIdentifyState`, `absorbFrame`, `identify`,
      `identifyTimedOut`, and the `Identity` shape
    - `src/lib/transport/sequence.spec.ts` lines 1–100 — the fixture harness: `heartbeatFrame`,
      `classesOf`, and the six-line `pump()` that is the reference implementation
    - `src/lib/transport/fake.ts` — `FakeTransport.writes`, `fromCapture`, and the fact that replay
      emits synchronously on `onData` registration in `instant` mode
    - `.planning/phases/04-first-experience/04-RESEARCH.md` §Architecture Pattern 7 and §Code Examples
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §Screen 4 — the state table and the copy
  </read_first>

  <behavior>
    - `capabilityOf({ hasSerial: false, secure: true })` is `"unsupported"`;
      `{ hasSerial: true, secure: false }` is `"insecure"`; `{ hasSerial: true, secure: true }` is
      `"ok"`; `{ hasSerial: false, secure: false }` is `"unsupported"` — absence wins over insecurity,
      because that is the message that names the fix.
    - A ZONA heartbeat resolves `identifyOnly` with `{ kind: "identified" }` carrying the firmware and
      the active page the heartbeat reported.
    - After a full open, identify and close cycle against `FakeTransport`, `transport.writes` is empty.
    - A heartbeat from a module whose hardware config is not a ZONA's resolves `{ kind: "not-zona" }`
      carrying the reported module type, once the identify window has passed.
    - A port that answers nothing resolves `{ kind: "silent" }` once the window has passed.
    - The path registers exactly one data callback and never calls the transport's write method.
  </behavior>

  <action>
Create `src/lib/device/try-on.ts`.

```ts
export const TRY_ON_LABEL = "TRY ON DEVICE";

export type Capability = "unsupported" | "insecure" | "ok";
export function capabilityOf(env: { hasSerial: boolean; secure: boolean }): Capability;

/** UI-SPEC Screen 4's state machine, as a union the component holds. */
export type TryOnState =
  | "unsupported" | "insecure" | "idle" | "choosing" | "opening" | "identifying"
  | "identified" | "not-zona" | "silent" | "failed" | "unplugged-after";

export type IdentifyOutcome =
  | { kind: "identified"; identity: Identity }
  | { kind: "not-zona"; moduleType: string | undefined }
  | { kind: "silent" };

/**
 * Listen until the module names itself. Takes an ALREADY OPEN transport: the
 * click handler must call requestPort() as its first statement, so nothing on
 * this path may sit in front of it.
 */
export function identifyOnly(
  transport: GridTransport,
  opts?: { now?: () => number; pollMs?: number; sleep?: (ms: number) => Promise<void> },
): Promise<IdentifyOutcome>;
```

Implementation, and the reasons that make it safe:

- One `FrameScanner`, one `newIdentifyState(now())`, one `transport.onData` registration. For each
  chunk: scan to frames, `decodeFrame` each, skip any frame whose `ok` is false (the decode guard
  returns `undefined` on all seven failure exits and never a wrong answer), and `absorbFrame` the
  classes.
- Poll with `identify(state)`; on `identifyTimedOut(state, now())`, resolve `not-zona` if any module
  was seen at all and `silent` if none was — those are two different sentences on screen and must be
  two different outcomes here.
- **No `RequestQueue` and no keeper.** Nothing is requested, so nothing is written; and the keeper
  timer would itself be a write. Say that in the header comment, citing `docs/SKELETON-RESULTS.md` (a)
  and (d): the host heartbeat is not required and the module reports its page unprompted four times a
  second, which is what makes a passive fold sufficient.
- `now`, `pollMs` and `sleep` are injected with real defaults (`performance.now`, 50,
  `setTimeout`-backed) purely so the spec can drive the 1500 ms window without spending 1.5 s of wall
  clock. Say that too.
- `identifyOnly` never closes the transport — the caller owns the port's lifetime, because the port
  belongs to the session and not to one identification attempt (UI-SPEC W-20).

Create `src/lib/device/try-on.spec.ts` — exactly **6 tests**, one per bullet in `<behavior>` with the
first bullet covering all four capability cases in one test.

Driving the fake with inbound frames and no outbound ones: `FakeTransport`'s live responder only fires
on a write, and this path never writes, so build the transport from a capture instead —

```ts
const toHex = (b: number[]) => b.map((n) => n.toString(16).padStart(2, "0")).join("");
const rxOnly = (frames: number[][]) =>
  FakeTransport.fromCapture(
    { events: frames.map((f, n) => ({ n, t: n, dir: "rx", kind: "chunk", hex: toHex([...f, TERMINATOR]) })) } as unknown as Capture,
    { speed: "instant" },
  );
```

`fromCapture` reads only `events`, and in `instant` mode it emits every recorded chunk synchronously
the moment `onData` is registered — so the heartbeat is already absorbed by the time the first poll
runs. Comment that, so the next reader does not add a timer to "wait for" it.

Test 3 is the invariant D-22 exists for, and it has two halves that must both assert:

1. Run the full cycle — build the transport, `await identifyOnly(t)`, `await t.close()` — and assert
   `t.writes` has length 0.
2. Read `src/lib/device/try-on.ts`, strip comments, and assert the source contains no method call
   named `write` — search for the exact substring formed by joining `["." , "write("]`, in the
   `forbidden-instructions.spec.ts` style where the needle is assembled from fragments so the spec's
   own source does not contain the thing it forbids. Also assert it names neither `RequestQueue` nor
   `hostHeartbeat`.

Test 4 needs a non-ZONA heartbeat: reuse `heartbeatFrame` from
`src/lib/transport/fixtures/synthetic.ts` with a hardware config that is not `ZONA_HWCFG` (any other
value in the package's own table; pick one and name it in a comment), plus an injected clock that
jumps past `IDENTIFY_WINDOW_MS` on its second call.

**Negative check.** Add a `void transport.write(new Uint8Array([0]));` line to `identifyOnly`, run the
spec, confirm test 3 goes red on **both** halves (the recorded write and the structural scan), then
remove it. Both halves going red is the point — either alone would be a weaker gate. Record both red
lines in the SUMMARY.

Run `npx prettier --write` on both files, then `npm run lint` and `npm run check`.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/device/try-on.spec.ts 2>&1 | grep -E "Tests +6 passed \(6\)" && npx vitest run --project server src/lib/protocol/forbidden-instructions.spec.ts 2>&1 | grep -E "Tests +5 passed \(5\)" && npm run check 2>&1 | grep -Ei "0 errors" && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/device/try-on.spec.ts 2>&1 | grep -E "Tests +6 passed \(6\)"` matches.
    - The path cannot write, over the comment-stripped source. The pattern is backslash-free on purpose: inside `node -e "…"` a shell eats the backslash and a check like `/[]bimport[]b/` silently becomes one that matches nothing. `node -e "const strip=t=>t.replace(/^[ ]*[/][/].*$/gm,'').replace(/[/][*][^]*?[*][/]/g,'').replace(/<!--[^]*?-->/g,'');const s=strip(require('node:fs').readFileSync('src/lib/device/try-on.ts','utf8'));for(const bad of ['.'+'write(','RequestQueue','hostHeartbeat','sendConfig','storePage','fetchConfig']){if(s.includes(bad)){console.error('try-on.ts reaches '+bad);process.exit(1)}}"` exits 0.
    - No user-agent sniffing anywhere on the path: `node -e "const s=require('node:fs').readFileSync('src/lib/device/try-on.ts','utf8');for(const bad of ['userAgent','Chromium','navigator.vendor']){if(s.includes(bad)){console.error('try-on.ts sniffs the browser: '+bad);process.exit(1)}}"` exits 0.
    - Both halves of the negative check were observed red and the SUMMARY quotes both lines; afterwards `git diff --quiet -- src/lib/device/try-on.ts` exits 0.
    - `npm run test:quick` is green at the expected count (counting convention above: baseline from the previous SUMMARY plus this task's delta, through `scripts/check-counts.mjs`); `npm run check 2>&1 | grep -Ei "0 errors"` matches; `npm run lint` exits 0.
  </acceptance_criteria>

  <done>A visitor's ZONA can be opened, listened to and named, and the code path that does it has no reachable write — proven twice, once by a recorded transport and once by reading the source.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project server src/lib/transport/transport.spec.ts src/lib/device/try-on.spec.ts` reports 13 passed in 2 files.
- `npx playwright test e2e/skeleton.e2e.ts` reports 2 passed — Phase 2's page still behaves.
- `npm run check` reports 0 errors; `npm run lint` exits 0; `npm run test:quick` shows no failures.
- `git diff --quiet -- src/vendor` exits 0.
</verification>

<success_criteria>
- `failureCopy` takes a trailing, defaulted control label and interpolates it at all six sites, with
  every Phase 2 caller and assertion untouched.
- `identifyOnly` resolves into three distinct, tested outcomes and records zero writes across a full
  cycle against `FakeTransport`.
- `docs/TESTING.md` reports the new per-file count rather than a stale one.
</success_criteria>

<output>
After completion, create `.planning/phases/04-first-experience/04-04-SUMMARY.md`.

Record in it: the six interpolated sites as they now read; the exported surface of `try-on.ts`
verbatim (plan 04-08 renders directly against it); the non-ZONA hardware config value the spec used
and where it came from; and both negative-check red lines.
</output>
