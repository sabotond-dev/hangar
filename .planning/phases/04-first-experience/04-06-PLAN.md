---
phase: 04-first-experience
plan: 06
type: execute
wave: 4
depends_on: ["04-02", "04-05"]
files_modified:
  - src/lib/ui/PadFrame.svelte
  - src/lib/ui/PadCanvas.svelte
  - src/lib/ui/Coverflow.svelte
  - src/lib/ui/FrontDoor.svelte
  - src/routes/+page.svelte
  - e2e/first-experience.e2e.ts
autonomous: true
requirements: [PREV-01, PREV-02, PREV-04, IDENT-01]

must_haves:
  truths:
    - "Opening the site shows a row of ZONA pads, one large and centred, its neighbours receding left and right in depth and falling off the edges of the viewport."
    - "Every visible pad is running its own configuration in the firmware-faithful simulator, with no hardware attached and no hand-authored animation anywhere."
    - "Stepping the row with the keyboard, the wheel, a swipe or a click on a side pad brings that pad to the centre, and the row wraps at both ends."
    - "The centre pad accepts mouse-as-finger, so the instrument can be played before it is chosen."
    - "The front door's first chunk does not carry the compiler: the simulator is imported inside onMount and the row paints its structure before it arrives."
  artifacts:
    - path: "src/lib/ui/PadFrame.svelte"
      provides: "The four-layer pad: static dot field, the canvas, the static gutter grid, the frame"
    - path: "src/lib/ui/PadCanvas.svelte"
      provides: "The 9x9 canvas element, its role=img labelling, and its registration with the host"
    - path: "src/lib/ui/Coverflow.svelte"
      provides: "The band, the slot window, every stepping input, the listbox semantics and the hero's pointer routing"
    - path: "src/lib/ui/FrontDoor.svelte"
      provides: "The composition both routes render: header wordmark, headline, coverflow"
    - path: "e2e/first-experience.e2e.ts"
      provides: "The browser half of the proof, against the production build through the Worker"
  key_links:
    - from: "src/lib/ui/Coverflow.svelte"
      to: "src/lib/sim/host.ts"
      via: "one SimHost per mount; register/setHero/setInWindow/destroy"
      pattern: "SimHost"
    - from: "src/lib/ui/Coverflow.svelte"
      to: "src/vendor/botor/pad-sim.ts"
      via: "a dynamic import inside onMount constructs one PadSim per row entry"
      pattern: "import\\("
    - from: "src/lib/ui/Coverflow.svelte"
      to: "src/lib/coverflow/slots.ts"
      via: "every slot's transform comes from slotFor(slotOffset(...))"
      pattern: "slotFor"
    - from: "src/routes/+page.svelte"
      to: "src/lib/ui/FrontDoor.svelte"
      via: "the front door is one component both routes render"
      pattern: "FrontDoor"
---

<objective>
Make the machines run. This is the plan where the site stops being a scaffold: a receding row of live
9x9 pads, each one stepping its own compiled configuration at the firmware's 100 Hz, steppable by
keyboard, wheel, swipe and click, with the centre pad playable under a finger.

Purpose: PREV-01 and PREV-02 (every visible pad animating live, from the compiler's own input),
PREV-04 (mouse-as-finger), IDENT-01 (the 9x9 outline as the pad frame), D-01, D-02, D-10 and D-21.
Output: four components, the front door route, and the first three end-to-end assertions.

Deliberately **not** here: the name plate, the fidelity line and the splash (plan 04-07), choosing
and the panel (plan 04-08), and the deep-link route (plan 04-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/FIRST-EXPERIENCE.md
@.planning/phases/04-first-experience/04-CONTEXT.md
@.planning/phases/04-first-experience/04-UI-SPEC.md
@.planning/phases/04-first-experience/04-VALIDATION.md
@.planning/phases/04-first-experience/04-02-SUMMARY.md
@.planning/phases/04-first-experience/04-05-SUMMARY.md
@docs/TESTING.md

<standing_rules>
- **No `.svelte.spec.ts` files.** `vite.config.ts`'s `server` project excludes that pattern and no
  other project picks it up, so such a file is collected by nothing and never runs — the
  green-and-vacuous trap `docs/TESTING.md` names. Component behaviour is proven in Playwright against
  the real build; everything decidable lives in the pure modules plans 04-02, 04-03 and 04-05 already
  tested.
- **Nothing that holds a `PadSim`, a canvas or a frame buffer goes into a rune.** `$state` deep-proxies
  objects and arrays, and a proxy trap inside a 100 Hz loop is a silent performance cliff
  (04-RESEARCH §Pitfall 3). Only scalars cross into Svelte: `centre` (number), `radius` (number),
  `ready` (boolean). If a non-scalar must be reactive, use `$state.raw`.
- **CSS grouping properties flatten a 3D scene.** `overflow` at any value other than `visible` or
  `clip`, `opacity` below 1, any `filter`, `mask-image`, `mix-blend-mode` and `contain: paint` all
  force `transform-style: flat` on descendants. The clip and the mask go on an **outer** wrapper that
  carries no `preserve-3d`; `filter: brightness()` goes on the **slot wrapper**, which is a leaf of
  the 3D tree. Never on the stage. (04-RESEARCH §Pitfall 5, 04-CONTEXT D-16.)
- **No CSS may author a colour the simulator did not emit** on a pad: no `drop-shadow`, `blur`,
  `hue-rotate`, `sepia` or `invert` anywhere near a pad canvas. `filter: brightness()` is permitted
  and required because it only scales emitted channels toward black.
- **Never edit anything under `src/vendor/`.**
- `npm run format` is scoped to the files the task created.
- No Claude or Anthropic attribution anywhere. No emojis. Real apostrophes (U+2019) and a real
  ellipsis (U+2026) in every visitor-facing string.
- Every new file carries the house header comment ending
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- **Before any Playwright run:** `netstat -ano | grep -w LISTENING | grep ":4173"`. If occupied, walk
  the process tree to the `npx-cli.js` root and `taskkill /PID <root> /T /F` from PowerShell — killing
  `workerd.exe` alone leaves a zombie that accepts connections it never answers. Capture output into
  `.tmp-e2e/`, never under `test-results/`. **No Playwright test title may contain the word `failed`.**
</standing_rules>

<interfaces>
**Row data** (plan 04-02, `src/lib/catalog/front-door.ts`): `FRONT_DOOR: readonly { id, name,
description, motion, quiet? }[]`, eight entries, ring order `aurora, pinwheel, ninepads, starfield,
joystick, radar, faders, dial`; `frontDoorIndex(id): number`.

**Geometry** (plan 04-02, `src/lib/coverflow/slots.ts`): `step`, `slotOffset`, `visibleWindow`,
`slotFor(offset, heroPx) -> { translateX, translateZ, rotateY, scale, opacity, brightness, zIndex,
hero, mounted }`, `radiusForWidth(px) -> 1 | 2 | 3`, `MAX_SLOT = 3`.

**The host** (plan 04-05, `src/lib/sim/host.ts`): `new SimHost(deps?)`, `register(id, canvas,
engine)`, `setHero(id)`, `setInWindow(id, boolean)`, `touchDown/touchMove/touchEnd`, `unregister`,
`destroy`. It sets the canvas to 9x9 itself. `mapAxis(offset, extent, max)` is exported from
`src/lib/sim/touch.ts`.

**The engines**, constructed once per row entry, inside `onMount`, from a dynamic import. Plan 04-02's
spec guarantees every id in `FRONT_DOOR` today is a `preview: "padsim"` catalog entry, but the catalog
grows underneath this phase, so the construction is written defensively rather than on that guarantee:

```ts
const [{ PadSim }, { presetById }] = await Promise.all([
  import("../../vendor/botor/pad-sim"),
  import("../../vendor/botor/_pad"),
]);
const engine = new PadSim(presetById(id)!.state);
```

`new PadSim(state)` costs 0.044 ms measured, so all eight are built eagerly — there is no reason to
lazy-construct (04-RESEARCH §Measured Facts). The import must be dynamic: `_pad.ts` imports
`@intechstudio/grid-protocol` at module scope, and that chunk is 131,101 bytes, which a splash should
not wait on and a static import would put on `/`'s critical path (§Pitfall 9). Phase 8 plan 08-03 will
add `createEngine(entry)` over the same shape; when it lands, this call site becomes a one-line change
and nothing else moves.

**The pad, exactly** (04-UI-SPEC §The pad). Four layers, and **two of the four never repaint**:

| Layer | What | How |
|---|---|---|
| 1 | unlit-cell dot field | CSS `radial-gradient(circle at 50% 50%, var(--color-line-soft) 0 6%, transparent 6.5%)` at `background-size: 11.111% 11.111%` |
| 2 | the LEDs | `<canvas width="9" height="9">`, `image-rendering: pixelated`, CSS-sized to the face; one `putImageData` per paint; unlit cells at alpha 0 so layer 1 shows through |
| 3 | gutter grid | two CSS `repeating-linear-gradient`s in `#000000`, hard stops at `0 0.9%` / `0.9% 11.111%`, one per axis, `background-position: -0.45% -0.45%` |
| 4 | frame | `border: 1px solid` — `--color-line` on the hero, `--color-line-soft` on side pads; `border-radius: 10px`; canvas inset **6px** so the corner arc cannot clip a corner LED; hero only: `box-shadow: 0 0 40px var(--color-glow)` |

**The band** (04-UI-SPEC §Coverflow geometry):

```css
--pad-hero: clamp(260px, 52vmin, 560px);
/* outer wrapper: no preserve-3d here */
.band { width: min(100vw, 1280px); overflow: clip;
        mask-image: linear-gradient(to right, transparent 0%, #000 14%, #000 86%, transparent 100%); }
/* inner stage: the 3D context */
.stage { position: relative; height: var(--pad-hero);
         perspective: 1400px; perspective-origin: 50% 42%; transform-style: preserve-3d; }
/* each slot */
.slot { position: absolute; top: 0; left: calc(50% - var(--pad-hero) / 2);
        width: var(--pad-hero); height: var(--pad-hero);
        transform: translate3d(Xpx, 0, Zpx) rotateY(Rdeg) scale(S);
        transition: transform 420ms cubic-bezier(0.22, 0.61, 0.36, 1),
                    opacity 420ms cubic-bezier(0.22, 0.61, 0.36, 1),
                    filter 420ms cubic-bezier(0.22, 0.61, 0.36, 1); }
```

The transition is on the CSS property rather than driven from JavaScript so a rapid second step
re-targets from the current computed value instead of restarting.

**Stepping inputs** (04-UI-SPEC §Stepping, D-10): `←`/`→` one step, wrapping; `Home`/`End` to first
and last; horizontal wheel accumulating `|deltaX|` with a 40 px threshold and a 260 ms cooldown, with
vertical `deltaY` **ignored** so the page still scrolls to the footer; a click on a side pad steps to
it (a click on slot ±2 steps two); a horizontal touch drag of 48 px or more steps one, while a
vertical drag scrolls. `onwheel` in Svelte 5 attaches directly to the element (`wheel` is not in
Svelte's delegated-event list) and element-level wheel listeners are non-passive by default, so
`event.preventDefault()` works with no plumbing.

**Accessibility** (04-UI-SPEC §Accessibility Contract): the band is `role="listbox"` with
`aria-label="ZONA configurations"`, `tabindex="0"` and `aria-activedescendant` pointing at the centred
slot's id; each slot is `role="option"` with `aria-selected` true only on the centre; each canvas is
`role="img"` with `aria-label="{name}, live pad simulation"` and the slot's accessible name is
completed by a visually-hidden `{description}`. Cursor: `crosshair` on the hero (it is an instrument),
`pointer` on side pads.

**Test ids:** `coverflow`, `pad-{id}`, `pad-canvas-{id}`.

**Copy that ships in this plan** (04-UI-SPEC §Copywriting Contract, verbatim):
header wordmark `HANGAR`; headline `You’ve got to start somewhere…` (Body role, `--color-ink-quiet`,
centred, with a real U+2019 and a real U+2026).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4-06-01: The pad — four layers, two of which never repaint</name>
  <files>src/lib/ui/PadFrame.svelte, src/lib/ui/PadCanvas.svelte</files>

  <read_first>
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §The pad (the layer table and the four hard
      prohibitions) and §Color (the pads-are-content exemption)
    - `src/vendor/botor/pad-sim-host.ts` lines 92–122 — the vendored `blit`, read only to know what is
      being replaced
    - `.planning/phases/04-first-experience/04-05-SUMMARY.md` — `SimHost.register`'s exact signature
    - `src/app.css` — the tokens plan 04-01 landed
  </read_first>

  <action>
**`src/lib/ui/PadFrame.svelte`** — props `{ entry, hero, children }` (or a snippet slot for the
canvas). It renders the pad box and its three static layers, and nothing else:

- the root element carries the border, the radius, and on the hero the `--color-glow` box-shadow;
- an absolutely positioned dot-field div, inset 6px, `pointer-events: none`;
- the canvas slot, inset 6px;
- an absolutely positioned gutter div, inset 6px, `pointer-events: none`, above the canvas.

Layer order bottom to top is dots, canvas, gutters — the canvas's unlit cells are transparent, so the
dots show through them, and the black gutter grid on top is what turns a gapless nearest-neighbour
upscale back into 81 discrete lights. Put that sentence in the component's comment: it is the whole
reason three of these four layers exist.

`data-testid="pad-{entry.id}"`.

**`src/lib/ui/PadCanvas.svelte`** — props `{ entry, hero }` plus a callback the parent uses to collect
the element (`onready: (id: string, canvas: HTMLCanvasElement) => void`), called from `onMount`.

- one `<canvas>` with `data-testid="pad-canvas-{entry.id}"`, `role="img"`,
  `aria-label="{entry.name}, live pad simulation"`;
- CSS: `display: block; inline-size: 100%; block-size: 100%; image-rendering: pixelated;`
- **do not** set `width`/`height` attributes here — `SimHost.register` sets the 9x9 backing store, so
  there is exactly one place that decides it;
- **no** `getContext` call in this component; the host owns the context and the scratch buffer.

Both components are presentational: no `PadSim`, no import from `src/vendor/`, no `$effect` that
touches a canvas.

Wire nothing yet — the next task mounts them. Verification for this task is the type checker, the
linter and a build, plus the structural greps below.

Run `npx prettier --write src/lib/ui/PadFrame.svelte src/lib/ui/PadCanvas.svelte`, then `npm run lint`
and `npm run check`.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -E "0 errors" && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - Both files exist and neither reaches the simulator: `node -e "const fs=require('node:fs');for(const f of ['src/lib/ui/PadFrame.svelte','src/lib/ui/PadCanvas.svelte']){const s=fs.readFileSync(f,'utf8');for(const bad of ['vendor/','PadSim','getContext','intechstudio']){if(s.includes(bad)){console.error(f+' reaches '+bad);process.exit(1)}}}"` exits 0.
    - The canvas is upscaled by the compositor, not by JavaScript: `grep -q "image-rendering: pixelated" src/lib/ui/PadCanvas.svelte` exits 0.
    - The three static layers are CSS, not draw calls: `grep -q "radial-gradient" src/lib/ui/PadFrame.svelte` exits 0 and the gutter grid is two gradients, counted as occurrences rather than lines: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/PadFrame.svelte','utf8');const n=(s.match(/repeating-linear-gradient/g)||[]).length;if(n!==2){console.error('gradients: '+n);process.exit(1)}"` exits 0.
    - No colour-authoring filter near a pad: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/PadFrame.svelte','utf8');for(const bad of ['drop-shadow','hue-rotate','blur(','sepia','invert(']){if(s.includes(bad)){console.error('PadFrame authors light the simulator did not: '+bad);process.exit(1)}}"` exits 0.
    - The canvas inset matches the radius rule: `grep -q "6px" src/lib/ui/PadFrame.svelte` exits 0.
    - `npm run check 2>&1 | grep -E "0 errors"` matches; `npm run lint` exits 0; `npm run build` exits 0.
  </acceptance_criteria>

  <done>A pad is one canvas over a static dot field under a static gutter grid inside a lime frame, with the backing store owned by the host and no draw call anywhere in the markup.</done>
</task>

<task type="auto">
  <name>Task 4-06-02: The coverflow — the band, the window, and every way to step it</name>
  <files>src/lib/ui/Coverflow.svelte</files>

  <read_first>
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §Coverflow geometry, §Stepping,
      §Accessibility Contract, §Interface motion
    - `.planning/phases/04-first-experience/04-02-SUMMARY.md` — `slots.ts` and `front-door.ts` surfaces
    - `.planning/phases/04-first-experience/04-05-SUMMARY.md` — `SimHost` surface and `HostDeps`
    - `.planning/phases/04-first-experience/04-RESEARCH.md` §Pitfall 3, §Pitfall 5, §Pitfall 9,
      §Pitfall 10, §Architecture Pattern 8
    - `src/routes/dev/fidelity/+page.svelte` — the established dynamic-import-inside-`onMount` shape
  </read_first>

  <action>
**`src/lib/ui/Coverflow.svelte`** — props `{ initialId?: string }`. One component owns the row.

**State.** `let centre = $state(...)` (a number), `let radius = $state(3)`, `let ready = $state(false)`.
Nothing else is reactive. The `SimHost` and the engines live in plain `let` bindings — a `Map<string,
HostEngine>` and the host instance — never in a rune. Canvas elements are collected into a plain
`Map<string, HTMLCanvasElement>` by `PadCanvas`'s `onready` callback.

**Markup.** All eight entries are rendered in a keyed `{#each FRONT_DOOR as entry (entry.id)}` in
**stable catalog order, never reordered**. Stepping changes `centre` only; every slot's transform is
derived from `slotFor(slotOffset(index, centre, FRONT_DOOR.length), heroPx)`. A keyed each that
reordered would move DOM nodes and fight the layout engine instead of the compositor.

Slots with `mounted === false` render nothing at all — no element, no canvas, no engine registration
(04-UI-SPEC W-14: bounding the canvas count at 7 is what keeps compositor memory flat as the catalog
grows). Keep the engine itself alive in the map: an entry leaving the window and returning must not
restart at tick 0.

The band, the stage and the slot use exactly the CSS in `<interfaces>`. `translateX` is in pixels and
comes from `slotFor`, which takes the hero's measured pixel side length — read it once per resize from
the stage element's own computed height (the CSS `clamp()` owns the value; JavaScript reads it and
never sets it).

**Mount.** In `onMount`:

1. construct the `SimHost`;
2. `await` the two dynamic imports and build one engine per row entry. Build an engine **only** for an
   entry the vendored shelf knows: `presetById(id)` returning `undefined` means the catalog has grown
   past what a `PadSim` can run (Phase 8 appends Lua-sourced entries, and their engine arrives with
   plan 08-03), so skip that entry, `console.warn` once naming the id, and render its slot as the frame
   and dot field with **no canvas** — the broken-entry state 04-UI-SPEC §Empty and broken states
   already specifies. One entry without an engine must never blank the row;
3. register every mounted canvas with the host, `setHero(FRONT_DOOR[centre].id)`, and
   `setInWindow(id, true)` for the window;
4. set `ready = true`.

Nothing before step 4 blocks the markup: the row's frames, dots and gutters are in the prerendered
HTML and visible immediately; the canvases fill in when the simulator arrives. That is what gives the
splash (plan 04-07) a real job rather than a decorative one.

**Stepping.** One `stepBy(delta)` function calling `step(centre, delta, count)`, then updating the
host: `setHero(newId)`, and `setInWindow` true/false across the changed window. Inputs, all of them:

- `onkeydown` on the band: `ArrowLeft`/`ArrowRight` (one step, `preventDefault`), `Home`/`End`;
- `onwheel` on the band: accumulate `Math.abs(e.deltaX)`, fire one step at 40 and start a 260 ms
  cooldown, `preventDefault()` only when a horizontal step is taken; ignore `deltaY` entirely;
- `onclick` on a side slot: step by that slot's offset;
- `onpointerdown`/`onpointerup` on the band for the touch drag: a horizontal movement of 48 px or more
  steps one; a vertical drag is left alone so the page scrolls.

**Resize.** A `resize` listener, debounced 200 ms, recomputing `radius = radiusForWidth(innerWidth)`
and the hero pixel size, and skipping all work when neither changed — reallocating backing stores per
resize event is its own jank source (§Pitfall 10). Remove the listener in `onDestroy`.

**The hero's finger (PREV-04).** `onpointerdown`, `onpointermove`, `onpointerup`, `onpointercancel`
and `onlostpointercapture` on the hero slot only. On down: `setPointerCapture` inside a `try`/`catch`
(the canvas can detach between the event and the capture), then map the client coordinates through the
canvas's `getBoundingClientRect()` and `mapAxis(offset, extent, engine.coordMax)` and call
`host.touchDown(...)`. Move and end likewise. The host does the tick-locked delivery; this component
does the geometry and nothing more.

D-11's "hover already shows mouse-as-finger before choosing" falls out for free, because the listeners
follow whichever entry is centred. **The tap-versus-choose rule (250 ms, 6 px) belongs to plan 04-08**,
which owns choosing; do not add a half of it here.

**Teardown.** `onDestroy`: `host.destroy()`, remove the resize listener, drop the engine map.

**Verification for this task is a real browser.** Add nothing to Playwright yet — the next task
creates the e2e file and the route that renders this. For now, prove it type-checks, lints, builds,
and that the structural rules hold.

Run `npx prettier --write src/lib/ui/Coverflow.svelte`, then `npm run lint` and `npm run check`.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -E "0 errors" && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - Every import of the vendored tree is dynamic: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/Coverflow.svelte','utf8');const statics=[...s.matchAll(/^\s*import[^;]*?from\s*[\x22\x27]([^\x22\x27]+)[\x22\x27]/gm)].map(m=>m[1]);for(const sp of statics){if(/vendor|intechstudio|lib\/pad/.test(sp)){console.error('static import of '+sp);process.exit(1)}}if(!/await\s+Promise\.all|await\s+import\(/.test(s)){console.error('no dynamic import found');process.exit(1)}console.log(statics.join(' '))"` exits 0.
    - The 3D context and the clip are on different elements: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/Coverflow.svelte','utf8');const band=/\.band\s*\{[^}]*\}/.exec(s);const stage=/\.stage\s*\{[^}]*\}/.exec(s);if(!band||!stage){console.error('band or stage rule not found');process.exit(1)}if(!band[0].includes('overflow: clip')){console.error('band does not clip with overflow: clip');process.exit(1)}if(band[0].includes('preserve-3d')){console.error('the clipping wrapper carries preserve-3d');process.exit(1)}if(!stage[0].includes('preserve-3d')||!stage[0].includes('perspective')){console.error('stage is not the 3D context');process.exit(1)}if(/filter/.test(stage[0])){console.error('a filter on the stage flattens the scene');process.exit(1)}"` exits 0.
    - `overflow: hidden` appears nowhere in the component: `grep -c "overflow: hidden" src/lib/ui/Coverflow.svelte` prints `0`.
    - The geometry comes from the tested module rather than from inline numbers: `grep -q "slotFor" src/lib/ui/Coverflow.svelte` exits 0 and `grep -q "slotOffset" src/lib/ui/Coverflow.svelte` exits 0.
    - No engine, canvas or frame buffer is in a rune: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/Coverflow.svelte','utf8');const runes=[...s.matchAll(/\$state(?:\.raw)?\(([^)]*)\)/g)].map(m=>m[1].trim());for(const r of runes){if(/PadSim|new Map|SimHost|canvas|frame/i.test(r)){console.error('a non-scalar is in a rune: '+r);process.exit(1)}}console.log(runes.join(' | '))"` exits 0.
    - The listbox semantics are present: `grep -q 'role="listbox"' src/lib/ui/Coverflow.svelte`, `grep -q "aria-activedescendant" src/lib/ui/Coverflow.svelte` and `grep -q 'role="option"' src/lib/ui/Coverflow.svelte` all exit 0.
    - `npm run check 2>&1 | grep -E "0 errors"` matches; `npm run lint` exits 0; `npm run build` exits 0.
  </acceptance_criteria>

  <done>One component owns the row: eight entries mounted once in stable order, seven of them visible, every transform derived from the tested slot math, four ways to step it, and the centre pad taking a finger.</done>
</task>

<task type="auto">
  <name>Task 4-06-03: The front door renders, and a browser proves the pads are alive</name>
  <files>src/lib/ui/FrontDoor.svelte, src/routes/+page.svelte, e2e/first-experience.e2e.ts</files>

  <read_first>
    - `src/routes/+page.svelte` and `src/routes/+layout.svelte` — the placeholder being replaced and
      the footer that stays
    - `e2e/smoke.e2e.ts` — it asserts a level-1 heading is visible on `/`; that must stay true
    - `e2e/fidelity.e2e.ts` and `e2e/skeleton.e2e.ts` — the house e2e style: assert a precondition
      before counting, filter console output to errors, use trailing slashes
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §Screen 2 (the vertical stack and its
      spacing) and §Copywriting Contract
    - `docs/TESTING.md` §"Before an e2e run"
  </read_first>

  <action>
**`src/lib/ui/FrontDoor.svelte`** — props `{ initialId?: string; splash?: boolean }`. The composition
both routes render:

- the header wordmark, top-left at the 32 px gutter, Micro role (12px / 600 / 0.18em / uppercase),
  as the page's single `<h1>`. It is an `<h1>` deliberately: it is the site's name on its front page,
  it keeps `e2e/smoke.e2e.ts` green, and a screen reader gets a document title rather than a stray
  uppercase string. It is not focusable;
- the headline, Body role in `--color-ink-quiet`, centred, 48 px below the top at desktop:
  `You’ve got to start somewhere…`;
- `<Coverflow {initialId} />`, 32 px below the headline;
- the section is `min-block-size: 100svh` with a 32 px page gutter at desktop and 24 px below 640 px.

The `splash` prop is accepted and **unused** in this plan — plan 04-07 renders the splash from it.
Declare it now so the route files do not change twice.

**`src/routes/+page.svelte`** becomes `<FrontDoor splash />` and nothing else. The layout's GPLv3
footer is untouched (FOUND-04) and now follows below the fold.

**`e2e/first-experience.e2e.ts` — exactly 3 tests in this plan.** Header comment: what it covers, that
it runs against `build/` served by `worker/index.js` under `wrangler dev`, and that Web Serial itself
is not automatable so the device half is a human check.

1. **"the front door animates"** — go to `/`, wait for `coverflow` to be visible, then wait for the
   hero canvas to have a non-empty picture, then sample its pixels twice 400 ms apart and assert the
   two samples differ. Sample with

   ```ts
   const sample = (id: string) =>
     page.evaluate((sel) => {
       const c = document.querySelector(sel) as HTMLCanvasElement | null;
       if (!c) return null;
       const ctx = c.getContext("2d");
       if (!ctx) return null;
       return Array.from(ctx.getImageData(0, 0, 9, 9).data).join(",");
     }, `[data-testid="pad-canvas-${id}"]`);
   ```

   Assert the first sample is non-null and contains at least one non-zero byte **before** comparing —
   a test that compares two nulls passes for the wrong reason. Use `aurora`, whose motion is declared
   `animated` and pinned by plan 04-02's gate.

2. **"a still configuration really is still"** — the same sampling on `ninepads`, asserting the two
   samples are **equal** and non-empty. This is the other half of the honesty claim: the classification
   in `front-door.ts` is about the configuration, not about a stalled loop, and a row where everything
   changed between samples would be as wrong as one where nothing did. Reach `ninepads` by pressing
   `ArrowRight` twice from the opening centre (it is index 2) and waiting for the transition; assert
   the band's `aria-activedescendant` names it before sampling.

3. **"the row steps with the keyboard and wraps"** — focus the band, assert `aria-activedescendant`
   names `aurora`, press `ArrowLeft` once, assert it names `dial` (the wrap), press `ArrowRight` twice,
   assert `pinwheel`, then press `Home` and assert `aurora` again.

Every test also asserts the console produced no `error`-level messages, filtered the way
`e2e/skeleton.e2e.ts` does — the protocol package logs at `console.log` from module scope and an
unfiltered assertion would be permanently red.

Run the suite, capture to `.tmp-e2e/`, and check the port first.

Update `docs/TESTING.md`: add `e2e/first-experience.e2e.ts` to the e2e paragraph with its count, and
correct the `npm run test:e2e` row's total.

Run `npx prettier --write` on the three files plus `docs/TESTING.md`, then `npm run lint` and
`npm run check`.
  </action>

  <verify>
    <automated>npm run build && mkdir -p .tmp-e2e && npx playwright test e2e/first-experience.e2e.ts 2>&1 | tee .tmp-e2e/04-06.log | grep -E "3 passed" && npx playwright test 2>&1 | tee .tmp-e2e/04-06-suite.log | grep -E "passed" && ! grep -q failed .tmp-e2e/04-06-suite.log && npm run check 2>&1 | grep -E "0 errors" && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - `npx playwright test e2e/first-experience.e2e.ts` reports `3 passed`, the captured log under `.tmp-e2e/` does not contain the word `failed`, and no test title contains it either.
    - the full suite is green: the captured log of `npx playwright test` contains `passed` and does **not** contain the word `failed` (an absolute suite total is deliberately not asserted — Phase 8 plan 08-07 adds a Playwright test of its own and its waves interleave with these). The three pre-existing e2e files still pass by name: `npx playwright test e2e/smoke.e2e.ts e2e/fidelity.e2e.ts e2e/skeleton.e2e.ts 2>&1 | grep -E "8 passed"` matches.
    - `e2e/smoke.e2e.ts` still passes — the front door renders exactly one level-1 heading: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/FrontDoor.svelte','utf8');const n=(s.match(/<h1/g)||[]).length;if(n!==1){console.error('h1 count: '+n);process.exit(1)}"` exits 0.
    - `/` is a real prerendered file that already contains the row's structure: `test -f build/index.html` exits 0 and `grep -q 'data-testid="coverflow"' build/index.html` exits 0.
    - The front door's entry does not preload the compiler's chunk: `node -e "const fs=require('node:fs');const dir='build/_app/immutable/chunks';const heavy=fs.readdirSync(dir).filter(f=>f.endsWith('.js')&&fs.readFileSync(dir+'/'+f,'utf8').includes('GRID_PARAMETER_ELEMENT_POTMETER'));if(heavy.length===0){console.error('the grid-protocol chunk was not found - the probe is stale, fix it before trusting it');process.exit(1)}const html=fs.readFileSync('build/index.html','utf8');for(const f of heavy){if(html.includes(f)){console.error('index.html references '+f);process.exit(1)}}console.log('front door is clear of '+heavy.join(', '))"` exits 0 and prints the chunk name.
    - The copy is the contract's, with real typography: `node -e "const s=require('node:fs').readFileSync('src/lib/ui/FrontDoor.svelte','utf8');if(!s.includes('You’ve got to start somewhere…')){console.error('the headline is not the approved string with a real apostrophe and ellipsis');process.exit(1)}"` exits 0.
    - `npm run test:quick 2>&1 | grep -E "Tests +[0-9]+ passed \| 1 todo"` matches; `npm run check 2>&1 | grep -E "0 errors"` matches; `npm run lint` exits 0; `npm run build` exits 0.
    - No stray `wrangler dev` is left listening on 4173 when the plan ends.
  </acceptance_criteria>

  <done>Opening the site shows a receding row of pads that are genuinely running the firmware simulator, steppable from the keyboard, with a browser test that fails if they stop moving — or if the one that is supposed to be still starts.</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/first-experience.e2e.ts` reports 3 passed and the full-suite log holds no `failed`.
- `npm run build` exits 0; `build/index.html` contains the coverflow markup and does not reference the grid-protocol chunk.
- `npm run check` reports 0 errors; `npm run lint` exits 0; `npm run test:quick` shows no failures.
- `git diff --quiet -- src/vendor` exits 0.
</verification>

<success_criteria>
- The front door renders a live coverflow with no hardware attached: one hero, neighbours receding in
  depth, every visible pad painting from its own `PadSim`.
- An animated pad provably changes between two samples and a still pad provably does not.
- Keyboard stepping wraps both ways and is announced through `aria-activedescendant`.
- The compiler's 131 KB chunk is not on `/`'s critical path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-first-experience/04-06-SUMMARY.md`.

Record in it: the props of all four components verbatim (plans 04-07, 04-08 and 04-09 all extend
them); the measured hero pixel size at the default Playwright viewport; the grid-protocol chunk
filename the guard found; whether the wheel and touch-drag thresholds needed adjusting from 40 px and
48 px; and anything about the 3D stacking that behaved differently from the UI spec's ladder.
</output>
