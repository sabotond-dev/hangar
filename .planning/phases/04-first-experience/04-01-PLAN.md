---
phase: 04-first-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - package.json
  - package-lock.json
  - scripts/gen-licenses.mjs
  - THIRD-PARTY.md
  - licenses/
  - src/lib/assets/favicon.svg
  - src/lib/ui/identity.spec.ts
autonomous: true
requirements: [IDENT-01, CAT-04, CONT-01]

must_haves:
  truths:
    - "The catalog module Phase 8 plan 08-01 created is present and green before any Phase 4 code is written; if it is absent the phase stops with a message naming 08-01 rather than inventing a second catalog."
    - "The whole design system is two colours over black: eight `@theme` tokens in src/app.css, every text token at or above 4.7:1 on #000000, and no third hue anywhere."
    - "Quicksand ships from the repository, not from a CDN, and `npm run licenses` accepts its OFL-1.1 licence as a deliberate, commented allowlist extension rather than failing the build."
    - "The site's mark is the 9x9 pad outline, so the browser tab stops showing Svelte's logo."
  artifacts:
    - path: "src/app.css"
      provides: "The IDENT-01 token block: ground, ink, ink-quiet, ink-dim, line, line-soft, accent, glow, plus the two font stacks and the global focus ring"
      contains: "--color-ink-quiet"
    - path: "scripts/gen-licenses.mjs"
      provides: "OFL-1.1 in ALLOWED with the reasoning, and a header paragraph naming the typeface"
      contains: "OFL-1.1"
    - path: "src/lib/assets/favicon.svg"
      provides: "The 9x9 outline mark, hand-drawn, 32x32"
      contains: "d6ff4e"
    - path: "src/lib/ui/identity.spec.ts"
      provides: "The token ladder, the accent reservation and the favicon asserted as structure rather than promised in a comment"
  key_links:
    - from: "src/app.css"
      to: "@fontsource/quicksand"
      via: "two @import lines for the latin 400 and 600 faces"
      pattern: "fontsource/quicksand"
    - from: "scripts/gen-licenses.mjs"
      to: "THIRD-PARTY.md"
      via: "npm run licenses regenerates the notices with the font listed"
      pattern: "quicksand"
    - from: "src/lib/ui/identity.spec.ts"
      to: "src/app.css"
      via: "every token named in the UI spec is asserted present with its exact value"
      pattern: "color-line-soft"
---

<objective>
Lay the identity foundation the rest of Phase 4 renders against, and prove the phase's one external
dependency — the catalog module — is already on disk before anything is built on top of it.

Purpose: IDENT-01's tokens and mark, D-24's font decision and the licence-gate extension it forces,
and the hard stop that keeps Phase 4 from creating a second catalog.
Output: the `@theme` token block, Quicksand pinned and licensed, the 9x9 favicon, and one new spec.

Wave 1, alone. Every other Phase 4 plan depends on this one, and the reason is mechanical rather
than semantic: this plan runs `npm install`, which rewrites `package-lock.json` and churns
`node_modules`, while `src/lib/licence-notices.spec.ts` reads `package-lock.json` on every single
`npm run test:quick`. A plan running in parallel with this one would see red tests for reasons that
have nothing to do with its own work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/FIRST-EXPERIENCE.md
@.planning/phases/04-first-experience/04-CONTEXT.md
@.planning/phases/04-first-experience/04-UI-SPEC.md
@.planning/phases/04-first-experience/04-VALIDATION.md

<standing_rules>
- **Never edit anything under `src/vendor/`.** `src/lib/fidelity/vendored-diff.spec.ts` reconstructs
  the upstream bytes from every vendored file and fails on any change. Nothing in Phase 4 needs one.
- **Never run `npm run format` across the repository without checking what it touches.** Scope
  Prettier to the files the task created (`npx prettier --write <paths>`), then `npm run lint`.
- **`npm install` rewrites `package-lock.json`'s indentation on this machine** (Phase 1 recorded it:
  tabs become two spaces). That is expected here and the reformatted lockfile is committed with the
  dependency — do **not** `git checkout` it.
- Files a task creates are `git add`ed **before** any negative check is run against them. On an
  untracked path `git checkout --` fails outright and `git diff --quiet` passes vacuously.
- `grep -c` counts matching **lines**, not matches. Only `0` means the same thing either way.
- No Claude or Anthropic attribution anywhere, in code, comments, commit messages or documents. No
  emojis. Real typographic apostrophes (U+2019) and a real ellipsis (U+2026) in visitor-facing copy.
- Sibling repositories are read-only. The executor never deploys.
- Every new first-party source file carries the house header comment ending
  `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- **Counting convention (shared tree).** Phase 8 plan 08-01 lands `scripts/check-counts.mjs`, which
  reads a Vitest or Playwright summary on stdin and compares it against an expected file/test count,
  **reporting the todo count without asserting it**. Whole-suite gates in this phase use it with
  **baseline plus delta**: take the observed totals recorded in the previous plan's SUMMARY (for this
  phase's first plan, capture them once before touching anything and write them down), add this task's
  own delta, and pipe `npm run test:quick 2>&1 | node scripts/check-counts.mjs $EXPECTED_FILES $EXPECTED_TESTS`.
  Never hard-code an absolute total in a criterion and never assert a todo count — the tree is shared
  with Phase 8. If `scripts/check-counts.mjs` is absent, fall back to the pair
  `npm run test:quick 2>&1 | grep -E "Tests +[0-9]+ passed"` matching **and**
  `npm run test:quick 2>&1 | grep -qE "Tests +[0-9]+ failed"` exiting non-zero, and say so in the SUMMARY.
  Every SUMMARY records the observed `test:quick` file and test totals so the next plan has a baseline.
- **Every structural scan strips comments first, with one uniform expression.** Six of these checks
  look for a token the same task deliberately *names* in a comment (the painter's header lists the
  forbidden calls; the host cites the vendored file; the spinner explains that it is the loading
  state). A scan over raw source would then fail on correct code. Use this form everywhere, and note
  that it is deliberately **backslash-free** — inside `node -e "…"` both bash and PowerShell eat a
  backslash, which silently turns a pattern into one that matches nothing:

  ```js
  const strip = (t) => t
    .replace(/^[ ]*[/][/].*$/gm, "")      // line comments
    .replace(/[/][*][^]*?[*][/]/g, "")    // block comments
    .replace(/<!--[^]*?-->/g, "");        // markup comments
  ```

  If inline quoting gets fragile, write the check into a scratch `.mjs` under the system temp
  directory and run it with `node` — never into the repository.
</standing_rules>

<interfaces>
**The exact token ladder (04-UI-SPEC.md §Color — approved, do not re-derive):**

| Token | Value | Contrast on #000 | Used for |
|---|---|---|---|
| `--color-ground` | `#000000` | — | the page ground, the pad face behind unlit cells, the gutter grid |
| `--color-ink` | `rgb(214 255 78 / 0.72)` | 9.3:1 | default text, the name in the name plate |
| `--color-ink-quiet` | `rgb(214 255 78 / 0.55)` | 5.6:1 | the headline, the fidelity line, region captions |
| `--color-ink-dim` | `rgb(214 255 78 / 0.50)` | 4.7:1 | labels of disabled controls |
| `--color-line` | `rgb(214 255 78 / 0.40)` | 3.3:1 | functional borders |
| `--color-line-soft` | `rgb(214 255 78 / 0.20)` | decorative | dot field, side-pad frames, hairlines |
| `--color-accent` | `#d6ff4e` | 18.3:1 | the reserved list only (wordmark, triangles, primary fill, focus ring, walking cell) |
| `--color-glow` | `rgb(214 255 78 / 0.18)` | — | one static box-shadow bloom |

`--color-ground` and `--color-accent` already exist in `src/app.css` as placeholders; this plan
replaces the comment above them and adds the other six.

**Font stacks (04-UI-SPEC.md §Design System):**

```css
--font-sans: "Quicksand", ui-rounded, "SF Pro Rounded", "Segoe UI Variable Display",
             system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
--font-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas,
             "Liberation Mono", monospace;
```

**The catalog module this phase consumes** (created by Phase 8 plan 08-01, which the orchestrator
guarantees ran first):

```ts
// src/lib/catalog/index.ts
export const CATALOG: readonly CatalogEntry[];
export function byId(id: string): CatalogEntry | undefined;
export function byFeatured(): readonly CatalogEntry[];
export function byNewest(): readonly CatalogEntry[];
export function byName(): readonly CatalogEntry[];
// re-exports from ./types:
export type CatalogEntry = {
  id: string; name: string; description: string;
  tags: readonly string[]; featured: boolean; addedAt: string;
  source: { kind: "preset"; presetId: string } | { kind: "state"; state: PadState }
        | { kind: "lua"; setup: string; timer: string };
  preview: "padsim" | "lua";      // the ENGINE selector, not a motion field
  knobs: readonly LuaKnob[];
  defaults: Readonly<Record<string, number>>;
  restsBlack: boolean;            // 08-01’s own recorded fact: writes no LEDs at rest
};
export function build(entry, lua, version): PadConfigObject;
```

Phase 4 **consumes** this and never creates a second catalog. Note that `preview` here is the engine
selector (`"padsim" | "lua"`), not an object — so 04-CONTEXT D-20's `preview.motion` lands in Phase 4
as a sibling field on a Phase 4-owned row module, built in plan 04-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4-01-01: Preflight the catalog module, then write the identity tokens</name>
  <files>src/app.css, src/lib/ui/identity.spec.ts</files>

  <read_first>
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §Design System, §Spacing Scale,
      §Typography, §Color, §Accessibility Contract — the approved contract; every number below is
      quoted from it
    - `src/app.css` — four lines today, two placeholder tokens
    - `src/lib/catalog/index.ts` and `src/lib/catalog/types.ts` — what Phase 8 plan 08-01 left
    - `src/lib/config-shape.spec.ts` — the house style for a structural spec: strip line comments
      before matching, and assert the walk was not empty before asserting the result is empty
  </read_first>

  <action>
**Step 1 — the preflight. Run this before writing anything.**

```bash
node -e "const fs=require('node:fs');const need=['src/lib/catalog/types.ts','src/lib/catalog/index.ts','src/lib/catalog/entries/ported.ts'];for(const f of need){if(!fs.existsSync(f)){console.error('STOP: '+f+' is missing. Phase 8 plan 08-01 creates the catalog module and must run before Phase 4. Do not create a second catalog.');process.exit(1)}}const i=fs.readFileSync('src/lib/catalog/index.ts','utf8');for(const n of ['CATALOG','byId','build']){if(!i.includes(n)){console.error('STOP: src/lib/catalog/index.ts does not export '+n+'. Re-run Phase 8 plan 08-01.');process.exit(1)}}console.log('catalog module present')"
npx vitest run --project server src/lib/catalog/catalog.spec.ts
```

If either command fails, **stop the plan** and report exactly that: Phase 8 plan 08-01 has not run,
Phase 4 cannot proceed, and nothing in Phase 4 may create `src/lib/catalog/entries.ts` or any other
second catalog. Do not work around it.

**Step 2 — `src/app.css`.** Keep `@import "tailwindcss";` as the first line. Replace the placeholder
comment with a real one that says what the block is (the IDENT-01 design system, sourced from
`04-UI-SPEC.md` §Color) and add the six missing colour tokens and the two font stacks to the
existing `@theme` block, exactly as tabled in `<interfaces>`. Tailwind v4 is CSS-first: there is no
config file and none is created.

Then, **below** the `@theme` block, add the small amount of global CSS the whole phase depends on.
Keep it to these and nothing else:

```css
html {
  background: var(--color-ground);
  color-scheme: dark;
}
body {
  background: var(--color-ground);
  color: var(--color-ink);
  font-family: var(--font-sans);
  margin: 0;
  -webkit-font-smoothing: antialiased;
}
:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 4px;
  border-radius: inherit;
}
```

The focus ring is never removed and never animated (04-UI-SPEC §Accessibility Contract). Do **not**
add a `prefers-reduced-motion` block here — motion belongs to the components that own it, and a
blanket `* { animation: none }` would also disable the pad canvases, which are not CSS animations
but must still be reasoned about per component.

The pre-existing footer in `src/routes/+layout.svelte` ships unchanged (FOUND-04). It will now
inherit lime-on-black; that is correct and is the one place where Micro at weight 400 in sentence
case is permitted.

**Step 3 — `src/lib/ui/identity.spec.ts`, exactly 5 tests.** Create the `src/lib/ui/` directory with
this file. It is a structural spec over `src/app.css`, in the `config-shape.spec.ts` style: read the
file, strip `/* ... */` comments before matching so a comment can never pass or fail a check.

1. **"the token ladder is exactly the eight tokens the spec approved"** — for each of the eight
   names and values in the table above, assert the stripped CSS contains `--name: value;`. Then
   assert the `@theme` block declares no `--color-` token outside that set of eight: extract every
   `--color-[a-z-]+` from the block, and compare the sorted unique list against the sorted list of
   eight names. This is the test that makes "there is no grey" mechanical.
2. **"every text token clears WCAG AA on black"** — compute the contrast ratio of each of
   `--color-ink`, `--color-ink-quiet`, `--color-ink-dim` against `#000000` from the alpha and the
   accent's sRGB, in the spec (composite over black is a straight multiply on each channel, then the
   standard relative-luminance formula), and assert each is `>= 4.5`. Assert `--color-line` is
   `>= 3.0`. Write the three computed numbers into the assertion messages so a future token change
   reports what it actually produced.
3. **"the two font stacks are declared and Quicksand leads the sans stack"** — `--font-sans` starts
   with `"Quicksand"`, `--font-mono` starts with `ui-monospace`, and the sans stack names at least
   four fallbacks.
4. **"the focus ring is present and is the accent at 2px"** — the stripped CSS contains a
   `:focus-visible` rule with `outline: 2px solid var(--color-accent)` and `outline-offset: 4px`.
5. **"the ground is true black and nothing declares a third hue"** — `--color-ground` is exactly
   `#000000`, and every colour literal in the whole file is one of `#000000`, `#d6ff4e`,
   `rgb(214 255 78 / <alpha>)` or a `var(--color-*)` reference. Implement it by extracting every
   `#[0-9a-fA-F]{3,8}` and every `rgb(` / `rgba(` / `hsl(` / `oklch(` occurrence from the stripped
   source and asserting the resulting set is a subset of the permitted forms. Assert the extraction
   found at least one literal first, so the test cannot pass on an empty walk.

`vite.config.ts` sets `expect: { requireAssertions: true }` — every branch of every test must assert.

**Negative check.** Change `--color-ink-dim`'s alpha from `0.50` to `0.20`, run the spec, confirm
test 2 goes red naming the computed ratio, then `git checkout -- src/app.css`. Record the red line
verbatim in the SUMMARY. `git add` both files before perturbing anything.

Run `npx prettier --write src/app.css src/lib/ui/identity.spec.ts`, then `npm run lint` and
`npm run check`.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/identity.spec.ts 2>&1 | grep -E "Tests +5 passed \(5\)" && npm run check 2>&1 | grep -Ei "0 errors" && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - The preflight command printed `catalog module present` and
      `npx vitest run --project server src/lib/catalog/catalog.spec.ts 2>&1 | grep -E "Tests +10 passed \(10\)"` matched. If it did not, the plan stopped here and the SUMMARY says so.
    - `npx vitest run --project server src/lib/ui/identity.spec.ts 2>&1 | grep -E "Tests +5 passed \(5\)"` matches.
    - All eight tokens are present: `node -e "const s=require('node:fs').readFileSync('src/app.css','utf8');for(const n of ['--color-ground','--color-ink','--color-ink-quiet','--color-ink-dim','--color-line','--color-line-soft','--color-accent','--color-glow','--font-sans','--font-mono']){if(!s.includes(n+':')){console.error('missing '+n);process.exit(1)}}"` exits 0.
    - No second catalog was created: `test ! -f src/lib/catalog/entries.ts` exits 0.
    - `src/vendor/` is untouched: `git diff --quiet -- src/vendor` exits 0 and `npx vitest run --project server src/lib/fidelity/vendored-diff.spec.ts 2>&1 | grep -E "Tests +[0-9]+ passed"` matches with no `failed` in the output.
    - `npm run test:quick` is green at the expected count (counting convention above: baseline from the previous SUMMARY plus this task's delta, through `scripts/check-counts.mjs`).
    - `npm run check 2>&1 | grep -Ei "0 errors"` matches; `npm run lint` exits 0.
    - The SUMMARY quotes the negative-check red line and the three contrast ratios test 2 computed.
  </acceptance_criteria>

  <done>The catalog module is proven present and green, and `src/app.css` carries the whole approved token ladder with a spec that fails if a ninth colour, a third hue or a below-AA text token ever appears.</done>
</task>

<task type="auto">
  <name>Task 4-01-02: Quicksand, self-hosted, through the licence gate</name>
  <files>package.json, package-lock.json, scripts/gen-licenses.mjs, THIRD-PARTY.md, licenses/, src/app.css</files>

  <read_first>
    - `scripts/gen-licenses.mjs` — `ALLOWED` at line 27 and the fixed `header` array at line 142
    - `src/lib/licence-notices.spec.ts` — test 4 asserts every non-dev entry in `package-lock.json`
      appears in `THIRD-PARTY.md`; `THIRD-PARTY.md` is fully machine-generated, so nothing may be
      hand-written into it
    - `.planning/phases/04-first-experience/04-RESEARCH.md` §Pitfall 8 — the three steps, in order
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §Design System (W-01, W-02)
  </read_first>

  <action>
D-24 and UI-SPEC W-01/W-02. Three steps, in this order — reversing them makes `npm run licenses`
exit 1 with a message that does not mention fonts.

**1. Extend the allowlist.** In `scripts/gen-licenses.mjs`, add `"OFL-1.1"` to `ALLOWED` with a
comment above it recording the reasoning, in the file's own voice:

> The SIL Open Font License is an FSF-approved free licence. The fonts are served as separate static
> `.woff2` assets and are never linked into the JavaScript bundle, so this is aggregation beside the
> GPLv3 work rather than combination with it. The allowlist's own contract is that it is extended
> deliberately, in a commit, with a reason — this is that.

**2. Install the typeface, exactly pinned.**

```bash
npm i --save-exact @fontsource/quicksand@5.3.0
```

`--save-exact` is not optional: D-24 says exact pin, and a caret on a font package would let a
future `npm install` change the metrics the whole layout was tuned against.

If `5.3.0` is not published, install the newest published `5.x` with `--save-exact`, record the
version actually installed in the SUMMARY, and continue. If the package cannot be reached at all,
**stop and report** — do not fall back to a CDN (`04-CONTEXT` §Specific Ideas: free, self-hosted,
no third-party requests) and do not substitute a different family without the user.

Confirm the two faces the CSS will import actually exist before importing them:

```bash
node -e "const fs=require('node:fs');for(const f of ['node_modules/@fontsource/quicksand/latin-400.css','node_modules/@fontsource/quicksand/latin-600.css']){if(!fs.existsSync(f)){console.error('missing '+f);process.exit(1)}}console.log('faces present')"
```

If the file names differ in the published package, use the ones that exist (list
`node_modules/@fontsource/quicksand/` and pick the latin 400 and 600 stylesheets), and record the
names used in the SUMMARY.

**3. Import them in `src/app.css`**, immediately after `@import "tailwindcss";` and before the
`@theme` block — every `@import` must precede other rules or the browser drops it:

```css
@import "@fontsource/quicksand/latin-400.css";
@import "@fontsource/quicksand/latin-600.css";
```

Two weights only. No variable face, no other family, no `@font-face` written by hand.

**4. Add a paragraph to the generated header.** In `gen-licenses.mjs`'s `header` array, after the
`@wasm-fmt/lua_fmt` paragraph, add two entries (the text line and an empty string) naming Quicksand,
its designer attribution as the OFL requires, and the fact that it is served as separate font files.
Keep the array's existing style: plain strings, no template literals, no trailing whitespace.

**5. Regenerate and commit the output.**

```bash
npm run licenses
```

It must exit 0 and its final line must now count one more production dependency. `THIRD-PARTY.md`
and `licenses/` are committed artefacts (Phase 1 D-decision: the build must be reproducible from a
clean clone without first running the checker), so both are staged with this change.

**Negative check.** Remove `"OFL-1.1"` from `ALLOWED`, run `npm run licenses`, confirm it exits 1
with a message containing `outside the GPL-compatible allowlist` and naming the font package, then
restore the line. Record the message verbatim in the SUMMARY — it is the proof that the extension
was load-bearing rather than decorative.

Run `npx prettier --write src/app.css scripts/gen-licenses.mjs THIRD-PARTY.md`, then `npm run lint`
and `npm run check`.
  </action>

  <verify>
    <automated>npm run licenses && npx vitest run --project server src/lib/licence-notices.spec.ts 2>&1 | grep -E "Tests +7 passed \(7\)" && npm run lint && npm run check 2>&1 | grep -Ei "0 errors"</automated>
  </verify>

  <acceptance_criteria>
    - The dependency is exact, with no range character: `node -e "const d=require('./package.json').dependencies['@fontsource/quicksand'];if(!d||!/^[0-9]+[.][0-9]+[.][0-9]+$/.test(d)){console.error('not exactly pinned: '+d);process.exit(1)}console.log(d)"` exits 0 and prints the version.
    - `grep -q '"OFL-1.1"' scripts/gen-licenses.mjs` exits 0.
    - The notices name the typeface: `grep -qi 'quicksand' THIRD-PARTY.md` exits 0, and the OFL text really materialised. `licenses/` is written with one **scope directory** per scoped package, so the text lands at `licenses/@fontsource/quicksand@<version>-*.txt` and a flat `readdirSync` cannot see it — walk it recursively: `node -e "const fs=require('node:fs');const hit=fs.readdirSync('licenses',{recursive:true}).some(f=>/quicksand/i.test(String(f)));if(!hit){console.error('STOP: THIRD-PARTY.md names the font but no licence text shipped for it. Do not accept this - the notices would reference a licence the archive does not carry.');process.exit(1)}"` exits 0. (`recursive` needs Node 18.17+; this repository pins `engines.node >= 24`.) **If no licence text materialises, stop and report** rather than accept the notices.
    - `src/app.css` imports the faces from the package, not from a network origin: `grep -c 'fontsource/quicksand' src/app.css` prints `2`, and `node -e "const s=require('node:fs').readFileSync('src/app.css','utf8');if(/https?:/.test(s)){console.error('src/app.css reaches a network origin');process.exit(1)}"` exits 0.
    - `npm run licenses` exits 0; the negative check was observed exiting 1 with the allowlist message and the SUMMARY quotes it.
    - `npx vitest run --project server src/lib/licence-notices.spec.ts 2>&1 | grep -E "Tests +7 passed \(7\)"` matches.
    - `npm run build` exits 0 and `test -f build/THIRD-PARTY.md` exits 0 — the font did not break the artefact pipeline.
    - `npm run test:quick` is green at the expected count (counting convention above: baseline from the previous SUMMARY plus this task's delta, through `scripts/check-counts.mjs`); `npm run lint` exits 0; `npm run check 2>&1 | grep -Ei "0 errors"` matches.
    - The reformatted `package-lock.json` is staged with the change rather than reverted, and the SUMMARY says so.
  </acceptance_criteria>

  <done>Quicksand ships from `node_modules` into the bundle's static assets, the licence gate accepts OFL-1.1 as a recorded policy decision, and the regenerated notices name the font without any hand-editing.</done>
</task>

<task type="auto">
  <name>Task 4-01-03: The 9x9 mark replaces Svelte's logo</name>
  <files>src/lib/assets/favicon.svg, src/lib/ui/identity.spec.ts</files>

  <read_first>
    - `.planning/phases/04-first-experience/04-UI-SPEC.md` §Favicon — the exact recipe
    - `src/lib/assets/favicon.svg` — Svelte's default logo, still shipping
    - `src/routes/+layout.svelte` — how it is referenced (`<link rel="icon">`); that line does not
      change
  </read_first>

  <action>
IDENT-01 names the 9x9 outline as the mark, and the tab still shows Svelte's logo.

Replace `src/lib/assets/favicon.svg` with a hand-written 32x32 SVG:

- `viewBox="0 0 32 32"`, `width="32" height="32"`.
- A `#000000` rounded-square ground filling the box (`rx="6"`).
- A rounded-square outline in `#d6ff4e`, 1.5px stroke, inset 2px, `rx="5"`, `fill="none"` — the pad
  frame, which is the same shape the hero pad wears.
- 81 dots on a 9x9 lattice inside the frame at `fill="#d6ff4e"` `fill-opacity="0.35"`, radius 0.9.
  This alpha is deliberately outside the token ladder: it is a generated mark, not an interface
  surface (04-UI-SPEC §Color, "Generated texture is outside the ladder, by design").
- Five of those cells drawn again at full `#d6ff4e` and radius 1.4, forming a diagonal from the
  top-left cell to the centre — the one lit path that makes the mark read as a pad rather than as
  graph paper.

Write the dots out literally rather than generating them at load: an SVG favicon has no script and
must be a static document. Keep the file under 4 KB so Vite inlines it as a data URI — the acceptance check below reads `build/index.html` and relies on that (write the circles compactly: one `<g fill="#d6ff4e" fill-opacity=".35">` group, bare `<circle cx cy r/>` children).

The `<title>` element must say `HANGAR`, not `svelte-logo`.

**Add test 6 to `src/lib/ui/identity.spec.ts`** — "the favicon is the 9x9 mark, not a framework
logo":

- the file contains no `svelte` substring in any case;
- its `<title>` is `HANGAR`;
- it declares a `viewBox` of `0 0 32 32`;
- it contains exactly 86 `<circle` elements (81 dots plus the 5 lit cells) — count them, and assert
  the count rather than asserting "some";
- every colour literal in it is `#000000` or `#d6ff4e` (the same subset rule as test 5, applied to a
  different file).

The spec's test count goes from 5 to 6; update the expected count in
`.planning/phases/04-first-experience/04-VALIDATION.md`'s per-task table if it disagrees.

**Negative check.** Restore the old favicon from git (`git show HEAD:src/lib/assets/favicon.svg`
into the path), run the spec, confirm test 6 goes red naming the framework string, then restore the
new one. Record the red line in the SUMMARY.

Run `npx prettier --write src/lib/ui/identity.spec.ts` (leave the SVG alone if Prettier reflows it
in a way you did not intend — check `npm run lint` either way), then `npm run lint` and
`npm run check`.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/ui/identity.spec.ts 2>&1 | grep -E "Tests +6 passed \(6\)" && npm run build && npm run lint && npm run check 2>&1 | grep -Ei "0 errors"</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/ui/identity.spec.ts 2>&1 | grep -E "Tests +6 passed \(6\)"` matches.
    - `node -e "const s=require('node:fs').readFileSync('src/lib/assets/favicon.svg','utf8');if(/svelte/i.test(s)){console.error('framework logo still present');process.exit(1)}const n=(s.match(/<circle/g)||[]).length;if(n!==86){console.error('circles: '+n);process.exit(1)}"` exits 0.
    - `npm run build` exits 0 and the built page carries the new mark. Vite **inlines** an SVG under 4096 bytes as a data URI rather than emitting an asset file (the current favicon is 1,569 bytes), so the assertion is against `build/index.html`, not against `build/_app/immutable/assets/`: `node -e "const s=require('node:fs').readFileSync('build/index.html','utf8');if(/svelte-logo/i.test(s)){console.error('the framework logo is still inlined into the built page');process.exit(1)}if(!/d6ff4e/i.test(s)&&!/%23d6ff4e/i.test(s)){console.error('the accent hex is absent from the built page - the icon did not land');process.exit(1)}"` exits 0. If the mark ever grows past 4 KB Vite emits it as a file instead; the SUMMARY records which of the two happened, and the source-level test 6 covers the mark either way.
    - `npm run test:quick` is green at the expected count (counting convention above: baseline from the previous SUMMARY plus this task's delta, through `scripts/check-counts.mjs`); `npm run lint` exits 0; `npm run check 2>&1 | grep -Ei "0 errors"` matches.
    - The negative check was observed red and the SUMMARY quotes the failing line.
  </acceptance_criteria>

  <done>The browser tab carries HANGAR's own 9x9 mark, and a spec fails if a framework logo ever returns.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project server src/lib/ui/identity.spec.ts` reports 6 passed in 1 file.
- `npm run licenses` exits 0 and `THIRD-PARTY.md` names Quicksand.
- `npm run build` exits 0 and `build/THIRD-PARTY.md` exists.
- `npm run check` reports 0 errors; `npm run lint` exits 0.
- `npm run test:quick` shows no failures.
- `git diff --quiet -- src/vendor` exits 0.
</verification>

<success_criteria>
- The phase cannot proceed on a missing catalog: the preflight either printed `catalog module
  present` or the plan stopped naming Phase 8 plan 08-01.
- `src/app.css` carries eight colour tokens and two font stacks, all asserted, with no third hue.
- Quicksand is exactly pinned, self-hosted, and its OFL-1.1 licence is an explicit, commented
  extension of the build's allowlist with regenerated notices committed beside it.
- The favicon is the 9x9 outline; no framework logo ships.
</success_criteria>

<output>
After completion, create `.planning/phases/04-first-experience/04-01-SUMMARY.md`.

Record in it: the exact `@fontsource/quicksand` version installed and the two face filenames
imported; the three contrast ratios test 2 computed; the verbatim allowlist-refusal message from the
negative check; whether `package-lock.json` was reformatted by the install; and the confirmation that
`src/lib/catalog/` was found rather than created.
</output>
