---
phase: 06-device-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/transport/transport.ts
  - src/lib/transport/transport.spec.ts
  - src/lib/device/try-on.ts
  - src/lib/device/try-on.spec.ts
autonomous: true
requirements: [CONN-04, CONN-07]

must_haves:
  truths:
    - "Every count this phase asserts is an observed baseline plus a stated delta, measured on this tree at this moment rather than copied from a document."
    - "A racing open() lands in a named state with a sentence a visitor can read, never as `The port is already open.`"
    - "A rig on the cable is named by the module that is actually on the USB cable, not by whichever module spoke first."
    - "A runes module under src/lib is proven to be compiled and collected by the quick project before anything is built on top of it."
  artifacts:
    - path: "src/lib/transport/transport.ts"
      provides: "OpenFailure gains already-open; classifyOpenError gains the InvalidStateError branch; failureCopy gains its case"
      contains: "already-open"
    - path: "src/lib/device/try-on.ts"
      provides: "not-zona names the heartbeatType 1 module, falling back to arrival order"
      contains: "heartbeatType"
  key_links:
    - from: "src/lib/transport/transport.ts"
      to: "DOMException.name"
      via: "classifyOpenError branches on the name, never on the message text"
      pattern: "InvalidStateError"
    - from: "src/lib/device/try-on.ts"
      to: "src/lib/transport/sequence.ts"
      via: "the identify state's seen map, preferring heartbeatType === 1"
      pattern: "heartbeatType === 1"
---

<objective>
Measure the ground, then close the two holes in the shipped device vocabulary that a site-wide
session opens.

Wave 1. Nothing visible ships here. Three things land:

1. **The four baselines**, re-measured on a clean tree. Phase 5.1 was landing plans while this phase
   was planned, so no number written in `06-VALIDATION.md` may be trusted: `BASE_FILES`,
   `BASE_TESTS`, `BASE_SWEEP` and `PREV_E2E` are observed here and carried forward by every later
   SUMMARY.
2. **`already-open`.** `classifyOpenError` today drops both `InvalidStateError` forms — *"The port is
   already open."* and *"A call to open() is already in progress."* — into `unknown`, which renders
   the raw browser text on screen. A per-panel connection could not reach them. A session with a
   header control **and** a panel control bound to one action reaches them on a double click, so the
   in-flight guard (plan 06-03) makes them unreachable and this row is the net under it.
3. **The rig-aware `not-zona`.** `identifyOnly` returns `seen[0].moduleType` — the first module *by
   arrival order*, which on a chained rig may not be the module on the cable. `grid_decode.c:695-700`
   makes `heartbeatType === 1` the USB-attached module by definition, and `identify()` already uses
   exactly that rule to find the ZONA. The refusal sentence names a module; it should name the right
   one.

Purpose: CONN-04's "a named state instead of a raw exception" and CONN-07's "refused with a plain
message", both a layer below any component.
Output: two shipped modules amended, two specs grown, four numbers recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-device-session/06-CONTEXT.md
@.planning/phases/06-device-session/06-RESEARCH.md
@.planning/phases/06-device-session/06-UI-SPEC.md
@.planning/phases/06-device-session/06-VALIDATION.md
@src/lib/transport/transport.ts
@src/lib/transport/transport.spec.ts
@src/lib/device/try-on.ts
@src/lib/device/try-on.spec.ts
@src/lib/transport/fixtures/synthetic.ts
@scripts/check-counts.mjs

<standing_rules>
- **Never edit anything under `src/vendor/`.** Never touch a sibling repository. Never deploy.
- No Claude or Anthropic attribution. No emojis. No exclamation marks. No paid services. **No new
  dependency** — this phase installs nothing.
- Every new file carries `Copyright (C) 2026 Botond Sandor. Licensed under the GNU GPL v3 or later.`
- Prettier defaults; `npm run format` then `npm run lint`; `npm run check 2>&1 | grep -Ei "0 errors"`.
- **No user-agent read anywhere, in any file, in this phase.** Capability is
  `capabilityOf({ hasSerial, secure })` and nothing else.
- **NEVER WRITE TO A DEVICE.** Nothing in this phase sends a byte to hardware. No plan may introduce
  `RequestQueue`, a host heartbeat, a config write or a page store into the session path.
- **No `.svelte.spec.ts`** — `vite.config.ts`'s `server` project excludes that glob, so a spec named
  by that convention is collected by nothing and the suite is green and vacuous.
- **No tenth token, no fourth hex, no `--color-over`.** `src/app.css` is not edited in this phase.
- **Strip comments before every structural scan.** The house helper is `stripComments` in
  `src/lib/config-shape.spec.ts` (line, block and markup, deliberately backslash-free).
- **No backslashes inside `node -e "…"`.** Write a scratch `.mjs` under the session scratch directory
  instead. **Large TypeScript files go through the editor tool, never a Bash heredoc** — 05.1-01
  recorded `unexpected EOF while looking for matching '` on exactly that.
- Whole-suite totals are the previous SUMMARY's numbers plus this plan's stated delta, through
  `scripts/check-counts.mjs`. **Per-file counts are exact and are asserted absolutely.**
- **This plan's SUMMARY records the four-name block** — `BASE_FILES`, `BASE_TESTS`, `BASE_SWEEP`,
  `PREV_E2E` — and every later plan in this phase carries it forward whether or not it moved it.
</standing_rules>

<interfaces>
**`classifyOpenError`, after this plan.** The union widens and the switch has a `default`, so all
three existing call sites keep working with no edit:

```ts
export type OpenFailure =
  | "no-web-serial"
  | "insecure-context"
  | "cancelled"
  | "port-busy"
  | "unplugged"
  | "already-open"   // NEW
  | "unknown";
```

The new branch, inside the existing `err instanceof DOMException` block:

```ts
// Both InvalidStateError forms are HANGAR bugs wearing a DOMException:
// "The port is already open."            (serial_port.cc:121-122)
// "A call to open() is already in progress." (serial_port.cc:114-116)
// A site-wide session with two connect controls is what makes them reachable
// at all; plan 06-03's in-flight guard is the fix and this is the net.
if (err.name === "InvalidStateError") return "already-open";
```

**`failureCopy("already-open", …)`** renders the `unknown` row's title with a sentence in place of a
raw report and **no steps at all** (06-UI-SPEC, the failure table's `unknown` row):

```ts
case "already-open":
  return {
    title: "The port would not open",
    detail: "HANGAR is already connecting — one moment.",
    steps: [],
  };
```

The em dash is U+2014 and the sentence is one literal, never a concatenation. `raw` is deliberately
**not** interpolated: the browser's own words for this failure describe HANGAR's bug and mean nothing
to a visitor.

**`identifyOnly`'s `not-zona` outcome**, after this plan:

```ts
const seen = [...state.seen.values()];
if (seen.length === 0) return { kind: "silent" };
// The module on the USB cable reports heartbeat type 1 (grid_decode.c:695-700),
// which is the same rule identify() uses to find the ZONA. Arrival order is
// non-deterministic on a rig, so seen[0] names an arbitrary module.
const onCable = seen.find((m) => m.heartbeatType === 1) ?? seen[0];
return { kind: "not-zona", moduleType: onCable.moduleType };
```

**The synthetic rig, for the spec.** `heartbeatFrame` builds real encoder-derived frames for any
hwcfg. hwcfg values read from `grid.module_hwcfgs()` against the pinned package: ZONA RevH **161**,
EN16 RevH **195**, BU16 RevH **131**. Confirm all three against the package before using them; if a
number differs, use the package's and record the correction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 06-01-01: the four baselines, and the runes-in-node spike</name>
  <files>.planning/phases/06-device-session/06-01-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/05.1-catalog-browse/05.1-11-SUMMARY.md` (or the last 05.1 SUMMARY on disk) —
      its four-name block, and whether `BASE_SWEEP` is still `3 13`
    - `06-VALIDATION.md` § Test Infrastructure and § The four-name carry-forward block
    - `vite.config.ts`'s two Vitest projects, including the `src/**/*.svelte.{test,spec}.{js,ts}`
      exclude
  </read_first>

  <action>
On a clean tree, before any edit, measure and record all four:

```
npm run check      2>&1 | tail -5          # provenance only: files + "0 errors"
npm run test:quick 2>&1 | tail -6          # BASE_FILES / BASE_TESTS
npm run test:sweep 2>&1 | tail -6          # BASE_SWEEP - expected "3 13"
npm run build && npm run test:e2e 2>&1 | tail -6   # PREV_E2E
```

**If `test:sweep` does not report `3 13`, stop and record the number Phase 5.1 left**: the sweep
literal is this repository's one documented exception to D-17 and every later plan in this phase
quotes it. Kill the whole wrangler process tree afterwards (npx-cli -> wrangler.js -> cli.js ->
workerd.exe); a survivor holds 4173 and answers nothing.

Then the spike, and it is a real gate rather than a formality. **Plan 06-03 puts a Svelte 5 runes
class in `src/lib/device/session.svelte.ts` and tests it in node**, and the repository contains zero
`.svelte.ts` files today, so nothing proves the `server` project can compile one. Prove it now, in
five minutes, with a throwaway pair under the session scratch directory copied into the tree:

- `src/lib/device/spike.svelte.ts` — a two-line class with one `$state` scalar and one `$state.raw`
  field, exported as a singleton.
- `src/lib/device/spike.spec.ts` — one test that assigns and reads both.

Run `npx vitest run --project server src/lib/device/spike.spec.ts` and record verbatim what happened:
whether it compiled, whether the rune survived as a plain assignment (the SSR transform) or as a
signal, and whether the file was collected. **Then delete both files** and confirm `test:quick` is
back at `BASE_FILES` / `BASE_TESTS`.

**If the spike fails to compile or the spec is not collected**, do not improvise: record it as a
blocking finding in the SUMMARY, name it for plan 06-03, and state the documented fallback — the
state machine moves to a plain `src/lib/device/session-machine.ts` (pure, node-testable) with
`session.svelte.ts` reduced to a runes shell that forwards to it, which satisfies D-05's "one
instance per page load" and loses nothing but tidiness.
  </action>

  <verify>
    <automated>npm run test:quick 2>&1 | tail -6</automated>
  </verify>

  <acceptance_criteria>
    - All four numbers are recorded in the SUMMARY as the phase's baseline, each with the command that
      produced it and the date.
    - `BASE_SWEEP` is quoted as the literal the sweep actually printed.
    - The spike ran, its outcome is recorded verbatim, and **both spike files are deleted** —
      `test:quick` after deletion equals `BASE_FILES` / `BASE_TESTS` exactly.
    - No wrangler or workerd process is listening on 4173 at the end of the task.
  </acceptance_criteria>

  <done>Every later plan in this phase has a number to assert against, and the one architectural assumption under plan 06-03 has been tested rather than believed.</done>
</task>

<task type="auto">
  <name>Task 06-01-02: already-open — the row a two-control session makes reachable</name>
  <files>src/lib/transport/transport.ts, src/lib/transport/transport.spec.ts</files>

  <read_first>
    - `src/lib/transport/transport.ts` in full — the union, `classifyOpenError`'s comment about
      branching on `name` and never on the message, and `failureCopy`'s trailing-label contract
    - `06-RESEARCH.md` § The complete taxonomy for the session — the two `InvalidStateError` rows with
      their Chromium source lines
    - `06-UI-SPEC.md` § The failure states — the `unknown` row's `InvalidStateError` sentence
  </read_first>

  <action>
Widen `OpenFailure`, add the `InvalidStateError` branch and the `failureCopy` case exactly as the
interfaces block gives them. Write the reason at both sites in prose: the message text is Chromium's
and localisable, so the branch reads `err.name`; and the copy carries no steps because there is
nothing for the visitor to do about a bug in this site.

Then two tests in `transport.spec.ts`, taking it from **7 to 9**:

- *"a racing open is its own state and never the raw browser text"* — `classifyOpenError(new
  DOMException("The port is already open.", "InvalidStateError"))` is `"already-open"`, and so is the
  in-progress form. Assert the two Chromium messages are **not** matched on: pass a `NetworkError`
  carrying the string `The port is already open.` and assert it still classifies as `port-busy`.
- *"the already-open copy is a sentence with no recovery and no raw report"* — `failureCopy
  ("already-open", "The port is already open.", "CONNECT ZONA").steps` is `[]`, the detail is the
  exact sentence, and the detail does **not** contain the raw text it was handed.

Negative check: delete the `if (err.name === "InvalidStateError")` line and observe the first test go
red naming `unknown`; restore. Then delete `steps: []`'s emptiness (give it one step) and observe the
second go red; restore.

`npm run check` and `npm run lint` must both stay clean — the union widens and every existing switch
has a `default`, so no call site changes. Confirm that by grepping the three `failureCopy` call sites
and stating in the SUMMARY that none was edited.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/transport/transport.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/transport/transport.spec.ts` reports **9 passed**.
    - `npm run check 2>&1 | grep -Ei "0 errors"` matches; `npm run lint` exits 0.
    - Both negative checks were observed red and restored; the SUMMARY names what each said.
    - No `failureCopy` or `classifyOpenError` call site was edited; the SUMMARY says so and names them.
    - The detail sentence is one literal containing a U+2014 em dash, quoted in the SUMMARY.
  </acceptance_criteria>

  <done>The two failures a site-wide session invents cannot reach a visitor as browser internals.</done>
</task>

<task type="auto">
  <name>Task 06-01-03: the refusal names the module that is actually on the cable</name>
  <files>src/lib/device/try-on.ts, src/lib/device/try-on.spec.ts</files>

  <read_first>
    - `src/lib/device/try-on.ts`'s `identifyOnly` tail — the `seen[0]` it replaces
    - `src/lib/transport/sequence.ts`'s `identify()` — the `heartbeatType === 1 && hwcfg === ZONA_HWCFG`
      rule this borrows half of
    - `src/lib/transport/fixtures/synthetic.ts`'s `heartbeatFrame` signature
    - `06-RESEARCH.md` § Multi-module, and why the picker filter is not a filter
  </read_first>

  <action>
Replace the `seen[0]` read with the `heartbeatType === 1` preference from the interfaces block, with
the firmware citation in a comment beside it and the fallback stated: a module that reports no type 1
at all still names *something* rather than nothing.

Then one test in `try-on.spec.ts`, taking it from **6 to 7**: *"a rig refuses by naming the module on
the cable, not the first one that spoke"*. Build three synthetic heartbeats through `heartbeatFrame`
— an EN16 RevH at `sx 1, sy 0, type 0`, a BU16 RevH at `sx 2, sy 0, type 0`, and the module on the
cable at `sx 0, sy 0, type 1` with a **non-ZONA hwcfg** — and feed them to `identifyOnly` through a
`FakeTransport` in an order that puts a type-0 module **first**. Assert the outcome is `not-zona` and
`moduleType` is the type-1 module's name.

The test must be able to go red: with the old `seen[0]` restored it names the EN16. Observe that,
then restore. Verify the three hwcfg numbers against `grid.module_hwcfgs()` before hard-coding them,
and record any correction.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/device/try-on.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run --project server src/lib/device/try-on.spec.ts` reports **7 passed**.
    - The negative check (restore `seen[0]`) was observed red naming the wrong module, and restored.
    - The three hwcfg values were read from the pinned package and are quoted in the SUMMARY.
    - `npm run test:quick 2>&1 | node scripts/check-counts.mjs $BASE_FILES $((BASE_TESTS + 3))` exits 0
      — no new file, three new tests.
    - `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 3 13` exits 0 (or the literal task 01
      recorded).
    - `npm run lint` exits 0; `npm run check 2>&1 | grep -Ei "0 errors"` matches.
    - The SUMMARY carries the four-name block, with `PREV_E2E` unchanged and the plan that measured it
      named.
  </acceptance_criteria>

  <done>A visitor with a rig on the cable is told what they actually plugged in.</done>
</task>

</tasks>

<verification>
```
npm run check 2>&1 | grep -Ei "0 errors"
npm run lint
npx vitest run --project server src/lib/transport/transport.spec.ts src/lib/device/try-on.spec.ts
npm run test:quick 2>&1 | node scripts/check-counts.mjs $BASE_FILES $((BASE_TESTS + 3))
npm run test:sweep 2>&1 | node scripts/check-counts.mjs 3 13
```
</verification>

<success_criteria>
- Four baselines measured on a clean tree and recorded, with `BASE_SWEEP` quoted as printed.
- The runes-in-node spike ran, was recorded and was deleted; plan 06-03's assumption is now a
  measurement.
- `already-open` exists end to end: classification, copy, two tests, two observed negative checks.
- `not-zona` names the module reporting heartbeat type 1, proven against a synthetic rig.
- `transport.spec.ts` 9, `try-on.spec.ts` 7, quick at baseline +0 files / +3 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-device-session/06-01-SUMMARY.md`, recording the four
baselines with their commands, the spike's verbatim outcome, the three hwcfg numbers, and both
negative checks with what they said when red.
</output>
