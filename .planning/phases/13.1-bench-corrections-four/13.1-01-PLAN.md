---
phase: 13.1-bench-corrections-four
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ui/intro/Intro.svelte
  - src/lib/ui/intro/HeroSurface.svelte
  - src/lib/ui/intro/StartCard.svelte
  - src/lib/ui/shell/layout.ts
  - src/routes/+layout.svelte
  - src/lib/ui/intro.spec.ts
  - src/lib/ui/shell.spec.ts
  - e2e/first-experience.e2e.ts
  - src/lib/ui/CatalogCard.svelte
  - src/lib/ui/browse-ui.spec.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [IDENT-01, CAT-01]

must_haves:
  truths:
    - "At 1920 x 1080, 1440 x 900, 1366 x 768 and 1280 x 720 the intro's document does not scroll: scrollHeight equals clientHeight, and the three-step strip is inside the viewport (D-01, bench line 1)."
    - "On the phone project the intro may scroll and still renders every element 13-07 pinned (intro.spec.ts 1 unchanged in substance)."
    - "Every gallery card's Explore is a filled grey box in the display face with the word and the arrow on one row at every card width the grid produces (D-02, bench line 2)."
    - "No new token, no new radius, no string moved: the eleven tokens hold, the allowlist stays empty, the Explore word and the arrow are the PDF's."
  artifacts:
    - path: "src/lib/ui/intro/Intro.svelte"
      provides: "A height-aware intro: the hero's square bounded by the height left after the strip; clamped spacing; the strip at the foot"
    - path: "src/routes/+layout.svelte"
      provides: "`.centre.intro` sized to the viewport less the header and the footer in the wide and compact bands, `overflow: hidden`; auto below 1024"
    - path: "src/lib/ui/CatalogCard.svelte"
      provides: "`.explore` as the PDF draws it: `--color-raised` fill, `--font-display`, inline-flex, nowrap"
    - path: "e2e/first-experience.e2e.ts"
      provides: "One new title: the intro fits at four desktop viewports"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/ui/shell/layout.ts"
      via: "the intro centre's block-size reads HEADER_H and FOOTER_H through the custom properties the layout already sets"
      pattern: "centre\\.intro"
    - from: "e2e/first-experience.e2e.ts"
      to: "document.scrollingElement"
      via: "scrollHeight <= clientHeight at each of the four viewports"
      pattern: "setViewportSize"
---

<objective>
Two of the bench's nine, the two that are look alone: the intro fits the screen without a page
scroll (bench line 1, D-01), and the gallery's Explore buttons match PDF page 2 — the grey fill, the
display face, the arrow on the same row (bench line 2, D-02).

Purpose: the front door and the gallery are what a visitor sees first; both differ from the PDF in
ways the user named on sight.
Output: `Intro.svelte`, `+layout.svelte`, `CatalogCard.svelte` changed; one intro fit scan (+1 unit
test), one four-viewport e2e title (+1 / +1); `13.1-COPY-NEW.md` created empty of rows (this plan
writes no string) with the register header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-07-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-08-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-20-SUMMARY.md
@src/lib/ui/intro/Intro.svelte
@src/lib/ui/intro/HeroSurface.svelte
@src/lib/ui/intro/StartCard.svelte
@src/lib/ui/shell/layout.ts
@src/routes/+layout.svelte
@src/lib/ui/CatalogCard.svelte
@src/lib/ui/intro.spec.ts
@src/lib/ui/browse-ui.spec.ts
@e2e/first-experience.e2e.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged before and after. `.planning/ROADMAP.md` is not
  edited. `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a Playwright chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` (none of them is this plan's).
- **No `border-radius` above zero anywhere; the allowlist stays EMPTY; the six circles are
  `ColourPicker.svelte:840, :867, :882` and `Knob.svelte:730, :785, :807` and neither file is edited.**
- **D-05's register for every string; every new string ledgered in `13.1-COPY-NEW.md`.** This plan
  writes none — the Explore word and the arrow are the PDF's.
- **Every count a carried name plus a delta** from the observed block `94 / 961 (+1 todo) / 83 / 99 /
  check 657 / sweep 4 19 / allowlist 0`. This plan's term: **+0 / +1; e2e +1 / +1**.
- **The sweep is not run** (no entry file, knob list or vendored file moves). `src/vendor/` and
  `library.ts` untouched.
- **Quick suite at `--maxWorkers=2`.** Playwright in chunks on a fresh detached server stopped through
  PowerShell (13-20's `e2e-chunks-1320.sh` shape, copied into this session's scratchpad); this plan runs
  **c2** (`browse.e2e.ts`, `browse-webkit.e2e.ts`) and **c4** (`catalog`, `fidelity`, `first-experience`,
  `library`, `sandbox`); every red rerun alone and named.
</standing_rules>

<interfaces>
From `src/lib/ui/shell/layout.ts` (13-05; every number once, with provenance):
```ts
export const HEADER_H = 76;
export const CONTEXT_H = 59;
export const FOOTER_H = 50;
export const SURFACE_MAX = 600;
```
From `src/routes/+layout.svelte` (the intro's centre today):
```css
.centre.intro { padding: 0; overflow: visible; }
.frame { block-size: calc(100dvh - var(--header-h) - var(--context-h) - var(--footer-h)); }
```
From `src/lib/ui/intro/Intro.svelte` (13-07; the PDF at a 1500px render: page inset 72, columns
72-710 and 806-1425, strip y 845-930):
```css
.intro { padding: 57px 72px 48px; gap: 44px; }
.columns { grid-template-columns: minmax(0, 638fr) minmax(0, 619fr); column-gap: 96px; }
.cards { margin-block: 44px 40px; }
.steps { padding: 32px 4px 0; border-block-start: 1px solid var(--color-divider); }
@media (max-width: 1023.98px) { .columns { grid-template-columns: minmax(0, 1fr); } }
```
From `src/lib/ui/CatalogCard.svelte` (13-08):
```css
.explore { display: grid; place-items: center; min-block-size: 44px; border: 1px solid var(--color-boundary);
  font-family: var(--font-sans); font-size: 16px; font-weight: 500; color: var(--color-ink); }
.card:hover .explore { border-color: var(--color-action); }
.arrow { color: var(--color-action); }
```
The markup: `<span class="explore" aria-hidden="true">{EXPLORE} <span class="arrow">{ARROW}</span></span>`
with `EXPLORE = "Explore"`, `ARROW = "↗"`. `browse-ui.spec.ts` test "a card shows one category…"
(`:649`) holds the box rendered and not aria-hidden.
The eleven tokens (`src/app.css` `@theme`, `identity.spec.ts` 1-4): `--color-workspace #101210`,
`--color-panel #191c18`, `--color-raised #22261f`, `--color-divider #383e32`, `--color-boundary #758168`,
`--color-ink-quiet #acb3a2`, `--color-action #dcff71`, and four more; **no twelfth**.
The chromium project's viewport is 1280 x 720 (`playwright.config.ts`, not edited).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-01-01: The intro fits the screen at every desktop viewport</name>
  <files>src/lib/ui/intro/Intro.svelte, src/lib/ui/intro/HeroSurface.svelte, src/lib/ui/intro/StartCard.svelte, src/lib/ui/shell/layout.ts, src/routes/+layout.svelte, src/lib/ui/intro.spec.ts, src/lib/ui/shell.spec.ts, e2e/first-experience.e2e.ts</files>
  <behavior>
    - e2e (chromium, one title, +1): for each of 1920 x 1080, 1440 x 900, 1366 x 768, 1280 x 720 —
      `page.setViewportSize`, `goto("/")`, wait for `intro-steps` visible — assert
      `document.scrollingElement.scrollHeight <= clientHeight` (+0 tolerance), the header, the strip
      and the hero all inside the viewport by `boundingBox`, and `intro-steps` not clipped
      (`bottom <= innerHeight`). Add the title to `first-experience.e2e.ts` beside the intro titles.
    - unit (+1, `intro.spec.ts` 5): `Intro.svelte` declares the height rule — a `dvh` term in the
      hero's size and the headline's clamp — and `+layout.svelte`'s `.centre.intro` rule declares
      `overflow: hidden` and a `block-size` built from `--header-h` and `--footer-h` inside the
      `min-width: 1024` band and `auto` below it; `shell.spec.ts` 5's frame table unchanged.
    - unit (unchanged): `intro.spec.ts` 1-4 still green — every element 13-07 pinned is present.
  </behavior>
  <action>
    Read the bench line and D-01 first. Then, before any edit, re-measure the block: `npm run test:quick
    2>&1 | node scripts/check-counts.mjs 94 961`, `grep -c "test(" e2e/*.e2e.ts` summed (83),
    `npx playwright test --list | tail -1` (99), and record the three on the SUMMARY's count line.

    1. **The layout's intro centre.** In `src/routes/+layout.svelte`, `.centre.intro` gains
       `block-size: calc(100dvh - var(--header-h) - var(--footer-h))`, `min-block-size: 0`,
       `overflow: hidden` — the same arithmetic `.frame` already uses, less the context bar the intro
       has no bar for. Inside `@media (max-width: 1023.98px)` set `.centre.intro { block-size: auto;
       overflow: visible; }` — the stacked band and the phone may scroll (D-01). Write the reason in the
       comment above the rule: the desktop intro is one screen by the user's word; the phone stacks
       (13-07) and D.10 is still open.
    2. **The intro's height awareness** in `Intro.svelte`: the root becomes a grid of two rows,
       `minmax(0, 1fr) auto` (the columns, then the strip), `block-size: 100%`, `min-block-size: 0`;
       the columns row gets `min-block-size: 0` and `align-items: stretch`; the hero panel's square
       becomes `inline-size: min(100%, var(--hero-side))` where `--hero-side` is the height available
       to the columns row (a `calc` on `100dvh` less the header, the footer, the intro's block
       padding and the strip's height) — put the strip's height and the intro's paddings in
       `layout.ts` as `INTRO_PAD_TOP`, `INTRO_PAD_BOTTOM`, `INTRO_STRIP_H` (measured from the PDF's
       y 845-930 and 13-07's numbers; provenance in the comment) and read them through custom
       properties as every other number is. The words column's fixed spacing becomes clamps against
       `dvh`: the headline `font-size: clamp(34px, 6.2dvh, 60px)` set on `.headline` locally
       (`app.css`'s `.type-display` untouched — say so in a comment, D-17), `.cards`'s block margins
       and `.eyebrow` / `.sub` margins as `clamp(min, Ndvh, max)` with the PDF's value as the max,
       and `StartCard.svelte`'s vertical padding on a custom property the intro sets. The hero surface
       (`HeroSurface.svelte`) keeps its aspect ratio and takes the bounded side.
    3. **Tune until the four viewports hold.** Build, serve on a detached server, and check
       `scrollHeight` against `clientHeight` at each of the four sizes by a scratch Playwright script
       before the title is written; the harness's own 1280 x 720 is the tight one (594px under the
       header and the footer). Shrink by the clamps, never by hiding an element 13-07 pinned; record
       in the SUMMARY which values each viewport landed on and what was shrunk to reach 720.
    4. **The title and the scan** as the behaviours above. The e2e title restores the project's
       viewport at the end (`setViewportSize` back to 1280 x 720) so later titles in the file are
       unaffected.
    5. Verify no `border-radius` was introduced (`grep -n border-radius` on the three files → none),
       no new hex, no new font family.
  </action>
  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs 94 962 && npm run build && npx playwright test e2e/first-experience.e2e.ts --workers 3</automated>
  </verify>
  <done>The four-viewport title is green in chromium; `intro.spec.ts` is five tests; `check-counts.mjs 94 962` matches; the stacked band still stacks and the phone project's intro titles are unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 13.1-01-02: The Explore button as PDF page 2 draws it</name>
  <files>src/lib/ui/CatalogCard.svelte, src/lib/ui/browse-ui.spec.ts, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <action>
    1. **Measure the PDF's fill.** Render page 2 of `.planning/phases/13-gui-overhaul/bible/HANGAR for
       ZONA.pdf` (the Read tool shows it; or `pdftoppm` if present) and sample the Explore box's fill
       against the eleven tokens; the nearest is `--color-raised` (`#22261f`) by D-02's reading, with
       `--color-panel` (`#191c18`) the alternative. Record the sample and the choice in the SUMMARY.
       **Never a twelfth token** (`identity.spec.ts` 1-4).
    2. **`.explore`** in `CatalogCard.svelte`: `display: inline-flex; align-items: center;
       justify-content: center; gap: 10px; white-space: nowrap;` (the arrow never wraps — the bench's
       "second row"); `background: var(--color-raised)` (or the measured token); `border: 1px solid
       transparent` at rest so the hover's `border-color: var(--color-action)` keeps its box without
       a layout shift — the PDF draws no boundary on the filled box; `font-family:
       var(--font-display); font-weight: 700; font-size: 17px; letter-spacing: 0.02em` (the PDF's word
       is the display face at roughly the group-title size; D-17's 17 is the nearest measured rung);
       `min-block-size: 44px` stays. `.arrow` stays the action colour. The reduced-motion rule stays.
       Update the component header's paragraph about the Explore box (it says "outlined").
    3. **`browse-ui.spec.ts`** test "a card shows one category and one tag…" (`:649`): extend its
       Explore clause to read the `.explore` rule through `rulesOf` and assert `font-family:
       var(--font-display)`, `white-space: nowrap`, `background: var(--color-raised)` (the chosen token)
       and `display: inline-flex`; and that `border-radius` does not occur in the file. +0 tests.
       The gallery-tokens test (`:317`) must still pass with the token used.
    4. Create `13.1-COPY-NEW.md` with 13-COPY-NEW's register paragraph (copied, credited) and an empty
       rows table with the columns `Symbol · Module · The state it names · The fact it must carry ·
       Proposed · Why there is no Bible line`, and a line saying this plan added no row (the word and
       the arrow are the PDF's). Later plans append.
  </action>
  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs 94 962 && npm run check && npm run lint</automated>
  </verify>
  <done>Every card's Explore renders as one row in the display face on the raised fill; `browse-ui.spec.ts` pins the four declarations; the token census is unchanged at eleven; `13.1-COPY-NEW.md` exists with the register and no row.</done>
</task>

<task type="auto">
  <name>Task 13.1-01-03: Chunks c2 and c4, the count line, STATE, the commit</name>
  <files>.planning/STATE.md</files>
  <action>
    1. `npm run build`; run chunk **c2** (`browse.e2e.ts`, `browse-webkit.e2e.ts`) and chunk **c4**
       (`catalog`, `fidelity`, `first-experience`, `library`, `sandbox`) on fresh detached servers,
       `--workers 3`; every red rerun alone at `--workers 1` and named in the SUMMARY with its line
       (13-20's transients: `browse:1404`, `browse-webkit:357`). `test-results/` removed after.
    2. Prettier on the files this plan wrote, by path. `npm run check` (report the count as
       provenance), `npm run lint`.
    3. STATE.md by script against a copy: one line under Phase 13.1 naming this plan's term
       (`+0 / +1; +1 / +1`), the observed block, and the four viewports' fit; the three asserted fields
       unchanged before and after.
    4. Two commits with `git commit --only <paths> -F <msgfile>`: task 01's files
       (`feat(13.1-01): the intro fits the screen - …`) and task 02's (`feat(13.1-01): Explore as page 2
       draws it - …`); STATE and the SUMMARY in a third. No trailer.
  </action>
  <verify>
    <automated>git log --oneline -3 && git diff --quiet -- .planning/ROADMAP.md src/vendor/ && npx playwright test --list | tail -1</automated>
  </verify>
  <done>c2 and c4 green (reruns named); `--list` reports 100; three commits; ROADMAP and src/vendor/ byte-unchanged.</done>
</task>

</tasks>

<verification>
- `check-counts.mjs 94 962` matches on the quick suite; `--list` is 100 runs / 85 titles is NOT yet
  the tree (only +1 here: 84 titles / 100 runs); state the observed pair.
- The four-viewport title green; `intro.spec.ts` 1-5 green; `browse-ui.spec.ts` green.
- `grep -rn "border-radius" src/lib/ui/intro src/lib/ui/CatalogCard.svelte src/routes/+layout.svelte`
  finds nothing above zero; `radius.spec.ts` A and B green after the build.
- `git diff --stat HEAD~3 -- src/vendor/ src/lib/catalog/entries/ src/lib/store/library.ts` empty.
</verification>

<success_criteria>
- Bench lines 1 and 2 answered in the tree: no document scroll on `/` at four desktop viewports;
  Explore filled, in the display face, on one row.
- Term written as `+0 / +1; +1 / +1` against the carried block; the observed block re-measured first.
- No new token, string, radius or dependency; the six circles unmoved.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-01-SUMMARY.md` with the
count line (carried, delta, observed), the four viewports' measured values and what was shrunk, the
PDF fill sample and the token chosen, every rerun named, and the co-author-trailer conflict reported.
</output>
