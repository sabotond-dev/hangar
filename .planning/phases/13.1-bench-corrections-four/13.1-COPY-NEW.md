# Phase 13.1 — the copy ledger (closed 2026-09-12 by plan 13.1-08, the gate)

Created 2026-09-12 by plan 13.1-02 (the first plan in the phase's serial order: 02 → 04 → 01 → 03 →
05 → 06 → 07 → 08) as the queue every plan of this phase appends its new, moved and retired strings
to, for the gate's (13.1-08) batch. Its shape is `13-gui-overhaul/13-COPY-NEW.md`'s: the register
first, then the rows, each with the state it names, the fact it must carry and the proposal — and
this phase adds one column, the reason there is no Bible line to take verbatim. Every string this
phase writes is in D-05's register (13-CONTEXT); every string 13-18 landed that this phase moves
keeps D-23's words or adds a row here (13.1-CONTEXT D-12).

## The register (D-05), in full

Copied unchanged from `13-gui-overhaul/13-COPY-NEW.md` (13-01, 2026-09-11; 13-CONTEXT D-05), which
is its source; this file does not restate it in its own words.

The Bible's copy — the PDF's five screens and §16's eleven rows — is the reference for tone and vibe.
Every string written for HANGAR from this phase on follows it:

- **Sentence case, short, second person.** _Make ZONA your own._ _Find a gesture you love. Build a
  surface that works the way you do._ _Pick up where you left off._
- **Names the action and its result together.** _Applied to Page 2. Store on ZONA to keep it after
  power-off._ _Discover configurations. Try one. Make it yours._
- **Plain about state, never coy.** _Preview only. Connect ZONA when you're ready._ _Changes not
  applied._ _Stored on ZONA · Page 2._ _The device stopped responding. Your draft is safe; device state
  could not be verified._
- **Reassuring where the stakes are real, in one clause.** _Reset Page 2 to its firmware default? Your
  browser draft will remain available._
- **Uppercase only for short section labels and breadcrumbs** — `PLAYGROUND / CONFIGURATIONS`,
  `YOUR LIBRARY`, `SELECTED ELEMENT / FADER` — never for sentences, headlines or instructions.
  Headlines are large, sentence-case and warm; the eyebrow above them is the small uppercase line.
- **Verbs on buttons, plainly:** _Explore_, _Apply to ZONA_, _Save copy_, _Share snapshot_, _Resume
  draft_, _New surface_, _Connect ZONA_.
- **Non-negotiables that carry over regardless of tone:** real apostrophes (_you're_, _you've_), never
  a straight-quote contraction; no exclamation marks; no emoji; no uppercase paragraphs. The three copy
  specs' other rules — no "Error", no browser engine named, no control label paraphrased in prose —
  carry over too.

The **facts** Phase 10's strings carried survive in the new words: RAM versus flash, the snapshot, the
firmware default, the promise that nothing writes without a click — and a fifth from Phase 12.1, that a
store carries the page's own scripts.

## The rows

Columns: `Symbol · Module · The state it names · The fact it must carry · Proposed · Why there is no
Bible line`. A retired row's "Proposed" column holds the string as it was, struck; its last column
names the decision that retired it.

### Retired by 13.1-02 (D-05)

Bench line 5, verbatim: *"Page switch doesnt need a confirmation window. When you change page form
the drop down just change the page and thats it."* Strikes 13-CONTEXT D-06 clause 2 (section 9's
destination review); `DestinationReview.svelte` is deleted with these four. The wire's envelope —
the restore heartbeat first, the ACK gate on the module's own report, `unverified` on the window's
close — is unmoved; `switchingLine` and `unverifiedLine` stay as they were.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `SWITCH_PAGE_LABEL` | `install-copy.ts` (was the fifth entry of `WRITE_CLICKS`) | the review's affirmative — the click that moved the page | — (retired) | ~~`Switch page`~~ (batch row I.3.8, "keep") | Retired by the user's word, D-05: there is no review, so there is no affirmative; the fifth write click is the Target select's change (`TARGET_CLICK`, below). Named in `install-copy.ts`'s header with the date; `install-copy.spec.ts` asserts it is not exported |
| `KEEP_PAGE_LABEL` | `install-copy.ts` | the review's negative — the click that did not move the page | — (retired) | ~~`Keep this page`~~ (batch row I.3.8, "keep"; J.17 "keep the click") | Retired with the review. J.17's "keep the click" is kept in another form: the way back from `unverified` is the select itself — choosing the page the module reports is a cancel (`page-target.ts` `request()`, unchanged), so no negative button is needed |
| `switchReviewLine(to, from)` | `page-target.ts` | the review's sentence, naming both pages | — (retired) | ~~`Switch your ZONA to Page 3? It will stop playing Page 1.`~~ (13-CONTEXT D-06's sentence, verbatim; never ledgered because it was the Bible's own) | Retired by the user's word, D-05: nothing renders it. Named in `page-target.ts`'s words paragraph with the date |
| `replaceReviewTitle(page)` | `page-target.ts` | the review's title | — (retired) | ~~`Replace the configuration on ZONA · Page 3?`~~ (Bible §16 "Target review" row, verbatim; batch row I.3.11's block rendered it) | Retired with the review. §16's row still stands in the Bible as text; HANGAR has no surface that shows it, and the gate's qualifier of SAFE-01 says so |

### Added by 13.1-02 (D-05, D-11 b)

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `TARGET_CLICK` | `install-copy.ts` (the fifth entry of `WRITE_CLICKS`) | not a visible string: the Target select as a write click, for SAFE-01's count | that the one control which moves the hardware is a select, and its change is the user's gesture | `Target` — `page-target.ts`'s `TARGET_LABEL` carried twice, because neither module may import the other (the `pageName` precedent); `install-copy.spec.ts` pins the twin equal to `TARGET_LABEL` | The PDF's own word for the select (pages 3 and 5, `Target`); the constant renders nowhere — the select renders `TARGET_LABEL` — so there is nothing new for a visitor to read. Note for the gate's bench row 5: on a focused, closed `<select>` Chromium fires `change` on every ArrowUp / ArrowDown, so each arrow press is a switch until the select disables at `switching` (13.1-PLAN-CHECK W-03) |

### Kept, re-purposed by 13.1-04 (D-08) — no new string

Bench line 7's first half, verbatim: *"Edit color should not be pop up window in the left upper corne
but instead open down seamlessly to edit color."* `Swatch.svelte`'s `<dialog>` is gone; the picker
opens inline under the swatch row. This plan adds no string. One existing row changes the state it
names, recorded here so F.8's row in `13-gui-overhaul/13-COPY-NEW.md` is not read as still describing
a popover.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `POPOVER_CLOSE` (symbol kept; F.8) | `inspector-copy.ts` | was: the colour popover's visible close. Now: the swatch row's `Edit color` toggle while its inline colour block is open (`aria-expanded="true"`) | that the same control opens and closes the block; that nothing floats and nothing traps focus — Escape inside the block closes it too | `Close` — F.8's approved word, unchanged (batch 13-18, "keep") | The PDF draws `Edit color` and no close; the open state needs a word, and F.8's was already approved for the same click. Not renamed: `Swatch.svelte`, `tune-ui.spec.ts` and `e2e/tuning.e2e.ts` read the symbol |
| `EDIT_COLOR` (unchanged) | `inspector-copy.ts` | the swatch row's toggle while its block is closed | — | `Edit color` — PDF page 5, verbatim | The PDF's own; listed only because the same control now carries two words by state |

### 13.1-01 (D-01, D-02) — no row

Bench lines 1 and 2, verbatim: *"I dont want the index page to be scrollable, always fit on the
screen"* and *"in playground: the explore buttons are not the proper grey color, not grifter, and
the arrow is on the second row for each button, make these changes to match the pdf."* Both are
look alone. The intro's every visible string is 13-07's and the PDF's, unmoved; the gallery card's
`Explore` and its `↗` are the PDF's page 2, unmoved (`CatalogCard.svelte`'s `EXPLORE` and `ARROW`,
the same two literals 13-08 wrote). This plan wrote no string, moved none and retired none.

### Not strings: the palette (13.1-03, D-03)

Bench line 3, verbatim: *"in sandbox: you should be able to resize each element by draggin its
points. it has visible points but they cannot be resized by that as of now. also, each element
should have its own color and better visualization, now its too minimal."* This plan wrote no
visible string: the drag's refusals are `geometry.ts`'s own lines (section 16's overlap line, the
off-surface and too-small lines, already ledgered), shown in the plate's status line as
`applyEdit` returns them; the marks' `OFF` and the `X 0.50 Y 0.50` readout are PDF page 3's own
glyphs, `aria-hidden` with the SVG. What the gate's bench row must carry is the PALETTE - four
RGB444 triples in `src/lib/sandbox/editor.ts`, cycled by creation order. Two are the page's,
measured at its 1500 render; two are proposed (13.1-CONTEXT question 4) and the row asks.

| # | Colour | RGB444 | Hex | Where it comes from |
| --- | --- | --- | --- | --- |
| 1 | lime | `13, 15, 7` | `#DDFF77` (the action colour `#DCFF71` snapped to the lattice) | PDF page 3, Filter; `DEFAULT_COLOUR`, every region's colour before this plan |
| 2 | teal | `7, 11, 10` | `#77BBAA` | PDF page 3, Space: its 1px boundary stroke samples `#74b9aa` at x 900 / y 500 of the 1500 render; snapped. The plan's starting point `3, 11, 11` (`#33BBBB`) was bluer than the page |
| 3 | amber | `15, 10, 3` | `#FFAA33` | **Proposed** - not on the page; a warm third in the same key |
| 4 | violet | `10, 7, 15` | `#AA77FF` | **Proposed** - not on the page; a cool fourth in the same key |

### Retired and added by 13.1-05 (D-04): the reset's label is the user's word

Bench line 4, verbatim: *"CLEAR button. we need a CLEAR button it should live all the time in the
top right corner next to ZONA connected."* The store's `clearToDefault()` (12-03) gets its control
in the header's connection zone, left of the connection control, on every page the shell renders
and in both header variants. One click, A-45's shipped behaviour, kept — batch row I.6.5 (§16's
confirmation) is declined by this phase (13.1-CONTEXT D-04; question 2 for the gate's bench row).
One label moves; nothing else the control says changes: `clearingLabel` (`Resetting Page 2…`),
`clearLine` and the three `CLEAR_REASONS` are D-23's words, unmoved — the reasons are now the
control's caption where the header's row has room for them (measured, 13.1-05-SUMMARY.md) and its
accessible description everywhere.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `CLEAR_LABEL` (retired value) | `install-copy.ts` (the fourth entry of `WRITE_CLICKS`) | the reset control, at the foot of the workspace's install column | — (retired value; the symbol stays) | ~~`Reset active device page`~~ (§9's own label, verbatim; 13-18, D-23) | Retired by the user's word, D-04: four words do not fit the PDF's 218 × 37 box beside `ZONA connected`, and the user named the control `CLEAR`. §9's row stands in the Bible as text; `install-copy.ts`'s header names the retirement with the date; `install-copy.spec.ts` asserts the old words are the value of no export |
| `CLEAR_LABEL` (new value) | `install-copy.ts` (the fourth entry of `WRITE_CLICKS`) | the header's Clear box, live and disabled alike; the busy leg reads `clearingLabel` | that the click restores the firmware default and leaves the browser draft alone — carried by `clearLine`, the control's `aria-describedby` description, not by the label | `Clear` — the user's word in D-05's case | The user's own word (bench line 4); no Bible line names the control in one word. Not a stem A-48 forbids (`clears`, `empt`, `remove`); the register's re-casing rule excuses it as it excuses `Put back`, because `settledBody`'s "changing page on your ZONA clears it" (D-23) is the wire's verb, not the control. `install-copy.spec.ts`'s batch containment passes it by E.11's row (the search field's own `Clear`) — a substring, not a review; this row is the review. Note for the gate's bench row: the playground's search field also reads `Clear` (accessible name `Clear the search`, E.11) — two controls, one word, different accessible names |

### Retired and rewritten by 13.1-06 (D-06, D-07)

Bench line 6, verbatim: *"in the configuration panel the block i attached a screenshot of is totally
unnecessary remove that. we dont even need the Put back function that totally unnecessary if we have a
clear button. the second row in the page (so under the logo) the right side should look like this:
Target PAGE 1 on ZONA selector, Apply to ZONA button, Store on ZONA button."* — and the user's answer
**"remove"** to the orchestrator's question (D-07). The install column under the workspace's surface is
gone (batch row J.13); the context bar's right zone is one component for both routes
(`DestinationZone.svelte`); Put back is on no screen. **What stays:** the snapshot at connect before any
write (SAFE-03), its durable record (SAFE-04), the re-snapshot on a page change, and the store's
`putBack()` with its `restoring` / `restored` / `restored-unconfirmed` phases — reachable from the
`/dev/install/` probe's own button alone, proved there and on the fake. **The consequence, plainly:**
after an Apply the way back to the module's own page is Grid Editor or the header's `Clear` to the
firmware default. Every sentence below that offered Put back as the way back now names the way back that
exists. `install-copy.spec.ts` test 2 reads this file as its fourth document: a rewritten string must be
here verbatim (the sample page is wire 1, read as `Page 2`, as the batch's samples are).

The register's re-casing excuse for `Put back` (it was the register's own verb) retires with the control;
`Clear`'s excuse (13.1-05: `settledBody`'s "clears it", the wire's verb) retires with `settledBody` —
every "clear" left in `install-copy.ts` is the control, named as the control reads.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `PUT_BACK_LABEL` | `install-copy.ts` (was the second entry of `WRITE_CLICKS`) | the restore control | — (retired) | ~~`Put back`~~ (batch row I.3.2) | Retired by the user's word, D-07: the control is on no screen. `WRITE_CLICKS` is four — Apply to ZONA, Store on ZONA, Clear, Target. Named in `install-copy.ts`'s header with the date; `install-copy.spec.ts` asserts it is not exported |
| `puttingBackLabel(page)` | `install-copy.ts` | the restore's progress label | — (retired) | ~~`Putting Page 2 back…`~~ (I.3.3) | Retired with the control; the probe reads the store's phase, not a label |
| `STEP_OR_PUT_BACK` | `install-copy.ts` | the step four failure blocks shared | — (retired; replaced by `stepOrClear` below) | ~~`Or click Put back to restore what was there when you connected`~~ (I.5.8) | A step must name a control that exists |
| `stepOrClear(page)` | `install-copy.ts` (the second step of `keptMismatchBlock`, `unconfirmedBlock`, `partialBlock`) | the same three blocks' way back | that the header's Clear returns the page to the firmware default — the way back that exists; the page named as the visitor reads it | `Or click Clear to return Page 2 to its firmware default` | Supersedes I.5.8 under D-07. §16 has no way-back step; `Clear` is the control as it reads (13.1-05), the firmware default is `clearLine`'s fact |
| `restoredUnconfirmedBlock(page).steps[0]` | `install-copy.ts` | the restore's store leg did not confirm — **the probe's alone since 13.1-06** | the probe's action, in the probe's words | `Send the restore again` — was ~~`Click Put back again`~~ (I.5.6) | No control named because none exists; the block is reachable from `/dev/install/` only |
| `nothingLandedBlock("put-back", page).steps[0]` | `install-copy.ts` | nothing landed after a restore — **the probe's alone since 13.1-06** | the same | `Send the restore again` — was ~~`Click Put back to send it again`~~ (I.5.5) | As above |
| `nothingLandedBlock("try", page).detail` | `install-copy.ts` | nothing landed after an apply | that the module's own configuration is still playing | `Nothing got through. Page 2 is unchanged, so your own configuration is still playing.` — was ~~`… still playing and there’s nothing to put back.`~~ (I.5.5) | The clause named the retired control's verb as a comfort; the fact stands without it |
| `lostBlock(...).steps[2]` | `install-copy.ts` | unplugged mid-write, both forms | the way back after re-plugging and re-applying | `Then click Clear if you want the firmware default back` — was ~~`Then click Put back to restore what was there when you connected`~~ (I.5.12) | Names the control that exists; the firmware default is what it brings |
| `SNAPSHOT_FAILED_TITLE` (`snapshotFailedBlock(page).title`) | `install-copy.ts` | the snapshot could not be taken | that nothing is copied yet, so nothing is written | `Nothing copied yet` — was ~~`Nothing to put back yet`~~ (I.5.7) | The fact is the copy, not the control; the detail (`… won’t write over something it hasn’t copied`) is unchanged |
| `CONFIRM_WAY_BACK` | `install-copy.ts` (`KeepConfirm.svelte`'s third sentence) | beside the store confirmation | that a way back survives the store | `Clear still returns the page to its firmware default.` — was ~~`Put back still restores what was there when you connected.`~~ (I.5.10) | Names the control that exists |
| `KEEP_REASONS["after-partial"]` | `install-copy.ts` | Store on ZONA disabled after a partial apply | the two ways on: apply again, or the firmware default | `Not after a partial apply. Apply to ZONA again, or click Clear.` — was ~~`… Apply to ZONA again, or put your own back.`~~ (I.6.2) | Names the control that exists |
| `HONESTY_SNAPSHOTTING` | `install-copy.ts` (Apply to ZONA's description while the snapshot is read) | snapshotting | the second fact: the copy is taken first | `Reading what your ZONA holds first.` — was ~~`Reading what your ZONA holds first, so anything you apply can be put back.`~~ (I.4.3) | The copy is a safety feature, not a control; the promise of a put-back would be false |
| `restoredCaption(page)` | `install-copy.ts` (the bar's device clause for `restored` — **read from the probe alone since 13.1-06**) | the restore landed | the module's own page is back | `Page 2 put back` — was ~~`Put back · Page 2`~~ (I.4.13, the label and the page) | The label retired; the register's own verb stays (it is the machinery's phase, not a control) |
| `RESTORED_UNCONFIRMED_TITLE` (unchanged) | `install-copy.ts` | the restore's store leg did not confirm — the probe's | — | `Put back in memory, not yet stored` (I.5.6, kept) | The machinery's own words; listed because it is one of the two strings that may still say "put back", by name in `install-copy.spec.ts` |
| `PUT_BACK_NEEDS_ZONA` → `NEEDS_ZONA` | `install-copy.ts` (`CLEAR_REASONS["no-session"]` reads it) | the header's Clear with no session | — (the same sentence, renamed) | `Needs your ZONA connected.` (I.3.4, unchanged) | The sentence's one reader is Clear's reason now; the name said Put back |
| `snapshottingBody`, `identifiedBody`, `settledBody`, `keptBody`, `restoredBody`, `KEPT_PROOF_LINE`, `clearedBody`, `RESTORED_STORED_LINE` | `install-copy.ts` | the install block's six success phases under the surface | — (retired; the facts survive in the bar's captions and in `confirmReplaces`) | ~~I.4.6, I.4.7, I.4.10, I.4.11, I.4.14, I.4.15, I.4.17 and the restore's stored line~~ | `InstallState.svelte` was their one reader and is deleted with the column (D-06; 13.1-PLAN-CHECK W-10: nothing dead is left exported). `RESTORED_STORED_LINE` is the eighth, not in the plan's seven, retired on the same rule |
| `STILL_WRITING_LINE` (kept) | `install-copy.ts` → `DestinationZone.svelte` (`still-writing`, beneath the bar's row while the store's `slow` flag is set) | a write past 2000 ms | SAFE-08's visible half | `Still writing. Your ZONA is taking longer than usual.` (I.4.9, unchanged) | Re-homed, not retired (W-11) |
| `putBackPageLine(page)`, `putBackPageLineAfterKeep(page)` | `page-target.ts` | the line under Put back with a snapshot in hand | — (retired) | ~~`Puts Page 2 back to what it was playing when you connected.`~~ / ~~`… and stores it so it stays.`~~ (13-12; the batch's Put back row) | Retired with the control; the probe's readout names the page from the store |
| `SNAPSHOT_DURABLE_LINE` | `session-copy.ts` (Device actions' snapshot line, durable) | a copy in localStorage | the second fact: the copy is kept, where, and that it survives a tab | `A copy of the page your ZONA was on when you connected is kept in this browser, and kept on record even from a new tab.` — was ~~`… so it can be put back even from a new tab.`~~ (I.2.15) | The clause promised a control the user removed |
| `SNAPSHOT_SESSION_LINE` | `session-copy.ts` (the tab-only twin) | a copy held for the session | the same, until the tab closes | `A copy of the page your ZONA was on when you connected is held until this tab closes, and kept on record while you’re here.` — was ~~`… so it can be put back while you’re here.`~~ (I.2.16) | As above |
| `STORE_LABEL` | `sandbox/copy.ts` | the Sandbox's Store on ZONA | — (retired symbol; the words are `install-copy.ts`'s `KEEP_LABEL`, the same three) | ~~`Store on ZONA`~~ (13-17, "From 13-17"; §9 verbatim) | `SurfaceActions`' destination half — its one reader — moved into `DestinationZone.svelte`, which reads `KEEP_LABEL` so the button and the confirmation's affirmative read one constant (D-11 a; D-12) |

### Added by 13.1-07 (D-09): the MIDI output's two typed fields

Bench line 7, screenshot 2, verbatim: *"replace MIDI channel selector with MIDI output selector with
input fields, exactly as on the attached screenshot."* PDF page 5's MIDI output: `CC number` holding
74, `Channel` holding 1, the helper *"Map this CC to a parameter in your instrument or DAW."*
beneath — the two labels and the helper are the PDF's and taken verbatim (`CC_NUMBER_LABEL`,
`CHANNEL_LABEL`, `MIDI_HELPER` unchanged since 13-09). A field is a text input OVER the knob's own
closed list (`MidiField.svelte`; Phase 13 D-14 Q5 overridden for these two fields and no other): a
typed whole number the knob offers moves the knob to that index, one it does not is refused in the
field with the offered values named, and the last good value survives (13-16's rule and shape). The
three strings below are HANGAR's — the PDF draws no refusal and no cue. **X-08 is kept**: a Lua
entry's Channel shows the firmware's zero-based literal (`0`) where the PDF and every preset show
the DAW's `1`; 13.1-CONTEXT question 5 stands for the gate's bench row, and the cue is what keeps the
asked state from being a silent off-by-one until it is answered (13.1-PLAN-CHECK W-16).

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `TYPE_A_NUMBER` | `inspector-copy.ts` (`MidiField.svelte`'s message under a refused keystroke that is not a whole number) | a keystroke that is not a whole number | the way, never "invalid" | `Type a whole number.` | Modelled on G.27 (`WHOLE_NUMBER`, the Sandbox's same line); a second constant because neither import-nothing module may read the other, the `pageName` precedent |
| `offeredLine(id, literals)` — the list form | `inspector-copy.ts` (`MidiField.svelte`'s message under a whole number the knob does not offer) | a typed value the closed list does not hold | which values the knob offers, in the knob's own literals | `A controller number here is one of 1, 16, 20, 74 or 102.` (Arc's `cc`; the literals joined by commas, `or` before the last; `here` because the list is this configuration's, not MIDI's) | 13.1-CONTEXT D-09's ledgered sentence, kept as written (13.1-PLAN-CHECK W-15). The Sandbox's G.28 `A controller number is 0 to 127.` names the full range; the workspace's list is not the range, so the refusal names the values |
| `offeredLine(id, literals)` — the range form | `inspector-copy.ts` | the same, on a knob whose literals are a contiguous run (every channel knob in the tree) | the run's bounds in the knob's own base | `A channel here is 0 to 15.` on a Lua entry; `A channel here is 1 to 16.` on a preset | **G.29's `A channel is 1 to 16.` is NOT reused (W-15):** it cannot serve a Lua channel's `0` to `15` without lying, and one builder for both bases is the one place to change when question 5 is answered. The noun is the knob's (`channel` → `A channel`, `cc` / `ccBase` / `send` → `A controller number`); the form is the list's shape, so no knob is ever told a range it does not have |
| `LUA_CHANNEL_CUE` | `inspector-copy.ts` (under a Lua entry's Channel field and in its `aria-describedby`; absent on a preset's) | a zero-based channel under the PDF's `Channel` label | that the firmware counts from 0 and the DAW from 1, so a typed `1` is the DAW's channel 2 | `The firmware counts channels from 0; your DAW’s channel 1 is 0 here.` (a real apostrophe) | W-16: the PDF draws `1`; the tree writes the firmware's literal (X-08); until question 5 is answered the field says which base it is in. Retire the cue by name when the question is answered either way |
| `CC_NUMBER_LABEL`, `CHANNEL_LABEL` (the PDF's, verbatim) | `inspector-copy.ts` (`midiFieldLabel(knob)`: the PDF's word for a knob with id `cc` or `channel`, the knob's own for `ccBase` and `send`) | the field's visible label | the PDF's page 5 | `CC number` / `Channel` (PDF page 5, verbatim); `CC base` / `Send` stay the knob's (question 6, shipped this way) | Verbatim; listed so the label rule is on the record — the PDF's `CC number` would misname morph's base |

### Retired by 13.1-07 (D-10): the workspace's meters are hidden

Bench line 8, verbatim: *"TUNING, so code limit visualiztation should be removed, lets not show
that."* The workspace inspector's fourth group — the `TUNING` caption, the two `{used} / 908` meters,
the forecast ghost and the rack's forecast delta — is not rendered. `cost()` still measures every
change; the two numbers ride on the tuning region's root as `data-setup` / `data-timer` /
`data-busy` for the suite, painted by nothing; TUNE-05 renders as `BudgetMessage.svelte`'s line and
the zone's disabled Apply with its refusal, exactly as before. `BudgetMeter.svelte` is **not**
deleted: the Sandbox mounts it twice under its own room line, which D-10 keeps and the gate's bench
row asks about (question 3). One string lost its only reader with the branch that rendered it:

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `METERS_UNAVAILABLE` | `tune/copy.ts` (was `TuningRegion.svelte`'s one Body line in the meters' place when the formatter never resolved, D-08) | the character counter could not load | — (retired) | ~~`The character counter couldn’t load, so the two budgets aren’t shown. Everything else here still works.`~~ (13-19's register rewrite of 05-10's line) | Retired by name and dated in `copy.ts`'s header (D-12's rule; 13.1-PLAN-CHECK W-14): the meters it excused are hidden by the user's word, so the branch that rendered it is gone. The state it named is not left silent by accident: an unmeasurable budget leaves the tuner's pair undefined, and the zone's Apply is a real `disabled` on `config === undefined`, so no visitor is left with an enabled write and no number. `copy.spec.ts` asserts it is not exported. The meter family (`TUNING_CAPTION`, `SETUP_CAPTION`, `TIMER_CAPTION`, `meterNumerals`, `meterPercent`, `MEASURING`, `meterExpansion`, `emptyTimerExpansion`, `forecastDelta`, `forecastExpansion`) stays exported: the Sandbox's meters still read it, the forecast pair keeps U+2212's one scope, and whether the forecast strings retire with the ghost is the gate's to decide by name |

## Where every row went (closed 2026-09-12 by plan 13.1-08, the gate)

The ledger is complete for the phase. Every row above is a string that LANDED (a module and an
export, held by the spec named), a string that was RETIRED by name (dated in its module's header,
asserted not exported), a string KEPT whose state changed, or a thing that is not a string and is
carried to the bench. Nothing in this file is owed to a later plan; the two proposed colours and
the two numbering questions are carried to the bench as questions, not as work. 13-18's closing
shape.

| Section | Rows | Landed (module, export) | Retired by name | Kept, re-purposed | Carried to the bench (13.1-08 task 03) |
| --- | --- | --- | --- | --- | --- |
| Retired by 13.1-02 (D-05) | 4 | — | `SWITCH_PAGE_LABEL`, `KEEP_PAGE_LABEL` (`install-copy.ts`, "THE REVIEW'S TWO LABELS ARE RETIRED BY NAME"); `switchReviewLine`, `replaceReviewTitle` (`page-target.ts`); `DestinationReview.svelte` deleted | — | row 5 / 10: the switch on hardware with the arrow key |
| Added by 13.1-02 | 1 | `TARGET_CLICK` = `Target` (`install-copy.ts`, the fourth entry of `WRITE_CLICKS` since 13.1-06; pinned equal to `page-target.ts`'s `TARGET_LABEL` by `install-copy.spec.ts`) | — | — | the arrow-key fact (question at row 5) |
| Kept by 13.1-04 (D-08) | 2 | — | — | `POPOVER_CLOSE` (`inspector-copy.ts`, F.8's `Close`, now the toggle's open word); `EDIT_COLOR` unchanged | questions M, N, O (the lime `Close`, the hairline, Escape on the toggle) |
| 13.1-01 (D-01, D-02) | 0 | — | — | — | question A (the arrow's colour) and 13.1-01's intro questions (V) |
| Not strings: the palette (13.1-03) | 4 colours | `PALETTE` (`src/lib/sandbox/editor.ts`): lime `13,15,7` and teal `7,11,10` measured off page 3; `DEFAULT_COLOUR` = `PALETTE[0]` | — | — | **amber `15,10,3` and violet `10,7,15` PROPOSED** - question B; the tint, the boundary, the cells (C, D, E) |
| Retired and added by 13.1-05 (D-04) | 2 | `CLEAR_LABEL` = `Clear` (`install-copy.ts`, the third entry of `WRITE_CLICKS`; `install-copy.spec.ts`) | `Reset active device page` as the value of no export ("SECTION 9'S RESET LABEL IS RETIRED BY NAME") | `clearingLabel`, `clearLine`, `CLEAR_REASONS` unmoved (the reasons are the caption where the zone has room) | questions F, G, H (the caption's precedence, the gate, the label) |
| Retired and rewritten by 13.1-06 (D-06, D-07) | 22 | `stepOrClear(page)`; `lostBlock(...).steps[2]`; `nothingLandedBlock("try").detail`; `SNAPSHOT_FAILED_TITLE`; `CONFIRM_WAY_BACK`; `KEEP_REASONS["after-partial"]`; `HONESTY_SNAPSHOTTING`; `restoredCaption(page)`; `restoredUnconfirmedBlock(page).steps[0]` and `nothingLandedBlock("put-back").steps[0]` (the probe's); `NEEDS_ZONA` (renamed from `PUT_BACK_NEEDS_ZONA`) - all `install-copy.ts`, held by `install-copy.spec.ts` reading this file as its fourth document with the seventeen pinned by name; `SNAPSHOT_DURABLE_LINE`, `SNAPSHOT_SESSION_LINE` (`session-copy.ts`, `session-copy.spec.ts`) | `PUT_BACK_LABEL`, `puttingBackLabel`, `STEP_OR_PUT_BACK`; `snapshottingBody`, `identifiedBody`, `settledBody`, `keptBody`, `restoredBody`, `KEPT_PROOF_LINE`, `clearedBody`, `RESTORED_STORED_LINE` (`install-copy.ts`); `putBackPageLine`, `putBackPageLineAfterKeep` (`page-target.ts`); `STORE_LABEL` (`sandbox/copy.ts`); `TryOnDevice.svelte`, `InstallState.svelte`, `KeepOnDevice.svelte`, `PutBack.svelte` deleted | `STILL_WRITING_LINE` re-homed in `DestinationZone.svelte` under `still-writing` (W-11); `RESTORED_UNCONFIRMED_TITLE` unchanged (the probe's) | questions J, K, L, T (the failure block's place, the way-back wording, the honesty line, the zone after an unplug); SAFE-03 / SAFE-04 / SAFE-09 amended at the gate |
| Added by 13.1-07 (D-09) | 5 | `TYPE_A_NUMBER`, `offeredLine(id, literals)` (the list form and the range form from one builder), `LUA_CHANNEL_CUE`, `midiFieldLabel(knob)` over `CC_NUMBER_LABEL` / `CHANNEL_LABEL` - all `inspector-copy.ts`, rendered by `MidiField.svelte`, held by `tune-ui.spec.ts` and `tuning.e2e.ts`'s CC number title | — | — | **questions P and Q**: channel numbering on a Lua entry (retire `LUA_CHANNEL_CUE` by name when answered either way), the field's label on a `ccBase` / `send` knob |
| Retired by 13.1-07 (D-10) | 1 | — | `METERS_UNAVAILABLE` (`tune/copy.ts`, dated in its header; `copy.spec.ts`) | the meter family (`TUNING_CAPTION`, `SETUP_CAPTION`, `TIMER_CAPTION`, `meterNumerals`, `meterPercent`, `MEASURING`, `meterExpansion`, `emptyTimerExpansion`, `forecastDelta`, `forecastExpansion`) kept exported for the Sandbox's two meters and `Knob.svelte`'s forecast delta; `BudgetMeter.svelte` kept | question R (the Sandbox's meters and room line); the forecast's retirement by name recorded in `deferred-items.md` with the exact edit |

**The two things the bench must carry that are not strings**, as the plan asked: the palette's amber and violet (question B) and the channel numbering on Lua entries (question P). Both are in `docs/INSTALL-RUNBOOK.md` under "The fourth bench, 2026-09-12 … the nine changes handed over".

**The ledger is complete for the phase.** No row is open; no string is owed; the copy specs read this file as their fourth document and pin the seventeen 13.1-06 strings and the two snapshot lines by name, so it cannot be appended to without a spec moving.


### Rewritten after the gate by the round-4c quick task (2026-09-12): Clear stores the defaults

The user's word, verbatim, `BENCH-2026-09-12.txt` "Round 4c": *"clear should not be RAM only
though!! it should be like Store but with Clear!"* — after being asked whether Clear was greyed out
(a), cleared RAM but the Editor's config came back after a power-cycle (b), or left the Editor's
behaviour running (c): *"b"*. The store's `clearToDefault()` now runs the five defaults through
the one writer as before and then the same ACK-gated store leg Store on ZONA runs (`#storeLeg`:
one `PAGESTORE/EXECUTE`, the module's next heartbeat, the five-string re-fetch proof, D-12), and
says `cleared` only after the proof; a store acknowledged but read back different lands
`kept-mismatch` (reused — its sentence is exactly true of a clear's store and its second step names
Clear as the retry), one never acknowledged lands `unconfirmed` with `FIRMWARE_DEFAULT_NAME` as
the block's name. One click, no confirmation — 13.1 D-04 stands, as the user decided the same day.
Four strings say the store now, and one is new; the batch rows they supersede are named. The
"after a clear a KEEP stores nothing" fact (`cfg_default_flag`, 12-03) is unchanged and is now
what the clear's OWN store does: the module deletes the cfg file rather than writing one, which is
the same power-cycle-safe default by another route.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `clearLine(page)` | `install-copy.ts` (the header's Clear: its `aria-describedby` description when live) | the third fact, before the click | the firmware default, that it is STORED so it survives power-off, and that the browser draft stays | `Returns Page 2 to its firmware default and stores it, so it stays after power-off. Your browser draft stays as it is.` — was ~~`Returns Page 2 to its firmware default. Your browser draft stays as it is.`~~ (I.6.3) | §16's confirmation words for the default; the store clause is `keepLineEnabled`'s own (`so it stays after power-off`), the first fact flash-side, because a description silent about the one thing the click now makes permanent would be the never-writes class of omission |
| `clearingLabel(page)` | `install-copy.ts` (the Clear box's label with `aria-busy` through both legs) | the reset control while it writes and stores | in progress, both legs | `Resetting and storing Page 2…` — was ~~`Resetting Page 2…`~~ (I.4.20) | §9's progress shape (`Applying to…`, `Storing on…`), the two verbs joined because the click runs both and `action` stays `clear` through both |
| `clearedCaption(page)` | `install-copy.ts` (the bar's device clause for `cleared`, `device-clause.ts`) | the reset acknowledged AND stored, after the proof | the firmware default is back on the page and it is stored | `Page 2 reset to its firmware default and stored` — was ~~`Page 2 reset to its firmware default`~~ (I.4.16) | §16 has the question, not the result; `stored` is §16's own word (`Stored on ZONA · Page 2`), spoken here only after the `PAGESTORE` acknowledgement and the re-fetch proof, as `keptCaption` is |
| `liveCleared(page)` | `install-copy.ts` (the live region, once, on entry to `cleared`) | the same, spoken | the same | `Page 2 is reset to its firmware default and stored.` — was ~~`Page 2 is reset to its firmware default.`~~ (I.7.5) | none; the caption as a sentence |
| `FIRMWARE_DEFAULT_NAME` | `install-copy.ts` (the store sets it as `name` on the one row: a clear whose store never acknowledged; `DestinationZone.svelte`'s `unconfirmedBlock(shownName, page)` reads it) | `unconfirmed` after a clear | what is running in memory is the firmware default, not the route's entry | `The firmware default` — new | `unconfirmedBlock`'s sentence interpolates a name (`Aurora is still running on Page 2 in memory…`, I.5.1); after a clear the honest name is this one, and the zone's fallback (the entry's name) would be a lie. `keepReason()` reads `never-tried` for the row, the closed set's own answer |

**Counts**: the ledgered list in `install-copy.spec.ts` grows from seventeen to twenty-two (the
four rewrites and the one new string); the A-48 stem scan's twelve strings are the same twelve
(none says `clears`, `empt` or `remove` — `stores` and `stored` are neither). The gate's
qualifier on SAFE-02 is the bench's, not this task's: the store to flash is proved on the fake
(`install.spec.ts`, three titles) and on the shim (`e2e/install.e2e.ts`, the Clear walk) and never
on a module — runbook row N.
