# Phase 13.1 — the copy ledger

Created 2026-09-12 by plan 13.1-02 (the first plan in the phase's serial order: 02 → 04 → 01 → 03 →
05 → 06 → 07 → 08) as the queue every plan of this phase appends its new, moved and retired strings
to, for the gate's (13.1-08) batch. Its shape is `13-gui-overhaul/13-COPY-NEW.md`'s: the register
first, then the rows, each with the state it names, the fact it must carry and the proposal — and
this phase adds one column, the reason there is no Bible line to take verbatim. Every string this
phase writes is in D-05's register (13-CONTEXT); every string 13-18 landed that this phase moves
keeps D-23's words or adds a row here (13.1-CONTEXT D-12).

## The register (D-05), in full

Copied unchanged from `13-gui-overhaul/13-COPY-NEW.md` (13-01, 2026-09-11; 13-CONTEXT D-05), which
is its source; this file does not restate it in its own words.

The Bible's copy — the PDF's five screens and §16's eleven rows — is the reference for tone and vibe.
Every string written for HANGAR from this phase on follows it:

- **Sentence case, short, second person.** _Make ZONA your own._ _Find a gesture you love. Build a
  surface that works the way you do._ _Pick up where you left off._
- **Names the action and its result together.** _Applied to Page 2. Store on ZONA to keep it after
  power-off._ _Discover configurations. Try one. Make it yours._
- **Plain about state, never coy.** _Preview only. Connect ZONA when you're ready._ _Changes not
  applied._ _Stored on ZONA · Page 2._ _The device stopped responding. Your draft is safe; device state
  could not be verified._
- **Reassuring where the stakes are real, in one clause.** _Reset Page 2 to its firmware default? Your
  browser draft will remain available._
- **Uppercase only for short section labels and breadcrumbs** — `PLAYGROUND / CONFIGURATIONS`,
  `YOUR LIBRARY`, `SELECTED ELEMENT / FADER` — never for sentences, headlines or instructions.
  Headlines are large, sentence-case and warm; the eyebrow above them is the small uppercase line.
- **Verbs on buttons, plainly:** _Explore_, _Apply to ZONA_, _Save copy_, _Share snapshot_, _Resume
  draft_, _New surface_, _Connect ZONA_.
- **Non-negotiables that carry over regardless of tone:** real apostrophes (_you're_, _you've_), never
  a straight-quote contraction; no exclamation marks; no emoji; no uppercase paragraphs. The three copy
  specs' other rules — no "Error", no browser engine named, no control label paraphrased in prose —
  carry over too.

The **facts** Phase 10's strings carried survive in the new words: RAM versus flash, the snapshot, the
firmware default, the promise that nothing writes without a click — and a fifth from Phase 12.1, that a
store carries the page's own scripts.

## The rows

Columns: `Symbol · Module · The state it names · The fact it must carry · Proposed · Why there is no
Bible line`. A retired row's "Proposed" column holds the string as it was, struck; its last column
names the decision that retired it.

### Retired by 13.1-02 (D-05)

Bench line 5, verbatim: *"Page switch doesnt need a confirmation window. When you change page form
the drop down just change the page and thats it."* Strikes 13-CONTEXT D-06 clause 2 (section 9's
destination review); `DestinationReview.svelte` is deleted with these four. The wire's envelope —
the restore heartbeat first, the ACK gate on the module's own report, `unverified` on the window's
close — is unmoved; `switchingLine` and `unverifiedLine` stay as they were.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `SWITCH_PAGE_LABEL` | `install-copy.ts` (was the fifth entry of `WRITE_CLICKS`) | the review's affirmative — the click that moved the page | — (retired) | ~~`Switch page`~~ (batch row I.3.8, "keep") | Retired by the user's word, D-05: there is no review, so there is no affirmative; the fifth write click is the Target select's change (`TARGET_CLICK`, below). Named in `install-copy.ts`'s header with the date; `install-copy.spec.ts` asserts it is not exported |
| `KEEP_PAGE_LABEL` | `install-copy.ts` | the review's negative — the click that did not move the page | — (retired) | ~~`Keep this page`~~ (batch row I.3.8, "keep"; J.17 "keep the click") | Retired with the review. J.17's "keep the click" is kept in another form: the way back from `unverified` is the select itself — choosing the page the module reports is a cancel (`page-target.ts` `request()`, unchanged), so no negative button is needed |
| `switchReviewLine(to, from)` | `page-target.ts` | the review's sentence, naming both pages | — (retired) | ~~`Switch your ZONA to Page 3? It will stop playing Page 1.`~~ (13-CONTEXT D-06's sentence, verbatim; never ledgered because it was the Bible's own) | Retired by the user's word, D-05: nothing renders it. Named in `page-target.ts`'s words paragraph with the date |
| `replaceReviewTitle(page)` | `page-target.ts` | the review's title | — (retired) | ~~`Replace the configuration on ZONA · Page 3?`~~ (Bible §16 "Target review" row, verbatim; batch row I.3.11's block rendered it) | Retired with the review. §16's row still stands in the Bible as text; HANGAR has no surface that shows it, and the gate's qualifier of SAFE-01 says so |

### Added by 13.1-02 (D-05, D-11 b)

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `TARGET_CLICK` | `install-copy.ts` (the fifth entry of `WRITE_CLICKS`) | not a visible string: the Target select as a write click, for SAFE-01's count | that the one control which moves the hardware is a select, and its change is the user's gesture | `Target` — `page-target.ts`'s `TARGET_LABEL` carried twice, because neither module may import the other (the `pageName` precedent); `install-copy.spec.ts` pins the twin equal to `TARGET_LABEL` | The PDF's own word for the select (pages 3 and 5, `Target`); the constant renders nowhere — the select renders `TARGET_LABEL` — so there is nothing new for a visitor to read. Note for the gate's bench row 5: on a focused, closed `<select>` Chromium fires `change` on every ArrowUp / ArrowDown, so each arrow press is a switch until the select disables at `switching` (13.1-PLAN-CHECK W-03) |

### Kept, re-purposed by 13.1-04 (D-08) — no new string

Bench line 7's first half, verbatim: *"Edit color should not be pop up window in the left upper corne
but instead open down seamlessly to edit color."* `Swatch.svelte`'s `<dialog>` is gone; the picker
opens inline under the swatch row. This plan adds no string. One existing row changes the state it
names, recorded here so F.8's row in `13-gui-overhaul/13-COPY-NEW.md` is not read as still describing
a popover.

| Symbol | Module | The state it names | The fact it must carry | Proposed | Why there is no Bible line |
| --- | --- | --- | --- | --- | --- |
| `POPOVER_CLOSE` (symbol kept; F.8) | `inspector-copy.ts` | was: the colour popover's visible close. Now: the swatch row's `Edit color` toggle while its inline colour block is open (`aria-expanded="true"`) | that the same control opens and closes the block; that nothing floats and nothing traps focus — Escape inside the block closes it too | `Close` — F.8's approved word, unchanged (batch 13-18, "keep") | The PDF draws `Edit color` and no close; the open state needs a word, and F.8's was already approved for the same click. Not renamed: `Swatch.svelte`, `tune-ui.spec.ts` and `e2e/tuning.e2e.ts` read the symbol |
| `EDIT_COLOR` (unchanged) | `inspector-copy.ts` | the swatch row's toggle while its block is closed | — | `Edit color` — PDF page 5, verbatim | The PDF's own; listed only because the same control now carries two words by state |

### 13.1-01 (D-01, D-02) — no row

Bench lines 1 and 2, verbatim: *"I dont want the index page to be scrollable, always fit on the
screen"* and *"in playground: the explore buttons are not the proper grey color, not grifter, and
the arrow is on the second row for each button, make these changes to match the pdf."* Both are
look alone. The intro's every visible string is 13-07's and the PDF's, unmoved; the gallery card's
`Explore` and its `↗` are the PDF's page 2, unmoved (`CatalogCard.svelte`'s `EXPLORE` and `ARROW`,
the same two literals 13-08 wrote). This plan wrote no string, moved none and retired none.

### Not strings: the palette (13.1-03, D-03)

Bench line 3, verbatim: *"in sandbox: you should be able to resize each element by draggin its
points. it has visible points but they cannot be resized by that as of now. also, each element
should have its own color and better visualization, now its too minimal."* This plan wrote no
visible string: the drag's refusals are `geometry.ts`'s own lines (section 16's overlap line, the
off-surface and too-small lines, already ledgered), shown in the plate's status line as
`applyEdit` returns them; the marks' `OFF` and the `X 0.50 Y 0.50` readout are PDF page 3's own
glyphs, `aria-hidden` with the SVG. What the gate's bench row must carry is the PALETTE - four
RGB444 triples in `src/lib/sandbox/editor.ts`, cycled by creation order. Two are the page's,
measured at its 1500 render; two are proposed (13.1-CONTEXT question 4) and the row asks.

| # | Colour | RGB444 | Hex | Where it comes from |
| --- | --- | --- | --- | --- |
| 1 | lime | `13, 15, 7` | `#DDFF77` (the action colour `#DCFF71` snapped to the lattice) | PDF page 3, Filter; `DEFAULT_COLOUR`, every region's colour before this plan |
| 2 | teal | `7, 11, 10` | `#77BBAA` | PDF page 3, Space: its 1px boundary stroke samples `#74b9aa` at x 900 / y 500 of the 1500 render; snapped. The plan's starting point `3, 11, 11` (`#33BBBB`) was bluer than the page |
| 3 | amber | `15, 10, 3` | `#FFAA33` | **Proposed** - not on the page; a warm third in the same key |
| 4 | violet | `10, 7, 15` | `#AA77FF` | **Proposed** - not on the page; a cool fourth in the same key |
