---
phase: 13.1-bench-corrections-four
plan: 04
type: execute
wave: 2
depends_on: ["13.1-02"]
# Execution order (13.1-PLAN-CHECK section 4): SERIAL - 02 -> 04 -> 01 -> 03 -> 05 -> 06 -> 07 -> 08.
# In substance this plan depends on nothing; it follows 02 because the four wave-1 plans share one
# tree, one build/, one port 4173, STATE.md and 13.1-COPY-NEW.md.
files_modified:
  - src/lib/ui/Swatch.svelte
  - src/lib/tune/inspector-copy.ts
  - src/lib/ui/tune-ui.spec.ts
  - e2e/tuning.e2e.ts
  - e2e/radius.e2e.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [IDENT-01]

must_haves:
  truths:
    - "Edit color opens the picker directly under that swatch row, in the inspector's flow; nothing floats, no backdrop, the rows below move down (D-08, bench line 7)."
    - "The picker inside is ColourPicker.svelte unchanged — its three circles at :840, :867, :882 do not move; the RGB444 lattice, the rails, the cheap-step marks and the budget arithmetic are untouched."
    - "Focus: no trap, no return. Edit color is a toggle that reads Close while open; Escape inside the block closes it and focus lands on the toggle only because the focused element leaves the DOM."
    - "The Sandbox's Appearance row (RegionInspector reuses Swatch.svelte) gets the same inline block with no edit of its own."
  artifacts:
    - path: "src/lib/ui/Swatch.svelte"
      provides: "The inline colour block: a toggle with aria-expanded / aria-controls per row, one picker block rendered under the open row, keyed on the knob"
    - path: "e2e/tuning.e2e.ts"
      provides: "`openColourEditor` (the helper renamed), the RESET ALL title holding the block open across the reset, the Escape gesture on the toggle"
  key_links:
    - from: "src/lib/ui/Swatch.svelte"
      to: "src/lib/ui/ColourPicker.svelte"
      via: "<ColourPicker … selectedId={openFor}> inside the inline block, keyed on openFor"
      pattern: "<ColourPicker"
    - from: "e2e/radius.e2e.ts"
      to: "the inline block"
      via: "the picker's three circles measured with the block open on the workspace and the Sandbox"
      pattern: "colour-editor"
---

<objective>
Bench line 7's first half, D-08: `Edit color` opens down, seamlessly, below the swatch — not a popup.

Purpose: the user saw a modal box in the top-left corner; the PDF's inspector is one column that
grows.
Output: `Swatch.svelte` without a `<dialog>`; the toggle; the e2e gestures moved; and — pre-empting
13.1-06's deletion of the workspace column — `radius.e2e.ts:354`'s wait on `chosen-panel` swapped
for `tuning-region` (present today) so layer C of the radius gate never goes red (W-08 / B-01). Term
**+0 / +0; +0 / +0**.

**Execution order.** Second in the serial order (02 -> 04 -> 01 -> 03 -> …); the carried pair is
13.1-02's observed count line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-09-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-18-BATCH.md
@src/lib/ui/Swatch.svelte
@src/lib/ui/ColourPicker.svelte
@src/lib/ui/sandbox/RegionInspector.svelte
@src/lib/tune/inspector-copy.ts
@src/lib/ui/tune-ui.spec.ts
@src/lib/ui/radius-allowlist.ts
@e2e/tuning.e2e.ts
@e2e/radius.e2e.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's.
- **No `border-radius` above zero; the allowlist stays EMPTY; the six circles are
  `ColourPicker.svelte:840, :867, :882` and `Knob.svelte:730, :785, :807` — `ColourPicker.svelte` is
  NOT edited by this plan (`git diff --stat` on it must be empty), so the three lines cannot move.**
- **D-05's register; every new string ledgered in `13.1-COPY-NEW.md`.** This plan adds none —
  `Edit color` (the PDF's) and `Close` (F.8, D-23) are kept.
- **Every count a carried name plus a delta** from 13.1-02's observed count line (projected `94 /
  961 / 83 / 99`). Term **+0 / +0; +0 / +0**; `check-counts.mjs <files> <carried+0>`, never a literal
  the plan invents.
- **The sweep is not run.** `src/vendor/`, `library.ts`, the entries untouched.
- **Quick at `--maxWorkers=2`.** Playwright chunks **c3** (`tuning.e2e.ts`, `tuning-webkit.e2e.ts`)
  and **c5** (`artifacts`, `radius`, `skeleton`, `smoke`) on fresh detached servers; reds rerun alone
  and named.
</standing_rules>

<interfaces>
From `src/lib/ui/Swatch.svelte` (13-09) — the shape this plan replaces:
```svelte
<button class="edit" data-testid="edit-color" aria-describedby="{uid}-{knob.id}-label" onclick={(e) => open(knob, e)}>{EDIT_COLOR}</button>
<dialog bind:this={dialog} class="popover" data-testid="colour-popover" aria-labelledby={titleId} onclick={onBackdropClick} onclose={onClosed}>
  <div class="sheet"><div class="head"><p class="title type-micro" id={titleId}>{opened?.label}</p>
    <button class="close" data-testid="colour-popover-close" onclick={close}>{POPOVER_CLOSE}</button></div>
    {#key openFor}<ColourPicker {entry} {knobs} {held} {budget} {onchange} {onreset} {onhold} {onforecast} {onresult} selectedId={openFor} />{/key}
  </div>
</dialog>
```
Props: `entry, knobs, held, budget, onchange, onreset, onhold, onforecast?, onresult?`. The picker
"rendered once, closed, so the picker's rails are in the DOM" — that reason goes with the dialog.
From `inspector-copy.ts`: `EDIT_COLOR = "Edit color"`, `POPOVER_CLOSE = "Close"` (F.8).
`tune-ui.spec.ts` "every component that renders a control declares the 44px floor" (`:517-556`)
asserts `<dialog`, `showModal()`, the backdrop test and the focus return; `:672-677` counts the
Close button's one accent declaration in `Swatch.svelte`.
`e2e/tuning.e2e.ts`: `openColourPopover` (`:276`), `turnColourRail` (`:317`), the RESET ALL title
(`:438`) presses Escape and asserts `colour-popover` not open and `edit-color` focused (`:466-474`);
`knobIndices` (`:170`) reads `colour-rail-*` rails whenever they are in the DOM.
`e2e/radius.e2e.ts` (`:370-375`, `:401-405`) opens the popover and waits for its `open` attribute on
the workspace and on `/sandbox/?new`, then measures the circles; `:354` waits for `chosen-panel`
(the workspace column, which 13.1-06 deletes) before the workspace's measurement.
`RegionInspector.svelte` mounts `<Swatch>` with the region's colour as the one knob (13-16).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-04-01: The inline colour block</name>
  <files>src/lib/ui/Swatch.svelte, src/lib/tune/inspector-copy.ts, src/lib/ui/tune-ui.spec.ts</files>
  <behavior>
    - `tune-ui.spec.ts` (the popover clauses rewritten in place, +0): `Swatch.svelte` contains no
      `<dialog`, no `showModal`, no `::backdrop`, no `position: absolute` / `fixed` and no `z-index`;
      the toggle carries `aria-expanded={open}` and `aria-controls`; the block is `role="group"`
      labelled by the row's label id; the toggle's text is `EDIT_COLOR` closed and `POPOVER_CLOSE`
      open; `<ColourPicker` occurs once, inside an `{#if openFor === knob.id}` under that row, keyed;
      Escape handling names the toggle (`toggle?.focus()` after closing) and the file contains no
      `focusTrap`, `inert`, `aria-modal`; the `.edit` rule keeps `min-block-size: 44px` and
      `min-inline-size: 44px`; `border-radius` occurs 0 times; rendered with `svelte/server`, a
      Swatch with two knobs renders two toggles and no picker.
    - the accent census (`:618-677`): `Swatch.svelte`'s one accent declaration moves from `.close`
      to the toggle's open state or stays at one — assert one, whichever rule carries it, and say
      which.
  </behavior>
  <action>
    1. **`Swatch.svelte`**: delete the `<dialog>`, `open()`'s `showModal`, `close()`, `onBackdropClick`,
       `onClosed`, `trigger`, the `onDestroy`. State: `openFor: string | undefined`. Each row's button
       becomes the toggle: `aria-expanded={openFor === knob.id}`, `aria-controls="{uid}-{knob.id}-editor"`,
       text `openFor === knob.id ? POPOVER_CLOSE : EDIT_COLOR`, `onclick` toggling `openFor` (a click
       on another row's toggle moves the block to that row). Under the row's `.value` div, inside the
       same `.row`: `{#if openFor === knob.id}<div class="editor" id=… role="group"
       aria-labelledby="{uid}-{knob.id}-label" data-testid="colour-editor" data-knob={knob.id}
       onkeydown={onEditorKeydown}>{#key knob.id}<ColourPicker … selectedId={knob.id} />{/key}</div>{/if}`.
       `onEditorKeydown`: Escape → `openFor = undefined`, then `tick()` and focus the row's toggle
       (bind the toggles by knob id). CSS: `.editor` is a block in the flow with the panel's own
       surface, `padding-block: 12px`, a 1px `--color-divider` rule above; no shadow, no radius, no
       position. The `.row`'s `gap: 8px` holds. Rewrite the header: the popover paragraph becomes the
       inline paragraph (D-08, bench line 7); the "rendered once, closed" reason goes and says why
       (the rails are in the DOM only while open; the e2e that read them opens the block first).
    2. **`inspector-copy.ts`**: `POPOVER_CLOSE`'s comment says it is now the toggle's open-state word
       (F.8's `Close`, kept); the symbol is not renamed (three files read it).
    3. **`tune-ui.spec.ts`** as the behaviour says. `RegionInspector.svelte` is not edited — verify
       it still mounts `<Swatch>` with the same props and that `sandbox-ui.spec.ts` stays green.
    4. `git diff --stat -- src/lib/ui/ColourPicker.svelte` must be empty; `radius.spec.ts` A prints the
       six circles at the same six lines.
  </action>
  <verify>
    <automated>git diff --quiet -- src/lib/ui/ColourPicker.svelte src/lib/ui/Knob.svelte && npx vitest run --project server src/lib/ui/tune-ui.spec.ts src/lib/ui/sandbox-ui.spec.ts src/lib/ui/radius.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>No dialog anywhere in `Swatch.svelte`; the block opens under the row on the served page and the rows below move down; the picker file untouched; the specs green with the popover clauses rewritten.</done>
</task>

<task type="auto">
  <name>Task 13.1-04-02: The e2e gestures move to the toggle; chunks c3 and c5; STATE; commits</name>
  <files>e2e/tuning.e2e.ts, e2e/radius.e2e.ts, .planning/STATE.md</files>
  <action>
    1. **`tuning.e2e.ts`**: rename `openColourPopover` to `openColourEditor` — click
       `edit-color` first when no `colour-rail-r` rail is visible, wait for `colour-editor` visible
       (the same shape). In "RESET ALL puts every knob back…": take `home` AFTER opening the editor
       (`openColourEditor` before `knobIndices`) so the picker's rails are in both snapshots; keep
       `turnRail(0)`, `turnRail(1)`, `turnColourRail`; move the Escape gesture to the end of the
       title: press Escape with focus inside the editor, assert `colour-editor` count 0 and
       `edit-color` first is focused and reads `Edit color`; before that, assert the toggle reads
       `Close` while open. Every other title that turned the colour rail keeps working through the
       renamed helper. Update the header comment (`:160-169`) that explains the picker's testids.
    2. **`radius.e2e.ts`**: on the workspace and on `/sandbox/?new`, replace "wait for
       `colour-popover` `open`" with "click `edit-color`, wait for `colour-editor` visible" (`:370-375`,
       `:401-405`); the measurement then covers the picker's three circles inline. The `.thumb` /
       `.dot` / `.home` preconditions unchanged. **And `:354`**: the workspace's precondition `await
       expect(page.getByTestId("chosen-panel")).toBeVisible()` becomes `tuning-region` (the
       inspector's root, present today and after 13.1-06) — so the strict layer-C title stays green
       when the column goes (W-08; B-01's radius half is closed here, not at 06 or 07).
    2b. Because this plan edits `radius.spec.ts`'s subjects and `sandbox-ui.spec.ts` runs in its
       verify: both are green on this plan's tree alone; 03 (which edits both) runs after this plan
       in the serial order, so no transient is possible.
    3. `npm run build`; chunks **c3** and **c5** on fresh detached servers; reds rerun alone and named
       (13-20's transients in these chunks: none named; `browse-webkit:357` is c2's).
    4. Prettier by path; `check-counts.mjs 94 <carried+0>` (13.1-02's observed pair); `npm run check`,
       `npm run lint`.
    5. STATE by script against a copy (one line). Two commits by `--only`: task 01 (`feat(13.1-04):
       Edit color opens inline - …`) and task 02 (`test(13.1-04): the popover's gestures on the
       toggle …` with STATE and the SUMMARY). No trailer.
  </action>
  <verify>
    <automated>npx playwright test e2e/tuning.e2e.ts e2e/radius.e2e.ts --workers 3</automated>
  </verify>
  <done>c3 and c5 green with reruns named; the six circles measured square in both engines with the block open; `check-counts.mjs 94 <carried+0>` (13.1-02's observed pair) matches; `grep -n chosen-panel e2e/radius.e2e.ts` empty; two commits.</done>
</task>

</tasks>

<verification>
- `grep -rn "colour-popover\|showModal\|<dialog" src e2e` → nothing (the dialog is gone from the tree
  and from the suite); `grep -n chosen-panel e2e/radius.e2e.ts` → nothing.
- `radius.spec.ts` A: `6 circles (D-15): ColourPicker.svelte:840, :867, :882, Knob.svelte:730, :785,
  :807` unchanged; layer B after the build tolerating nothing.
- The Sandbox's Appearance row opens inline with no edit to `RegionInspector.svelte`.
</verification>

<success_criteria>
- Bench line 7's first half answered: the picker opens down under the swatch, seamlessly, the
  picker unchanged inside, focus simplified.
- Term `+0 / +0; +0 / +0` stated; c3 and c5 green.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-04-SUMMARY.md` with the
count line, `git diff --stat` on the two circle files (empty), the accent census's answer, every
rerun named, and the trailer conflict reported.
</output>
