---
phase: 13.1-bench-corrections-four
plan: 06
type: execute
wave: 3
depends_on: ["13.1-02", "13.1-05"]
files_modified:
  - src/lib/ui/DestinationZone.svelte
  - src/lib/ui/sandbox/SurfaceActions.svelte
  - src/routes/sandbox/[draftId]/+page.svelte
  - src/routes/playground/[id]/+page.svelte
  - src/lib/ui/TryOnDevice.svelte
  - src/lib/ui/InstallState.svelte
  - src/lib/ui/KeepOnDevice.svelte
  - src/lib/ui/PutBack.svelte
  - src/lib/ui/KeepConfirm.svelte
  - src/lib/ui/DeviceDetails.svelte
  - src/lib/ui/shell/device-clause.ts
  - src/lib/device/install-copy.ts
  - src/lib/device/session-copy.ts
  - src/lib/device/page-target.ts
  - src/lib/device/install.svelte.ts
  - src/lib/device/install-copy.spec.ts
  - src/lib/device/session-copy.spec.ts
  - src/lib/device/page-target.spec.ts
  - src/lib/device/install.spec.ts
  - src/lib/ui/device-ui.spec.ts
  - src/lib/ui/instrument.spec.ts
  - src/lib/ui/sandbox-ui.spec.ts
  - src/lib/config-shape.spec.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [SAFE-01, SAFE-03, SAFE-05, SAFE-09]

must_haves:
  truths:
    - "Under the workspace's surface there is no install column: no second Apply, no honesty block, no NEXT, no Put back, no Store, no Reset, no Disconnect (D-06, bench line 6, J.13)."
    - "The context bar's right zone on the workspace reads Target · Apply to ZONA · Store on ZONA, the same component the Sandbox renders; Store's confirmation renders in its place; a write's failure shows its detail and steps beneath the zone."
    - "Put back is nowhere on the site (D-07, the user's 'remove'): not in either zone, not under Device actions; the snapshot is still taken at connect before any write, and the store's putBack() still restores it for the /dev/install/ probe."
    - "Every sentence that offered Put back as the way back now names the way back that exists — Clear, to the firmware default — in D-05's register, ledgered; every 'Click X' step names a control that exists."
    - "Apply is the same write as before (install.tryOnDevice), the ACK gate, the retry bound and SAFE-07's settled-from-acknowledgements rule untouched."
  artifacts:
    - path: "src/lib/ui/DestinationZone.svelte"
      provides: "Target select · Apply to ZONA (described by the honesty line) · Store on ZONA / KeepConfirm; beneath: the store reason, the refusal, the switching / unverified line, the failure block"
    - path: "src/lib/device/install-copy.ts"
      provides: "`WRITE_CLICKS` four (Apply, Store, Clear, Target); Put back's label, progress label, page lines and steps retired by name; the way-back sentences rewritten"
    - path: "src/lib/ui/device-ui.spec.ts"
      provides: "The column's tests deleted by title; the zone's test; the survivors list"
  key_links:
    - from: "src/routes/playground/[id]/+page.svelte"
      to: "src/lib/ui/DestinationZone.svelte"
      via: "the destination snippet mounts <DestinationZone name config refusal /> when the module has reported a page"
      pattern: "<DestinationZone"
    - from: "src/lib/ui/DestinationZone.svelte"
      to: "src/lib/device/install.svelte.ts"
      via: "install.tryOnDevice(config, name); install.openConfirm(); install.switchPage(page)"
      pattern: "install\\.tryOnDevice\\("
---

<objective>
Bench line 6, D-06 and D-07: the install column under the workspace's surface goes (J.13, owed since
the gate); the context bar's right zone becomes one shared component — Target, Apply to ZONA, Store
on ZONA — for the workspace and the Sandbox; and Put back leaves the interface by the user's word,
its snapshot machinery staying.

Purpose: the workspace has carried two Apply controls since 13-12 and a block the user calls
unnecessary; the Sandbox already has the shape the user drew.
Output: `DestinationZone.svelte`; four components deleted by name; the strings rewritten and
ledgered; the unit specs re-pinned. **This plan runs the quick suite only** — the e2e titles that
read the column are named here and re-aimed by 13.1-07 in the next wave; between the two, chunks c1,
c4 and c5 carry the reds named below, by design and by title. Term **+0 / −3; e2e +0 / +0 here**
(13.1-07 carries the e2e term).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@.planning/phases/13.1-bench-corrections-four/13.1-02-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-05-SUMMARY.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-11-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-12-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-17-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-18-BATCH.md
@.planning/phases/13-gui-overhaul/deferred-items.md
@src/lib/ui/sandbox/SurfaceActions.svelte
@src/routes/sandbox/[draftId]/+page.svelte
@src/routes/playground/[id]/+page.svelte
@src/lib/ui/TryOnDevice.svelte
@src/lib/ui/InstallState.svelte
@src/lib/ui/KeepOnDevice.svelte
@src/lib/ui/PutBack.svelte
@src/lib/ui/KeepConfirm.svelte
@src/lib/ui/FailureBlock.svelte
@src/lib/ui/shell/device-clause.ts
@src/lib/device/install-copy.ts
@src/lib/device/session-copy.ts
@src/lib/device/page-target.ts
@src/lib/device/install.svelte.ts
@src/lib/ui/device-ui.spec.ts
@src/lib/device/install-copy.spec.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's; SAFE-03 / SAFE-04 / SAFE-09's amendments are the
  gate's, by the user's word.
- **No `border-radius` above zero; the allowlist stays EMPTY; the six circles unmoved; `Knob.svelte`
  and `ColourPicker.svelte` not edited.**
- **D-05's register; every new, rewritten or retired string ledgered in `13.1-COPY-NEW.md`** with the
  D-23 batch row it supersedes named.
- **Every count a carried name plus a delta** from the block 13.1-05 left. Term **+0 / −3**; prove
  the count gate against one negative before the first deletion (13-01's precedent).
- **The sweep is not run.** `src/vendor/`, `library.ts`, the entries untouched. `config-shape.spec.ts`
  test 13 (the chunk guard) walks the workspace route — the zone must add no static specifier naming
  the vendored tree, the protocol package or the compile surface.
- **Quick at `--maxWorkers=2`.** No Playwright chunk in this plan (see the objective); the reds it
  leaves are named by title in the SUMMARY and closed by 13.1-07.
</standing_rules>

<interfaces>
From `src/lib/ui/sandbox/SurfaceActions.svelte` (13-17) — the destination half to extract (`:96-256`):
```ts
props: { zone: "destination" | "share"; name: string; config?: ConfigStrings; refusal?: string; exported?: string; onexport?: () => void }
const reportedPage = $derived(session.identity?.activePage);
const targetPages = $derived(install.pages.length > 0 ? install.pages : reportedPage === undefined ? [] : [reportedPage]);
const targetValue = $derived(install.pageRequested ?? reportedPage);
const applyDisabled = $derived(!install.applyReady || refusal !== undefined || config === undefined || writing || install.phase === "snapshotting");
const storeReason = $derived(install.keepReason(capable)); // held through a write
function apply() { void install.tryOnDevice(config, name); }
function store() { install.openConfirm(); }           // KeepConfirm renders in the button's place
function closeConfirm() { install.dismissConfirm(); focus back to the store button }
// markup: destination-page select · apply-to-zona · store-on-zona | store-confirm · put-back (GOES) · store-on-zona-line · apply-refusal · destination-line
```
From `src/lib/ui/TryOnDevice.svelte` — what the honesty line derives from (port the derivation, delete the file):
`HONESTY_NO_SESSION` (no session), `honestyReady(page)` (ready), `HONESTY_SNAPSHOTTING` (snapshotting),
`HONESTY_INCAPABLE` (the two capability phases); it renders `<InstallState>` beneath.
From `src/lib/ui/InstallState.svelte`: the seven failure-shaped phases render `<FailureBlock
block={…}>` from `install-copy`'s builders (`unconfirmedBlock`, `keptMismatchBlock`, `partialBlock`,
`nothingLandedBlock`, `restoredUnconfirmedBlock`, `snapshotFailedBlock`, `lostBlock`); port that
mapping into the zone; the six success bodies retire.
From `src/lib/ui/shell/device-clause.ts`: `deviceClause(phase, page)` maps the fifteen phases onto
the captions the bar's status zone shows — unchanged; `restored`'s clause reads `restoredCaption`.
From `install-copy.ts` — the strings that name Put back (`grep -n "ut back\|PUT_BACK"`): `PUT_BACK_LABEL`
`:212`, `puttingBackLabel` `:215`, `WRITE_CLICKS` `:240`, `HONESTY_SNAPSHOTTING` `:257`,
`snapshottingBody` `:269`, `identifiedBody` `:275`, `restoredCaption` `:292`, `clearedBody` `:316`,
`RESTORED_UNCONFIRMED_TITLE` `:325`, `SNAPSHOT_FAILED_TITLE` `:329`, `STEP_OR_PUT_BACK` `:332` (used
`:341`, `:351`, `:400`), `restoredUnconfirmedBlock`'s step `:360`, `nothingLandedBlock("put-back")`
`:378-384`, `lostBlock`'s third step `:422`, `CONFIRM_WAY_BACK` `:450`, `PUT_BACK_NEEDS_ZONA` `:479`
(also `CLEAR_REASONS["no-session"]`), `KEEP_REASONS["after-partial"]` ("or put your own back").
`session-copy.ts:360, :367` (the snapshot's two persistence lines, "so it can be put back").
`page-target.ts`: `putBackPageLine`, `putBackPageLineAfterKeep` (`:392-410`).
`device-ui.spec.ts` titles: `:267` (the seven listed), `:622` (the three install leaves), `:789`
(every install sentence from the copy modules), `:829` (the reserved cells…), `:1180` (the header
locks under a write), and — already handled — `:1297` rewritten and `:1398` deleted at 13.1-05.
`instrument.spec.ts` lists the device components by file. `install.spec.ts`'s put-back tests drive
`install.putBack()` on the fake — they STAY (the machinery is proved there and on the probe).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-06-01: `DestinationZone.svelte`, shared by the Sandbox and the workspace; the column removed; four components deleted</name>
  <files>src/lib/ui/DestinationZone.svelte, src/lib/ui/sandbox/SurfaceActions.svelte, src/routes/sandbox/[draftId]/+page.svelte, src/routes/playground/[id]/+page.svelte, src/lib/ui/TryOnDevice.svelte, src/lib/ui/InstallState.svelte, src/lib/ui/KeepOnDevice.svelte, src/lib/ui/PutBack.svelte, src/lib/ui/KeepConfirm.svelte, src/lib/ui/DeviceDetails.svelte, src/lib/ui/device-ui.spec.ts, src/lib/ui/instrument.spec.ts, src/lib/ui/sandbox-ui.spec.ts, src/lib/config-shape.spec.ts</files>
  <behavior>
    - `device-ui.spec.ts` (new, +1) "the destination zone: one component for both routes — Target,
      Apply described by the honesty line, Store or its confirmation in the same place, the reason,
      the refusal, the switching or unverified line, a failure's block; no Put back; a group that is
      not a dialog": source-scan `DestinationZone.svelte` for the four test ids, no `put-back`, no
      `role="dialog"`, `aria-describedby` on `apply-to-zona`, `<FailureBlock`, the 44px floor on
      `.destination-select`, `.destination-apply`, `.destination-store`, `border-radius` 0; the
      workspace route and the Sandbox route each mount `<DestinationZone` once and mount no
      `<TryOnDevice`, `<PutBack`, `<KeepOnDevice`, `<InstallState`; `SurfaceActions.svelte` has no
      destination half (its `zone` prop is gone or share-only); rendered with `svelte/server` under
      a hand-set mirror: `phase: "nothing-landed"` renders the block's title and two steps and the
      steps name `Apply to ZONA` and `Clear`.
    - `device-ui.spec.ts` "the seven are listed and on disk…" re-pinned to the survivors (Clear,
      KeepConfirm, DeviceDetails, DeviceSlot, DeviceActions, SessionAnnouncer, FailureBlock … —
      whatever the list holds after the four go; name the count).
    - **Deleted by title**: "the three install leaves: no compiler specifier, the 44px floor, the twin
      cells, and a group that is not a dialog"; "the reserved cells are the measured arithmetic, the
      honesty slot holds five twins and no never-writes literal, SAFE_NOTE is not a twin, KEEP ON
      DEVICE is borderless, and the install row is a column". **Re-aimed**: "every install sentence
      on screen comes from the copy modules" (the zone's markup authors no sentence); "the header
      locks under a write, says where the copy is, and the announcer is untouched" (the lock is read
      off the zone's controls now).
    - `instrument.spec.ts`: the rows for the four deleted files go. `config-shape.spec.ts` 13 green
      (no new specifier on the first paint).
    - `sandbox-ui.spec.ts`: whatever asserted `SurfaceActions`'s destination half (grep `zone=` and
      `destination` in it) re-aimed at `DestinationZone`.
  </behavior>
  <action>
    1. **Prove the count gate against one negative** before deleting anything: a scratch copy of a
       spec with one test removed, `check-counts.mjs <files> <tests-1>` matches, restore from the copy
       with sha256 asserted either side.
    2. **`src/lib/ui/DestinationZone.svelte`** (new): props `{ name: string; config?: ConfigStrings;
       refusal?: string }`. Move `SurfaceActions.svelte`'s destination half here whole — the
       `reportedPage` / `targetPages` / `targetValue` derivations, `onTargetChange` (already
       `install.switchPage`), `apply()`, `store()`, `closeConfirm()` with the focus return to the
       store button (KeepConfirm's one focus rule, kept), the markup and the style — WITHOUT the
       `.put-back` div and its import. Add: (a) `apply-to-zona`'s `aria-describedby` pointing at an
       sr-only `<span id=… data-testid="apply-honesty">` whose text is `TryOnDevice.svelte`'s
       derivation — `HONESTY_INCAPABLE` on the two capability phases, `HONESTY_NO_SESSION` with no
       session, `HONESTY_SNAPSHOTTING` while snapshotting, `honestyReady(page)` when ready — and the
       refusal id appended when a refusal is set (the same `aria-describedby` can name both); (b)
       beneath the lines, `{#if failure}<FailureBlock block={failure} />{/if}` where `failure` is
       `InstallState.svelte`'s phase-to-builder mapping for the seven failure-shaped phases (ported
       verbatim, the `label` argument now `APPLY_LABEL`), and nothing for the success phases (the
       bar's device clause carries their captions). Header comment: what this is (13.1 D-06), that it
       is the ONE component for both routes, that nothing here is a second write path (13-17's
       paragraph, kept), that Put back is gone by the user's word (D-07) and where the snapshot
       machinery still lives, and that the honesty line is the control's description rather than a
       block.
    3. **`SurfaceActions.svelte`**: delete the destination half, the `zone` prop's destination arm
       and the imports it alone used; it is the share control. **The Sandbox route**: mount
       `<DestinationZone name config refusal />` in its destination snippet where `<SurfaceActions
       zone="destination" …>` was; the share mount unchanged.
    4. **The workspace route**: the destination snippet becomes `<DestinationZone name={listed?.name
       ?? ""} config={configStrings} refusal={overBudgetReason} />`; delete the route's own
       `destination-row` markup, `targetSelect`, `onTargetChange`, `applyDisabled`, `applyToZona`,
       `targetId`; delete the column — the `<section class="panel">` block, `panelRoot`, `tryOn`,
       `keep`, `wasOpen` / `returnFocus` (the confirm's focus rule now lives in the zone), the
       `TryOnDevice` / `KeepOnDevice` / `KeepConfirm` / `PutBack` imports, the `.panel`, `.rule`,
       `.caption`, `.install-row` and the `.destination-*` styles (moved into the zone), and
       `onDestroy`'s `tryOn?.release()` — check what `release()` did (the try-on's engine handle) and
       keep the store call it made if the store needs it on leave. `onWindowKeyDown`'s Escape rule
       stays (it reads `install.confirmOpen`). Rewrite the header's install-column paragraph.
    5. **Delete** `TryOnDevice.svelte`, `InstallState.svelte`, `KeepOnDevice.svelte`, `PutBack.svelte`
       (`git rm`). `KeepConfirm.svelte`'s header names PUT BACK as "the cell above it" — reword.
       `DeviceDetails.svelte`'s comments naming the column — reword. `npm run check` must find no
       dangling import.
    6. The specs as the behaviours say.
  </action>
  <verify>
    <automated>npm run check && npx vitest run --project server src/lib/ui/device-ui.spec.ts src/lib/ui/instrument.spec.ts src/lib/ui/sandbox-ui.spec.ts src/lib/config-shape.spec.ts src/lib/ui/radius.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>Both routes render the one zone; the workspace's surface has nothing beneath it but the matrix line, the readout, the quiet line, the monitor and the fidelity line; the four files are gone; the specs green with the deletions named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-06-02: Put back's strings retired and the way-back sentences rewritten; `WRITE_CLICKS` four</name>
  <files>src/lib/device/install-copy.ts, src/lib/device/session-copy.ts, src/lib/device/page-target.ts, src/lib/device/install.svelte.ts, src/lib/ui/shell/device-clause.ts, src/lib/device/install-copy.spec.ts, src/lib/device/session-copy.spec.ts, src/lib/device/page-target.spec.ts, src/lib/device/install.spec.ts, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <behavior>
    - `install-copy.spec.ts` "the closed sets…": `WRITE_CLICKS` equals `[TRY_ON_LABEL, KEEP_LABEL,
      CLEAR_LABEL, TARGET_CLICK]`, length 4, distinct 4; `PUT_BACK_LABEL`, `puttingBackLabel`,
      `STEP_OR_PUT_BACK` not exported; the `Click X` walk (`:779-801`) finds every step naming a
      member of `WRITE_CLICKS`; no exported string or builder sample contains `Put back` / `put back`
      except the two phase captions the probe still reaches (`restoredCaption`,
      `RESTORED_UNCONFIRMED_TITLE`), which are listed by name as the probe's.
    - `install-copy.spec.ts` "holds every string against the documents…" re-pinned: the D-23 rows
      superseded by 13.1 D-07 (I.3.2-I.3.5, I.4.3, I.4.6, I.4.7, I.4.17, I.5.1-I.5.3, I.5.5-I.5.8,
      I.5.10, I.5.12, I.6.2 `after-partial`) hold the NEW words, each commented with the row and the
      decision.
    - `page-target.spec.ts`: the `putBackPageLine` tests deleted by name (−1 if they are their own
      title; otherwise the clauses).
    - `session-copy.spec.ts`: the two snapshot persistence lines re-pinned.
    - `install.spec.ts`: the put-back tests STAY green (the store action is untouched); the
      `nothingLandedBlock("put-back")` branch still builds for the probe.
  </behavior>
  <action>
    1. **`install-copy.ts`**, in D-05's register, each ledgered with its old row:
       - retire `PUT_BACK_LABEL`, `puttingBackLabel`, `STEP_OR_PUT_BACK`; `PUT_BACK_NEEDS_ZONA`
         becomes `NEEDS_ZONA` (the same words; `CLEAR_REASONS["no-session"]` reads it);
       - `WRITE_CLICKS` = `[TRY_ON_LABEL, KEEP_LABEL, CLEAR_LABEL, TARGET_CLICK]`, the comment
         "four since 13.1-06 — Put back removed by the user's word (D-07)";
       - `HONESTY_SNAPSHOTTING` → `Reading what your ZONA holds first.`;
       - `snapshottingBody(page)` → the same sentence without `— so it can be put back` (the copy
         is still taken; say `before anything is written`);
       - `identifiedBody(fw, page)` → `Firmware …, on Page N. A copy of the page is saved here — its
         touch Setup and Timer and its own init, timer and utility scripts.`;
       - `clearedBody(page)` → `Page N is running the firmware’s own default. Your browser draft is
         untouched.`;
       - a new `stepOrClear(page)` = `Or click Clear to return Page N to its firmware default` in
         the three blocks that used `STEP_OR_PUT_BACK` (the builders take `page` already);
       - `restoredUnconfirmedBlock`'s step and `nothingLandedBlock("put-back")`'s step keep naming
         the probe's action in the probe's words (`Send the restore again`) — these two blocks are
         reachable from `/dev/install/` only; say so in their comments;
       - `lostBlock`'s third step → `Then click Clear if you want the firmware default back`;
       - `CONFIRM_WAY_BACK` → `Clear still returns the page to its firmware default.`;
       - `KEEP_REASONS["after-partial"]` → `Not after a partial apply. Apply to ZONA again, or click
         Clear.`;
       - `restoredCaption(page)` → `Page N put back` and `RESTORED_UNCONFIRMED_TITLE` → `Put back in
         memory, not yet stored` kept as the phase's own words (the machinery's, reachable from the
         probe; the bar's clause would read them only there);
       - `SNAPSHOT_FAILED_TITLE` → `Nothing copied yet` and `snapshotFailedBlock`'s detail
         unchanged in substance (`won’t write over something it hasn’t copied`).
       The header's retired list gains every retired symbol with the date and D-07.
    2. **`page-target.ts`**: retire `putBackPageLine` and `putBackPageLineAfterKeep`; the header's
       "PUT BACK NAMES the page" paragraph becomes a sentence saying the snapshot is per page and the
       restore is the probe's since 13.1-06.
    3. **`session-copy.ts:360, :367`**: `… so it can be put back even from a new tab.` → `… and kept
       on record even from a new tab.`; the tab-only twin the same way.
    4. **`install.svelte.ts`**: the header's every "PUT BACK" clause reworded to "the restore
       (`putBack()`, the probe's since 13.1-06)"; no code moves; `putBack()` stays with a comment
       naming its one caller.
    5. **`device-clause.ts`**: the `restored` and `restoring` rows read the reworded captions; note
       they are reachable from the probe only.
    6. The specs as the behaviours say; **`13.1-COPY-NEW.md`**: one row per string above, old and
       new, the batch row superseded, D-07 cited.
  </action>
  <verify>
    <automated>npx vitest run --project server src/lib/device --maxWorkers=2 && grep -rn "Put back\|put back\|PUT BACK\|PUT_BACK" src/lib src/routes --include=*.ts --include=*.svelte | grep -v "spec.ts\|dev/install\|// \|\* " ; true</automated>
  </verify>
  <done>Four write clicks pinned; no visitor-facing sentence names Put back; every step names a control that exists; the device specs green; the ledger carries every change with its superseded row.</done>
</task>

<task type="auto">
  <name>Task 13.1-06-03: The quick suite at the new count; the e2e reds named; STATE; commits</name>
  <files>.planning/STATE.md</files>
  <action>
    1. `npm run test:quick 2>&1 | node scripts/check-counts.mjs 94 <carried-3>` (the carried total is
       13.1-05's observed pair; state it); `npm run check` (provenance: 657 − 4 + 1 − 1 = 653 expected;
       report the observed), `npm run lint`, prettier by path.
    2. `npm run build` so the tree on disk is buildable; **do not run a chunk** — instead list, by
       file and title, every e2e title that reads a removed test id (`try-on-device`, `chosen-panel`,
       `next-caption`, `put-back`, `put-back-line`, `put-back-page-line`, `keep-on-device`,
       `keep-on-device-line`, `install-state`, `honesty`): `install.e2e.ts` `:1434`, `:1580`, `:1721`,
       `:1842`, `:1977`, `:2090`, `:2232`, `:2397`, `:2489`; `session.e2e.ts` `:1307`, `:1423`;
       `first-experience.e2e.ts` `:269`; `sandbox.e2e.ts` `:362`; `browse.e2e.ts` `:1587`;
       `radius.e2e.ts` `:312`; `tuning.e2e.ts` `:597`, `:648`, `:875`, `:943`;
       `tuning-webkit.e2e.ts` `:318`, `:532` (grep the tree to confirm the list; the line numbers are
       13-20's). Write the list in the SUMMARY under "Red until 13.1-07 / 13.1-08, by design" —
       the tuning ones are 13.1-08's.
    3. STATE by script against a copy: one line naming the term, the deletions and the reds owed.
    4. Three commits by `--only`: task 01 (`feat(13.1-06): one destination zone for both routes; the
       column gone …`), task 02 (`feat(13.1-06): Put back retired by the user's word - …`), task 03
       (STATE and the SUMMARY). No trailer.
  </action>
  <verify>
    <automated>npm run test:quick 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Quick suite green at the carried total minus three; the build clean; the owed e2e titles listed by title; three commits; ROADMAP byte-unchanged.</done>
</task>

</tasks>

<verification>
- `ls src/lib/ui/TryOnDevice.svelte src/lib/ui/InstallState.svelte src/lib/ui/KeepOnDevice.svelte
  src/lib/ui/PutBack.svelte` → none exist; `git log --diff-filter=D --name-only -1` names them.
- `grep -rn "data-testid=\"put-back\"\|try-on-device\|chosen-panel" src` → nothing.
- `install.spec.ts` test 4 (zero writes by class) and the put-back tests green and unedited in
  their assertions; `sequence.spec.ts` untouched.
- The allowlist empty; the six circles' files not in `git diff --stat`; `config-shape.spec.ts` 13 green.
</verification>

<success_criteria>
- Bench line 6 answered in the tree: no column; Target · Apply to ZONA · Store on ZONA in the bar;
  Put back gone; the snapshot machinery whole and proved on the fake and the probe.
- Every string rewritten in the register and ledgered with its superseded row.
- Term stated; the e2e reds owed to 13.1-07 and 13.1-08 named by title.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-06-SUMMARY.md` with the
count line, the four deletions and the deleted / re-aimed tests by title, every rewritten string with
its old form, the owed e2e titles, and the trailer conflict reported.
</output>
