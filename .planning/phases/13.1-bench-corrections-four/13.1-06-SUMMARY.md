---
phase: 13.1-bench-corrections-four
plan: 06
subsystem: destination-zone
tags:
  [
    destination-zone,
    install-column,
    put-back,
    apply-to-zona,
    store-on-zona,
    failure-block,
    still-writing,
    write-clicks,
    d-05-register,
    bench-2026-09-12,
    d-06,
    d-07,
  ]
requires:
  - "13.1-CONTEXT D-06 (bench line 6, verbatim): the install column under the workspace's surface removed; the bar's right zone reads Target · Apply to ZONA · Store on ZONA on both routes"
  - "13.1-CONTEXT D-07 (the user's 'remove'): Put back leaves the interface everywhere; the snapshot machinery and the store's putBack() stay, the probe's"
  - "13.1-02: install.switchPage on the select's change; TARGET_CLICK as the fifth write click"
  - "13.1-05: Clear in the header; CLEAR_LABEL = `Clear`; the column's Clear already gone; the carried count line 94 / 964 (+1 todo) / 85 / 101 / check 656"
  - "13-17: SurfaceActions.svelte's destination half (the shape the user drew); 13-12: the Target select, canApply(), the bar's zones; 13-11: InstallState's fifteen blocks, device-clause.ts, the anti-collapse test"
  - "13.1-PLAN-CHECK B-01 (sandbox.e2e.ts's put-back clicks), B-03 (term -1, no putBackPageLine test), W-10 (retire, do not rewrite, the InstallState-only strings), W-11 (STILL_WRITING_LINE homed), W-12 (install-failure)"
provides:
  - "src/lib/ui/DestinationZone.svelte: the ONE destination zone both routes mount - Target (the select's change is the switch), Apply to ZONA described by the honesty line (aria-describedby, the refusal's id appended over budget), Store on ZONA with KeepConfirm in its place; beneath: the store's reason, the refusal, the switching or unverified line, STILL_WRITING_LINE under still-writing, the seven failure-shaped phases' blocks under install-failure held through a write; no Put back"
  - "src/routes/playground/[id]/+page.svelte: no install column under the surface; the destination snippet mounts the zone; TryOnDevice's observeConfig effect kept in the route"
  - "src/routes/sandbox/[draftId]/+page.svelte: the destination snippet mounts the zone; SurfaceActions is the share control alone"
  - "src/routes/dev/tune/+page.svelte: the probe mounts the zone with the refusal where it mounted TryOnDevice"
  - "install-copy.ts: WRITE_CLICKS four; PUT_BACK_LABEL, puttingBackLabel, STEP_OR_PUT_BACK and the eight InstallState-only strings retired by name and dated; stepOrClear; every way-back sentence naming Clear; NEEDS_ZONA; the probe's two strings kept"
  - "page-target.ts: putBackPageLine and putBackPageLineAfterKeep retired by name; session-copy.ts: the two snapshot lines without a put-back; sandbox/copy.ts: STORE_LABEL retired"
  - "device-ui.spec.ts: the zone's rendered test; two column tests deleted by title; the anti-collapse, sentences, live-region and no-review tests re-aimed; INSTALL_LEAVES two"
  - "install-copy.spec.ts and session-copy.spec.ts: 13.1-COPY-NEW.md read as the fourth document; the seventeen ledgered strings pinned by name"
  - "e2e/sandbox.e2e.ts: the whole-loop title without Put back, green alone; install.e2e.ts loads again; the chosen-panel waits swapped for tuning-region in six files"
  - "13.1-COPY-NEW.md: the 13.1-06 section, one row per string, the old form struck, the batch row superseded"
affects: [13.1-07, 13.1-08]
tech-stack:
  added: []
  patterns:
    - "One component for the one control that moves hardware, mounted by every route that has a destination: the derivations, the handlers, the markup and the style travel together, and the route hands three props (name, config, refusal)"
    - "A phase's failure blocks as one switch case per phase in a $derived.by, so an anti-collapse test can read that four uncertain outcomes keep four builders without rendering fifteen states"
    - "A control's explanatory line as its aria-describedby target (an sr-only span) rather than a painted block with sizing twins, with a second id appended while a refusal is set"
    - "A phase's copy ledger read as a fourth authoring document by the copy specs, with the strings only it carries pinned by name so the ledger cannot become a blanket"
key-files:
  created:
    - src/lib/ui/DestinationZone.svelte
    - .planning/phases/13.1-bench-corrections-four/13.1-06-SUMMARY.md
  modified:
    - src/lib/ui/sandbox/SurfaceActions.svelte
    - src/routes/playground/[id]/+page.svelte
    - src/routes/sandbox/[draftId]/+page.svelte
    - src/routes/dev/tune/+page.svelte
    - src/lib/ui/KeepConfirm.svelte
    - src/lib/device/install-copy.ts
    - src/lib/device/session-copy.ts
    - src/lib/device/page-target.ts
    - src/lib/device/install.svelte.ts
    - src/lib/ui/shell/device-clause.ts
    - src/lib/sandbox/copy.ts
    - src/lib/ui/device-ui.spec.ts
    - src/lib/ui/instrument.spec.ts
    - src/lib/ui/tune-ui.spec.ts
    - src/lib/device/install.spec.ts
    - src/lib/device/install-copy.spec.ts
    - src/lib/device/session-copy.spec.ts
    - e2e/sandbox.e2e.ts
    - e2e/install.e2e.ts
    - e2e/tuning.e2e.ts
    - e2e/tuning-webkit.e2e.ts
    - e2e/session.e2e.ts
    - e2e/browse.e2e.ts
    - e2e/first-experience.e2e.ts
    - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
    - .planning/STATE.md
  deleted:
    - src/lib/ui/TryOnDevice.svelte
    - src/lib/ui/InstallState.svelte
    - src/lib/ui/KeepOnDevice.svelte
    - src/lib/ui/PutBack.svelte
key-decisions:
  - "The zone reads install-copy's KEEP_LABEL for Store on ZONA, not sandbox/copy's STORE_LABEL (the same three words): the button and the confirmation's affirmative read one constant, and STORE_LABEL is retired by name rather than left exported with no reader (D-12)"
  - "RESTORED_STORED_LINE is retired as the eighth InstallState-only string, beside the plan's seven: it lost its one reader with the same file, and D-12's rule (nothing dead is left exported) does not stop at a list"
  - "The workspace route keeps TryOnDevice's observeConfig effect: without it the store never sees the tuner's pair, Store on ZONA reads `knobs-moved` forever and the confirmation never closes on a knob move (Rule 2 - a correctness requirement the plan's file list did not name)"
  - "The tune probe mounts the zone with the refusal alone where it mounted TryOnDevice, so the over-budget state still has a screen for 13.1-07's e2e to read"
  - "install.e2e.ts's eleven retired imports are removed so the file loads: an ESM link error on a missing named export took every title in it down (the CLEAR titles and the zero-writes proof included) and Playwright's --list with them; the titles whose bodies still name them are red by design, per title, which is the honest hand-off"
  - "The chosen-panel visibility waits in six e2e files' helpers and single-hit titles are swapped for tuning-region (12 one-locator swaps, 13.1-04's precedent in radius.e2e.ts): a helper wait would have made every title in tuning, tuning-webkit and install red for a reason no title's name states"
  - "The lostBlock's step interpolates APPLY_LABEL (page-target's, the control as the zone renders it) where InstallState passed TRY_ON_LABEL - the same three words"
patterns-established:
  - "Where a deletion orphans a helper's wait in an e2e file the plan hands to the next plan, swap the one locator and say so, rather than hand over a file that is red for a reason no title names"
requirements-completed: []
requirements-qualified: [SAFE-01, SAFE-05, SAFE-08]
requirements-for-the-gate: [SAFE-03, SAFE-04, SAFE-09]
duration: 60min
completed: 2026-09-12
---

# Phase 13.1 Plan 06: One destination zone for both routes, the column gone, Put back removed Summary

**Bench line 6 answered in the tree: there is nothing under the workspace's surface but the matrix
line, the readout, the quiet line, the monitor and the fidelity line; the context bar's right zone
on the workspace and the Sandbox is one component - `Target` over the pages the module reports,
`Apply to ZONA`, `Store on ZONA` - with the store's reason, the over-budget refusal, the switching or
unverified line, the still-writing line and a write's failure block beneath it; Put back is on no
screen, by the user's word, and the snapshot before every write, the durable record and the
store's `putBack()` are untouched and proved on the fake and the probe.**

## Count line

Carried (13.1-05's observed line): **94 files / 964 tests (+1 todo) / 85 e2e titles / 101 runs /
check 656 / sweep `4 19` / allowlist 0 rows** at `0fdedec`. Re-measured before the first edit:
`vitest run --project server --maxWorkers=2` 94 / 964 (+1 todo); `grep -cE '^\s*test\('` summed
85 - no difference. The count gate proved against one negative before the first deletion: a
scratch copy of `session-copy.spec.ts` with one test skipped ran 94 / 963, `check-counts.mjs 94 963`
matched and `94 964` refused (`tests: observed 963, expected 964`); the file restored from the copy,
sha256 `42c10b6e…` either side.

This plan's term: **+0 files / -1 tests; e2e +0 titles / +0 runs; check -3** - two tests deleted by
title in `device-ui.spec.ts` ("the three install leaves: no compiler specifier, the 44px floor, the
twin cells, and a group that is not a dialog"; "the reserved cells are the measured arithmetic, the
honesty slot holds five twins and no never-writes literal, SAFE_NOTE is not a twin, KEEP ON DEVICE
is borderless, and the install row is a column"), one added (the zone's); four components deleted,
one added (check 656 - 4 + 1 = 653).

Observed after the commits: **94 / 963 (+1 todo)** (`check-counts.mjs 94 963` green twice at
`--maxWorkers=2`); **85 titles / 101 runs** by grep and `--list` (`Total: 101 tests in 15 files` -
the list was `0 tests in 0 files` until `install.e2e.ts`'s imports were fixed, deviation 6);
**svelte-check 653 / 0 / 0**; lint clean; sweep not run (`4 19` by declaration; no entry, knob list
or vendored file moved); radius layer A `32 declarations in 64 files scanned; 0 above zero remaining
in 0 allowlisted files (); 6 circles (D-15): src/lib/ui/ColourPicker.svelte:840, :867, :882,
src/lib/ui/Knob.svelte:730, :785, :807; 26 exempt` (67 files became 64: four deleted, one added);
layer B green on the fresh build. **The next plan (13.1-07) carries 94 / 963 (+1 todo) / 85 / 101;
check 653.**

## The untouchables: diff proof

`git diff --stat 0fdedec -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte src/vendor/
src/lib/catalog/library.ts src/lib/device/sequence.ts src/lib/device/snapshot.ts .planning/ROADMAP.md
.planning/REQUIREMENTS.md` is empty. `install.svelte.ts`'s diff is comment-only (asserted by
filtering the diff's `+`/`-` lines for anything that is not a comment: none). The six circles are
where 13.1-05 left them. CAT-04 stays `[ ]`.

## `DestinationZone.svelte`, as shipped on both routes

**The file.** `src/lib/ui/DestinationZone.svelte`, props `{ name: string; config?: ConfigStrings;
refusal?: string }`. `SurfaceActions.svelte`'s destination half moved here whole: `reportedPage`,
`targetPages` (the store's enumeration, else the reported page alone), `targetValue`
(`install.pageRequested ?? reportedPage`), `targetPending`, `writing`, `applyDisabled`
(`!install.applyReady || refusal !== undefined || config === undefined || writing ||
install.phase === "snapshotting"`), `capable`, `storeReason` = `install.keepReason(capable)` held
through a write, `storeDisabled`; `onTargetChange` awaiting `install.switchPage(value)` and snapping
the select back on false (13.1-02); `apply()` = `install.tryOnDevice(config, name)`; `store()` =
`install.openConfirm()`; `closeConfirm()` = `install.dismissConfirm()` then focus back to the store
button after a tick (KeepConfirm's one focus rule, kept). The markup and the style are
SurfaceActions' without the `.put-back` div and its import: `destination` (with `data-status`),
`destination-page`, `apply-to-zona`, `store-confirm` / `store-on-zona`, `store-on-zona-line`,
`apply-refusal`, `destination-line` (switching / unverified). The select 44 x 104 minimum, Apply
44 x 194 filled in the action colour, Store 44 x 44 bordered and never filled; every
`border-radius` is `0`.

**Added (a): the honesty line as Apply's description.** An sr-only `<span id="{uid}-honesty"
data-testid="apply-honesty">` beneath Apply carries TryOnDevice's derivation, ported:
`HONESTY_INCAPABLE` when the session is `unsupported` / `insecure`, `HONESTY_SNAPSHOTTING` while
the store snapshots, `honestyReady(page)` once connected, `HONESTY_NO_SESSION` otherwise; Apply's
`aria-describedby` is that id, with the refusal's id appended while a refusal is set
(`"{uid}-honesty {uid}-refusal"` - `install.spec.ts`'s `/aria-describedby="[^"]*-refusal"/` still
matches). Nothing is painted; the five sizing twins are gone with the column.

**Added (b): the failure block.** `failure` is a `$derived.by` switch over `shown` (`writing ?
heldPhase : install.phase`, InstallState's hold-through-a-write rule) with one case per
failure-shaped phase, InstallState's mapping verbatim: `unconfirmed` → `unconfirmedBlock(shownName,
page)`, `kept-mismatch` → `keptMismatchBlock(page)`, `partial` → `partialBlock(install.landed ?? …,
install.failed ?? "the Setup", page)`, `nothing-landed` → `nothingLandedBlock(install.action ===
"try" ? "try" : "put-back", page)`, `restored-unconfirmed` → `restoredUnconfirmedBlock(page)`,
`lost` → `lostBlock(install.leg === "store", APPLY_LABEL, page)`, `snapshot-failed` →
`snapshotFailedBlock(page)`; `default` → undefined (the six success phases: the bar's device clause
carries their captions). Rendered as `<FailureBlock block={failure} testid="install-failure" />`
inside a `.failure` wrapper (420px, start-aligned, `aria-busy` through a write) beneath the lines.
`page` is `install.snapshotPage ?? reportedPage ?? 0`.

**Added (c): the still-writing line.** `{#if install.slow}<p class="destination-line"
data-testid="still-writing">{STILL_WRITING_LINE}</p>{/if}` beneath the row - `install.slow` is the
flag InstallState read (the store's 2000 ms timer, Z-09). SAFE-08's visible half keeps a screen
(W-11); the live region still speaks `LIVE_STILL_WRITING` from the store.

**Where it mounts.** The workspace: `{#snippet destination()}<DestinationZone name={listed?.name ??
""} config={configStrings} refusal={overBudgetReason} />{/snippet}`, handed to the shell only when
the module has reported a page, as before. The Sandbox: `<DestinationZone {name}
config={landing?.config} {refusal} />` where `<SurfaceActions zone="destination" …>` was; the
share mount is `<SurfaceActions {exported} onexport={export_surface} />`. The tune probe
(`/dev/tune/`): `<DestinationZone name={shown.name} refusal={budgetReason} />` where
`<TryOnDevice entry={shown} {budgetReason} />` was, so the over-budget refusal still has a screen
(the plan did not list the probe; deviation 2). `device-ui.spec.ts`'s zone test counts one
`<DestinationZone` on each route and none of `<TryOnDevice`, `<PutBack`, `<KeepOnDevice`,
`<InstallState`, `<KeepConfirm`.

**Escape.** The workspace's window handler (Z-10) stays: while writing nothing; while the
confirmation is open, `install.dismissConfirm()`. KeepConfirm's own Escape (inside the block) calls
the zone's `closeConfirm`, which returns focus to Store on ZONA - the same two listeners the column
had.

## The column's removal

The workspace route: the destination snippet's own markup (`destination-row`, the select, Apply),
`targetSelect`, `targetId`, `onTargetChange`, `applyDisabled`, `applyToZona` and every
`.destination-*` rule are gone (they live in the zone); the `<section class="panel"
data-testid="chosen-panel">` with `<TryOnDevice>`, the hairline, `NEXT`, `<PutBack />`,
`<KeepConfirm>` / `<KeepOnDevice>` is gone with `panelRoot`, `tryOn`, `keep`, `wasOpen` /
`returnFocus`, `onDestroy`'s `tryOn?.release()` (it did nothing since 06-12: "Nothing to clear"),
the four imports, the `tick` import, and the `.panel`, `.rule`, `.caption`, `.install-row` rules;
the header's install-column paragraph rewritten. **Kept, moved into the route (deviation 1):**
TryOnDevice's effect `untrack(() => install.observeConfig(pair))` on `configStrings` - the one thing
the column did that the zone does not, and what arms Store on ZONA and closes the confirmation on
a knob move (Z-05, Z-21); the Sandbox route already does the same for its landing. Beneath the
fidelity line there is a comment and nothing else.

`TryOnDevice.svelte`, `InstallState.svelte`, `KeepOnDevice.svelte`, `PutBack.svelte`: `git rm`
(`git log --diff-filter=D --name-only -1 057329f` names the four). `SurfaceActions.svelte` is the
share control alone: the `zone` prop and its destination arm, the `name` prop (the share half never
read it), and the install / page-target / session / KeepConfirm / PutBack imports are gone; the
header says where the half went. `KeepConfirm.svelte`'s header names the zone's Store on ZONA where
it named the row's KEEP ON DEVICE and PUT BACK "the cell above it". `npm run check`: 653 / 0 / 0
after the deletions (the one error it found was `install.spec.ts`'s `zone` prop, re-aimed).

## Put back's removal everywhere, and the store's `putBack()` still pinned

`grep -rn 'data-testid="put-back"\|try-on-device\|chosen-panel' src` → nothing. `device-ui.spec.ts`'s
zone test walks every `.svelte` under `src/lib/ui/` and `src/routes/` outside `src/routes/dev/` for
`data-testid="put-back"` and `install.putBack(` and finds none; it counts `install.putBack(` once
in `src/routes/dev/install/+page.svelte` (the probe's button) and `async putBack()` once in the
store. `install.spec.ts`'s put-back titles ("…PUT BACK restores…", the store-after-keep title, the
Z-04 title) drive `store.putBack()` on the fake and are **green, unedited** in their assertions
(84 tests across `src/lib/device`); `sequence.spec.ts` untouched. The probe keeps its `Put back`
button (`install-put-back-click`) and its `putBackState()` readout.

`install.svelte.ts` is edited in comments only: a dated header paragraph ("PUT BACK HAS NO CONTROL
ON THE SITE SINCE 13.1-06") saying every PUT BACK below is the restore - `putBack()` - whose one
caller is the probe's button, and that no code moved; `putBack()`'s and `putBackState()`'s doc
comments say the same; the `InstallPhase` comment's "tells a visitor not to click PUT BACK" is
past tense. `snapshot.ts` is untouched (its comments still say PUT BACK; the file is on the
untouchables list).

## The seven retired strings, the eighth, and `STILL_WRITING_LINE`'s home

Retired by name, dated 2026-09-12 in `install-copy.ts`'s header under "THE INSTALL BLOCK'S SIX
SUCCESS BODIES ARE RETIRED BY NAME" (W-10; ledgered): `snapshottingBody` (I.4.6), `identifiedBody`
(I.4.7), `settledBody` (I.4.10), `keptBody` (I.4.11), `restoredBody` (I.4.14), `KEPT_PROOF_LINE`,
`clearedBody` (I.4.17) - and **`RESTORED_STORED_LINE`** (the line under the restore's body after a
store), the eighth, not in the plan's seven, retired on the same rule because InstallState was its
one reader (deviation 4). `install-copy.spec.ts` asserts each is not exported and is named in the
header. **`STILL_WRITING_LINE` (I.4.9) is exported and homed in the zone** under `still-writing`;
the zone test renders it on with `install.slow = true` and off without.

Put back's three: `PUT_BACK_LABEL` (I.3.2), `puttingBackLabel` (I.3.3), `STEP_OR_PUT_BACK` (I.5.8)
- retired by name and dated under "PUT BACK'S STRINGS ARE RETIRED BY NAME". `PUT_BACK_NEEDS_ZONA` is
`NEEDS_ZONA`, the same sentence, `CLEAR_REASONS["no-session"]` reading it (the spec's identity and
written-once assertions re-pinned). `WRITE_CLICKS` = `[TRY_ON_LABEL, KEEP_LABEL, CLEAR_LABEL,
TARGET_CLICK]`, four, distinct.

## Every rewritten string, old and new (D-05's register; the ledger's 13.1-06 section)

| Symbol | Old | New |
| --- | --- | --- |
| `stepOrClear(page)` (new; the second step of `keptMismatchBlock`, `unconfirmedBlock`, `partialBlock`) | `Or click Put back to restore what was there when you connected` (`STEP_OR_PUT_BACK`) | `Or click Clear to return Page 2 to its firmware default` |
| `lostBlock(…).steps[2]` | `Then click Put back to restore what was there when you connected` | `Then click Clear if you want the firmware default back` |
| `CONFIRM_WAY_BACK` | `Put back still restores what was there when you connected.` | `Clear still returns the page to its firmware default.` |
| `KEEP_REASONS["after-partial"]` | `Not after a partial apply. Apply to ZONA again, or put your own back.` | `Not after a partial apply. Apply to ZONA again, or click Clear.` |
| `nothingLandedBlock("try").detail` | `… so your own configuration is still playing and there’s nothing to put back.` | `… so your own configuration is still playing.` |
| `HONESTY_SNAPSHOTTING` | `Reading what your ZONA holds first, so anything you apply can be put back.` | `Reading what your ZONA holds first.` |
| `SNAPSHOT_FAILED_TITLE` | `Nothing to put back yet` | `Nothing copied yet` |
| `restoredUnconfirmedBlock(page).steps[0]` (the probe's) | `Click Put back again` | `Send the restore again` |
| `nothingLandedBlock("put-back").steps[0]` (the probe's) | `Click Put back to send it again` | `Send the restore again` |
| `restoredCaption(page)` (the probe's) | `Put back · Page 2` | `Page 2 put back` |
| `RESTORED_UNCONFIRMED_TITLE` (the probe's) | `Put back in memory, not yet stored` | unchanged |
| `SNAPSHOT_DURABLE_LINE` (`session-copy.ts`) | `… kept in this browser, so it can be put back even from a new tab.` | `… kept in this browser, and kept on record even from a new tab.` |
| `SNAPSHOT_SESSION_LINE` (`session-copy.ts`) | `… held until this tab closes, so it can be put back while you’re here.` | `… held until this tab closes, and kept on record while you’re here.` |
| `putBackPageLine`, `putBackPageLineAfterKeep` (`page-target.ts`) | `Puts Page 2 back to what it was playing when you connected.` / `… and stores it so it stays.` | retired by name, dated |
| `STORE_LABEL` (`sandbox/copy.ts`) | `Store on ZONA` | retired by name; the zone reads `KEEP_LABEL` (deviation 5) |

`install-copy.spec.ts` "the closed sets…" and the `Click X` walks (tests 4 and 6) hold: every step
that says click names a member of `WRITE_CLICKS` or the handed-in label (13 and 12 named). The
`saysPutBack` scan over every export and builder sample finds exactly `restoredCaption` and
`restoredUnconfirmedBlock.title` - the probe's two, by name. The re-casing excuses for `Put back`
and `Clear` are gone (nothing needs them: `settledBody`'s "clears it" retired with it); A-48's
three stems hold over twelve Clear strings (the four way-back sentences added). Contractions ≥ 8,
the ellipsis and the em dash still present.

**The ledger as a document.** `install-copy.spec.ts` and `session-copy.spec.ts` read
`13.1-COPY-NEW.md` as their fourth document (heading `### Retired and rewritten by 13.1-06 (D-06,
D-07)`, at least 10,000 bytes) beside the Bible, the batch and D-23; a string neither the Bible, the
batch nor the ledger carries is red by name, and the strings only the ledger carries are pinned as
exactly the seventeen D-07 rewrites (`install-copy`) and the two snapshot lines (`session-copy`), so
the ledger cannot become a blanket.

## The anti-collapse test, as moved

"fifteen phases are each accounted for, the four the spec has no row for are present by name, the
four uncertain phases keep four distinct bodies and four distinct clauses, and the bar takes the
draft and the device as two props" reads the zone where it read `InstallState.svelte`: the fifteen
from the store's union; the seven failure-shaped phases each with a `case "{phase}":` in the zone's
mapping (a name missing is a collapse, a name added is a success phase that grew a body); every
phase but `idle` and `writing` has either a case in the zone or a clause in the bar; the four
uncharted phases present by name - `restored-unconfirmed` and `snapshot-failed` as cases,
`restored` and `cleared` as `deviceClause` returning `restoredCaption(1)` / `clearedCaption(1)`;
the four uncertain phases through four different builders read off each case's `return`
(`unconfirmedBlock`, `keptMismatchBlock`, `partialBlock`, `nothingLandedBlock`), four distinct
titles, four distinct clauses; the bar's two props unchanged. The zone's `writing ? heldPhase :
install.phase`, `aria-busy={writing ? "true" : undefined}` and `testid="install-failure"` are read
by name.

## Deleted and re-aimed tests, by title

- **Deleted** (`device-ui.spec.ts`): "the three install leaves: no compiler specifier, the 44px
  floor, the twin cells, and a group that is not a dialog"; "the reserved cells are the measured
  arithmetic, the honesty slot holds five twins and no never-writes literal, SAFE_NOTE is not a twin,
  KEEP ON DEVICE is borderless, and the install row is a column".
- **Added** (`device-ui.spec.ts`): "the destination zone: one component for both routes - Target,
  Apply described by the honesty line, Store or its confirmation in the same place, the reason, the
  refusal, the switching or unverified line, a failure's block; no Put back; a group that is not a
  dialog" - the source scan (the ten test ids, `install-failure`, no `put-back`, no `<PutBack`, no
  `install.putBack(`, no dialog / modal / aria-label / interval, Apply described, the four
  constants read, the 44px floor and `border-radius: 0` per control class, every radius zero, the
  specifiers permitted, both routes mounting once and none of the five, the workspace without
  `chosen-panel` / `next-caption` / `install-row` / `panelRoot` and with `observeConfig`,
  SurfaceActions without a destination or a zone, the walk for Put back outside the probe) and
  the render under a hand-set mirror (ready; over budget; no session / snapshotting / incapable;
  `nothing-landed` with its title, detail and two `<li>` steps naming Apply to ZONA; kept-mismatch's
  step; still-writing on and off; the confirmation in Store's place as a group).
- **Re-aimed**: "every install sentence on screen comes from the copy modules" (the two leaves:
  the zone and KeepConfirm); "exactly one session live region on the site, and the destination
  zone is not it" (was "…and the panel is not it"; the zone is the subject); the anti-collapse test
  (above); "no destination review…" (the one zone carries `onTargetChange` and
  `install.switchPage(value)`; the route and SurfaceActions carry neither); `instrument.spec.ts`'s
  PILLED (TryOnDevice's `primary`, PutBack's `control` gone), QUIET (KeepOnDevice's `control`,
  TryOnDevice's `disconnect` gone) and index-form list; `tune-ui.spec.ts`'s error-ink carriers
  (`DestinationZone.svelte` for `sandbox/SurfaceActions.svelte`); `install.spec.ts`'s over-budget
  render (`render(DestinationZone, …)`).
- **Unchanged, as the plan said**: "the seven are listed and on disk…" (`DEVICE_COMPONENTS` names
  none of the four); "the header locks under a write…" (it reads DeviceDetails, DeviceSlot and the
  announcer, not the column - the plan's "the lock is read off the zone's controls now" describes
  no assertion in it); `page-target.spec.ts` (no `putBackPageLine` test, B-03); `sandbox-ui.spec.ts`
  (nothing in it asserted SurfaceActions' destination half - the plan's premise the tree does not
  support); `config-shape.spec.ts` 13 green (the zone adds no specifier naming the vendored tree,
  the protocol package or the compile surface).

## The e2e: `sandbox.e2e.ts` re-aimed and green alone; the reds owed to 13.1-07

**`sandbox.e2e.ts`** "the whole loop on a fake: a two-element surface exported as a file,
re-imported on My configs, opened, applied to the fake ZONA as five acknowledged writes with 255/4
among them, and the store refused" (was "…and put back"): the put-back half - `put-back` visible,
the click, RESTORED in the bar, the fake's ten writes and the owner's five back - deleted; kept:
export, import, open, apply (five acknowledged writes in SLOTS order, the restore heartbeat, the
surface's five in RAM, the module's own in flash), Store's confirmation opened and closed with
nothing stored; added: `put-back` count 0 twice, Apply's `aria-describedby` ending in `-honesty`,
five writes and no more, the flash's utility untouched. `restoredCaption` no longer imported. Run
alone on a fresh detached server (`e2e-chunks-1306.sh sb`): **4 passed**, no rerun; the loop's log
`3 heartbeat(s) to identify; 255/4 carried 706 characters; Setup 387, Timer 29`; the server stopped
(HTTP 000).

**Two one-locator classes fixed on the way** (deviation 6): `install.e2e.ts`'s import lists lose the
eleven retired names (`PUT_BACK_LABEL`, `puttingBackLabel`, `PUT_BACK_NEEDS_ZONA`, `KEPT_PROOF_LINE`,
`RESTORED_STORED_LINE`, `settledBody`, `keptBody`, `restoredBody`, `clearedBody`, `putBackPageLine`,
`putBackPageLineAfterKeep`) with a comment saying why - without it the file failed to LOAD (an ESM
`SyntaxError: The requested module '../src/lib/device/install-copy' does not provide an export named
'KEPT_PROOF_LINE'`), every title in it was red and `playwright test --list` reported `0 tests in 0
files`; the bodies still name them and those titles are red by design, below. And the
`chosen-panel` visibility waits were swapped for `tuning-region` (13.1-04's precedent) in
`tuning.e2e.ts` (`openPanel` and two titles), `tuning-webkit.e2e.ts` (`choose` and one title),
`install.e2e.ts` (the open helper and two titles), `session.e2e.ts` (two titles), `browse.e2e.ts`
(one), `first-experience.e2e.ts` (one) - twelve swaps; the four titles that read nothing else
("a shared link lands on the tuned configuration with the panel open", "a link that cannot be read
lands on the configuration as it ships", "a hand-authored configuration's pad runs on arrival and
keeps running once the tuner has published", "a session transition is announced once, and the
other two regions stay quiet") run green alone on a fresh server (4 passed) - they are NOT on the
red list. The `toHaveCount(1)` and `querySelector` reads of `chosen-panel` and tuning-webkit's
three-id measurement list are left for 13.1-07.

### Red until 13.1-07, by design - 17 titles, all 13.1-07's

Every one reads a removed test id (`try-on-device`, `connect-status`, `install-state`, `put-back`,
`put-back-line`, `put-back-page-line`, `keep-on-device`, `keep-on-device-line`, `next-caption`,
`#try-on-reason`) through the helpers `primary()`, `honesty()`, `installState()`,
`putBackControl()`, `stateShows()`, or a retired name; the subjects to re-aim at are the zone's
`apply-to-zona`, `apply-honesty`, `install-failure`, `still-writing`, `store-on-zona`,
`store-on-zona-line` and the bar's `status-device`. No chunk was run (the plan's rule); the list
is by grep (`reds.mjs` in the scratchpad), line numbers as of this commit.

| File | Line | Title |
| --- | --- | --- |
| `install.e2e.ts` | :557 | TRY ON DEVICE lands all five acknowledgements, PUT BACK restores, and the trace names every state |
| `install.e2e.ts` | :982 | a store that never confirms, on both the keep and the put-back legs, is said out loud |
| `install.e2e.ts` | :1447 | the panel writes on a click, says PLAYING NOW, and locks the header while it writes |
| `install.e2e.ts` | :1593 | the flash confirmation replaces the control, names what it replaces, and moves focus deliberately |
| `install.e2e.ts` | :1734 | after a keep, PUT BACK stores too and KEEP ON DEVICE waits for another try-on |
| `install.e2e.ts` | :1855 | the live region speaks once per outcome, and Escape is ignored while writing |
| `install.e2e.ts` | :1990 | @webkit CLEAR sends on the click with no confirmation, the panel reads FACTORY DEFAULT, and PUT BACK brings the visitor's own back @webkit |
| `install.e2e.ts` | :2120 | a knob turned before the click is the pair the module receives - LUMEN's depth, on the real panel |
| `install.e2e.ts` | :2262 | the destination menu lists the pages the module reports and sends nothing on open; a change sends the heartbeat then the switch… |
| `install.e2e.ts` | :2414 | @webkit the install controls are present and disabled with their reasons, and PUT BACK is absent @webkit |
| `install.e2e.ts` | :2506 | @webkit CLEAR is present and disabled where PUT BACK is absent, and the difference is one assertion @webkit |
| `first-experience.e2e.ts` | :414 | with no Web Serial the control is present, disabled, and says why |
| `session.e2e.ts` | :1307 | @webkit the header names the browsers that can install and offers nothing to press @webkit |
| `tuning.e2e.ts` | :917 | an over-budget configuration reddens its meter and disables the primary control |
| `tuning.e2e.ts` | :985 | the one-click back-off puts it back inside the budget |
| `tuning-webkit.e2e.ts` | :320 | the workspace opens with its panel and the rack never scrolls sideways @webkit |
| `tuning-webkit.e2e.ts` | :534 | a shared link lands with the knobs restored, and install is present but disabled @webkit |

Chunks c1 (`install`, `session`), c3 (`tuning`, `tuning-webkit`) and c4 (`first-experience`) carry
them; c2 and c5 carry none (`radius.e2e.ts` was closed by 13.1-04; `browse.e2e.ts`'s one read is
swapped and green). Two the plan did not list: `install.e2e.ts` :557 (clicks `put-back`) and :982
(the whole column through the helpers). The `@webkit` titles run in two projects, so the runs owed
are 21 of 101.

## The negative checks - every file restored from a scratch copy with sha256 either side

| # | What was planted | Where | Result |
| --- | --- | --- | --- |
| 0 | One test skipped (the count gate) | `session-copy.spec.ts` | 94 / 963; `check-counts 94 963` matched, `94 964` refused |
| A | HEAD's workspace route (the column, its own Target / Apply) under the new zone test | `+page.svelte` | 2 failed / 13 passed (`the workspace route mounts the zone once: expected +0 to be 1`; the no-review test) |
| B | `border-radius: 2px` on `.destination-store` | `DestinationZone.svelte` | the zone test and radius layer A red |
| C | `or put your own back` back in `KEEP_REASONS["after-partial"]` | `install-copy.ts` | `install-copy.spec.ts` red (the ledger-only list shrank by one) |

## The suites, as run

| What | Result |
| --- | --- |
| Baseline before the first edit: quick at `--maxWorkers=2`; grep | 94 / 964 (+1 todo); 85 |
| The count gate against one negative | 963 matched; 964 refused; restored |
| `npm run check` after the source deletions | 653 / 1 error (`install.spec.ts`'s `zone` prop - re-aimed) → 653 / 0 / 0 |
| The affected specs after task 1 (device-ui, instrument, sandbox-ui, config-shape, tune-ui, identity, shell, install.spec) | 97 passed in 8 files |
| Negative A, B | red; restored |
| Build 1 at task 1's tree; radius A / B | 32 in 64, 0 above zero, allowlist 0, six circles; B green |
| `npm run lint` after task 1 | one unused constant (`LAYOUT`, its reader deleted) - re-read by the Clear test; clean |
| `src/lib/device` after task 2 | 84 passed in 8 files |
| Negative C | red; restored |
| `npm run check`, `npm run lint` after task 2 | 653 / 0 / 0; clean |
| Quick run 1 | 94 / 963 (+1 todo), `check-counts 94 963` green |
| Build 2 at `f2eaa27`; `playwright test --list` | `0 tests in 0 files` (install.e2e.ts's link error) → 101 tests in 15 files after the import fix |
| `sandbox.e2e.ts` alone on a fresh detached server | 4 passed, no rerun |
| The four chosen-panel swap titles alone on a fresh server | 4 passed |
| Quick run 2 | 94 / 963 (+1 todo), `check-counts 94 963` green |

`test-results/` removed after each run; every server a detached wrangler on 4173 stopped through
PowerShell; the user's dev server on 5173 was not touched (it answered nothing when checked; not
started either).

## Deviations from the plan

### Auto-fixed

**1. [Rule 2 - Missing critical functionality] The tuner's pair no longer reached the store.**
Found during task 1: `TryOnDevice.svelte` carried the `$effect` that called
`install.observeConfig(config)` - the only caller on the workspace - and the plan deleted the file
without naming a new home. Without it `install.armed` never recomputes, Store on ZONA reads
`knobs-moved` forever and the confirmation never closes on a knob move. The effect is kept in the
workspace route (the Sandbox route already has its own), with the header saying why. `057329f`.

**2. [Rule 3 - Blocking] `src/routes/dev/tune/+page.svelte` mounted `<TryOnDevice>`.** Not in the
plan's file list; `npm run check` would have failed on the deleted import. The probe mounts
`<DestinationZone name={shown.name} refusal={budgetReason} />` in its place, so the over-budget
refusal keeps a screen for 13.1-07's `tuning.e2e.ts` titles. `057329f`.

**3. [Rule 3 - Blocking] Two specs the plan did not list read the moved half.** `install.spec.ts`
rendered `SurfaceActions` with `zone: "destination"` (a type error after the prop went) - re-aimed
at the zone; `tune-ui.spec.ts`'s error-ink carriers named `sandbox/SurfaceActions.svelte` for the
refusal's `--color-error-ink` - now `DestinationZone.svelte`. `057329f`.

**4. [D-12 - nothing dead left exported] `RESTORED_STORED_LINE` retired as the eighth.** The plan's
seven left it; it was InstallState's alone (grep). Ledgered. `f2eaa27`.

**5. [D-12] `sandbox/copy.ts`'s `STORE_LABEL` retired.** The zone reads install-copy's `KEEP_LABEL`
(the same three words) so the button and the confirmation's affirmative read one constant;
`STORE_LABEL` lost its one reader. Ledgered. `f2eaa27`.

**6. [Rule 3 - Blocking, by the orchestrator's "if re-aiming is a one-locator change, do it and
say so"] `install.e2e.ts`'s imports and six files' `chosen-panel` waits.** The retired names in the
import list made the file fail to load (all of it red, `--list` empty) - removed, with a comment;
the `chosen-panel` visibility waits in helpers (`openPanel`, `choose`, install's open) and
single-hit titles would have made every title in three chunk files red for a reason no title
names - twelve swapped for `tuning-region`. The bodies' other reads are untouched and red by
design (the table above). Test commit.

**7. [Rule 1 - Bug in the plan's file list] `instrument.spec.ts`'s index-form list read the deleted
files by path.** Its scan 5 `code()`s KeepOnDevice, PutBack and TryOnDevice - ENOENT after the
deletions; the list is the three survivors plus the zone. `057329f`.

### Not done, and why

- `DeviceDetails.svelte` is untouched: the plan says its comments name the column; they name Y-11's
  "chosen panel" prose signal (`panelOwnsProse`), which is 13-11's and not the column's. Nothing to
  reword.
- `sandbox-ui.spec.ts` is untouched: nothing in it asserted `SurfaceActions`' destination half
  (grep `zone=` / `destination`: none).
- `page-target.spec.ts` is untouched (B-03).
- No chunk run beyond `sandbox.e2e.ts` alone and the four swap titles; c1, c3, c4 are 13.1-07's.
- `install.svelte.ts`'s fourteen "PUT BACK" comment mentions are covered by one dated header
  paragraph plus the reworded `putBack()`, `putBackState()` and `InstallPhase` comments, not
  fourteen individual rewrites: the paragraph says every PUT BACK below means the restore.

## What the plan asserts that the tree does not support

1. **"`DeviceDetails.svelte`'s comments naming the column - reword"** - they name the chosen panel's
   prose signal (Y-11), not the column.
2. **"`sandbox-ui.spec.ts`: whatever asserted `SurfaceActions`'s destination half … re-aimed"** -
   nothing in it did.
3. **"the header locks under a write … (the lock is read off the zone's controls now)"** - that test
   reads DeviceDetails, DeviceSlot and the announcer; it never read the column and needs no re-aim.
4. **"`STILL_WRITING_LINE` … the one `InstallState`-only string that gets a home"** - true, but the
   InstallState-only list is eight, not seven: `RESTORED_STORED_LINE` (deviation 4).
5. **The e2e list of nine `install.e2e.ts` titles** - eleven read the column (:557 and :982 as
   well); and the line numbers (`:1434` …) are 13-20's - the tree's are in the table.
6. **"`sandbox.e2e.ts`'s whole-loop title … `:362` … `put-back` at `:532` and `:584`"** - `:496`,
   `:666`, `:718` at HEAD (13.1-03 added a title above it).
7. **"`onDestroy`'s `tryOn?.release()` - check what `release()` did … keep the store call it made"** -
   it made none ("Nothing to clear: this component holds no state of its own", 06-12); nothing to
   keep.
8. **The plan's `files_modified` omits** `src/routes/dev/tune/+page.svelte`, `src/lib/ui/tune-ui.spec.ts`,
   `src/lib/sandbox/copy.ts` and the five other e2e files (deviations 2, 3, 5, 6).
9. **"check … 657 − 1 (13.1-02) − 4 + 1 = 653"** - the carried was 656 (13.1-05's observed);
   656 − 4 + 1 = 653, the same answer by a different sum.

## Questions for the user (for the gate's bench row)

1. **The way back after an Apply** is the header's `Clear` (to the firmware default) or Grid Editor;
   the rewritten steps say "Or click Clear to return Page N to its firmware default" - confirm the
   wording (13.1-CONTEXT question 9).
2. **The failure block's place**: a write's detail and steps now render beneath the bar's right zone,
   right-aligned under the row, start-aligned inside the block, no wider than 420px. The PDF draws
   no failure state in the bar; is this where you want it?
3. **The honesty line is no longer painted** - Apply's accessible description carries it. Should
   "Applies this to Page N in about a second. It stays until power-off unless you store it." be
   visible somewhere (the bar has no room in its row), or is the description enough?
4. **`Page 2 put back`** is the bar's clause for the probe's restore; nothing on the site reaches it
   now. Keep the phase's words, or fold it into the requirement amendments at the gate?

## What 13.1-07 finds

- 94 / 963 (+1 todo); 85 / 101; check 653; radius A 32 in 64; the six circles unmoved.
- 17 e2e titles red by design (the table), 21 runs; `install.e2e.ts` loads; its helpers `primary()`,
  `honesty()`, `installState()`, `putBackControl()`, `stateShows()` still point at removed ids and
  are the first thing to re-aim (to `apply-to-zona`, `apply-honesty`, `install-failure` /
  `status-device`; `put-back` reads become "absent" assertions); the eighteen column-primary clicks
  are through `primary()` at `install.e2e.ts:1216` (`honesty()` :1213, `putBackControl()` :1217, `installState()` :1237, `stateShows()` :1331).
- The zone's ids: `destination`, `destination-page`, `apply-to-zona`, `apply-honesty` (sr-only;
  `toHaveText` works, `visibleLine` does not), `store-on-zona`, `store-confirm`,
  `store-on-zona-line`, `apply-refusal`, `destination-line`, `still-writing`, `install-failure`.
- `tuning-webkit.e2e.ts` :320's three-id measurement list still names `chosen-panel`; the
  `toHaveCount(1)` and `querySelector` reads of `chosen-panel` in `install.e2e.ts` (:1503, :1653,
  :1897, :1902, :2470) are untouched.
- The tune probe renders the zone with the refusal alone (no session): `apply-to-zona` disabled,
  `apply-refusal` carrying `tryOnBudgetReason`'s sentence.

## The trailer conflict

The harness asks for a `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` trailer on every
commit. The repository's standing rule (the plan's `<standing_rules>`, the orchestrator's brief)
forbids any Claude or Anthropic attribution and any co-author trailer. The repository's rule wins:
none of the three commits carries a trailer, and this paragraph is the report of the conflict.

## Commits

| Hash | Message (first clause) |
| --- | --- |
| `057329f` | `feat(13.1-06): one destination zone for both routes; the column gone - DestinationZone.svelte extracted whole from SurfaceActions' destination half …` |
| `f2eaa27` | `feat(13.1-06): Put back retired by the user's word - PUT_BACK_LABEL, puttingBackLabel and STEP_OR_PUT_BACK gone from install-copy.ts's exports …` |
| (test commit) | `test(13.1-06): the Sandbox loop without Put back …` with the e2e swaps, STATE and this SUMMARY |

## Self-Check: PASSED

`src/lib/ui/DestinationZone.svelte` and this SUMMARY exist; the four deleted files are gone and `git log --diff-filter=D --name-only -1 057329f` names them; commits `057329f` and `f2eaa27` are in the log; the test commit carries this file.
