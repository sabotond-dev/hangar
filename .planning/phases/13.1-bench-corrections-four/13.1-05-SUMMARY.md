---
phase: 13.1-bench-corrections-four
plan: 05
subsystem: header-clear-control
tags:
  [
    clear,
    header,
    connection-zone,
    firmware-default,
    write-click,
    container-query,
    device-slot,
    d-05-register,
    bench-2026-09-12,
    d-04,
  ]
requires:
  - "13.1-CONTEXT D-04 (bench line 4, verbatim): a persistent CLEAR control, top right, beside ZONA connected; one click (A-45 kept, I.6.5 declined); the label `Clear`; disabled with its reason"
  - "12-03: install.clearToDefault() and the store's clearReason(capable) over the closed ClearReason union; 10-13's Clear.svelte (A-45 to A-52) as the script this plan keeps line for line"
  - "13-05: Header.svelte's reserved connection slot (CONNECTION_SLOT 218 x 37) rendered from a snippet; 13-11: ConnectionControl.svelte / DeviceSlot.svelte as the box this control mirrors"
  - "13-18 (D-23): clearingLabel, clearLine and CLEAR_REASONS unmoved; pageName numbering from one"
  - "13.1-03: the carried count line 94 / 964 (+1 todo) / 85 / 101 / check 656; the chunk script's shape"
  - "13.1-PLAN-CHECK B-04 (carried+0, never a literal), W-09 (run c2 for the 375px header), I-01 (the :1398 deletion is this plan's, VALIDATION's +1 is wrong)"
provides:
  - "src/lib/ui/Clear.svelte: the header's Clear - DeviceSlot's box (44px both axes, 16px inline padding, the boundary token), the label span over an aria-hidden caption that reads the reason when disabled and nothing when live, the sr-only description the button points at (clearLine when live, the reason when not), aria-busy with clearingLabel through the leg, no confirmation, no radius, no transition, the caption under a container query at 480px of zone"
  - "src/lib/ui/shell/Header.svelte: the `clear` snippet rendered inside .connection before the connection snippet; the zone gap 12px, flex 1 1 var(--connection-w), container-type inline-size"
  - "src/routes/+layout.svelte: {#snippet clear()}<Clear />{/snippet} handed to the header on every page and both variants"
  - "src/lib/device/install-copy.ts: CLEAR_LABEL = \"Clear\"; section 9's `Reset active device page` retired by name and dated; WRITE_CLICKS unchanged at five"
  - "src/lib/ui/shell/ConnectionControl.svelte: the host no longer claims the zone's whole width (min-inline-size: 100% dropped)"
  - "src/lib/ui/DeviceSlot.svelte: the caption is nowrap (Y-10's fixed 14px line box, made true)"
  - "src/routes/playground/[id]/+page.svelte: no Clear in the column, no import"
  - "src/lib/ui/device-ui.spec.ts: the header's Clear as a rendered test over the real singletons (replacing the column's cell test); the shapeless-alike test deleted by title; test 10 reading the header"
  - "src/lib/ui/shell.spec.ts 7: the clear snippet before the connection snippet in both variants, the layout's one mount, the zone's four declarations"
  - "src/lib/device/install-copy.spec.ts: `Clear` held, the old words the value of no export, `Clear` excused from the re-casing rule as `Put back` is"
  - "src/lib/ui/instrument.spec.ts: QUIET without Clear.svelte"
  - "e2e/install.e2e.ts: the two CLEAR titles reading the header's label, accessible name, caption and description; one Clear inside shell-connection"
  - "13.1-COPY-NEW.md: the retired and the new value of CLEAR_LABEL, with the E.11 collision named"
  - "deferred-items.md A.2 (the store's reason precedence, now visible in the header) and A.3 (the 768 edge); B's 13.1-05 questions"
affects: [13.1-06, 13.1-08]
tech-stack:
  added: []
  patterns:
    - "A header control whose caption is rendered where the row has room for it: the zone is a flex-grow inline-size container on the PDF's reserved basis, and the control's caption sits under @container (min-width: N) with N the widest pair the store can put together, measured and written beside the number"
    - "A rendered spec over a store singleton: the reachable states through the store's own rule with its runes set by hand, the unreachable one (a page target only a session can make) through a configurable instance property deleted afterwards and the prototype asserted back"
    - "A label the user gave in one word, ledgered with the Bible's own retired and the substring accident named: the batch-containment check passes it by another control's row, so the ledger row is the review"
key-files:
  created:
    - .planning/phases/13.1-bench-corrections-four/13.1-05-SUMMARY.md
  modified:
    - src/lib/ui/Clear.svelte
    - src/lib/ui/shell/Header.svelte
    - src/lib/ui/shell/ConnectionControl.svelte
    - src/lib/ui/DeviceSlot.svelte
    - src/routes/+layout.svelte
    - src/routes/playground/[id]/+page.svelte
    - src/lib/device/install-copy.ts
    - src/lib/device/install-copy.spec.ts
    - src/lib/ui/device-ui.spec.ts
    - src/lib/ui/shell.spec.ts
    - src/lib/ui/instrument.spec.ts
    - e2e/install.e2e.ts
    - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
    - .planning/phases/13.1-bench-corrections-four/deferred-items.md
    - .planning/STATE.md
key-decisions:
  - "The label ships as `Clear` - the user's own word (bench line 4: 'CLEAR button'), in D-05's sentence case - and not section 9's `Reset active device page`, which is four words that do not fit the PDF's 218 x 37 box beside `ZONA connected`; the fact the old label carried is clearLine's, the control's description. Ledgered; 13.1-CONTEXT question 1 for the bench"
  - "One click, no confirmation: A-45's shipped behaviour kept by the user's word ('a button'); batch row I.6.5 declined and recorded; question 2 for the bench"
  - "Clear is on the intro's header too: the user said 'all the time', the intro has a connection control, and the layout hands the same snippet to both variants. The unfilled bench pages under /dev/ have no header and no Clear (13-09's choice, unchanged)"
  - "The caption is rendered only where the connection zone is at least 480px wide - a container query, not a media query - because the reasons (173 to 254px at 12px) do not fit the box the plan asked for at 1024 (88px of slack beside the slot) or at 768 (none). 480 is the widest reachable pair (no-snapshot 288.2 + 12 + `ZONA connected` 160.3 = 460.5) rounded up; the zone is 503 at 1280 and 247 at 1024. The description carries the reason at every width"
  - "The caption is 12px, DeviceSlot's caption register (the pattern D-04 cites), not the plan's 11px: identity.spec.ts test 7 and section 12 reserve 11px for the tracked uppercase micro role"
  - "The zone takes the row's slack (flex 1 1 var(--connection-w); container-type inline-size) rather than its content's width, so a wide caption can never push the header sideways; the two boxes sit at its end. ConnectionControl's host stops claiming 100% of it"
  - "DeviceSlot's caption is nowrap: where the zone is 9 to 23px short for the pair (375 on the intro beside Quick guide; 768 on the app pages) WebKit folded `Not in this browser` under the label inside the fixed 14px line box; now the pair overflows toward the gap beside it instead"
  - "The e2e reads the label span (clear-label) and the accessible name, not the button's text: the button's textContent carries its aria-hidden caption, so toHaveText(CLEAR_LABEL) on the button - the plan's form - cannot hold while a reason is shown"
patterns-established:
  - "Where a plan's caption does not fit the plan's box, measure the row before choosing between a shorter string and a narrower moment: here the shortest reason did not fit at 1024 either, so the rule is about room, not words"
requirements-completed: []
requirements-qualified: [CONN-08, SAFE-01]
duration: 40min
completed: 2026-09-12
---

# Phase 13.1 Plan 05: Clear in the header, one click, the reason as its caption Summary

**Bench line 4 answered in the tree: the store's `clearToDefault()` has a control in the header's
connection zone, 12px left of the connection control, on every page the shell renders and in both
header variants - the box `ZONA connected` wears, reading `Clear` over its reason when it cannot
act and `Clear` alone when it can, one click and no confirmation, `Resetting Page N…` with
aria-busy through the one leg, `Returns Page N to its firmware default. Your browser draft stays
as it is.` as its accessible description; the column's Clear is gone; the header is 76px before
and after at every desktop width and scrolls sideways nowhere.**

## Count line

Carried (13.1-03's observed line): **94 files / 964 tests (+1 todo) / 85 e2e titles / 101 runs /
check 656 / sweep `4 19` / allowlist 0 rows** at `0c848d4`. Re-measured before the first edit:
`vitest run --project server --maxWorkers=2` 94 / 964 (+1 todo); `grep -cE '^\s*test\('` summed
85; `--list` 101 in 15 files - no difference.

This plan's term: **+0 files / +0 tests; e2e +0 titles / +0 runs; check +0** - one test deleted
by title in `device-ui.spec.ts` ("CLEAR and KEEP ON DEVICE are shapeless alike, the retired Bare
tier left no trace, and the confirmation that was cut is absent"), one added in `shell.spec.ts`
(7), one rewritten in place (`device-ui.spec.ts`, the column's CLEAR cell test).

Observed after the commits: **94 / 964 (+1 todo)** (`check-counts.mjs 94 964` green twice at
`--maxWorkers=2`); **85 titles / 101 runs** by grep and `--list`; **svelte-check 656 / 0 / 0**;
lint clean; sweep not run (`4 19` by declaration; no entry, knob list or vendored file moved);
radius layer A `32 declarations in 67 files scanned; 0 above zero remaining in 0 allowlisted files
(); 6 circles (D-15): src/lib/ui/ColourPicker.svelte:840, :867, :882, src/lib/ui/Knob.svelte:730,
:785, :807; 26 exempt`; layer B green on the fresh build. **The next plan (13.1-06) carries 94 /
964 (+1 todo) / 85 / 101; check 656.**

**Where the planner was wrong (I-01):** 13.1-VALIDATION's first draft put the `:1398` deletion at
13.1-06 and this plan at +1. The plan's own task list is the truth - the deletion is this plan's
and the term is +0 / +0 - and the observed pair confirms it.

## The two circle files: diff proof

`git diff --stat 0c848d4 -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte src/vendor/
src/lib/catalog/library.ts src/lib/device/sequence.ts src/lib/device/install.svelte.ts
.planning/ROADMAP.md` is empty. The six circles are where 13.1-03 left them.

## Clear, as shipped

**Where it mounts.** `+layout.svelte` declares `{#snippet clear()}<Clear />{/snippet}` beside the
existing connection snippet and hands both to `Header.svelte`, which renders `{@render clear()}`
inside `.connection` (`shell-connection`) before `{@render connection()}`. Both header variants:
the app pages (`/playground/`, `/playground/[id]/`, `/sandbox/`, `/my-configs/`) and the intro
(`/`) - the intro has a connection control (`Connect ZONA`) and the user said "all the time", so
Clear sits beside it there too. The unfilled shape (the bench instruments under `/dev/`) has no
header and no Clear, as 13-09 left it. One `clear` test id on any page: `shell.spec.ts` 7 renders
both shapes and counts one; the e2e counts one inside `shell-connection` on the workspace in both
projects; the scratch measurement counted one on `/`, `/playground/arc/` and `/sandbox/?new`.

**The label, and why.** `CLEAR_LABEL = "Clear"` - the user's own word ("CLEAR button"), in D-05's
sentence case. Section 9's `Reset active device page` (13-18, D-23; the Bible's §9 row "**Reset
active device page** lives under Device actions") is retired by name and dated in
`install-copy.ts`'s header, is the value of no export (asserted from fragments), and its fact -
the firmware default, the draft untouched - is `clearLine`'s, unchanged. D-23's batch approved
the long label for the column's quiet control; D-04 moves the control into a 218 x 37 box beside
`ZONA connected`, where four words do not fit and the user named it in one. Ledgered in
`13.1-COPY-NEW.md` (retired value and new value as two rows); 13.1-CONTEXT question 1 stands for
the bench. Note for the gate: `install-copy.spec.ts`'s batch-containment check passes `Clear` by
13-18-BATCH's E.11 row (the search field's own `Clear`, accessible name `Clear the search`) - a
substring accident, not a review; the ledger row is the review, and the playground page now has
two controls reading `Clear` with different accessible names.

**The disabled reasons.** `install.clearReason(capable)` unchanged: `incapable` (`This browser
can’t write to a ZONA.`), `no-snapshot` (`Needs a copy of what is on your ZONA first.`),
`no-session` (`Needs your ZONA connected.`), in the store's precedence. The button carries a real
`disabled` for any reason, through a write (`writing`) and through a page target not at rest
(`pending`, 13-12), and holds its last non-writing reason through both, as the column's did. Never
hidden (DEGR-02). The reason is the caption (aria-hidden, inside the box) where the zone has room,
and the sr-only description (`clear-line`, the button's `aria-describedby`) everywhere.

**The page named.** When live, the description is `clearLine(install.snapshotPage ?? 0)` -
`Returns Page 2 to its firmware default. Your browser draft stays as it is.` for wire page 1
(13-18's `pageName`, numbered from one); busy reads `Resetting Page 2…` in the label with
`aria-busy="true"`.

**The box.** DeviceSlot's, declaration for declaration where the two share a fact: 44px on both
axes, `padding: 0 16px`, `border: 1px solid var(--color-boundary)`, transparent fill, `--font-sans`,
two 14px fixed line boxes (`.lines`), the label 13px 600 (ink; the quiet ink when disabled), the
caption 12px 400 in the quiet ink. Hover: `border-color: var(--color-action)`, instantly. No radius,
no transition, no animation. Measured on the served build: 288px wide with the widest reason
(chromium, `no-snapshot`), 240 with `incapable` (webkit), 68 with the label alone.

**`WRITE_CLICKS` before and after:** five before (`Apply to ZONA`, `Put back`, `Store on ZONA`,
`Reset active device page`, `Target`), five after (`Apply to ZONA`, `Put back`, `Store on ZONA`,
`Clear`, `Target`) - the fourth member's value changed, the count did not. `install-copy.spec.ts`
holds the list and the five distinct. The zero-writes proof (`install.e2e.ts` "sends nothing on
open") is by class and unchanged; the eleventh title counts the clear's five CONFIG/EXECUTE and
zero PAGESTORE/EXECUTE as before.

## The header, measured (scratch Playwright on the served build, chromium and webkit)

| Width | Header before | Header after | Zone after | Clear box | Slot | Document sw / cw |
| --- | --- | --- | --- | --- | --- | --- |
| 1920 x 1080 | 76 | **76** | 1143 (x 757) | 288 / 240 at x 1453 / 1487, caption shown | 147 / 161 at x 1753 / 1739 | 1920 / 1920 |
| 1280 x 720 | 76 | **76** | 503 (x 757) | 288 / 240 at x 813 / 847, caption shown | 147 / 161 at x 1113 / 1099 | 1280 / 1280 |
| 1024 x 768 | 76 | **76** | 247 (x 757) | 68 at x 777 / 763, label alone | 147 / 161 at x 857 / 843 | 1024 / 1024 |
| 768 x 1024 | 76 | **76** | 218 (x 530) | 68 at x 521 / 507, label alone (9 / 23px into the gap) | 147 / 161 at x 601 / 587 | 768 / 768 |
| 375 x 659 (app) | 209 | **209** | 335 (x 20, its own row) | 68 at x 128 / 114, label alone | 147 / 161 at x 208 / 194 | 375 / 375 |
| 375 x 659 (intro) | 113 | **113** | 232 (x 123, beside Quick guide) | 68 at x 128 / 114, label alone | 147 / 161 at x 208 / 194 | 375 / 375 |

Chromium / webkit where they differ (the slot's width is its S1 label in chromium and its S0a
caption `Not in this browser` in webkit). The rows are `/playground/arc/`; `/` and
`/sandbox/?new` measured the same at every width. **The header's row height does not grow**
(76 at 1920 / 1280 / 1024 / 768, as before); **the 375px header scrolls sideways nowhere**
(`browse-webkit.e2e.ts`'s own assertion green in c2: `339px content width, 1 columns`).

**The room rule, and its arithmetic.** The three reasons measure 172.6 / 205.6 / 254.2 at 12px
(Inter; both engines agree to the pixel), so the box is 206.6 / 239.6 / 288.2 with one inside. The
connection control is its label or its caption plus 52 (16 + 16 padding, 2 border, the 8px dot and
its 10px gap): 160.3 at `ZONA connected`, 161.1 at `Not in this browser`, 220.1 at `Disconnected ·
draft retained`. The pairs the store can put together: no-snapshot beside a connected or idle
control 288.2 + 12 + 160.3 = **460.5**; incapable beside a capability caption 239.6 + 12 + 161.1 =
412.7; no-session beside the unplugged caption 206.6 + 12 + 220.1 = 438.7. The threshold is 480.
The zone is 503 at 1280, 247 at 1024, 218 at 768 (the row has no slack: mark 71 and nav 343 are at
their minimums), 335 on the phone's own row, 232 on the intro's second row beside Quick guide.

**The 768 edge** (deferred-items A.3): the label-only pair is 227 (chromium) / 241 (webkit) against
the zone's 218 until the row gains that slack (about 776 / 790); it overflows leftward into the
48px gap before the nav, collides with nothing, scrolls nothing.

## Task 1 - `Clear.svelte` as the header's control; the label; the column's Clear removed (`6e5ae88`)

`install-copy.ts`: `CLEAR_LABEL = "Clear"`; the header's verbatim list drops the reset label and
names the user's one word; fact 3 names `Clear`; a dated paragraph "SECTION 9'S RESET LABEL IS
RETIRED BY NAME, 2026-09-12" with the bench line, the box, the register's stems and the spec
that holds the absence; the reasons' section comment renamed. `clearingLabel`, `clearLine`,
`CLEAR_REASONS`, `WRITE_CLICKS` untouched.

`Clear.svelte`: the script kept line for line (`capable`, `reason`, `writing`, `busy`, `pending`,
`disabled`, `held` and its effect, `shown`, `page`, `clear()`), plus two deriveds (`caption`,
`description`) reading `CLEAR_REASONS[shown]` and `clearLine(page)`; the markup and the style
replaced as described above; the header comment rewritten for the header, A-45 kept, I.6.5
declined, the room rule with its arithmetic.

`Header.svelte`: `clear?: Snippet` rendered before `connection` inside `.connection`; `.connection`
gains `gap: 12px; flex: 1 1 var(--connection-w); container-type: inline-size` (the reserved
`min-inline-size` and `min-block-size` stay); the header comment names the pair and the zone's new
sizing. `+layout.svelte`: the import, the snippet, `{clear}` on `<Header>`, a paragraph in the
shell comment. The workspace route: the import and `<Clear />` gone, the column's comment says the
reset is the header's since 13.1-05 and the remainder is 13.1-06's.

`ConnectionControl.svelte`, `DeviceSlot.svelte`: the two Rule 1 fixes below.

The specs: `device-ui.spec.ts` (the header paragraph; the imports; test 10's column clauses now
`occurrences(panel, "<Clear ") === 0`, no import, the layout mounting it once inside its clear
snippet and handing `{clear}` to the header, the install row found by `<KeepOnDevice`, the
Quiet-clause loop with one subject and Clear's `background: transparent` held separately; the
CLEAR cell test rewritten as the rendered test; the shapeless-alike test deleted by title);
`shell.spec.ts` (the header says seven; `Header` imported; test 7); `install-copy.spec.ts` (the
header's D-04 paragraph; `RETIRED_RESET_LABEL` from fragments; the verbatim list without
`CLEAR_LABEL`; the D-04 block; `Clear` excused in the re-casing loop); `instrument.spec.ts` (QUIET
without `Clear.svelte`, the reason in the comment). `identity.spec.ts` test 5 finds the new
box's `--color-boundary` without an edit - the plan listed the file; nothing in it needed to
change, and it is not in the commit.

`13.1-COPY-NEW.md`: the section "Retired and added by 13.1-05 (D-04)" with two rows.

Negative checks from scratch copies, sha256 either side: the new `device-ui.spec.ts` title and
`shell.spec.ts` 7 against HEAD's `Clear.svelte` and `Header.svelte` - **2 failed / 21 passed**;
restored byte-identical (`5a205857…` / `f6532a9e…`).

## Task 2 - the CLEAR titles read the header; the chunks; STATE (the commit carrying this file)

`install.e2e.ts`: three locators added (`clearLabel`, `clearCaption`, `clearDescription`); the
file's header paragraphs for the eleventh and the thirteenth; `noConfirmOnScreen`'s comment. The
eleventh (`@webkit CLEAR sends on the click with no confirmation, the panel reads FACTORY DEFAULT,
and PUT BACK brings the visitor's own back @webkit`): at rest the label span reads `Clear`, the
accessible name is `Clear`, the caption is empty, the description is `clearLine(ACTIVE_PAGE)`,
`aria-describedby="clear-line"`, one Clear on the page and it is inside `shell-connection`; the
busy read moves to the label span; after the leg the caption is empty and the description
`clearLine` again. The `next-caption`, KEEP ON DEVICE and PUT BACK assertions stay until 13.1-06.
The thirteenth (`@webkit CLEAR is present and disabled where PUT BACK is absent…`): the label,
the accessible name, the caption's text and the description as `CLEAR_REASONS.incapable`, the
caption `aria-hidden`, and - by the zone's measured width - the caption visible at or above 480
and hidden below, logged per project (`chromium: zone 503px, caption shown`; `webkit-phone: zone
353px, caption in the description alone`); one Clear inside the zone. `:2232`'s disabled-while-
switching assertion unchanged. `session.e2e.ts` asserts no count of the zone's children
(`shell-connection` and `connection-control` appear nowhere in it) and is unchanged.

Negative check: the seven source files of task 1 swapped for HEAD's from scratch copies, built,
the two CLEAR titles run on a fresh server - **4 failed** (both titles, both projects) - restored
byte-identical (7 of 7 sha256 OK), rebuilt.

STATE by script against a copy: `stopped_at`, `last_updated`, the Phase 13.1 line's tail (4 of 8
-> 5 of 8; 13.1-06 next), the Status line with 13.1-03's retained; `status: executing`,
`completed_phases: 11`, `percent: 100` asserted unchanged before and after.

`deferred-items.md`: A.2 (the store's reason precedence, seen in the header) and A.3 (the 768
edge) with their numbers; B's 13.1-05 line.

## The suites, as run

| What | Result |
| --- | --- |
| Baseline before the first edit: quick at `--maxWorkers=2`; grep; `--list` | 94 / 964 (+1 todo); 85; 101 - the carried line, no difference |
| The header measured before the first edit (scratch Playwright, three routes, five widths, two engines) | 76 at every desktop width; 209 / 113 at 375; zone 218; the column's Clear at y 1375-2269 below the fold |
| `npm run check` after the source edits | 656 / 0 / 0 |
| The seven affected specs after the spec edits | 58 passed in 6 files (device-ui 16, shell 7, identity 11, install-copy 6, instrument 4, config-shape 14) |
| Negative: HEAD's Clear.svelte and Header.svelte under the new specs | 2 failed / 21 passed; restored, sha256 equal |
| Build 1; the header measured | Clear at x 457 at 1280 - OUTSIDE the zone (ConnectionControl's host at 100%); found and fixed |
| The reasons and the slot's labels and captions measured at 12px / 13px | 172.6 / 205.6 / 254.2; 81.0 to 108.3; 81.2 to 168.1 (both engines equal) |
| Build 2; the header measured | The pair in the zone at every width; WebKit folding `Not in this browser` at 375 (intro) and 768 (app) - found and fixed |
| Build 3; the header measured; screenshots at 375 (intro, webkit), 768 (both), 1280 (both) | The table above; no fold; no overflow of the document |
| `playwright test e2e/install.e2e.ts --grep CLEAR` (the plan's verify) | 4 passed (two titles x two projects) |
| Negative: a build of HEAD's seven source files under the new e2e | 4 failed; restored, 7 of 7 sha256 OK |
| Build 4 at the restored tree; `npm run check`; quick run 1 | 656 / 0 / 0; 94 / 964 (+1 todo), `check-counts 94 964` green |
| `npm run lint` | clean |
| Radius layer A / B | 32 in 67, 0 above zero, allowlist 0, six circles; B green on build 4 |
| Commit `6e5ae88` (task 1) | 12 files |
| Chunk c1 (`install`, `session`) on a fresh detached server | 34 passed, no rerun |
| Chunk c2 (`browse`, `browse-webkit`) | 21 passed, no rerun; the 375px header: no sideways scroll |
| Chunk c5 (`artifacts`, `radius`, `skeleton`, `smoke`) | 10 passed, **1 red**: `artifacts.e2e.ts` "the static build is complete" - the build's source archive was stamped `0c848d4` and HEAD had moved to `6e5ae88` with the task-1 commit (a build predating a commit, not a defect) |
| Build 5 at `6e5ae88`; `artifacts.e2e.ts` alone on a fresh server | 3 passed |
| Chunk c4 (`catalog`, `fidelity`, `first-experience`, `library`, `sandbox`) | 15 passed, no rerun |
| Chunk c3 (`tuning`, `tuning-webkit`) | 20 passed, no rerun |
| Quick run 2 | 94 / 964 (+1 todo), `check-counts 94 964` green |

All five chunks: 34 + 21 + 20 + 15 + 11 = 101, the `--list` count. The plan asked for c1 and
c2; c3, c4 and c5 were run as well because the header is on every page. The one red is named
above and was rerun alone after a rebuild. `test-results/` removed after each run. The chunk
script was `1305/e2e-chunks-1305.sh`, 13.1-03's with this session's paths; every server a
detached wrangler on 4173, stopped through PowerShell; the user's dev server on 5173 (answering
200 when checked) was not touched.

## Deviations from the plan

### Auto-fixed

**1. [Rule 1 - Bug] ConnectionControl's host took the whole zone.** Found during task 1 by
measuring the first build: `.connection-control { min-inline-size: 100% }` (13-11, "fills the
reserved box") made the host as wide as the zone, so the Clear box was laid out OUTSIDE the zone
and over the nav at 1280 (zone at x 757, Clear at x 457). The declaration is dropped; the zone
justifies the pair. `src/lib/ui/shell/ConnectionControl.svelte` (not in the plan's file list);
`6e5ae88`.

**2. [Rule 1 - Bug] DeviceSlot's caption folded in WebKit.** Found during task 1 by measuring the
second build: the caption had no `white-space: nowrap`, so where the zone was 9 to 23px short for
the pair (375 on the intro beside Quick guide; 768 on the app pages) WebKit shrank the control and
wrapped `Not in this browser` onto two lines inside the fixed 14px line box, pushing `Preview only`
to the floor (screenshot `1305/intro-375-webkit.png`). The caption is nowrap now (the label already
was); the pair overflows toward the gap beside it instead. `src/lib/ui/DeviceSlot.svelte` (not in
the plan's file list); `6e5ae88`.

**3. [Rule 3 - Blocking] `instrument.spec.ts` demanded a borderless Clear.** Its QUIET list held
`Clear.svelte -> .control` to `border: 0` and `background: transparent`; the plan asks for a 1px
boundary. The row is removed with the reason in the comment (Clear wears the box now); the derived
pill walk still reads the file. `src/lib/ui/instrument.spec.ts` (not in the plan's file list);
`6e5ae88`.

**4. [Rule 2 - Convention] The caption at 12px, not the plan's 11px.** Section 12 and
`identity.spec.ts` test 7 reserve 11px for the tracked uppercase micro role; a sentence-case
reason at 11px would be the one exception. DeviceSlot's caption - the pattern D-04 cites - is
12px 400, and Clear's is the same. `6e5ae88`.

**5. [Rule 1 - Bug in the plan's assertion] The caption is rendered only where the zone has room
(a container query at 480px), and the e2e reads the label span and the accessible name rather than
the button's text.** The plan's box carried its reason at every width; measured, the widest
reason's box is 288px and the row's slack beside the slot is 88px at 1024 and none at 768, so the
plan's shape would have scrolled the compact and stacked bands sideways (and the phone: 288 + 12 +
161 > 335). The zone became a flex-grow inline-size container and the caption sits under
`@container (min-width: 480px)` with the arithmetic in the file; the description carries the reason
everywhere. And `toHaveText(CLEAR_LABEL)` on the button - the plan's task 2 form - cannot hold
while a reason is shown, because the button's `textContent` includes its aria-hidden caption; the
e2e reads `clear-label` and `toHaveAccessibleName`. `6e5ae88` and the test commit.

### Not fixed, logged

- **The store's reason precedence** (deferred-items A.2): with Web Serial present and nothing
  connected, `clearReason` tests the snapshot before the session and the header reads `Needs a copy
  of what is on your ZONA first.` rather than `Needs your ZONA connected.` on every page.
  `install.svelte.ts` is untouched by the phase's rule; for the gate's bench row.
- **The 768 edge** (deferred-items A.3): 9 / 23px of the pair into the 48px gap before the nav until
  about 776 / 790 wide.

## What the plan asserts that the tree does not support

1. **"the caption … reads the shown reason when disabled"** at every width - it cannot at 1024, 768
   or on the phone (deviation 5); the description does.
2. **"Every `toHaveText(CLEAR_LABEL)` now reads `Clear` through the import"** - on the button,
   with the caption inside it, the assertion fails whenever a reason is shown; the label span is
   read (deviation 5).
3. **"the reason twins"** / "the caption twins" - there are no twins: the box's height is fixed by
   two 14px line boxes, not by hidden siblings, so one caption span whose text changes is enough
   (DeviceSlot's pattern, which the plan cites).
4. **The caption at 11px** - section 12's micro rule (deviation 4).
5. **`grep -rn "Reset active device page" src e2e` -> "only the retired-list comment and the spec's
   absence assertion"** - three hits: the module's dated retirement paragraph, the spec's header
   comment, and the spec's Bible-containment row (`**Reset active device page** lives under Device
   actions` - the Bible's own text, kept because the Bible still says it); the absence assertion
   itself is assembled from fragments and carries no literal.
6. **`identity.spec.ts` in `files_modified`** - nothing in it needed an edit; test 5 classifies
   the new box's boundary without one.
7. **"`check-counts.mjs 94 <carried+0>`"** - honoured as `94 964`, the observed pair from 13.1-03.
8. **The plan's `files_modified` omits** `ConnectionControl.svelte`, `DeviceSlot.svelte`,
   `instrument.spec.ts` and `deferred-items.md`, all four touched (deviations 1-3; the log).

## The trailer conflict

The harness asks for a `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` trailer on every
commit. The repository's standing rule (13.1-PLAN's `<standing_rules>`, the orchestrator's brief)
forbids any Claude or Anthropic attribution and any co-author trailer. The repository's rule wins:
neither commit carries a trailer, and this paragraph is the report of the conflict.

## Questions for the user

1. **The label:** `Clear` (shipped, your word) or section 9's `Reset active device page`
   (13.1-CONTEXT question 1).
2. **The gate:** one click (shipped, A-45) or section 16's confirmation (`Reset Page 2 to its
   firmware default? Your browser draft will remain available.` - I.6.5, declined; question 2).
3. **The caption's precedence:** with no ZONA connected in Chromium the box reads `Needs a copy of
   what is on your ZONA first.` - the store's order, now in the top right of every page. Should
   "connect first" come before "the copy first"? (deferred-items A.2)
4. **The caption at the compact band:** at 1024 to about 1256 wide the box reads `Clear` alone and
   the reason is the accessible description only. Acceptable, or should the box always show its
   reason at the cost of the header wrapping there?
5. **Two `Clear`s on the playground page:** the header's and the search field's (`Clear the search`).
   Different accessible names; the same visible word.
6. **The intro's Clear:** the front door has no device band, yet the control is there beside
   `Connect ZONA` because you said "all the time". Keep, or app pages only?

## What was NOT done, and why

- The column's remainder (PUT BACK, KEEP ON DEVICE, NEXT, the honesty line) is untouched: 13.1-06's
  (D-06, D-07).
- `install.svelte.ts`, `sequence.ts`, `library.ts`, `src/vendor/`, `Knob.svelte`,
  `ColourPicker.svelte`, `.planning/ROADMAP.md`, `12-*`, `13-gui-overhaul/` untouched; the sweep not
  run; no device touched; nothing deployed; CAT-04 stays `[ ]`; no `gsd-tools state` command; no
  `git checkout` / `restore` / `stash` / `clean` (every restore was a copy from the scratchpad with
  sha256 either side).
- The docs (`TESTING.md`, `INSTALL-RUNBOOK.md`, `HARDWARE-AUDITION.md`, `REQUIREMENTS.md`) are not
  this plan's; `INSTALL-RUNBOOK.md`'s "`CLEAR` is `Reset active device page`" reading (13-20) is
  now stale by one label - for the gate's dated paragraph.

## Commits

| Commit | Type | What |
| --- | --- | --- |
| `6e5ae88` | feat(13.1-05) | Clear in the header, one click, the reason as its caption: Clear.svelte, Header.svelte, ConnectionControl.svelte, DeviceSlot.svelte, +layout.svelte, the workspace route, install-copy.ts and its spec, device-ui.spec.ts, shell.spec.ts, instrument.spec.ts, 13.1-COPY-NEW.md |
| (this commit) | test(13.1-05) | the CLEAR titles read the header: install.e2e.ts, STATE.md, deferred-items.md, this SUMMARY |

No trailer on either.

## Self-Check: PASSED

Every file in the key-files list is on disk; `6e5ae88` is in `git log` (12 files, 763 insertions, 384 deletions); the source no longer names section 9's label outside the dated retirement and the spec's Bible row; the count line is the observed one (94 / 964 (+1 todo) / 85 / 101 / check 656) at `+0 / +0; +0 / +0`.
