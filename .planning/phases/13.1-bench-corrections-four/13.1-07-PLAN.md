---
phase: 13.1-bench-corrections-four
plan: 07
type: execute
wave: 4
depends_on: ["13.1-04", "13.1-06"]
files_modified:
  - src/lib/ui/MidiField.svelte
  - src/lib/ui/TuningRegion.svelte
  - src/lib/tune/inspector-copy.ts
  - src/lib/ui/BudgetMeter.svelte
  - src/lib/ui/tune-ui.spec.ts
  - src/lib/ui/instrument.spec.ts
  - e2e/tuning.e2e.ts
  - e2e/tuning-webkit.e2e.ts
  - e2e/install.e2e.ts
  - e2e/session.e2e.ts
  - e2e/first-experience.e2e.ts
  - e2e/browse.e2e.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [TUNE-03, TUNE-05]

must_haves:
  truths:
    - "The workspace's MIDI output is two typed fields — CC number and Channel — with the PDF's helper line; a typed value the knob offers moves the knob to that index, an out-of-range one is refused inline and the last good value survives (D-09, bench line 7)."
    - "The knobs underneath are unchanged: the option lists, the stamp, the forecast and the sweep are what they were; a field is a text input over a closed list, not a free numeric; X-08 kept."
    - "The budget meters, the TUNING caption and the forecast are not rendered; cost() still measures, the over-budget refusal still disables Apply and names the cause with zero frames on the wire, and the fit-ladder line stays (D-10, bench line 8)."
    - "The tuning region carries the two measured numbers as data attributes so the suite reads them; nothing paints them. Every workspace install title clicks the bar's zone, not the deleted column."
  artifacts:
    - path: "src/lib/ui/MidiField.svelte"
      provides: "One typed field over a knob's closed list: the knob's own literal shown, a keystroke mapped to an index or refused inline with the offered values named and the last good value kept"
    - path: "src/lib/ui/TuningRegion.svelte"
      provides: "The MIDI section renders MidiField per MIDI knob; the fourth group (meters, caption, forecast) removed; data-setup / data-timer / data-busy on the region root"
    - path: "e2e/install.e2e.ts"
      provides: "Every workspace install title re-aimed from the column to the bar's zone, the put-back clauses removed, the two @webkit titles merged"
  key_links:
    - from: "src/lib/ui/MidiField.svelte"
      to: "src/lib/tune/view.ts"
      via: "a typed integer -> the knob's index in view.values; changeKnob(id, index)"
      pattern: "changeKnob"
    - from: "src/lib/ui/TuningRegion.svelte"
      to: "src/lib/tune/model.ts"
      via: "cost() still measured; over-budget still disables Apply through onbudget; no BudgetMeter rendered"
      pattern: "onbudget"
---

<objective>
The bench's last two look changes and the e2e clean-up the column's removal owes: MIDI output as two
typed fields (bench line 7, D-09); the budget meters hidden (bench line 8, D-10); and every workspace
install title re-aimed from the deleted column to the bar's zone.

Purpose: the user asked for typed CC and Channel fields and for the code-limit visualisation to go;
and the e2e titles 13.1-06 left red must click the bar, not the column.
Output: `MidiField.svelte`; the MIDI section and the fourth group changed; the meters deleted; the
install and tuning e2e titles re-aimed. Term **+0 / +1; e2e +1 / +1** — the @webkit CLEAR merge
collapses two install titles into one (−1 / −2) and three titles are added (+1 intro at 13.1-01 is
not this plan's; here: none new — state the observed pair; the net across the phase is the
VALIDATION chain's).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@.planning/phases/13.1-bench-corrections-four/13.1-06-SUMMARY.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-09-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-10-SUMMARY.md
@.planning/phases/13-gui-overhaul/bible/HANGAR-ZONA-GUI-design-specification.md
@src/lib/ui/TuningRegion.svelte
@src/lib/ui/BudgetMeter.svelte
@src/lib/ui/BudgetMessage.svelte
@src/lib/ui/Knob.svelte
@src/lib/ui/KnobRack.svelte
@src/lib/tune/view.ts
@src/lib/tune/inspector-copy.ts
@src/lib/tune/surprise.ts
@src/lib/catalog/entries/arc.ts
@src/lib/tune/knobs.preset.ts
@src/lib/ui/tune-ui.spec.ts
@src/lib/sandbox/copy.ts
@e2e/tuning.e2e.ts
@e2e/install.e2e.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's; TUNE-03 and TUNE-05's qualifiers are the gate's.
- **No `border-radius` above zero; the allowlist stays EMPTY; the six circles unmoved; `Knob.svelte`
  and `ColourPicker.svelte` not edited** (the MIDI knobs stop being handed to the rack; `Knob.svelte`
  itself does not change, so its three circles at `:730 / :785 / :807` do not move — verify with
  `git diff --stat`).
- **D-05's register; every new string ledgered in `13.1-COPY-NEW.md`** with the batch row it models:
  the over-range CC line is new (G.28's Sandbox line is the model), `TYPE_A_NUMBER` (G.27's model).
- **Every count a carried name plus a delta** from the block 13.1-06 left; prove each negative against
  one before it lands. Term **+0 / +1** unit; e2e as observed (the @webkit merge is −1 / −2).
- **The sweep is not run** — X-08 kept, no knob's option list moves, no stamp changes. `src/vendor/`,
  `library.ts`, the entries untouched.
- **Quick at `--maxWorkers=2`.** Playwright chunks **c1** (`install`, `session`), **c3** (`tuning`,
  `tuning-webkit`), **c4** (`first-experience`), **c2** (`browse`) on fresh detached servers; reds
  rerun alone and named (13-20: `install:1434` 7-for-5, `install:1977` the phone margin, `browse:1404`,
  `browse-webkit:357`).
</standing_rules>

<interfaces>
From `src/lib/tune/view.ts`: `KnobView = { id; label; kind; widget; values: { label }[]; index;
default; readout? }`; `integerReadout(values, index)` returns the raw literal when every value is an
integer (X-08). Arc's `cc` values `["1","16","20","74","102"]`, `channel` `["0"…"15"]`;
`knobs.preset.ts` `channel` `["1"…"16"]` (one-based), `send` an option list.
From `src/lib/ui/TuningRegion.svelte`: `midiKnobs` (`:388`), `changeKnob(id, index)` (`:711`),
`resetKnob`, `{#snippet midi()}` (`:871`) renders `<KnobRack knobs={midiKnobs} layout="grid">` then
`<p class="helper">{MIDI_HELPER}</p>`; the fourth group (`:908-931`) renders `<hr>`,
`{TUNING_CAPTION}`, two `<BudgetMeter>` and `<BudgetMessage>`; `over` / `onbudget` / `receiveOver`
(`:639`) disable Apply; `forecast` / `rackForecast` feed the ghost.
From `inspector-copy.ts`: `SECTION_MIDI = "MIDI output"`, `MIDI_HELPER = "Map this CC to a parameter
in your instrument or DAW."`.
From `src/lib/sandbox/copy.ts` (the Sandbox's typed-field lines, the model): `WHOLE_NUMBER = "Type a
whole number."`, `CC_RANGE = "A controller number is 0 to 127."`, `CHANNEL_RANGE = "A channel is 1 to
16."`. The workspace list is NOT the full range, so the refusal names the offered values instead.
`tune-ui.spec.ts`: the meter/ghost tests at `:705-885`, the widget boundary test at `:1292`.
`e2e/tuning.e2e.ts`: `meterText`/`meter-setup`/`meter-timer`/`.numerals`/`.percent` (`:204-262`,
`:400`, `:875`, `:943`, `:1007`); `settled`/`recomputed` read `aria-busy` on `meter-setup`.
`e2e/install.e2e.ts`: the workspace titles listed in 13.1-06's SUMMARY (`:1434`, `:1580`, `:1721`,
`:1842`, `:1977`, `:2090`, `:2232`, `:2397`, `:2489`), their helpers `primary = getByTestId(
"try-on-device")`, `putBackControl`, `keepControl`, `honesty`, `visibleLine`.
The PDF page 5: `CC number` holds `74`, `Channel` holds `1`, the helper beneath; two fields side by
side in the 2 x 2 grid's top row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-07-01: `MidiField.svelte`, the typed field over a closed list</name>
  <files>src/lib/ui/MidiField.svelte, src/lib/ui/TuningRegion.svelte, src/lib/tune/inspector-copy.ts, src/lib/ui/tune-ui.spec.ts, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <behavior>
    - `tune-ui.spec.ts` (new, +1) "MIDI output is two typed fields over the knobs' closed lists":
      a `MidiField` for Arc's `cc` shows `74` at index 3; an input of `20` calls `onchange("cc", 1)`
      (the index of `"20"`); `99` (not offered) refuses inline — `aria-invalid`, a message naming the
      offered values, the model's value still index 3; `1` (offered) moves to index 0; a channel field
      shows the zero-based literal on a Lua entry (X-08); the field is `type="text"
      inputmode="numeric"` with the PDF's label and the 44px floor; `border-radius` 0.
    - `tune-ui.spec.ts` widget boundary (`:1292`): the MIDI section renders `MidiField`, not a
      `KnobRack`, for `midiKnobs`; Behavior and Appearance still render the rack.
  </behavior>
  <action>
    1. **`MidiField.svelte`** (new): props `{ knob: KnobView; onchange: (id, index) => void; onreset?:
       (id) => void }`. The value shown is `integerReadout(knob.values, knob.index)` (the knob's own
       literal — X-08; on a Lua channel that is `0`, on a preset `1`). A text input,
       `inputmode="numeric"`, `autocomplete="off"`, the label the PDF's (`CC number` for `cc`,
       `Channel` for `channel`; the knob's own for `ccBase`/`send`). On input: trim; if not
       `/^-?\d+$/`, refuse with `TYPE_A_NUMBER`; else the index of the typed literal in `knob.values`;
       if found, `onchange(knob.id, index)` and clear; if not, refuse with `offeredLine(knob)`. The
       refused text stays with `aria-invalid` and the message under it until a keystroke validates
       (13-16's shape). Uncontrolled in one direction: the value is the model's literal or the refused
       text. Header: a text input OVER a closed list (D-09; D-14 Q5 overridden for these two only),
       never a free numeric — the index is what the stamp, the forecast and the sweep need; X-08 kept;
       the range/offered inconsistency on Lua channels named for the bench (13.1-CONTEXT question 5).
    2. **`inspector-copy.ts`**: add `TYPE_A_NUMBER = "Type a whole number."` and `offeredLine(knob):
       string` — for a contiguous integer run, "The value here is {min} to {max}." (a channel: "0 to
       15" / "1 to 16"); otherwise "The values here are {list}." with the literals joined by commas and
       "or" before the last (`cc`: "1, 16, 20, 74 or 102"). Ledger both, G.27 / G.28 the models, D-09.
    3. **`TuningRegion.svelte`** `{#snippet midi()}`: a 2 x 2 grid of `<MidiField>` per `midiKnob`
       (`data-testid="midi-grid"` and the 454 reflow kept), `onchange={changeKnob}`,
       `onreset={resetKnob}`; the `{MIDI_HELPER}` line stays; `midiKnobs` and `isMidiDestination`
       unchanged.
    4. Specs as the behaviours; ledger the strings.
  </action>
  <verify>
    <automated>npx vitest run --project server src/lib/ui/tune-ui.spec.ts src/lib/tune/view.spec.ts src/lib/tune/surprise.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>The MIDI section is two typed fields; a valid type moves the knob, an invalid one is refused with the offered values and the last value kept; the knobs' lists, stamp and forecast unmoved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-07-02: The budget meters hidden; the numbers as data attributes; the tuning e2e re-aimed</name>
  <files>src/lib/ui/TuningRegion.svelte, src/lib/ui/BudgetMeter.svelte, src/lib/ui/tune-ui.spec.ts, src/lib/ui/instrument.spec.ts, e2e/tuning.e2e.ts, e2e/tuning-webkit.e2e.ts, e2e/browse.e2e.ts</files>
  <behavior>
    - `tune-ui.spec.ts` (the meter/ghost tests rewritten): no `<BudgetMeter` in `TuningRegion.svelte`;
      no `TUNING_CAPTION`, `forecast`, `ghost`; the region root carries `data-setup`, `data-timer`,
      `data-busy`; `<BudgetMessage>` still rendered; `over` / `onbudget` still wire Apply's disable;
      the error-ink test reads `BudgetMessage.svelte` alone; the accent census total drops by the
      meter's one accent use (assert the new total).
    - `instrument.spec.ts`: no `BudgetMeter.svelte` row.
  </behavior>
  <action>
    1. Prove the count gate against one negative first (a scratch copy, restore with sha256).
    2. **`TuningRegion.svelte`**: delete the fourth group's meter markup (`:908-931`) — the `<hr>`,
       the caption, the `metersUnavailable` branch, the two `<BudgetMeter>`; keep `<BudgetMessage
       {ladder} {over}>` and the sr-only live region. Delete `forecast`, `rackForecast`,
       `forecastEvent`, `forecastKnob`, the `onforecast` props to the racks and the swatch, and the
       forecast bindings. Keep `cost()`, `view`, `over`, `receiveOver`, `onbudget` — TUNE-05's disable
       stays. On the region root add `data-setup={view?.setup.used}`, `data-timer={view?.timer.used}`,
       `data-busy={busy}`. Rewrite the fourth-group header paragraph: hidden by the user's word (D-10);
       measured and carried as attributes; TUNE-05 is `BudgetMessage`'s line; TUNE-03 qualified at the
       gate.
    3. **Delete `BudgetMeter.svelte`** (`git rm`). Do NOT edit `Knob.svelte` — stop PASSING the
       forecast from `TuningRegion`; leave `Knob.svelte`'s forecast props in place if touching them
       would move `:730 / :785 / :807`; confirm `git diff --stat src/lib/ui/Knob.svelte` empty.
       `KnobRack.svelte` may drop the forecast pass-through if it carries no circle.
    4. **`e2e/tuning.e2e.ts`**: `meterText`/`meterUsed`/`meterPct` read the region root's `data-setup`
       / `data-timer`; `settled`/`recomputed` read `data-busy` on `tuning-region`. Rewrite the titles:
       "the two meters read two different numbers…" → "the two measured numbers differ and both change
       on a knob turn"; "an over-budget configuration reddens its meter and disables the primary
       control" → "…shows the refusal line and disables Apply" (`budget-message`, `turn-it-down`, the
       disabled `apply-to-zona`); "the one-click back-off…" keeps `turn-it-down` and the numbers back
       under 908. Colour-rail titles use 13.1-04's `openColourEditor`.
    5. **`e2e/tuning-webkit.e2e.ts`** and **`e2e/browse.e2e.ts:1587`**: the `meter-${event}` reads →
       the attributes.
  </action>
  <verify>
    <automated>git diff --quiet -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte && npm run check && npx vitest run --project server src/lib/ui/tune-ui.spec.ts src/lib/ui/instrument.spec.ts src/lib/ui/radius.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>No meter, no caption, no ghost in the workspace inspector; the numbers survive as attributes; TUNE-05's refusal line and ladder stay; `Knob.svelte` untouched and its circles unmoved.</done>
</task>

<task type="auto">
  <name>Task 13.1-07-03: The workspace install titles re-aimed at the bar; chunks c1/c2/c3/c4; the count; STATE; commits</name>
  <files>e2e/install.e2e.ts, e2e/session.e2e.ts, e2e/first-experience.e2e.ts, .planning/STATE.md</files>
  <action>
    1. **`install.e2e.ts`**: `primary = getByTestId("apply-to-zona")` (the bar's control); delete
       `putBackControl`, `keepControl` re-pointed to `store-on-zona`, `honesty` re-pointed to the
       `apply-honesty` sr-only span. For each workspace title:
       - `:1434` "the panel writes on a click…": the ready sentence read off `apply-honesty`; the
         PUT BACK block deleted; `store-on-zona` disabled with `never-tried`; click `apply-to-zona`;
         the busy label and the header lock as before (the store's `writing`); no `chosen-panel`.
       - `:1580` "the flash confirmation replaces the control…": `store-on-zona` → `store-confirm` in
         its place, the naming sentence, focus deliberate — the zone's `closeConfirm` focus; keep.
       - `:1721` "after a keep, PUT BACK stores too and KEEP ON DEVICE waits…" → "after a store, a
         second store waits for another apply": the put-back half deleted; the store-waits half kept.
       - `:1842` "the live region speaks once per outcome, and Escape is ignored while writing":
         `chosen-panel` → `destination`; the put-back line assertion deleted.
       - `:1977` and `:2489` (the two @webkit) **merged** into one: "@webkit Clear sends on the click
         with no confirmation and the panel reads the firmware default; and where the browser cannot
         write, Clear is present and disabled with its reason @webkit" — the header's Clear (13.1-05),
         no put-back assertions. (−1 title, −2 runs.)
       - `:2090` "a knob turned before the click is the pair…": `apply-to-zona`; keep.
       - `:2232` "the destination menu…": already re-written at 13.1-02; delete its remaining
         put-back-page-line clause and `putBackControl` disabled-while-switching assertion.
       - `:2397` "@webkit the install controls are present and disabled…": the controls are the bar's
         `apply-to-zona` and `store-on-zona` now; the "PUT BACK is absent" clause is trivially true
         (assert count 0 of `put-back`) and folds into the merged @webkit title or stays as one
         assertion.
    2. **`session.e2e.ts` `:1307`, `:1423`** and **`first-experience.e2e.ts` `:269`**: `chosen-panel`
       → `destination`; `try-on-device` → `apply-to-zona`; the put-back / keep assertions deleted;
       "with no Web Serial the control is present, disabled, and says why" reads the bar's Apply and
       the header's connection caption.
    3. `npm run build`; chunks **c1**, **c3**, **c4**, **c2** on fresh detached servers; reds rerun
       alone and named. Confirm 13.1-06's owed reds are now green.
    4. Prettier by path; `check-counts.mjs 94 <carried+1>`; `npm run check`, `npm run lint`.
    5. STATE by script against a copy (one line). Three commits by `--only`: task 01 (`feat(13.1-07):
       MIDI output as two typed fields …`), task 02 (`feat(13.1-07): the budget meters hidden, the
       numbers kept …`), task 03 (`test(13.1-07): the workspace install titles read the bar …` with
       STATE and the SUMMARY). No trailer.
  </action>
  <verify>
    <automated>npx playwright test e2e/install.e2e.ts e2e/tuning.e2e.ts e2e/session.e2e.ts e2e/first-experience.e2e.ts --workers 3</automated>
  </verify>
  <done>Every workspace install and tuning title green in both projects (reruns named); no `try-on-device`, `chosen-panel`, `put-back`, `meter-` locator left in e2e; `check-counts.mjs` matches; three commits.</done>
</task>

</tasks>

<verification>
- `grep -rn "meter-setup\|meter-timer\|<BudgetMeter\|try-on-device\|chosen-panel\|putBackControl" src e2e` → nothing (or only comments).
- `tune-ui.spec.ts` at the new count; `instrument.spec.ts` without the meter row; `radius.spec.ts` A
  with the six circles unmoved; `git diff --stat -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte` empty.
- `install.spec.ts` test 4 green and unedited; the sweep not run and `4 19` unmoved (the gate proves it).
</verification>

<success_criteria>
- Bench lines 7 (the MIDI half) and 8 answered: two typed fields over the closed lists; the meters
  hidden with the refusal line kept.
- Every e2e title the column and the meters touched clicks the bar and reads the attributes.
- Term stated against the carried block; the four chunks green.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-07-SUMMARY.md` with the
count line, the @webkit merge named, the X-08 channel-numbering inconsistency named for the bench,
`git diff --stat` on the circle files (empty), every rerun named, and the trailer conflict reported.
</output>
