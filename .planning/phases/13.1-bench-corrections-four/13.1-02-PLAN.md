---
phase: 13.1-bench-corrections-four
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/device/install.svelte.ts
  - src/lib/device/page-target.ts
  - src/lib/device/install-copy.ts
  - src/lib/ui/DestinationReview.svelte
  - src/routes/playground/[id]/+page.svelte
  - src/lib/ui/sandbox/SurfaceActions.svelte
  - src/routes/dev/install/+page.svelte
  - src/lib/ui/device-ui.spec.ts
  - src/lib/ui/instrument.spec.ts
  - src/lib/device/install-copy.spec.ts
  - src/lib/device/page-target.spec.ts
  - src/lib/device/install.spec.ts
  - e2e/install.e2e.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [SAFE-01, SAFE-09]

must_haves:
  truths:
    - "Changing the Target select switches the module's page: the restore heartbeat leaves first, then exactly one switch, with no review in between (D-05, bench line 5)."
    - "Opening the select still sends nothing; only a change sends — the zero-writes proof holds and its title says so (SAFE-01)."
    - "`switching` ends only on the module's own report; the window's close renders the unverified line; Apply is enabled by `canApply()` alone — none of the wire's envelope moved."
    - "No component under `src/lib/ui/` renders a destination review; `DestinationReview.svelte` is gone; the review's two words and two sentences are retired by name."
  artifacts:
    - path: "src/lib/device/install.svelte.ts"
      provides: "`switchPage(page)`: the target's `request()` then `confirm()` in one call; `confirmPage` still the only caller of `target.confirm(`"
    - path: "src/lib/device/install-copy.ts"
      provides: "`WRITE_CLICKS` with the Target select as its fifth member (`TARGET_CLICK`), `SWITCH_PAGE_LABEL` and `KEEP_PAGE_LABEL` retired by name in the header"
    - path: "src/lib/ui/device-ui.spec.ts"
      provides: "The review test inverted: the review must not appear"
  key_links:
    - from: "src/routes/playground/[id]/+page.svelte"
      to: "src/lib/device/install.svelte.ts"
      via: "onTargetChange -> install.switchPage(value)"
      pattern: "install\\.switchPage\\("
    - from: "src/lib/device/install.svelte.ts"
      to: "src/lib/device/page-target.ts"
      via: "the one caller of target.confirm()"
      pattern: "target\\.confirm\\("
---

<objective>
Bench line 5, D-05: the page switch happens on the select's change, with no destination review.
Phase 13 D-06's second clause is struck by the user; the rest of the envelope — heartbeat first, the
ACK gate, `unverified`, the per-page snapshot — is the wire's and does not move.

Purpose: the one control that moves the hardware behaves the way the user asked, and the proofs that
it is still a click and still gated are re-pinned rather than weakened.
Output: `switchPage` on the store; the review component and its four strings retired; the workspace
and the Sandbox switching on change; the review test inverted; the row-I e2e title re-written.
Term **+0 / +0; e2e +0 / +0** (one deletion of a render test balanced by the inverted test's new
render check — state the observed pair).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-12-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-17-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-18-BATCH.md
@src/lib/device/install.svelte.ts
@src/lib/device/page-target.ts
@src/lib/device/install-copy.ts
@src/lib/ui/DestinationReview.svelte
@src/routes/playground/[id]/+page.svelte
@src/lib/ui/sandbox/SurfaceActions.svelte
@src/routes/dev/install/+page.svelte
@src/lib/ui/device-ui.spec.ts
@src/lib/device/install-copy.spec.ts
@src/lib/device/page-target.spec.ts
@e2e/install.e2e.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's; the SAFE-01 qualifier is the gate's.
- **No `border-radius` above zero; the allowlist stays EMPTY; the six circles unmoved; `Knob.svelte`
  and `ColourPicker.svelte` not edited.**
- **D-05's register; every new or retired string ledgered in `13.1-COPY-NEW.md`** (append rows; the
  file exists from 13.1-01 — if this plan runs first in the wave, create it with the register header).
- **Every count a carried name plus a delta** from `94 / 961 (+1 todo) / 83 / 99`. Term **+0 / +0;
  +0 / +0**.
- **The sweep is not run.** `src/vendor/`, `library.ts`, the entries untouched.
- **Quick at `--maxWorkers=2`.** Playwright chunk **c1** (`install.e2e.ts`, `session.e2e.ts`) on a fresh
  detached server stopped through PowerShell; reds rerun alone and named (`install:1434` 7-for-5,
  `install:1977`, `session:851`, `session:1009` are 13-20's known transients).
</standing_rules>

<interfaces>
From `src/lib/device/page-target.ts` (unchanged by this plan except its words):
```ts
export class PageTarget {
  canApply(): boolean;                 // status === "reported" && reported !== undefined && requested === reported
  observeReport(page: number): void;   // the module's heartbeat page; ends `switching` on the requested page
  request(page: number): boolean;      // opens `requested`; refused while switching, before a report, and for the reported page (which cancels)
  cancel(): void;                      // back to `reported`; a no-op while switching
  confirm(q, P, address): Promise<void>; // `requested` -> `switching`: hostHeartbeat() THEN pageActive(); arms the window
}
export const switchingLine = (to: number) => `Switching to ${pageName(to)}…`;
export const unverifiedLine = (requested: number, lastReported: number | undefined) => string;
export const TARGET_LABEL = "Target";
export const APPLY_LABEL = "Apply to ZONA";
// retired by this plan: switchReviewLine, replaceReviewTitle
```
From `src/lib/device/install.svelte.ts` (`:1159-1190`):
```ts
requestPage(page: number): boolean   // refused in flight / writing / no session; opens the target's `requested`
cancelPage(): void
async confirmPage(): Promise<void>   // the ONLY caller of target.confirm(q, P, id.zona)
pageStatus: TargetStatus; pageReported; pageRequested; pages; applyReady
```
From `src/lib/device/install-copy.ts` (`:212-244`):
```ts
export const SWITCH_PAGE_LABEL = "Switch page";   // retires
export const KEEP_PAGE_LABEL = "Keep this page";  // retires
export const WRITE_CLICKS = [TRY_ON_LABEL, PUT_BACK_LABEL, KEEP_LABEL, CLEAR_LABEL, SWITCH_PAGE_LABEL] as const;
```
`install-copy.spec.ts` "the closed sets…" (`:689-697`) holds `WRITE_CLICKS` equal to the five labels,
length 5, distinct 5; `:779`, `:801`, `:1008` iterate it. `device-ui.spec.ts` `:1950` is the review
test (its assertions are listed in the task). `instrument.spec.ts:339` lists `DestinationReview.svelte`.
The workspace route's handler (`:596-604`) and snippet (`:706-745`); `SurfaceActions.svelte`'s
(`:139-153`, `:241-256`). The `/dev/install/` probe calls `requestPage` / `confirmPage` /
`cancelPage` from three buttons (`:385-399`) and mounts no review.
`e2e/install.e2e.ts:2232` is the row-I title; `zona.seen(PAGE_SWITCH, "EXECUTE")`,
`zona.seen("HEARTBEAT", "EXECUTE")`, `writesOf(page)` and `beatUntilShows` are its instruments.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-02-01: `switchPage` on the store; the review's words retired; the fifth click is the select</name>
  <files>src/lib/device/install.svelte.ts, src/lib/device/page-target.ts, src/lib/device/install-copy.ts, src/lib/device/install-copy.spec.ts, src/lib/device/page-target.spec.ts, src/lib/device/install.spec.ts, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <behavior>
    - `install.spec.ts` (an existing target test extended, +0): `switchPage(3)` on a connected fake
      puts HEARTBEAT/EXECUTE then PAGEACTIVE/EXECUTE on the wire in that order, lands `switching`, and
      the fake's next report of 3 lands `reported` with `applyReady` true; `switchPage(reported)`
      sends nothing and returns false; `switchPage` while `writing` sends nothing and returns false.
    - `page-target.spec.ts` unchanged in its four titles (request/confirm ordering, the ACK gate, the
      window, the enumeration) — the class API does not move.
    - `install-copy.spec.ts` "the closed sets…": `WRITE_CLICKS` equals `[TRY_ON_LABEL, PUT_BACK_LABEL,
      KEEP_LABEL, CLEAR_LABEL, TARGET_CLICK]`, length 5, distinct 5, and `TARGET_CLICK === "Target"`
      equals `page-target.ts`'s `TARGET_LABEL` (read the other module in the spec, as the three
      `pageName` twins are pinned); `SWITCH_PAGE_LABEL` and `KEEP_PAGE_LABEL` are not exported.
  </behavior>
  <action>
    1. **`install.svelte.ts`**: add `async switchPage(page: number): Promise<boolean>` beside
       `requestPage`: `if (!this.requestPage(page)) return false; await this.confirmPage(); return
       true;` — `confirmPage` stays the one caller of `target.confirm(`, `requestPage` and
       `cancelPage` stay (the probe uses them; `cancelPage` is `unverified`'s programmatic way out).
       Rewrite the header's page-target paragraph (`:35-52`): a switch is a click — the select's
       change — and the review is gone by the user's word (cite 13.1 D-05); the heartbeat-first order
       and the ACK gate are stated as before. No other store line moves.
    2. **`page-target.ts`**: delete `switchReviewLine` and `replaceReviewTitle` and the comment block
       about the affirmative's labels (`:366-382`); rewrite the header's "THE SWITCH IS A CLICK"
       paragraph: `request()` sets the target and `confirm()` sends, and the install store's
       `switchPage` calls both from the select's change — no review, by the user's word; keep every
       other paragraph (the finding, the heartbeat, the ACK gate, the window, the enumeration). The
       class does not change.
    3. **`install-copy.ts`**: retire `SWITCH_PAGE_LABEL` and `KEEP_PAGE_LABEL` (delete the exports;
       name them in the header's retired list beside the honesty caps with the date and D-05); add
       `export const TARGET_CLICK = "Target";` with the comment that it is `page-target.ts`'s
       `TARGET_LABEL` twin because neither module may import the other (the `pageName` precedent),
       and that the fifth write click is the select's change; `WRITE_CLICKS`'s fifth member becomes
       `TARGET_CLICK`; the comment above it says five, the fifth a control that is a select.
    4. **The specs** as the behaviours above. In `install-copy.spec.ts`, wherever `SWITCH_PAGE_LABEL`
       was imported or iterated (`:59`, `:689-697`, the `Click X` walk at `:779-801`, the allowed
       set at `:1008`), substitute `TARGET_CLICK` and add the twin assertion against `TARGET_LABEL`.
    5. **`13.1-COPY-NEW.md`**: four rows under "Retired by 13.1-02 (D-05)": `SWITCH_PAGE_LABEL`,
       `KEEP_PAGE_LABEL`, `switchReviewLine`, `replaceReviewTitle` — each with the batch row it came
       from (I.3.8, I.3.11) and the reason; one row for `TARGET_CLICK` (not a visible string: a twin).
  </action>
  <verify>
    <automated>npx vitest run --project server src/lib/device/install.spec.ts src/lib/device/install-copy.spec.ts src/lib/device/page-target.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>`switchPage` proved on the fake in the frame order; `WRITE_CLICKS` five with the select's twin pinned equal to `TARGET_LABEL`; the four review strings gone from both modules and ledgered as retired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-02-02: The two routes switch on change; `DestinationReview.svelte` deleted; the review test inverted</name>
  <files>src/lib/ui/DestinationReview.svelte, src/routes/playground/[id]/+page.svelte, src/lib/ui/sandbox/SurfaceActions.svelte, src/routes/dev/install/+page.svelte, src/lib/ui/device-ui.spec.ts, src/lib/ui/instrument.spec.ts</files>
  <behavior>
    - `device-ui.spec.ts` "the destination review appears on first use…" becomes "no destination
      review: a change on the Target select is the whole switch, nothing under src/lib/ui/ mounts a
      review, and the store's one caller of the target's confirm() is confirmPage" — asserts: no file
      named `DestinationReview.svelte` on disk; `grep` over `src/lib/ui/**/*.svelte` and the two
      routes for `destination-review`, `<DestinationReview`, `switchReviewLine`, `replaceReviewTitle`
      → 0; the workspace route and `SurfaceActions.svelte` each contain `install.switchPage(value)`
      in their `onTargetChange`; neither contains `install.requestPage(`; the store contains exactly
      one `target.confirm(`; `page-target.ts` still contains `if (this.status !== "requested")
      return;`; the DONT_ASK / SKIP / REMEMBER needles still 0 in the store and the target (kept —
      there is nothing to remember now either).
    - `instrument.spec.ts`: the component list no longer names `DestinationReview.svelte`; whatever
      that row asserted (44px floor, no dialog) is asserted for nothing — the row goes.
  </behavior>
  <action>
    1. **Delete `src/lib/ui/DestinationReview.svelte`** (`git rm`).
    2. **The workspace route**: `onTargetChange` calls `void install.switchPage(value)` and, when it
       resolves false, snaps the select back to `targetValue` (the same snap as today). Delete
       `closeReview` and the `{#if install.pageStatus === "requested"}` branch — the snippet renders
       the switching line and the unverified line only; the `requested` status is never on screen
       (it exists for one microtask between `request()` and `confirm()`). Remove the
       `DestinationReview` import. Rewrite the snippet's comment and the header's "THE DESTINATION
       ZONE" paragraph: the select's change is the switch (13.1 D-05); nothing sends on open; Apply
       waits for the report. Keep `targetSelect` and the `aria-describedby` to the line.
    3. **`SurfaceActions.svelte`**: the same three edits in its destination half (`onTargetChange`,
       `closeReview` gone, the `requested` branch gone, the import gone; the header's paragraph
       rewritten).
    4. **The probe** (`/dev/install/`): keep its three buttons (they exercise `requestPage`,
       `confirmPage`, `cancelPage` directly, which the store still exposes); add a fourth, `Switch
       (one call)`, wired to `switchPage` so the probe can drive the shipped path on hardware; no
       review was ever mounted there.
    5. **`device-ui.spec.ts`**: invert the review test as the behaviour says — keep its title
       position, rewrite its title and body; delete the render block (there is no component to
       render) and the `import DestinationReview` at `:82`. **`instrument.spec.ts:339`**: remove the
       row.
    6. `npm run check` — the deleted component must leave no import behind.
  </action>
  <verify>
    <automated>npm run check && npx vitest run --project server src/lib/ui/device-ui.spec.ts src/lib/ui/instrument.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>No review anywhere under `src/lib/ui/` or the routes; both routes switch on change through `switchPage`; the inverted test is green and names what it forbids; `svelte-check` clean at 656 (provenance).</done>
</task>

<task type="auto">
  <name>Task 13.1-02-03: The row-I e2e title re-written; chunk c1; the count line; STATE; commits</name>
  <files>e2e/install.e2e.ts, .planning/STATE.md</files>
  <action>
    1. **`install.e2e.ts:2232`** becomes "the destination menu lists the pages the module reports and
       sends nothing on open; a change sends the heartbeat then exactly one switch with no review;
       Apply waits for the module's own report, and PUT BACK then names the new page" (the put-back
       clause is 13.1-06's to remove). Body: keep the menu block verbatim (four options, the reported
       one marked, opening sends nothing — `seenBefore()` all zero after `select.click()`); replace the
       two review blocks with: `await select.selectOption(String(TO))` → `destination` has
       `data-status="switching"` (never `requested` — assert it was not observed by polling the
       attribute once immediately after the change and accepting only `switching` or `reported`),
       the switching line reads `Switching to Page 4…` (`pageName(TO)`), `seenBefore()` is
       `{ switches: 1, heartbeats: 1, writes: 0 }`, the last two frames decode to
       `HEARTBEAT/EXECUTE` then `PAGEACTIVE/EXECUTE` (the existing `classOf`), `zona.state.activePage
       === TO`; keep the ACK-gate block (Apply, the primary, the select, put-back and clear disabled
       until the report), the report block (`beatUntilShows` to `reported`), the options re-marked,
       the put-back page line (until 13.1-06), and the final wire block. Delete the `review` locator
       and the "EVERY CHANGE reviews again" block. Add one assertion that `destination-review` has
       count 0 throughout.
    2. Also in the same file, the "nothing is written without a click" title (`:441`) — read it; if
       it opens the menu, add the sentence to its comment that a change is the click and opening is
       not; no assertion moves.
    3. `npm run build`; chunk **c1** on a fresh detached server; reds rerun alone and named.
    4. Prettier by path; `npm run lint`; `check-counts.mjs 94 961` on the quick suite (state the
       observed pair — this plan's projected term is `+0 / +0`; if the inverted test's render block's
       removal moved the count, report it as observed).
    5. STATE.md by script against a copy (one line for this plan). Three commits by `--only`: task 01
       (`feat(13.1-02): the switch is the select's change - …`), task 02 (`feat(13.1-02): the review
       retired by the user's word - …`), task 03 (`test(13.1-02): row I without a review …` plus STATE
       and the SUMMARY). No trailer.
  </action>
  <verify>
    <automated>npx playwright test e2e/install.e2e.ts --workers 3 --grep "sends nothing on open"</automated>
  </verify>
  <done>The re-written row-I title green in chromium; c1 green with reruns named; `check-counts.mjs 94 961` (or the observed pair, stated); three commits; ROADMAP byte-unchanged.</done>
</task>

</tasks>

<verification>
- `grep -rn "DestinationReview\|destination-review\|switchReviewLine\|replaceReviewTitle\|SWITCH_PAGE_LABEL\|KEEP_PAGE_LABEL" src e2e` → only the retired-list mentions in comments and the
  inverted test's needles.
- `install.spec.ts` test 4 (zero writes by class) green and unedited in its assertions.
- `page-target.spec.ts` four titles green and unedited.
- The allowlist empty; the six circles' files not in `git diff --stat`.
</verification>

<success_criteria>
- Bench line 5 answered: a change on the select switches the page; the heartbeat leads; the ACK gate
  and `unverified` unchanged; no review anywhere.
- SAFE-01's proof intact and stated: nothing on open, five clicks with the select the fifth.
- Term stated against the carried block; chunk c1 green.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-02-SUMMARY.md` with the
count line, the frame-order assertion's evidence from the fake, the retired strings, every rerun
named, and the trailer conflict reported.
</output>
