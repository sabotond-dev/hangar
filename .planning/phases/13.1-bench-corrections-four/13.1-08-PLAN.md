---
phase: 13.1-bench-corrections-four
plan: 08
type: execute
wave: 8
depends_on: ["13.1-07"]
# Execution order (13.1-PLAN-CHECK section 4): SERIAL - 02 -> 04 -> 01 -> 03 -> 05 -> 06 -> 07 -> 08.
# The gate runs last and asserts against every SUMMARY's observed count line.
files_modified:
  - docs/TESTING.md
  - docs/INSTALL-RUNBOOK.md
  - docs/HARDWARE-AUDITION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: false
requirements: [IDENT-01, CAT-01, BUILD-06, BUILD-07, SAFE-01, SAFE-03, SAFE-04, SAFE-05, SAFE-08, SAFE-09, TUNE-02, TUNE-03, TUNE-05, CONN-08]

must_haves:
  truths:
    - "Every count is re-measured on a clean tree against a fresh production build and stated as the carried block plus this phase's chain; every projection this phase's VALIDATION got wrong is named."
    - "The radius allowlist is EMPTY and that assertion is the proof; the six circles are ColourPicker.svelte:840, :867, :882 and Knob.svelte:730, :785, :807, measured square in both engines, neither file edited this phase."
    - "SAFE-03, SAFE-04 and SAFE-09 are amended by the user's word - Put back is gone (D-07) - with the old text kept; SAFE-01, SAFE-05, SAFE-08, TUNE-02, TUNE-03, TUNE-05, IDENT-01, CAT-01, BUILD-06, BUILD-07, CONN-08 are qualified by name; PROJECT.md's 'always recoverable' is named as falsified."
    - "The bench's four answered rows - I, L, H, M - are recorded verbatim under a dated heading in the runbook as the first hardware-verified rows of Phases 12, 12.1 and 13, and the rows that rested on them are qualified."
    - "The nine changes are handed to the user's eye - each by the page it lives on - with the page switch without a review and Clear tried on hardware, and the ten open questions listed."
  artifacts:
    - path: "docs/TESTING.md"
      provides: "A Phase 13.1 section appended whole: the eight-term chain from 13-20's block, the empty allowlist, the six circles, the deletions, where this phase's planner was wrong"
    - path: "docs/INSTALL-RUNBOOK.md"
      provides: "The four answered bench rows recorded verbatim; the nine changes as a new bench for the user"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SAFE-03/04/09 amended by the user's word; the qualifiers on the rows this phase moved"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md"
      via: "each amendment cites the decision (D-07) and the bench line it comes from"
      pattern: "SAFE-03"
---

<objective>
The phase gate, in 13-20's register but short: re-measure everything, assert the allowlist empty and
the six circles by file and line, run the e2e suite in chunks, amend the requirement rows Put back's
removal falsifies and qualify the rest by name, record the bench's four answered rows verbatim, and
hand the nine changes to the user's eye.

What this gate is for: the phase changed the one control that moves hardware and deleted six
components; the gate proves the tree still holds together, states which requirements the user's word
moved, and gives the user a fresh bench — the nine changes, plus the page switch without a review and
Clear on hardware. It is not a victory lap; a gate that names no planner error has not looked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@.planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
@.planning/phases/13.1-bench-corrections-four/13.1-01-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-02-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-03-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-04-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-05-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-06-SUMMARY.md
@.planning/phases/13.1-bench-corrections-four/13.1-07-SUMMARY.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-20-SUMMARY.md
@.planning/phases/13-gui-overhaul/deferred-items.md
@docs/TESTING.md
@docs/INSTALL-RUNBOOK.md
@docs/HARDWARE-AUDITION.md
@src/lib/ui/radius-allowlist.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** `npm run deploy` is not run. Sibling
  repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11`, `total_phases: 14`, `total_plans` and `percent: 100` unchanged; Phase 13.1
  recorded as 8 of 8 landed, gate landed, bench pending. `advance-plan`, `record-session`,
  `roadmap update-plan-progress` and `requirements mark-complete` are NOT run. `.planning/ROADMAP.md`
  is NOT edited (the row for whoever updates it goes in this SUMMARY).
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path;
  `.planning/REQUIREMENTS.md` left to its own formatting with `prettier --check` clean.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md`: a section inserted whole, a criterion extended by a dated clause with
  the old text kept — never a reflow.
- **No `border-radius` above zero; the allowlist EMPTY; exactly SIX circles by file and line;
  `Knob.svelte` and `ColourPicker.svelte` unedited across the whole phase (`git diff --stat` empty).**
- **CAT-04 stays `[ ]`.** `src/vendor/` untouched; the manifest unmoved; `firmware-oracle.spec.ts`,
  `protocol-pin.spec.ts` green and unedited.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs
  (`artifacts.e2e.ts` reads `HEAD`).
- **State every count as the carried block `94 / 961 (+1 todo) / 83 / 99 / check 657 / sweep 4 19 /
  allowlist 0` plus this phase's eight-term chain.** The sweep is run once to assert `4 19` unmoved.
</standing_rules>

<interfaces>
The carried block (13-20, observed at `24e2790`, unchanged by `bbe3265`): quick 94 / 961 (+1 todo) ·
e2e 83 titles / 99 runs · svelte-check 657 · sweep 4 19 · catalog 26 (8 + 18) · static/og 26 at
154,136 B · audition 28 · runbook A-M · radius allowlist 0 rows · six circles
`ColourPicker.svelte:840, :867, :882` and `Knob.svelte:730, :785, :807` · library 842 / 66 + 873 / 35
· manifest 6 / 38.
The chain this phase projects (13.1-VALIDATION, in EXECUTION order 02 04 01 03 05 06 07 08): tests
+0 +0 +1 +2 +0 −1 +1 +0 = +3 → 964; files +0 → 94; e2e titles +0 +0 +1 +1 +0 +0 +0 +0 = +2 → 85;
runs +0 +0 +1 +1 +0 +0 −1 +0 = +1 → 100; check 657 −1 (02) −4 +1 (06) −1 +1 (07) = 653
(provenance, not asserted). **The primary form is each SUMMARY's OBSERVED count line, chained; the
projection above is what the gate names as right or wrong** — where they differ, the SUMMARY wins
and the difference goes under "where this phase's planner was wrong" (the first draft of this
VALIDATION put 05 at +1 and 06 at −4 with `:1398` at 06 — I-01 — and its check row at 652).
The requirement rows: SAFE-03 (`:34`), SAFE-04 (`:35`), SAFE-05 (`:36`), SAFE-09 (`:40`), SAFE-01
(`:32`, `:193`), TUNE-03 (`:68`, `:217`), TUNE-05 (`:70`, `:219`), IDENT-01 (`:88`), CAT-01 (`:53`),
BUILD-06 (`:100`), BUILD-07 (`:101`), CONN-08 (`:28`). PROJECT.md:114 and CLAUDE.md:32's "always
recoverable" — named in this SUMMARY and in SAFE-03's qualifier as falsified by D-07; PROJECT.md is
not edited by an agent.
The runbook's four answered rows: bench lines 1-4 of `BENCH-2026-09-12.txt` are rows I, L, H, M
"working properly / works" — the first hardware-verified rows of Phases 12, 12.1 and 13.
`e2e-chunks-1320.sh` (scratchpad) is the chunk runner shape; copy it to this session's scratchpad as
`e2e-chunks-1308.sh`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.1-08-01: The re-measured block, the empty allowlist, the six circles, the e2e in chunks</name>
  <files>docs/TESTING.md</files>
  <action>
    1. On a clean tree, against `npm run build`: the quick suite three times at `--maxWorkers=2`
       piped to `check-counts.mjs <files> <carried+0>` — the carried pair is 13.1-07's observed count
       line (projected `94 / 964`), never a literal this plan invents — a fourth for the JSON;
       the sweep piped to `check-counts.mjs 4 19`; `npm run check`; `npm run lint`; the free memory
       beside each run. Record the eight-term chain from 13-20's block, every plan's term written out,
       and the reconciliation from the per-file JSON.
    2. The radius gate, three layers. Layer A (`radius.spec.ts`): `ALLOWLIST` is empty, the
       declarations across the built files all exempt or the six circles, the six named
       `ColourPicker.svelte:840, :867, :882, Knob.svelte:730, :785, :807`; **plus a source grep no
       layer performs (W-06): `grep -rnE "\brx=|\bry=" src --include=*.svelte` → nothing outside
       `src/lib/assets/favicon.svg`'s consumer (the favicon is square since J.1 and carries no `rx`
       either) — an SVG `rx` / `ry` is a rounded corner layer A cannot see, and 13.1-03 added three
       `<rect>` kinds to the Sandbox plate.** Layer B on the built
       stylesheets tolerating nothing. Layer C (`radius.e2e.ts`) STRICT over every route in both
       engines, including `/sandbox/?new` (the drag handles and the fuller marks) and `/playground/arc/`
       (the inline colour block). The six boxes measured square by a probe outside the suite in both
       engines (2 / 12 / 2 / 8 / 12 / 2), the width equal to the height. A planted `2px` caught by A,
       then B, then C (the negative check), restored from a copy with sha256 either side.
    3. Assert `git diff --stat 24e2790 -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte
       src/vendor/` is empty across the whole phase and state it. The manifest `dae35d39…` at 6 / 38;
       `firmware-oracle.spec.ts` and `protocol-pin.spec.ts` green and unedited.
    4. The e2e suite in five chunks, twice, on fresh detached servers stopped through PowerShell
       (`e2e-chunks-1308.sh`); every red rerun alone and named with its line (13-20's transients:
       `install:1434`, `install:1977`, `session:851`, `session:1009`, `browse:1404`,
       `browse-webkit:357`). `test-results/` removed after each run.
    5. `docs/TESTING.md`: append a Phase 13.1 section whole (no deletion; a "Corrections to the Phase
       13 section, by line" note if a count it stated moved): the block, the eight-term chain, the six
       deleted components (`DestinationReview`, `TryOnDevice`, `InstallState`, `KeepOnDevice`,
       `PutBack`, `BudgetMeter`) and the two added (`DestinationZone`, `MidiField`), the allowlist
       assertion, the six circles, the runs with memory, and "Where this phase's planner was wrong" —
       every term 13.1-VALIDATION projected that a SUMMARY observed differently, by name.
  </action>
  <verify>
    <automated>npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19 && git diff --stat 24e2790 -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte src/vendor/</automated>
  </verify>
  <done>The block re-measured and stated as a chain; the allowlist empty and the six circles square in both engines; the two circle files and src/vendor/ unedited; the suite green in chunks twice with reruns named; the Phase 13.1 section appended.</done>
</task>

<task type="auto">
  <name>Task 13.1-08-02: The requirements amended and qualified; the ledger closed</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <action>
    1. Amend, by a dated clause appended inside the criterion with the old text kept
       (`_(amended 2026-09-12 by plan 13.1-08, for plan 13.1-06 under 13.1-CONTEXT D-07, the user's
       "remove": the text above is kept as written and superseded — …)_`):
       - **SAFE-03**: "and `PUT BACK` restores them with one click at any time" — Put back is removed
         from the interface by the user's word; the snapshot is still taken at connect before any
         write and is still restorable through the store's `putBack()`, reachable now from the
         `/dev/install/` probe only; the way back to the user's own page is Grid Editor or a Clear to
         the firmware default. The criterion now reads: at connect, before any write, HANGAR snapshots
         the five strings (unchanged); the restore path exists and is proved on the fake and the probe
         (`install.spec.ts`), but no visitor-facing control offers it.
       - **SAFE-04**: "so `PUT BACK` survives a closed tab" — the record still survives a closed tab
         (`SAFE-04`'s mechanism is untouched); it is no longer surfaced as Put back.
       - **SAFE-09**: "with `PUT BACK` still offered" — a lost connection mid-write still ends in a
         named failure state with bounded retries; the step it offers is now Clear (the firmware
         default), Put back being gone.
    2. Add a `PHASE 13.1 GATE QUALIFIER, 2026-09-12 (plan 13.1-08)` on the traceability row of each,
       with PROVED / NOT PROVED, and on:
       - **SAFE-01**: five write clicks then four (Apply, Store, Clear, Target — the fifth was the
         select's change; Put back removed); `WRITE_CLICKS` is four, asserted equal to the labels;
         zero writes by class across connect, the snapshot, a switch on open; **NOT PROVED**: nothing
         on a module beyond the bench's rows I/H.
       - **TUNE-03**: the two meters are measured and HIDDEN by the user's word (D-10); the numbers
         live as `data-setup` / `data-timer`; the Sandbox's room line kept (question 3).
       - **TUNE-05**: the refusal — Apply disabled, the cause named, the one-click back-off, zero
         frames — stays as `BudgetMessage`'s line; "turns the offending meter red" no longer has a
         meter and is qualified.
       - **IDENT-01**: the Explore fill and face (D-02); the header gains a Clear control (D-04); no
         twelfth token; the allowlist still empty and the six circles by file and line; and — named
         here — PROJECT.md's "the module's original config is always recoverable" is FALSIFIED by
         D-07 (Grid Editor or a Clear is the way back), a Safety constraint the gate cannot edit but
         names for the user.
       - **CAT-01**, **BUILD-06**, **BUILD-07**, **CONN-08**: qualified as 13.1-VALIDATION's table says.
       - **TUNE-02** (W-17): its Phase 10 qualifier is about the live cost FORECAST (hover / focus,
         the ghost, `transition: none` while a forecast shows) — 13.1-07 retired the forecast's
         rendering with the meters (D-10); `cost()` is still the only measurement and `fit()` still
         has one call site (10-14's guard, unmoved); PROVED: every knob change still recompiles and
         re-simulates live; NOT PROVED: nothing new. Qualified by the user's word, not amended.
       - **SAFE-08** (W-17): the still-writing line lost its column with `InstallState.svelte` and
         was re-homed in the zone (13.1-06, `still-writing`); the live announcement
         (`LIVE_STILL_WRITING`) never moved. Qualified.
       - **SAFE-05**: qualified — the store's confirmation is unchanged; its home is the bar's zone.
    3. Assert every original line survives as a prefix (a script; 13-20's shape). CAT-04 untouched.
    4. `13.1-COPY-NEW.md`: close it — every row landed or retired named, the palette's two proposed
       colours and the channel-numbering question carried to the bench, a line saying the ledger is
       complete for the phase.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); for(const id of ['SAFE-03','SAFE-04','SAFE-09']) if(!s.includes(id+'**')) throw new Error(id); console.log('rows present')" && npx prettier --check .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>SAFE-03, SAFE-04 and SAFE-09 amended by the user's word with the old text kept; the twelve rows qualified by name; "always recoverable" named as falsified; every original line survives as a prefix; the ledger closed; CAT-04 still [ ].</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 13.1-08-03: The bench - the four answered rows recorded, the nine changes handed over</name>
  <what-built>
    The nine bench lines the user wrote at the fourth bench — ten decisions, D-06 and D-07 being one
    line — each in the tree: the intro fits the screen
    (D-01); the gallery's Explore buttons match the PDF (D-02); the Sandbox's handles resize by drag
    and each element has its own colour and a fuller look (D-03); a persistent Clear top right (D-04);
    the page switch on the select's change with no review (D-05); the install column gone and the bar
    reading Target / Apply to ZONA / Store on ZONA (D-06); Put back removed (D-07); Edit color inline
    (D-08); MIDI output as two typed fields (D-09); the budget meters hidden (D-10). Nothing was
    hardware-verified by an agent; the two probes and the four rows below were the user's.
  </what-built>
  <how-to-verify>
    First, record the four answered rows. Append to `docs/INSTALL-RUNBOOK.md` under a dated heading
    "The fourth bench, 2026-09-12 (the user's ZONA over `npm run dev`)": rows I, L, H, M with the
    user's words verbatim ("1) working properly. 2) working properly 3) works 4) works"), naming them
    the first hardware-verified rows of Phases 12, 12.1 and 13, and note that "working properly" for
    row L is taken as no jump at the wrap, a value held, the dead centre unbroken, 3x3 usable. Append
    one dated paragraph to `docs/HARDWARE-AUDITION.md` (the table unchanged) pointing at those rows.
    Then hand the nine changes to the user, by page:
    1. `/` at 1920x1080 and at a laptop size — the page does not scroll; on a phone it may.
    2. `/playground/` — the Explore buttons are grey, in the display face, the arrow on the same row.
    3. `/sandbox/?new` — place a fader, a button, a knob, an XY pad; each a different colour and a
       fuller look; drag a handle to resize; a too-small or overlapping drag is refused with a line.
    4. Every page — `Clear` top right beside the connection control; one click resets; disabled with
       its reason when not connected.
    5. Connect a ZONA; change the Target select — the page switches with no confirmation window; the
       heartbeat still leads and the switch still waits for the module's report. Then focus the
       closed select and press ArrowDown once (W-03): Chromium fires `change` on the arrow, so that
       press is a switch too — confirm the module moves one page and the select disables until it
       reports; say whether that is what you want from an arrow key.
    6. `/playground/arc/` — no block under the surface; the bar's right side reads Target · Apply to
       ZONA · Store on ZONA; no Put back anywhere.
    7. The inspector's `Edit color` opens the picker down, seamlessly, not a popup.
    8. MIDI output is two typed fields, CC number and Channel, with the helper line; a value the knob
       does not offer is refused inline.
    9. No code-limit meters under TUNING; an over-budget state still disables Apply and names the cause.
    Then answer the ten open questions in `13.1-CONTEXT.md` (Clear's label and gate, the Sandbox room
    line, the palette's two proposed colours, channel numbering on Lua entries, the MIDI field label
    on a base/send knob, the phone stack, unverified's way back, the way back after an Apply, the
    Explore fill).
  </how-to-verify>
  <resume-signal>Type "approved" or name the changes to redo and the questions' answers.</resume-signal>
</task>

</tasks>

<verification>
- `check-counts.mjs <files> <carried+0>` (13.1-07's observed pair; 964 projected) on the quick
  suite; the sweep `4 19`; the e2e suite green in five chunks twice at the carried runs (100
  projected, 85 titles).
- The allowlist empty; the six circles square in both engines; `git diff --stat 24e2790 --
  src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte src/vendor/` empty.
- SAFE-03, SAFE-04, SAFE-09 amended with the old text kept; the qualifiers present; CAT-04 `[ ]`.
- `git diff --quiet -- .planning/ROADMAP.md` exits 0.
</verification>

<success_criteria>
- The block re-measured and stated as a chain; the planner's wrong projections named.
- The requirements moved by the phase amended or qualified by name; "always recoverable" named as
  falsified for the user.
- The four answered bench rows recorded verbatim; the nine changes handed over; the ten questions asked.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-08-SUMMARY.md` with the
re-measured chain, the allowlist and six-circle assertions, the amendments and qualifiers by name,
the four bench rows recorded, the nine changes as handed over, the ROADMAP row for whoever updates
it (I-03: that row states the Put back question as ANSWERED — "remove", D-07 — and names the rows
this gate amended; it does NOT propose adding SAFE-03 / SAFE-04 to the entry's requirements line —
the orchestrator edits ROADMAP and decides its line), and the trailer conflict reported. Record the
phase as gate landed, bench pending.
</output>
