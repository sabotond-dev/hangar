---
phase: 13.1-bench-corrections-four
plan: 05
type: execute
wave: 5
depends_on: ["13.1-03"]
# Execution order (13.1-PLAN-CHECK section 4): SERIAL - 02 -> 04 -> 01 -> 03 -> 05 -> 06 -> 07 -> 08.
# In substance this plan depends on 13.1-02 (install-copy.ts, device-ui.spec.ts, install.e2e.ts,
# the workspace route); it follows 03 because the phase is serial on one tree.
files_modified:
  - src/lib/ui/Clear.svelte
  - src/lib/ui/shell/Header.svelte
  - src/routes/+layout.svelte
  - src/lib/device/install-copy.ts
  - src/routes/playground/[id]/+page.svelte
  - src/lib/device/install-copy.spec.ts
  - src/lib/ui/device-ui.spec.ts
  - src/lib/ui/shell.spec.ts
  - src/lib/ui/identity.spec.ts
  - e2e/install.e2e.ts
  - e2e/session.e2e.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [CONN-08, SAFE-01]

must_haves:
  truths:
    - "A `Clear` control sits at the top right of every page, left of the connection control, in both header variants, all the time (D-04, bench line 4)."
    - "One click resets the module's active page to the firmware default (A-45, kept); disabled with its reason as a visible caption when there is no session, no snapshot, or the browser cannot write; described by `clearLine(page)` when live."
    - "The workspace's column no longer carries a Clear — one control on the page, one test id."
    - "The box is 44px on both axes, square, in the eleven tokens; the header's row height does not grow."
  artifacts:
    - path: "src/lib/ui/Clear.svelte"
      provides: "The header's Clear control: the box, the label, the caption twins, the sr-only description"
    - path: "src/lib/ui/shell/Header.svelte"
      provides: "The connection zone renders the `clear` snippet before the `connection` snippet, 12px apart"
    - path: "src/lib/device/install-copy.ts"
      provides: "`CLEAR_LABEL = \"Clear\"` with section 9's long label retired by name and its fact kept in `clearLine`"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/ui/Clear.svelte"
      via: "the layout mounts <Clear /> into the header's clear snippet on every page"
      pattern: "<Clear"
    - from: "src/lib/ui/Clear.svelte"
      to: "src/lib/device/install.svelte.ts"
      via: "install.clearToDefault() on the click; install.clearReason(capable) for the caption"
      pattern: "clearToDefault"
---

<objective>
Bench line 4, D-04: a persistent CLEAR control top right, beside ZONA connected. The store's
`clearToDefault()` (12-03) re-homed from the column to the header; one click; the reason visible when
it cannot act.

Purpose: the user wants the firmware default one click away from anywhere, and with Put back leaving
(D-07, 13.1-06) Clear is the way back HANGAR keeps.
Output: `Clear.svelte` as a header control; the column's Clear removed; the label `Clear`; the
specs re-pinned; term **+0 / +0; +0 / +0** (one column test deleted — `device-ui.spec.ts:1398` —
and one header test added — `shell.spec.ts` 7). 13.1-VALIDATION's first draft put the `:1398`
deletion at 06 and this plan at +1; the plan's own task list is the truth (I-01) — name the
difference in the SUMMARY and the gate's "where the planner was wrong".

**Execution order.** Fifth in the serial order; the carried pair is 13.1-03's observed count line
(projected `94 / 964 / 85 / 101`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-11-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-18-BATCH.md
@.planning/phases/13-gui-overhaul/deferred-items.md
@src/lib/ui/Clear.svelte
@src/lib/ui/DeviceSlot.svelte
@src/lib/ui/shell/ConnectionControl.svelte
@src/lib/ui/shell/Header.svelte
@src/lib/ui/shell/layout.ts
@src/routes/+layout.svelte
@src/lib/device/install-copy.ts
@src/lib/device/install.svelte.ts
@src/lib/ui/device-ui.spec.ts
@src/lib/ui/shell.spec.ts
@e2e/install.e2e.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's.
- **No `border-radius` above zero; the allowlist stays EMPTY; the six circles unmoved; `Knob.svelte`
  and `ColourPicker.svelte` not edited.** The header's status dot is a radial gradient on a square
  box (13-05's pattern) if one is drawn — prefer none.
- **D-05's register; every new string ledgered in `13.1-COPY-NEW.md`**: `Clear` is a new label
  (the user's word, sentence case) — ledger it with the old label named as retired.
- **Every count a carried name plus a delta** from the block 13.1-03 left (projected `94 / 964 / 85 /
  101`; its observed pair wins). Term **+0 / +0; +0 / +0**; `check-counts.mjs <files> <carried+0>`,
  never a literal the plan invents (B-04: a literal `961` here fails under every order — the four plans
  before this one add three).
- **The sweep is not run.** `src/vendor/`, `library.ts`, the entries untouched.
- **Quick at `--maxWorkers=2`.** Playwright chunks **c1** (`install`, `session`) and **c2**
  (`browse`, `browse-webkit` — `browse-webkit.e2e.ts` measures the header at 375px for sideways
  scroll, and this plan puts a second 44px box beside the 218px slot inside a wrapping header; W-09)
  on fresh detached servers; reds rerun alone and named.
</standing_rules>

<interfaces>
From `src/lib/ui/Clear.svelte` (12-03 / 13-18; the column's quiet-tier control):
```ts
const capable = $derived(session.phase !== "unsupported" && session.phase !== "insecure");
const reason = $derived(install.clearReason(capable));            // ClearReason | undefined
const busy = $derived(install.phase === "writing" && install.action === "clear");
const pending = $derived(session.phase === "connected" && !install.applyReady);
const disabled = $derived(reason !== undefined || writing || pending);
const page = $derived(install.snapshotPage ?? 0);
// markup: <button data-testid="clear" aria-describedby="clear-line">{busy ? clearingLabel(page) : CLEAR_LABEL}</button>
//         <div id="clear-line" data-testid="clear-line"> the line and the three reason twins, one visible </div>
```
From `install-copy.ts`: `CLEAR_LABEL = "Reset active device page"` (section 9's), `clearingLabel(page)`
= `Resetting Page N…`, `clearLine(page)` = `Returns Page N to its firmware default. Your browser draft
stays as it is.`, `CLEAR_REASONS = { "no-snapshot": "Needs a copy of what is on your ZONA first.",
"no-session": "Needs your ZONA connected.", incapable: "This browser can’t write to a ZONA." }`.
`install-copy.spec.ts:428` and `:451` hold `CLEAR_LABEL` to section 9's words; `:633`, `:693` iterate it.
From `Header.svelte` (13-05): `{ variant, section, secondary, connection }: { connection?: Snippet }`;
`.connection { margin-inline-start: auto; min-inline-size: var(--connection-w); min-block-size: var(--connection-h); }`
(`CONNECTION_SLOT` 218 x 37; the control inside is 44px tall).
From `+layout.svelte`: the header is mounted with `{#snippet connection()}<ConnectionControl />{/snippet}`.
From `DeviceSlot.svelte` (the box beside which Clear sits): `.device-slot` with `.lines` holding a
`.caption` (`device-slot-caption`, aria-hidden) over a `.label` (`device-slot-label`) and an sr-only
description — the two-line box shape D-04 asks Clear to mirror.
`device-ui.spec.ts`: "the seven are listed and on disk…" (`:267`) lists `Clear.svelte`; "the CLEAR
cell is the measured arithmetic…" (`:1297`) and "CLEAR and KEEP ON DEVICE are shapeless alike…"
(`:1398`) are the column's tests. `shell.spec.ts` 1-6 render the header with `svelte/server`.
`e2e/install.e2e.ts`: `clearControl = getByTestId("clear")` (`:1212`); `:1977` (the CLEAR title,
@webkit) clicks it and reads `clear-line`; `:2489` (@webkit) reads it disabled with its reason;
`:2232` reads it disabled while switching. The workspace route's column renders `<Clear />` in
`.install-row` (`:1017`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-05-01: `Clear.svelte` as the header's control; the label; the column's Clear removed</name>
  <files>src/lib/ui/Clear.svelte, src/lib/ui/shell/Header.svelte, src/routes/+layout.svelte, src/lib/device/install-copy.ts, src/routes/playground/[id]/+page.svelte, src/lib/device/install-copy.spec.ts, src/lib/ui/device-ui.spec.ts, src/lib/ui/shell.spec.ts, src/lib/ui/identity.spec.ts, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <behavior>
    - `shell.spec.ts` 7 (+1): rendered with `svelte/server`, the header in both variants carries the
      `clear` snippet inside `shell-connection` BEFORE the connection snippet; `+layout.svelte` mounts
      `<Clear />` in that snippet once; `Header.svelte`'s `.connection` declares `gap: 12px`.
    - `device-ui.spec.ts` "the CLEAR cell…" (`:1297`) rewritten as "the header's Clear: a 44px box in
      the connection zone, one click and no confirmation (A-45 kept by the user's word, 13.1 D-04),
      the reason as the caption when disabled, clearLine as the description when live, the busy label
      through its one leg, nothing animates": renders `Clear` with the install singleton's mirror set
      by hand for the three reasons and for live, asserts the caption text equals the reason /
      is empty when live, the sr-only description equals `clearLine(page)`, `aria-describedby`
      present, `disabled` on the three reasons and on `pending`, no `type="checkbox"`, no
      `ClearConfirm`, `border-radius` 0 times, the `.control` rule declares `min-block-size: 44px`
      and `min-inline-size: 44px`, no `transition` / `animation`.
    - `device-ui.spec.ts` "CLEAR and KEEP ON DEVICE are shapeless alike…" (`:1398`) **deleted by
      title** — its subject was the column's pair; the A-45 clause lives in the rewritten test.
    - `install-copy.spec.ts`: `CLEAR_LABEL` equals `"Clear"` (the user's word, D-04) and section 9's
      `Reset active device page` is asserted absent from the module's exports; `:451`'s citation of
      section 9 moves to `clearLine` (which still carries the fact); `WRITE_CLICKS` still holds it.
    - `identity.spec.ts` test 5 (the control classifier) finds the new control's boundary on
      `--color-boundary`.
  </behavior>
  <action>
    1. **`install-copy.ts`**: `CLEAR_LABEL = "Clear"`; the comment: the user's word (13.1 D-04, bench
       line 4) in D-05's case; section 9's `Reset active device page` retired by name here and in
       the header's retired list; the fact — the firmware default, the draft untouched — is
       `clearLine`'s. `clearingLabel` unchanged (`Resetting Page 2…`).
    2. **`Clear.svelte`** rewritten for the header (the same script — `reason`, `busy`, `pending`,
       `disabled`, `held`, `shown`, `page`, `clear()` — kept verbatim; the markup and the style
       replaced): a `<div class="clear" data-testid="clear-control">` holding one `<button
       data-testid="clear" class="control">` shaped as `DeviceSlot`'s box — 44px on both axes,
       `padding-inline: 16px`, `border: 1px solid var(--color-boundary)`, transparent fill, the label
       in `--font-sans` 13px 600 and, beneath it inside the box, a `.caption` line (11px,
       `--color-ink-quiet`, `aria-hidden`) that reads the shown reason when disabled and nothing when
       live (an empty caption keeps the box's height — the two-line shape is fixed, as DeviceSlot's
       is) — plus an sr-only `<span id="clear-line">` carrying `clearLine(page)` when live and the
       reason when not, referenced by `aria-describedby`. `disabled` on the button as today; hover
       moves the boundary to `--color-action`; `aria-busy` while busy with `clearingLabel(page)` as
       the label. Rewrite the header comment: the control's home is the header by the user's word;
       one click, A-45 kept and I.6.5 declined (13.1 D-04); the reason visible in the caption; no
       radius; no confirmation. `data-testid="clear-line"` stays on the sr-only span so the e2e
       `visibleLine`/`clear-line` reads keep a subject (adjust them in task 02 if `visibleLine`
       filters on visibility).
    3. **`Header.svelte`**: a new `clear?: Snippet` prop rendered inside `.connection` before the
       connection snippet; `.connection` gains `gap: 12px`; the header comment names the pair (the
       PDF's box and the user's Clear beside it). **`+layout.svelte`**: `{#snippet clear()}<Clear
       />{/snippet}` handed to the header on every page and both variants, beside the existing
       connection snippet; import `Clear` from `$lib/ui/Clear.svelte`.
    4. **The workspace route**: delete `<Clear />` from `.install-row` and its import; the row's
       comment says the reset lives in the header since 13.1-05. (The column's remainder is
       13.1-06's.)
    5. **The specs** as the behaviours say; `device-ui.spec.ts` "the seven are listed and on disk"
       keeps `Clear.svelte` (it is on disk and mounted). `13.1-COPY-NEW.md`: one row for `Clear`
       (new label; old retired; the fact in `clearLine`).
    6. `npm run check`; `radius.spec.ts` A on the source.
  </action>
  <verify>
    <automated>npm run check && npx vitest run --project server src/lib/ui/device-ui.spec.ts src/lib/ui/shell.spec.ts src/lib/ui/identity.spec.ts src/lib/device/install-copy.spec.ts src/lib/ui/radius.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>The header shows `Clear` left of the connection control on every route in the served build; the column has no Clear; one `clear` test id on the page; the specs green at the same count.</done>
</task>

<task type="auto">
  <name>Task 13.1-05-02: The CLEAR e2e titles read the header; chunk c1; STATE; commits</name>
  <files>e2e/install.e2e.ts, e2e/session.e2e.ts, .planning/STATE.md</files>
  <action>
    1. **`install.e2e.ts:1977`** ("@webkit CLEAR sends on the click with no confirmation, the panel
       reads FACTORY DEFAULT, and PUT BACK brings the visitor's own back @webkit"): the control is the
       header's — `clearControl` unchanged as a locator; the `clear-line` reads move to the sr-only
       span's text (`toHaveText`, not `visibleLine`, with a comment) or to the caption
       (`clear-caption`, the visible reason) as each assertion means; the `next-caption` /
       column assertions stay until 13.1-06; the title's "the panel reads FACTORY DEFAULT" clause is
       the bar's device clause and the block (both still render) — leave; the PUT BACK half is
       13.1-06's. **`:2489`** (@webkit, disabled with its reason where PUT BACK is absent): the reason
       is the caption now — assert `clear-caption` reads `CLEAR_REASONS.incapable` and the button
       is disabled. **`:2232`**'s `clearControl(page)` disabled-while-switching assertion unchanged.
       Every `toHaveText(CLEAR_LABEL)` now reads `Clear` through the import.
    2. **`session.e2e.ts`**: any title that counts controls in the header's connection zone or reads
       `shell-connection`'s children (grep `shell-connection`, `connection-control`) — extend the
       expectation by the Clear box where a count is asserted; nothing else.
    3. `npm run build`; chunks **c1** and **c2** on fresh detached servers; reds rerun alone and named
       (`install:1977` on the phone engine is 13-20's known margin, A.21; `browse-webkit:357` its
       scroll transient). Measure and state the header's width at 375px with the Clear box beside the
       connection control — no sideways scroll (`browse-webkit.e2e.ts`'s own assertion) — and the
       header's row height at 1280 before and after.
    4. Prettier by path; `check-counts.mjs 94 <carried+0>` (13.1-03's observed pair); `npm run lint`.
    5. STATE by script against a copy (one line). Two commits by `--only`: task 01 (`feat(13.1-05):
       Clear in the header, one click, the reason as its caption - …`) and task 02 (`test(13.1-05):
       the CLEAR titles read the header …` with STATE and the SUMMARY). No trailer.
  </action>
  <verify>
    <automated>npx playwright test e2e/install.e2e.ts --workers 3 --grep "CLEAR"</automated>
  </verify>
  <done>The three CLEAR-reading titles green in both projects (reruns named); c1 and c2 green; `check-counts.mjs 94 <carried+0>` matches; two commits.</done>
</task>

</tasks>

<verification>
- `grep -rn "Reset active device page" src e2e` → only the retired-list comment and the spec's
  absence assertion.
- Exactly one element with `data-testid="clear"` on `/playground/arc/` and on `/sandbox/?new` in the
  served build (a scratch Playwright count).
- The allowlist empty; the six circles' files not in `git diff --stat`; the eleven tokens hold.
</verification>

<success_criteria>
- Bench line 4 answered: a persistent `Clear` top right beside the connection control, one click,
  disabled with its reason otherwise.
- I.6.5's confirmation declined and recorded (13.1-CONTEXT question 2 for the bench).
- Term `+0 / +0; +0 / +0` stated against 13.1-03's observed pair; c1 and c2 green; the 375px header
  width stated.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-05-SUMMARY.md` with the
count line, the header's measured row height before and after (it must not grow), the deleted test
named by title, every rerun named, and the trailer conflict reported.
</output>
