---
phase: 13.1-bench-corrections-four
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ui/sandbox/SurfaceEditor.svelte
  - src/lib/sandbox/editor.ts
  - src/lib/sandbox/copy.ts
  - src/lib/ui/shell/layout.ts
  - src/routes/sandbox/[draftId]/+page.svelte
  - src/lib/ui/sandbox-ui.spec.ts
  - src/lib/sandbox/emit.spec.ts
  - e2e/sandbox.e2e.ts
  - .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md
  - .planning/STATE.md
autonomous: true
requirements: [BUILD-06, BUILD-07]

must_haves:
  truths:
    - "Dragging any of a selected element's eight handles resizes it: the proposed bounds follow the pointer, snapped to the lattice, and the release commits through the editor; a box the rules refuse leaves the element as it was and the status line says why (D-03, bench line 3)."
    - "A drag is one undoable entry; Play still hides the handles; the numeric fields and the keyboard route are unchanged — no drag is ever required (13-16's rule, kept)."
    - "Each new element takes the next colour of a four-entry palette, so a surface of four kinds shows four colours; the picker still recolours any of them."
    - "A region draws as PDF page 3 draws it: a denser tint, the 1px boundary, the name, and the kind's own mark — a fader's track and thumb, an XY pad's crosshair and dot, a button's OFF chip, a knob's circle with its pointer."
    - "No radius, no new token, the Knob's circle still an SVG <circle>, the cost of every surface unmoved at the picker corner."
  artifacts:
    - path: "src/lib/sandbox/editor.ts"
      provides: "`resizeSelectedTo(box): Problem | undefined` through `applyEdit` with kind `resize`; `PALETTE` (four RGB444 triples) and `nextColour()` cycling by creation order"
    - path: "src/lib/ui/sandbox/SurfaceEditor.svelte"
      provides: "Handle pointer handling (down on a handle starts a drag; move draws the proposal; up commits), per-kind marks, the denser tint"
    - path: "e2e/sandbox.e2e.ts"
      provides: "One new title: a handle drag resizes through the editor, a refused drag leaves the region as it was, Undo takes it back"
  key_links:
    - from: "src/lib/ui/sandbox/SurfaceEditor.svelte"
      to: "src/lib/sandbox/editor.ts"
      via: "onresize(box) -> editor.resizeSelectedTo(box) -> applyEdit(…, 'resize')"
      pattern: "resizeSelectedTo"
    - from: "src/lib/sandbox/editor.ts"
      to: "src/lib/sandbox/geometry.ts"
      via: "every drag commit is an applyEdit; a refusal returns the previous surface"
      pattern: "applyEdit\\("
---

<objective>
Bench line 3, D-03: the Sandbox's handles resize by drag, each element has its own colour, and the
surface reads fuller — PDF page 3's look rather than 13-16's minimum.

Purpose: the handles the user sees promise a drag the tree never built; the surface reads as a
wireframe where the PDF reads as an instrument.
Output: `resizeSelectedTo` and the palette on the editor; the plate's drag; the per-kind marks; two
unit tests (+2), one e2e title (+1 / +1); the palette's proposed third and fourth colours ledgered
for the bench row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-bench-corrections-four/13.1-CONTEXT.md
@.planning/phases/13.1-bench-corrections-four/13.1-VALIDATION.md
@BENCH-2026-09-12.txt
@.planning/phases/13-gui-overhaul/13-CONTEXT.md
@.planning/phases/13-gui-overhaul/13-16-SUMMARY.md
@.planning/phases/13-gui-overhaul/13-17-SUMMARY.md
@.planning/phases/13-gui-overhaul/bible/HANGAR-ZONA-GUI-design-specification.md
@src/lib/ui/sandbox/SurfaceEditor.svelte
@src/lib/sandbox/editor.ts
@src/lib/sandbox/geometry.ts
@src/lib/sandbox/history.ts
@src/lib/sandbox/model.ts
@src/lib/sandbox/copy.ts
@src/lib/ui/shell/layout.ts
@src/lib/ui/sandbox-ui.spec.ts
@e2e/sandbox.e2e.ts

<standing_rules>
- **Every agent on Opus.** No Claude or Anthropic attribution anywhere and no co-author trailer —
  the harness asks for one; the repository's rule wins and the SUMMARY reports the conflict. No emojis.
- **No agent connects to, writes to, or deploys to a device.** Sibling repositories are read-only.
- **No `git checkout`, `git restore`, `git stash`, `git clean`.** Scratch copies with sha256 either side.
- **No `gsd-tools state` commands.** STATE.md by a script against a copy, asserting `status: executing`,
  `completed_phases: 11` and `percent: 100` unchanged. `.planning/ROADMAP.md` is not edited.
  `requirements mark-complete` is not run; **CAT-04 stays `[ ]`**.
- **Never `prettier --write .`** — prettier on the files this plan wrote, by path.
- **Commit with `git commit --only <paths> -F <msgfile>`.** No push. No commit while a chunk runs.
- **Append-only** on `docs/TESTING.md`, `docs/INSTALL-RUNBOOK.md`, `docs/HARDWARE-AUDITION.md`,
  `.planning/REQUIREMENTS.md` — none is this plan's.
- **No `border-radius` above zero; the allowlist stays EMPTY; the six circles unmoved; `Knob.svelte`
  and `ColourPicker.svelte` not edited.** The Sandbox Knob's circle is an SVG `<circle>` (13-16) and
  stays one; `SurfaceEditor.svelte` declares no radius.
- **D-05's register; every new string ledgered in `13.1-COPY-NEW.md`** (append; create with the
  register header if this plan runs first).
- **Every count a carried name plus a delta** from `94 / 961 (+1 todo) / 83 / 99`. Term **+0 / +2;
  +1 / +1**.
- **The sweep is not run.** `src/vendor/`, `library.ts`, the entries untouched. `emit.spec.ts` 1's
  five costs must read the same (the palette is measured at the corner already).
- **Quick at `--maxWorkers=2`.** Playwright chunks **c4** (`catalog`, `fidelity`, `first-experience`,
  `library`, `sandbox`) and **c5** (`artifacts`, `radius`, `skeleton`, `smoke` — `radius.e2e.ts` counts
  the eight handles and measures the Sandbox's route) on fresh detached servers; reds rerun alone.
</standing_rules>

<interfaces>
From `src/lib/sandbox/editor.ts` (13-16):
```ts
export const DEFAULT_SIZES: Record<ElementKind, { w: number; h: number }>; // fader 2x6, button 2x2, knob 3x3, xy 3x3
export const DEFAULT_COLOUR: readonly [number, number, number] = [13, 15, 7]; // #DCFF71 on RGB444
export type EditorState = { surface; selectedId; selected; mode; placement; focus; fields; … };
export class SandboxEditor {
  private applyPatch(patch: Partial<Omit<Region, "id">>, kind: EditKind, key?: string): Problem | undefined; // :543
  editNumber(field: NumericField, text: string): boolean;  // w/h go through applyPatch(…, "resize", fieldKey(id, field))
  private newRegion(kind, box, orientation?, name?): Region; // :366 — colour: [...DEFAULT_COLOUR]
  private record(kind, before, after, id, key?): void;
  setMode; select; undo; redo; remove; duplicate …
}
```
From `src/lib/sandbox/geometry.ts`: `applyEdit(surface, id, patch, rules): { ok: true; surface } |
{ ok: false; problem: { field?, message } }` — off-surface, overlap (section 16's line), tooSmall
(a knob under 3 x 3, a fader under 2 on its axis, an XY pad under 2 x 2) all refuse and return the
previous surface. `history.ts`: `record()` with an optional coalesce key; `seal()`.
From `src/lib/ui/sandbox/SurfaceEditor.svelte` (13-16): props `view, onclick(col,row), onmove,
onmark, oncancel, ondelete, onfinger, preview`; `cellOf(event)` maps a pointer to a cell; `proposed`
draws the dashed rect from `hover ?? view.focus`; `handles` are eight `[x, y]` pairs (corners and
edge midpoints) drawn as `<rect class="handle" data-testid="surface-handle">` at `SANDBOX_HANDLE`
(8px); `.region .body { fill-opacity: 0.18 }`; the marks are 1px lines at `stroke-opacity: 0.7`;
the name is an 11px uppercase `<text>` at the top-left.
From `layout.ts`: `SANDBOX_PLATE 571`, `SANDBOX_PITCH 571/9`, `SANDBOX_HANDLE 8`, `SANDBOX_LABEL_SIZE 11`.
`sandbox-ui.spec.ts` test 2 (`:214`) asserts no `/pointer|drag|hover/i` method on the prototype and
that `onpointermove` never calls `onclick`; tests 4 and 6 count handles (8 in Edit, 0 in Play) and
walk the history. `sandbox.e2e.ts:105` places by clicks; `:142`, `:275`, `:336` count handles.
The PDF's page 3 (read it): Filter lime with a wide thumb and `74`; Space teal with a crosshair,
a dot and `X 0.62 Y 0.47`; Hold with an `OFF` chip; Texture with a circle and pointer, the name
centred beneath; the tints visibly denser than 0.18 and the boundary 1px.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-03-01: `resizeSelectedTo` and the palette on the editor</name>
  <files>src/lib/sandbox/editor.ts, src/lib/ui/sandbox-ui.spec.ts, src/lib/sandbox/emit.spec.ts, .planning/phases/13.1-bench-corrections-four/13.1-COPY-NEW.md</files>
  <behavior>
    - `sandbox-ui.spec.ts` 7 (+1): on a surface with a fader at (0,0) 2x6, `resizeSelectedTo({col:0,
      row:0, w:3, h:6})` returns undefined and the region is 3x6, one history entry (undo restores
      2x6, redo re-applies); `resizeSelectedTo` with a box overlapping a second region returns the
      overlap problem and the surface is the same object; a knob resized to 2x2 returns the
      tooSmall problem; in Play it returns undefined and changes nothing; with nothing selected it
      returns undefined and changes nothing.
    - `sandbox-ui.spec.ts` 8 (+1): four elements placed in order carry `PALETTE[0..3]`; a fifth
      carries `PALETTE[0]`; a deleted-then-placed element continues the cycle (the counter is
      creation order, not the live count); `PALETTE[0]` equals `DEFAULT_COLOUR`; every triple is on
      the RGB444 lattice (0-15 each) and the four are distinct.
    - `sandbox-ui.spec.ts` 2's prototype clause inverted: the editor exposes exactly one method whose
      name starts `resize` and none matching `/pointer|hover/i`; the plate's `onpointermove` never
      calls `onclick` (kept).
    - `emit.spec.ts` 1: the five costs unchanged (the spec reads them; run it).
  </behavior>
  <action>
    1. **`editor.ts`**: add `export const PALETTE: readonly (readonly [number, number, number])[]`
       — `[13, 15, 7]` (the action colour, `DEFAULT_COLOUR`, the PDF's Filter), the PDF's Space teal
       measured off page 3's boundary stroke and snapped to the lattice (start from `[3, 11, 11]`,
       `51,187,187`, and correct it to the measurement), and two proposed in the same key — amber
       `[15, 10, 3]` and violet `[10, 7, 15]` — with a comment saying the last two are proposed and
       the bench row asks (13.1-CONTEXT question 4). `newRegion` takes `PALETTE[this.minted %
       PALETTE.length]` (the mint counter already advances per creation). Keep `DEFAULT_COLOUR`
       exported and equal to `PALETTE[0]` (the template and the tests read it).
    2. **`resizeSelectedTo(box: { col; row; w; h }): Problem | undefined`**: refuses silently in
       Play or with nothing selected; otherwise `applyPatch({ col, row, w, h }, "resize", dragKey(id))`
       then `history.seal()` so one drag is one entry even when `applyPatch` coalesces; returns the
       problem. Column and row arrive zero-based (the plate's own cells; the numeric fields' one-based
       door is theirs).
    3. Write the specs as the behaviours say; add the palette's four rows to `13.1-COPY-NEW.md` under
       "Not strings: the palette (13.1-03, D-03)" so the gate's batch carries the question.
  </action>
  <verify>
    <automated>npx vitest run --project server src/lib/ui/sandbox-ui.spec.ts src/lib/sandbox/emit.spec.ts src/lib/sandbox/geometry.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>Two new tests green; test 2's clause inverted and green; `emit.spec.ts` 1's five costs byte-identical to 13-20's (345 / 460 / 588 / 748 / 882).</done>
</task>

<task type="auto">
  <name>Task 13.1-03-02: The plate — handle drags, the proposal that follows, the fuller marks</name>
  <files>src/lib/ui/sandbox/SurfaceEditor.svelte, src/lib/ui/shell/layout.ts, src/routes/sandbox/[draftId]/+page.svelte, src/lib/sandbox/copy.ts</files>
  <action>
    1. **The drag.** Each handle `<rect>` gains `data-handle` (`nw n ne w e sw s se` in the existing
       order), `pointer-events: all`, the matching resize cursor, and a larger invisible hit rect
       beneath it (`SANDBOX_HANDLE_HIT`, 16px, added to `layout.ts` with the reason: 8px is the PDF's
       drawn size, a fingertip's target is 44 but a plate's cell pitch is 63 and a 16px hit square
       leaves the cell clickable). A `pointerdown` on a handle: `stopPropagation()` (the plate's
       click-to-select must not fire), capture the pointer on the plate, record `drag = { handle,
       anchor }` where `anchor` is the opposite corner or edge of the selected region. On
       `pointermove` while `drag`: the cell under the pointer through `cellOf`, the new box from the
       anchor and that cell (a corner handle moves two edges, an edge handle one), clamped inside the
       surface and never below 1 x 1; set `dragBox`, and `proposed` renders `dragBox` when it is
       set (the existing dashed rect). On `pointerup` / `pointercancel`: `onresize(dragBox)` — a new
       prop `onresize: (box) => Problem | undefined` the route wires to `editor.resizeSelectedTo` —
       and keep the returned message in a local `refused` that the status line shows (role="status",
       so it is announced) until the next pointerdown or keydown; clear `drag` and `dragBox`. A drag
       that ends on the same box commits nothing. In Play the handles are not rendered (unchanged).
       Update the header: "NO DRAG IS EVER REQUIRED" stays true and is restated — the numeric fields
       and the keyboard are the complete routes; a handle drag is the third, and it commits through
       the same `applyEdit` on release (section 8: show proposed bounds before committing).
    2. **Own colour.** Nothing to do on the plate — `fillOf(r)` reads the region's colour; the
       palette lands at creation (task 01).
    3. **The fuller look, read off PDF page 3.** `.region .body { fill-opacity }` to the measured
       value (sample the PDF's Filter over the workspace ground; expect roughly 0.30-0.36; record the
       sample); the marks at `stroke-opacity: 1` and `stroke-width: 2` for the primary mark; per kind:
       **fader** — the track line as today plus a thumb (a filled `<rect>` in the region's colour,
       about 0.6 of the region's width by `PITCH * 0.22`, centred on the track at its midpoint) and
       the CC number as an 11px numeral (`--font-mono`, tabular) centred below the track (the PDF's
       `74` — it is the region's `cc`); **XY pad** — the crosshair as today plus a filled circle
       (`<circle>`, `PITCH * 0.12`) at the centre and an `X 0.50 Y 0.50` 11px numeral at the bottom
       left (the PDF's readout at rest; a static mark in Edit — the live picture in Play is the
       route's canvas); **button** — the name centred, and beneath it a chip: a `<rect>` outlined in
       the region's colour with `OFF` in 11px uppercase inside (the PDF's `OFF`; at rest a button is
       off whether it latches or not — say so in the comment); **knob** — the circle as today at
       `stroke-width: 2`, the pointer to twelve o'clock, and the name centred BELOW the circle
       rather than top-left. The selected region's name stays the action colour. Every numeral and
       chip word is a mark, `aria-hidden` with the SVG (unchanged), and no string is ledgered — `OFF`
       and the readout form are the PDF's.
    4. **The route** wires `onresize={(box) => editor.resizeSelectedTo(box)}` and nothing else.
       `copy.ts`: no new string unless the status line needs one for "a drag was refused" — it does
       not: `applyEdit`'s own message is shown (the overlap line, the tooSmall lines, the
       off-surface line).
    5. `grep -n border-radius src/lib/ui/sandbox/SurfaceEditor.svelte` → none. `radius.spec.ts` A on
       the source.
  </action>
  <verify>
    <automated>npm run check && npm run lint && npx vitest run --project server src/lib/ui/sandbox-ui.spec.ts src/lib/ui/radius.spec.ts --maxWorkers=2</automated>
  </verify>
  <done>A handle drag on the served page resizes; a refused drag leaves the region and names why in the status line; the four kinds draw their marks; no radius; the handle count is still eight in Edit and zero in Play.</done>
</task>

<task type="auto">
  <name>Task 13.1-03-03: The drag e2e title; chunks c4 and c5; the count line; STATE; commits</name>
  <files>e2e/sandbox.e2e.ts, .planning/STATE.md</files>
  <action>
    1. **`sandbox.e2e.ts`** gains one title beside `:105` — "a handle drag resizes an element through
       the editor, a refused drag leaves it as it was, and Undo takes the drag back": open a fresh
       surface, place a fader by the palette and one click (the existing gesture), read `field-w` =
       2; take the `se` handle's box (`[data-testid="surface-handle"][data-handle="se"]`),
       `page.mouse.move` to its centre, `down`, move in three steps to the centre of the cell one
       column right and two rows down of the current far corner, `up`; assert `field-w` = 3,
       `field-h` = 8 (or the arithmetic the default 2x6 at the placed cell gives — compute it in the
       test from the placed cell), the region's `<rect class="body">` width grew, the handle count is
       8; place a button two columns right so a drag would overlap it, drag the fader's `e` handle
       onto it, assert the status line contains `overlaps` (section 16's line through `geometry.ts`)
       and `field-w` unchanged; click `Undo` once, assert `field-w` = 2 and `field-h` = 6 (one entry
       for the whole drag). Chromium only (a mouse drag; the phone project is not asked to drag —
       13-16's rule that no drag is required is what the phone relies on).
    2. Keep `:105`'s title and body — "never a drag" is still true of placement.
    3. `npm run build`; chunks **c4** and **c5** on fresh detached servers; reds rerun alone and named
       (`radius.e2e.ts` measures `/sandbox/?new` with a placed knob and the popover — unchanged here).
    4. Prettier by path; `check-counts.mjs 94 963`; `npm run check`, `npm run lint`.
    5. STATE by script against a copy (one line). Three commits by `--only` (task 01
       `feat(13.1-03): resizeSelectedTo and the palette …`, task 02 `feat(13.1-03): the handles drag,
       the marks as page 3 draws them …`, task 03 `test(13.1-03): a drag on the served page …` with
       STATE and the SUMMARY). No trailer.
  </action>
  <verify>
    <automated>npm run test:quick 2>&1 | node scripts/check-counts.mjs 94 963 && npx playwright test e2e/sandbox.e2e.ts --workers 3</automated>
  </verify>
  <done>The drag title green; c4 and c5 green with reruns named; `check-counts.mjs 94 963` matches; `--list` +1; three commits.</done>
</task>

</tasks>

<verification>
- `sandbox-ui.spec.ts` at 8 titles, test 2's inverted clause green; `emit.spec.ts` 1 unchanged.
- `radius.spec.ts` A and B green after the build; `radius.e2e.ts` C green on `/sandbox/?new`.
- `git diff --stat -- src/lib/ui/Knob.svelte src/lib/ui/ColourPicker.svelte src/vendor/` empty.
- The PDF samples (the tint, the teal) recorded in the SUMMARY with the values chosen.
</verification>

<success_criteria>
- Bench line 3 answered: the eight handles resize by drag; four colours on four elements; the
  surface reads as page 3.
- 13-16's rule kept and restated: no drag is required.
- Term `+0 / +2; +1 / +1` stated against the carried block.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-bench-corrections-four/13.1-03-SUMMARY.md` with the
count line, the PDF samples, the palette's four triples with the two proposed named as proposed, the
drag's arithmetic, every rerun named, and the trailer conflict reported.
</output>
