---
phase: 11-bench-corrections
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - src/lib/catalog/entries/euclid.ts
  - src/lib/catalog/entries/sonar.ts
  - src/lib/catalog/entries/steps.ts
  - src/lib/catalog/entries/morph.ts
  - src/lib/sim/lua-smoke.spec.ts
  - src/lib/catalog/frames.json
autonomous: true
requirements: [CONT-02, TUNE-05]

must_haves:
  truths:
    - "A finger swiped across EUCLID, SONAR or STEPS arms every cell it crosses, once each, instead of nothing at all."
    - "A finger resting inside one cell changes it once and not a hundred times a second."
    - "SONAR's centre is lit whatever else is happening, and its notes stop instead of hanging."
    - "Clock sync is named as not built, with both of its blockers stated, rather than quietly dropped from a phase that touched every entry it would have affected."
    - "MORPH sends nothing when a corner weight has not changed, and a corner sitting at zero produces no traffic at all - which is the second clause of a bench note whose first and third clauses were delivered without it."
  artifacts:
    - path: "src/lib/catalog/entries/euclid.ts"
      provides: "MOVE accepted with a per-contact last-cell guard"
      contains: "s.q"
    - path: "src/lib/sim/lua-smoke.spec.ts"
      provides: "A swipe toggles once per cell crossed, asserted as a count; and a MORPH drag emits only the corners whose weight moved"
    - path: "src/lib/catalog/entries/morph.ts"
      provides: "A per-contact last-sent guard on the four corner weights"
      contains: "self.p"
  key_links:
    - from: "src/lib/catalog/entries/euclid.ts"
      to: "src/lib/catalog/touch-guard.spec.ts"
      via: "the per-contact clear is written in the 11-02 convention, not re-derived"
      pattern: "e<9"
---

<objective>
Three entries the user could not draw on.

> EUCLID: *"doesn't sense the finger its really difficult to add or remove, and its not precise at all,
> and you should be able to add by swiping your finger, MIDI sync the circles"*
> SONAR: *"not precise enough, notes should disappear after a while. include synchronization, clock
> sync, the center should be always lit up"*
> STEPS: *"same as SONAR or EUCLID"*

**And a fourth entry, for one clause of its note that no other plan carries.**

> MORPH: *"mapping mode needed in, **if something doesn't change don't send it don't send 0 value**,
> also the LED's colors stuck again"*

MORPH's third clause is 11-02's (class A and class B). Its second clause — the suppression — is
**this plan's**, because it is the same idiom this plan gives the other three: remember what a contact
last did and do not repeat it. Its first clause, *"mapping mode"*, is genuinely ambiguous and is a
question at 11-09's checkpoint rather than a guess here.

**The named cause is event filtering, not arithmetic**, and it is a different class from 11-02's fast
tap: these three handle code 9 correctly and simply **never look at drags**.

```
euclid  if e~=4 and e~=9 then return end
sonar   if e~=4 and e~=9 then return end
steps   if e~=4 and e<9  then return end
```

**Accepting MOVE is not enough on its own, and this is the part that costs characters.** All three
**toggle** a cell, and a swipe delivers a MOVE every 10 ms — so a finger resting inside one cell would
toggle it back and forth at **100 Hz**. A per-contact last-cell guard is required, and it is what most
of the budget goes on.

**Two of the four asks are not built, and they are named rather than dropped.** Clock sync — EUCLID's
*"MIDI sync the circles"*, SONAR's *"include synchronization, clock sync"*, STEPS's *"same as"* — is
**blocked twice over**, and even a yes on the first blocker leaves the second standing. This plan
plans nothing that depends on it and says so where the entries live, so the next person to read
`euclid.ts` finds the reason rather than a gap.

Purpose: three entries answer a finger, with the toggle-storm the naive fix would cause designed out.
Output: three entries edited, one behaviour test, and one clearly-labelled non-delivery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bench-corrections/11-VALIDATION.md
@.planning/phases/11-bench-corrections/11-02-SUMMARY.md
@.planning/phases/11-bench-corrections/11-07-SUMMARY.md
@.planning/phases/11-bench-corrections/BENCH-2026-09-09.txt
@src/lib/catalog/entries/euclid.ts
@src/lib/catalog/entries/sonar.ts
@src/lib/catalog/entries/steps.ts
@src/lib/catalog/touch-guard.spec.ts
@docs/MIDI-IN-PROBE.md

<standing_rules>
- **`src/vendor/` is not touched.** `git diff --stat HEAD -- src/vendor/` matches 11-04's output.
- **Never touch a sibling repository.**
- **No agent connects to or writes to a device, and no agent deploys.** Nothing here is
  hardware-verified.
- No Claude or Anthropic attribution anywhere. No emojis anywhere.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- **State every count as a carried name plus a delta.**
- **Every cost is `max(GridScript.compressScript(lua).length, lua.length)` after `await padReady()`.**
- **A request that cannot fit 908 is a finding, not a failure.**
- **Both 11-02 gates apply.** Any contact-end test is `e==3 or e>=5 and e<9`; any onset test is
  `e==4 or e>8`; any decay lands on phase 0. Do not add a `DECLARED_EXCEPTIONS` row.
- The project spends no money.
</standing_rules>

<interfaces>

## The guard, and what it costs

The shape, costed on EUCLID against the real minifier:

```lua
self.q={}
...
if e~=1 and e~=4 and e<9 then s.q[i]=nil return end
local m=x*9//128+y*9//128*9
if s.q[i]==m then return end
s.q[i]=m
```

**EUCLID Setup 702 → 778, `+76`, 130 free, `checkSyntax` accepts.** Affordable, and it is the tightest
of the three by a wide margin. **SONAR (432 Setup, 475 free) and STEPS (388, 518) have far more
headroom**, so if the shape has to be trimmed for EUCLID it should still be written in full for the
other two rather than degraded uniformly — one entry's budget is not a house style.

Note the guard **clears on contact end** (`s.q[i]=nil`) as well as filtering repeats. Without the
clear, a second contact on the same cell after a lift would be swallowed. Write the clear in the 11-02
convention.

**Two details worth checking rather than assuming:**

1. **EUCLID's cell index is not the pad's cell index.** `euclid.ts` maps a touched cell to a ring
   position through `self.i[m]`, and returns early when the cell is not on a ring. The guard should
   remember the **cell** (`m`), not the ring position, so that swiping across a non-ring cell and back
   onto the same ring cell behaves sanely. Read the mapping before writing the guard.
2. **STEPS's grid is 8x8 inside a 9x9 pad.** `steps.ts` computes `n=c+r*8` and returns early when
   `c>7 or r>7`. The guard's key must be consistent with whichever index the toggle uses, or a swipe
   along the ninth column will behave oddly at the boundary.

## SONAR's other two asks

**"The center should be always lit up."** SONAR's Setup paints all 81 cells at `glp(c,1,0)` and lights
armed cells from the touch callback. Cell 40 is the sweep's origin. Lighting it permanently is a
`glc` + `glp` pair in Setup on a layer the sweep does not overwrite — **check which layer the Timer
writes** (`sonar.ts:59` arms layer 2 for the sweep and layer 1 for armed cells) before choosing, and
confirm the centre stays lit **while the sweep passes over it**, which is the case the user will
actually look at. 475 free; cost it.

**"Notes should disappear after a while."** SONAR fires note-on as the sweep crosses an armed cell and
today the note-off timing is what it is; read the Timer at `sonar.ts` before designing. Two honest
readings, and **they are not equally likely**:

- **The note is released after a fixed time** — a note length. Straightforwardly a `gms` 128 issued a
  known number of ticks later, or immediately, depending on how the sweep is structured.
- **The armed cells themselves clear after a while** — the pattern fades. That is a bigger behaviour
  change and it is not what *"notes should disappear"* most naturally says.

**Read the current note handling first.** If notes already hang because a note-off is missing or
mistimed, the ask is a bug report and the fix is small. If they are already released, the ask is for a
shorter length and the fix is a constant. **Say which it turned out to be**, because the two produce
very different SUMMARY entries and the user's words support either.

## Clock sync: not built, and named

Both gates are shut and the second does not open when the first does.

1. **The hardware answer is unknown.** `docs/MIDI-IN-PROBE.md` is a written, minifier-checked, 264-
   and 328-character pair of probe scripts for exactly this question, and its last line reads **"None
   yet. This probe has not been run."** The document is explicit that `gts` is dead on ZONA and
   `rtmrx_cb` is the only clock route the hardware has, so **a no on Part B does not send this family
   down a different road; it closes it.**
2. **Even a yes does not unblock the site.** HANGAR's Lua host has **no inbound MIDI path of any
   kind**: `grxm` is a recorded no-op that discards its slot argument, and `midirx_cb` and `rtmrx_cb`
   appear nowhere under `src/`. A clock-locked card would run on a real ZONA and sit **motionless in
   its own catalog card**. The prerequisite is a synthetic MIDI source and a synthetic clock in
   `src/lib/sim/`, which is a phase, not a task.

**Write both blockers into `euclid.ts`'s and `sonar.ts`'s headers**, in two sentences each, beside the
existing design notes — not into a planning document. The next person to read the entry should find
the reason there. `steps.ts` cites SONAR's note rather than restating it a third time.

**Do not plan, sketch, stub or scaffold anything that depends on the answer.** 11-16 hands the probe
to the user as a hardware row.

## MORPH's suppression — the same idiom, on a card that sends four CCs per sample

`morph.ts:60`, the whole send path:

```lua
self.touch_cb=function(s,i,e,x,y)
  if i>0 or e==3 or e>=5 then return end
  local u=127-x local v=127-y
  local w={u*v//127, x*v//127, u*y//127, x*y//127}
  for j=1,4 do s:gms(@CH,176,@CCB+j,w[j],0) ... end
  ...
end
```

**Four CC messages on every accepted sample, unconditionally, at 100 Hz while a finger moves.** And the
weights are a bilinear corner split, so **at any edge two of the four are exactly 0 and at any corner
three are** — which is the user's *"don't send 0 value"* stated as arithmetic rather than as a
complaint. This is not a guess about what they meant; it is what the expression produces.

**The idiom is the one this plan is already using**, so use it rather than deriving a second: a
per-state last-sent table, consulted before the send and updated after.

```lua
self.p={0,0,0,0}
...
for j=1,4 do local z=w[j] if z~=s.p[j] then s.p[j]=z s:gms(@CH,176,@CCB+j,z,0) end ... end
```

**Initialise the table to zeros, and that one choice answers both halves of the clause with one
mechanism.** A corner whose weight is 0 and was 0 — which includes every corner at Setup and every
corner the finger is far from — never sends. A corner that *falls* to 0 when the finger leaves it
sends 0 **once**, which is required: the alternative literal reading of *"don't send 0 value"* — never
emit 0 at all — leaves the receiver holding a stale non-zero value for a corner that is no longer
touched, which is a worse bug than the one being reported. **State that reasoning in the entry's
header and in the SUMMARY.** It is a reading, it is defensible, and it should be visible as a reading
rather than buried in a table initialiser.

**The budget is not the constraint.** MORPH is 507 at defaults and **508 at worst, so 400 free at
worst**, before 11-02 spends +8 for the DOWNUP escape and re-cuts `@DECAY`. Cost this change before
and after at the worst knob position against whatever 11-02 left, and state the margin at worst.

**One thing to check rather than assume:** `s.p` is indexed by corner, not by contact, because MORPH's
handler already returns for `i>0` — it is single-contact by construction. **Read that line before
writing the table**; if the guard has moved, the table's key has to move with it.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 11-08-01: Three entries accept a swipe, once per cell</name>
  <files>src/lib/catalog/entries/euclid.ts, src/lib/catalog/entries/sonar.ts, src/lib/catalog/entries/steps.ts, src/lib/sim/lua-smoke.spec.ts, src/lib/catalog/frames.json</files>

  <read_first>
    - all three entry files in full, including their headers' existing design notes — `steps.ts:67`
      and `life.ts`'s deleted twin both documented the onset-only guard as deliberate, and **a
      deliberate decision being reversed must be recorded as a reversal**
    - `euclid.ts`'s `self.i[m]` ring mapping, and `steps.ts`'s 8x8-inside-9x9 index
    - `src/lib/catalog/touch-guard.spec.ts` — the convention the clear must be written in
    - `11-07-SUMMARY.md` — CONSOLE's mute-row guard, so the two plans use one idiom
  </read_first>

  <action>
**1. EUCLID.** Accept MOVE with a per-contact last-cell guard, keyed on the **cell** and not the ring
position. Clear on contact end in the 11-02 convention. Expected **702 → 778, +76, 130 free** —
measure and quote both.

Read `self.i[m]` first: swiping across a non-ring cell and back onto the same ring cell should behave
sanely, and the guard's key decides whether it does.

**2. SONAR and STEPS.** The same shape, written in full — **they have 475 and 518 free and must not
inherit EUCLID's economy.** STEPS's key must be consistent with its 8x8 index or the ninth column
misbehaves at the boundary; state which index you chose.

**3. Record the reversal.** `steps.ts:67`'s header documents the onset-only guard as a deliberate
choice. The user is overriding it. Note in each header that the entry now toggles on a drag, that the
per-contact guard is what makes that survivable, and **what it costs** — a swipe that crosses a cell
twice toggles it twice, which is correct but is not what a "paint" gesture does. If a set-rather-than-
toggle swipe would serve the user better, **say so as a finding**; do not ship it as an
interpretation.

**4. `lua-smoke.spec.ts` +1.** One test covering all three entries:
- a swipe across N cells changes exactly N cells, **counted**;
- a finger resting inside one cell changes it **exactly once**, counted, not asserted as a boolean;
- a second contact on the same cell after a lift is **not** swallowed, which is what the clear buys.

Print the observed counts per entry.

**5. Regenerate `frames.json`.**

**Three negative checks:**
- Accept MOVE with no guard on EUCLID. Expect the resting-finger assertion red **and record the
  observed count** — a number in the hundreds is the clearest statement of why the guard exists.
- Drop the `s.q[i]=nil` clear. Expect the second-contact assertion red.
- Write the clear as `e>=5`. Expect `touch-guard.spec.ts` test 1 red.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts</automated>
    Expect `lua-smoke.spec.ts` **+1** with per-entry counts printed; all three entries inside 908 at
    every knob position with EUCLID's final margin stated as a number.
  </verify>

  <done>
Three entries accept MOVE with a per-contact last-cell guard, written in full on the two that can
afford it, each costed before and after with free characters. EUCLID's guard keyed on the cell with
the ring mapping read first; STEPS's key consistent with its 8x8 index and the choice stated. The
reversal of the deliberate onset-only guard recorded in each header with what it costs. One test
counting swipes, resting fingers and second contacts across all three. Three negative checks red with
the unguarded count recorded.
  </done>
</task>

<task type="auto">
  <name>Task 11-08-02: SONAR's centre and its notes, and clock sync named as not built</name>
  <files>src/lib/catalog/entries/sonar.ts, src/lib/catalog/entries/euclid.ts, src/lib/catalog/entries/steps.ts, src/lib/sim/lua-smoke.spec.ts, src/lib/catalog/frames.json</files>

  <read_first>
    - `src/lib/catalog/entries/sonar.ts` in full — which layer the Timer writes, how armed cells are
      lit, and exactly how and when note-off is issued today
    - `docs/MIDI-IN-PROBE.md` in full, including its last line
    - `src/lib/sim/lua-host.ts` — `grxm` as a recorded no-op, and the absence of `midirx_cb` and
      `rtmrx_cb` anywhere under `src/`
  </read_first>

  <action>
**1. The centre always lit.** Light cell 40 in Setup on a layer the sweep does not overwrite, and
**verify it stays lit while the sweep passes over it** — that is the case the user will look at, and
it is the one a Setup-only change can get wrong. Cost it against SONAR's free characters.

**2. Notes that disappear.** **Read the current note handling before designing anything.** Establish
which of the two readings is true:
- notes hang because a note-off is missing or mistimed — the ask is a bug report and the fix is small;
- notes are released and the ask is for a shorter length — the fix is a constant.

**Say which it turned out to be in the SUMMARY.** If it is a third thing — the armed cells themselves
should fade — that is a bigger behaviour change than the words most naturally support, and it should
be raised as a finding rather than chosen.

Any decay this introduces goes through 11-01's idiom and `decay-idiom.spec.ts` will check it.

**3. Clock sync, named.** Write into `sonar.ts`'s and `euclid.ts`'s headers, beside the existing design
notes, two sentences each:

- the probe at `docs/MIDI-IN-PROBE.md` has never been run, `gts` is dead on ZONA, `rtmrx_cb` is the
  only clock route the hardware has, and **a no closes this rather than redirecting it**;
- even a yes leaves the card **unpreviewable**, because HANGAR's Lua host has no inbound MIDI at all —
  `grxm` is a recorded no-op and neither callback appears under `src/` — so a clock-locked card would
  run on a real ZONA and sit motionless in its own catalog card.

`steps.ts` cites SONAR's note rather than restating it. **Three statements of one fact drift; two are
already one too many, which is why this is written in the two entries that carry the request and
nowhere else.**

**Build nothing that depends on the answer.** No stub, no flag, no `@SYNC` knob reserved "for later".
A reserved knob is a stamp slot, and a stamp slot spent on a feature that may never exist is a link
format nobody can take back.

**4. Extend the swipe test** rather than adding a new one: SONAR's centre cell is lit at tick 0 and
still lit at a tick when the sweep is over it, and a note fired by the sweep is released within a
stated number of ticks.

**Two negative checks:**
- Light the centre on the layer the sweep writes. Expect the still-lit-during-sweep assertion red —
  this is the check that proves the layer choice was made rather than guessed.
- Remove the note release. Expect the release assertion red, with the hanging note named.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/decay-idiom.spec.ts src/lib/catalog/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts</automated>
    Expect the extended test green, `decay-idiom.spec.ts` green on any new decay, and SONAR inside 908
    with its margin stated.
  </verify>

  <done>
SONAR's centre is lit at rest and while the sweep crosses it, on a layer chosen by reading rather than
guessing. Its notes are released, with the SUMMARY saying which of the two readings the source turned
out to support. Both clock-sync blockers are written into the two entries that carry the request, and
nothing was stubbed, flagged or reserved against an answer nobody has. Two negative checks red.
  </done>
</task>

<task type="auto">
  <name>Task 11-08-03: MORPH stops repeating itself, and stops shouting zeros</name>
  <files>src/lib/catalog/entries/morph.ts, src/lib/sim/lua-smoke.spec.ts, src/lib/catalog/frames.json</files>

  <read_first>
    - `src/lib/catalog/entries/morph.ts` in full **as 11-02 left it** — the DOWNUP escape and the
      `@DECAY` re-cut have both already landed, so cost against that text and not against the shipped one
    - `11-02-SUMMARY.md` — MORPH's costs after class A and class B, and the sentence owed for a
      re-cut knob
    - this plan's task 01 guard, as the idiom to reuse rather than re-derive
  </read_first>

  <action>
**1. Add the per-corner last-sent guard.** `self.p={0,0,0,0}` in Setup; in the send loop, compare
before sending and store after. Change the `glp` paint to use the same local so the picture and the
wire cannot disagree — **or deliberately leave the paint unconditional and say why**, because a
suppressed send with a suppressed repaint is one decision and a suppressed send with a live repaint is
another, and the second is defensible (the picture is a readout, the wire is traffic).

**2. Cost it at the worst knob position**, before and after, against what 11-02 left. State the
margin at worst. MORPH had 400 free at worst before this phase and has spent +8 plus whatever the
`@DECAY` re-cut moved.

**3. Say in the entry's header what "don't send 0 value" was taken to mean**, in two sentences: a
corner that is at zero and was at zero sends nothing, and a corner that falls to zero sends zero once
so the receiver does not hold a stale value. Name the alternative reading and why it was not taken.
**This is a reading of the user's words and it must be legible as one.**

**4. `lua-smoke.spec.ts` +1**, asserted as counts, not as a boolean:

- drive a slow drag across MORPH and assert that the number of emitted messages is **strictly fewer**
  than four times the number of accepted samples — with both numbers printed;
- assert that for a finger held still, the message count after the first sample is **zero**;
- assert that a corner the finger is nowhere near emits **nothing at all** during the drag, and that
  the corner the finger leaves emits its 0 **exactly once**.

The last two are the clause. A test that only counted messages would pass on a card that suppressed
the wrong ones.

**5. Regenerate `frames.json`** if the paint decision moved the picture, and list the rows that moved.

**Two negative checks:**
- Remove the `z~=s.p[j]` comparison. Expect the held-finger assertion red with the observed count in
  the hundreds — **record that number**, because it is the clearest statement of what the user was
  reporting.
- Initialise `self.p` to `{-1,-1,-1,-1}` instead of zeros. Expect the never-touched-corner assertion
  red, naming the corner that emitted a 0 it had no reason to — the proof that the initialiser is the
  half of the mechanism that answers *"don't send 0 value"*.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts</automated>
    Expect `lua-smoke.spec.ts` **+1** with both message counts printed, MORPH inside 908 at every knob
    position with its margin stated at worst, and both 11-02 gates green with no new exception row.
  </verify>

  <done>
MORPH emits a corner only when that corner's weight changed, and a corner sitting at zero emits
nothing. The zero-initialised table is named in the entry's header as the reading of *"don't send 0
value"* that it is, with the alternative reading and its stale-value failure stated. The change is
costed at the worst knob position against what 11-02 left. One test asserting three counts, including
the untouched corner's silence and the departed corner's single zero. Two negative checks red, with
the unguarded held-finger count recorded.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+2` —
  `lua-smoke.spec.ts` **+2**: one for the swipe guard, one for MORPH's suppression. **This plan's term
  in the phase chain is `+2`, not `+1`**, and the SUMMARY states it as a term.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` — all four entries inside 908 at
  every knob position, with **EUCLID's and MORPH's final margins stated as numbers at the worst knob
  position**.
- `decay-idiom.spec.ts` and `touch-guard.spec.ts` green, neither with a new exception row.
- `grep -rn "rtmrx_cb\|midirx_cb" src/` still empty — the honest proof that nothing was stubbed.
- `git diff --stat HEAD -- src/vendor/` matches 11-04's output.
</verification>

<success_criteria>
1. A swipe across EUCLID, SONAR or STEPS changes exactly one cell per cell crossed, counted through
   the real Lua host.
2. A resting finger changes a cell exactly once, and the unguarded count is recorded so the guard's
   necessity is a number rather than an argument.
3. A second contact on a cell after a lift is not swallowed.
4. SONAR's centre is lit at rest and while the sweep crosses it, on a layer chosen by reading the
   Timer.
5. SONAR's notes are released, with the SUMMARY saying which reading the source supported.
6. Clock sync is named as not built in the two entries that carry the request, with both blockers, and
   **nothing was stubbed, flagged or reserved** — proved by a grep.
7. MORPH sends a corner only when that corner changed; a held finger emits nothing after its first
   sample; an untouched corner emits nothing at all; and a departed corner emits its zero exactly
   once — all four asserted as counts, with the unguarded count recorded.
8. The reading of *"don't send 0 value"* is written in MORPH's header as a reading, with the
   alternative named and its failure mode stated.
9. Nothing under `src/vendor/` moved and no agent touched a device.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bench-corrections/11-08-SUMMARY.md` carrying: the carried
counts plus `+0 / +2`; four costs before and after with free characters at worst, and EUCLID's and
MORPH's final margins; MORPH's suppression with its before-and-after message counts, the paint
decision and its reason, and the two readings of *"don't send 0 value"* with the one taken;
EUCLID's guard key and the ring-mapping reasoning; STEPS's index choice; the reversal of the deliberate
onset-only guard, per entry, with what a twice-crossed cell now does; the unguarded resting-finger
count; which of the two readings SONAR's note handling turned out to support; the layer chosen for the
centre and how it was verified during a sweep; the two clock-sync blockers as written into the headers;
the `grep` proving nothing was stubbed; and the five negative checks with both exit codes.
</output>
