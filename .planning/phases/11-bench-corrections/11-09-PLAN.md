---
phase: 11-bench-corrections
plan: 09
type: execute
wave: 9
depends_on: ["11-08"]
files_modified:
  - src/lib/catalog/entries/arc.ts
  - src/lib/catalog/entries/pomodoro.ts
  - src/lib/catalog/entries/stage.ts
  - src/lib/sim/lua-smoke.spec.ts
  - src/lib/share/stamp.spec.ts
  - src/lib/catalog/frames.json
autonomous: false
requirements: [CONT-02, SHARE-01, SHARE-03, TUNE-05]

must_haves:
  truths:
    - "ARC's picture stops lying: the heart's amplitude is the amplitude it is emitting, so a user can see what the card is sending."
    - "POMODORO has a one-minute and a five-minute interval, and every POMODORO link ever shared still renders the interval it always rendered."
    - "Three notes that admit two readings each are answered by the user rather than guessed by the planner, and the evidence for each reading is put in front of them."
    - "STAGE gets the work the answer permits, and nothing else."
  artifacts:
    - path: "src/lib/catalog/entries/arc.ts"
      provides: "A heart scaled by the same depth the CC is scaled by"
    - path: "src/lib/catalog/entries/pomodoro.ts"
      provides: "Six intervals, the two new ones appended"
      contains: "@MINS"
  key_links:
    - from: "src/lib/catalog/entries/pomodoro.ts"
      to: "src/lib/share/stamp.ts"
      via: "one base-32 character per knob BY INDEX - appending is safe, inserting re-points every shared link"
      pattern: "MINS"
---

<objective>
Two corrections that are unambiguous and cheap, and **three notes that admit two readings each** —
put to the user together, once, with the evidence in front of them, rather than guessed at three
separate times.

> ARC: *"cannot see amplitude need visual feedback for that. if you press the center it stops, pressing
> it again resumes, not intuitive enough"*
> POMODORO: *"make a 1 minute and a 5 minute one."*
> STAGE: *"Lining up breathing is missing."*
> (and LUMEN's *"the color depth / opacity doesn't work"*, whose sysex half is 11-10's)

**ARC's first half has a named cause.** The 3x3 heart is painted `glp(...,1,v)` with the **raw**
triangle, while the CC is `glim(64+(v-128)*s.d//255,0,127)` — **depth-scaled**. At the bottom edge
`s.d = 127 - y = 0`, so the output is pinned at 64 and dead **while the heart keeps pulsing at full
scale**. The picture lies about the amplitude. Scaling the heart to the same value costs **+21** of
527 free.

**ARC's second half is not ambiguous so much as impossible as described.** ARC has **no stop, no
resume and no toggle anywhere in its source**, and `s.r = 1 + x*31//127` is at least 1 for every x, so
the oscillator cannot reach rate 0. Either "stops" means the output went flat — which is the same
defect as the first half and one fix answers both — or the user saw something on hardware the
simulator does not reproduce.

**STAGE's ambiguity has evidence the research did not notice, and it points one way.** The card's own
shipped description at `listing.ts:447` reads *"Nine scenes for your stream: the live one glows and
**the one you are lining up breathes**."* The code has only live (rate 4) and touched (rate 24). **The
copy already promises the feature the user says is missing.** That is strong, and it is still not a
decision the planner gets to make.

**POMODORO is free with one hard rule.** `@MINS` is `["15","20","25","50"]`, reaching one site. One-
digit values are *shorter* than two-digit ones, so the budget moves the right way. **The two new
values must be APPENDED, never inserted** — the stamp encodes one base-32 character per knob **by
index**, and inserting at the front would silently re-point every POMODORO link ever shared at a
different interval. No test catches it.

Purpose: two cheap corrections shipped, and three two-reading notes answered by the person who wrote
them.
Output: two entries edited, one checkpoint, and STAGE's work bounded by an answer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bench-corrections/11-VALIDATION.md
@.planning/phases/11-bench-corrections/11-CONTEXT.md
@.planning/phases/11-bench-corrections/11-08-SUMMARY.md
@.planning/phases/11-bench-corrections/BENCH-2026-09-09.txt
@src/lib/catalog/entries/arc.ts
@src/lib/catalog/entries/pomodoro.ts
@src/lib/catalog/entries/stage.ts
@src/lib/catalog/entries/lumen.ts
@src/lib/catalog/listing.ts
@src/lib/share/stamp.spec.ts

<standing_rules>
- **`src/vendor/` is not touched.** `git diff --stat HEAD -- src/vendor/` matches 11-04's output.
- **Never touch a sibling repository.**
- **No agent connects to or writes to a device, and no agent deploys.** Nothing here is
  hardware-verified.
- No Claude or Anthropic attribution anywhere. No emojis anywhere.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- **State every count as a carried name plus a delta.**
- **Every cost is `max(GridScript.compressScript(lua).length, lua.length)` after `await padReady()`.**
- **A request that cannot fit 908 is a finding, not a failure.**
- **Both 11-02 gates apply**, and `decay-idiom.spec.ts` will check any decay written here.
- **APPEND, never insert.** Knob positions are stamp payload.
- **ARC and POMODORO both carry capitalised "do not fix" notes in their headers about legitimate
  keeper timeouts.** Read the note before touching either. `glt(a,l,65535)` beside a fast rate is a
  strobe; beside a zero rate it is a keeper, and these two are keepers on purpose.
- The project spends no money.
</standing_rules>

<interfaces>

## ARC — the amplitude, +21 of 527 free

`arc.ts:50` paints the heart with the raw triangle and emits the depth-scaled value:

```lua
-- painted:  glp(a,1,v)                         -- raw
-- emitted:  glim(64+(v-128)*s.d//255,0,127)    -- depth-scaled by s.d = 127 - y
```

At `y = 127` the depth is 0, the CC pins at 64, and the heart keeps pulsing at full scale. Scale the
heart by the same `s.d` and the picture becomes the output. **251 → 272 in the Timer, +21, 636 free.**

**Read `arc.ts`'s header first.** It carries a capitalised note about its keeper timeout
(`glt(a,2,65535)` beside a zero-rate `glpfs`), which is legitimate and must not be "fixed" —
`lua-smoke.spec.ts` test 3 guards the strobe case and this is the other one.

## POMODORO — two values appended, and the one rule

`@MINS` is `["15","20","25","50"]`, knob index 4 of 5, reaching one site: `self.n=@MINS*60`.
Appending `"1"` and `"5"` gives six values. One-digit values are shorter, so the worst-case Setup cost
falls rather than rises — **measure it and state the new worst**.

**The ring arithmetic survives**, verified: `m = s.t*32//s.n` at `n=60` drains the 32-cell ring in 32
steps of 1.875 s. **Check the same at `n=300`** (five minutes: 32 steps of 9.375 s) and confirm the
Timer's re-arm still behaves — and read the header's capitalised note about the **655-second `glt`
ceiling** before assuming a longer interval is the risky one. It is the *existing* 25- and 50-minute
values that need the 300-second re-issue; the new short ones are the easy case, and saying that
correctly matters because the obvious worry is the wrong one here.

**`stamp.spec.ts` +1**: a POMODORO stamp carrying knob index 0, 1, 2 and 3 still decodes to 15, 20, 25
and 50. **Capture the four literals before the change and assert them after.** This is the only guard
against the insertion mistake, because `stamp.spec.ts` otherwise compares indices and would stay green
through a re-pointing.

## The three questions, and the evidence for each

**Ask them together, once.** Three separate pauses across three plans would be three interruptions for
one conversation.

### ARC — "if you press the center it stops, pressing it again resumes"

- **Reading 1: "stops" means the output went flat.** At the bottom edge `s.d = 0`, the CC pins at 64,
  and the card is dead while the heart still moves. Task 01 fixes exactly that. Consistent with
  *"pressing it again resumes"* if the second press was higher up the pad.
- **Reading 2: something on hardware the simulator does not reproduce.** ARC has no stop, no resume
  and no toggle in its source, and its oscillator cannot reach rate 0.

**Either way this is also a bench row**, because reading 2 can only be settled at the module.

### STAGE — "Lining up breathing is missing"

- **Reading 1: a third zone state.** Live and held exist; a **preview / standby** zone — the scene you
  are *about to* cut to — has no indication at all. **The shipped description already promises it**:
  *"the live one glows and the one you are lining up breathes"* (`listing.ts:447`). Real feature work,
  399 characters free.
- **Reading 2: the four corners breathe out of step.** Read against the source they do not — `Z(z,f)`
  arms `glpfs(a,1,0,f,3)` on all four corners **from phase 0 in the same call**. If the user saw them
  drift, that is a hardware finding.

**Put reading 1's evidence in front of the user**, quoting their own card's description. Do not
present the two as equally weighted when the shipped copy already takes a side — but do not decide.

### LUMEN — "the color depth / opacity doesn't work"

Asked here so 11-10 is not blocked on a second pause.

- **Reading 1: the ramp is too shallow.** `d = 36 - row*@DEPTH` runs 36..4, so the bottom row is 78 %
  of the anchor at DEPTH 1 and 11 % at DEPTH 4, and **never black**. Turning the knob at the low
  settings changes very little. Work: re-cut `@DEPTH`, possibly to reach black.
- **Reading 2: opacity should be an emitted value.** LUMEN sends only raw X and Y on `@CC` and
  `@CC+1`; a lighting desk receives a **position**, not a colour and not an intensity. Work: a third
  stream — or the hex-over-sysex change 11-10 makes it moot.

## STAGE, if the answer is reading 1

`stage.ts:141`'s `Z(z,f)` takes a breathe rate and already distinguishes live (4) from held (24). A
third state is a third rate and a third colour, plus a way to **select** the preview zone that does
not collide with the existing gesture — today `e==4 or e>8` cuts to a zone and `e>=5` returns the last
zone to live. Budget: **505 Setup / 109 Timer, 399 free**.

**The selection gesture is the design problem, not the breathing.** A second gesture on a nine-zone
pad with no modifier is a real constraint; a long press, a second finger, or a dedicated row are all
possible and all cost differently. **Cost at least two before choosing**, and state the one chosen and
why.

**If the answer is reading 2, this plan ships nothing for STAGE** and hands it to 11-16 as a hardware
row — and says so, rather than shipping reading 1's work anyway because it was already designed.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 11-09-01: ARC's picture stops lying about its amplitude</name>
  <files>src/lib/catalog/entries/arc.ts, src/lib/sim/lua-smoke.spec.ts, src/lib/catalog/frames.json</files>

  <read_first>
    - `src/lib/catalog/entries/arc.ts` in full, **including the capitalised keeper-timeout note in its
      header**
    - `src/lib/sim/lua-smoke.spec.ts` test 3 — the strobe guard, and why ARC is not it
  </read_first>

  <action>
Scale the 3x3 heart by the same `s.d` the CC is scaled by, so the brightness a user sees is the
amplitude the card is emitting. Expected **251 → 272 in the Timer, +21, 636 free**; measure and quote.

**Do not touch the keeper timeout.** `glt(a,2,65535)` beside a zero-rate `glpfs` is the legitimate
form and the header says so in capitals.

**`lua-smoke.spec.ts` +1**: at three depths — top edge, middle, bottom edge — the heart's peak
brightness and the emitted CC's excursion from 64 move **together**. At the bottom edge both are
flat. Assert the correlation across the three, not an exact brightness, so a future re-scaling does not
redden it for nothing.

Regenerate `frames.json`; ARC's rows should move at the ticks where the heart is at its peak.

**One negative check:** paint the heart with the raw `v` again. Expect the new test red, showing a
moving heart beside a flat CC — **the lie, made visible**, which is the sentence worth putting in the
failure message.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/sim/lua-smoke.spec.ts src/lib/catalog/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts</automated>
    Expect `lua-smoke.spec.ts` **+1**, ARC inside 908 with its margin stated, and the keeper timeout
    untouched.
  </verify>

  <done>
ARC's heart is scaled by the depth its CC is scaled by, measured at +21 with its free characters
stated. The keeper timeout is untouched. One test asserting picture and output move together at three
depths and both go flat at the bottom edge. One negative check red with the lie visible in the
message.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 11-09-02: Three notes, two readings each, answered once</name>

  <action>
Present the decision and the six options below to the user and stop. Task 01 has already shipped ARC's
amplitude fix; nothing else in this plan runs until all three questions are answered. **Do not pick a
reading, do not ship the work for one "provisionally", and do not soften an option because its evidence
is weaker.** Where the evidence favours a reading - STAGE's shipped description already promises
reading 1 - say so, and still let the user decide.
  </action>

  <decision>
Three of your bench notes each support two different pieces of work. Each answer decides what gets
built next, and none of them is a planner's call.
  </decision>

  <context>
The rest of the phase has been built from your notes without interpretation. These three are where the
words genuinely fit two things, and where guessing would mean building the wrong one. Everything else
in this plan is already done.
  </context>

  <options>
    <option id="arc-flat">
      <name>ARC — "stops" meant the output went flat</name>
      <pros>Already fixed. At the bottom edge of the pad the depth is zero, so the card was pinned at
      64 and sending nothing while the heart kept pulsing at full brightness. Task 01 of this plan
      makes the heart show the real amplitude, so the dead zone is now visible instead of hidden. If
      that is what you saw, there is nothing more to do.</pros>
      <cons>ARC has no stop, no resume and no toggle anywhere in its code, and its oscillator cannot
      reach rate zero — so if you really did see it halt and restart, this reading is wrong.</cons>
    </option>
    <option id="arc-hardware">
      <name>ARC — you saw something the simulator does not reproduce</name>
      <pros>Recorded as a hardware row for your bench, with the exact thing to look for.</pros>
      <cons>Nothing ships for it in this phase.</cons>
    </option>
    <option id="stage-third-state">
      <name>STAGE — a third, "lining up" zone state</name>
      <pros>Live and held exist; a preview scene has no indication at all. **Your card's own
      description already promises this**: "Nine scenes for your stream: the live one glows and the one
      you are lining up breathes." 399 characters free, so it fits.</pros>
      <cons>It needs a second gesture on a nine-zone pad with no modifier key — a long press, a second
      finger, or a dedicated row. That choice will be costed and brought back to you if it is not
      obvious.</cons>
    </option>
    <option id="stage-out-of-step">
      <name>STAGE — the four corners breathe out of step</name>
      <pros>Recorded as a hardware row.</pros>
      <cons>The code arms all four corners from phase zero in one call, so they cannot drift in the
      simulator. Nothing ships for it in this phase.</cons>
    </option>
    <option id="lumen-ramp">
      <name>LUMEN — the depth ramp is too shallow to read</name>
      <pros>Measured: the bottom row is 78 % of the top at DEPTH 1 and 11 % at DEPTH 4, and never
      black. Turning the knob at the low settings changes almost nothing. Re-cutting the depth values,
      possibly so the bottom reaches black, is straightforward and LUMEN has more free room than any
      other card.</pros>
      <cons>It is a picture change, not a change to what the card sends.</cons>
    </option>
    <option id="lumen-emitted">
      <name>LUMEN — opacity should be a value it sends, not a picture</name>
      <pros>LUMEN currently sends only raw X and Y, so a lighting desk receives a position and not a
      colour or an intensity. A third stream would fix that.</pros>
      <cons>The hex-over-sysex change already planned for 11-10 may make it unnecessary — worth saying
      if you want both.</cons>
    </option>
  </options>

  <resume-signal>
Answer all three. For example: "arc-flat, stage-third-state, lumen-ramp" — or describe something none
of these captures, which is also a useful answer.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 11-09-03: POMODORO's two intervals appended, and STAGE per the answer</name>
  <files>src/lib/catalog/entries/pomodoro.ts, src/lib/catalog/entries/stage.ts, src/lib/share/stamp.spec.ts, src/lib/catalog/frames.json</files>

  <read_first>
    - `src/lib/catalog/entries/pomodoro.ts` in full, **including the capitalised note about the
      655-second `glt` ceiling and the 300-second re-issue**
    - `src/lib/share/stamp.spec.ts` — how a knob index becomes a base-32 character
    - `src/lib/catalog/entries/stage.ts:141` — `Z(z,f)` and the existing gesture
    - task 02's answer
  </read_first>

  <action>
**1. POMODORO.** Append `"1"` and `"5"` to `@MINS`. **Append. Do not insert, do not sort, do not
tidy.** The stamp encodes one base-32 character per knob by index and the four existing values must
keep indices 0..3.

Measure the worst-case Setup and Timer across all six values and state the new worst — it should
**fall**, because one-digit values are shorter than two.

**Check the ring arithmetic at both new values**: `m = s.t*32//s.n` drains 32 cells in 32 steps of
1.875 s at `n=60` and 9.375 s at `n=300`. Then read the header's 655-second ceiling note and say
explicitly, in the SUMMARY, that the new short intervals are the **easy** case and it is the existing
25- and 50-minute values that depend on the Timer re-issuing `glpfs` **and** `glt` every 300 seconds.
The obvious worry is the wrong one here and saying so stops the next reader worrying about it.

**`stamp.spec.ts` +1.** Capture four real POMODORO stamps at knob indices 0..3 **before** the change,
commit them as literals, and assert after the change that they still decode to 15, 20, 25 and 50. This
is the only guard against the insertion mistake.

**2. STAGE, per task 02's answer.**

- **If `stage-third-state`:** add a third breathe rate and colour for a preview zone. **Cost at least
  two selection gestures before choosing one** — a long press, a second finger, a dedicated row — and
  state the chosen one and why in the entry's header. 399 free. Any new decay goes through 11-01's
  idiom. Update `listing.ts`'s description only if the promise it already makes is now inaccurate in a
  new way; **it already describes reading 1**, so most likely it does not move, and saying that is
  worth a line.
- **If `stage-out-of-step`:** ship nothing for STAGE. Record it as a hardware row for 11-16 with the
  source evidence — all four corners armed from phase 0 in one call — so the bench knows exactly what
  to look at. **Do not ship reading 1's work anyway** because it was already designed.

**3. Record ARC's and LUMEN's answers** for 11-16 and 11-10 respectively. If `arc-hardware`, write the
bench row with the exact observation to make. If `lumen-emitted`, hand 11-10 a second stream to cost
alongside its sysex work.

**Two negative checks:**
- Insert `"1"` at the front of `@MINS` instead of appending. Expect the new `stamp.spec.ts` test red,
  **naming the interval a previously-shared link would now render**. Note that the rest of
  `stamp.spec.ts` stays **green**, which is precisely why this test had to exist.
- If STAGE work shipped: make the preview zone's breathe rate equal to live's. Expect whatever
  assertion distinguishes them red — and if nothing does, add it, because three states that look like
  two is the bug the user reported in the first place.
  </action>

  <verify>
    <automated>npx vitest run --project server src/lib/share/stamp.spec.ts src/lib/catalog/ && npx vitest run --project sweep src/lib/catalog/lua-entries.sweep.spec.ts src/lib/share/stamp-roundtrip.sweep.spec.ts</automated>
    Expect `stamp.spec.ts` **+1** with the four captured literals still decoding to their original
    intervals; both entries inside 908 at every knob position; the roundtrip sweep's total moved by the
    two new POMODORO states and the number stated.
  </verify>

  <done>
POMODORO carries six intervals with the two new ones appended, indices 0..3 proved unmoved by four
literals captured before the change, and the worst-case cost stated as having fallen. The 655-second
ceiling correctly attributed to the long intervals rather than the new short ones. STAGE shipped or
not shipped strictly per the answer, with the selection gesture costed at least twice if it shipped.
ARC's and LUMEN's answers recorded for 11-16 and 11-10. Two negative checks red, with the
stamp-stays-green observation stated.
  </done>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs PREV_FILES PREV_TESTS+2`.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19` — the member list unchanged; the
  **reachability and roundtrip totals move** with POMODORO's two new values, and both new totals are
  recorded with their deltas.
- Every edited entry costed before and after with free characters, and the two keeper timeouts
  confirmed untouched: `grep -n "65535" src/lib/catalog/entries/arc.ts src/lib/catalog/entries/pomodoro.ts`.
- `git diff --stat HEAD -- src/vendor/` matches 11-04's output.
</verification>

<success_criteria>
1. ARC's heart shows the amplitude ARC is emitting, at three depths, proved by a correlation rather
   than a brightness literal.
2. POMODORO has one- and five-minute intervals, appended, with four pre-change stamps proving indices
   0..3 unmoved — and the SUMMARY notes that the rest of `stamp.spec.ts` would have stayed green.
3. The 655-second ceiling is attributed to the long intervals, not the new short ones.
4. Three two-reading notes were put to the user in one pause, with the evidence for each — including
   STAGE's own shipped description, which already promises reading 1.
5. STAGE received exactly the work the answer permits and nothing else.
6. Both keeper timeouts are untouched.
7. Nothing under `src/vendor/` moved and no agent touched a device.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bench-corrections/11-09-SUMMARY.md` carrying: the carried
counts plus `+0 / +2`; ARC's cost before and after with its margin; POMODORO's worst-case cost stated
as having fallen, with the ring arithmetic checked at 60 and 300 seconds and the ceiling correctly
attributed; the four captured stamp literals; the user's three answers verbatim; STAGE's shipped work
with both costed selection gestures, or its non-delivery with the source evidence for the bench row;
ARC's and LUMEN's answers handed onward; the sweep totals that moved; and the four negative checks with
both exit codes.
</output>
