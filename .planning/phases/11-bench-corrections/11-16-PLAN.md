---
phase: 11-bench-corrections
plan: 16
type: execute
wave: 17
depends_on: ["11-15"]
files_modified:
  - docs/TESTING.md
  - docs/HARDWARE-AUDITION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/11-bench-corrections/deferred-items.md
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CAT-01, CAT-03, CAT-04, FOUND-02, PREV-01, PREV-02, PREV-06, TUNE-01, TUNE-05, SHARE-01, SHARE-03, SHARE-04]

must_haves:
  truths:
    - "Every projection this phase planned against has been replaced by an observation taken on this machine, on a clean tree, against a fresh production build - and every wrong projection is named as wrong."
    - "The phase total is a chain from the baseline 11-01 measured, in seventeen terms, and the count of terms is itself asserted against every prose statement of it."
    - "Two requirements whose wording this phase deliberately falsified are amended by name and dated, rather than left standing as claims the code no longer supports."
    - "Six things this phase could not do are named with what would close each, rather than quietly dropped."
    - "Nothing under src/vendor is undeclared, no agent touched a device, and every hardware row is handed over unanswered."
  artifacts:
    - path: "docs/TESTING.md"
      provides: "Every count and every cost re-measured at the end of the phase, replacing 11-VALIDATION's projections"
    - path: ".planning/phases/11-bench-corrections/deferred-items.md"
      provides: "Every deferred and undeliverable item with what would close it - a work list, not a wish list"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "src/lib/fidelity/upstream-manifest.json"
      via: "CONT-01 and FOUND-02 amended to point at the enumerated divergence rather than to claim there is none"
      pattern: "intendedDivergence"
---

<objective>
The phase gate: every number re-measured, two requirements amended by name, six non-deliveries named,
and the hardware rows handed to the user.

**What a gate is for in this repository.** Not a victory lap. Sixteen plans have each asserted a
carried name plus a delta; this plan is the only one that asserts against `BASE_FILES` and
`BASE_TESTS`, and its number is the phase total **written as a chain** so 11-01's baseline and 11-16's
total are visibly the same arithmetic seen from two ends.

**And it is where the projections die.** `11-VALIDATION.md` carries a delta table the planner
estimated. Every row of it is replaced here by an observation, and **any row that was wrong is named as
wrong** rather than quietly corrected — a validation document whose estimates were never checked
teaches the next phase to estimate carelessly.

**Phase 10 miscounted seven times**, and every one was somebody quietly reconciling a number instead of
reporting a disagreement. This phase carried that discipline in every plan; this is where it is
checked, including the one number that drifted twice in Phase 10 **after being fixed**: **the count of
terms in the chain.**

**Two requirements were deliberately falsified by this phase and 11-03 recorded both in advance**, so
that this plan amends them rather than discovering them. That is the difference between a phase that
knew what it was doing and one that found out.

Purpose: the phase closed honestly, with everything it could not do named.
Output: one re-measured document, two amended requirements, one deferred-items file, one checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-bench-corrections/11-VALIDATION.md
@.planning/phases/11-bench-corrections/11-CONTEXT.md
@.planning/phases/11-bench-corrections/11-01-SUMMARY.md
@.planning/phases/11-bench-corrections/11-03-SUMMARY.md
@.planning/phases/11-bench-corrections/11-04-SUMMARY.md
@.planning/phases/11-bench-corrections/BENCH-2026-09-09.txt
@docs/TESTING.md
@docs/HARDWARE-AUDITION.md
@docs/MIDI-IN-PROBE.md
@docs/PIN-POLICY.md

<standing_rules>
- **`src/vendor/` is not touched by this plan**, and its diff must equal `upstream-manifest.json`'s
  `intendedDivergence` rows exactly, file by file, hunk by hunk.
- **Never touch a sibling repository.**
- **No agent connects to or writes to a device, and no agent deploys.** `npm run deploy` is not run.
- No Claude or Anthropic attribution anywhere, and no co-author trailer. No emojis anywhere.
- Prettier defaults. **PowerShell is the shell.** Long content through the Write tool.
- **This plan asserts against `BASE_*`. Every other plan asserted against `PREV_*`.**
- e2e at `--workers 3`; remove `test-results/` by hand and confirm `git status --porcelain` empty.
- **Do not edit `.planning/ROADMAP.md`.** This phase was planned under an instruction not to, and the
  roadmap's Phase 11 block is left exactly as it stands; anything this plan would have written there
  goes in the SUMMARY for whoever updates it.
- **A shipped SUMMARY is a record of what was observed and believed.** A superseded number inside one
  is **not** edited — it is left standing and pointed at from here.
- The project spends no money.
</standing_rules>

<interfaces>

## The chain to write out, not the total alone

From 11-01's `BASE_TESTS`, plan by plan, **each term re-derived from `11-VALIDATION.md`'s per-file
table rather than copied from the plan's own `check-counts` line**:

```
BASE_TESTS
  +3 (11-01: decay-idiom.spec.ts created with 3)
  +5 (11-02: touch-guard.spec.ts created with 3, lua-smoke +2)
  +2 (11-03: vendored-diff 14 -> 15, preset-baseline +1)
  +1 (11-04: lua-smoke +1, fast-tap parity over the nine compiled presets)
  +4 (11-05: presets.spec.ts created with 4)
  +1 (11-06: knobs.preset +1, the ninepads grid knob)
  +4 (11-07: lua-smoke +4 - CONSOLE's full scale and inert mute, FORGE's travel sweep,
              LATTICE's clamp sweep)
  +2 (11-08: lua-smoke +2 - the swipe guard, and MORPH's per-corner suppression)
  +3 (11-08.1: host.spec.ts 18 -> 21 - a lost 2D context noticed by event AND by paint-time
              guard, recovered by both routes, and both listeners removed at teardown)
  +2 (11-09: lua-smoke +1, stamp +1)
  +2 (11-10: lua-host +1, host-surface +1)
  +1 (11-11: GHOST)
  +1 (11-12: SHUTTLE)
  +2 (11-13: STRIP)
  +T14 (11-14: lua-smoke +1 under ring-seven, new-entry or ring-eight-lua;
              +0 under fold-into-sonar - named either way, never omitted)
  +2 (11-15: the wheels)
  +0 (11-16: the gate writes no tests - written out, not omitted, so the term count is the plan count)
  = BASE_TESTS + 36, or BASE_TESTS + 35 under fold-into-sonar
```

**The sixteen fixed terms sum to 35; `T14` is the seventeenth and it is a function of the answer 11-14
recorded.** `3+5+2+1+4+1+4+2+3+2+2+1+1+2+2+0 = 35`. **Read `11-14-SUMMARY.md`'s branch-table row before
asserting a total**, and write the answer beside the arithmetic.

**Seventeen terms, one per plan.** The count of terms is **itself asserted** against every prose
statement of it in `11-VALIDATION.md` and in this plan — it drifted twice in Phase 10 after being
fixed, which is why it is a number under test rather than a description.

**Two terms in that chain are larger than `11-VALIDATION.md`'s plan-level table states, deliberately,
and the gate must not report the difference as a plan defect.** 11-07 gained two tests (FORGE's and
LATTICE's travel sweeps) when the phase was revised to answer two bench notes that previously reached
no plan; 11-08 gained one (MORPH's suppression clause, likewise). **Both plans' own `check-counts`
lines were moved with them** — 11-07 asserts `PREV_TESTS+4` and 11-08 asserts `PREV_TESTS+2` — so the
reconciliation below should find agreement. If `11-VALIDATION.md` still says `+2` and `+1` for those
two rows, **the validation document is the stale side and this plan says so by name.**

**A term that disagrees with its plan's own `check-counts` line is a defect in one of the two, and the
gate is the place it surfaces: reconcile before running, not after.**

**One reference table in `11-VALIDATION.md` is wrong and this plan tells the executor to derive terms
from it, so correct it before deriving anything.** `11-VALIDATION.md:255`'s per-file row for
`src/lib/sim/lua-smoke.spec.ts` reads `+2, +2, +1, +1, +2` against **five** plans (11-02, 11-07,
11-08, 11-09, 11-15), totalling **+8**. The real set is **ten plans**, every one of which declares the
file in its own `files_modified`:

| Plan | lua-smoke delta | what it adds |
|---|---|---|
| 11-02 | +2 | the matched-pair residue probe and the fast-tap parity probe |
| 11-04 | +1 | fast-tap parity across the nine compiled presets |
| 11-07 | +4 | CONSOLE's full scale and inert mute; FORGE's travel sweep; LATTICE's clamp sweep |
| 11-08 | +2 | the swipe guard; MORPH's per-corner suppression |
| 11-09 | +1 | ARC's picture and output moving together |
| 11-11 | +1 | GHOST |
| 11-12 | +1 | SHUTTLE |
| 11-13 | +2 | STRIP's three streams and the 14-bit loss |
| 11-14 | +1 or +0 | RADAR, per the branch |
| 11-15 | +2 | the wheels |
| **total** | **+17**, or +16 under `fold-into-sonar` | |

**The plan-level table and the phase total are correct; the per-file reference row is the defect.**
Note it as such in `docs/TESTING.md` — this is exactly the class of error task 01 is told to name
rather than quietly correct.

**`src/lib/sim/host.spec.ts` is the one per-file row this phase gained after `11-VALIDATION.md` was
written**, at **18 → 21**, and it is 11-08.1's whole term. It is the only test file in the phase that
is not about a catalog entry, a fixture or a vendored contract, which is why a reader scanning the
per-file table for the third time will not expect it.

`BASE_FILES + 3`, in **seventeen** terms — sixteen written-out zeros and 11-01's, 11-02's and
11-05's ones — three created files: `decay-idiom.spec.ts`, `touch-guard.spec.ts`,
`presets.spec.ts`. **11-08.1 created a fourth file, `e2e/poll.ts`, and its term is still `+0`**: the
gate counts the vitest `server` project's files, and `e2e/` is in neither that project nor
`svelte-check`'s reach. Say so, or the fourth file reads as an unexplained discrepancy. **Note the shape of that number**: this phase **deleted nine source files** and the
spec-file count still rose. Say so, because a reader who expects a removal phase to lower a file count
will otherwise think the gate is wrong.

**`BASE_E2E + 2`, in seventeen terms** — sixteen written-out zeros and **one `+2`, which is 11-08.1's**.
That plan added ONE Playwright title, tagged `@webkit`, proving that a pad whose 2D backing store is
dropped gets its picture back. No other title is added, moved or removed anywhere in this phase.

**AND THE TWO E2E NUMBERS ARE DIFFERENT NUMBERS, WHICH NOTHING IN THIS PHASE HAD TO SAY UNTIL NOW.**
`BASE_E2E` decomposes as **85 titles run by chromium** (which carries no `grep`) **plus 18 `@webkit`
titles run a second time by `webkit-phone`**: `85 + 18 = 103`. A tagged title therefore counts **once**
in a source grep and **twice** in a run, so 11-08.1 moves `grep -c "test("` from **85 to 86** and the
Playwright total from **103 to 105**. **Assert both, and state why they differ** — a gate that checked
only the grep would accept a run total two short and call it a match.

**`grep -c "test("` is a sound proof of a ZERO term and is not a sound proof of a non-zero one.**
`11-VALIDATION.md`'s sampling-rate instruction says suite-running plans prove their term that way; it
was written when every term was zero. **Record the limitation in `docs/TESTING.md`** rather than
leaving the instruction to be applied to a term it cannot measure.

**FIVE plans ran the suite, not four and not three**: **11-01** (nine routes and nine OG images stop
being generated), **11-05** (the preset entries change hands), **11-08.1** (the canvas context-loss
fix, which is unprovable any other way), **11-15** (a new route and a new OG image — `11-15:320` and
`:361`) and **this one**.

**Three was wrong and it was written in three places** — `11-16:141` as it originally stood,
`11-VALIDATION.md:238` and `11-VALIDATION.md:64`'s `PREV_E2E` row — and four became wrong the moment
11-08.1 was inserted. `11-VALIDATION.md:60`'s own contract says a plan that moves a baseline names
itself, so the omission of 11-15 was a defect in the document rather than in the plan. **Record both
corrections in `docs/TESTING.md` as rows the planner got wrong.**

**The catalog chain**, seventeen terms:

```
BASE_CATALOG (36) −9 (11-01) +0 ×13 + T14 (11-14) +1 (11-15) +0 (11-16) = 28 or 29
```

`T14` is read from the branch table `11-14-SUMMARY.md` carries, and it decides four downstream numbers
at once. **Copy the applicable row here before asserting anything:**

| 11-14's answer | `T14` | catalog | preset / Lua | `static/og/` | audition rows | test total |
|---|---|---|---|---|---|---|
| `ring-seven` | +0 | **28** | 8 + 20 | 28 | 24 | `BASE_TESTS + 36` |
| `ring-eight-lua` | +0 | **28** | 8 + 20 | 28 | 24 | `BASE_TESTS + 36` |
| `new-entry` | **+1** | **29** | 9 + 20 | **29** | **25** | `BASE_TESTS + 36` |
| `fold-into-sonar` | +0 | **28** | 9 + 19 | 28 | 24 | `BASE_TESTS + 35` |

**11-14's term is stated explicitly rather than folded in, and no number in this plan's verification or
success criteria is written as a literal that only one of those rows would satisfy.** Two plans
previously hard-coded the `ring-seven` row into unconditional commands, which would have made a
correct tree fail a gate under two of the four answers — and this plan is instructed to name a chain
disagreement as a defect, so it would have reported a planning gap as one.

## The two amendments, recorded in advance by 11-03

Both are amended **by name and dated**, with the current wording quoted and the reason stated.

- **CONT-01**: *"...each compiling to the same Lua as BOTOR at the pinned protocol version"*. After
  11-04, five of the nine do not: **aurora, pinwheel, starfield, radar, dial** — every one whose
  `touch.kind` is `comet` or `perFinger`. joystick, ninepads, faders and tpad are unchanged. The
  amendment points at `upstream-manifest.json`'s enumerated divergence rather than deleting the
  clause, because the clause is still true of everything not enumerated.
- **FOUND-02**: *"...with their existing test suites passing unchanged"*. `tests/pad.test.js` carries
  the literal `glpfs(a,1,255,250,0)` and took one hunk. The amendment names the divergence count
  rather than removing the promise.

**Neither is deleted and neither is softened into vagueness.** A requirement amended to say less is a
requirement nobody can fail.

## The six this phase could not do, each with what would close it

**A phase that quietly drops a request is worse than one that names it**, and the count below is
**six unconditional plus up to three conditional** — the conditional three exist only in the branch
where a measurement found nothing or a user answer deferred work. **State the actual count, with the
branch, rather than the word "six".**

| # | What | Why not | What would close it |
|---|---|---|---|
| 1 | **Clock sync** (EUCLID, SONAR, STEPS, and RADAR's original grouping) | Blocked twice. `docs/MIDI-IN-PROBE.md` has never been run, and even a yes leaves the cards **unpreviewable**: HANGAR's Lua host has no inbound MIDI at all, `grxm` is a recorded no-op, and `midirx_cb` / `rtmrx_cb` appear nowhere under `src/` | Ten minutes at a bench for the probe, **then** a synthetic MIDI source and clock in `src/lib/sim/` — which is a phase, not a task |
| 2 | **DIAL's counter-clockwise rate** | The simulator says the two directions are **exactly symmetric**: 192 messages each way, values 65 and 63, at 64 and at 128 samples per turn. The compiler emits binary-offset relative CC. **Strong hypothesis:** the user's host is in signed-bit mode, where 63 reads as **+63** — violent in exactly the one direction reported. Not descriptor-reachable either way: `DialMode` offers only `relative` and `absolute` and the convention is hard-coded at `_pad.ts:2043` | One bench question — what encoder mode is the control set to? — then either a documented "set your host to binary offset" or a hand-authored dial |
| 3 | **TRACKPAD's animation** | Blocked twice. `normalisePadState` forces `look.kind` and `touch.kind` to `"none"` for a trackpad state — **measured**: a `breathe`/`comet` variant normalises straight back and compiles to the **identical 902 characters**. And there are **six characters free** of 908, one at the worst reachable knob position | A hand-authored trackpad re-implementing a 902-character compiled handler that drains the firmware touch queue with `touch_pop`/`tid`/`tev`/`txv`/`tyv`. A wave of its own, and it may be an honest never |
| 4 | **SNAKE** | **Deferred by the user in their own words** — *"not right now"* — with design notes attached: pathfinding to placed food, follow-the-finger, one note per movement and a higher note when it eats. Recorded, acted on by nothing. **And the deferral was right rather than merely convenient**: SNAKE's Timer is **870 of 908, 36 characters free**, so "one note per movement" does not fit as the card stands | The user un-deferring it, and a Timer with room — which means restructuring the card, not adding to it |
| 5 | **Four faders** | **Never tested.** No verdict is invented for it anywhere in this phase and none should be | A bench run |
| 6 | **`bloom` and `disturb`'s residue** | Left alone by 11-04 deliberately: both would need a **shape** change rather than a constant change, and **no preset selects either** and `touch.kind` is not a knob, so neither is reachable from any catalog entry at any knob position | A preset that needs one, plus a decision on whether the compiler's emitted shape may change as well as its constants |
| 7 | **FORGE's precision, if task 11-07-03's sweep found nothing** | Conditional row. `11-07` sweeps FORGE's whole travel through the real Lua host — reachable macro indices against twenty-seven, boundary positions, target sizes, macro 26's reduced area behind the bank corner, and what a down-then-slide emits from an onset-gated handler. **If it found a defect, this row does not exist and the fix is in the delivered list. If it found nothing, this row carries the numbers**, because *"waay not precise enough"* is one of the thirty-five and a phase that closes without naming it has dropped it | A bench answer about what "precise" meant — the target geometry, or the fact that a press is final on a labelless twenty-seven-target pad. FORGE has 189 free at worst in Setup and 532 in Timer, so budget is not what is missing |
| 8 | **LATTICE's "clamp it better", if task 11-07-03's sweep found nothing** | Conditional row, same shape. LATTICE's other two clauses were delivered — the fast tap in 11-02 at +8, which took it from **0 messages on a fast tap against 2 on a slow one** to parity. The clamp clause was swept in 11-07 because CONSOLE's identical-sounding clamp complaint turned out to be a real ceiling at 111 of 127 | A bench answer, or a value the sweep names. 283 free at worst after 11-02's +8 |
| 9 | **MORPH's "mapping mode"** | The other two clauses of MORPH's note **were** delivered — the stuck colours in 11-02 (class A and class B) and the suppression in 11-08 — and this one was put to the user at 11-09's checkpoint rather than guessed, because it admits an assignment mode, a mapping-curve knob, or nothing at all. Record the user's answer verbatim | Whichever reading they chose, costed against the margin 11-08 left. `morph-nothing` closes it as a decision rather than a gap |

**MORPH's note must be reported clause by clause**, against 11-02, 11-08 and 11-09's answer. A
three-clause note reported as "answered" because one clause was is the failure this phase's gates
exist to catch, and it is why FORGE — whose single-clause note reached no plan at all until this phase
was revised — is a row above rather than a silence.

**Plus the nine dead `/c/<id>/` addresses**, shipped as the honest default and restated here as an
Open-for-the-user item **with its reversal costed**: a prerendered stub per removed id, or a
`_redirects`-style rule at the Worker — the second possible only because the site is on Cloudflare
Workers rather than GitHub Pages, which is a live consequence of a Phase 1 hosting decision.

## What `docs/TESTING.md` owes

Every count and every cost re-measured on a clean tree against a fresh production build:

- **per-file counts** for every file this phase touched, plus the three created — including
  `lua-smoke.spec.ts`'s corrected **+17** (or +16) across ten plans, against
  `11-VALIDATION.md:255`'s **+8** across five, **named as wrong**;
- **the cost table**: the sweep wall clock against `BASE_SWEEP_WALL` (it should have **fallen** — 36
  entries became 28, and a wall clock that did not fall is a finding), the build time, the e2e wall
  time and free memory, and `static/og/`'s total against `BASE_OG_BYTES` at the branch table's file
  count (28, or 29 under `new-entry`);
- **the delta chain**, reconciled row by row, with **every row the planner got wrong named as wrong**;
- the prose counts 11-01 moved: *"twenty-seven configurations, eighteen of them hand-authored"* becomes
  the phase's final numbers; the `restsBlack` pair; `static/og/`'s file count; the sweep's knob totals.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 11-16-01: Every projection replaced by an observation, and every wrong one named</name>
  <files>docs/TESTING.md, .planning/phases/11-bench-corrections/deferred-items.md</files>

  <read_first>
    - all **sixteen** SUMMARYs in `.planning/phases/11-bench-corrections/`
    - `11-VALIDATION.md`'s delta table, per-file table and cost table
    - `docs/TESTING.md` in full
    - `docs/PIN-POLICY.md` item 4, as 11-03 extended it
  </read_first>

  <action>
**1. Re-measure everything, on a clean tree, in this order**, recording free memory beside each:

```
npm run check 2>&1 | grep -Ei "error|warning"
npm run lint
npm run test:quick 2>&1 | node scripts/check-counts.mjs <observed> <observed>
npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19
npm run build
npm run test:quick     (again - three specs read build/)
npx playwright test --workers 3 2>&1 | node scripts/check-counts.mjs --playwright <observed>
```

Then `rm -r test-results` by hand and confirm `git status --porcelain` empty.

**2. Replace `11-VALIDATION.md`'s three projected tables with observations, in `docs/TESTING.md`:**
per-file counts, the cost table, and the delta chain reconciled row by row.

**Name every row the planner got wrong.** Not "corrected to" — **wrong, and by how much, and why the
estimate missed.** A validation document whose estimates were never checked teaches the next phase to
estimate carelessly, and this phase's whole discipline rests on that not happening.

**3. The sweep wall clock is a finding either way.** It should have **fallen**: the catalog went from
36 entries to 28 and the sweep enumerates per entry. State it against `BASE_SWEEP_WALL` with the
percentage. **A wall clock that did not fall means the removal did not reach the sweep** and that is
worth chasing, not rounding.

**4. The prose counts.** `docs/TESTING.md` names *"thirty-six configurations, twenty-seven of them
hand-authored"*, the `restsBlack` triple, `static/og/`'s file count and the sweep's knob totals. Move
every one to the phase's final numbers, and **grep for each rather than remembering where they are** —
this is the exact shape of Phase 10's seven miscounts.

**5. `deferred-items.md`.** The six from `<interfaces>` plus everything the sixteen SUMMARYs recorded,
each with **what would close it** — a work list, not a wish list. Include, at minimum, the gaps the
plans found rather than the work they deferred:

- **no gate asserts that a card is legible** (11-12), which is why *"don't understand how it works"*
  could not have been caught by this suite;
- **no gate catches a Timer that stops silently** on a zero period (11-12), the failure the entry's own
  header warns about;
- **no test asserts what a demo path depicts** (11-11), so an OG image of the wrong gesture ships
  green;
- **nothing validates seven-bit sysex data** (11-10), worth knowing before a second entry uses it;
- **`bloom` and `disturb` are forbidden by a header comment rather than a gate** (11-05, 11-06);
- **no gate asserts that a card's whole travel is reachable** — 11-07 had to sweep CONSOLE, FORGE and
  LATTICE by hand to find out, and the three sweeps it left behind are per-card assertions rather than
  a rule any future card inherits;
- **a knob's value list can be re-cut without any test noticing what existing links now render**
  (11-02, 11-09, 11-10) — `stamp.spec.ts` compares indices, and the only guard built this phase was
  POMODORO's four captured literals.

**Negative check:** raise one non-vacuity floor by one — `facets.spec.ts`'s, or `decay-idiom.spec.ts`'s
`KNOWN_VIOLATIONS` emptiness. Expect its own test red, naming the observed count. Revert; confirm
`git diff --quiet`.
  </action>

  <verify>
    <automated>npm run check 2>&1 | grep -Ei "error|warning" ; npm run lint && npm run test:quick && npm run test:sweep && npm run build</automated>
    Every gate green, every number recorded, `test-results/` removed, tree clean.
  </verify>

  <done>
`docs/TESTING.md` carries observations where `11-VALIDATION.md` carried projections, with every wrong
estimate **named as wrong** and by how much. The sweep wall clock stated against `BASE_SWEEP_WALL` with
its percentage and chased if it did not fall. Every prose count found by grep and moved.
`deferred-items.md` exists with the six non-deliveries and the six gates that do not exist, each with
what would close it. One negative check red.
  </done>
</task>

<task type="auto">
  <name>Task 11-16-02: The phase gate against a fresh production build, and two requirements amended</name>
  <files>.planning/REQUIREMENTS.md, docs/HARDWARE-AUDITION.md</files>

  <read_first>
    - `11-01-SUMMARY.md` — `BASE_FILES`, `BASE_TESTS`, `BASE_SWEEP_WALL`, `BASE_E2E`, `BASE_CATALOG`,
      `BASE_OG_BYTES`, **and any disagreement it recorded against the brief's block**
    - `11-03-SUMMARY.md` — the two amendments recorded in advance, with proposed wording
    - `11-04-SUMMARY.md` — the vendored diff verbatim and the manifest's rows
    - `.planning/REQUIREMENTS.md` CONT-01 and FOUND-02, verbatim
  </read_first>

  <action>
**1. The gate, against a fresh production build**, every arithmetic written out:

- `npm run test:quick` at **`BASE_FILES + 3` / `BASE_TESTS + 36`** — or **`+ 35`** if 11-14's
  recorded answer was `fold-into-sonar` — with the chain from `<interfaces>` written out plan by plan
  **and each term checked against that plan's own `check-counts` line**. **State which branch applies
  and why before running the command.** A plan whose asserted delta and whose tasks disagree is a
  defect that must be **named here rather than absorbed into the total** — but a term that differs from
  `11-VALIDATION.md`'s stale plan-level row for 11-07 or 11-08, or from its wrong `lua-smoke.spec.ts`
  reference row, is a **document** defect and is named as that instead;
- **the count of terms asserted at seventeen**, equal to the plan count, and checked against every prose
  statement of it in `11-VALIDATION.md` and in this plan. This is the number that drifted twice in
  Phase 10 after being fixed;
- `npm run test:sweep` at `4 19` with the wall clock against `BASE_SWEEP_WALL`;
- `npx playwright test --workers 3` at **`BASE_E2E + 2`**, as the seventeen-term chain, every term
  written out, **with `grep -c "test("` asserted at 86 beside it and the reason the two deltas differ
  stated**, with the **five** suite-running plans named — 11-01, 11-05, 11-08.1, 11-15 and this one —
  and the documents that said three, and then four, corrected by name;
- **the catalog at the branch table's total for 11-14's recorded answer — 28, or 29 under
  `new-entry`** — as a seventeen-term chain with the split beside it and `T14` stated explicitly rather
  than folded in;
- `static/og/` at **28 files, or 29 under `new-entry`**, with its byte total against `BASE_OG_BYTES`
  and the branch named;
- **`git diff --stat HEAD -- src/vendor/` equal to `upstream-manifest.json`'s `intendedDivergence`
  rows, file by file and hunk by hunk** — pasted verbatim, both sides, side by side;
- **`src/lib/fidelity/firmware-oracle.spec.ts` green and unedited** (`git diff --quiet`), which is the
  mechanical proof D-02's boundary held: the compiler's constants moved and the simulator's phase walk
  did not;
- **`src/lib/fidelity/preset-baseline.json` byte-unchanged** (`git diff --quiet`), which is the proof
  D-08 survived a phase that was allowed to edit the vendored tree.

**2. Amend CONT-01 and FOUND-02**, by name and dated, using 11-03's recorded wording. Quote the current
text, state the new text, name the five diverging presets and the one diverging vendored test file, and
point at `upstream-manifest.json` as the authority. **Neither is deleted and neither is softened.**

**3. Record every other requirement this phase touched with a qualifier saying what was proved AND
what was not.** At minimum: CONT-02 (every entry still fits 908 at every knob position, with the
tightest margin named); CONT-03 (tags re-cut, eight FOR terms, the FEELS floor at 6 and what breaks
next); CAT-03/CAT-04 (the vocabulary and the catalog's shape); PREV-01/PREV-02/PREV-06 (the preview
still faithful, and **which parts of that are unverifiable without a module**); TUNE-01/TUNE-05 (the
new knobs, the reachability totals); SHARE-01/SHARE-03 (**every knob value list re-cut this phase,
named, with the sentence that existing links still decode and now render something different**);
SHARE-04 (28 OG images).

**4. `docs/HARDWARE-AUDITION.md`** — final row count stated from the branch table (**23 after 11-01,
24 after 11-15**; or **24 after 11-14 and 25 after 11-15** under `new-entry`), contiguous,
with every row from this phase's redesigns and the wheels present, and **every row still the user's and
unanswered.**

**5. Six ROADMAP-adjacent facts for the SUMMARY, not for the ROADMAP.** This phase was planned under an
instruction not to edit `.planning/ROADMAP.md`, so the Phase 11 block's `Requirements: TBD` and
`Plans: 0` are **left exactly as they stand**, and everything this plan would have written there —
the requirement list, the seventeen plans, the final counts — goes into the SUMMARY for whoever updates
it. **Say that explicitly**, so a later reader does not mistake an untouched roadmap for an oversight.

**Negative check:** change one `intendedDivergence` row's `reason` to an empty string. Expect
`vendored-diff.spec.ts`'s justification test red, naming the file and the field — the record's own
guarantee, verified at the gate rather than trusted from wave 3.
  </action>

  <verify>
    <automated>npm run test:quick && npm run test:sweep && npm run build && npx playwright test --workers 3 && git diff --stat HEAD -- src/vendor/</automated>
    Every gate green at the stated arithmetic; the vendored diff equal to the manifest's rows; the tree
    clean after `test-results/` is removed.
  </verify>

  <done>
The gate is green against a fresh production build at `BASE_FILES + 3` / `BASE_TESTS + 36` — or `+ 32`
if 11-14 recorded `fold-into-sonar` — written as a seventeen-term chain with `T14` named, with the term
count asserted and every disagreement between a term and its plan's own line named rather than
absorbed, and a disagreement against `11-VALIDATION.md` named as a document defect instead. Sweep `4 19` with its wall clock; e2e `BASE_E2E + 2` in seventeen
terms with both e2e numbers stated; the catalog at the branch table's total as a chain with `T14` explicit. The vendored diff equals
the manifest's
rows, pasted verbatim. `firmware-oracle.spec.ts` green and unedited; `preset-baseline.json`
byte-unchanged. CONT-01 and FOUND-02 amended by name and dated; every other touched requirement given a
qualifier stating what was **not** proved. The untouched ROADMAP explained rather than left ambiguous.
One negative check red.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 11-16-03: The bench rows, and everything this phase could not do</name>

  <files>docs/HARDWARE-AUDITION.md, docs/MIDI-IN-PROBE.md</files>

  <read_first>
    - `docs/HARDWARE-AUDITION.md` at its final row count, every row still the user's
    - `docs/MIDI-IN-PROBE.md` in full, including its last line
    - `.planning/phases/11-bench-corrections/deferred-items.md` as task 01 wrote it
  </read_first>

  <action>
Present the three sections below to the user and stop. **Do not run any part of the bench list.**
**No agent in this phase connected to a device, performed a write, or deployed**, and this checkpoint
is where that promise is handed over rather than quietly kept. Order the bench list by answers per
minute rather than by importance: the probe and the DIAL question together take about eleven minutes
and unblock more than everything else combined.
  </action>

  <what-built>
Thirty-five of your thirty-six configurations were tested on hardware; this phase answered them. Nine
were removed. Two class bugs — a cell that could never fade to black, and a fast tap that produced no
MIDI at all — were fixed everywhere they occurred and both now have a test that stops them coming back.
Five preset cards you could not previously change are now HANGAR's to change, and five of your notes
were answered by doing so. GHOST, SHUTTLE, STRIP and RADAR were re-authored. The pitch and mod wheels
were added. **State the catalog size from the branch table rather than as a literal** — twenty-eight,
or twenty-nine if you chose to build the radar as a second card.

**Nothing in any of it has touched a ZONA.** Every claim above was measured in the simulator, in the
Lua host, or read out of firmware C.
  </what-built>

  <how-to-verify>
**The bench rows, in the order that gets the most answered per minute.**

1. **The MIDI-in probe** (`docs/MIDI-IN-PROBE.md`, ten minutes). Two short scripts are already written
   and already checked against the character budget. Part B answers whether ZONA can receive MIDI
   clock at all. **A no closes the clock-sync request rather than redirecting it**, which is worth
   knowing either way — four of your notes depend on it and none of them can move until it is
   answered.

2. **DIAL, one question, no hardware needed.** What relative-CC mode is the control set to in your
   host? The simulator says both directions are exactly symmetric — 192 messages each way, one sending
   65 and the other 63. If your host is in signed-bit mode, 63 reads as **+63**, which is violent in
   exactly the direction you reported. If that is it, the fix is either one line in your host's
   settings or a hand-written dial, and knowing which decides whether it is a phase of work or none.

3. **AURORA and STARFIELD's glow.** The trail used to leave one or two units of light on every cell a
   finger crossed, permanently, and that is now fixed at its source. **Is the glow gone?** If something
   larger is still there, the simulator does not reproduce it and only your bench can say.

4. **GHOST.** Run the same gesture sequence twice, including the reset, and see whether it behaves the
   same both times. That is the specific complaint and it is the one no test can settle.

5. **SHUTTLE.** Pick it up cold, without being told how it works, and see whether the direction, the
   zero point and the stop are readable from the pad. **No gate in this repository asserts that a card
   is legible**, which is exactly why your original note could not have been caught by the suite.

6. **The wheels.** Does the pitch spring-back look like travel rather than a jump? And after you lift,
   is a held note still bent? The second one is asserted in software down to the exact centre value,
   but only a module and an ear can confirm it.

7. **FORGE and LATTICE.** Both were swept through the simulator's Lua host across their whole travel
   because your notes on them said "not precise". **Report what the sweep found and what it did not**
   — and if it found nothing, the question that only your hands can answer is what "precise" meant:
   whether the targets are too small, or whether the problem is that a press is final and sliding onto
   the right one does not correct it.

8. **Four faders — still untested.** No verdict has been invented for it anywhere in this phase.

**The things this phase could not do**, each recorded in `deferred-items.md` with what would close it
— six certain, plus FORGE's and LATTICE's precision notes if their sweeps came back clean, plus MORPH's
"mapping mode" per your answer:
clock sync (blocked twice over); DIAL's encoder convention; TRACKPAD's animation (its state normalises
any animation away, and it has six characters free of 908); SNAKE (**you deferred it, and it turned out
to be the right call — its Timer has 36 characters free, so one note per movement would not have
fitted**); Four faders; and the `bloom`/`disturb` residue, which no card can currently reach.

**And one decision that is yours rather than a gap:** nine `/c/<id>/` addresses stop resolving when
this deploys. Anyone who shared a link to HOLD, KEYS, LEARN, SWITCH, ETCH, GRIDLOCK, LIFE, SLAM or
TABLE will get a 404. That is shipped as-is. Reversing it is either a stub page per removed id or one
redirect rule at the Worker — the second is available only because the site is on Cloudflare rather
than GitHub Pages.
  </how-to-verify>

  <resume-signal>
Type "approved" to close the phase, or describe what the bench found. Anything you answer here becomes
a row in the next phase rather than a note in this one.
  </resume-signal>
</task>

</tasks>

<verification>
- `npm run check 2>&1 | grep -Ei "error|warning"` reads `0 ERRORS 0 WARNINGS`; `npm run lint` clean.
- `npm run test:quick 2>&1 | node scripts/check-counts.mjs BASE_FILES+3 BASE_TESTS+36` — or
  `BASE_TESTS+35` under `fold-into-sonar` — the chain written out in seventeen terms with `T14` named,
  and the term count asserted at seventeen.
- `npm run test:sweep 2>&1 | node scripts/check-counts.mjs 4 19`, wall clock against
  `BASE_SWEEP_WALL`, **expected to have fallen**.
- `npm run build`, then `npm run test:quick` again, then `npx playwright test --workers 3` at
  `BASE_E2E + 2` with `grep -c "test("` at 86 beside it. `test-results/` removed by hand;
  `git status --porcelain` empty.
- `git diff --stat HEAD -- src/vendor/` pasted beside `upstream-manifest.json`'s `intendedDivergence`
  rows and asserted equal file by file and hunk by hunk.
- `git diff --quiet -- src/lib/fidelity/firmware-oracle.spec.ts src/lib/fidelity/preset-baseline.json`.
- `git diff --quiet -- .planning/ROADMAP.md` — **untouched, deliberately.**
</verification>

<success_criteria>
1. Every projection in `11-VALIDATION.md` is replaced by an observation, and every wrong one is named
   as wrong and by how much.
2. The phase total is `BASE_FILES + 3` / `BASE_TESTS + 36` — or `+ 35` under `fold-into-sonar` —
   written as a seventeen-term chain with `T14` named, the **term count itself asserted**, and every term
   reconciled against its plan's own line. A term that disagrees with `11-VALIDATION.md` rather than
   with its plan is named as a **document** defect, including that document's `lua-smoke.spec.ts` row
   (+8 across five plans, against the real +17 across ten).
3. The catalog is **the branch table's total for 11-14's recorded answer**, 28 or 29, stated as a
   seventeen-term chain with its split, with `T14` explicit and the answer named beside it — and
   `static/og/` and the audition row count read from the same row.
4. The e2e total is `BASE_E2E + 2` in seventeen terms — sixteen written-out zeros and 11-08.1's `+2`
   — with **both** e2e numbers stated and the reason they differ, with **five** suite-running plans
   named, and with the documents that said three, and then four, corrected.
5. `git diff --stat HEAD -- src/vendor/` equals the manifest's enumerated divergence exactly, and
   `firmware-oracle.spec.ts` and `preset-baseline.json` are both untouched — the two mechanical proofs
   that D-02's boundary and D-08's rule both held through a phase that was allowed to edit the vendored
   tree.
6. CONT-01 and FOUND-02 are amended by name and dated, neither deleted nor softened, and every other
   touched requirement carries a qualifier saying what was **not** proved.
7. Every non-delivery is named with what would close it — the six certain ones, plus FORGE's and
   LATTICE's precision notes if their sweeps came back clean, plus MORPH's "mapping mode" per the
   user's answer — and **MORPH's three clauses are reported separately** against 11-02, 11-08 and
   11-09. **No bench note closes the phase without a plan, a removal, a deferral or a measurement
   attached to it by name**, and the count is stated with its branch rather than as the word "six".
8. The seven missing gates are named with what would close each, and the hardware rows are handed over
   unanswered by an agent that never touched a device.
</success_criteria>

<output>
After completion, create `.planning/phases/11-bench-corrections/11-16-SUMMARY.md` carrying: the
re-measured block against 11-01's baseline; **11-14's recorded answer and the branch-table row every
downstream number was derived from**; the seventeen-term test chain with every term reconciled and every
disagreement named, distinguishing a plan defect from a stale `11-VALIDATION.md` row; the term count
asserted; the catalog chain with its split and `T14` explicit;
the e2e chain in seventeen terms with both e2e numbers and the reason they differ; the sweep wall clock with its fall as a percentage; the
vendored diff beside the manifest's rows; the two amendments with their before-and-after wording; every
other requirement's qualifier with its unproved half; every non-delivery with its branch — including
FORGE's and LATTICE's sweep results and MORPH's three clauses reported separately — and the seven
missing gates;
the nine dead links with their costed reversal; the user's checkpoint answer verbatim; and the note
that `.planning/ROADMAP.md` was deliberately not edited, with the requirement list, the seventeen plans
and the final counts written here for whoever updates it.
</output>
