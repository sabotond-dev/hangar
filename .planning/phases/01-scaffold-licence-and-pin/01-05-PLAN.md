---
phase: 01-scaffold-licence-and-pin
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - scripts/deploy.mjs
  - docs/DEPLOY.md
  - package.json
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: false
requirements: [FOUND-04]
user_setup:
  - service: github
    why: "D-01 — the repository lives at github.com/sabotond-dev/hangar and stays private permanently (D-02)"
    dashboard_config:
      - task: "Create the repository `hangar` under the `sabotond-dev` account, PRIVATE. Do not initialise it with a README, .gitignore or licence."
        location: "https://github.com/new"
    notes: "No `gh` CLI on this machine; push uses the stored credential via Windows Credential Manager."
  - service: cloudflare
    why: "D-05/D-07 — Workers static assets behind a Basic Auth gate at hangar.sabotond.workers.dev"
    env_vars:
      - name: SITE_USER
        source: "Set as a Worker secret AFTER the first `wrangler deploy` has created the Worker: `npx wrangler secret put SITE_USER` (value: hangar)"
      - name: SITE_PASSWORD
        source: "Set as a Worker secret AFTER the first `wrangler deploy`: `npx wrangler secret put SITE_PASSWORD` (a password you choose; never committed). The Worker fails closed, so it returns 401 to everything until this exists"
    dashboard_config:
      - task: "Confirm the account is authenticated and the workers.dev subdomain is `sabotond`"
        location: "`npx wrangler whoami` on this machine"

must_haves:
  truths:
    - "`npm run deploy` refuses to run when the working tree is dirty"
    - "`npm run deploy` builds, verifies the compliance artefacts exist, and then runs wrangler deploy"
    - "A visitor loading https://hangar.sabotond.workers.dev over HTTPS is challenged by Basic Auth"
    - "After signing in, the page renders and its footer Source link downloads an archive whose filename SHA matches the deployed commit"
    - "Responses carry X-Robots-Tag: noindex"
  artifacts:
    - path: "scripts/deploy.mjs"
      provides: "The one-command manual deploy with a clean-tree gate (D-08)"
      contains: "status --porcelain"
    - path: "docs/DEPLOY.md"
      provides: "The deploy procedure, the secret procedure, and the launch-day gate-removal note"
      contains: "wrangler secret put"
  key_links:
    - from: "scripts/deploy.mjs"
      to: "git status --porcelain"
      via: "hard exit(1) when non-empty — the archive is of HEAD but the bundle is of the working tree"
      pattern: "porcelain"
    - from: "scripts/deploy.mjs"
      to: "wrangler deploy"
      via: "execSync after the artefact assertions pass"
      pattern: "wrangler deploy"
---

<objective>
Make the deploy a single command that cannot ship a non-compliant artifact, then have the user run it and
confirm the four things only a real deploy can prove: HTTPS, the Basic Auth challenge, a rendering page,
and a Source link that downloads the archive for exactly the deployed commit.

Purpose: this closes success criterion 1 in its amended form. Every gate in the script exists because the
corresponding failure is silent — `git archive` archives HEAD while Vite bundles the working tree, so a
dirty-tree deploy serves an archive that does not correspond to the served bundle, which is precisely the
thing GPLv3 section 6(d) requires.
Output: `scripts/deploy.mjs`, `docs/DEPLOY.md`, and a live gated preview at
`https://hangar.sabotond.workers.dev`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-licence-and-pin/01-CONTEXT.md
@.planning/phases/01-scaffold-licence-and-pin/01-RESEARCH.md
@.planning/phases/01-scaffold-licence-and-pin/01-03-SUMMARY.md
@.planning/phases/01-scaffold-licence-and-pin/01-04-SUMMARY.md

<interfaces>
<!-- Verified facts and locked values. -->

**Before the first task, if this plan is running in a fresh `git worktree`:** run `npm ci` from the repo
root. Plan 01 installed into the main working tree; a worktree checkout has no `node_modules/`, and this
plan needs `wrangler` on disk and `npm run build` to work. Skip only if `test -d node_modules/wrangler`
already succeeds.

**Locked deployment values:**
- Worker name: `hangar` (from `wrangler.jsonc`)
- Preview URL: `https://hangar.sabotond.workers.dev` (D-06). The account's workers.dev subdomain is
  `sabotond`; the user's personal domain is `sanbotond.com` — **different spellings, do not conflate**.
  A custom domain is a launch-day decision and is out of scope.
- Git remote: `https://github.com/sabotond-dev/hangar.git` (D-01). Branch is `master`, not `main`.
- Cloudflare OAuth config is already present at
  `%APPDATA%/xdg.config/.wrangler/config/default.toml`; `gh` CLI is NOT installed and is not needed.
- `git remote -v` is currently empty.

**Why a Node script rather than an npm shell chain (D-08):** this machine is Windows. `SHA=$(...)` and
`&&`-chained env assignment behave differently in PowerShell, cmd and Git Bash. `execSync` with
`{ stdio: 'inherit' }` sidesteps the whole class of problem and makes the clean-tree refusal a real
`process.exit(1)` rather than a shell truthiness accident.

**The launch-day path (do NOT implement it now — D-03's embargo date is TBD):** removing the gate means
deleting the `SITE_PASSWORD` secret and the `run_worker_first` flag, not rewriting the Worker. The
fail-closed pattern is what makes that a one-line change. Nothing in Phase 1 performs an un-gated deploy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1-05-01: The one-command deploy with a clean-tree gate, and the deploy documentation</name>
  <files>scripts/deploy.mjs, docs/DEPLOY.md, package.json</files>

  <read_first>
    - `C:\Users\sabot\Documents\Claude\hangar\scripts\postbuild.mjs` (what `npm run build` already produces — do not duplicate it)
    - `C:\Users\sabot\Documents\Claude\hangar\package.json` (the current script surface)
    - `C:\Users\sabot\Documents\Claude\hangar\wrangler.jsonc` (worker name and assets directory)
    - `C:\Users\sabot\Documents\Claude\hangar\worker\index.js` (what the secrets are called)
    - `.planning/phases/01-scaffold-licence-and-pin/01-RESEARCH.md` §Pattern 5, §Pitfall 6, §Environment Availability
    - `.planning/phases/01-scaffold-licence-and-pin/01-CONTEXT.md` D-01, D-02, D-03, D-06, D-07, D-08
  </read_first>

  <action>
**1. `scripts/deploy.mjs`.** Steps in this exact order, each a hard failure:

```
1. const sha = git('rev-parse HEAD')            -> exit 1 with a clear message if git fails
2. git('status --porcelain')                    -> if NON-EMPTY: print the offending paths and exit 1.
                                                   NOT a warning. `git archive HEAD` archives the commit
                                                   while Vite bundles the working tree; a dirty deploy
                                                   serves an archive that does not match the bundle.
3. execSync('npm run build', { stdio: 'inherit' })
4. assert each of these exists, else exit 1:
     build/index.html
     build/404.html
     build/LICENSE
     build/THIRD-PARTY.md
     build/licenses
     build/source-<sha>.tar.gz
5. assert `tar -tzf build/source-<sha>.tar.gz` contains package-lock.json and contains
   no '.planning/' entry -> exit 1 otherwise, naming the offending entries
6. execSync('npx wrangler deploy', { stdio: 'inherit' })
7. print:  Deployed <sha> to https://hangar.sabotond.workers.dev
           Source archive: /source-<sha>.tar.gz
           The preview is gated by Basic Auth (D-07).
```

Header comment carries `Copyright (C) 2026 Botond Sandor`. No Claude attribution anywhere in the file or
in any commit message this plan produces.

Wire it up: `"deploy": "node scripts/deploy.mjs"`.

**2. `docs/DEPLOY.md`.** Required sections:

- `# Deploying HANGAR` and the one-line summary: deploys are manual from this machine, `npm run deploy`.
  No GitHub Actions, no Cloudflare Git integration (D-08).
- `## One-time setup` — the exact commands, in order:
  ```bash
  npx wrangler whoami                    # confirm the Cloudflare account
  npx wrangler secret put SITE_USER      # value: hangar
  npx wrangler secret put SITE_PASSWORD  # a password you choose
  npx wrangler secret list               # confirm both are set
  ```
  State the ordering constraint plainly: **on a brand new Worker the three `secret` commands only work
  after the first `npm run deploy`** — `wrangler secret put` targets a Worker that does not exist until
  something has been deployed. The Worker fails closed, so between the first deploy and the secrets being
  set every request returns 401 and the site is never publicly readable.
  State plainly: **the password is never committed.** It lives in the Worker secret and, for local
  `wrangler dev` and Playwright, in the gitignored `.dev.vars`. `.dev.vars.example` documents the shape.
- `## Deploying` — `npm run deploy`, and what each gate refuses and why. Include the note that a dirty
  tree is refused on purpose and the fix is to commit, not to bypass.
- `## What gets deployed` — `build/` only: the prerendered site, `LICENSE`, `THIRD-PARTY.md`,
  `licenses/` and `source-<sha>.tar.gz`. The Worker gates all of it.
- `## The preview gate` — the gate exists because a public deploy is **embargoed until a date the user
  will set (D-03, currently TBD)**. Removing it on launch day means deleting the `SITE_PASSWORD` secret
  and the `run_worker_first` flag in `wrangler.jsonc` — not rewriting the Worker, because the check is a
  single fail-closed early return. **Ask before any deploy that removes the gate.**
- `## Manual verification after a deploy` — the checklist Task 1-05-03 runs, written down so it can be
  repeated: HTTPS loads, Basic Auth prompt appears, page renders, the footer SHA matches
  `git rev-parse HEAD`, the Source link downloads an archive that opens, and
  `curl -I -u <user>:<pass> https://hangar.sabotond.workers.dev` shows `x-robots-tag: noindex`.
- `## Custom domain` — out of scope for Phase 1; launch-day decision (deferred).

**3. Land the D-02 amendment in the two documents that still contradict it.** ROADMAP criterion 1 and
`01-CONTEXT.md` already record that the repository is private permanently and that corresponding source
is discharged by a per-deploy archive, but `REQUIREMENTS.md` and `PROJECT.md` still say "public source".
Leaving them is how a future phase re-derives the wrong obligation. Make exactly these two edits — no
others, and **do not touch `CLAUDE.md`, which is generated**:

- `.planning/REQUIREMENTS.md`, FOUND-04: change "and the deployed site links to its public source" to
  "and the deployed site serves a source archive of the deployed commit (GPLv3 section 6(d) — the
  repository itself stays private, per D-02)".
- `.planning/PROJECT.md`, Constraints, Licensing bullet: change "HANGAR ships GPLv3 with public source"
  to "HANGAR ships GPLv3; the corresponding source is served as a per-deploy archive from the site
  itself and the repository stays private (D-02, GPLv3 section 6(d))". Apply the same correction to the
  "GPLv3, public source" row in the decisions table on the same page.

**4. Add the git remote** (idempotent;
exists yet — the push happens in the next task, after the user confirms it exists):
```bash
git remote get-url origin >/dev/null 2>&1 || git remote add origin https://github.com/sabotond-dev/hangar.git
```

**5. Prove the clean-tree gate bites.** Record in the SUMMARY:
```bash
echo "" >> README.md
npm run deploy        # MUST exit non-zero at step 2, before building, naming README.md
git checkout README.md
```
Do NOT run a real deploy in this task — that is the user's, in Task 1-05-03.
  </action>

  <acceptance_criteria>
    - `test -f scripts/deploy.mjs` succeeds
    - `node -e "const p=require('./package.json'); if(p.scripts.deploy!=='node scripts/deploy.mjs') process.exit(1)"` exits 0
    - `grep -q "status --porcelain" scripts/deploy.mjs` succeeds
    - `[ "$(grep -c 'process.exit(1)' scripts/deploy.mjs)" -ge 4 ]` succeeds (one hard exit per gate; `grep -q` cannot count, so this counts)
    - `grep -q "wrangler deploy" scripts/deploy.mjs` succeeds
    - `grep -q "hangar.sabotond.workers.dev" scripts/deploy.mjs` succeeds
    - `grep -q "sanbotond" scripts/deploy.mjs docs/DEPLOY.md` FAILS (the personal domain is a different spelling and is not used here)
    - `grep -q "\.planning/" scripts/deploy.mjs` succeeds (the archive-content gate is present)
    - `grep -q "Copyright (C) 2026 Botond Sandor" scripts/deploy.mjs` succeeds
    - `grep -qi "claude" scripts/deploy.mjs docs/DEPLOY.md` FAILS
    - `test -f docs/DEPLOY.md` succeeds
    - `grep -q "npx wrangler secret put SITE_PASSWORD" docs/DEPLOY.md` succeeds
    - `grep -q "never committed" docs/DEPLOY.md -i` succeeds
    - `grep -q "embargo" docs/DEPLOY.md -i` succeeds and `grep -q "TBD" docs/DEPLOY.md` succeeds
    - `grep -q "run_worker_first" docs/DEPLOY.md` succeeds (the launch-day removal path is documented)
    - `git remote get-url origin` outputs `https://github.com/sabotond-dev/hangar.git`
    - `grep -q "public source" .planning/REQUIREMENTS.md` FAILS and `grep -q "source archive of the deployed commit" .planning/REQUIREMENTS.md` succeeds
    - `grep -q "public source" .planning/PROJECT.md` FAILS and `grep -q "per-deploy archive" .planning/PROJECT.md` succeeds
    - `git diff --stat CLAUDE.md` is empty (the generated file was not touched)
    - With a deliberately dirty tree, `npm run deploy` exits non-zero and its output names the dirty path; the SUMMARY records this
    - `npm run lint` exits 0
  </acceptance_criteria>

  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts.deploy!=='node scripts/deploy.mjs') process.exit(1)" && grep -q "status --porcelain" scripts/deploy.mjs && grep -q "wrangler deploy" scripts/deploy.mjs && test -f docs/DEPLOY.md && git remote get-url origin | grep -q "sabotond-dev/hangar"</automated>
  </verify>

  <done>`npm run deploy` exists as a single Node script that refuses a dirty tree before building, verifies all six compliance artefacts and the archive contents, and only then runs `wrangler deploy`; the procedure, the secret handling and the launch-day gate-removal path are written down in `docs/DEPLOY.md`; the git remote points at the private repository.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1-05-02: Create the private repository, push, and confirm the Cloudflare account</name>
  <files>(no files — human action: GitHub repository creation, git push, `wrangler whoami`)</files>

  <read_first>
    - `C:\Users\sabot\Documents\Claude\hangar\docs\DEPLOY.md` §One-time setup (written in the previous task)
    - `.planning/phases/01-scaffold-licence-and-pin/01-CONTEXT.md` D-01, D-02, D-07
  </read_first>

  <action>
This task cannot be automated: creating a GitHub repository needs the web UI (no `gh` CLI on this
machine), and the push uses a credential only the user holds.

**Why the secrets are NOT in this task.** `wrangler secret put` targets a Worker that does not exist
until the first `wrangler deploy`. Asking for them here would make the checkpoint's own acceptance check
(`wrangler secret list` naming both) unsatisfiable. Setting the secrets is therefore a numbered step
INSIDE Task 1-05-03, immediately after the first deploy and before the browser checks. The Worker fails
closed, so in the gap between the first deploy and the secrets being set **every** request returns 401 —
the site is never publicly readable at any point.

**What Claude has already done:** the git remote is configured, the deploy script and its gates are
written, and the full test suite is green against the local static build.

**What you need to do, in order:**

1. **Create the repository.** Go to <https://github.com/new> and create `hangar` under the
   `sabotond-dev` account. Set it to **Private**. Do NOT initialise it with a README, a `.gitignore` or a
   licence — the local repository already has all three and an initialised remote would force a merge.

2. **Push the branch** (the branch is `master`, not `main`):
   ```bash
   git push -u origin master
   ```
   Windows Credential Manager holds the `sabotond-dev` credential, so this should not prompt. If it does,
   sign in as `sabotond-dev`.

3. **Confirm the Cloudflare account:**
   ```bash
   npx wrangler whoami
   ```
   Expect the account that already hosts zona-docs, with the workers.dev subdomain `sabotond`.
  </action>

  <acceptance_criteria>
    - `git ls-remote origin` exits 0 and lists a `refs/heads/master` ref
    - The GitHub repository page for `sabotond-dev/hangar` shows the **Private** badge
    - `npx wrangler whoami` exits 0 and names the expected account, with the workers.dev subdomain `sabotond`
    - No password appears in any tracked file: `git grep -i "SITE_PASSWORD=" -- . ':!.dev.vars.example'` returns no matches with a real value
  </acceptance_criteria>

  <verify>
    <automated>git ls-remote origin | grep -q "refs/heads/master"</automated>
  </verify>

  <resume-signal>Type "done" once the private repo exists, the push succeeded and `npx wrangler whoami` named the right account — or describe what failed. The Worker secrets come next, inside Task 1-05-03.</resume-signal>

  <done>The private repository exists at github.com/sabotond-dev/hangar with `master` pushed, and `npx wrangler whoami` confirms the Cloudflare account and the `sabotond` workers.dev subdomain. The Worker secrets are deliberately not set yet — the Worker does not exist until the first deploy in Task 1-05-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1-05-03: Run the first deploy and verify the live gated preview</name>
  <files>(no files — human verification of the live deploy)</files>

  <read_first>
    - `C:\Users\sabot\Documents\Claude\hangar\docs\DEPLOY.md` §Deploying and §Manual verification after a deploy
    - `.planning/ROADMAP.md` §Phase 1 success criteria 1 and 2 (criterion 1 in its CONTEXT-amended form)
  </read_first>

  <action>
PAUSE. This step is the user's, not Claude's. Do not run `npm run deploy` on their behalf, and do not
remove or weaken the Basic Auth gate under any circumstances.

Present the <what-built> summary and the numbered <how-to-verify> checklist below to the user verbatim,
then wait for their report. When they respond:
- If everything matched, record the deployed SHA and the observed status codes in the SUMMARY and close
  the phase.
- If the browser loaded WITHOUT a sign-in prompt AFTER the secrets were set (step 2), treat it as a
  blocker: the gate is not working. Stop,
  and report it rather than continuing.
- If the Source link 404s, compare the SHA in the footer against `git rev-parse HEAD` and against the
  archive filename in `build/` — a mismatch means the build and the archive were produced from
  different commits, which the clean-tree gate is supposed to prevent.

Never write the Basic Auth password into a file, a commit, or the SUMMARY.
  </action>
  <what-built>
Everything the deploy needs is in place and verified locally:
- `npm run deploy` — refuses a dirty tree, builds, asserts the six compliance artefacts and the archive
  contents, then runs `wrangler deploy`. Its dirty-tree refusal was observed working.
- `worker/index.js` — a fail-closed Basic Auth gate; `wrangler dev` was observed returning 401 without
  credentials, 401 with a wrong password and 200 with the right one.
- `build/` — the prerendered site plus `LICENSE`, `THIRD-PARTY.md`, `licenses/` and
  `source-<sha>.tar.gz`, with the footer Source link pointing at the archive for the exact built commit.
- The full test suite (Vitest + Playwright against the real static build through the real Worker) is
  green.

The only thing that cannot be proven without you: the real Cloudflare account, the real subdomain, the
real secret, and HTTPS. The first deploy is also what creates the Worker, so setting the two Worker
secrets is step 2 of the checklist below rather than something done beforehand.
  </what-built>

  <how-to-verify>
Run these in order from the repo root. Everything must be committed first — the deploy refuses a dirty
tree on purpose.

1. **Deploy.** This is also what creates the Worker, which is why the secrets come after it.
   ```bash
   git status --porcelain    # must be empty
   npm run deploy
   ```
   Expect it to print the deployed SHA and `https://hangar.sabotond.workers.dev`. Note the SHA.

2. **Set the two Worker secrets** (each opens an interactive prompt; the Worker now exists, so these
   succeed):
   ```bash
   npx wrangler secret put SITE_USER       # enter: hangar
   npx wrangler secret put SITE_PASSWORD   # enter a password you choose
   npx wrangler secret list                # both must be listed
   ```
   Choose any password you like. **Do not paste it into this session, into a file, or into a commit.**
   If you want the local `wrangler dev` and Playwright runs to use the same one, edit `.dev.vars`
   yourself — it is gitignored.

   Between step 1 and this step the Worker fails closed: **every** request returns 401 because
   `SITE_PASSWORD` is unset. The site is never publicly readable in the gap. Setting a secret takes
   effect on the deployed Worker immediately, with no redeploy.

3. **The Basic Auth challenge.** Open `https://hangar.sabotond.workers.dev` in a browser.
   - Expected: the browser shows a sign-in prompt for the realm **HANGAR preview** before any content.
   - If the page loads with no prompt, **stop** — the gate is not working and that is a blocker, not a
     cosmetic issue.

4. **The page renders.** Sign in with `hangar` and the password you set.
   - Expected: the page loads over HTTPS with the `HANGAR` heading and a footer.

5. **The Source link (success criterion 1, amended form).** In the footer:
   - Expected: a `Source` link, a `GPLv3` link, a `Third-party notices` link, and a 40-character commit
     SHA displayed beside them.
   - The displayed SHA must equal the SHA that `npm run deploy` printed and `git rev-parse HEAD`.
   - Click `Source`. Expected: a file named `source-<that same sha>.tar.gz` downloads.
   - **Open the downloaded archive in your archive tool.** Expected: it opens, contains a
     `hangar-<short-sha>/` folder, and inside it `package-lock.json`, `package.json`, `vite.config.ts`
     and `LICENSE`. It must contain **no `.planning/` folder and no `CLAUDE.md`**.

6. **The licence files (success criterion 2).** Visit `https://hangar.sabotond.workers.dev/LICENSE` and
   `.../THIRD-PARTY.md`.
   - Expected: the first is the GNU GPL v3 text; the second lists `@intechstudio/grid-protocol` as
     GPL-3.0 and `@wasm-fmt/lua_fmt` as MIT.

7. **The noindex header.**
   ```bash
   curl -I -u hangar:YOURPASSWORD https://hangar.sabotond.workers.dev/
   ```
   - Expected: `x-robots-tag: noindex, nofollow, noarchive` in the response headers.

Report back: the deployed SHA, whether each of the seven steps matched, and anything that did not.
**Do not paste the password.**
  </how-to-verify>

  <acceptance_criteria>
    - `npm run deploy` exits 0 and prints a SHA equal to `git rev-parse HEAD`
    - After the first deploy, `npx wrangler secret list` output contains both `SITE_USER` and `SITE_PASSWORD` (this criterion belongs here, not in Task 1-05-02: the Worker does not exist before the deploy)
    - `https://hangar.sabotond.workers.dev` returns 401 with no credentials (verifiable with `curl -s -o /dev/null -w '%{http_code}' https://hangar.sabotond.workers.dev/`)
    - With credentials the same URL returns 200 and the body contains `HANGAR`
    - `curl -I -u <user>:<pass> https://hangar.sabotond.workers.dev/` shows `x-robots-tag: noindex`
    - `curl -s -o /dev/null -w '%{http_code}' -u <user>:<pass> https://hangar.sabotond.workers.dev/source-$(git rev-parse HEAD).tar.gz` outputs `200`
    - `curl -s -u <user>:<pass> https://hangar.sabotond.workers.dev/LICENSE | head -n 1` contains `GNU GENERAL PUBLIC LICENSE`
    - `curl -s -u <user>:<pass> https://hangar.sabotond.workers.dev/THIRD-PARTY.md | grep -c "@intechstudio/grid-protocol"` is at least 1
    - The downloaded archive opens and contains no `.planning/` folder and no `CLAUDE.md`
    - The user confirms the browser showed a `HANGAR preview` sign-in prompt before any content
  </acceptance_criteria>

  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' https://hangar.sabotond.workers.dev/ | grep -q 401</automated>
  </verify>

  <resume-signal>Type "approved" with the deployed SHA, or describe what did not match.</resume-signal>

  <done>The site is live at https://hangar.sabotond.workers.dev over HTTPS behind a working Basic Auth gate; the page renders; the footer Source link downloads a source archive of exactly the deployed commit with the SHA shown beside it and no internal planning material inside; `/LICENSE` and `/THIRD-PARTY.md` are served; responses carry `X-Robots-Tag: noindex`.</done>
</task>

</tasks>

<verification>
```bash
git status --porcelain                # empty before deploying
npm run deploy                        # exits 0, prints the SHA and the URL
curl -s -o /dev/null -w '%{http_code}\n' https://hangar.sabotond.workers.dev/          # 401
curl -s -o /dev/null -w '%{http_code}\n' -u USER:PASS https://hangar.sabotond.workers.dev/  # 200
curl -sI -u USER:PASS https://hangar.sabotond.workers.dev/ | grep -i x-robots-tag       # noindex
curl -s -o /dev/null -w '%{http_code}\n' -u USER:PASS \
  "https://hangar.sabotond.workers.dev/source-$(git rev-parse HEAD).tar.gz"             # 200
```

Negative gate (run once, recorded in the SUMMARY):
```bash
echo "" >> README.md && npm run deploy    # MUST refuse, before building
git checkout README.md
```
</verification>

<success_criteria>
- `npm run deploy` is a single command that builds, verifies and deploys, and refuses a dirty working tree before it builds anything — verified by observing the refusal.
- `docs/DEPLOY.md` documents the procedure, the secret handling ("never committed"), the TBD embargo date, and the exact launch-day path for removing the gate.
- The repository is pushed to the private `github.com/sabotond-dev/hangar` on branch `master`.
- `https://hangar.sabotond.workers.dev` is live over HTTPS, returns 401 without credentials and renders after signing in.
- The footer Source link downloads `source-<deployed sha>.tar.gz`, the SHA is shown beside it, the archive opens, and it contains the lockfile but no `.planning/` and no `CLAUDE.md`.
- `/LICENSE` and `/THIRD-PARTY.md` are served from the site root; responses carry `X-Robots-Tag: noindex`.
- The Basic Auth gate is still in place at the end of the phase. Nothing in this plan removed it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-licence-and-pin/01-05-SUMMARY.md`, recording the
deployed SHA, the observed status codes, the result of the dirty-tree negative check, and the fact that
the embargo date (D-03) is still TBD and must be asked before any un-gated deploy. **Do not record the
Basic Auth password.**
</output>
