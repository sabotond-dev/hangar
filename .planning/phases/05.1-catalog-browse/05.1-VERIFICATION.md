---
phase: 05.1-catalog-browse
verified: 2026-09-05T01:26:30Z
status: passed
score: 12/12 must-haves verified
re_verification: false
human_verification:
  - test: "Open /browse/ at 1280x720 and scroll the whole wall, then open one card and press BACK TO BROWSE"
    expected: "Sixteen pads paint without stutter; the return lands on the same scroll offset with the same chips lit; the three resting-dark cards read their sentence rather than looking broken"
    why_human: "Perceived smoothness and 'does a black square read as honest or as broken' are judgements no grep or paint counter can make. The paint count (148 frames / 2 s, 4 of 16 cards on screen) and the scroll delta (573 -> 573, 2px tolerance) are recorded, but the feel is not."
  - test: "Navigate /browse/ with a screen reader (NVDA or VoiceOver): arrive on a filtered link, type a query, press a chip, change the sort"
    expected: "One announcement per settled change, naming the sort once and the count once; the always-present expansion tells a cold arrival how much of the catalog is showing"
    why_human: "The live region's timing, the one-voice rule and whether the wording is actually useful can only be heard. The DOM structure (one aria-live, one aria-hidden visible line, one always-present sr-only expansion) is verified mechanically."
  - test: "Load a bare cold /browse/ with the network panel open, at the default Featured sort"
    expected: "The Lua VM (glue.wasm, ~271 KB) IS fetched, because five of the eight featured entries are hand-authored Lua and sit in the first screenful"
    why_human: "This is a product decision, not a defect. D-06's guard is worded 'a cold /browse/ WITH NO LUA CARD IN VIEW fetches no WebAssembly' and the shipped e2e honours exactly that wording, saying so in a comment. The user should confirm the first-screenful VM fetch is acceptable, or ask for a curated default order that defers it."
---

# Phase 5.1: Catalog Browse — Verification Report

**Phase Goal:** The sophisticated catalog behind the front door — the whole collection browsable, sortable and searchable without leaving the identity.
**Verified:** 2026-09-05T01:26:30Z
**Status:** passed
**Re-verification:** No — initial verification

Every number below was taken on this machine during this verification. Nothing is quoted from a SUMMARY.

## Goal Achievement

### Observable Truths

Derived from the three ROADMAP Success Criteria, which take priority over the Goal sentence.

| #   | Truth                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `/browse/` shows every configuration with name, one-line description, feel tags and the Featured flag               | ✓ VERIFIED | `build/browse/index.html` carries sixteen distinct `data-testid="card-<id>"`, sixteen `pad-canvas-*`, and eight `FEATURED` band words (the ninth occurrence is the sort control's own label). Card DOM order is `arc aurora chorus euclid lattice morph ninepads pinwheel dial faders ghost joystick radar sonar starfield tpad` — the `featuredOrder` comparator applied to the listing, element for element. |
| 2   | The listing is one source, gated equal to CATALOG and `frames.json`                                                 | ✓ VERIFIED | `src/lib/catalog/listing.ts` (326 lines) imports nothing at runtime — one `import type` of `./front-door`. `listing.spec.ts` tests 1–5 assert field-by-field equality with the catalog in both directions, motion derived from `restsBlack` then `frames.json`, the row/exclusion partition, and the no-runtime-import rule. All green.                          |
| 3   | Cards are live pads on one SimHost, with Lua engines built lazily per card                                          | ✓ VERIFIED | `BrowseGrid.svelte` constructs exactly one `new SimHost()`; `import("$lib/sim/engine")` sits INSIDE the IntersectionObserver callback, per card, guarded by a `started` Set. e2e `browse.e2e.ts:611` measured **148 pad frames in two seconds** (1280×720, 4 of 16 cards on screen, 4 engines built).                                                          |
| 4   | The resting-black note appears on tpad, GHOST and MORPH — never an unexplained black square                          | ✓ VERIFIED | `restsBlack: true` on exactly three entries (listing.ts:208, 257, 282). `RESTS_DARK_NOTE` appears exactly **3 times** in `build/browse/index.html`. D-14's restsBlack-first classification is stated in `listing.ts` and pinned by `listing.spec.ts` test 2.                                                              |
| 5   | The display transform curls the three ASCII apostrophes without touching `src/vendor/`                              | ✓ VERIFIED | `git diff --stat 16e5232..HEAD -- src/vendor/` is **empty**. `vendored-diff.spec.ts` green. ghost.ts and morph.ts carry U+2019 at source (bytes `e2 80 99`); Radar's vendored sentence keeps ASCII in `listing.ts` and renders curled — `build/browse/index.html` shows `finger’s`, while `build/c/radar/index.html`'s `meta description` keeps `finger's`. |
| 6   | Sort by Featured, Newest and Name, gated against the shipped comparators, with no locale dependence                  | ✓ VERIFIED | `sort.ts` restates `byNameThenId`/`featuredOrder`/`newestOrder`; `sort.spec.ts` tests 2–4 gate the id sequences against `byFeatured()`/`byNewest()`/`byName()` and test 5 scans comment-stripped source. Repo-wide grep for `localeCompare`/`Intl.` finds **two hits, both inside comments** (sort.ts:33, catalog/index.ts:60).                            |
| 7   | No popularity metric is shown or faked                                                                              | ✓ VERIFIED | `browse-ui.spec.ts` test 1 scans the five components, the page and every `src/lib/browse/*.ts` for eight fragment-assembled needles, with a self-gate that proves the detector still recognises them. Green. `ListingEntry` carries no count field of any kind. W-04 refuses a count on any chip.                          |
| 8   | Search folds case and diacritics; nine standing chips at count ≥ 2, AND-combined, disabled at zero, outsider removable | ✓ VERIFIED | `fold()` = NFD → `\p{Diacritic}` strip → `toLowerCase` (never `toLocaleLowerCase`, asserted). `chipTags` derives ≥ 2; `build/browse/index.html` renders exactly **nine** `tag-*` chips (playable, generative, gestural, hypnotic, ambient, colour, drums, expressive, readable). `filterListing` intersects. `disabledTags` feeds a real `disabled` attribute on a real checkbox. `BrowseToolbar`'s `row` appends any active non-standing tag after the nine. |
| 9   | The count is said three ways; the URL projects `?sort=&q=&tag=`; the popstate re-seed is fixed                       | ✓ VERIFIED | Three elements shipped: visible `browse-count` (aria-hidden), always-present sr-only `browse-count-expansion`, and one `aria-live="polite"` `browse-live`. `serialiseBrowseQuery` emits the empty string for the default so `/browse/` stays canonical. The `afterNavigate` popstate re-seed reads `window.location.search` and is proven by `browse.e2e.ts:827`, which passed. |
| 10  | All sixteen configurations open at `/c/<id>/`; off-row entries get a row of one; the tuning region docks              | ✓ VERIFIED | `entries()` maps `LISTING` → **sixteen** `build/c/*/index.html` and **sixteen** `build/og/*.png` present after a fresh build. `row` is `[listed]` when `frontDoorIndex(id) === -1` — eight such entries (tpad plus seven Lua). `Coverflow.svelte` mounts `TuningRegion`, `ChosenPanel`, `TryOnDevice`; `KeepOnDevice.svelte` ships a real `disabled` attribute. `first-experience.e2e.ts:467` passed. |
| 11  | `BACK TO BROWSE` restores scroll, sort, query and tags via `hangar:browse-return`                                    | ✓ VERIFIED | `browse.e2e.ts:1030` passed, printing **`browse round trip: scrollY 573 before, 573 after (tolerance 2px)`**. `BrowseLink.svelte` carries one slot with two labels (W-01/D-19); `readBrowseReturn` shape-checks `href` and `scrollY` and never throws; only the recorded QUERY crosses, the path is the component's own literal. |
| 12  | The D-18 engine hazard is closed and proven in a browser; the keyboard is a roving tabindex with clamping arrows      | ✓ VERIFIED | `tune/model.ts` `destroy()` closes an engine only when `engine !== published` — ownership transfers at `onpreview`. `browse.e2e.ts:1169` ("un-choosing a hand-authored configuration leaves its pad running") passed. `browse.e2e.ts:896` passed, printing **`browse keyboard: 11 Tab presses from the search field into the grid`** — one stop for the whole wall. `nextIndex` clamps, returns `undefined` for unowned keys so Space and Enter survive. |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact                          | Expected                                          | Status     | Details                                                                                                              |
| --------------------------------- | ------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------- |
| `src/lib/catalog/listing.ts`      | Import-free sixteen-entry browse data             | ✓ VERIFIED | 326 lines, one `import type`, read by the route, `gen-og.mjs`, `og/build.spec.ts`, `artifacts.e2e.ts`, `BrowseGrid`     |
| `src/lib/browse/sort.ts`          | Three total orders                                | ✓ VERIFIED | 105 lines; id tie-break makes all three total; `sortListing` never reorders in place                                   |
| `src/lib/browse/filter.ts`        | Folded search, AND tags, derived chips            | ✓ VERIFIED | 152 lines; nine chips derived not declared                                                                             |
| `src/lib/browse/grid.ts`          | Keyboard arithmetic                               | ✓ VERIFIED | 114 lines; clamps, never wraps; `undefined` for unowned keys                                                           |
| `src/lib/browse/query.ts`         | URL parse/serialise                               | ✓ VERIFIED | 137 lines; drops unknown tags, trims on read only                                                                      |
| `src/lib/browse/return.ts`        | The session record                                | ✓ VERIFIED | 161 lines; shape-checked, never throws                                                                                 |
| `src/lib/browse/typographic.ts`   | The display transform                             | ✓ VERIFIED | 58 lines; imports nothing; lookahead not lookbehind (Safari < 16.4)                                                    |
| `src/lib/ui/CatalogCard.svelte`   | One link, one tab stop, pad as card               | ✓ VERIFIED | 320 lines; anchor + `::after` overlay; pad wrapper `aria-hidden`; no pointer handler                                    |
| `src/lib/ui/BrowseGrid.svelte`    | Sixteen live pads, one clock                      | ✓ VERIFIED | 491 lines; one SimHost, two observers, per-card lazy build, `repaintAll` after reorder                                  |
| `src/lib/ui/BrowseToolbar.svelte` | Search, sort row, chips, one live region          | ✓ VERIFIED | 620 lines; Escape scoped to the field (W-18)                                                                           |
| `src/lib/ui/TagChip.svelte`       | Checkbox chip, colour never the only channel      | ✓ VERIFIED | 147 lines; real `disabled`; 44px floor                                                                                 |
| `src/lib/ui/BrowseLink.svelte`    | One slot, two labels                              | ✓ VERIFIED | 254 lines; anchor when cold, button when a record exists                                                                |
| `src/routes/browse/+page.svelte`  | The page, the runes, the projection               | ✓ VERIFIED | 542 lines; only catalog specifier is `$lib/catalog/listing`                                                            |
| `src/routes/c/[id]/+page.ts`      | `entries()` over the listing                      | ✓ VERIFIED | Sixteen entries; `beforeNavigate` (not `afterNavigate`) ends the record's life, with the measurement recorded in-file    |
| `src/lib/sim/host.ts`             | `repaintAll`                                      | ✓ VERIFIED | Guards `destroyed`, paints every registered entry at one `now()`                                                        |
| `src/lib/tune/model.ts`           | The D-18 handover                                 | ✓ VERIFIED | `published` tracks the handed-over engine; `destroy()` never closes it                                                  |
| `src/lib/ui/fidelity-line.ts`     | One source for the 231-character claim            | ✓ VERIFIED | Imports nothing; read by both `FidelityLine.svelte` and `/browse/`                                                      |
| `scripts/gen-og.mjs`              | Sixteen images from `ROUTED`                      | ✓ VERIFIED | `ssrLoadModule("/src/lib/catalog/listing.ts")` → `ROUTED`; sixteen PNGs emitted                                          |
| `e2e/browse.e2e.ts`               | Eleven browser tests                              | ✓ VERIFIED | All eleven passed                                                                                                      |
| `e2e/browse-webkit.e2e.ts`        | Three phone tests                                 | ✓ VERIFIED | All three passed on `webkit-phone`                                                                                     |

### Key Link Verification

| From                    | To                                | Via                                              | Status  | Details                                                                                       |
| ----------------------- | --------------------------------- | ------------------------------------------------ | ------- | --------------------------------------------------------------------------------------------- |
| `/browse/`              | `listing.ts`                      | `import { LISTING }`                             | ✓ WIRED | The page's ONLY catalog specifier; held by `config-shape.spec.ts` tests 13 and 14              |
| `/browse/`              | `BrowseToolbar` / `BrowseGrid`    | props + four callbacks                           | ✓ WIRED | `shown = sortListing(filterListing(LISTING, q, tags), sort)` reaches the grid                   |
| `BrowseGrid`            | `SimHost`                         | `register()` after `collect()` + `build()`       | ✓ WIRED | Canvas from `PadCanvas.onready`, engine from the lazy import, both required before `register`   |
| `BrowseGrid`            | `$lib/sim/engine`                 | `await import()` inside the observer callback     | ✓ WIRED | Never at module or mount scope — proven by the cold-load e2e                                    |
| `CatalogCard`           | `/c/<id>/`                        | `resolve("/c/[id]", { id })` on one anchor        | ✓ WIRED | Sixteen real prerendered targets exist on disk                                                 |
| `/browse/` state        | address bar                       | `replaceState(resolve(...))` on a 500 ms timer    | ✓ WIRED | Flushed by `beforeNavigate`; never read back except on popstate                                 |
| popstate                | the three runes                   | `afterNavigate` + `window.location.search`        | ✓ WIRED | Proven by `browse.e2e.ts:827`                                                                  |
| `/browse/`              | `hangar:browse-return`            | `writeBrowseReturn` in `beforeNavigate`           | ✓ WIRED | Written from live state, not from the address bar                                              |
| `BrowseLink`            | the recorded browse view          | `goto(resolve(...), { noScroll: true })`          | ✓ WIRED | `replaceState` deliberately refused; the Kit 2.70.3 reason is in the file                        |
| `/c/[id]/`              | record end-of-life                | `beforeNavigate` + `clearBrowseReturn`            | ✓ WIRED | Two exceptions (`/c/`, `/browse/`); `willUnload` skipped                                       |
| `tune/model.destroy()`  | the row's engine                  | `engine !== published` guard                      | ✓ WIRED | D-18 closed; proven in a browser                                                               |
| `entries()` / `gen-og`  | `ROUTED`                          | one declaration, four readers                     | ✓ WIRED | Sixteen pages and sixteen images cannot disagree                                               |

### Data-Flow Trace (Level 4)

| Artifact              | Data Variable                | Source                                        | Produces Real Data | Status     |
| --------------------- | ---------------------------- | --------------------------------------------- | ------------------ | ---------- |
| `/browse/`            | `shown`                      | `LISTING` (sixteen literals) → filter → sort   | Yes — sixteen cards in the prerendered HTML | ✓ FLOWING |
| `BrowseGrid`          | `engines` Map                | `createEngine(byId(id))` after a lazy import   | Yes — 4 engines built, 148 frames in 2 s     | ✓ FLOWING |
| `CatalogCard`         | `entry.description`, `.tags` | `ListingEntry`, gated against CATALOG          | Yes — real sentences and tags in the HTML    | ✓ FLOWING |
| `BrowseToolbar`       | `standing`, `blocked`        | `chipTags` / `disabledTags` over `LISTING`     | Yes — nine chips rendered, derived not declared | ✓ FLOWING |
| `BrowseLink`          | `recorded`                   | `sessionStorage` via `readBrowseReturn`        | Yes — round trip restored scrollY 573 → 573  | ✓ FLOWING |
| `/c/[id]/`            | `listed`, `row`              | `LISTING[data.index]` from `load()`            | Yes — sixteen pages with distinct heads       | ✓ FLOWING |

No hollow props found: no `<Component prop={[]}>`, no `={{}}` and no `={null}` at any browse call site.

### Behavioral Spot-Checks

| Behavior                                          | Command                                              | Result                                                                                | Status |
| ------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------- | ------ |
| The server unit project is whole                  | `npm run test:quick`                                 | 66 files, 691 passed \| 1 todo (692); 22.31 s                                          | ✓ PASS |
| The sweep project is whole                        | `npm run test:sweep`                                 | 3 files, 13 passed; 78.52 s; **0 over budget** across 32,852 costed states + 1,296 combos | ✓ PASS |
| Types are clean                                   | `npm run check`                                      | 517 files, **0 errors, 0 warnings, 0 files with problems**                              | ✓ PASS |
| Format and lint are clean                         | `npm run lint`                                       | prettier `All matched files use Prettier code style!`, eslint silent, exit 0             | ✓ PASS |
| The build emits the routed set                    | `npm run build` then count                           | **16** `build/c/*/index.html`, **16** `build/og/*.png`, `build/browse/index.html` (31,699 B) | ✓ PASS |
| The browser suite is whole                        | `npx playwright test`                                | **61 passed** (44.7 s) — chromium 53 + webkit-phone 8                                    | ✓ PASS |
| Vendored tree is byte-untouched                   | `git diff --stat 16e5232..HEAD -- src/vendor/`       | empty                                                                                   | ✓ PASS |
| Phase-4 and Phase-8 guards still armed            | `vitest run vendored-diff config-shape identity front-door` | 4 files, 42 passed                                                                | ✓ PASS |
| No locale-dependent ordering in code              | repo-wide grep for `localeCompare` / `Intl.`         | 2 hits, both inside comments                                                            | ✓ PASS |
| The harness left nothing behind                   | `netstat -ano \| grep 4173 \| grep LISTENING`        | no match; `tasklist` shows no `workerd`/`wrangler`                                       | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan  | Description                                                                       | Status      | Evidence                                                                                                                                                                                                                                                              |
| ----------- | ------------ | --------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CAT-02      | 05.1-02/07/10 | Sort by Featured, Newest and Name; no popularity metrics shown or faked            | ✓ SATISFIED | Truths 6 and 7. **The qualifier is honest**: it is about provenance, not behaviour. UI-SPEC W-08 (line 1121) does name the shipped `nameAsc` code-point comparator and agrees with the amendment; what is superseded is `05.1-RESEARCH.md`'s two `Intl.Collator` rows, and the requirement row says so. |
| CAT-03      | 05.1-04/05/09/11 | Focus/detail view with full frame rate, interaction, knobs, budget meters and install controls | ✓ SATISFIED | Truths 10, 11, 12. **The qualifier is honest and load-bearing**: install is Phase 7's and is absent or genuinely disabled. `KeepOnDevice.svelte` ships a real `disabled` attribute, not `aria-disabled`. The ROADMAP criterion itself says "docked by Phases 5 and 7", so the install half being unbuilt is phase design, not a shortfall. |

No orphaned requirements: `REQUIREMENTS.md` maps only CAT-02 and CAT-03 to Phase 5.1, and both are claimed by plans.

**CAT-01's row is stale, and that is recorded rather than hidden.** `deferred-items.md` item 7 states plainly that the row still says "eight prerendered pages" when there are now sixteen, and explains why correcting a closed Phase 4 requirement's evidence was left as a deliberate act. The requirement is over-met, not under-met.

### Anti-Patterns Found

| File                        | Line | Pattern                    | Severity | Impact                                                                                                       |
| --------------------------- | ---- | -------------------------- | -------- | ------------------------------------------------------------------------------------------------------------ |
| `BrowseToolbar.svelte`      | 20   | the word `PLACEHOLDER`      | ℹ️ Info  | It is a comment explaining that the field has a real `<label for>` and deliberately has NO placeholder. Not a stub. |

No `TODO`, `FIXME`, `XXX`, `HACK`, "coming soon" or "not yet implemented" anywhere in the phase's twenty new and modified source files. No console-log-only handler. No empty implementation. The two `return undefined` paths (`nextIndex` for an unowned key, `readBrowseReturn` for a malformed record) are both specified behaviour with their reasons written beside them.

### Deliberate Deviations, Each With Its Gate

None of these is a gap. Each is a documented departure from a written decision, and each has a mechanical guard standing where the decision used to.

1. **D-01 says "the coverflow's data file is the only source"; the code restates it in `listing.ts`.** D-12 amends this on a measured ground (`entries/ported.ts:13` drags a 131,101-byte chunk onto a page whose job is to list names). The single-source property is preserved by `listing.spec.ts` test 1 asserting equality **in both directions**, not by an import. Verified green.
2. **D-16 says "seeded once inside `onMount`"; the code seeds at component init.** Recorded in `05.1-08-SUMMARY.md`; both of D-16's stated reasons (prerender safety, shallow-routing staleness) are satisfied by a `browser`-guarded init read, and an `onMount` seed would repaint the grid a frame after Kit restores scroll.
3. **UI-SPEC W-20 asks for `replaceState` on the return; `BrowseLink` ships `{ noScroll: true }` alone.** The Kit 2.70.3 mechanism is quoted line-by-line in the file, the failure was observed on a served build, and `browse.e2e.ts` test 10 asserts the shipped behaviour. Deferred item 2 records it.
4. **UI-SPEC W-19 asks for `MORE TAGS`; it is not built.** D-15 is binding and says the nine; `BrowseToolbar.svelte`:45–47 records the refusal beside the code, and deferred item 4 records it with the cost of adding it later.
5. **D-06's `/browse/` laziness guard is tested in its literal form, not a looser one.** `browse.e2e.ts:707` loads `/browse/?q=aurora` rather than a bare `/browse/`, and the comment states the reason without softening it: at the default Featured sort the first screenful holds Lua cards, so a bare cold `/browse/` **does** fetch the VM, correctly and immediately. D-06's own wording is "with no Lua card in view", so the test matches the decision. Flagged for the user above because it is a product question, not a defect. The protocol-chunk half of the claim **does** hold for a bare cold `/browse/`: `config-shape.spec.ts` test 14 walks `build/browse/index.html`'s transitive static import graph and finds no carrier.
6. **Four shipped gates were edited under D-13, and every edit is named.** `e2e/first-experience.e2e.ts`, `src/lib/og/build.spec.ts`, and `config-shape.spec.ts` tests 13 and 14. Confirmed by diff: across `bfe47ec..HEAD` the only pre-existing guard files touched are exactly those three (`config-shape.spec.ts` carries both test edits). `identity.spec.ts`, `front-door.spec.ts` and `front-door.ts` are **untouched**, and all pass.

### Guard Regression Check

| Guard                              | Expected            | Observed                                                       |
| ---------------------------------- | ------------------- | -------------------------------------------------------------- |
| `front-door.spec.ts`               | untouched, green    | Not in the phase diff; 4-file run green                         |
| `identity.spec.ts`                 | untouched, no new colour | Not in the phase diff; green. `TagChip` reuses Phase 5's declared `rgb(214 255 78 / 0.08)`; the ninth token appears nowhere on browse |
| `vendored-diff.spec.ts`            | green               | Green; `src/vendor/` diff empty                                 |
| Phase 8 laziness (`catalog.e2e.ts`) | cold `/` fetches no WASM | Both tests passed inside the 61                             |
| `config-shape.spec.ts` 13/14       | widened deliberately | Both amendments documented in-file and in `05.1-05`/`05.1-08` SUMMARYs; green |
| `package.json` / `package-lock.json` | unchanged         | **Zero diff** across the phase — no new dependency, no paid dependency |

### Hygiene

- **No Claude/Anthropic attribution.** A scan of every tracked file outside `.planning/` returns exactly one hit: `.gitignore` line 1, a comment naming ignored session artifacts, introduced by `bafdc4d docs: initialize project` long before this phase. Commit messages across `bfe47ec..HEAD` contain no `Co-Authored-By` and no such name. The sole author is `Botond Sandor <botond.sandor@intech.studio>`.
- **No sibling repository touched.** Every file in the phase diff is inside this repository.
- **No paid dependency.** `dependencies` remains `@fontsource/quicksand`, `@intechstudio/grid-protocol` (exact pin `1.20260825.1135`), `wasmoon`.
- **`docs/TESTING.md` is honest.** Its re-measured table (66 files / 691+1, 3/13, 517 files 0 errors, 61 e2e) matches what this verification independently observed, including the sweep's 75.8 s-of-78 s dominant file.
- `AGENTS.md` (tracked, generated) and `static/og/` (gitignored, line 23) excluded from review as instructed.

### Deferred Items — Recorded, Not Hidden

All eight items in `deferred-items.md` are real, dated, owned and honest. Item 1 is marked RESOLVED with the wrong guess left visible beside the correct cause. Items 2, 3 and 5 admit that a UI spec sentence and a research sentence are now wrong and argue — reasonably — that a signed-off record should not be rewritten after the fact when the corrections live in the code and the tests. Item 6 lists seven open curation questions as the user's, not as defects. Item 7 flags the stale CAT-01 row. Item 8 re-records the `STATE.md` velocity-block staleness. Nothing found during this verification is missing from that file.

### Human Verification Required

Three items, in the frontmatter: the feel of the wall and the return, the screen-reader experience, and the product question about the Lua VM arriving on a bare cold `/browse/`. None blocks the phase.

### Gaps Summary

None. All twelve derived truths verified, every artifact passes existence, substance, wiring and data-flow, every key link is live, and every automated suite is green at the counts the phase claims: 66/691+1 unit, 3/13 sweep, 61 e2e, 0 type errors, clean lint, and a build that emits sixteen configuration pages, sixteen link images and the browse page.

The phase's most valuable property is not that it passes — it is that where the implementation departs from a written decision it says so **in the file, in a SUMMARY and in `deferred-items.md`**, and leaves a mechanical guard behind. The `config-shape` test 14 correction is the clearest case: the plan's expected negative check came back GREEN, the summary records that the plan was wrong, and the test was rewritten to walk the transitive graph. A phase that reported its own guard as blind is a phase whose green can be trusted.

---

_Verified: 2026-09-05T01:26:30Z_
_Verifier: gsd-verifier_
