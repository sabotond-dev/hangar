---
phase: 05.1-catalog-browse
plan: 06
subsystem: ui
tags:
  [browse, cards, simulator-host, lazy-imports, roving-tabindex, accessibility]

# Dependency graph
requires:
  - phase: 05.1-catalog-browse
    provides: "05.1-01's src/lib/catalog/listing.ts — LISTING, ListingEntry, RESTS_DARK_NOTE, and the import-free module that lets a sixteen-card page paint without the compiler"
  - phase: 05.1-catalog-browse
    provides: "05.1-02's src/lib/browse/grid.ts (columnsForWidth, columnsFromTemplate, nextIndex) and src/lib/browse/typographic.ts"
  - phase: 05.1-catalog-browse
    provides: "05.1-03's SimHost.repaintAll — the call this plan is the first and only consumer of"
  - phase: 05.1-catalog-browse
    provides: "05.1-04's structurally-typed PadFrame, PadCanvas and NamePlate, and the arrow-less plate ruling"
  - phase: 04-first-experience
    provides: "src/lib/sim/host.ts (SimHost), PadFrame/PadCanvas's four-layer pad recipe, PadSpinner's walking cell, and Coverflow.svelte's dynamic-import + skip-on-throw shape"
  - phase: 08-lua-catalog
    provides: "src/lib/sim/engine.ts's createEngine and its lazy Lua branch — the 271 KB VM this plan keeps off a cold load"
provides:
  - "src/lib/ui/CatalogCard.svelte — the pad as the card: the reserved FEATURED band, the arrow-less plate as the one link with its whole-card overlay, the typographic() description, the resting-dark note, quiet tag chips, and the pending and unavailable pad states"
  - "src/lib/ui/BrowseGrid.svelte — one SimHost for the page, per-card engines built on first intersection through a dynamic import, repaintAll after a reorder, the roving tabindex, and both empty states"
  - "An observed laziness seam on a served page: a cold wall registers eight ported engines and fetches zero .wasm; scrolling the Lua row into view fetches exactly one glue.wasm"
  - "A recorded open item for 05.1-08: the `import { createEngine } from \"$lib/sim/engine\"` mutation is invisible to every guard in the repository today, OBSERVED green"
affects: [05.1-07, 05.1-08, 05.1-10, 05.1-11]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two IntersectionObservers answering two different questions: the grid's own decides when to BUILD an engine, SimHost's decides when to TICK it. Same { threshold: 0, rootMargin: 200px } geometry, no new host API"
    - "A whole-card overlay link: one <a> with ::after { inset: 0 } over a position: relative card, so the card is clickable, the accessibility tree holds one link and the tab order gains one stop"
    - "A roving tabindex over real anchors — not role=listbox, not role=grid — with every arrow decision delegated to a node-tested pure function and preventDefault called only when that function returned a number"
    - "A focus ring RELOCATED rather than removed: outline: none on the link, the ring drawn on the card through :has(a:focus-visible) at the 10px radius so it matches the clickable area"

key-files:
  created:
    - src/lib/ui/CatalogCard.svelte
    - src/lib/ui/BrowseGrid.svelte
  modified: []

key-decisions:
  - "CatalogCard authors the plate as an ANCHOR instead of rendering NamePlate.svelte. The plan's sketch showed <NamePlate arrows={false} />, but NamePlate's name is a <button> carrying `Choose {name}` and a `disabled` state — a card navigates rather than choosing, and a button inside the card would be a second tab stop beside the overlay link. NamePlate is not in this plan's files_modified and four other call sites depend on it, so the card restates the plate's four declarations (44px, 6px radius, --color-line, Heading 20px/600) rather than widening a shipped component"
  - "CatalogCard takes two props the plan's interfaces block did not list — `pending` and `unavailable`. Both states are decided by the grid, which owns the engines, and the card cannot derive either; without them the two off-nominal states in 05.1-UI-SPEC Screen 2c could not render at all"
  - "BrowseGrid takes two props the plan's interfaces block did not list — `total` and `emptyReason`. The plan's own empty-state bullet requires both (the by-condition sentence is 'supplied by the page' and the next-step line names {total}), and `total` is what tells a filter miss from an empty catalog"
  - "MEASURED, and it contradicts a sentence in the UI spec: with the grid's approved `align-items: start`, the card's approved `margin-block-start: auto` on the tag row is INERT. Cards in a row end where their content ends (row 1 at 1280px: 524, 500, 500, 524). The alignment the plan asks to check — 'the sixteen pads line up across a row' — comes entirely from the reserved FEATURED band, and it holds exactly (padTop 56, 572, 1089, 1661, identical within every row). Neither declaration was changed: both are quoted verbatim from the approved spec, and the auto margin is already correct if a later plan ever stretches the tracks"
  - "`list` is the one element reference declared with $state, because the bind:this sits inside an {#if} and Svelte warns non_reactive_update on a plain binding there — which Coverflow.svelte's unconditional `stage` never trips. $state does not proxy a DOM element (only plain objects and arrays are proxied) and the element is never read on a tick, so the standing rule about engines, canvases and frame buffers is untouched. focusCard was also changed to take the list from the keydown event's currentTarget rather than the binding, so nothing a template handler can reach reads it"

patterns-established:
  - "A component whose structural claims are not yet gated writes the guard that WOULD catch a violation into the SUMMARY as an open item, with the mutation, the guard, and the plan that lands it — after observing that the mutation is green today rather than assuming it"

requirements-completed: []
requirements-contributed: [CAT-02, CAT-03]

# Metrics
duration: 61min
completed: 2026-09-05
---

# Phase 5.1 Plan 06: The wall — sixteen live pads, one clock, one tab stop Summary

**Two components and no new machinery: `CatalogCard.svelte` renders one configuration as its running pad, its name as the card's only link, its sentence through `typographic()`, its feel tags as quiet chips and an honest line when it rests dark; `BrowseGrid.svelte` hangs sixteen of them off ONE `SimHost`, builds each card's engine on its own first intersection through `await import("$lib/sim/engine")`, repaints after a reorder, and hands a keyboard one tab stop with arrows that clamp. Every claim in this plan was observed on a served page rather than argued: a cold wall registers eight ported engines and fetches ZERO WebAssembly, scrolling the Lua row into view fetches exactly ONE `glue.wasm`, seven Lua cards hold the walking cell when that chunk is delayed and read `{NAME} — unavailable` when it is aborted while the other nine go on running, and crossing the grid costs two Tab presses against seventeen without the roving index. Whole-suite delta: +0 files, +0 tests, +0 e2e.**

## The four-name carry-forward block

Read by 05.1-07, and by every plan after it. **Copy all four names forward verbatim**, whether or not
the plan moves them.

| Name         | Value                        | Measured                                                                                    |
| ------------ | ---------------------------- | ------------------------------------------------------------------------------------------- |
| `BASE_FILES` | **65**                       | `npm run test:quick` after this plan (65 before + 0 files — this plan adds no spec)          |
| `BASE_TESTS` | **685** (+ 1 todo = 686)     | same run (685 before + 0 tests — the behavioural proof lands in 05.1-08 and 05.1-10)         |
| `BASE_SWEEP` | **`3 13`**                   | a literal, never re-derived — a property of this phase's files                                |
| `PREV_E2E`   | **44 (measured by 05.1-05)** | carried verbatim: this plan runs no Playwright suite and changes no `e2e/` file               |

**The baseline was re-measured before a byte was touched:** `npm run test:quick` on a clean tree
printed `Test Files 65 passed (65)` and `Tests 685 passed | 1 todo (686)`, matching
`05.1-05-SUMMARY.md` exactly.

`BASE_CHECK` = **512 files while the throwaway probe route existed, 510 after it was deleted, 0 errors,
0 warnings** — provenance only. Only `0 errors` is ever asserted; the repository's own standard of
zero warnings was held, and holding it is what produced two of this plan's decisions (see Deviations).

## The sampling gap this plan leaves open, deliberately

**Both components land here; nothing in the repository scans them yet.** `src/lib/ui/browse-ui.spec.ts`
is written in **plan 05.1-08**, and the widening of `config-shape.spec.ts` test 13 into a
compile-surface rule lands in the same plan. So for two waves the structural claims below are held by
the hand-run comment-stripped scans quoted in this document and by nothing else; **wave 8 is where they
become gates, and wave 10 is where a real browser confirms them against the deployed bytes.** That gap
is deliberate — writing the spec before the components existed would have meant writing it against
imagined markup — and it is why every scan here is quoted rather than merely run.

**The one mutation that is deferred, with its reason.** Adding
`import { createEngine } from "$lib/sim/engine";` to `BrowseGrid.svelte` is **invisible to every guard
in the repository today**, and that was OBSERVED rather than assumed: with the static import in place,
`npx vitest run --project server src/lib/config-shape.spec.ts` printed `Tests 14 passed (14)`.

- **Test 13 cannot see it.** `COMPILER_MARKERS` is `["vendor", "intechstudio", "lib/pad"]` and it
  matches specifier TEXT; `$lib/sim/engine` contains none of the three, even though the module reaches
  `src/vendor/botor/_pad` transitively.
- **Test 14 cannot see it.** It walks the static import graph of built pages, and no route in `build/`
  imports `BrowseGrid.svelte` until `/browse/` exists in wave 8.

**Plan 05.1-08 widens test 13 into a compile-surface rule and carries exactly this mutation as a row in
its mutation table, where it is observed red.** Until then the discipline is the comment-stripped
named-versus-awaited scan quoted below.

## Performance

- **Duration:** 61 min
- **Started:** 2026-09-04T23:30:00Z
- **Completed:** 2026-09-05T00:31:00Z
- **Tasks:** 3
- **Files created:** 2 (320 + 491 lines)

## Task 5.1-06-01 — `CatalogCard.svelte`

**Commit `8ccc629`.** 320 lines.

### Acceptance, line by line

| Criterion                                                                        | Observed                                                                                                     |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `npm run check` `0 errors`                                                       | `COMPLETED 509 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`                                               |
| `npm run lint`                                                                   | exit 0                                                                                                        |
| `identity.spec.ts` / `tune-ui.spec.ts` / `config-shape.spec.ts`                  | `6 passed`, `5 passed`, `14 passed`                                                                           |
| `git diff --quiet HEAD -- src/app.css src/lib/ui/identity.spec.ts`               | exit 0 — no tenth token, no fourth hue, the guard untouched                                                   |

### The comment-stripped scans

Run through the house helper — `stripComments` copied verbatim from `src/lib/config-shape.spec.ts`
(line, block and markup, backslash-free) — because all three of the forbidden strings appear in this
file's own comments, correctly, and **a raw `grep` would have gone red on all three**:

```
src/lib/ui/CatalogCard.svelte | "<a": 1 outside comments, 1 in raw source
  code 58: <a
src/lib/ui/CatalogCard.svelte | "--color-over": 0 outside comments, 1 in raw source
src/lib/ui/CatalogCard.svelte | "crosshair": 0 outside comments, 1 in raw source
src/lib/ui/CatalogCard.svelte | "text-overflow": 0 outside comments, 1 in raw source
src/lib/ui/CatalogCard.svelte | "role="option"": 0 outside comments, 1 in raw source
src/lib/ui/CatalogCard.svelte | "role="gridcell"": 0 outside comments, 1 in raw source
```

The plan predicted that `crosshair` would be the one of the three mentioned in a comment. **All three
are** — the header explains why the cursor is `pointer` and never `crosshair`, why the description is
never clamped with `text-overflow`, and why `--color-over` has no place on a browse screen. So the
comment-stripped form is not a nicety on this file; it is the only form of the check that is correct.

The two required render sites, quoted:

```
src/lib/ui/CatalogCard.svelte | "typographic(": 1 outside comments, 1 in raw source
  code 71: {typographic(entry.description)}
src/lib/ui/CatalogCard.svelte | "entry.quiet": 2 outside comments, 2 in raw source
  code 75: {#if entry.restsBlack && entry.quiet !== undefined}
  code 76: <p class="quiet">{entry.quiet}</p>
```

The resting-dark sentence is **never typed into the component** — one string, one source
(`RESTS_DARK_NOTE` in `listing.ts`), and the gate is `restsBlack` rather than the presence of `quiet`,
because four entries carry their own non-dark `quiet` line.

Radar's card on screen, read back out of the DOM:

```
radar description on screen: Rings roll out from the centre, and the pad sends your finger’s position to your computer.
radar apostrophes rendered: ["’"] (U+2019 = 2019)
```

### The three on-screen checks

Measured in Chromium at 1280x720 against a throwaway `/dev/browse-probe/` route mounting all sixteen
cards (created for the browser checks, never committed, deleted before the final run).

**1. The pads line up across a row, featured or not.** `padTop` is identical within every row and the
pad is exactly the card width:

```
card-aurora    featured=true  cardTop=32   padTop=56   pad=286x286  plateTop=358  cardBottom=524
card-pinwheel  featured=true  cardTop=32   padTop=56   pad=286x286  plateTop=358  cardBottom=500
card-starfield featured=false cardTop=32   padTop=56   pad=286x286  plateTop=358  cardBottom=500
card-radar     featured=false cardTop=32   padTop=56   pad=286x286  plateTop=358  cardBottom=524
card-joystick  featured=false cardTop=548  padTop=572  pad=286x286  plateTop=874  cardBottom=1041
card-tpad      featured=false cardTop=1065 padTop=1089 pad=286x286  plateTop=1391 cardBottom=1613
card-ghost     featured=false cardTop=1637 padTop=1661 pad=286x286  plateTop=1963 cardBottom=2216
```

286px is the spec's own predicted four-column card width at 1280, and the reserved 16px band is what
holds `padTop` equal across `featured=true` and `featured=false` in the same row.

**What was ALSO observed, and it corrects a sentence in the UI spec:** `cardBottom` is **not** equal
within a row (524 / 500 / 500 / 524). Screen 2c says the tag row's `margin-block-start: auto` makes
"every card in a row end at the same line", but Screen 2b's grid declares `align-items: start`, which
does not stretch a track's items — so the auto margin has nothing to push against and is inert. Both
declarations were shipped exactly as approved; the alignment the plan asks to verify is the pads', and
that one holds. Recorded here so 05.1-08 can decide deliberately if it ever wants equal-height cards.

**2. A long description wraps and is never cut.** The four longest, with the computed properties that
would clip them:

```
card-description-euclid chars=109 lines=3 h=72 scrollH=72 clientH=72 text-overflow=clip line-clamp=none overflow=visible
card-description-arc    chars=106 lines=3 h=72 scrollH=72 clientH=72 text-overflow=clip line-clamp=none overflow=visible
card-description-ghost  chars=103 lines=3 h=72 scrollH=72 clientH=72 text-overflow=clip line-clamp=none overflow=visible
card-description-tpad   chars=99  lines=3 h=72 scrollH=72 clientH=72 text-overflow=clip line-clamp=none overflow=visible
```

`scrollHeight === clientHeight` on every one of the sixteen: nothing is clipped, and `text-overflow`
reports its initial value because the property is never declared.

**3. A card's chips are visibly not the toolbar's.** Two channels, both measured:

```
chip heights: [24.4]   border colour: ["rgba(214, 255, 78, 0.2)"]   (--color-line-soft)
plate heights: [44]
```

24.4px is the spec's own arithmetic (4 + 14.4 + 4 + 2), against the 44px interactive floor.

### The accessibility negative check, observed and restored

The pad wrapper's `aria-hidden="true"` was deleted from the source, the page reloaded, and the
accessibility tree read with Playwright's `ariaSnapshot()`:

**With `aria-hidden` (shipped):**

```
- listitem:
  - paragraph: FEATURED
  - link "Aurora":
    - /url: /c/aurora
  - paragraph: A band of light crosses the pad, and your finger leaves a glowing tail behind it.
  - list:
    - listitem: ambient
    - listitem: flowing
    - listitem: colour

"Aurora" appears 1 time(s) in the card's accessibility tree
links on the page: 19; images on the page: 0
```

**Without it (the mutation):**

```
  - paragraph: FEATURED
  - img "Aurora, live pad simulation"
  - link "Aurora":

"Aurora" appears 2 time(s) in the card's accessibility tree
links on the page: 19; images on the page: 16
```

Two names per card and sixteen images entering the tree — exactly W-17's prediction. **Restored**, and
the restored state re-observed at one name and zero images. The nineteen links are the sixteen cards
plus the Phase 1 footer's three.

## Task 5.1-06-02 — `BrowseGrid.svelte`

**Commit `cbb1a75`.** The list, the host, the two observers, the empty states.

### Acceptance, line by line

| Criterion                                                                | Observed                                            |
| ------------------------------------------------------------------------ | ---------------------------------------------------- |
| `npm run check` `0 errors`; `npm run lint` exit 0                        | `0 ERRORS 0 WARNINGS`; exit 0                       |
| `config-shape.spec.ts`, `sim/lazy.spec.ts`, `identity.spec.ts`           | `Test Files 3 passed (3)`, `Tests 23 passed (23)`   |

### The comment-stripped scans

```
src/lib/ui/BrowseGrid.svelte | "new SimHost": 1 outside comments, 1 in raw source
  code 99: host = new SimHost();
src/lib/ui/BrowseGrid.svelte | "requestAnimationFrame": 0 outside comments, 1 in raw source
src/lib/ui/BrowseGrid.svelte | "setHero": 1 outside comments, 2 in raw source
  code 102: host.setHero(undefined);
src/lib/ui/BrowseGrid.svelte | "onpointer": 0 outside comments, 2 in raw source
src/lib/ui/BrowseGrid.svelte | "pointerdown": 0 outside comments, 2 in raw source
src/lib/ui/BrowseGrid.svelte | "repaintAll": 1 outside comments, 1 in raw source
  code 155: host?.repaintAll();
```

One host, one call to `setHero` and it is called with `undefined`, **zero** `requestAnimationFrame`
owners in this file — the host owns the page's only loop, and the post-update repaint is sequenced
with Svelte's `tick()`, which is a microtask. The header names the three pointer handlers it does not
install (`onpointerdown`, `onpointermove`, `onpointerup`), which is why the raw source shows two hits
for words the code never contains.

`repaintAll`'s call site with its comment:

```svelte
      await tick();
      if (!mounted) return;
      // The column count is re-read after a reorder as well as after a resize:
      // a filter that drops the set below one row changes what auto-fill did.
      measureColumns();
      if (first) return;
      // A still pad does not tick, so nothing else would repaint it after its
      // <li> moved, and whether a canvas bitmap survives a re-parenting move is
      // not a question this repository wants to depend on (Pitfall 10).
      host?.repaintAll();
      onreorder?.();
```

### The named-versus-awaited scan

The same count `src/lib/ui/tune-ui.spec.ts` test 1 already runs for `$lib/tune/model`, applied by hand
and comment-stripped:

```
    line 74: import("$lib/sim/engine"),   [inside import(]
src/lib/ui/BrowseGrid.svelte | "$lib/sim/engine": 1 occurrence(s) outside comments, 0 on a static from form, 1 inside import(
    line 75: import("$lib/catalog"),   [inside import(]
src/lib/ui/BrowseGrid.svelte | "$lib/catalog": 1 occurrence(s) outside comments, 0 on a static from form, 1 inside import(
    line 4: import type { ListingEntry } from "$lib/catalog/listing";   [static from]
src/lib/ui/BrowseGrid.svelte | "$lib/catalog/listing": 1 occurrence(s) outside comments, 1 on a static from form, 1 inside import( -> 0
```

Every occurrence of the two lazy specifiers outside a comment sits inside `import(`, at the one call
site:

```svelte
      const [{ createEngine }, { byId }] = await Promise.all([
        import("$lib/sim/engine"),
        import("$lib/catalog"),
      ]);
```

The only static catalog specifier is the permitted `$lib/catalog/listing`, and it is an `import type`.

### The measurement, recorded and not gated

Chromium, **viewport 1280x720**, all sixteen cards rendered, the counter `e2e/first-experience.e2e.ts`
already uses (`CanvasRenderingContext2D.prototype.putImageData`, which `src/lib/sim/paint.ts` calls
exactly once per pad per paint):

| Position          | Cards visible | Engines registered | Paints over two seconds |
| ----------------- | ------------- | ------------------ | ----------------------- |
| Top of the wall   | **8**         | 8                  | **195**                 |
| Bottom of the wall| **4**         | 12                 | **114**                 |

Read honestly: `intervalFor(hero=false, lowPower=false)` is 50 ms, so a fully-animating wall of n cards
approaches **40n** paints in two seconds. At the top, eight cards are visible and **five** of them
animate (aurora, pinwheel, starfield, radar, dial; joystick, ninepads and faders are still instrument
layouts) — 5 x 40 = 200 against 195 observed, the difference being that the still three each paint once
on register and then cost nothing. **The self-cancelling loop is doing exactly what it was written to
do.** No number here gates anything and nothing was tuned; `SIDE_INTERVAL_MS` was not touched.

### The laziness seam, observed on a served page

```
cold load    | engines registered: aurora, pinwheel, starfield, radar, joystick, ninepads, faders, dial
cold load    | .wasm responses: []
after scroll | engines registered: ... + ghost, lattice, morph, sonar
after scroll | .wasm responses: ["glue.wasm"]
```

A visitor who never brings a Lua card into view **never downloads a Lua VM**, and a page filtered to
ported entries never touches the Lua branch at all. Scrolling in stages confirms the build decision is
per card and that nothing is ever unregistered:

```
cold:          aurora pinwheel starfield radar joystick ninepads faders dial
scrollY=600:   + tpad euclid chorus arc
scrollY=1200:  + ghost lattice morph sonar
scrollY=1800:  all sixteen, still 9x9
scrollY=2400:  all sixteen, still 9x9
```

### The two off-nominal pad states, observed

Forced with a Playwright route handler over the Lua chunk and `glue.wasm`:

**Delayed six seconds** — the walking cell, on exactly the seven Lua cards and none of the nine ported
ones:

```
spinners on: ["card-euclid","card-chorus","card-arc","card-ghost","card-lattice","card-morph","card-sonar"]
unavailable plates: 0 | links still links: 16
```

**Aborted** — one broken chunk, seven quiet cards, and a wall that does not blank:

```
canvases in the DOM: 9   (the seven Lua cards' canvases are gone; frame and dot field remain)
plate: ["card-name-euclid","EUCLID — unavailable","rgba(214, 255, 78, 0.5)","/c/euclid"]
plate: ["card-name-ghost","GHOST — unavailable","rgba(214, 255, 78, 0.5)","/c/ghost"]
...
unavailable plates: 7 | links still links: 16
```

`rgba(214, 255, 78, 0.5)` is `--color-ink-dim`, the em dash is U+2014, and **every one of the sixteen
plates still has an `href`** — a broken card is never a dead end.

### Negative check 1 — red today, and it proves the walk

`import { PadSim } from "../../vendor/botor/pad-sim";` added to `BrowseGrid.svelte`:

```
FAIL  |server| src/lib/config-shape.spec.ts > build configuration shape > the front door never reaches the compiler at module scope
AssertionError: a front-door file imports the compiler at module scope: expected [ Array(1) ] to deeply equal []
+   "src/lib/ui/BrowseGrid.svelte -> ../../vendor/botor/pad-sim",
      Tests  1 failed | 13 passed (14)
```

The guard names the new file and the specifier. **Restored**, and `14 passed` re-observed.

### Negative check 2 — not red until wave 8, and observed to be so

Covered in "The sampling gap" above: the `$lib/sim/engine` static import leaves `14 passed`. Recorded
as an open item for 05.1-08 rather than faked here.

## Task 5.1-06-03 — the keyboard

**Commit `a29ad81`.**

### The comment-stripped role scan, over BOTH components

```
CatalogCard.svelte | role="listbox": 0 code / 0 raw   role="option":   0 code / 1 raw
CatalogCard.svelte | role="grid":    0 code / 0 raw   role="gridcell": 0 code / 1 raw
BrowseGrid.svelte  | role="listbox": 0 code / 1 raw   role="option":   0 code / 0 raw
BrowseGrid.svelte  | role="grid":    0 code / 1 raw   role="gridcell": 0 code / 0 raw
```

None of the four appears in either component's code. Each file discusses the two roles that are about
its own level — the card refuses `role="option"` and `role="gridcell"`, the grid refuses
`role="listbox"` and `role="grid"` — so the raw form of this check reports the correct implementation
as a violation, on both files.

```
CatalogCard.svelte | "Enter":  0 outside comments, 1 in raw source
CatalogCard.svelte | "Space":  0 outside comments, 1 in raw source
BrowseGrid.svelte  | "Enter":  0 outside comments, 2 in raw source
BrowseGrid.svelte  | "Space":  0 outside comments, 2 in raw source
BrowseGrid.svelte  | "tabindex": 0 outside comments, 1 in raw source
CatalogCard.svelte | "tabindex": 1 outside comments, 2 in raw source
  code 63: tabindex={tabbable ? 0 : -1}
```

No handler names either key. Both files say in prose why there is no handler, which is the whole
reason the scan strips comments first.

### The keyboard journey, walked

Chromium at 1280x900 (four columns), with a control before the grid and a control after it. Every line
reports `document.activeElement` and the number of `tabindex="0"` elements inside the grid:

```
focus the control before the grid  -> <BUTTON> before-grid       tabindex0-in-grid=[card-name-aurora] (-1 count 15)
Tab (one press)                    -> <A> card-name-aurora       tabindex0-in-grid=[card-name-aurora] (-1 count 15)
ArrowRight                         -> <A> card-name-pinwheel     tabindex0-in-grid=[card-name-pinwheel]
ArrowRight                         -> <A> card-name-starfield    tabindex0-in-grid=[card-name-starfield]
ArrowDown (one row)                -> <A> card-name-faders       (index 2 + 4 columns = index 6)
ArrowUp (one row)                  -> <A> card-name-starfield
ArrowLeft                          -> <A> card-name-pinwheel
Home                               -> <A> card-name-aurora
ArrowLeft at the first card        -> <A> card-name-aurora       (clamped, not wrapped)
ArrowUp at the first row           -> <A> card-name-aurora       (clamped)
End                                -> <A> card-name-sonar        scrollY=1525
ArrowRight at the last card        -> <A> card-name-sonar        (clamped)
ArrowDown at the last row          -> <A> card-name-sonar        (clamped)
Home                               -> <A> card-name-aurora       scrollY=0
Space (must scroll, not move)      -> <A> card-name-aurora       scrollY 0 -> 308, focus unmoved
Alt+ArrowRight (not intercepted)   -> <A> card-name-aurora       scrollY 308 -> 701, focus unmoved
Shift+Tab back out of the grid     -> <BUTTON> before-grid
Tab back in (the roving card)      -> <A> card-name-aurora
Tab out, forwards                  -> <BUTTON> after-grid
Shift+Tab in again                 -> <A> card-name-aurora

Enter: http://[::1]:4173/dev/browse-probe/  ->  http://[::1]:4173/c/pinwheel/
```

`document.activeElement` is an `<a>` at every step inside the grid; **exactly one** element in the grid
carries `tabindex="0"` and **fifteen** carry `-1`, at every step; `Enter` has no handler and follows
the link natively, including the static host's trailing-slash redirect.

### Negative check — every card tabbable, observed and restored

```
--- correct implementation ---
cards carrying tabindex="0": 1;  Tab presses from the control before the grid to the control after it: 2
--- negative check: every card tabbable ---
cards carrying tabindex="0": 16; Tab presses from the control before the grid to the control after it: 17
```

Two presses means the whole grid costs **one** tab stop (in, then out). Seventeen means it costs
sixteen. **Restored**, and the one-stop result re-observed.

## Whole-suite verification

| Command                                                                     | Result                                                            |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `npm run check`                                                             | `COMPLETED 510 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`   |
| `npm run lint`                                                              | exit 0                                                             |
| `npx vitest run --project server src/lib/config-shape.spec.ts src/lib/ui/ src/lib/sim/` | `Test Files 11 passed (11)`, `Tests 79 passed (79)`  |
| `npm run test:quick \| node scripts/check-counts.mjs 65 685`                | `observed 65 files, 685 tests passed, 1 todo` — **matches**       |
| `npm run test:sweep \| node scripts/check-counts.mjs 3 13`                  | `observed 3 files, 13 tests passed` — **matches**                 |
| `git diff --quiet HEAD -- src/app.css src/lib/ui/identity.spec.ts`          | exit 0                                                             |

No e2e run: this plan changes no `e2e/` file and adds no route, so `PREV_E2E` is carried verbatim from
`05.1-05-SUMMARY.md`, which measured it.

## Deviations from Plan

### Auto-fixed and auto-decided

**1. [Rule 3 — Blocking] The card's plate is an anchor authored here, not `NamePlate.svelte`.**

- **Found during:** Task 1.
- **Issue:** The plan's interfaces block sketches `<NamePlate {entry} arrows={false} />` as the card's
  plate, and the accompanying prose requires "the plate's `<a href=…>`". `NamePlate.svelte`'s name is a
  `<button type="button">` with `aria-label="Choose {name}"` and a `disabled` state; it has no `href`
  and cannot have one without a prop that four other call sites would have to reason about. A button
  inside the card would also be a second tab stop beside the overlay link, which is the one thing the
  roving-tabindex model cannot tolerate.
- **Fix:** `CatalogCard` restates the plate's four declarations — 44px min-block-size, 6px radius,
  `1px solid var(--color-line)`, Heading 20px/600/0.01em with `text-wrap: balance` — on its own `<a>`,
  and says so in a comment naming `NamePlate.svelte` and the reason. `NamePlate.svelte` is untouched,
  as this plan's `files_modified` requires.
- **Files:** `src/lib/ui/CatalogCard.svelte`. **Commit:** `8ccc629`.

**2. [Rule 2 — Missing critical functionality] Two props the card cannot do without.**

- **Found during:** Task 1.
- **Issue:** The interfaces block lists four props (`entry`, `tabbable`, `onready`, `onfocus`), but
  05.1-UI-SPEC Screen 2c requires the card to render two off-nominal states — pending and unavailable —
  and both are decided by the grid, which owns the engines. The card can derive neither.
- **Fix:** `pending?: boolean` and `unavailable?: boolean`, both defaulting to `false`, each documented
  with the state it renders. Observed working in both directions (see Task 2's off-nominal section).
- **Files:** `src/lib/ui/CatalogCard.svelte`. **Commit:** `8ccc629`.

**3. [Rule 2 — Missing critical functionality] Two props the grid's empty states cannot do without.**

- **Found during:** Task 2.
- **Issue:** The interfaces block lists `entries` and `onreorder`, but the same block's empty-state
  bullet requires "the by-condition sentence supplied by the page" and the line
  `CLEAR FILTERS brings back all {total} configurations.`, and the two empty states are told apart by
  whether the CATALOG is empty rather than the filtered set.
- **Fix:** `total?: number` (defaulting to `entries.length`, so a grid handed nothing and told nothing
  reads as an empty shelf) and `emptyReason?: string`. Both copy strings are verbatim from the
  contract, including Phase 4's two.
- **Files:** `src/lib/ui/BrowseGrid.svelte`. **Commit:** `cbb1a75`.

**4. [Rule 1 — Bug] `list` is a rune, and `focusCard` takes the element from the event.**

- **Found during:** Task 3.
- **Issue:** `npm run check` reported `non_reactive_update` on `bind:this={list}` — the binding sits
  inside an `{#if}`, so Svelte assigns it from a template effect and requires a rune, which
  `Coverflow.svelte`'s unconditional `stage` never trips. The repository's standard is zero warnings.
- **Fix:** `let list: HTMLUListElement | undefined = $state(undefined)`, with a comment stating exactly
  why this does not loosen the standing rule: the rule forbids an ENGINE, a CANVAS or a FRAME BUFFER in
  a rune because of proxy traps in a 100 Hz loop, `$state` does not proxy a DOM element at all (only
  plain objects and arrays are proxied), and this element is read on mount, on a debounced resize and
  after a reorder — never on a tick. Separately, `focusCard` was changed to take the list from the
  keydown event's `currentTarget`, so nothing a template handler can reach reads the binding.
- **Files:** `src/lib/ui/BrowseGrid.svelte`. **Commit:** `a29ad81`.

**5. [Rule 3 — Blocking] The delegated keydown needs a suppression.**

- **Found during:** Task 3.
- **Issue:** `a11y_no_noninteractive_element_interactions` fires on a `keydown` attached to a
  `<ul role="list">`.
- **Fix:** `<!-- svelte-ignore a11y_no_noninteractive_element_interactions -->` with its reason in a
  **separate** comment above it (the house rule: words after the rule name inside a `svelte-ignore`
  are parsed as further rule names). The reason is that the listener is delegated — the thing that
  takes focus and receives the key is a real `<a>` — and that obeying the rule would mean either the
  interactive role this component exists to refuse or sixteen copies of one handler.
- **Files:** `src/lib/ui/BrowseGrid.svelte`. **Commit:** `a29ad81`.

### Observed, changed nothing, recorded

**6. `align-items: start` makes the card's `margin-block-start: auto` inert.** Both declarations are
quoted verbatim from the approved spec and both shipped as written; the spec's claim in Screen 2c that
"every card in a row ends at the same line" does not follow from Screen 2b's grid, and the measurement
is in Task 1 above. The alignment the plan asks to check is the pads', which holds exactly. Nothing was
changed, because changing either declaration would be a design decision rather than a bug fix.

### Not deviations, but worth writing down

- A throwaway route, `src/routes/dev/browse-probe/`, existed for the browser checks and was **deleted
  before the final verification run**; it is in no commit. It is the same shape as
  `src/routes/dev/catalog/+page.svelte`, and 05.1-08 lands the real `/browse/`.
- `src/vendor/` untouched, `src/app.css` untouched, `identity.spec.ts` untouched, no dependency added,
  no new SVG, no new token, no new hex, `SIDE_INTERVAL_MS` untouched.
- `.planning/phases/06-device-session/` was being edited by the read-only plan checker throughout;
  nothing in this plan staged or touched it.

## Known Stubs

None. Both components are fully wired to real data and a real simulator; the two states that look like
stubs — the walking cell and the unavailable plate — are the contract's own off-nominal states and were
observed rendering from real failures.

## What 05.1-07 needs to know

- **`BrowseGrid`'s props are `entries`, `total`, `emptyReason`, `onreorder`.** The page sorts and
  filters; the grid never does. `emptyReason` is one of the copy contract's three by-condition
  sentences and the page picks it.
- **The grid is the second tab stop group, not the first.** The toolbar's controls come before it in
  the DOM and the roving index makes the whole wall one stop after them.
- **`onreorder` fires only after a real change of order**, in a post-update tick, after `repaintAll` —
  never on first render.
- **The four-name block is unchanged**: `BASE_FILES` 65, `BASE_TESTS` 685, `BASE_SWEEP` `3 13`,
  `PREV_E2E` 44 (measured by 05.1-05).

## Self-Check: PASSED

Files:

```
FOUND: src/lib/ui/CatalogCard.svelte   (320 lines)
FOUND: src/lib/ui/BrowseGrid.svelte    (491 lines)
```

Commits:

```
FOUND: 8ccc629  feat(05.1-06): CatalogCard.svelte - one configuration, as a card
FOUND: cbb1a75  feat(05.1-06): BrowseGrid.svelte - one clock, sixteen canvases, engines built on approach
FOUND: a29ad81  feat(05.1-06): the keyboard - one tab stop, arrows that clamp, Enter that is just a link
```

Contains: `CatalogCard.svelte` carries `card-{entry.id}` and `card-name-{entry.id}`;
`BrowseGrid.svelte` carries `browse-grid` and `browse-empty`. Both exceed their `min_lines`.
